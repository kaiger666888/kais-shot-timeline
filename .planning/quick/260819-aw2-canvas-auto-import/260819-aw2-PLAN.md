---
phase: quick
plan: 260819-aw2
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/canvas_import.py
  - run_pipeline.py
  - tests/test_canvas_import.py
autonomous: true
requirements:
  - QUICK-260819-AW2-01  # 导出后自动导入画布：canvas_import.py + pipeline 可选接线 + 最小 pytest
user_setup: []

must_haves:
  truths:
    - "standalone：python3 scripts/canvas_import.py --asset-dir <dir> --project-name X 在画布已有名为 X 的项目时复用其 id（全程零 addProject 调用）"
    - "项目 X 不存在且允许创建时，脚本以 mode=\"canvas-v2\" + 空串可选字段建项目（对齐现有「小江湖·逆推资产集(ep01)」行取值），intro 取 asset.json source.video_filename 的 stem"
    - "addProject 响应不含 id（已在 kap 源码 src/routes/project/addProject.ts:44 验证：只返 {message}）——脚本建完后必须回读 /api/canvas/projects 按 name 再匹配拿 id"
    - "import-from-dir 请求 body 为 {projectId:int, episodesId:int, workdir:绝对路径, mode:\"merge\"|\"replace\"}，UTF-8 编码 JSON（workdir 含 CJK+全角标点不炸）"
    - "python run_pipeline.py --canvas-auto-import 在 step_export 成功（asset.json 存在）后 subprocess 调 canvas_import.py，project-name 默认 f\"小江湖·逆推资产集({ep_label})\"（ep_label = --ep-name 或 video stem）"
    - "canvas_import 失败时 pipeline 只打 [canvas-import] warning 并继续走到 [done]（exit 0，graceful-degrade 与 route 步骤同口径）"
    - "不加 --canvas-auto-import 时 pipeline 行为与现状逐字节一致（无新子进程、无新输出）"
  artifacts:
    - path: "scripts/canvas_import.py"
      provides: "独立 CLI：按名找/建画布项目 → POST import-from-dir 导入资产目录；纯 urllib 零新增依赖"
      min_lines: 120
    - path: "run_pipeline.py"
      provides: "--canvas-auto-import / --ep-name / --canvas-project-name 三个 flag + step_export 后的可选 post-step 接线（plain label，不 bump [N/9] step counter）"
      contains: "canvas_import.py"
    - path: "tests/test_canvas_import.py"
      provides: "mock urllib.request.urlopen 的三场景测试 + wiring 冒烟"
      min_lines: 80
  key_links:
    - from: "run_pipeline.py"
      to: "scripts/canvas_import.py"
      via: "subprocess.run([sys.executable, HERE/\"scripts\"/\"canvas_import.py\", \"--asset-dir\", work_dir, \"--project-name\", name], check=False)"
      pattern: "canvas_import\\.py"
    - from: "scripts/canvas_import.py"
      to: "http://127.0.0.1:10588/api/canvas/projects + /api/project/addProject + /api/canvas/v2/import-from-dir"
      via: "urllib.request.Request(data=json.dumps(..., ensure_ascii=False).encode(\"utf-8\"))"
      pattern: "urllib\\.request"
    - from: "scripts/canvas_import.py"
      to: "<asset-dir>/asset.json"
      via: "json.load 读 source.video_filename → intro（fallback: asset-dir basename）"
      pattern: "asset\\.json"
---

<objective>
在 kst 仓库新增「导出后自动导入画布」能力：新建 scripts/canvas_import.py（纯 urllib），并在 run_pipeline.py step_export 成功后接一个可选 post-step（失败仅 warning 不阻断），配最小 pytest。

Purpose: 让「小江湖·逆推资产集」类资产在管线末端一键出现在 kap 画布里，省掉手工 curl 三连（找项目 → 建项目 → import-from-dir）。

Output: scripts/canvas_import.py（新）、run_pipeline.py（接线）、tests/test_canvas_import.py（新）。

 kap-side API 事实（全部已验证 —— 127.0.0.1:10588 live probe + kap 源码直读）：
  * POST /api/canvas/projects（body `{}`）→ {code:200, data:[{id,name,mode,...}]}（live：61 个项目）
  * POST /api/project/addProject —— validateFields 必填 11 个全字符串字段；响应**只含** {code:200, data:{message:"新增项目成功"}}，**无项目 id**（src/routes/project/addProject.ts:44 `res.send(success({message}))` + id 服务端 Date.now()）→ 客户端建完必须回读 projects 按 name 匹配拿 id
  * POST /api/canvas/v2/import-from-dir —— zod schema：projectId z.number()、episodesId z.number()、workdir z.string().min(1)、projectName optional、mode z.enum(["merge","replace"]) optional（默认 merge）；200 → data:{imported,links,artifacts,phases,mode,workdir}；400 → {code:400,message}
  * ep01 项目行实测：projectType='' type='' mode='canvas-v2' videoRatio='' imageModel='' videoModel='' imageQuality='' userId=1
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@run_pipeline.py
@scripts/export_asset.py
@scripts/serve.py
@tests/test_pipeline_vision_wiring.py

<interfaces>
<!-- 本 plan 消费/对接的既有契约（executor 直接用，无需再探索） -->

run_pipeline.py 接线点（step_export 调用在 main() 末尾，约 line 850-853）：
```python
    stems_source_dir = stems_dir  # stems/htdemucs/<stem>/
    step_export(work_dir, video, stems_source_dir, asset_json,
                args.skip_export, args.force)

    print(f"\n[done] timeline: {html}")
```
- run_step(cmd, label) 是 check=True 的子进程 helper —— **不能**用于本 post-step（失败会 raise 阻断管线）；post-step 需自写 subprocess.run(..., check=False) + returncode 判断。
- 既有「无编号步骤」先例（本 plan 沿用）：attach_refs pre-step（step_timeline 内）与 local-vision pre-step（step 5 后）都用 **plain label banner**（不带 [N/M] 前缀），保持 [N/9] step-counter grep count 不变 —— tests/test_pipeline_vision_wiring.py::test_step_banner_count_unchanged 用 `re.findall(r"\[\d/9\]", src)` + `assert len(numbered) >= 9` 锁死了这一点。
- work_dir / stem / asset_json 变量在 main() 内已就位：work_dir = output/<video-stem>/，stem = Path(video).stem。

asset.json 结构（intro 数据源 —— asset.json **没有** episode 字段，最近似字段是 source.video_filename）：
```json
{"schema_version": "1.2", "asset_type": "shottimeline",
 "source": {"video_filename": "虫虫武侠小故事《小江湖》第01话：….mp4", "duration_sec": 308.352},
 "generator": {...}, "data": {...}, "media": {...}}
```
→ intro = Path(source.video_filename).stem；asset.json 不可读时 fallback = Path(asset_dir).name。

代码风格契约（CLAUDE.md / CONVENTIONS.md 摘要，新文件必须遵守）：
- 中文模块 docstring（目的/行为/用法）+ argparse 每个 flag 中文 help + main() + `if __name__ == "__main__":`
- 日志：bracketed 中文标签 print（如 `[canvas-import] …`）
- JSON body：json.dumps(..., ensure_ascii=False).encode("utf-8")
- 子进程一律 list-form argv（不经 shell）
- 测试：tests/ 下无 conftest，各 test 文件自建 `sys.path.insert(0, str(REPO_ROOT / ...))` 后 import 被测模块（mirror tests/test_qwen_eye_client.py 的 FakeHTTP 记录式替身 + monkeypatch 模式）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 scripts/canvas_import.py（纯 urllib 的画布导入 CLI）</name>
  <files>scripts/canvas_import.py</files>
  <behavior>
    - 场景A（复用）：/api/canvas/projects 返回含 name 精确匹配行 → 复用其 int id，零 addProject 调用，直接 POST import-from-dir
    - 场景B（创建）：projects 无匹配且未传 --no-create-project → POST addProject（11 字段）→ 响应无 id → 回读 projects 按 name 匹配拿新 id → POST import-from-dir
    - 场景B'（拒绝创建）：projects 无匹配且传了 --no-create-project → sys.exit 非 0 + 中文错误
    - import body 字段：projectId/episodesId 为 JSON number（int，非字符串）、workdir 为 os.path.abspath(asset_dir)、mode ∈ {"merge","replace"}
    - addProject body 字段：projectType/type/artStyle/directorManual/videoRatio/imageModel/videoModel/imageQuality 均空串 ""，name/intro/mode 三键取实值，mode="canvas-v2"
    - intro 来源：asset.json source.video_filename 去 extension；asset.json 缺失/损坏 → asset_dir basename（graceful，不 exit）
    - 网络错误（URLError/HTTPError/非 200 code）→ 中文错误 + sys.exit 非 0（standalone fail-loud；pipeline 侧 graceful-degrade 由 Task 2 负责）
  </behavior>
  <action>
    新建 scripts/canvas_import.py，风格 mirror scripts/export_asset.py（中文 docstring + argparse + main()）。CLI 参数：--asset-dir（required，资产根目录 output/&lt;video-stem&gt;/）、--base-url（默认 http://127.0.0.1:10588）、--project-name（required）、--episodes-id（type=int 默认 1）、--mode（choices=["merge","replace"] 默认 replace）、--no-create-project（store_true → allow_create=False）。

    结构（自上而下）：
    1. `_post_json(base_url: str, path: str, payload: dict, timeout: float) -> dict` —— 唯一 HTTP 出口：`urllib.request.Request(base_url + path, data=json.dumps(payload, ensure_ascii=False).encode("utf-8"), headers={"Content-Type": "application/json; charset=utf-8"}, method="POST")`；`urlopen(req, timeout=timeout)` → read → json.loads；捕获 `urllib.error.HTTPError`（读 e.read() body，把 HTTP status + envelope message 拼进中文错误）与 `urllib.error.URLError`（连接拒绝/超时的中文错误）。envelope `code != 200` 视为失败。**禁止** httpx/requests —— 脚本层零新增依赖（CONTEXT lock）。
    2. `_episode_intro(asset_dir: str) -> str` —— 读 asset.json 的 source.video_filename 取 stem；OSError/JSONDecodeError/KeyError 一律 fallback `Path(asset_dir).name`（注释说明 asset.json 无 episode 字段，video_filename stem 是最近似的「episode 名」）。
    3. `find_project(base_url, name) -> int | None` —— POST /api/canvas/projects body `{}` → 遍历 data 行，name 精确相等 → `int(row["id"])`。
    4. `create_project(base_url, name, intro) -> int` —— POST /api/project/addProject，body 11 字段（projectType/name/intro/type/artStyle/directorManual/videoRatio/imageModel/videoModel/imageQuality/mode，除 name/intro/mode="canvas-v2" 外全空串，对齐 ep01 行取值）；**addProject 响应不含 id**（kap src/routes/project/addProject.ts:44 只返 message）→ 建完必须回读 find_project 再匹配；仍 None → sys.exit 中文错误。
    5. `run_import(base_url, project_id, episodes_id, workdir, mode) -> dict` —— POST /api/canvas/v2/import-from-dir，body `{"projectId": int(project_id), "episodesId": int(episodes_id), "workdir": os.path.abspath(workdir), "mode": mode}`（projectId/episodesId 必须是 JSON number —— zod z.number() 会拒 string）。超时：_post_json 调用点传 30s（projects/addProject）/ 600s（import，扫媒体目录慢）。
    6. `main()` —— argparse（全部中文 help）；`asset_dir = os.path.abspath(args.asset_dir)`，`os.path.isdir` 不成立 → sys.exit 中文错误；流程打 `[canvas-import]` bracketed 中文日志：查找项目 → 复用 id=X / 新建 id=X → 导入 → 末尾打印 data 里 imported/links/artifacts/phases 计数摘要 + done 行。

    避免事项及原因：不要用 run_pipeline.run_step（那是 check=True helper，且跨模块 import 违反「stage 不互相 import」惯例）；不要把 CJK 路径拼进 URL（只有 body 携带 workdir，URL 是纯 ASCII path）；不要静默吞错（standalone CLI fail-loud 是项目惯例，graceful-degrade 责任在 Task 2 的 wiring 层）。
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-shot-timeline && python3 scripts/canvas_import.py --help && python3 scripts/canvas_import.py --asset-dir /nonexistent --project-name x; test $? -ne 0</automated>
    （--help 须列出全部 6 个 flag；不存在目录须非 0 退出。live 冒烟可选且**非变更**：server 在 127.0.0.1:10588 运行时，`python3 scripts/canvas_import.py --asset-dir output/&lt;任一ep&gt; --project-name 不存在的名字-260819 --no-create-project` 应非 0 退出并报「项目不存在」—— 只走 list+match 路径，不建项目。**不要**在 live 上跑无 --no-create-project 的创建路径，会往用户画布写真实项目行。）
  </verify>
  <done>脚本存在（≥120 行）且 --help 六 flag 齐全；不存在目录 fail-loud；纯 stdlib import（grep 无 httpx/requests）；全部日志带 [canvas-import] 前缀。</done>
</task>

<task type="auto">
  <name>Task 2: run_pipeline.py 接线 —— --canvas-auto-import 可选 post-step（graceful-degrade）</name>
  <files>run_pipeline.py</files>
  <action>
    两处修改：

    (a) argparse（放在 --skip-speaker-link 之后、--local-vision 块之前，保持 flag 分组语义）：新增三个 flag，全部中文 help ——
    - `--canvas-auto-import`（store_true，默认 False）：导出成功后自动调 scripts/canvas_import.py 把资产目录导入画布（kap 默认 http://127.0.0.1:10588；失败仅 warning 不阻断管线）
    - `--ep-name`（default None）：剧集短名（画布项目命名用；缺省取 video 文件 stem）
    - `--canvas-project-name`（default None）：覆盖画布项目名（缺省 f"小江湖·逆推资产集({ep_label})"，ep_label = --ep-name 或 video stem）

    (b) main() 内 step_export(...) 调用之后、`print(f"\n[done] timeline: {html}")` 之前，插入 post-step 块：
    - 触发条件：`args.canvas_auto_import and os.path.exists(asset_json)`（step_export 成功或 cached 都算「成功」—— step_export cached 路径也返回 asset_json）。
    - ep_label = `args.ep_name or stem`；project_name = `args.canvas_project_name or f"小江湖·逆推资产集({ep_label})"`。
    - argv 只传 `--asset-dir work_dir` 与 `--project-name project_name` 两项 —— episodes-id/mode 走 canvas_import.py 自身默认（1 / replace），不重复透传。
    - **不得用 run_step**（check=True 会 raise 阻断管线，违反「失败打 warning 不阻断」的 CONTEXT lock）：自写 `subprocess.run([sys.executable, str(HERE / "scripts" / "canvas_import.py"), "--asset-dir", work_dir, "--project-name", project_name], check=False)`，外层 try/except OSError；rc != 0 或 OSError → `print(f"[canvas-import] warning: ...（graceful-degrade，管线继续）")`，绝不 re-raise。
    - banner 用 **plain label**（如 `print(f"\n{'='*60}\ncanvas auto-import (canvas_import post-step)\n{'='*60}")`）—— **不带 [10/10] 前缀**：mirror attach_refs / local-vision 无编号先例，保住 tests/test_pipeline_vision_wiring.py::test_step_banner_count_unchanged 的 `re.findall(r"\[\d/9\]", src)` grep count。既有的 [N/9] banner 一律不动。
    - `args.canvas_auto_import` 为 True 但 asset.json 不存在 → 打一行 `[canvas-import] warning: asset.json 不存在（export 被跳过或失败），跳过画布导入` 后继续。
    - 同步更新模块 docstring：步骤清单末尾加 `10. 导出后自动导入画布（可选，--canvas-auto-import 开启；失败 warning 不阻断 —— plain label post-step）`，usage 段补 `[--canvas-auto-import] [--ep-name NAME] [--canvas-project-name NAME]`。docstring 编号 10 只是文档；运行时 banner 保持无编号。

    避免事项及原因：不要 bump 任何既有 [N/9] banner（会破坏 vision wiring 测试 + step-counter 惯例）；不要把失败升级为 sys.exit（与现有 route graceful-degrade 口径相反）；不要在 canvas 块内读 asset.json 任何字段（intro 的推导属于 canvas_import.py 单一职责）。
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-shot-timeline && python3 run_pipeline.py --help | grep -e --canvas-auto-import -e --ep-name -e --canvas-project-name && python3 -m pytest tests/test_pipeline_vision_wiring.py -q</automated>
    （--help 三 flag 齐全；vision wiring 既有测试全绿 = step banner 未被 bump。）
  </verify>
  <done>--help 含三个新 flag；run_pipeline.py 源码含 canvas_import.py 调用块且 check=False；不传 --canvas-auto-import 时无任何新行为；test_step_banner_count_unchanged 仍通过。</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_canvas_import.py —— mock urllib.request 三场景 + wiring 冒烟</name>
  <files>tests/test_canvas_import.py</files>
  <behavior>
    - Test 1（复用）：projects 返回含目标名行 → 只发生 2 次 HTTP（projects + import），零 addProject；import body projectId == 已有行 id
    - Test 2（自动创建）：projects 空 → addProject 11 字段断言（8 个空串 + name + intro == asset.json video_filename stem + mode=="canvas-v2"）→ 回读 projects 返回新行 id 777 → import projectId == 777
    - Test 3（import body 字段）：projectId/episodesId 为 int（type 断言，非 str）、episodesId == 1、workdir == str(tmp_path)（abspath）、mode == "replace"、Content-Type 含 application/json、raw bytes 可 utf-8 decode 且含 CJK 项目名
    - Test 4（--no-create-project 拒绝）：projects 空且传 flag → SystemExit code != 0，零 addProject 调用
    - Test 5-6（wiring 冒烟，mirror tests/test_pipeline_vision_wiring.py）：run_pipeline --help 含 --canvas-auto-import/--ep-name/--canvas-project-name；静态断言 canvas_import.py 调用块在 step_export( 调用之后、check=False、无 "[10/" banner
  </behavior>
  <action>
    新建 tests/test_canvas_import.py，文件头 `sys.path.insert(0, str(REPO_ROOT / "scripts"))` 后 `import canvas_import as ci`（mirror tests/test_qwen_eye_client.py 的 import 模式）。

    替身设施：
    - `_FakeResponse`：`__init__(self, body)` 存 bytes；实现 `read() -> bytes`、`__enter__`/`__exit__`（urlopen 语境管理器协议）、`status = 200`。
    - `_fake_urlopen(responses)` 工厂：闭包内按调用序 pop 预设 body；把 `(url, data_bytes, headers)` 追加进 `calls` 列表后返回 `_FakeResponse`；`monkeypatch.setattr(ci.urllib.request, "urlopen", fake)` —— 直接 mock urllib.request.urlopen 层（CONTEXT 指定），顺带覆盖 UTF-8 编码与 header 断言。
    - 被 import 的 body 解码 helper：`json.loads(call[1].decode("utf-8"))`。

    夹具：`tmp_path` 下写最小 `asset.json`（`{"source": {"video_filename": "虫虫武侠小故事《小江湖》第01话：测试.mp4"}}`）当 asset-dir；调 `ci.main()` 用 `monkeypatch.setattr(sys, "argv", ["canvas_import.py", "--asset-dir", str(tmp_path), "--project-name", "小江湖·逆推资产集(ep01)"])`（Test 4 追加 "--no-create-project"；需要 SystemExit 断言的用 `pytest.raises(SystemExit)`）。

    预设响应序列（envelope 形状）：
    - Test 1：`{"code":200,"data":[{"id":123,"name":"小江湖·逆推资产集(ep01)","mode":"canvas-v2"}]}` → `{"code":200,"data":{"imported":5,"links":5,"artifacts":9,"phases":3,"mode":"replace","workdir":"..."}}`
    - Test 2：`{"code":200,"data":[]}` → `{"code":200,"data":{"message":"新增项目成功"}}`（**刻意无 id**，钉死 kap 真实形状）→ `{"code":200,"data":[{"id":777,"name":"小江湖·逆推资产集(ep01)"}]}` → import 成功。Test 2 断言 addProject 调用的 URL 以 /api/project/addProject 结尾、且后续确实发生第 4 次调用（回读行为被钉住）。
    - Test 3 复用 Test 1 的序列，断言落在 capture 到的 import call 上。

    wiring 冒烟（Test 5-6，subprocess + 源码静态断言，mirror test_pipeline_vision_wiring.py 的 test_flags_exist_in_help / test_pre_step_wiring_static 写法）：`subprocess.run([sys.executable, str(REPO_ROOT / "run_pipeline.py"), "--help"], ...)` 断言三 flag 在 stdout；`(REPO_ROOT / "run_pipeline.py").read_text(encoding="utf-8")` 断言 `"canvas_import.py" in src`、`src.index("step_export(work_dir") < src.index("canvas_import.py")`、canvas 块附近有 `check=False`、`"[10/" not in src`。

    避免事项：不要打真网络（server 可能不在/会写用户数据）；不要 mock ci._post_json 之外的私有函数再叠 urlopen mock（二选一，本 plan 用 urlopen 层，保 UTF-8/header 覆盖）；不要在 wiring 测试里跑 main() body（会拉起整条管线）。
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-shot-timeline && python3 -m pytest tests/test_canvas_import.py -q && python3 -m pytest tests/ -q</automated>
    （新文件全绿 + 全仓无回归；全套件均为 mock/静态断言，秒级完成。）
  </verify>
  <done>test_canvas_import.py ≥6 个测试全绿；三场景（复用/创建/body 字段）+ --no-create-project 拒绝 + 2 个 wiring 冒烟全覆盖；既有 tests/ 无回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地脚本 → kap HTTP API | canvas_import.py 经明文 HTTP 访问 127.0.0.1:10588（无 TLS/无鉴权），body 携带本地资产路径 |
| 磁盘 → 脚本 | asset.json 读取（intro 来源），恶意/损坏 JSON 不应炸出未捕获 traceback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AW2-01 | Tampering | workdir 路径（CJK+全角标点）进 JSON body / argv | mitigate | json.dumps(ensure_ascii=False).encode("utf-8") + list-form subprocess argv（run_pipeline 既有 T-14-02 惯例），零 shell 插值 |
| T-AW2-02 | Spoofing | localhost HTTP 无鉴权，恶意本地进程可伪造 kap | accept | 与既有 route 步骤（127.0.0.1:8000 shot-analysis 等）同威胁模型：可信本机 + tailscale 内网，导入操作可重跑幂等（replace 模式） |
| T-AW2-03 | DoS | kap 宕/导入超时若阻断管线会让已完成导出白跑 | mitigate | Task 2 强制 subprocess check=False + [canvas-import] warning + 管线继续到 [done]（graceful-degrade lock） |
| T-AW2-04 | Info disclosure | 日志打印 workdir / 计数 | accept | 无凭据/无 PII；与仓库既有日志口径一致 |
</threat_model>

<verification>
1. `python3 -m pytest tests/ -q` —— 全绿（新增 6+ 用例，零回归）。
2. `python3 run_pipeline.py --help` —— --canvas-auto-import / --ep-name / --canvas-project-name 三 flag 出现且带中文 help。
3. `python3 -m pytest tests/test_pipeline_vision_wiring.py -q` —— step-counter 锁仍绿（未 bump [N/9]）。
4. （可选 live，非变更）server 运行时 `python3 scripts/canvas_import.py --asset-dir output/<ep> --project-name 不存在名-260819 --no-create-project` → 非 0 退出 + 中文「项目不存在」错误，证明 list/match 路径 end-to-end 可用。
5. 不加 --canvas-auto-import 跑一次既有命令 → 输出与改动前一致（无 [canvas-import] 行）。
</verification>

<success_criteria>
- scripts/canvas_import.py：三场景行为正确（复用 id / 建项目+回读 id / import body 字段与类型），纯 stdlib，中文 [canvas-import] 日志，standalone fail-loud。
- run_pipeline.py：可选 post-step 接线完成，失败仅 warning；默认行为零变化；step counter 未 bump。
- tests/test_canvas_import.py：≥6 测试全绿，全 mock 零网络。
- addProject「响应无 id → 回读」行为被测试钉死（ kap 源码 addProject.ts:44 验证过的事实）。
</success_criteria>

<output>
Create `.planning/quick/260819-aw2-canvas-auto-import/260819-aw2-SUMMARY.md` when done
</output>
