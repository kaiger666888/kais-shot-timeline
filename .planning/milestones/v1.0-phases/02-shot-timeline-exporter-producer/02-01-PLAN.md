---
phase: 02-shot-timeline-exporter-producer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/export_asset.py
  - run_pipeline.py
autonomous: true
requirements: [EXPORT-01, EXPORT-02]
user_setup: []

must_haves:
  truths:
    - "After `python3 run_pipeline.py --video <path>` finishes, output/<video-stem>/asset.json exists alongside the existing 5 data JSONs"
    - "The written asset.json PASSES spec/schemas/asset.schema.json validation (inline Draft202012Validator, zero errors)"
    - "output/<video-stem>/video.mp4 is a symlink resolving to the ORIGINAL video (with audio stream), not to h264.mp4"
    - "output/<video-stem>/stems/{vocals,drums,other}.wav are symlinks resolving to stems/htdemucs/<stem>/<name>.wav (bass excluded)"
    - "Existing timeline.html and its <basename>_vocals.wav references still resolve (additive-only invariant)"
    - "Detection / transcription / separation / timeline-html source files are byte-identical to main branch (zero algorithm changes)"
    - "Running pipeline with --skip-export skips asset export cleanly; running with --force clears asset.json alongside other caches"
    - "Run with missing prompts.json exits non-zero with an actionable Chinese error mentioning prompts.json"
  artifacts:
    - path: "scripts/export_asset.py"
      provides: "ShotTimelineAsset 导出器：manifest 写入 + canonical symlinks + inline jsonschema 自校验"
      min_lines: 150
      contains: "Draft202012Validator"
    - path: "run_pipeline.py"
      provides: "step_export（step 6）+ --skip-export flag + --force clears asset.json + [N/6] 全量替换"
      contains: "def step_export("
  key_links:
    - from: "run_pipeline.py:step_export"
      to: "scripts/export_asset.py"
      via: "subprocess.run([sys.executable, str(HERE / 'scripts' / 'export_asset.py'), ...], check=True)"
      pattern: "HERE / \"scripts\" / \"export_asset.py\""
    - from: "scripts/export_asset.py"
      to: "spec/schemas/asset.schema.json"
      via: "inline Draft202012Validator(schema).iter_errors(asset_dict)"
      pattern: "Draft202012Validator"
    - from: "scripts/export_asset.py"
      to: "output/<video-stem>/video.mp4"
      via: "os.symlink(original_video_abs_path, link_path)"
      pattern: "os.symlink"
---

<objective>
实现 Phase 2 的生产端核心：新增 `scripts/export_asset.py`（manifest 写入 + canonical symlinks + inline jsonschema 自校验），并把 `run_pipeline.py` 从 5 步扩成 6 步（新增 `step_export` + `--skip-export` flag + `--force` 一并清 asset.json + 全量 `[N/5]`→`[N/6]` 替换）。

Purpose: 把 `run_pipeline.py` 现有产出打包成符合 Phase 1 spec 的自描述 ShotTimelineAsset，且严格 additive-only（不碰检测/转录/分离/timeline-html 算法）。覆盖 EXPORT-01（导出器存在并产出符合 spec 的产物）+ EXPORT-02（带版本号 + 自描述 manifest + additive-only）。

Output: `scripts/export_asset.py`（新文件）+ `run_pipeline.py`（修改：step_export、--skip-export、--force asset.json 清理、[N/6] 全量替换、main 中调用 step_export）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shot-timeline-exporter-producer/02-CONTEXT.md
@.planning/phases/02-shot-timeline-exporter-producer/02-RESEARCH.md
@.planning/phases/02-shot-timeline-exporter-producer/02-PATTERNS.md
@.planning/phases/02-shot-timeline-exporter-producer/02-VALIDATION.md
@spec/SPEC.md
@spec/schemas/asset.schema.json
@spec/fixtures/minimal/asset.json
@run_pipeline.py
@scripts/serve.py
@spec/validate.py

<interfaces>
<!-- 关键契约与现有签名，executor 直接使用，无需 codebase 探索 -->

From spec/schemas/asset.schema.json (asset.json 权威 schema):
- required 顶层字段: schema_version, asset_type, source, generator, data, media
- additionalProperties: false (全层严格)
- schema_version pattern: ^(0|[1-9]\d*)(\.(0|[1-9]\d*))?$ (接受 "1", "1.1"; 拒绝 "v1", "1.1.1")
- asset_type const: "shottimeline"
- source.required: [video_filename, duration_sec]
- generator.required: [tool, version, generated_at]
- data.required: [shots, audio_analysis, transcript, frames, prompts] (每个 pattern 拒绝 .. 穿越与 Windows 保留字符)
- media.required: [video, stems]; stems.required: [vocals, drums, other] (bass 显式剔除)
- media.video pattern: ^(?!.*\.\.)([^/]+/)*video\.mp4$ (必须以 video.mp4 结尾)

From spec/fixtures/minimal/asset.json (canonical 参考形态):
- 字面量值: schema_version="1", asset_type="shottimeline", generator.tool="kais-shot-timeline"
- data.{shots,audio_analysis,transcript,frames,prompts}: ["<name>.json", ...]
- media.video: "video.mp4"; media.stems.{vocals,drums,other}: "stems/<name>.wav"

From run_pipeline.py (现有 5-step orchestrator):
- HERE = Path(__file__).parent.resolve() (line 38)
- probe_duration(path: str) -> float (line 49-56, ffprobe 调用)
- run_step(cmd: list, label: str) (line 77-81, subprocess.run check=True)
- step_timeline(video, work_dir, shots_json, audio_json, transcript, frames_json, stems_dir, out_html, video_src, stem_basename) -> str (line 139-166, mtime-cache 模板)
- step_detect/step_separate/step_transcribe 的 skip 参数处理 (line 86-88, 104-106, 123-125)
- main(): argparse 块 line 169-199; video=os.path.abspath(args.video) line 201; --force 块 line 218-222; step_timeline 调用 line 245-246

From scripts/serve.py (Wave 2 修改目标，本 plan 不动):
- _Partial class (line 85-96) 缺 close() 方法 — 本 plan 不修，留给 02-02

From spec/validate.py:49:
- SMOKE_SHAPES = ["shots", "audio_analysis", "transcript", "frames", "prompts"] — 显式排除 asset
- 这是为什么 export_asset.py 必须 INLINE 校验，不能 subprocess 到 spec/validate.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create scripts/export_asset.py (manifest writer + canonical symlinks + inline jsonschema)</name>
  <files>scripts/export_asset.py</files>
  <read_first>
    - spec/schemas/asset.schema.json (权威 schema — 字段名、pattern、required 全在此)
    - spec/fixtures/minimal/asset.json (canonical 字面量参考: schema_version="1", asset_type="shottimeline", tool="kais-shot-timeline")
    - spec/SPEC.md (§3 字段表 + §5 5 数据形状 producer 脚本 + §6 媒体路径约定)
    - .planning/phases/02-shot-timeline-exporter-producer/02-PATTERNS.md (Pattern 1/2/3 + argparse/JSON write/ffprobe fallback/inline jsonschema/sys.exit 模板)
    - .planning/phases/02-shot-timeline-exporter-producer/02-RESEARCH.md (§Code Examples: build_asset_dict、_git_sha、validate_asset_json 完整示例; §Pitfalls 1/4/5: video.mp4 必须指原始 abs path、prompts.json 无 in-repo producer、SPEC.md §5 gen_prompts_html.py 不存在)
    - html/gen_timeline_html.py:1-25 + 1005-1030 (CLI 入口 + argparse + Chinese help 模板)
    - spec/validate.py:49-57 + 87-94 (SMOKE_SHAPES 排除 asset + load_validator/iter_errors 模板)
    - run_pipeline.py:49-56 (probe_duration ffprobe 模板) + 203 (sys.exit 模板)
    - audio/transcribe.py (transcript.json 字段结构 — 验证 source/duration 字段存在)
    - 任一 output/《小江湖》第03话…/transcript.json + prompts.json 实际产物（确认字段命名）
  </read_first>
  <behavior>
    - 导出器在 prompts.json 缺失时退出码非零，stderr 含 "prompts.json 不存在" + 解释 schema required + 提示外部产出
    - 写入的 asset.json 经 inline Draft202012Validator 校验零错误
    - video.mp4 symlink 解析到的目标经 ffprobe -show_entries stream=codec_type 确认含 audio 流（非 h264.mp4）
    - stems/{vocals,drums,other}.wav symlink 解析到 stems/htdemucs/<video-stem>/<name>.wav 真实文件
    - bass.wav 不被创建 canonical symlink（保留 htdemucs 原始 bass 不动）
    - --force 时清空已存在的 4 个 symlinks + asset.json，重新创建
    - 不 --force 时若 symlinks 已存在且 target 正确则跳过（idempotent re-run 不报错）
  </behavior>
  <action>
    创建 `/data/workspace/kais-shot-timeline/scripts/export_asset.py`，一个自包含的 argparse CLI，结构沿用 `html/gen_timeline_html.py` 的 shebang + Chinese module docstring + `def main()` + `if __name__ == "__main__"` 形态。

    Module docstring（Chinese）描述：目的（把 `output/<video-stem>/` 打包成 ShotTimelineAsset）、5 个输入 JSON、4 个 canonical symlinks 产出、CLI 用法。

    Imports：argparse、json、os、sys、subprocess、from datetime import datetime, timezone、from pathlib import Path。jsonschema 的 `Draft202012Validator` 在校验 helper 函数体内 lazy import（沿用 CLAUDE.md 的 lazy-import 惯例）。

    REPO 常量：`REPO = Path(__file__).parent.parent.resolve()`（scripts/ → repo root，用于定位 spec/schemas/asset.schema.json）。

    定义 4 个 helper（全部 snake_case + Chinese docstring）：

    1. `_probe_duration(path: str) -> float` — 直接照搬 run_pipeline.py:49-56 的 ffprobe 模板。

    2. `_git_sha() -> str` — 用 `subprocess.run(["git", "-C", str(REPO), "rev-parse", "--short", "HEAD"], capture_output=True, text=True, timeout=2)`，return stdout.strip()；except (subprocess.SubprocessError, OSError) 时 return "dev"。

    3. `ensure_symlink(link_path: str, target: str) -> None` — 幂等 symlink 创建（02-PATTERNS.md Pattern 2 完整示例）。关键逻辑：若 `os.path.islink(link_path)` 为真，比较 `os.readlink(link_path) == target`，相等则 return（skip），不等则 `os.unlink` + 重建；若 `os.path.exists(link_path)` 为真但非 symlink，raise `FileExistsError(f"refusing to overwrite non-symlink: {link_path}")`；最后 `os.symlink(target, link_path)`。

    4. `validate_asset_json(asset_dict: dict) -> None` — inline jsonschema 校验（02-PATTERNS.md Pattern 3 + RESEARCH §Code Examples）。lazy import `from jsonschema import Draft202012Validator`；`schema_path = REPO / "spec" / "schemas" / "asset.schema.json"`；`validator = Draft202012Validator(json.load(open(schema_path, encoding="utf-8")))`；`errors = sorted(validator.iter_errors(asset_dict), key=lambda e: list(e.absolute_path))`；若有 errors，逐条格式化 `at <path>: <message>` 并 `sys.exit(f"asset.json failed schema validation ({len(errors)} error(s)):\n" + "\n".join(lines))`。**绝不 subprocess 到 spec/validate.py**（其 SMOKE_SHAPES 排除 asset，会让无效 manifest 通过）。

    `build_asset_dict(work_dir: str, video_path: str) -> dict` —— 按 RESEARCH §Code Examples 实现。关键字段 sourcing：
    - 读 `transcript.json`：`with open(os.path.join(work_dir, "transcript.json"), encoding="utf-8") as f: transcript = json.load(f)`
    - `video_filename = os.path.basename(video_path)`；若 `transcript.get("source")` 与 video_filename 不一致，`print(f"[warn] transcript.source={...} != video basename={...}")` —— warn 不 fail
    - `duration = transcript.get("duration")`；若 falsy 则 fallback `_probe_duration(video_path)`
    - 返回 dict 字面量（逐字对齐 spec/fixtures/minimal/asset.json 字段）：
      - `schema_version: "1"`（hardcoded）
      - `asset_type: "shottimeline"`（const）
      - `source: {"video_filename": video_filename, "duration_sec": duration}`
      - `generator: {"tool": "kais-shot-timeline", "version": _git_sha(), "generated_at": datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")}`
      - `data: {"shots": "shots.json", "audio_analysis": "audio_analysis.json", "transcript": "transcript.json", "frames": "frames.json", "prompts": "prompts.json"}`
      - `media: {"video": "video.mp4", "stems": {"vocals": "stems/vocals.wav", "drums": "stems/drums.wav", "other": "stems/other.wav"}}`

    `main()` —— argparse：`--work-dir` (required)、`--video` (required, 原始视频 abs path，来自 run_pipeline 的 os.path.abspath(args.video))、`--stems-source-dir` (required, 指向 stems/htdemucs/<video-stem>/)、`--output` (required, asset.json 写入路径)、`--force` (action="store_true")。所有 `help=` 字符串用中文。

    main 流程（顺序敏感）：
    (a) prompts.json 存在性 guard（02-RESEARCH Pitfall 4）：`prompts_path = os.path.join(args.work_dir, "prompts.json")`；若 `not os.path.exists(prompts_path)` 则 `sys.exit(f"prompts.json 不存在: {prompts_path}\n  asset.schema.json 的 data.prompts 是 required 字段 —— 不可省略。\n  prompts.json 当前由独立步骤产出（未接入 run_pipeline）；请先就位再运行导出。")`。
    (b) video abs path：`video = os.path.abspath(args.video)`；若不存在 `sys.exit(f"input video not found: {video}")`。
    (c) 验证 video 含 audio 流（02-RESEARCH Pitfall 1）：`r = subprocess.run(["ffprobe", "-v", "quiet", "-show_entries", "stream=codec_type", "-of", "csv=p=0", video], capture_output=True, text=True)`；若 `"audio"` 不在 r.stdout 则 `sys.exit(f"video has no audio stream: {video} (h264.mp4 transcode was -an stripped — exporter needs original)")`。
    (d) 创建 `stems/` 子目录：`os.makedirs(os.path.join(args.work_dir, "stems"), exist_ok=True)`。
    (e) 4 个 canonical symlinks（关键：target 必须是 abs path，且 video target 是 --video 指向的原始文件，**不是** work_dir 内的 `<original-name>.mp4`，后者链到 h264.mp4 无音轨）：
       - `ensure_symlink(os.path.join(args.work_dir, "video.mp4"), video)`（原始视频 abs path）
       - `ensure_symlink(os.path.join(args.work_dir, "stems", "vocals.wav"), os.path.join(args.stems_source_dir, "vocals.wav"))`
       - `ensure_symlink(os.path.join(args.work_dir, "stems", "drums.wav"), os.path.join(args.stems_source_dir, "drums.wav"))`
       - `ensure_symlink(os.path.join(args.work_dir, "stems", "other.wav"), os.path.join(args.stems_source_dir, "other.wav"))`
       - **不创建** bass.wav canonical symlink（schema 拒绝；但 htdemucs 原始 bass.wav 不动）
    (f) 若 args.force 且 args.output 存在：`os.unlink(args.output)`（让写入路径干净）。
    (g) 构建 dict：`asset = build_asset_dict(args.work_dir, video)`。
    (h) 写入：`with open(args.output, "w", encoding="utf-8") as f: json.dump(asset, f, indent=2, ensure_ascii=False)`（**必须** ensure_ascii=False —— Chinese video_filename）。
    (i) Inline schema 校验：`validate_asset_json(asset)`（invalid 时 helper 内 sys.exit 非 0）。
    (j) Post-write assert：4 个 canonical paths 都 resolve 到真实文件。`for p in ["video.mp4", "stems/vocals.wav", "stems/drums.wav", "stems/other.wav"]: assert os.path.exists(os.path.join(args.work_dir, p)), f"canonical path missing after export: {p}"`。
    (k) Final-status print（CLAUDE.md bracketed-tag 惯例）：`print(f"[export-asset] wrote asset.json → {args.output}")` 后接 `print(f"[export-asset] canonical symlinks: video.mp4, stems/{{vocals,drums,other}}.wav")`。

    路径解析注意：`scripts/export_asset.py` 不会跨 stage import run_pipeline.py 的 probe_duration —— 自带 `_probe_duration` 私有副本（沿用项目"不跨 stage import"约定）。
  </action>
  <verify>
    <automated>
    # 1. 语法/import 健康
    python3 -c "import ast; ast.parse(open('scripts/export_asset.py').read())" && echo "syntax OK"
    python3 scripts/export_asset.py --help | head -5

    # 2. prompts.json 缺失时 fails loud（tmp 空目录）
    EMPTY=$(mktemp -d)
    python3 scripts/export_asset.py --work-dir "$EMPTY" --video /dev/null --stems-source-dir "$EMPTY" --output "$EMPTY/asset.json" 2>&1 || true | grep -q "prompts.json 不存在" && echo "guard OK"
    rm -rf "$EMPTY"

    # 3. source 包含关键 identifier
    grep -q "from jsonschema import Draft202012Validator" scripts/export_asset.py && echo "inline-validator OK"
    grep -q "os.symlink" scripts/export_asset.py && echo "symlink-api OK"
    grep -q '"video.mp4"' scripts/export_asset.py && echo "canonical-video OK"
    grep -q '"stems/vocals.wav"' scripts/export_asset.py && echo "canonical-vocals OK"
    ! grep -q "bass.wav" scripts/export_asset.py && echo "bass-excluded OK"

    # 4. 端到端：对一个真实 output 子目录跑一次（reset + 验证）
    TARGET=$(ls -d output/*/ | head -1)
    rm -f "$TARGET/asset.json" "$TARGET/video.mp4" "$TARGET/stems/vocals.wav" "$TARGET/stems/drums.wav" "$TARGET/stems/other.wav" 2>/dev/null
    python3 scripts/export_asset.py \
      --work-dir "$TARGET" \
      --video "$(readlink -f "$TARGET/《小江湖》"*.mp4 2>/dev/null || ls "$TARGET"/*.mp4 2>/dev/null | grep -v h264 | head -1)" \
      --stems-source-dir "$TARGET/stems/htdemucs/$(basename "$TARGET")" \
      --output "$TARGET/asset.json"
    # 校验产物
    python3 -c "
    import json, sys, os
    from jsonschema import Draft202012Validator
    schema = json.load(open('spec/schemas/asset.schema.json'))
    asset = json.load(open('$TARGET/asset.json'))
    errs = list(Draft202012Validator(schema).iter_errors(asset))
    assert not errs, f'schema errors: {[e.message for e in errs][:3]}'
    for p in ['video.mp4', 'stems/vocals.wav', 'stems/drums.wav', 'stems/other.wav']:
        assert os.path.exists(os.path.join('$TARGET', p)), f'missing: {p}'
    # video.mp4 必须 resolve 到含 audio 的原始视频
    import subprocess
    r = subprocess.run(['ffprobe', '-v', 'quiet', '-show_entries', 'stream=codec_type', '-of', 'csv=p=0', os.path.join('$TARGET', 'video.mp4')], capture_output=True, text=True)
    assert 'audio' in r.stdout, 'video.mp4 symlink target has NO audio — likely chained to h264.mp4'
    print('end-to-end OK')
    "

    # 5. idempotent re-run 不报错
    python3 scripts/export_asset.py \
      --work-dir "$TARGET" \
      --video "$(readlink -f "$TARGET"/*.mp4 2>/dev/null | grep -v h264 | head -1)" \
      --stems-source-dir "$TARGET/stems/htdemucs/$(basename "$TARGET")" \
      --output "$TARGET/asset.json" && echo "idempotent OK"

    # 6. spec/validate.py 不被破坏（Phase 1 invariant）
    python3 spec/validate.py | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `scripts/export_asset.py` 文件存在，行数 ≥ 150（含 module docstring + 4 helper + build_asset_dict + main）
    - `grep -q "from jsonschema import Draft202012Validator" scripts/export_asset.py` 通过（inline 校验，非 subprocess）
    - `! grep -qE "subprocess.run.*spec/validate.py" scripts/export_asset.py` 通过（不 subprocess 到 spec/validate.py）
    - `grep -c "os.symlink" scripts/export_asset.py` ≥ 1
    - `grep -q '"video.mp4"' scripts/export_asset.py` 与 `grep -q '"stems/vocals.wav"' scripts/export_asset.py` 通过
    - `! grep -q '"stems/bass.wav"' scripts/export_asset.py` 通过（bass 不在 canonical 集合）
    - `python3 scripts/export_asset.py --help` 退出码 0
    - 对一个空的 mktemp 目录调用，退出码非 0，stderr 含字面量 "prompts.json 不存在"
    - 对一个真实 output/《小江湖》*/ 目录调用后：asset.json 经 Draft202012Validator 校验零错误；4 个 canonical paths 存在；ffprobe 确认 video.mp4 解析的目标含 audio 流
    - 连续两次调用：第二次退出码 0（idempotent）
    - `python3 spec/validate.py` 仍报 minimal 6/6 [valid]（Phase 1 invariant 未破坏）
  </acceptance_criteria>
  <done>
    scripts/export_asset.py 落地，可独立 CLI 调用产出符合 spec 的 asset.json + 4 个 canonical symlinks；prompts.json 缺失 fails loud；video.mp4 symlink 目标含 audio 流；idempotent re-run 不报错；inline jsonschema 校验通过。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire step_export into run_pipeline.py (bump [N/5]→[N/6], add --skip-export, --force clears asset.json)</name>
  <files>run_pipeline.py</files>
  <read_first>
    - run_pipeline.py (整文件 258 行 — 重点：HERE/probe_duration/step_detect/step_separate/step_transcribe/step_timeline/main/argparse/--force 块/step 调用序列)
    - .planning/phases/02-shot-timeline-exporter-producer/02-PATTERNS.md (§run_pipeline.py changes: step_export 模板、--skip-export、--force asset.json 追加、[N/5]→[N/6] 全量替换行号清单)
    - .planning/phases/02-shot-timeline-exporter-producer/02-RESEARCH.md (§Pitfall 3: [N/5]→[N/6] 强制要求；§Open Question 2: step_export 失败 = export_asset.py sys.exit → subprocess check=True raises → run_pipeline 崩，这是项目既定 "fails loud" 惯例)
    - scripts/export_asset.py (Task 1 产出 — 确认 --work-dir/--video/--stems-source-dir/--output/--force 是 step_export 要拼的命令行)
  </read_first>
  <behavior>
    - run_pipeline.py 调用序列变成 6 步：ensure_h264 → step_detect → step_separate → step_transcribe → step_timeline → step_export
    - 所有 stage-prefix 由 [N/5] 升为 [N/6]（14 处全覆盖）
    - --skip-export flag 存在并能干净跳过 export
    - --force 同时清掉 asset.json
    - step_export mtime cache 命中时直接返回，未命中时 subprocess 到 scripts/export_asset.py
    - step_export 失败（export_asset.py sys.exit 非 0）→ subprocess.run check=True raises CalledProcessError → run_pipeline 崩（fails loud，符合项目惯例）
  </behavior>
  <action>
    在 `/data/workspace/kais-shot-timeline/run_pipeline.py` 做 4 处修改（顺序无关，但建议按下方顺序）：

    **修改 A — 全量替换 [N/5] → [N/6]（02-RESEARCH Pitfall 3，14 处）**
    用 grep 先确认所有命中行：`grep -n "\[[0-9]/5\]" run_pipeline.py`。预期命中 14 行：line 63 `[1/5]`、line 67 `[1/5]`、line 69 `[1/5]`、line 87 `[2/5]`、line 90 `[2/5]`、line 97 `[2/5]`、line 105 `[3/5]`、line 108 `[3/5]`、line 116 `[3/5]`、line 124 `[4/5]`、line 127 `[4/5]`、line 135 `[4/5]`、line 147 `[5/5]`、line 165 `[5/5]`。全部改成对应 `[N/6]`。可用 sed 批量：`sed -i 's|\[1/5\]|[1/6]|g; s|\[2/5\]|[2/6]|g; s|\[3/5\]|[3/6]|g; s|\[4/5\]|[4/6]|g; s|\[5/5\]|[5/6]|g' run_pipeline.py`。改完 `grep -c "\[[0-9]/5\]" run_pipeline.py` 必须 return 0。

    **修改 B — 新增 step_export 函数（仿 step_timeline + step_detect 的 skip 形态）**
    在 `step_timeline` 之后、`def main()` 之前插入新函数。签名：`def step_export(work_dir: str, video: str, stems_source_dir: str, asset_json: str, skip: bool, force: bool) -> str:`。

    函数体逻辑（02-PATTERNS.md "step_export template" 完整模板）：
    - skip 处理（仿 step_detect:86-88）：`if skip: print("[6/6] --skip-export: skipping asset export"); return asset_json if os.path.exists(asset_json) else None`
    - mtime cache（仿 step_timeline:143-148 但 inputs 不同）：
      ```
      inputs = [
          os.path.join(work_dir, "shots.json"),
          os.path.join(work_dir, "audio_analysis.json"),
          os.path.join(work_dir, "transcript.json"),
          os.path.join(work_dir, "frames.json"),
          os.path.join(work_dir, "prompts.json"),
          video,
      ]
      inputs_exist = [p for p in inputs if os.path.exists(p)]
      if (not force and os.path.exists(asset_json)
              and inputs_exist
              and os.path.getmtime(asset_json) > max(os.path.getmtime(p) for p in inputs_exist)):
          print(f"[6/6] cached asset: {asset_json}")
          return asset_json
      ```
    - subprocess 调用（仿 step_timeline:149-165 + run_step label）：
      ```
      cmd = [sys.executable, str(HERE / "scripts" / "export_asset.py"),
             "--work-dir", work_dir,
             "--video", video,
             "--stems-source-dir", stems_source_dir,
             "--output", asset_json]
      if force:
          cmd += ["--force"]
      run_step(cmd, "[6/6] ShotTimelineAsset export")
      return asset_json
      ```

    **修改 C — argparse 新增 --skip-export（在 --skip-transcribe 之后，仿其形态）**
    在 line 180 `--skip-transcribe` 之后插入：
    ```
    ap.add_argument("--skip-export", action="store_true",
                    help="跳过 ShotTimelineAsset 导出（asset.json + canonical symlinks）")
    ```

    **修改 D — main() 接入 step_export（5 处微改）**

    D1. 新增 asset_json 路径常量（与现有 line 210-214 path 常量并列）：在 `out_html = os.path.join(work_dir, "timeline.html")` 之后插入 `asset_json = os.path.join(work_dir, "asset.json")`。

    D2. --force 块追加 asset_json 清理（line 218-222 的 tuple 内）：将 `for p in (shots_json, frames_json, audio_json, transcript, out_html):` 改成 `for p in (shots_json, frames_json, audio_json, transcript, out_html, asset_json):`。

    D3. 在 step_timeline 调用之后（line 245-246 之后，line 248 `[done] timeline:` print 之前）插入 step 6 调用块：
    ```
    # 6. ShotTimelineAsset 导出
    stems_source_dir = stems_dir  # 已在 line 209 定义为 stems_root/demucs_model/stem
    step_export(work_dir, video, stems_source_dir, asset_json,
                args.skip_export, args.force)
    ```

    D4. 在 `[done] timeline:` print 块（line 248-253）之后追加：`print(f"       asset: {asset_json}")`。

    **不要修改**：detectors/、audio/、html/gen_timeline_html.py 任一文件（additive-only 硬约束，02-VALIDATION.md Manual-Only Verifications 会用 `git diff main` 验证）。

    **更新 module docstring**：在 run_pipeline.py 顶部 docstring 的步骤列表（line 4-10 区域）追加 `6. ShotTimelineAsset 导出（scripts/export_asset.py）`，并把 `[--skip-detect] [--skip-separate] [--skip-transcribe]` 行扩成含 `[--skip-export]`；输出布局（line 21-29）追加 `asset.json` + `stems/{vocals,drums,other}.wav` 两行注释。
  </action>
  <verify>
    <automated>
    # 1. 语法
    python3 -c "import ast; ast.parse(open('run_pipeline.py').read())" && echo "syntax OK"

    # 2. [N/5] 全量替换
    test "$(grep -c "\[[0-9]/5\]" run_pipeline.py)" = "0" && echo "no [N/5] left"
    test "$(grep -c "\[[0-9]/6\]" run_pipeline.py)" -ge "14" && echo "[N/6] present"

    # 3. step_export 与 --skip-export 存在
    grep -q "def step_export(" run_pipeline.py && echo "step_export defined"
    grep -q -- "--skip-export" run_pipeline.py && echo "--skip-export present"

    # 4. --force 清 asset.json
    grep -q "asset_json" run_pipeline.py
    grep -E "for p in \(.*asset_json.*\):" run_pipeline.py && echo "asset_json in --force tuple"

    # 5. main 中调用了 step_export
    grep -q "step_export(" run_pipeline.py
    grep -E "args\.skip_export" run_pipeline.py && echo "skip_export wired in main"

    # 6. --help 不崩，且列出 --skip-export
    python3 run_pipeline.py --help 2>&1 | grep -q -- "--skip-export"

    # 7. Additive-only invariant：core 算法文件未动
    git diff --stat main -- detectors/ audio/ html/gen_timeline_html.py | wc -l | grep -q "^0$" && echo "additive-only OK"

    # 8. 端到端：跑 --skip-detect --skip-separate --skip-transcribe（命中缓存）+ 不 skip export，看 asset.json 是否产出
    TARGET=$(ls -d output/*/ | head -1)
    VIDEO=$(readlink -f "$TARGET"/*.mp4 2>/dev/null | grep -v h264 | head -1)
    # 先清 asset.json 验证 step_export 真的会产出
    rm -f "$TARGET/asset.json"
    python3 run_pipeline.py --video "$VIDEO" --skip-detect --skip-separate --skip-transcribe 2>&1 | tail -20
    test -f "$TARGET/asset.json" && echo "pipeline produces asset.json"

    # 9. --skip-export 真的跳过（asset.json 不被重写）
    touch -d "1 hour ago" "$TARGET/asset.json" 2>/dev/null
    python3 run_pipeline.py --video "$VIDEO" --skip-detect --skip-separate --skip-transcribe --skip-export 2>&1 | grep -q "\-\-skip-export: skipping"
    </automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('run_pipeline.py').read())"` 退出码 0
    - `grep -c "\[[0-9]/5\]" run_pipeline.py` 返回 0（无遗留 [N/5]）
    - `grep -c "\[[0-9]/6\]" run_pipeline.py` ≥ 14（含原有 14 + 新增 step_export 的 [6/6]）
    - `grep -q "def step_export(" run_pipeline.py` 通过
    - `grep -q -- "--skip-export" run_pipeline.py` 通过
    - `grep -E "for p in \(.*asset_json.*\):" run_pipeline.py` 通过（--force 清 asset.json）
    - `grep -E "args\.skip_export" run_pipeline.py` 通过（main 中 wired）
    - `python3 run_pipeline.py --help` 输出含字面量 `--skip-export`
    - `git diff --stat main -- detectors/ audio/ html/gen_timeline_html.py` 完全为空（additive-only invariant）
    - 对现有 output/《小江湖》*/ 跑 `python3 run_pipeline.py --video <path> --skip-detect --skip-separate --skip-transcribe` 后：`asset.json` 文件存在并经 schema 校验通过
    - 加 `--skip-export` 再跑：日志含 `[6/6] --skip-export: skipping asset export`，asset.json 不被重写（mtime 不变）
  </acceptance_criteria>
  <done>
    run_pipeline.py 升级到 6 步：所有 stage-prefix 为 [N/6]；新增 `step_export` 函数（mtime cache + subprocess 调 scripts/export_asset.py）；新增 `--skip-export` flag；`--force` 清 asset.json；main 在 step_timeline 之后调用 step_export；detectors/audio/html/gen_timeline_html.py 零修改（additive-only）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI → run_pipeline.py | 用户在 shell 传入 --video/--skip-* 等 flag； trusted（单机离线 CLI） |
| run_pipeline.py → export_asset.py | orchestrator shell out 子进程；args 由 run_pipeline 拼装，非用户直接控制 |
| export_asset.py → filesystem (symlinks + asset.json) | 写入 `output/<video-stem>/` 资产根；目标目录受信 |
| export_asset.py → spec/schemas/asset.schema.json | 只读 schema 权威源；trust-on-first-use（Phase 1 已落地） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (path traversal via asset.json paths) | export_asset.py data/media field values | mitigate | asset.json 路径字段全是字面量（`"video.mp4"`、`"stems/vocals.wav"` 等），不接受用户输入；写后立即 inline `Draft202012Validator(asset.schema.json)` 校验，schema 的 6 个 pattern（`(?!.*\.\.)` 负向前瞻）拒绝任何 `../` 穿越尝试 |
| T-02-02 | Tampering (symlink swap / TOCTOU) | export_asset.py ensure_symlink | accept | 单机离线 CLI，单用户场景，无并发攻击者；`ensure_symlink` 的 `os.path.islink`/`os.path.exists` 分支拒绝覆盖非 symlink 的真实文件（防止误伤），原子 `os.symlink`；RESEARCH §Security Domain 评估为 low risk |
| T-02-05 | Spoofing (video.mp4 → 错误 target，consumer 听不到 audio) | export_asset.py video.mp4 symlink target | mitigate | 02-RESEARCH Pitfall 1 关键修复：target 必须是 `--video` 的原始 abs path（含 audio 流），不是 work_dir 内 `<original-name>.mp4`（后者链到 `-an` 去 audio 的 h264.mp4）。export_asset.py 内置 ffprobe 检查 video 含 audio stream，缺则 sys.exit 非 0 |
| T-02-06 | Information Disclosure (asset.json 含原始视频 abs path 泄漏用户目录结构) | export_asset.py source.video_filename | accept | schema 要求 `source.video_filename` 是 basename（非 abs path）；asset.json 内**只**写 basename，不写绝对路径（abs path 只用于 os.symlink target，不进 JSON）。 abs path 不外泄 |
| T-02-SC | Tampering (package install supply chain) | (无 pip install 任务) | N/A | 本 plan 零包安装（stdlib + 已装 jsonschema）；不需要 legitimacy checkpoint |
</threat_model>

<verification>
- 单元级：scripts/export_asset.py --help 退出 0；prompts.json 缺失退出非 0 + 含 "prompts.json 不存在"
- 集成级：对一个真实 output/《小江湖》*/ 调用 export_asset.py → asset.json 经 Draft202012Validator 校验零错误 + 4 个 canonical paths 存在 + ffprobe 确认 video.mp4 含 audio 流
- Pipeline 级：python3 run_pipeline.py --skip-detect --skip-separate --skip-transcribe（命中 cache）后产出 asset.json；--skip-export 跳过；--force 清 asset.json
- Invariant 级：`git diff --stat main -- detectors/ audio/ html/gen_timeline_html.py` 完全为空（additive-only）；`python3 spec/validate.py` 仍报 minimal 6/6 [valid]
- 所有 [N/5] 已升 [N/6]（14 处覆盖 + step_export 新增 [6/6]）
</verification>

<success_criteria>
- EXPORT-01 满足：run_pipeline.py 跑完后 output/<video-stem>/asset.json 存在，inline schema 校验通过，5 个数据 JSON + 4 个 canonical media 全部就位
- EXPORT-02 满足：asset.json 含 schema_version="1" + generator.{tool,version,generated_at}（自描述 manifest）；detectors/audio/html/gen_timeline_html.py 零修改（additive-only 经 git diff 验证）
- 为 EXPORT-03 铺路：canonical video.mp4 + stems/*.wav 路径就位（symlink resolves），供 02-02 的 Range check 探测
</success_criteria>

<output>
Create `.planning/phases/02-shot-timeline-exporter-producer/02-01-SUMMARY.md` when done
</output>
