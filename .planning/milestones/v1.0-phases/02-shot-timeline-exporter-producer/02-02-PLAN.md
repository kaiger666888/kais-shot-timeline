---
phase: 02-shot-timeline-exporter-producer
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - scripts/serve.py
  - scripts/check_range.py
autonomous: true
requirements: [EXPORT-03]
user_setup: []

must_haves:
  truths:
    - "scripts/serve.py 的 _Partial 类有 close() 方法，调用时关闭底层 file object"
    - "scripts/serve.py 在 206 success path 上不再 raise AttributeError: '_Partial' object has no attribute 'close'"
    - "50 次连续 Range 请求期间，serve.py 进程 FD 数稳定（不持续增长）"
    - "scripts/check_range.py 启动 serve.py → 发 Range: bytes=0-1023 请求 → 断言 206 + Content-Range: bytes 0-1023/<total> + Accept-Ranges: bytes + 1024 字节 body"
    - "check_range.py 默认对 output/ 下第一个含 video.mp4 的子目录自检通过（退出 0）"
    - "check_range.py 在 server 启动失败时退出非 0 并打印 actionable 错误"
  artifacts:
    - path: "scripts/serve.py"
      provides: "FD-leak 修复：_Partial 类含 close() 方法，构造函数把 f 存为 self._f"
      contains: "def close("
    - path: "scripts/check_range.py"
      provides: "Range-206 自检脚本：stdlib urllib + subprocess.Popen serve.py → 断言 206/Content-Range/Accept-Ranges"
      min_lines: 80
      contains: "Range"
  key_links:
    - from: "scripts/check_range.py"
      to: "scripts/serve.py"
      via: "subprocess.Popen([sys.executable, str(REPO / 'scripts' / 'serve.py'), asset_root, str(port)])"
      pattern: "serve\\.py"
    - from: "scripts/check_range.py"
      to: "output/<video-stem>/video.mp4"
      via: "urllib.request.Request(url, headers={'Range': 'bytes=0-1023'})"
      pattern: "Range.*bytes=0-1023"
    - from: "scripts/serve.py:_Partial.close"
      to: "underlying file object"
      via: "self._f.close()"
      pattern: "self\\._f\\.close"
---

<objective>
完成 Phase 2 的可服务性闭环：修复 scripts/serve.py 的 `_Partial` FD-leak bug（每 Range 请求泄漏一个 FD，因 `_Partial` 缺 close() 方法），并新增 scripts/check_range.py 作为 SC-3 「206 Partial Content responses observed」的机器可检证据。

Purpose: EXPORT-03 字面要求媒体文件经 Range-aware server 服务、消费端能 seek 不重传。206 本已工作，但 `_Partial.close()` 缺失是影响实际 seek 可靠性的真 bug（连续 seek 数百次后 EMFILE）。Range-206 自检脚本为 SC-3 提供自动化证据，并成为 Phase 4 回归 harness 的基础。

Output: scripts/serve.py（修改：_Partial class 重构）+ scripts/check_range.py（新文件）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shot-timeline-exporter-producer/02-CONTEXT.md
@.planning/phases/02-shot-timeline-exporter-producer/02-RESEARCH.md
@.planning/phases/02-shot-timeline-exporter-producer/02-PATTERNS.md
@.planning/phases/02-shot-timeline-exporter-producer/02-VALIDATION.md
@spec/SPEC.md
@scripts/serve.py
@scripts/export_asset.py

<interfaces>
<!-- 关键契约与现有签名，executor 直接使用，无需 codebase 探索 -->

From scripts/serve.py (修改目标):
- RangeRequestHandler(http.server.SimpleHTTPRequestHandler).send_head() 是 206/200/416 分派中枢
- 416 分支（line 59-61）：已 f.close() + return None —— 无需改
- NOT_FOUND 分支（line 37-39）：open() 失败进 except OSError，f 从未赋值 —— 无需改
- 200 分支：return f 直接 —— f 是真 file object，有原生 .close() —— 无需改
- 206 分支（line 66-78 设置 headers + line 80-96 构造 _Partial）—— BUG 在这：
  - 现 _Partial (line 85-96) 只定义 read()，捕获外层 closure var f / remaining / chunk_size
  - SimpleHTTPRequestHandler.do_GET 在 finally 块调用 f.close()（/usr/lib/python3.12/http/server.py:681）
  - f 是 _Partial 实例（return _Partial() if partial else f），_Partial 无 close() → AttributeError → exception propagates → underlying file object 永不 close → FD 泄漏
- 修复（02-PATTERNS.md + 02-RESEARCH Pitfall 2）：把 _Partial 改成 __init__(self, f, start, end, chunk_size) 把 f 存为 self._f，remaining/chunk_size 同样存为 self._*；read() 用 self 属性；新增 close() 方法调 self._f.close()。同时把 line 80-83 的 `if start > 0: f.seek(start)` + remaining/chunk_size 准备代码移进 __init__ 内
- return 语句改成 `return _Partial(f, start, end) if partial else f`

From scripts/export_asset.py (02-01 产出，本 plan 的 check_range 依赖其 canonical 媒体产出):
- 产出 output/<video-stem>/video.mp4 symlink
- 产出 output/<video-stem>/stems/{vocals,drums,other}.wav symlinks
- 这 4 个 canonical paths 是 check_range.py 默认探测目标

From spec/SPEC.md §6.2 (Range 206 契约):
- 必须响应 Range: bytes=start-end 请求头
- 返回 206 Partial Content + Content-Range: bytes start-end/total + Accept-Ranges: bytes
- scripts/serve.py 是 SPEC-03 reference impl

From 02-RESEARCH §Code Examples (check_range.py 完整骨架):
- REPO = Path(__file__).parent.parent.resolve()
- find_free_port() 用 socket bind 127.0.0.1:0 取 ephemeral port
- wait_ready(port, timeout) 用 socket.create_connection 轮询
- check(asset_root) 用 subprocess.Popen 启动 serve.py（非 run，长驻）
- urllib.request.Request(url, headers={"Range": "bytes=0-1023"}) + urlopen + 读 status/Content-Range/Accept-Ranges/body
- try/finally 保证 proc.terminate() + wait(timeout=2) + 失败 fallback proc.kill()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix scripts/serve.py _Partial FD-leak (add close() method + refactor to instance attrs)</name>
  <files>scripts/serve.py</files>
  <read_first>
    - scripts/serve.py (整文件 117 行 — 重点 line 19-96 RangeRequestHandler.send_head + line 85-96 当前 broken _Partial)
    - .planning/phases/02-shot-timeline-exporter-producer/02-RESEARCH.md (§Pitfall 2: _Partial.close() 真根因 + 修复示例；§Security Domain: FD-exhaustion DoS)
    - .planning/phases/02-shot-timeline-exporter-producer/02-PATTERNS.md (§scripts/serve.py _Partial.close() fix: minimal fix + defensive secondary fix)
    - .planning/phases/02-shot-timeline-exporter-producer/02-CONTEXT.md (§serve.py 加固范围: 只修 FD-leak；defer 0.0.0.0 unauth / bind-address)
    - /usr/lib/python3.12/http/server.py 的 SimpleHTTPRequestHandler.do_GET / do_HEAD finally 块（用 `python3 -c "import http.server, inspect; print(inspect.getsourcefile(http.server))"` 拿到路径后 grep finally 区段）— 确认 do_GET 调用 f.close() 的契约
  </read_first>
  <behavior>
    - 修改后 50 次连续 Range 请求期间，serve.py stderr 不再出现 `AttributeError: '_Partial' object has no attribute 'close'`
    - `/proc/<pid>/fd` 数量在 50 次连续 Range 请求前后稳定（不持续增长）
    - 206 响应保持原语义（status=206 + Content-Range: bytes 0-<end>/<total> + Accept-Ranges: bytes + body 长度正确）
    - 200/416/NOT_FOUND 分支行为不变（不破坏现有）
    - 200 分支仍直接 return f（真 file object，原生 close()）
  </behavior>
  <action>
    修改 `/data/workspace/kais-shot-timeline/scripts/serve.py` 的 `_Partial` 类（line 85-96 区域）+ 把外层 `if start > 0: f.seek(start)` / `remaining = end - start + 1` / `chunk_size = 64 * 1024` 三行准备代码（line 80-83）移进 `_Partial.__init__`。**不要**改 416/NOT_FOUND/200 分支（02-RESEARCH Pitfall 2 已确认它们正确）。

    新的 _Partial 类定义（替换 line 85-96 整块；class 仍内嵌在 send_head 方法体内，因为 closure 风格保留，但 closure var 全部消失）：

    关键签名（02-PATTERNS.md "Minimal fix" 模板）：
    - `class _Partial:` 内含三个方法
    - `def __init__(self, f, start, end, chunk_size=64 * 1024):` —— 把 f 存 `self._f = f`；算 `self._remaining = end - start + 1`；`self._chunk_size = chunk_size`；若 `start > 0` 则 `f.seek(start)`（注意：seek 仍在 __init__ 内执行，时机等价于原 line 80-81）
    - `def read(self, _n=None):` —— 逻辑与原 read 等价，但用 self 属性：`if self._remaining <= 0: return b""`；`n = min(self._chunk_size, self._remaining)`；`data = self._f.read(n)`；`self._remaining -= len(data)`；`return data`。**移除** `nonlocal remaining` 声明（不再需要 closure）
    - `def close(self):` —— 一行：`self._f.close()`。**这是修复的核心**（让 do_GET 的 `finally: f.close()` 不再 AttributeError）

    修改 line 80-83（外层准备代码）：把这三行 `if start > 0: f.seek(start)` / `remaining = end - start + 1` / `chunk_size = 64 * 1024` **删除**（它们已被移进 __init__）。

    修改 line 96（return 语句）：把 `return _Partial() if partial else f` 改成 `return _Partial(f, start, end) if partial else f`。

    Module docstring（line 1-11）不动；ThreadingHTTPServer / main 不动；class RangeRequestHandler 其他方法不动。

    **不要**改：
    - 416 分支（line 59-61）已 `f.close(); return None`，正确
    - NOT_FOUND 分支（line 37-39）open 失败进 except，f 未赋值，正确
    - 200 分支 return f 直接（真 file object 原生 close），正确
    - bind `0.0.0.0`（CONTEXT D-04 deferred，本 plan 不背）

    可选 belt-and-braces（02-RESEARCH §Pitfall 2 "Secondary fix"，建议加）：把 line 35-44 区域（`ctype = ... try: f = open(path, "rb") except OSError: ... try: total = os.fstat(f.fileno()).st_size except OSError: total = 0`）外再包一层 try/except，捕到任何意外 exception 时先 `f.close()` 再 raise。但 primary fix（_Partial.close()）已足够消灭已验证的 AttributeError。如时间紧可只做 primary fix。

    验证思路：原 line 80 `if start > 0: f.seek(start)` 的副作用（在构造 _Partial 之前 seek f）现在变成构造 _Partial 时 seek —— 时机等价，因为 seek 是在 `return _Partial(...)` 之前完成。200 分支不构造 _Partial，但其 `start == 0`（无 Range 头时 start=0, end=total-1），无需 seek，等价。
  </action>
  <verify>
    <automated>
    # 1. 语法
    python3 -c "import ast; ast.parse(open('scripts/serve.py').read())" && echo "syntax OK"

    # 2. _Partial.close() 存在
    grep -q "def close(" scripts/serve.py && echo "close() defined"
    grep -qE "self\._f\.close\(\)" scripts/serve.py && echo "close() closes underlying f"
    grep -q "def __init__(self, f, start, end" scripts/serve.py && echo "__init__ signature correct"

    # 3. closure var 已移除（_Partial.read 不再用 nonlocal）
    ! grep -q "nonlocal remaining" scripts/serve.py && echo "no nonlocal closure"

    # 4. 端到端：50 次 Range 请求，stderr 无 AttributeError + FD 数稳定
    TARGET=$(ls -d output/*/ | head -1)
    PORT=8765
    python3 scripts/serve.py "$TARGET" "$PORT" 2>/tmp/serve_stderr.log &
    SERVE_PID=$!
    # wait ready
    for i in {1..30}; do curl -s -o /dev/null "http://127.0.0.1:$PORT/video.mp4" 2>/dev/null && break || sleep 0.2; done

    FD_BEFORE=$(ls /proc/$SERVE_PID/fd 2>/dev/null | wc -l)
    for i in {1..50}; do
      curl -s -o /dev/null -H "Range: bytes=0-1023" "http://127.0.0.1:$PORT/video.mp4"
    done
    FD_AFTER=$(ls /proc/$SERVE_PID/fd 2>/dev/null | wc -l)
    kill $SERVE_PID 2>/dev/null; wait $SERVE_PID 2>/dev/null

    echo "FD before=$FD_BEFORE after=$FD_AFTER"
    ! grep -q "AttributeError" /tmp/serve_stderr.log && echo "no AttributeError in stderr"
    # FD drift 应小于 5（threading mix-in 会有少量 thread FD 波动，但不该持续增长）
    test $((FD_AFTER - FD_BEFORE)) -lt 5 && echo "FD stable"

    # 5. 206 语义保持
    python3 scripts/serve.py "$TARGET" 8766 2>/dev/null &
    SERVE_PID=$!
    for i in {1..30}; do curl -s -o /dev/null "http://127.0.0.1:8766/video.mp4" 2>/dev/null && break || sleep 0.2; done
    curl -s -D - -o /tmp/range_body -H "Range: bytes=0-1023" "http://127.0.0.1:8766/video.mp4" | head -10
    kill $SERVE_PID 2>/dev/null; wait $SERVE_PID 2>/dev/null
    echo "body bytes: $(wc -c < /tmp/range_body)"
    test "$(wc -c < /tmp/range_body)" = "1024" && echo "body 1024 bytes OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('scripts/serve.py').read())"` 退出码 0
    - `grep -q "def close(" scripts/serve.py` 通过（_Partial 新增 close 方法）
    - `grep -qE "self\._f\.close\(\)" scripts/serve.py` 通过（close 真的关底层 f）
    - `grep -q "def __init__(self, f, start, end" scripts/serve.py` 通过（构造函数签名正确）
    - `! grep -q "nonlocal remaining" scripts/serve.py` 通过（closure var 移除）
    - 对 output/《小江湖》*/ 启动 serve.py + 50 次 `curl -H 'Range: bytes=0-1023'`：stderr 不含字面量 `AttributeError`；FD 数增长 < 5
    - 单次 Range 请求仍返回 HTTP 206 + `Content-Range: bytes 0-1023/<total>` + `Accept-Ranges: bytes` + body 长度恰为 1024 字节
    - `git diff main -- scripts/serve.py` 仅含 `_Partial` 类与相邻准备代码的改动（未动 416/NOT_FOUND/200 分支、ThreadingHTTPServer、main、module docstring 主体）
  </acceptance_criteria>
  <done>
    serve.py 的 _Partial 类重构为 __init__(self, f, start, end, chunk_size) + read() + close() 三方法；f 由 closure var 变为 self._f；close() 调 self._f.close()。50 次连续 Range 请求 stderr 无 AttributeError，FD 数稳定，206 语义不变。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create scripts/check_range.py (Range-206 self-check, stdlib urllib + subprocess)</name>
  <files>scripts/check_range.py</files>
  <read_first>
    - .planning/phases/02-shot-timeline-exporter-producer/02-RESEARCH.md (§Code Examples "Verified Range-206 self-check" — 完整骨架代码 ~95 行可直接 lift)
    - .planning/phases/02-shot-timeline-exporter-producer/02-PATTERNS.md (§scripts/check_range.py: standalone verifier shape + subprocess + bracketed tag log + REPO constant)
    - scripts/serve.py (Task 1 修复后版本 — 确认 main(argv) 接受 [dir, port] positional args；Task 1 已修复 _Partial)
    - scripts/export_asset.py (02-01 产出 — 确认 default 探测路径 output/<video-stem>/video.mp4 是 symlink)
    - spec/validate.py:141-180 (standalone verifier 脚本形态：argparse + Chinese help + sys.exit(0/1) + bracketed log)
    - .planning/phases/02-shot-timeline-exporter-producer/02-CONTEXT.md (§Claude's Discretion: "Range-206 自检脚本的落地形式" — plan 阶段定；RESEARCH §Open Question 3 已决定：standalone scripts/check_range.py，不接 step_export)
  </read_first>
  <behavior>
    - 默认无参数：扫 output/，挑第一个含 video.mp4 的子目录作为 asset_root
    - 显式传 asset_root 位置参数：用该目录（必须含 video.mp4）
    - 找不到任何含 video.mp4 的目录：退出非 0，stderr 给 actionable 提示
    - 启动 serve.py 后用 Range: bytes=0-1023 GET，断言 4 条不变量：HTTP 206 + Content-Range 以 "bytes 0-1023/" 起始 + Accept-Ranges == "bytes" + body 长度恰为 1024
    - server 在 5 秒内未 ready：退出非 0，提示 port 或启动失败
    - 退出前 always terminate serve.py 子进程（包括 assertion fail / exception）
    - 不依赖 curl（用 stdlib urllib）
  </behavior>
  <action>
    创建 `/data/workspace/kais-shot-timeline/scripts/check_range.py`，自包含 argparse CLI，结构沿用 spec/validate.py:141-180 的 standalone verifier 形态（shebang + Chinese module docstring + helpers + `def main()` + `if __name__ == "__main__"`）。

    Module docstring（Chinese）：目的（启动 serve.py → Range 请求 → 断言 206/Content-Range/Accept-Ranges → tear down）、为何不用 curl（stdlib-only 可移植）、用法（`python3 scripts/check_range.py [asset_root]`）。

    Imports：argparse、http.client（仅用于常量 HTTPStatus if needed）、os、socket、subprocess、sys、time、urllib.request、from pathlib import Path。

    REPO 常量：`REPO = Path(__file__).parent.parent.resolve()`（scripts/ → repo root）。

    3 个 helper（全部 snake_case + Chinese docstring，02-RESEARCH §Code Examples 完整骨架可直接 lift）：

    1. `find_free_port() -> int` —— `with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s: s.bind(("127.0.0.1", 0)); return s.getsockname()[1]`。

    2. `wait_ready(port: int, timeout: float = 5.0) -> bool` —— deadline loop，每 0.1s 用 `socket.create_connection(("127.0.0.1", port), timeout=0.2)` 试连，连上 return True；deadline 超过 return False。

    3. `check(asset_root: str) -> int` —— 主逻辑，返回 0/1：
       - pre-check：`video_path = os.path.join(asset_root, "video.mp4")`；若不存在，`print(f"[check-range] no video.mp4 in {asset_root} — nothing to probe")` + return 1
       - `port = find_free_port()`
       - `proc = subprocess.Popen([sys.executable, str(REPO / "scripts" / "serve.py"), asset_root, str(port)], stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)`
       - `try:` 块：
         - `if not wait_ready(port): print(f"[check-range] FAIL: server did not start on port {port}"); return 1`
         - `url = f"http://127.0.0.1:{port}/video.mp4"`
         - `req = urllib.request.Request(url, headers={"Range": "bytes=0-1023"})`
         - `with urllib.request.urlopen(req, timeout=5) as resp:` 读 `status = resp.status`、`content_range = resp.headers.get("Content-Range")`、`accept_ranges = resp.headers.get("Accept-Ranges")`、`body = resp.read()`
         - 断言 4 条不变量（每条失败设 ok=False 并打印 FAIL 行）：
           - `status != 206` → `print(f"[check-range] FAIL: expected 206, got {status}")`
           - `not content_range or not content_range.startswith("bytes 0-1023/")` → `print(f"[check-range] FAIL: bad Content-Range: {content_range!r}")`
           - `accept_ranges != "bytes"` → `print(f"[check-range] FAIL: bad Accept-Ranges: {accept_ranges!r}")`
           - `len(body) != 1024` → `print(f"[check-range] FAIL: expected 1024-byte body, got {len(body)}")`
         - 若 ok：`print(f"[check-range] OK: 206 + Content-Range={content_range} + Accept-Ranges=bytes + 1024-byte body")`
         - `return 0 if ok else 1`
       - `finally:` 块：`proc.terminate()`；`try: proc.wait(timeout=2) except subprocess.TimeoutExpired: proc.kill()`
       - 用 try/finally 而非 try/except —— 任何异常（含 KeyboardInterrupt）都要保证 proc 被 terminate（沿用 audio/transcribe.py:150-155 temp wav cleanup 惯例）

    `main()`：argparse description `"Range-206 自检：启动 serve.py → Range 请求 → assert 206/Content-Range/Accept-Ranges"`；positional `asset_root`（nargs="?", default=None, help="asset 目录（含 video.mp4）；缺省时自动扫 output/ 下第一个含 video.mp4 的子目录"）；args.asset_root 为 None 时，扫 `REPO / "output"` 下 sorted children，找到第一个含 video.mp4 的子目录赋给 args.asset_root；若找不到，`sys.exit("[check-range] no asset_root given and no output/*/video.mp4 found — run pipeline first or pass asset_root positional arg")`。最后 `sys.exit(check(args.asset_root))`。

    **不要**接入 run_pipeline.py 的 step_export（02-RESEARCH §Open Question 3：standalone，每次 pipeline run 自动跑会带来 port 并发风险 + muddies 架构）。check_range 是 verification 工具，属 `/gsd:verify-work` 与 Phase 4 回归 harness 的范畴。
  </action>
  <verify>
    <automated>
    # 1. 语法 + import 健康
    python3 -c "import ast; ast.parse(open('scripts/check_range.py').read())" && echo "syntax OK"
    python3 scripts/check_range.py --help | head -5

    # 2. source 含关键 identifier
    grep -q "urllib.request" scripts/check_range.py && echo "urllib used"
    grep -qE 'Range.*bytes=0-1023' scripts/check_range.py && echo "Range header set"
    grep -q "scripts/serve.py" scripts/check_range.py && echo "boots serve.py"
    grep -q "proc.terminate" scripts/check_range.py && echo "tear-down on finally"
    ! grep -qE "^import curl|subprocess.run.*curl" scripts/check_range.py && echo "no curl dependency"

    # 3. 默认路径自检（02-01 产出后 output/<video-stem>/video.mp4 应存在）
    python3 scripts/check_range.py
    echo "exit=$?"

    # 4. 显式 asset_root 参数
    TARGET=$(ls -d output/*/ | head -1)
    python3 scripts/check_range.py "$TARGET"
    echo "exit=$?"

    # 5. 不存在的目录 → fails clean
    python3 scripts/check_range.py /tmp/nonexistent_dir 2>&1 | grep -q "no video.mp4"
    echo "exit=$?"
    </automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('scripts/check_range.py').read())"` 退出码 0
    - `grep -q "urllib.request" scripts/check_range.py` 通过（stdlib-only）
    - `grep -qE 'Range.*bytes=0-1023' scripts/check_range.py` 通过
    - `grep -q "scripts/serve.py" scripts/check_range.py` 通过
    - `grep -q "proc.terminate" scripts/check_range.py` 通过（tear-down）
    - `! grep -qE "subprocess.run.*curl|^import curl" scripts/check_range.py` 通过（无 curl 依赖）
    - `python3 scripts/check_range.py --help` 退出码 0
    - `python3 scripts/check_range.py`（默认扫 output/）退出码 0，stdout 含字面量 `[check-range] OK: 206`
    - `python3 scripts/check_range.py /tmp/nonexistent_dir` 退出码非 0，stderr/stdout 含 `no video.mp4` 字面量
    - 退出后 serve.py 子进程不残留（`pgrep -f "scripts/serve.py"` 仅返回与本测试无关的进程或无匹配）
  </acceptance_criteria>
  <done>
    scripts/check_range.py 落地，默认对 output/ 下第一个含 video.mp4 的子目录跑 Range-206 自检，断言 4 条不变量（206 + Content-Range + Accept-Ranges + 1024 字节 body），退出码反映 pass/fail；server 子进程 always torn down（含异常路径）；无 curl 依赖；不接入 step_export（standalone）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → scripts/serve.py | 任意 HTTP client 发 Range 请求；offline 单机 dev 工具，信任度低（serve.py binds 0.0.0.0）|
| scripts/check_range.py → scripts/serve.py (子进程) | check_range 自己启动的 serve.py 子进程；信任度高（自己 spawn）|
| scripts/serve.py → filesystem (asset root) | serve.py chdir 到 asset_root 后 read-only 服务静态文件；symlink 跨界到 asset_root 外（如 video.mp4 → 原始视频 abs path）|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service (FD exhaustion via sustained Range load) | scripts/serve.py _Partial（修复前） | mitigate | _Partial 类新增 close() 方法，调 self._f.close()；do_GET 的 `finally: f.close()` 不再 AttributeError；FD 在每次 Range 请求后正确释放，50 次连续请求 FD 数稳定（02-RESEARCH §Pitfall 2 根因 + 修复） |
| T-02-04 | Information Disclosure (serve.py binds 0.0.0.0 unauth) | scripts/serve.py main() | accept | 单机离线 dev 工具，CONTEXT D-04 明确 defer 到 v2.0；本 plan 不修（scope creep）；user 可用 `iptables` / `ssh -L` 自行限制；threat likelihood 低（攻击者需先到 LAN） |
| T-02-07 | Tampering (symlink crossing asset root boundary) | scripts/serve.py 跟随 video.mp4 → 原始视频 abs path | accept | 02-RESEARCH §Pitfall 1 决策：video.mp4 必须指向原始视频（含 audio），不能指向 h264.mp4（`-an` 去 audio）；target 是 abs path，在 asset_root 外。这是有意 trade-off（asset 目录非 portably cp-able），由 CONTEXT "zip/tar 分发 v2.0" 决策承担。check_range.py 验证 serving 行为正确 |
| T-02-08 | Denial of Service (check_range.py 占用端口失败导致 false negative) | scripts/check_range.py find_free_port | mitigate | find_free_port 用 socket bind 0.0.0.0:0 取内核分配的 ephemeral port；wait_ready 5 秒 timeout；timeout 后 actionable 错误信息；try/finally 保证 serve.py 被 terminate |
| T-02-SC | Tampering (package install supply chain) | (无 pip install 任务) | N/A | 本 plan 零包安装（纯 stdlib）；不需要 legitimacy checkpoint |
</threat_model>

<verification>
- 单元级：scripts/serve.py 语法 OK，_Partial 类含 close() 方法，不再有 nonlocal closure；scripts/check_range.py --help 退出 0
- 行为级：50 次连续 Range 请求，serve.py stderr 无 AttributeError，FD drift < 5
- 不变量级：单次 Range 请求返回 206 + Content-Range: bytes 0-1023/<total> + Accept-Ranges: bytes + body 长度恰为 1024 字节
- 集成级：`python3 scripts/check_range.py`（默认）退出 0，stdout 含 `[check-range] OK`
- 错误路径级：check_range.py 对不存在目录退出非 0，含 actionable 提示
- Phase gate（02-VALIDATION.md §Sampling Rate）：`python3 spec/validate.py --strict-smoke && python3 scripts/check_range.py` 全绿；`git diff main -- scripts/serve.py` 仅含 _Partial 改动
</verification>

<success_criteria>
- EXPORT-03 满足：媒体文件经 scripts/serve.py 服务，消费端可 Range seek 不重传；206 Partial Content 响应经 standalone 自检脚本机器可观察（status + Content-Range + Accept-Ranges + body-size 4 条不变量全 pass）
- FD-leak bug 修复：50 次连续 Range 请求 FD 数稳定，stderr 无 AttributeError
- 不破坏 02-01 产出：check_range.py 对 02-01 导出的 canonical 媒体（video.mp4 + stems/*.wav symlinks）默认探测成功
</success_criteria>

<output>
Create `.planning/phases/02-shot-timeline-exporter-producer/02-02-SUMMARY.md` when done
</output>
