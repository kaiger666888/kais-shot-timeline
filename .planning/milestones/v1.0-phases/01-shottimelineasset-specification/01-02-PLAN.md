---
phase: 01-shottimelineasset-specification
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - spec/SPEC.md
autonomous: false
requirements: [SPEC-01, SPEC-02, SPEC-03, SPEC-04]
user_setup: []

must_haves:
  truths:
    - "A single human-readable authoritative contract document exists at spec/SPEC.md"
    - "A reader can identify all 5 canonical data shapes and their field-level types from SPEC.md alone"
    - "A reader can state where schema_version lives and what to do on an unknown/newer version (graceful degrade, not reject)"
    - "A reader can state the canonical media path/naming convention AND the Range-aware HTTP 206 serving requirement"
    - "A reader can state what asset.json (the manifest) carries and why the asset is self-describing"
    - "SPEC.md references each schema file by filename so a reader can jump from prose to machine-checkable contract"
    - "A human reviewer confirms the document is clear enough that both producer and consumer implementers can work from it without tribal knowledge"
  artifacts:
    - path: "spec/SPEC.md"
      provides: "Authoritative human-readable ShotTimelineAsset contract document"
      contains: "schema_version"
      min_lines: 200
  key_links:
    - from: "spec/SPEC.md"
      to: "spec/schemas/asset.schema.json"
      via: "filename reference in the manifest section"
      pattern: "asset\\.schema\\.json"
    - from: "spec/SPEC.md"
      to: "spec/schemas/{shots,audio_analysis,transcript,frames,prompts}.schema.json"
      via: "filename references in the data-shapes section"
      pattern: "schema\\.json"
    - from: "spec/SPEC.md"
      to: "scripts/serve.py"
      via: "Range-aware HTTP 206 server reference"
      pattern: "serve\\.py|Range|206"
---

<objective>
Write the human-readable authoritative ShotTimelineAsset contract document (spec/SPEC.md). This is the overview that a producer or consumer implementer reads first; the JSON Schema files from Plan 01 are the machine-checkable source of truth referenced from this document.

Purpose: Tribal knowledge is the failure mode this milestone exists to kill. The 6 schema files from Plan 01 are machine-checkable but unreadable as a whole; SPEC.md is what makes the contract navigable — the 5 canonical shapes, the version rule, the media+Range requirement, and the self-describing manifest, all in one document a new contributor can absorb in 15 minutes.

Output: `spec/SPEC.md` — the authoritative prose contract covering all 4 phase success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-shottimelineasset-specification/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md

<!-- Plan 01 SUMMARY is required reading: it confirms the final schema filenames, $id values, -->
<!-- the exact graceful-degrade wording embedded in asset.schema.json, and the canonical media -->
<!-- path patterns. SPEC.md must quote these consistently — drift between SPEC.md and the schemas -->
<!-- is the exact failure mode the human-verify checkpoint in Task 2 exists to catch. -->
@.planning/phases/01-shottimelineasset-specification/01-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 01 that SPEC.md must reference by exact filename/path. -->
<!-- Read these from disk before writing SPEC.md so prose matches schema reality. -->

Schema files (Plan 01 output):
- spec/schemas/shots.schema.json — top-level array of {id:int, start_sec, end_sec, duration}
- spec/schemas/audio_analysis.schema.json — {episode, duration, stems, shots[], type_distribution}; dominant_type ∈ {dialogue, bgm, mixed, sfx}
- spec/schemas/transcript.schema.json — {backend, model, language, segments[{start,end,text}], text, duration, source}; backend ∈ {faster-whisper, openai-whisper}
- spec/schemas/frames.schema.json — array of {id, first_frame, last_frame}; values are data:image/jpeg;base64, URIs
- spec/schemas/prompts.schema.json — array of {shot_id, start_sec, end_sec, duration, subject, action, camera, scene, lighting, style, prompt_text}
- spec/schemas/asset.schema.json — manifest: schema_version (semver-lite), asset_type="shottimeline", source, generator, data{}, media{video, stems{vocals, drums, other}}

Locked decisions from CONTEXT.md (MUST be honored verbatim):
- D-01: JSON Schema draft 2020-12 is the machine-checkable authority; SPEC.md is the human-readable overview
- D-02: schema_version is a top-level manifest field, semver-lite ("1" / "1.1"). Consumer graceful-degrades on unknown/newer versions (ignore unknown fields, render known parts, warn — do NOT reject). New field = minor bump; breaking change = major bump.
- D-03: Media referenced by relative paths inside the asset directory with canonical naming `video.mp4` + `stems/vocals.wav` / `stems/drums.wav` / `stems/other.wav` (bass NOT in the canonical set — consumer frontend only uses 3 stems). Consumers access media via Range-aware HTTP 206 (Partial Content); the existing `scripts/serve.py` is the reference implementation.
- D-04: Single `asset.json` entry point. Carries schema_version, source (origin video), generator (tool name + version), 5-data-file inventory, media inventory. Frames stay inline base64 (per existing frames.json pattern — single-file self-contained).

External references:
- scripts/serve.py — Range-aware static server (must be cited as the SPEC-03 reference implementation)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write spec/SPEC.md — authoritative human-readable ShotTimelineAsset contract</name>
  <files>spec/SPEC.md</files>
  <read_first>
    - .planning/phases/01-shottimelineasset-specification/01-CONTEXT.md (locked decisions D-01..D-04 — non-negotiable)
    - .planning/phases/01-shottimelineasset-specification/01-01-SUMMARY.md (final schema filenames, $id values, exact graceful-degrade wording)
    - spec/schemas/*.schema.json (the 6 actual schema files — quote field names + enums verbatim, do not paraphrase)
    - scripts/serve.py (Range-aware 206 server — cited as SPEC-03 reference implementation)
    - .planning/codebase/CONVENTIONS.md (Chinese-commented / bilingual style is acceptable)
  </read_first>
  <action>
    Write a single Markdown file at `spec/SPEC.md`. Document language: bilingual is acceptable (Chinese narrative with English technical terms, matching the repo's existing convention); structure must be navigable via Markdown headings. Minimum 200 lines. Cover EXACTLY these sections in this order:

    1. **Title + Status** — `# ShotTimelineAsset — Contract Specification`. Status line: `Version: 1 (schema_version "1") | Owner: kais-shot-timeline | Consumers: @kais/infinite-canvas`. One-paragraph purpose statement (the contract that lets two repos interoperate without tribal knowledge).

    2. **Authority & Schema Layout (D-01)** — State that the JSON Schema files under `spec/schemas/` are the machine-checkable authority; this document is the human-readable overview. List the 6 schema files by filename with a one-line summary each. State that TS types can be generated from these schemas downstream (consumer side, Phase 3).

    3. **Asset Directory Layout (D-03, D-04)** — ASCII tree of a canonical asset directory:
       ```
       <asset-root>/
       ├── asset.json              # manifest (entry point)
       ├── shots.json
       ├── audio_analysis.json
       ├── transcript.json
       ├── frames.json             # base64 first/last frames inline (D-04)
       ├── prompts.json
       ├── video.mp4               # canonical video (D-03)
       └── stems/
           ├── vocals.wav          # canonical (D-03)
           ├── drums.wav
           └── other.wav
       ```
       Note explicitly: bass.wav is NOT part of the canonical set (consumer only uses 3 stems).

    4. **The Manifest: asset.json (SPEC-04, D-02, D-04)** — Field table:
       | Field | Type | Required | Description |
       With rows for: `schema_version` (string, semver-lite "1" or "1.1"), `asset_type` (const "shottimeline"), `source.video_filename` (string), `source.duration_sec` (number), `generator.tool` (string), `generator.version` (string), `generator.generated_at` (ISO-8601 string), `data.{shots,audio_analysis,transcript,frames,prompts}` (each a relative-path string ending in .json), `media.video` (string ending in video.mp4), `media.stems.{vocals,drums,other}` (each a relative-path string ending in .wav). End with: "Reference schema: `spec/schemas/asset.schema.json`".

    5. **Schema Versioning & Graceful Degrade (SPEC-02, D-02)** — Quote the rule verbatim from asset.schema.json's schema_version description. Explain:
       - Why semver-lite (not full semver): the asset is a single producer's output, not a published library; major.minor suffices.
       - The consumer's obligation on unknown/newer version: ignore unknown fields, render known parts, emit a warning — do NOT reject the asset.
       - Evolution rules: adding a field = minor bump (old consumers graceful-degrade silently); removing/rening/retyping a field = major bump (document the migration in this section, with a dated "Changelog" subsection).
       - Include an empty `### Changelog` subsection placeholder (first entry will be "1 — initial contract" dated 2026-07-20).

    6. **The 5 Canonical Data Shapes (SPEC-01)** — One subsection per shape (### Shots, ### Audio Analysis, ### Transcript, ### Frames, ### Prompts). In EACH subsection include:
       - The producer script that emits it (e.g. "Producer: `audio/separate_stems.py:analyze_shots`").
       - A field-level type table (field | type | required | notes) — types and names MUST match the schema in spec/schemas/<shape>.schema.json exactly.
       - For shapes with enums (audio_analysis.dominant_type, transcript.backend), list the exact enum values.
       - A tiny JSON snippet (2 items / 1 object) showing the shape.
       - End with: "Reference schema: `spec/schemas/<shape>.schema.json`".

    7. **Media References & Range-aware Serving (SPEC-03, D-03)** — Three subsections:
       - **Path & naming convention** — Canonical relative paths (no parent traversal, no absolute paths, no drive letters). Schema enforcement pointer: asset.schema.json media path patterns.
       - **Range-aware HTTP 206 requirement** — State that consumers depend on HTTP Range requests (Partial Content, status 206) for seek playback of both video.mp4 and the stem .wav files. Cite `scripts/serve.py` as the reference implementation (`python3 scripts/serve.py <asset-root> 8765`). Note the known Ubuntu/Debian quirk that `python3 -m http.server` does NOT honor Range headers — that is why scripts/serve.py exists.
       - **Why media is external, not inlined** — Files are too large to inline (video is multi-100-MB, stems are multi-10-MB). Frames are the exception: first/last JPEGs per shot stay inline as base64 in frames.json (D-04) so the JSON set is self-contained for browsing.

    8. **Validation** — How to check an asset against the contract: `python3 spec/validate.py`. Mention the `--strict-smoke` flag. Point to spec/fixtures/minimal/ as the canonical example asset.

    9. **Out of Scope (for v1.0)** — Bullet list: native canvas renderer, canvas-side Range media service, custom renderer / contract bump, multi-user / web UI for the producer, training-pipeline integration. (Mirrors PROJECT.md Out of Scope.)

    10. **References** — Bulleted: PROJECT.md, ROADMAP.md, REQUIREMENTS.md (SPEC-01..04), the 6 schema files, scripts/serve.py, the 5 producer scripts (detectors/detect_v3b.py, audio/separate_stems.py, audio/transcribe.py, html/gen_timeline_html.py for frames, prompts generator TBD).

    Do NOT modify any schema file, fixture, or producer code in this task. SPEC.md references them as-is.
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
p = Path('spec/SPEC.md')
assert p.exists() and p.stat().st_size > 0, 'SPEC.md missing or empty'
text = p.read_text(encoding='utf-8')
assert 'ShotTimelineAsset' in text, 'title missing'
assert 'schema_version' in text and 'graceful' in text.lower(), 'SPEC-02 content missing'
assert 'video.mp4' in text and 'stems/vocals.wav' in text, 'SPEC-03 canonical paths missing'
assert '206' in text and 'serve.py' in text, 'Range-aware 206 / serve.py reference missing'
assert 'asset.json' in text and 'manifest' in text.lower(), 'SPEC-04 manifest section missing'
for s in ['shots.schema.json','audio_analysis.schema.json','transcript.schema.json','frames.schema.json','prompts.schema.json','asset.schema.json']:
    assert s in text, f'missing schema reference: {s}'
for shape_sec in ['### Shots','### Audio Analysis' ,'### Transcript','### Frames','### Prompts']:
    assert shape_sec in text, f'missing shape section: {shape_sec}'
lines = text.splitlines()
assert len(lines) >= 200, f'too short: {len(lines)} lines'
print(f'OK: SPEC.md {len(lines)} lines, covers all 4 success criteria, references all 6 schemas')
"</automated>
  </verify>
  <done>
    spec/SPEC.md exists, ≥ 200 lines, contains all 10 required sections, references all 6 schema files by filename, covers all 4 phase success criteria (5 data shapes, schema_version + graceful degrade, canonical media paths + Range 206 + serve.py, manifest). Field names/types in the prose tables exactly match the schemas (no paraphrased field names). No other files modified.
  </done>
  <acceptance_criteria>
    - spec/SPEC.md exists at the exact path.
    - All 10 sections from <action> are present and in order.
    - Every canonical media path is mentioned (video.mp4, stems/vocals.wav, stems/drums.wav, stems/other.wav).
    - bass.wav is explicitly noted as NOT in the canonical set.
    - The graceful-degrade rule appears verbatim or near-verbatim (matches asset.schema.json's schema_version description from Plan 01's SUMMARY).
    - `scripts/serve.py` is cited as the SPEC-03 reference implementation, and the `python3 -m http.server` Range-header quirk is noted.
    - Each of the 5 shape subsections names its producer script and references its schema file.
    - dominant_type and backend enums are listed exactly as defined in the schemas.
    - A `### Changelog` subsection exists with the initial "1 — initial contract" entry dated 2026-07-20.
    - No producer code, schema files, or fixtures were modified.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review — confirm SPEC.md is implementable without tribal knowledge</name>
  <files>spec/SPEC.md</files>
  <read_first>
    - spec/SPEC.md (just produced by Task 1)
    - .planning/phases/01-shottimelineasset-specification/01-CONTEXT.md (locked decisions — verify the spec honors them)
    - spec/schemas/asset.schema.json (verify SPEC.md quotes the schema_version rule consistently)
  </read_first>
  <action>
    Human reviews spec/SPEC.md against the 6 schemas and the 4 phase success criteria. This is a verification checkpoint — Claude has already produced all artifacts (6 schemas in Plan 01 + SPEC.md in Plan 02 Task 1 + validate.py exit 0). The human's job is to confirm the contract is implementable without tribal knowledge and that SPEC.md does not drift from the machine-checkable schemas. Detailed review steps in <how-to-verify>; approval signal in <resume-signal>. No new files are produced unless issues are found (in which case the executor revises spec/SPEC.md and re-runs this checkpoint).
  </action>
  <what-built>
    A complete ShotTimelineAsset contract: 6 JSON Schema files (Plan 01) + this prose SPEC.md (Plan 02 Task 1). Together they cover all 4 phase success criteria. The validation runner confirms schemas are coherent against a hand-built fixture and a real producer fixture.
  </what-built>
  <how-to-verify>
    Open `spec/SPEC.md` and verify each of the following:
    1. **5 canonical shapes (SPEC-01)** — Sections for Shots / Audio Analysis / Transcript / Frames / Prompts each have a field-level type table. Cross-check 2 fields per shape against the matching `spec/schemas/<shape>.schema.json` — names and types must match exactly (no paraphrasing).
    2. **Version rule (SPEC-02)** — The schema_version field is described as semver-lite, and the graceful-degrade rule is stated unambiguously: "consumer ignores unknown fields, renders known parts, warns — does NOT reject". Confirm this matches what asset.schema.json's schema_version description says (open spec/schemas/asset.schema.json and compare).
    3. **Media + Range (SPEC-03)** — Canonical paths `video.mp4` and `stems/{vocals,drums,other}.wav` are documented; bass.wav is explicitly excluded. The HTTP 206 Range requirement is stated, and `scripts/serve.py` is named as the reference server.
    4. **Self-describing manifest (SPEC-04)** — asset.json manifest fields are listed (schema_version, asset_type, source, generator, data inventory, media inventory) and the document explains why the asset is self-describing (consumer needs no external docs).
    5. **Tribal-knowledge kill check** — Read the document fresh. Could a new contributor implement EITHER the producer exporter (Phase 2) OR the canvas consumer (Phase 3) using only this document + the referenced schemas, without asking anyone a question? If yes, approve. If no, list the specific gaps.
    6. Run `python3 spec/validate.py` once and confirm exit 0 with 6 `[valid]` lines (sanity check that Plan 01's deliverables still hold).
  </how-to-verify>
  <resume-signal>Type "approved" to close out Phase 1, or list specific issues in SPEC.md (or schema drift between SPEC.md and a schema file) for the executor to fix.</resume-signal>
  <verify>
    <automated>python3 spec/validate.py</automated>
    <human-check>
      Reviewer confirms all 6 checks in <how-to-verify> pass and types "approved" (resume signal). Any "no" answer blocks close-out and triggers revision of spec/SPEC.md.
    </human-check>
  </verify>
  <done>
    Human reviewer has typed "approved" (or listed specific issues that were resolved and re-verified). spec/SPEC.md is confirmed as the authoritative contract document sufficient for both producer (Phase 2) and consumer (Phase 3) implementation without tribal knowledge. `python3 spec/validate.py` still exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Document → Implementer | A new contributor reads SPEC.md and implements producer/consumer code from it |
| SPEC.md ↔ Schemas | The prose and the machine-checkable schemas must not drift — drift is the silent failure mode this checkpoint exists to catch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Repudiation | SPEC.md and a schema disagree on a field name/type, causing silent interop bugs | mitigate | Task 1 <verify> greps for schema filenames + canonical paths; Task 2 human-verify explicitly cross-checks SPEC.md against asset.schema.json's schema_version description |
| T-01-06 | Information Disclosure | SPEC.md omits the graceful-degrade rule, leading a consumer implementer to reject unknown versions | mitigate | Task 1 <action> Section 5 requires the rule verbatim; Task 1 <verify> greps for "schema_version" + "graceful"; Task 2 human-verify step 2 re-checks |
| T-01-07 | Tampering | SPEC.md documents a non-canonical media path (e.g. mentions bass.wav as canonical) | mitigate | Task 1 <verify> greps for the 3 canonical stem names; Task 2 human-verify step 3 |
| T-01-SC | Supply Chain | npm/pip/cargo package installs | accept (n/a) | This plan writes one Markdown file. No package installs. |
</threat_model>

<verification>
- spec/SPEC.md exists and passes the Task 1 automated structural grep (≥200 lines, all 6 schema files referenced, all canonical media paths present, graceful-degrade rule present, serve.py + 206 present).
- The Task 2 human reviewer confirms SPEC.md is implementable without tribal knowledge and that SPEC.md ↔ schemas are consistent.
- `python3 spec/validate.py` still exits 0 (Plan 01 deliverables intact).
</verification>

<success_criteria>
- A single authoritative contract document exists at spec/SPEC.md.
- All 4 phase success criteria are covered in prose AND cross-referenced to the matching schema file.
- A human reviewer (the user) has confirmed the document is sufficient for both producer (Phase 2) and consumer (Phase 3) implementation without tribal knowledge.
</success_criteria>

<output>
Create `.planning/phases/01-shottimelineasset-specification/01-02-SUMMARY.md` when done. The SUMMARY must include:
- The 10 section headings of SPEC.md
- The exact graceful-degrade rule wording used in SPEC.md (and confirmation it matches asset.schema.json)
- The human-review outcome (approved / issues raised + resolution)
- Confirmation that `python3 spec/validate.py` still exits 0
- Any notes the user wants carried forward to Phase 2 (exporter) or Phase 3 (consumer)
</output>
