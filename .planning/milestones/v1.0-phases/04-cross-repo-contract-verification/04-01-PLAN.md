---
phase: 04-cross-repo-contract-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_contract.py
autonomous: true
requirements: [VERIFY-02]
tags: [contract, regression, jsonschema, cross-repo]

must_haves:
  truths:
    - "运行 `python3 scripts/verify_contract.py --mode=producer` 对真实 ep01 asset 校验通过 (asset.json + 5 data shapes 全部 schema-valid),exit 0"
    - "运行 `python3 scripts/verify_contract.py --mode=consumer` 通过 shell-out 调起 Phase 3 的 verify-canvas-shot-timeline.ts (17 asserts 全绿),exit 0"
    - "运行 `PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer` 注入故意损坏的 asset (schema_version='v1'),harness 检测到 drift 并 exit 非 0 (meta-test 证明 fail-loud)"
    - "Producer schema 漂移 (asset.json 不符合任一 schema) 导致 mode=producer exit 1 并打印 actionable 中文错误"
    - "Consumer importer 漂移 (verify-canvas-shot-timeline.ts 失败) 导致 mode=consumer exit 1"
    - "CANVAS_CONSUMER_PATH 指向非 git worktree 时,consumer mode 以 actionable 中文错误 sys.exit (不静默通过)"
  artifacts:
    - path: "scripts/verify_contract.py"
      provides: "Canonical contract verification harness —— producer + consumer + self-test 三 mode (e2e mode 由 Plan 04-02 补完)"
      contains: ["run_producer_check", "run_consumer_check", "run_self_test", "validate_asset_json", "find_free_port", "main"]
      min_lines: 200
  key_links:
    - from: "scripts/verify_contract.py::run_producer_check"
      to: "spec/schemas/{asset,shots,audio_analysis,transcript,frames,prompts}.schema.json"
      via: "inline jsonschema.Draft202012Validator (绝不 subprocess 到 spec/validate.py —— 其 SMOKE_SHAPES L49 排除 asset shape)"
      pattern: "Draft202012Validator"
    - from: "scripts/verify_contract.py::run_consumer_check"
      to: "/data/workspace/kst-canvas-consumer/scripts/verify-canvas-shot-timeline.ts"
      via: "subprocess.run([npx, tsx, scripts/verify-canvas-shot-timeline.ts], cwd=CANVAS_CONSUMER_PATH)"
      pattern: "verify-canvas-shot-timeline"
    - from: "scripts/verify_contract.py::main"
      to: "环境变量 CANVAS_CONSUMER_PATH"
      via: "argparse default = os.environ.get('CANVAS_CONSUMER_PATH', '/data/workspace/kst-canvas-consumer')"
      pattern: "CANVAS_CONSUMER_PATH"
---

<objective>
Build the canonical contract verification harness `scripts/verify_contract.py` with three modes (producer / consumer / self-test), covering VERIFY-02 (regression protection against producer↔consumer drift). This plan delivers the harness core + the two "unit-level" modes + a deliberate-drift self-test. Plan 04-02 will add the e2e mode (VERIFY-01).

Purpose: Milestone v1.0 的核心价值是「两仓库独立演进,契约对齐有回归保护」。这个 harness 是回归网 —— 抓住任一侧的 silent schema 漂移。没有它,Phase 1-3 的契约工作没有自动化 guard。

Output: `scripts/verify_contract.py` (单文件,~250-300 行),含 producer mode (inline jsonschema 校验 6 个 schema) + consumer mode (shell-out 到 Phase 3 verify-canvas-shot-timeline.ts) + self-test mode (PHASE4_SELF_TEST=1 注入故意漂移)。本 plan 不实现 e2e mode —— 它在 04-02 落地。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cross-repo-contract-verification/04-CONTEXT.md
@.planning/phases/04-cross-repo-contract-verification/04-RESEARCH.md
@.planning/phases/04-cross-repo-contract-verification/04-PATTERNS.md
@.planning/phases/04-cross-repo-contract-verification/04-VALIDATION.md
@spec/schemas/asset.schema.json

<interfaces>
<!-- 关键 contract/signature —— executor 直接用,不需要 codebase 探索。-->

From scripts/check_range.py (server-lifecycle + standalone-script 模板):
```python
def find_free_port() -> int:
    """让内核分配一个空闲的 ephemeral 端口。"""
    with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
        s.bind(("127.0.0.1", 0))
        return s.getsockname()[1]

# standalone skeleton: 模块 docstring → imports → REPO = Path(__file__).parent.parent.resolve()
# → helpers → def main() argparse → sys.exit → if __name__ == "__main__": main()
```

From spec/validate.py (Draft202012Validator 模板):
```python
from jsonschema import Draft202012Validator
# L49: SMOKE_SHAPES = ["shots", "audio_analysis", "transcript", "frames", "prompts"]
#   ↑ 显式排除 "asset" —— 所以 producer mode 不能 shell out 到 validate.py 校验 asset
def load_validator(shape): return Draft202012Validator(json.load(open(SCHEMAS_DIR / f"{shape}.schema.json")))
errors = sorted(validator.iter_errors(instance), key=lambda e: list(e.absolute_path))
```

From scripts/export_asset.py:106-127 (inline asset 校验,可逐字 copy):
```python
def validate_asset_json(asset_dict):
    from jsonschema import Draft202012Validator
    schema = json.load(open(REPO / "spec" / "schemas" / "asset.schema.json"))
    errors = sorted(Draft202012Validator(schema).iter_errors(asset_dict),
                    key=lambda e: list(e.absolute_path))
```

Cross-repo (read-only —— shell-out target):
- `/data/workspace/kst-canvas-consumer/scripts/verify-canvas-shot-timeline.ts` —— Phase 3 verify script (17 asserts A-F)。运行方式: `npx tsx scripts/verify-canvas-shot-timeline.ts` (注意 cwd=worktree 根)。

Schema contract (本 plan 的 producer mode 校验依据):
- `spec/schemas/asset.schema.json` —— schema_version pattern `^(0|[1-9]\d*)(\.(0|[1-9]\d*))?$` ("1" 通过, "v1" 拒绝)
- `spec/schemas/{shots,audio_analysis,transcript,frames,prompts}.schema.json` —— 5 个 data shape schemas

ep01 真实 asset (producer mode 的输入):
- `/data/workspace/kais-shot-timeline/output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/asset.json`
- 已实测 schema_version="1", 93 shots, 5 data paths, 3 stems + 1 video —— schema-valid
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 构建 verify_contract.py harness skeleton + producer mode + consumer mode</name>
  <files>scripts/verify_contract.py</files>
  <read_first>
    - scripts/check_range.py (全文,157 行 —— standalone-script skeleton + bracketed [tag] print + argparse + sys.exit + find_free_port + try/finally server-lifecycle 模板)
    - spec/validate.py (全文,184 行 —— Draft202012Validator + iter_errors + _format_errors + load_validator 模式;注意 L49 SMOKE_SHAPES 排除 asset)
    - scripts/export_asset.py L106-127 + L308-313 (validate_asset_json inline 模式 + atomic JSON write)
    - spec/schemas/asset.schema.json (本 plan 的校验靶 —— schema_version pattern 字段)
    - /data/workspace/kst-canvas-consumer/scripts/verify-canvas-shot-timeline.ts L1-30 (shell-out target —— 确认运行方式 `npx tsx scripts/verify-canvas-shot-timeline.ts` + cwd 要求)
    - .planning/phases/04-cross-repo-contract-verification/04-PATTERNS.md (pattern map —— 每个子模式都有 in-repo analog + line refs)
    - .planning/phases/04-cross-repo-contract-verification/04-RESEARCH.md §Architecture Patterns §Pattern 1/4/5 + §Code Examples 示例 1/2/3
  </read_first>
  <behavior>
    - `python3 scripts/verify_contract.py --mode=producer` 对真实 ep01 asset 校验通过,输出含 `[producer] asset.json + 5 data shapes all schema-valid`,exit 0
    - `python3 scripts/verify_contract.py --mode=consumer` 通过 subprocess 调起 Phase 3 verify-canvas-shot-timeline.ts (17 asserts),exit 0
    - `python3 scripts/verify_contract.py` (默认 --mode=all) 跑 producer + consumer 两 mode (e2e 暂以 placeholder 跳过 —— 04-02 补完),两绿则 exit 0
    - `CANVAS_CONSUMER_PATH=/nonexistent python3 scripts/verify_contract.py --mode=consumer` exit 1 并打印 actionable 中文错误 (路径不存在 / 不是 git worktree)
    - `CANVAS_CONSUMER_PATH=/tmp python3 scripts/verify_contract.py --mode=consumer` exit 1 并提示「不是 feat/canvas-asset-collection 分支」
    - Producer mode 在 asset.json schema_version="v1" 时返回 (False, ...) 并在错误信息中包含 schema_version pattern 提示
    - 所有进度行带 bracketed `[verify-contract]` / `[producer]` / `[consumer]` 前缀 (per CLAUDE.md "Logging")
  </behavior>
  <action>
    Create `scripts/verify_contract.py` following the standalone-script skeleton in `scripts/check_range.py` (模块 docstring 含背景/用法/退出码; imports; `REPO = Path(__file__).parent.parent.resolve()`; helpers; `def main()` with argparse; `sys.exit`; `if __name__ == "__main__": main()`).

    **Module docstring** (Chinese, per CLAUDE.md "Comments"): 描述三种 mode (producer / consumer / self-test;e2e 由 04-02 补) + 用法示例 + 退出码 (0=全绿, 1=任一 mode fail) + 环境变量 (CANVAS_CONSUMER_PATH / PHASE4_RE_EXPORT / PHASE4_SELF_TEST / PHASE4_RUN_E2E / PHASE4_E2E_ASSET_DIR)。

    **Imports** (top-level, per CLAUDE.md "Import Organization" —— 不用 lazy import,jsonschema 是系统已装 hard dep): `argparse, json, os, pathlib, shutil, socket, sqlite3, subprocess, sys, tempfile, time, urllib.error, urllib.request`; `from pathlib import Path`; `from jsonschema import Draft202012Validator`.

    **Module constants:**
      - `REPO = Path(__file__).parent.parent.resolve()`
      - `SCHEMAS_DIR = REPO / "spec" / "schemas"`
      - `DEFAULT_CONSUMER_PATH = "/data/workspace/kst-canvas-consumer"`
      - `DEFAULT_E01_ASSET_DIR = REPO / "output" / "虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。"`
      - `SIX_SHAPES = ["asset", "shots", "audio_analysis", "transcript", "frames", "prompts"]` (per spec/validate.py L46 MINIMAL_ORDER)

    **Implement `validate_asset_json(asset_path) -> list[dict]`:**
      Load `spec/schemas/asset.schema.json`, instance = json.loads(Path(asset_path).read_text(encoding="utf-8")), 返回 sorted `Draft202012Validator(schema).iter_errors(instance)` (sort key = list(e.absolute_path)). 模板源: scripts/export_asset.py L106-127 —— 但 RETURN errors list (不 sys.exit;harness 收集后统一报告)。绝不 subprocess 到 spec/validate.py (其 SMOKE_SHAPES L49 排除 asset —— 04-PATTERNS.md L218-246 论证)。

    **Implement `validate_six_shapes(asset_dir) -> list[str]`:**
      For each shape in SIX_SHAPES:
        - schema = json.loads((SCHEMAS_DIR / f"{shape}.schema.json").read_text(encoding="utf-8"))
        - if shape == "asset": instance = manifest (already loaded); else: instance = json.loads((Path(asset_dir) / manifest["data"][shape]).read_text(encoding="utf-8"))
        - errs = sorted(Draft202012Validator(schema).iter_errors(instance), key=lambda e: list(e.absolute_path))
        - if errs: failures.append(f"{shape} at /{'/'.join(map(str, errs[0].absolute_path)) or '<root>'}: {errs[0].message}")
      返回 failures list (空 = 全绿)。模板源: spec/validate.py L52-94。

    **Implement `run_producer_check(args) -> tuple[bool, str]`:**
      1. asset_dir = Path(args.e2e_asset_dir); if not (asset_dir / "asset.json").is_file() → return (False, f"ep01 asset.json missing at {asset_dir}")
      2. (Optional re-export) If `os.environ.get("PHASE4_RE_EXPORT") == "1"`: subprocess.run([sys.executable, str(REPO / "scripts" / "export_asset.py"), "--work-dir", str(asset_dir), "--video", _resolve_canonical_video(asset_dir), "--stems-source-dir", str(asset_dir / "stems" / "htdemucs" / asset_dir.name), "--output", str(asset_dir / "asset.json"), "--force"], capture_output=True, text=True, timeout=60). If rc.returncode != 0 → return (False, f"export_asset.py failed (rc={rc.returncode}): {rc.stderr[:300]}"). 注意:不覆盖 worktree pinned golden (CONTEXT 决策 —— 仅本仓库 output/ 内的 ep01 可覆盖)。
      3. manifest = json.loads((asset_dir / "asset.json").read_text(encoding="utf-8"))
      4. failures = validate_six_shapes(asset_dir) (传 manifest 避免重复加载 —— 或 refactor signature 接受 manifest)
      5. if failures: return (False, "; ".join(failures)); else return (True, "asset.json + 5 data shapes all schema-valid")

      Helper `_resolve_canonical_video(asset_dir)`: 如果 `video.mp4` 是 symlink → `os.readlink`;否则直接 str(asset_dir / "video.mp4")。(Pitfall: Phase 2 决定 video.mp4 是 symlink target = --video abs path。)

    **Implement `run_consumer_check(args) -> tuple[bool, str]`:**
      1. consumer = args.consumer_path
      2. Guard: if not os.path.isdir(consumer) → return (False, f"CANVAS_CONSUMER_PATH 不存在: {consumer}\n  set CANVAS_CONSUMER_PATH=<worktree> 或 clone kais-aigc-platform + checkout feat/canvas-asset-collection")
      3. Guard: if not os.path.isdir(os.path.join(consumer, ".git")) → return (False, f"not a git worktree: {consumer}")
      4. verify_script = os.path.join(consumer, "scripts", "verify-canvas-shot-timeline.ts"); if not os.path.isfile(verify_script) → return (False, f"Phase 3 verify script missing: {verify_script}")
      5. rc = subprocess.run(["npx", "tsx", "scripts/verify-canvas-shot-timeline.ts"], cwd=consumer, capture_output=True, text=True, timeout=60)
      6. print(rc.stdout) (透传 Phase 3 输出 —— 含 17 assert 进度)
      7. if rc.returncode != 0 → return (False, f"verify-canvas-shot-timeline.ts exit {rc.returncode}\n{rc.stderr[:300]}"); else return (True, "Phase 3 17 asserts all green (importer accepts golden asset)")

    **Implement `find_free_port() -> int`** (本 plan 不用,但 04-02 e2e 需要 —— 预定义避免 04-02 改文件): per scripts/check_range.py L32-36, `socket.bind(("127.0.0.1", 0))` → `getsockname()[1]`。

    **Implement `main()`:**
      - argparse.ArgumentParser(description="Phase 4 跨仓库契约验证 harness —— 抓 producer / consumer 两端漂移")
      - `--mode` choices=["producer", "consumer", "e2e", "all"] default="all" help="验证模式 (producer=导出端 schema, consumer=消费端 importer, e2e=端到端, all=全部)"
      - `--consumer-path` default=os.environ.get("CANVAS_CONSUMER_PATH", DEFAULT_CONSUMER_PATH) help="consumer worktree 路径 (kais-aigc-platform feat/canvas-asset-collection)"
      - `--e2e-asset-dir` default=os.environ.get("PHASE4_E2E_ASSET_DIR", str(DEFAULT_E01_ASSET_DIR)) help="e2e 用的真实 asset 目录 (默认 ep01)"
      - `--e2e-skip` action="store_true" help="跳过 e2e mode (CI-friendly)"
      - results = []
      - If args.mode in ("producer", "all"): print("[verify-contract] mode=producer starting"); results.append(("producer", *run_producer_check(args)))
      - If args.mode in ("consumer", "all"): print("[verify-contract] mode=consumer starting"); results.append(("consumer", *run_consumer_check(args)))
      - If args.mode in ("e2e", "all") and not args.e2e_skip: print("[verify-contract] e2e mode 在 Plan 04-02 实现;设 PHASE4_RUN_E2E=1 + 04-02 完成后可用") —— placeholder,不 fail (避免本 plan 跑 default --mode=all 时因 e2e 未实现而 exit 1)
      - Print 汇总表 (每 mode 一行 OK/FAIL + detail 截断);任一 FAIL → sys.exit(1);全 OK → sys.exit(0)

    Per CLAUDE.md "Error Handling": 所有 sys.exit 用多行 actionable 中文消息 (2-space-indented hint lines)。所有 JSON I/O 用 `ensure_ascii=False` (ep01 video name 含 CJK + 全角标点)。所有进度 print 带 bracketed `[verify-contract]` / `[producer]` / `[consumer]` 前缀。

    不实现 run_e2e_check / run_self_test (留给 Task 2 + 04-02)。
  </action>
  <verify>
    <automated>python3 scripts/verify_contract.py --mode=producer && python3 scripts/verify_contract.py --mode=consumer && python3 scripts/verify_contract.py</automated>
  </verify>
  <done>
    - `python3 scripts/verify_contract.py --mode=producer` exit 0,输出含 `[producer] asset.json + 5 data shapes all schema-valid`
    - `python3 scripts/verify_contract.py --mode=consumer` exit 0,透传 Phase 3 verify-canvas-shot-timeline.ts 17 asserts 输出,最终打印 `[consumer] Phase 3 17 asserts all green`
    - `python3 scripts/verify_contract.py` (默认 --mode=all) 跑 producer + consumer,e2e placeholder 跳过,exit 0
    - `CANVAS_CONSUMER_PATH=/nonexistent python3 scripts/verify_contract.py --mode=consumer` exit 1 并打印 actionable 中文错误
    - `CANVAS_CONSUMER_PATH=/tmp python3 scripts/verify_contract.py --mode=consumer` exit 1 并提示不是 git worktree / 不是 feat/canvas-asset-collection 分支
    - `wc -l scripts/verify_contract.py` ≥ 200 行
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 加 PHASE4_SELF_TEST=1 self-test mode (注入故意漂移,证明 harness fail-loud)</name>
  <files>scripts/verify_contract.py</files>
  <read_first>
    - scripts/verify_contract.py (Task 1 产出 —— 确认 run_producer_check / validate_asset_json / main 的当前签名与 argparse 结构)
    - scripts/export_asset.py L308-313 (atomic JSON write: tmp=path+'.tmp' → json.dump ensure_ascii=False → os.replace)
    - .planning/phases/04-cross-repo-contract-verification/04-RESEARCH.md §Validation Architecture §Regression Invariants (deliberate-drift table) + §Open Question 4 (self-test 纳入 v1.0 的推荐)
  </read_first>
  <behavior>
    - `PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer` 注入一份故意损坏的 asset (schema_version 改为 "v1" 违反 semver-lite pattern),运行 validate_asset_json,断言返回 ≥1 error,exit 0 并打印 `[self-test] PASS: corrupt asset correctly rejected (N errors)`
    - 如果 harness 未能拒绝损坏 asset (errors 为空),exit 1 并打印 `[self-test] FAIL: harness 接受了损坏 asset —— 无法检测 producer drift!`
    - 真实 ep01 output/asset.json 字节不变 (用 temp dir 副本注入,finally 清理)
    - 无 PHASE4_SELF_TEST=1 时,harness 行为与 Task 1 完全一致 (self-test 是 opt-in)
    - temp dir 清理 (无 /tmp/phase4-selftest-* 残留)
  </behavior>
  <action>
    在 scripts/verify_contract.py 中新增 `run_self_test(args) -> tuple[bool, str]`:
      1. 用 `tempfile.mkdtemp(prefix="phase4-selftest-")` 创建 temp dir
      2. try:
           a. 把真实 ep01 asset.json + 5 个 referenced data files 复制到 temp dir (用 `manifest["data"]` 解析 path;shutil.copy2 保留 mtime)
           b. load 复制的 asset.json → manifest_copy
           c. 把 `manifest_copy["schema_version"]` 从 "1" 改为 "v1" (故意违反 spec/schemas/asset.schema.json L13 的 pattern `^(0|[1-9]\d*)(\.(0|[1-9]\d*))?$`)
           d. Atomic write back: `tmp = path + ".tmp"; with open(tmp, "w", encoding="utf-8") as f: json.dump(manifest_copy, f, indent=2, ensure_ascii=False); os.replace(tmp, path)` (per scripts/export_asset.py L308-313)
           e. errors = validate_asset_json(path) (复用 Task 1 的 helper)
           f. if len(errors) >= 1: return (True, f"corrupt asset (schema_version='v1') correctly rejected with {len(errors)} error(s): first = {errors[0].message}")
           g. else: return (False, "harness 接受了 schema_version='v1' —— 无法检测 producer drift (这是严重 regression)")
      3. finally: shutil.rmtree(temp_dir, ignore_errors=True)

    在 main() 中 wire self-test: 在 argparse 之后、dispatch 之前加判断 ——
      `if os.environ.get("PHASE4_SELF_TEST") == "1" and args.mode in ("producer", "all"):`
          `print("[verify-contract] self-test mode starting")`
          `results.insert(0, ("self-test", *run_self_test(args)))`  (插到 results 开头,self-test 失败则整体 exit 1)

    Self-test PASS 的语义:harness 正确检测到 drift (errors ≥ 1) → 整体 exit 0 (harness 工作)。Self-test FAIL:harness 未检测到 drift → exit 1 (harness 损坏)。这与 RESEARCH §Validation Architecture "corrupt asset → exit 1" 一致 —— 「exit 1」指底层 producer mode 遇到坏 asset 应 exit 1,而 self-test 是 meta-test 验证这个属性成立。

    打印 `[self-test]` 前缀的进度行 (per CLAUDE.md "Logging")。
  </action>
  <verify>
    <automated>PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer; echo "exit=$?"; test -z "$(find /tmp -maxdepth 1 -name 'phase4-selftest-*' 2>/dev/null)" && echo "temp-clean"; git diff --quiet output/ && echo "ep01-untouched"</automated>
  </verify>
  <done>
    - `PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer` exit 0 并打印 `[self-test] PASS: corrupt asset correctly rejected`
    - `python3 scripts/verify_contract.py --mode=producer` (无 PHASE4_SELF_TEST=1) 行为不变,exit 0,无 [self-test] 输出
    - `find /tmp -maxdepth 1 -name 'phase4-selftest-*'` 返回空 (temp dir 清理)
    - `git diff --quiet output/` exit 0 (真实 ep01 asset 未被修改)
    - `grep -c "run_self_test" scripts/verify_contract.py` ≥ 2 (定义 + main 调用)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Producer harness → filesystem (本仓库 output/) | harness 读 ep01 asset.json + 5 data shapes (只读 except PHASE4_RE_EXPORT=1 opt-in 重写) |
| Producer harness → filesystem (temp dir) | self-test 写 temp corrupt asset,scope 限定 /tmp/phase4-selftest-* |
| Consumer harness → cross-repo worktree (CANVAS_CONSUMER_PATH) | harness shell out `npx tsx` 到 worktree —— 须验证 path 是合法 git worktree |
| Harness → subprocess argv | 所有 subprocess 调用用 list argv (never shell=True),ep01 dir 含 CJK + 全角标点不构成注入风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-T1 | Tampering | CANVAS_CONSUMER_PATH 指向恶意 repo | mitigate | run_consumer_check Guard: 验证 path 存在 + 是 git worktree;若不是则 sys.exit actionable 中文错误 (不静默通过) |
| T-04-02-T1 | Tampering | subprocess 命令注入 via asset path (CJK + 全角) | mitigate | 全部 subprocess.run 用 list argv (不用 shell=True / string);path 作为 argv 元素直传,不经 shell 解释 |
| T-04-03-T1 | Information Disclosure | self-test temp dir 残留 | mitigate | try/finally shutil.rmtree(temp_dir, ignore_errors=True);verify 用 find 检查无残留 |
| T-04-04-T1 | Tampering | self-test 误改真实 ep01 asset | mitigate | 在 temp dir 副本上注入 drift (shutil.copy2);真实 output/ 字节不变;verify 用 git diff --quiet output/ 确认 |
| T-04-05-T1 | Repudiation | harness 运行无清晰审计轨迹 | mitigate | 所有 mode 打印 bracketed [verify-contract]/[producer]/[consumer] 进度;汇总表打印每 mode OK/FAIL + detail |
| T-04-SC | Tampering | npm/pip/cargo install 投毒 | accept (N/A) | 本 phase 零新包安装 (RESEARCH §Package Legitimacy Audit);全部依赖两 repo manifest [VERIFIED] —— 无 [ASSUMED] / [SUS] / [SLOP] 包需阻塞性 human checkpoint |
</threat_model>

<verification>
## Plan-Level Verification (Wave 1 gate)

```bash
# 1. Producer mode (秒级) —— VERIFY-02 producer 侧
python3 scripts/verify_contract.py --mode=producer
# 期望: exit 0 + "[producer] asset.json + 5 data shapes all schema-valid"

# 2. Consumer mode (~5s) —— VERIFY-02 consumer 侧
python3 scripts/verify_contract.py --mode=consumer
# 期望: exit 0 + "[consumer] Phase 3 17 asserts all green"

# 3. Self-test (秒级) —— 回归保护证明 (RESEARCH §Regression Invariants)
PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer
# 期望: exit 0 + "[self-test] PASS: corrupt asset correctly rejected"

# 4. Full suite (default --mode=all, e2e 暂 placeholder)
python3 scripts/verify_contract.py
# 期望: exit 0,3 mode (producer/consumer/self-test-if-env) 全绿

# 5. Negative test —— drift 真的被抓到
CANVAS_CONSUMER_PATH=/tmp python3 scripts/verify_contract.py --mode=consumer
# 期望: exit 1 + actionable 中文错误
```

## Requirements Coverage

| Requirement | How this plan covers it | Automated evidence |
|-------------|------------------------|-------------------|
| VERIFY-02 (字段 schema 与媒体引用两端对齐,有回归保护) | producer mode (6-schema inline 校验抓 producer 漂移) + consumer mode (Phase 3 17 asserts 抓 consumer 漂移) + self-test (证明 harness fail-loud) | 命令 1+2+3 全绿 |
</verification>

<success_criteria>
- [ ] `scripts/verify_contract.py` 存在,≥ 200 行,含 run_producer_check / run_consumer_check / run_self_test / validate_asset_json / find_free_port / main
- [ ] producer mode 对真实 ep01 asset exit 0 (6 schemas 全通过)
- [ ] consumer mode shell-out 到 Phase 3 verify-canvas-shot-timeline.ts exit 0
- [ ] self-test 注入 corrupt asset 正确检测 + 真实 ep01 字节不变 + temp 清理
- [ ] CANVAS_CONSUMER_PATH 防御 (非 git worktree 时 actionable exit 1)
- [ ] 所有进度行带 bracketed [tag] 前缀 (CLAUDE.md "Logging")
- [ ] 不实现 e2e mode (留给 04-02)
</success_criteria>

<output>
Create `.planning/phases/04-cross-repo-contract-verification/04-01-SUMMARY.md` when done.
</output>
