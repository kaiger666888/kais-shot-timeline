---
phase: 04-cross-repo-contract-verification
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - scripts/verify_contract.py
  - .planning/phases/04-cross-repo-contract-verification/04-VERIFICATION.md
autonomous: true
requirements: [VERIFY-01]
tags: [e2e, cross-repo, sqlite, backend-lifecycle, capstone]

must_haves:
  truths:
    - "运行 `PHASE4_RUN_E2E=1 python3 scripts/verify_contract.py --mode=e2e` 起消费者 backend → POST import-from-dir 喂真实 ep01 asset → SQL 直查 o_agentWorkData snapshot,exit 0"
    - "Snapshot 结构断言全绿:1 zone + ≥1 storyboard + 3 audio (vocals/drums/other) + ≥1 video artifact + (N-1) sequence edges (N=storyboard count)"
    - "WR-01 验证:sequence edges 在 primary appendAndSync 路径完整存活 (92 edges for ep01 N=93) —— save-v2 才会 strip,e2e 不走 save-v2"
    - "Backend subprocess 干净 teardown (try/finally 三层 terminate→wait→kill→wait-reap),无 orphan npx tsx 进程残留"
    - "Worktree 在 e2e 后保持干净:teardown 跑 `git -C $CANVAS_CONSUMER_PATH checkout -- src/types/database.d.ts` 兜底 dev-mode regen"
    - "Phase 3 leftover snapshot (projectId=9001, episodesId=9001) 被保留 (e2e 用自己的 timestamp-based ID,teardown 只 DELETE 自己的 test rows)"
    - "`04-VERIFICATION.md` 显式 accept WR-01/WR-04 为 documented limitations 并 cross-reference Phase 3 deferred-items.md"
    - "`04-VERIFICATION.md` 显式记录 SC-1 prompt-children scope reduction (observable collection = storyboard/audio/video;prompts 是 sidecar data refs per Phase 3 CONTEXT discretion)"
  artifacts:
    - path: "scripts/verify_contract.py"
      provides: "E2e mode —— backend lifecycle + SQL read-back + 结构断言 + worktree reconcile (补完 3-mode harness)"
      contains: ["run_e2e_check", "_poll_health", "start_backend", "_read_persisted_snapshot"]
      min_lines: 350
    - path: ".planning/phases/04-cross-repo-contract-verification/04-VERIFICATION.md"
      provides: "Phase 4 capstone verification report —— 3-mode 结果 + WR-01/04 acceptance + SC-1 scope reduction + 闭环 Phase 3 deferred items"
      contains: ["VERIFY-01", "VERIFY-02", "WR-01", "WR-04", "SC-1"]
      min_lines: 80
  key_links:
    - from: "scripts/verify_contract.py::run_e2e_check"
      to: "/data/workspace/kst-canvas-consumer/src/app.ts"
      via: "subprocess.Popen([npx, tsx, src/app.ts], cwd=CANVAS_CONSUMER_PATH, env={PORT, NODE_ENV=dev})"
      pattern: "npx.*tsx.*src/app\\.ts"
    - from: "scripts/verify_contract.py::run_e2e_check"
      to: "POST http://127.0.0.1:{port}/api/canvas/v2/import-from-dir"
      via: "urllib.request with JSON body {projectId, episodesId, workdir=<real ep01 dir>, mode='replace'}"
      pattern: "import-from-dir"
    - from: "scripts/verify_contract.py::run_e2e_check"
      to: "/data/workspace/kst-canvas-consumer/data/db2.sqlite table o_agentWorkData"
      via: "sqlite3 parameterized SELECT data WHERE projectId=? AND episodesId=? AND key='canvasGraph' (占位符,不 shell-interpolate)"
      pattern: "o_agentWorkData.*canvasGraph"
    - from: ".planning/phases/04-cross-repo-contract-verification/04-VERIFICATION.md"
      to: ".planning/phases/03-canvas-consumer/deferred-items.md"
      via: "cross-reference WR-01 + WR-04 (闭环 Phase 3 deferred items)"
      pattern: "deferred-items"
---

<objective>
Add the e2e mode to `scripts/verify_contract.py` (completing the 3-mode harness) and write the Phase 4 capstone verification report. Covers VERIFY-01 (real ShotTimelineAsset flows end-to-end from producer to consumer with correct persisted structure).

Purpose: 这是 v1.0 milestone 的 capstone —— 证明真实 producer asset 能成功 import 进 consumer backend,且持久化的 graph 结构正确 (zone + storyboard + audio + video + sequence edges)。同时正式 accept WR-01/WR-04 (save-v2 latent bugs,超出 v1.0 additive 范围) 并记录 SC-1 prompt-children scope reduction (prompts 是 sidecar data refs,不单独建节点 —— Phase 3 CONTEXT discretion)。

Output: (1) `scripts/verify_contract.py` 扩展 `run_e2e_check` (backend lifecycle + POST import-from-dir + SQL read-back + 结构断言 + worktree reconcile);(2) `.planning/phases/04-cross-repo-contract-verification/04-VERIFICATION.md` capstone 报告 (3-mode 结果 + WR-01/04 acceptance + SC-1 scope reduction + 闭环 Phase 3 deferred items)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cross-repo-contract-verification/04-CONTEXT.md
@.planning/phases/04-cross-repo-contract-verification/04-RESEARCH.md
@.planning/phases/04-cross-repo-contract-verification/04-PATTERNS.md
@.planning/phases/04-cross-repo-contract-verification/04-VALIDATION.md
@.planning/phases/04-cross-repo-contract-verification/04-01-SUMMARY.md
@.planning/phases/03-canvas-consumer/deferred-items.md
@.planning/phases/03-canvas-consumer/03-VERIFICATION.md

<interfaces>
<!-- 04-01 产出的 harness 接口 (executor 读 scripts/verify_contract.py 确认实际签名) -->

Expected from Plan 04-01 (scripts/verify_contract.py):
```python
REPO = Path(__file__).parent.parent.resolve()
SCHEMAS_DIR = REPO / "spec" / "schemas"
DEFAULT_CONSUMER_PATH = "/data/workspace/kst-canvas-consumer"
DEFAULT_E01_ASSET_DIR = REPO / "output" / "虫虫武侠小故事《小江湖》第01话：…"
SIX_SHAPES = ["asset", "shots", "audio_analysis", "transcript", "frames", "prompts"]

def find_free_port() -> int: ...   # 已在 04-01 预定义 —— e2e 直接用
def run_producer_check(args) -> tuple[bool, str]: ...
def run_consumer_check(args) -> tuple[bool, str]: ...
def main(): ...  # argparse with --mode/--consumer-path/--e2e-asset-dir/--e2e-skip
```

Cross-repo anchors (read-only —— harness 操作的目标):
| What | Where | Reference |
|------|-------|-----------|
| Express entrypoint | src/app.ts L267-269 | `port = ... || 10588; server.listen(port)` |
| Auth bypass | src/app.ts L225-234 | `/api/*` V6.0 无需 token |
| Health endpoint | src/routes/canvas/v2/health.ts | `GET /health` 返 200,是 readiness probe |
| Route body schema | src/routes/canvas/v2/import-from-dir.ts L1673-1680 | `projectId: z.number(), episodesId: z.number(), workdir: z.string(), mode default "merge"` |
| Persistence call | src/routes/canvas/v2/import-from-dir.ts L1756, 1828 | `appendAndSync(...)` —— primary 路径 |
| Snapshot query columns | src/routes/canvas/v2/import-from-dir.ts L1771-1776 | `db("o_agentWorkData").where("projectId", String(...)).andWhere("episodesId", String(...)).andWhere("key", "canvasGraph")` —— SQL 必须用这三个列名 |

Phase 3 leftover evidence (proves dual-track persistence):
- worktree `data/db2.sqlite` `o_agentWorkData`: 1 row (projectId=9001, episodesId=9001, key='canvasGraph', ~290KB JSON blob)
- `canvas_nodes` / `canvas_links`: 0 rows (empty —— 证明 load-v2 走 relational 不交叉)
- Snapshot 结构: 99 nodes (1 zone + 1 sum + 93 storyboard + 3 audio + 1 video + 1 sum-p13 video) + 190 links + 92 sequence edges

Phase 3 deferred items (must close in 04-VERIFICATION.md):
- WR-01: `FlowLinkV2Schema` 不声明 `data` 字段 → save-v2 路径 strip sequence edge data (primary 路径不受影响)
- WR-04: `sum-p13` summary node 在 save-v2 路径被 validateGraphNodes 拒 (primary 路径不受影响)

ep01 真实 asset dir (e2e 输入):
- `output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/`
- 含 asset.json (schema_version="1", 93 shots) + video.mp4 + stems/{vocals,drums,other}.wav + 5 data JSONs
- 注意:dir name 含 CJK + 全角标点 + space —— body 用 UTF-8 encoded JSON bytes 直传 (Pitfall 6)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 e2e mode —— backend lifecycle + POST import-from-dir + SQL read-back + 结构断言 + worktree reconcile</name>
  <files>scripts/verify_contract.py</files>
  <read_first>
    - scripts/verify_contract.py (04-01 产出 —— 确认 find_free_port / main 的 argparse / 现有 imports,在它们之上扩展)
    - scripts/check_range.py L32-48, L67-118 (server lifecycle 全套:find_free_port + wait_ready + Popen + try/finally 三层 teardown —— 这是 e2e 的直接模板,subprocess.Popen 用 DEVNULL 避免 pipe-buffer deadlock per Pitfall 7)
    - .planning/phases/04-cross-repo-contract-verification/04-RESEARCH.md §Architecture Patterns §Pattern 2 (backend lifecycle) + §Pattern 3 (SQL 直查 snapshot) + §Code Examples 示例 1 (e2e 完整骨架) + §Common Pitfalls (Pitfall 1-7 全部)
    - .planning/phases/04-cross-repo-contract-verification/04-PATTERNS.md §Cross-repo e2e anchors (列名 / endpoint / 行号)
    - /data/workspace/kst-canvas-consumer/src/routes/canvas/v2/import-from-dir.ts L1670-1864 (POST route + validateFields + appendAndSync + snapshot query 的精确列名)
    - /data/workspace/kst-canvas-consumer/src/app.ts L183-185, L267-276 (/health + server.listen + bootReady await gate)
  </read_first>
  <behavior>
    - `PHASE4_RUN_E2E=1 python3 scripts/verify_contract.py --mode=e2e` 完成:起 backend → /health 返 200 → POST import-from-dir 返 200 → SQL 查到 snapshot → 结构断言全绿 → teardown → exit 0,总耗时 ~30s
    - Backend 干净 teardown (proc.terminate → wait(10) → kill → wait(2) reap),无 orphan 进程
    - Snapshot 结构断言 (基于 ep01 N=93):1 zone, ≥1 storyboard (实测 93), 3 audio, ≥1 video artifact (实测 1 artifact + 1 sum-p13), N-1 sequence edges (实测 92) —— WR-01 验证 (primary 路径 sequence edges 存活)
    - Worktree 干净:teardown 跑 `git -C $CANVAS_CONSUMER_PATH checkout -- src/types/database.d.ts` 兜底 dev-mode regen;`git -C $CANVAS_CONSUMER_PATH status` 仅显示预期的(无)或 pre-existing dirty 文件
    - Phase 3 leftover (9001/9001) 保留:e2e 用 timestamp-based pid/eid (例如 `int(time.time())` 与 +1),teardown 只 DELETE 自己的 test rows (`WHERE projectId=? AND episodesId=?`)
    - 无 PHASE4_RUN_E2E=1 时,--mode=e2e 打印「set PHASE4_RUN_E2E=1 to enable」并跳过 (不 fail —— CI-friendly opt-in per CONTEXT)
    - Backend stdout 用 subprocess.DEVNULL (或 logfile) 避免 pipe-buffer deadlock (Pitfall 7)
  </behavior>
  <action>
    在 scripts/verify_contract.py 中新增以下 helpers + run_e2e_check,并更新 main() 把 e2e mode dispatch 接入 (替换 04-01 的 placeholder)。

    **新增 `_poll_health(port: int, timeout: float = 45.0) -> bool`:**
      `url = f"http://127.0.0.1:{port}/health"`;deadline = time.time() + timeout;while 循环:try `urllib.request.urlopen(url, timeout=2)` → status==200 return True;except URLError sleep 0.5;deadline 到 return False。模板源:scripts/check_range.py L93-102 (wait_ready 的 HTTP 版)。

    **新增 `_read_persisted_snapshot(db_path: str, project_id: int, episodes_id: int) -> dict | None`:**
      `conn = sqlite3.connect(db_path)`;try:`cur = conn.cursor()`;`cur.execute("SELECT data FROM o_agentWorkData WHERE projectId = ? AND episodesId = ? AND key = 'canvasGraph'", (str(project_id), str(episodes_id)))` —— **参数化占位符 (T-04-01-T2 mitigation:绝不 shell-interpolate)**;`row = cur.fetchone()`;if not row: return None;return json.loads(row[0]);finally:conn.close()。
      CRITICAL (Pitfall 1):绝不用 `/api/canvas/v2/load-v2` 做 read-back —— 它读 relational `canvas_nodes` (import-from-dir 不写,会返 null)。SQL 直查 `o_agentWorkData` JSON blob 是唯一非侵入路径。

    **新增 `run_e2e_check(args) -> tuple[bool, str]`:**
      1. consumer = args.consumer_path;guard:if not os.path.isdir(consumer) or not os.path.isdir(os.path.join(consumer, ".git")) → return (False, actionable msg)
      2. asset_dir = args.e2e_asset_dir;guard:if not os.path.isfile(os.path.join(asset_dir, "asset.json")) → return (False, f"asset.json missing at {asset_dir}")
      3. port = find_free_port() (复用 04-01 helper)
      4. env = os.environ.copy();env["PORT"] = str(port);env["NODE_ENV"] = "dev" (dev 模式起 tsx 直接跑 src/app.ts,production 需 esbuild bundle —— RESEARCH Open Question 1 锁定 dev + 兜底 checkout)
      5. proc = subprocess.Popen(["npx", "tsx", "src/app.ts"], cwd=consumer, env=env, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL) —— **DEVNULL 避免 pipe-buffer deadlock per Pitfall 7**
      6. pid = int(time.time());eid = pid + 1 (timestamp-based 唯一 ID,避免碰撞 Phase 3 leftover 9001/9001)
      7. try:
           a. if not _poll_health(port, timeout=45): return (False, f"backend failed /health within 45s (port {port})")
           b. print(f"[e2e] backend ready on port {port}")
           c. body = json.dumps({"projectId": pid, "episodesId": eid, "workdir": str(asset_dir), "mode": "replace"}).encode("utf-8") —— **UTF-8 encoded bytes (Pitfall 6:ep01 dir name 含 CJK + 全角标点)**
           d. req = urllib.request.Request(f"http://127.0.0.1:{port}/api/canvas/v2/import-from-dir", data=body, headers={"Content-Type": "application/json"}, method="POST")
           e. try: with urllib.request.urlopen(req, timeout=60) as r: resp = json.loads(r.read()); if r.status != 200: return (False, f"import-from-dir HTTP {r.status}: {resp}")
           f. except urllib.error.HTTPError as e: return (False, f"import-from-dir failed: HTTP {e.code} {e.read()[:200]}")
           g. print(f"[e2e] import-from-dir OK: {resp.get('data', {}).get('imported', '?')} nodes imported")
           h. db_path = os.path.join(consumer, "data", "db2.sqlite")
           i. graph = _read_persisted_snapshot(db_path, pid, eid)
           j. if graph is None: return (False, f"snapshot missing for pid={pid} eid={eid} —— import-from-dir 写入但 o_agentWorkData 查不到 (可能 primary 路径异常)")
           k. nodes = graph["nodes"]; links = graph["links"]
           l. zones = [n for n in nodes if n.get("type") == "zone"]
           m. storyboards = [n for n in nodes if n.get("type") == "storyboard"]
           n. audios = [n for n in nodes if n.get("type") == "audio"]
           o. videos = [n for n in nodes if n.get("type") == "video"]
           p. summaries = [n for n in nodes if str(n.get("id", "")).startswith("sum-")]
           q. seq_edges = [l for l in links if (l.get("data") or {}).get("linkType") == "sequence"]
           r. 结构断言 (用 assert + f-string,失败抛 AssertionError → catch 上层返 (False, msg)):
              - `assert len(zones) == 1, f"zones: want 1 got {len(zones)}"`
              - `assert len(storyboards) >= 1, f"storyboards: want ≥1 got {len(storyboards)}"` (ep01 实测 93,不硬编码 —— 用相对不变量)
              - `assert len(audios) == 3, f"audios: want 3 (vocals/drums/other) got {len(audios)}"`
              - `assert len(videos) >= 1, f"videos: want ≥1 artifact got {len(videos)}"` (实测 1 artifact + 1 sum-p13 = 2;sum-p13 由 buildPhaseTree 强制 type=video)
              - `assert len(seq_edges) == len(storyboards) - 1, f"seq_edges: want {len(storyboards)-1} (N-1) got {len(seq_edges)} —— WR-01 验证"`
              - print 结构汇总:`[e2e] snapshot valid: {len(nodes)} nodes, {len(links)} links, {len(zones)} zone, {len(storyboards)} storyboard, {len(audios)} audio, {len(videos)} video, {len(seq_edges)} seq edges (WR-01 data survives primary path)`
           s. return (True, f"snapshot valid: {len(storyboards)} storyboards + {len(seq_edges)} sequence edges survive primary appendAndSync path (WR-01/04 not surfaced)")
      8. finally (teardown —— 三层 + worktree reconcile + cleanup DELETE):
           a. proc.terminate();try: proc.wait(timeout=10);except subprocess.TimeoutExpired: proc.kill();try: proc.wait(timeout=2);except subprocess.TimeoutExpired: pass (模板源:scripts/check_range.py L104-118 + 02-REVIEW WR-06 注释)
           b. Worktree reconcile (Pitfall 2/5 —— database.d.ts dev-mode regen 噪音):`subprocess.run(["git", "-C", consumer, "checkout", "--", "src/types/database.d.ts"], capture_output=True, timeout=10)` (best-effort,失败不阻塞 —— 它是 auto-regen 副产品,生产路径不依赖)
           c. Cleanup test rows (保留 Phase 3 leftover 9001/9001):
              `conn = sqlite3.connect(db_path)`;try:`cur = conn.cursor()`;`cur.execute("DELETE FROM o_agentWorkData WHERE projectId = ? AND episodesId = ?", (str(pid), str(eid)))`;`cur.execute("DELETE FROM kv_canvasEvent WHERE projectId = ? AND episodesId = ?", (str(pid), str(eid)))`;`conn.commit()`;finally:conn.close()

    **更新 main() dispatch (替换 04-01 placeholder):**
      原 04-01 placeholder 改为:
      ```
      if args.mode in ("e2e", "all") and not args.e2e_skip:
          e2e_enabled = (args.mode == "e2e") or (os.environ.get("PHASE4_RUN_E2E") == "1")
          if e2e_enabled:
              print("[verify-contract] mode=e2e starting (heavy: starts backend)")
              results.append(("e2e", *run_e2e_check(args)))
          else:
              print("[verify-contract] e2e skipped (set PHASE4_RUN_E2E=1 to enable)")
      ```
      语义:`--mode=e2e` 显式 opt-in 时即使无 env var 也跑 (用户明确要 e2e);`--mode=all` 需要 PHASE4_RUN_E2E=1 才包含 e2e (CI-friendly)。

    Per CLAUDE.md:所有 SQL 用 sqlite3 占位符 (绝不字符串拼接);所有 subprocess argv 用 list (绝不 shell=True);所有进度 print 带 `[e2e]` / `[verify-contract]` 前缀;JSON I/O 用 ensure_ascii=False。
  </action>
  <verify>
    <automated>PHASE4_RUN_E2E=1 python3 scripts/verify_contract.py --mode=e2e; echo "exit=$?"; pgrep -af "tsx src/app.ts" | grep -v grep | wc -l | xargs -I{} echo "leftover-backend-procs={}"; git -C /data/workspace/kst-canvas-consumer status --short src/types/database.d.ts | wc -l | xargs -I{} echo "worktree-dirty-files={}"</automated>
  </verify>
  <done>
    - `PHASE4_RUN_E2E=1 python3 scripts/verify_contract.py --mode=e2e` exit 0,打印 snapshot 结构汇总 (含「92 seq edges」或 ep01 N-1 实际数)
    - 无 orphan backend 进程 (`pgrep -af "tsx src/app.ts"` 仅返回 grep 自身或空)
    - Worktree 在 teardown 后 `git status --short src/types/database.d.ts` 输出空 (reconcile 成功)
    - Phase 3 leftover 保留:`sqlite3 /data/workspace/kst-canvas-consumer/data/db2.sqlite "SELECT count(*) FROM o_agentWorkData WHERE projectId='9001'"` 返回 1
    - e2e 自己的 test rows 已清:`sqlite3 ... "SELECT count(*) FROM o_agentWorkData WHERE projectId='<timestamp_pid>'"` 返回 0
    - 无 PHASE4_RUN_E2E=1 时 `python3 scripts/verify_contract.py` (default --mode=all) 跳过 e2e 并打印提示,producer+consumer+self-test(若 PHASE4_SELF_TEST=1)仍跑
    - `grep -c "run_e2e_check\|_poll_health\|_read_persisted_snapshot" scripts/verify_contract.py` ≥ 6 (3 def + 3 call/ref)
  </done>
</task>

<task type="auto">
  <name>Task 2: 撰写 04-VERIFICATION.md capstone 报告 (WR-01/04 acceptance + SC-1 scope reduction + 3-mode 结果)</name>
  <files>.planning/phases/04-cross-repo-contract-verification/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-canvas-consumer/03-VERIFICATION.md (frontmatter 格式 + deferred 字段写法 —— 模板源)
    - .planning/phases/03-canvas-consumer/deferred-items.md (WR-01 + WR-04 详细记录 —— 04-VERIFICATION 要 cross-reference 并正式闭环)
    - .planning/phases/04-cross-repo-contract-verification/04-CONTEXT.md (E2E 测试范围 + WR-01/04 处置决策)
    - .planning/phases/04-cross-repo-contract-verification/04-01-SUMMARY.md (VERIFY-02 producer + consumer + self-test 结果 —— 04-VERIFICATION 引用)
    - .planning/REQUIREMENTS.md (VERIFY-01 + VERIFY-02 + Success Criteria 原文)
  </read_first>
  <action>
    Create `.planning/phases/04-cross-repo-contract-verification/04-VERIFICATION.md` 遵循 Phase 3 `03-VERIFICATION.md` 的 frontmatter + 章节 结构。

    **Frontmatter:**
      - `phase: 04-cross-repo-contract-verification`
      - `verified: <ISO datetime>`
      - `status: passed` (前提:e2e + producer + consumer + self-test 全绿 —— Task 1 完成后填)
      - `score: N/N must-haves verified` (数 must_haves truths,全绿则 N/N)
      - `overrides_applied: 0`
      - `re_verification: { previous_status: none, ... }`
      - `deferred: []` (Phase 4 本身不产生新 deferred items;但要在 §闭环 章节 cross-reference Phase 3 的 WR-01/04 并标记「Phase 4 accepted, not fixing」)

    **章节 1: Phase Success Criteria 验证**
      引用 ROADMAP.md Phase 4 的 2 个 SC,逐条 evidence:
      - SC-1 (real asset → import → 渲染分镜/stem/字幕/prompt 集合):引用 e2e SQL read-back 实测 (1 zone + N storyboard + 3 audio + video + N-1 seq edges)。**显式记录 scope reduction (SC-1 scope reduction):** SC-1 原文提「prompt 集合」,但 Phase 3 CONTEXT discretion (D3) 已显式 defer prompts 为 sidecar data refs (asset.json#data.prompts),不单独建 prompt canvas 节点。e2e observable 集合是 storyboard/audio/video (collection 节点)。prompts 在 asset.json#data.prompts 字段里携带,经 importer 校验后作为 sidecar 引用暴露给前端。这是 accepted scope reduction (非 gap),cross-reference Phase 3 SUMMARY D3 deferral note + .planning/phases/03-canvas-consumer/03-CONTEXT.md。
      - SC-2 (regression test 抓 drift):引用 producer mode (6-schema inline 校验) + consumer mode (17 asserts shell-out) + self-test (corrupt asset 检测)。三个 mode 兜住两个漂移方向 + 证明 fail-loud。

    **章节 2: Requirements Coverage**
      表格列 VERIFY-01 + VERIFY-02,每条对应:behavior → automated command → 状态 → evidence。

    **章节 3: WR-01 / WR-04 正式 Acceptance (闭环 Phase 3 deferred items)**
      - 引用 .planning/phases/03-canvas-consumer/deferred-items.md WR-01 + WR-04 原文
      - 显式 accept (not fixing in v1.0):
        - 「e2e 走 primary `appendAndSync` 路径,该路径不 Zod-parse graph —— 仅写 event log + JSON blob snapshot,不触发 FlowLinkV2Schema (WR-01) 或 validateGraphNodes summary-node 检查 (WR-04)」
        - 「e2e SQL read-back 实测:N-1 sequence edges 全部存活 in o_agentWorkData snapshot (key='canvasGraph'),证明 WR-01 在 primary 路径不浮现」
        - 「WR-01/04 是 secondary `save-v2` HTTP 路径的 latent bugs,修需动 consumer 仓库共享 production schema (FlowLinkV2Schema / flowgraph-v2-schema.ts),超出 v1.0 additive 范围 (CONTEXT D-WR 锁定)」
        - 「归属:consumer-repo backlog (kais-aigc-platform feat/canvas-asset-collection);v1.0 不修,后续 persistence-hardening phase 或 save-v2 重构时处置」
      - 标记 Phase 3 `03-VERIFICATION.md` deferred 字段里「addressed_in: "Phase 4"」的两条 (WR-01 sequence edge / WR-04 sum-p13) 正式闭环 (status: accepted in Phase 4, not deferred further)。

    **章节 4: 3-mode 运行结果**
      表格:mode / 命令 / 耗时 / 结果 / evidence 摘要 (引用 04-01-SUMMARY 与 Task 1 e2e 输出)。

    **章节 5: 闭环 Status**
      - Phase 4 milestone 完成 —— VERIFY-01 + VERIFY-02 全绿
      - 跨仓库 contract 验证体系落地 (scripts/verify_contract.py 单脚本 3 mode)
      - Phase 3 deferred items (WR-01/04) 正式 accept + 闭环
      - SC-1 prompt-children scope reduction 记录在案
      - v1.0 ShotTimelineAsset Contract milestone 全部 4 phase 闭合

    中文写作 (per CLAUDE.md "Comments" + 项目惯例);代码标识符 / endpoint / SQL 列名用英文 + backtick。
  </action>
  <verify>
    <automated>test -f .planning/phases/04-cross-repo-contract-verification/04-VERIFICATION.md && grep -c "VERIFY-01\|VERIFY-02\|WR-01\|WR-04\|SC-1\|scope reduction\|deferred-items\|appendAndSync" .planning/phases/04-cross-repo-contract-verification/04-VERIFICATION.md | xargs -I{} echo "key-refs={}"; test "$(grep -c '^## ' .planning/phases/04-cross-repo-contract-verification/04-VERIFICATION.md)" -ge 4 && echo "sections-ok"</automated>
  </verify>
  <done>
    - `.planning/phases/04-cross-repo-contract-verification/04-VERIFICATION.md` 存在,≥ 80 行
    - Frontmatter 含 phase / verified / status=passed / score=N/N
    - §SC-1 显式记录「prompt 集合」scope reduction (prompts = sidecar data refs per Phase 3 D3),非 gap
    - §WR-01/04 显式 accept + cross-reference Phase 3 deferred-items.md (闭环 Phase 3 `addressed_in: Phase 4`)
    - §Requirements Coverage 覆盖 VERIFY-01 + VERIFY-02
    - §3-mode 运行结果 表格含 producer/consumer/self-test/e2e
    - 中文 prose + English code identifiers (per CLAUDE.md "Comments")
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Harness → cross-repo backend subprocess | harness 起 `npx tsx src/app.ts` in CANVAS_CONSUMER_PATH —— 须验证 path 合法 + 走 ephemeral port 不抢默认 10588 |
| Harness → consumer DB (sqlite3 直查) | harness 读 o_agentWorkData snapshot + teardown DELETE test rows —— 须参数化 SQL,保留 Phase 3 leftover |
| Harness → HTTP POST localhost | harness POST /api/canvas/v2/import-from-dir with JSON body (ep01 dir 含 CJK) —— 须 UTF-8 encode,不 shell-interpolate |
| Backend → consumer filesystem | backend 在 e2e 期间写 o_agentWorkData + kv_canvasEvent + regen database.d.ts (dev mode) —— teardown 须 reconcile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-T2 | Tampering | SQL injection via projectId/episodesId | mitigate | harness 自己生成 timestamp-based int (int(time.time())),不接受 user input;sqlite3 参数化查询 (占位符 `?`),绝不字符串拼接 |
| T-04-02-T2 | Denial of Service | Backend subprocess leak (test crash 后未 teardown) | mitigate | try/finally 三层:proc.terminate() → wait(timeout=10) → kill() → wait(timeout=2) reap (模板:check_range.py L104-118 + 02-REVIEW WR-06) |
| T-04-03-T2 | Denial of Service | Pipe-buffer deadlock (backend stdout 阻塞) | mitigate | stdout=subprocess.DEVNULL (or logfile);不用 PIPE (Pitfall 7) |
| T-04-04-T2 | Information Disclosure | Stale test rows 累积 in o_agentWorkData | mitigate | teardown DELETE FROM o_agentWorkData/kv_canvasEvent WHERE projectId=? AND episodesId=? (只删自己的,保留 Phase 3 leftover 9001/9001) |
| T-04-05-T2 | Tampering | Backend writes to worktree src/types/database.d.ts (dev regen) | mitigate | teardown `git -C $CANVAS_CONSUMER_PATH checkout -- src/types/database.d.ts` (best-effort revert auto-regen noise,Pitfall 2/5) |
| T-04-06-T2 | Denial of Service | Port conflict (10588 被占) | mitigate | find_free_port() 拿 ephemeral port (bind 127.0.0.1:0),不抢默认 (04-PATTERNS.md L86-91) |
| T-04-07-T2 | Tampering | CANVAS_CONSUMER_PATH 指向恶意 repo | mitigate | run_e2e_check 入口 Guard:path 存在 + 是 git worktree (04-01 已落地,这里复用) |
| T-04-08-T2 | Repudiation | e2e 多次跑残留 backend / rows | mitigate | try/finally teardown 保证 cleanup + best-effort checkout;verify 用 pgrep + sqlite count 检查 |
| T-04-SC | Tampering | npm/pip/cargo install 投毒 | accept (N/A) | 零新包安装 (RESEARCH §Package Legitimacy Audit) —— 全部依赖两 repo manifest [VERIFIED];无 [ASSUMED] / [SUS] / [SLOP] 包 |
</threat_model>

<verification>
## Plan-Level Verification (Wave 2 gate —— 全 v1.0 milestone gate)

```bash
# 1. E2e mode (heavy, ~30s) —— VERIFY-01 + WR-01 验证
PHASE4_RUN_E2E=1 python3 scripts/verify_contract.py --mode=e2e
# 期望: exit 0 + snapshot 结构汇总含「seq edges」+ 「WR-01 data survives primary path」

# 2. 进程清理检查
pgrep -af "tsx src/app.ts" | grep -v grep
# 期望: 空 (无 orphan backend)

# 3. Worktree 干净
git -C /data/workspace/kst-canvas-consumer status --short src/types/database.d.ts
# 期望: 空 (reconcile 成功)

# 4. Phase 3 leftover 保留 (不删 9001/9001)
sqlite3 /data/workspace/kst-canvas-consumer/data/db2.sqlite \
  "SELECT count(*) FROM o_agentWorkData WHERE projectId='9001' AND episodesId='9001'"
# 期望: 1

# 5. E2e test rows 已清 (自己的 pid/eid 不留)
sqlite3 /data/workspace/kst-canvas-consumer/data/db2.sqlite \
  "SELECT count(*) FROM o_agentWorkData WHERE projectId NOT IN ('9001')"
# 期望: 0 (本次 e2e 的 rows 已 DELETE)

# 6. 全 3-mode 完整跑 (milestone gate)
PHASE4_RUN_E2E=1 PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py
# 期望: exit 0 (producer + consumer + self-test + e2e 全绿)

# 7. Capstone 报告存在 + 关键引用
test -f .planning/phases/04-cross-repo-contract-verification/04-VERIFICATION.md
grep -c "WR-01\|WR-04\|SC-1\|VERIFY-01\|VERIFY-02\|deferred-items" \
  .planning/phases/04-cross-repo-contract-verification/04-VERIFICATION.md
# 期望: ≥ 6 (覆盖所有关键 cross-reference)
```

## Requirements Coverage

| Requirement | How this plan covers it | Automated evidence |
|-------------|------------------------|-------------------|
| VERIFY-01 (导出端产物能被消费端成功 import,渲染分镜/stem/字幕/prompt 集合 —— observable e2e) | run_e2e_check:起 backend → POST import-from-dir → SQL read-back → 结构断言 (1 zone + N storyboard + 3 audio + video + N-1 seq edges) | 命令 1 + 6 全绿;SQL count + snapshot 结构汇总输出 |
</verification>

<success_criteria>
- [ ] `run_e2e_check` 在 scripts/verify_contract.py 实现 (≥ 350 行总)
- [ ] `PHASE4_RUN_E2E=1 python3 scripts/verify_contract.py --mode=e2e` exit 0,结构断言全绿
- [ ] Backend teardown 三层 (terminate → wait → kill → reap),无 orphan 进程
- [ ] Worktree database.d.ts 在 teardown 后干净 (git checkout 兜底)
- [ ] Phase 3 leftover (9001/9001) 保留,e2e 自己的 rows 清理
- [ ] SQL 全部参数化 (无字符串拼接 projectId/episodesId)
- [ ] 04-VERIFICATION.md 存在,含 WR-01/04 acceptance + SC-1 scope reduction + 3-mode 结果 + cross-reference Phase 3 deferred-items
- [ ] v1.0 milestone gate:`PHASE4_RUN_E2E=1 PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py` 全绿 (3 mode + self-test)
</success_criteria>

<output>
Create `.planning/phases/04-cross-repo-contract-verification/04-02-SUMMARY.md` when done.
</output>
