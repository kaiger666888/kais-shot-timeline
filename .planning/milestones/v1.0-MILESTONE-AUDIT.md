---
milestone: v1.0
milestone_name: ShotTimelineAsset Contract
audited: 2026-07-21
status: passed
scores:
  requirements: 12/12
  phases: 4/4
  integration: 11/11 cross-phase links (0 orphan, 0 missing)
  flows: 4/4 E2E flows complete
gaps:
  requirements: []   # no unsatisfied / orphaned requirements
  integration: []
  flows: []
tech_debt:
  - phase: 03-canvas-consumer (cross-repo kais-aigc-platform)
    items:
      - "WR-01: save-v2 FlowLinkV2Schema strips sequence-edge `data` field (latent on secondary save-v2 path; primary appendAndSync unaffected). Accepted → consumer-repo backlog."
      - "WR-04: sum-p13 node Zod-rejectable by save-v2 (pre-existing pattern since P01; primary path unaffected). Accepted → consumer-repo backlog."
      - "Phase 3 D3: transcript/audio_analysis/prompts read but `void`-ed at import-from-dir.ts:1135 (sidecar attach deferred). SC-1 scope formally reduced — observable collection = storyboard/audio/video."
  - phase: 01-shottimelineasset-specification
    items:
      - "No VALIDATION.md (pure spec/docs phase — schemas + SPEC.md; validated downstream by Phase 2 inline validation + Phase 4 producer mode). Benign."
  - phase: 02-shot-timeline-exporter-producer
    items:
      - "Local data-state: output/《小江湖》第03话…/ has stale asset.json referencing missing transcript.json/frames.json (interrupted cache-restore regen). Gitignored data dir, not a code defect; exporter WR-02 guard refuses regeneration. ep01 stable target used by all verifications."
nyquist:
  compliant_phases: [02, 03, 04]
  partial_phases: []
  missing_phases: [01]   # spec phase — no VALIDATION.md (benign)
  overall: COMPLIANT (Phase 1 spec validated downstream)
---

# v1.0 ShotTimelineAsset Contract — Milestone Audit

**Verdict: ✅ PASSED** — milestone achieved its definition of done.

## Summary

| Dimension | Score | Status |
|-----------|-------|--------|
| Requirements | 12/12 satisfied | ✅ |
| Phases verified | 4/4 passed | ✅ |
| Cross-phase integration | 11 links, 0 orphan, 0 missing | ✅ |
| E2E flows | 4/4 complete | ✅ |
| Blockers | 0 | ✅ |

All 4 phases verified `passed`. Cross-repo wiring (shot-timeline producer ↔ kais-aigc-platform consumer) is genuine — real backend, real SQLite persistence, real producer asset, no mocks. The contract (`spec/schemas/*.json`) is the single source of truth both repos evolve against — the milestone's loose-coupling goal is met.

## Requirements Coverage (3-source cross-reference)

Sources: VERIFICATION.md (all 4 `passed`) × REQUIREMENTS.md traceability (all `[x]` Complete) × SUMMARY frontmatter (n/a — not used by this project). All 12 v1.0 requirements satisfied; 0 orphaned.

| REQ-ID | Phase | Integration Path | Status |
|--------|-------|-----------------|--------|
| SPEC-01..04 | 1 | schemas → producer writes asset.json → consumer reads → Phase 4 validates 6 schemas inline | ✅ WIRED |
| EXPORT-01..03 | 2 | step_export → export_asset.py (manifest + symlinks + inline validate) + serve.py FD-fix + check_range.py | ✅ WIRED |
| CANVAS-01..03 | 3 | import-from-dir ShotTimelineAsset branch → zone + storyboard/audio/video children + sequence edges; 5 renderers reused, no contract bump | ✅ WIRED |
| VERIFY-01 | 4 | e2e: real ep01 → POST import-from-dir → SQL read-back → 1 zone + 93 storyboard + 3 audio + 2 video + 92 seq edges | ✅ WIRED (SC-1 prompt-children scope formally reduced — see tech debt) |
| VERIFY-02 | 4 | producer mode (inline schema) + consumer mode (Phase 3 shell-out) + self-test (fail-loud proof) | ✅ WIRED |

## E2E Flows (all complete, primary path)

1. **Producer artifact generation** — run_pipeline.py:step_export → export_asset.py → asset.json + canonical symlinks + inline validation. Additive-only (detectors/audio/html untouched).
2. **Producer → Consumer (cross-repo)** — real ep01 asset.json consumed by extractShotTimelineArtifacts; canonical media naming byte-identical (video.mp4, stems/{vocals,drums,other}.wav, bass excluded both sides).
3. **Producer → backend → SQL persistence** — verify_contract.py e2e boots consumer backend, POSTs import-from-dir, SQL-reads o_agentWorkData → 99 nodes / 92 sequence edges survive primary appendAndSync path (WR-01 does NOT surface).
4. **Contract regression detection** — producer mode (6-schema inline validate) + consumer mode (Phase 3 19 asserts) + self-test (corrupt schema_version → fail-loud). Both drift directions covered.

## Deferred Items (formally accepted, documented)

- **WR-01 / WR-04** (Phase 3 → Phase 4): save-v2 secondary-path latent bugs. Primary appendAndSync path unaffected (e2e proves 92 sequence edges + sum-p13 survive). Ownership transferred to consumer-repo backlog (`kais-aigc-platform` feat/canvas-asset-collection). v1.0 additive-only / no-contract-bump invariant preserved.
- **SC-1 prompt-children scope reduction** (Phase 3 D3): prompts/transcript are sidecar data refs in asset.json#data, not separate canvas nodes. Observable v1.0 collection = storyboard/audio/video. Locked upstream decision, cross-referenced in Phase 4 VERIFICATION.
- **Phase 1 VALIDATION.md**: absent (pure spec phase). Benign — its output (schemas) is validated downstream.

## Nyquist Compliance

Phases 2/3/4 `nyquist_compliant: true`. Phase 1 (spec) has no VALIDATION.md — benign (no code to test; schemas validated by Phase 2/4).

## Recommendation

Milestone achieved its definition of done. Proceed to **complete-milestone** (archive + tag). The deferred items are formally accepted design decisions / consumer-repo backlog, not loose ends requiring a closure phase.
