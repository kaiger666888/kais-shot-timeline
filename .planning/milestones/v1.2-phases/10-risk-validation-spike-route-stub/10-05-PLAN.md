---
phase: 10-risk-validation-spike-route-stub
plan: 05
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - spike/audio/run_whisperx_align.py
  - spike/audio/results/whisperx_align_ep01.json
  # Plus ephemeral: /tmp/whisperx-spike-venv/ (NOT committed — excluded via .gitignore pattern)
autonomous: true
requirements: [DIA-05]
tags: [spike, whisperx, alignment, dia-05, cuda-decision, isolated-venv]

must_haves:
  truths:
    - "WhisperX 3.8.6 installs in /tmp/whisperx-spike-venv with CPU torch wheel — system torch 2.6.0+cu124 UNAFFECTED (Pitfall 1 mitigation, verified by post-install torch.__version__ check)"
    - "whisperx.load_align_model(language_code='zh', device='cpu') succeeds (validates Assumption A1 — CPU mode works without CUDA 12.8 toolkit)"
    - "whisperx.align() accepts arbitrary faster-whisper segments from ep01 transcript.json (validates Assumption A2 — no re-transcription needed)"
    - "whisperx_align_ep01.json contains drift_stats {pct_under_200ms, mean_drift_ms, median_drift_ms} computed across ≥30 stratified ep01 segments"
    - "Result JSON explicitly records: device='cpu', system_torch='2.6.0+cu124', venv_torch=<version> — audit trail for CUDA 12.8 decision"
  artifacts:
    - path: "spike/audio/run_whisperx_align.py"
      provides: "WhisperX word-align spike — reads ep01 transcript.json, runs wav2vec2 align on vocals.wav, measures drift"
      min_lines: 100
      contains: "import whisperx"
    - path: "spike/audio/results/whisperx_align_ep01.json"
      provides: "Per-segment word drift stats on 30 ep01 segments — basis for DIA-05 ship-experimental / defer AND CUDA 12.8 vs stay-on-12.4 decision"
      contains: "pct_under_200ms"
  key_links:
    - from: "spike/audio/run_whisperx_align.py"
      to: "spike/audio/common.py"
      via: "from common import EP01_VOCALS, EP01_TRANSCRIPT, stratified_sample, write_result, _safe_error"
      pattern: "from \\.{0,1}common import"
    - from: "spike/audio/run_whisperx_align.py"
      to: "/tmp/whisperx-spike-venv/bin/python"
      via: "shebang or explicit interpreter invocation"
      pattern: "whisperx-spike-venv"
---

<objective>
Run the WhisperX word-align spike on 30 stratified ep01 vocals segments in an ISOLATED venv (/tmp/whisperx-spike-venv) on CPU. Produce the empirical word-drift evidence that gates the DIA-05 ship-experimental (<200ms on ≥80%) / defer decision AND resolves BLOCKER 1 (CUDA 12.8-upgrade vs stay-on-12.4).

Purpose: WhisperX is the ONLY model in the spike that requires an isolated venv (Pitfall 1 — its PyPI deps pull torch with CUDA 12.8 wheel tag, conflicting with project torch 2.6.0+cu124). The drift number produced here drives 2 of the 4 PROJECT.md Key Decisions: DIA-05 ship/defer + CUDA upgrade path.

Output: 2 artifacts — run_whisperx_align.py (throwaway script) + whisperx_align_ep01.json (committed evidence). The isolated venv is ephemeral (not committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-risk-validation-spike-route-stub/10-CONTEXT.md
@.planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md
@./CLAUDE.md

<interfaces>
<!-- From Plan 01 common.py -->
EP01_VOCALS: pathlib.Path -> ep01 vocals.wav (16-bit PCM, 44.1kHz stereo from Demucs)
EP01_TRANSCRIPT: pathlib.Path -> transcript.json ({backend, model, language, duration, segments:[{start, end, text}], text, source})
stratified_sample(segments, n=30, seed=10) -> list[(idx, seg_dict)]
write_result(model, fixture, payload)
_safe_error(msg)

<!-- WhisperX interface (research §Pattern 4, lines 444-475) -->
# Run inside /tmp/whisperx-spike-venv/bin/python (NOT system python)
import whisperx
audio = whisperx.load_audio("/path/to/vocals.wav")   # returns np.float32 mono 16kHz
model_a, metadata = whisperx.load_align_model(language_code="zh", device="cpu")  # default Chinese wav2vec2
aligned = whisperx.align(segments, model_a, metadata, audio, "cpu", return_char_alignments=False)
# aligned["segments"][i] = {"start","end","text","words":[{"word","start","end","score"}]}

<!-- Pitfall 1: torch wheel tag must be CPU (download.pytorch.org/whl/cpu), NOT CUDA 12.8 default -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /tmp/whisperx-spike-venv, install whisperx + CPU torch wheel, write run_whisperx_align.py, smoke-check Assumptions A1 + A2</name>
  <files>spike/audio/run_whisperx_align.py, /tmp/whisperx-spike-venv/</files>
  <read_first>
    - spike/audio/common.py
    - .planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md §Pattern 4 + §Pitfalls 1, 5
    - .planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md §Assumptions A1 + A2 (the smoke checks that gate this plan)
    - output/虫虫武侠小故事《小江湖》第01话…/transcript.json (verify schema, segment count 155)
  </read_first>
  <action>
**Step 1 — Verify system torch baseline (PRE-install canary):**
```
python3 -c "import torch; print('[canary] system torch:', torch.__version__)"
# Record this version. Post-install, assert unchanged.
```

**Step 2 — Create isolated venv + install WhisperX with CPU torch wheel:**
```
python3 -m venv /tmp/whisperx-spike-venv
/tmp/whisperx-spike-venv/bin/pip install --upgrade pip --quiet
# CRITICAL: install CPU torch wheel FIRST, BEFORE whisperx (Pitfall 1 — prevents pip from pulling CUDA 12.8 wheel)
/tmp/whisperx-spike-venv/bin/pip install --index-url https://download.pytorch.org/whl/cpu torch
/tmp/whisperx-spike-venv/bin/pip install whisperx==3.8.6
```
If the second `pip install` (whisperx) tries to reinstall torch with a different wheel tag, PIN torch first by adding `torch` to a constraints file. The `--index-url` for the torch-only install forces CPU wheel; whisperx will see torch already satisfied and not pull a new wheel.

**Verify A1 + Pitfall 1 mitigation:**
```
/tmp/whisperx-spike-venv/bin/python -c "import torch; print('[venv] torch:', torch.__version__); assert 'cu124' not in torch.__version__ and 'cu128' not in torch.__version__; print('[venv] CPU wheel confirmed')"
python3 -c "import torch; assert '+cu124' in torch.__version__; print('[canary] system torch intact post-venv-install')"
```
If the second assertion fails, ABORT — Pitfall 1 has fired. Restore system torch via `pip install --break-system-packages --force-reinstall torch==2.6.0+cu124 --extra-index-url https://download.pytorch.org/whl/cu124`.

**Step 3 — Write spike/audio/run_whisperx_align.py** (≥100 lines, Chinese docstring):
- Module docstring explains: "Run via /tmp/whisperx-spike-venv/bin/python spike/audio/run_whisperx_align.py — NOT system python (Pitfall 1). Phase 10 DIA-05 spike."
- `import whisperx, json, time, statistics`
- `from common import EP01_VOCALS, EP01_TRANSCRIPT, stratified_sample, write_result, _safe_error`
- `import torch as _system_torch_canary` (system torch probe — should be 2.6.0+cu124 when run with system python; verify it's NOT cu124 when run with venv python — both are fine, but record which)
- Load transcript.json segments; take stratified_sample(segments, n=30, seed=10)
- Load vocals audio: `audio = whisperx.load_audio(str(EP01_VOCALS))` (16kHz mono internally)
- Load align model: `model_a, metadata = whisperx.load_align_model(language_code="zh", device="cpu")` (A1 smoke check — if this fails on CPU, the spike records A1=FAILED and writes a stub result with caveat "WhisperX CPU-mode blocked — DIA-05 auto-defers per fallback rule")
- For each of the 30 sampled segments:
  - Pass the SINGLE segment (not the whole transcript) to `whisperx.align([{"start":s["start"],"end":s["end"],"text":s["text"]}], model_a, metadata, audio, "cpu", return_char_alignments=False)` (A2 smoke check — verifies align accepts arbitrary segments)
  - Extract per-word drift: `aligned_seg = result["segments"][0]; for w in aligned_seg.get("words", []): drift = abs(w["start"] - seg["start"])` etc.
  - Record per_sample: {shot_id (looked up by time-overlap with shots.json OR null + segment_idx), segment_start, segment_end, num_words, word_drifts_ms: [...], max_drift_ms, mean_drift_ms}
- Compute drift_stats across ALL 30 segments:
  - `pct_under_200ms` = fraction of ALL words across 30 segments with |drift| < 200ms
  - `mean_drift_ms` and `median_drift_ms`
- write_result("whisperx_align", "ep01", {sample_size:30, drift_stats, per_sample, methodology:"WhisperX wav2vec2 forced align on existing faster-whisper segments (no re-transcription). Drift = aligned word start - transcript segment start. CPU mode.", caveat:"WhisperX Chinese align drift on 30 stratified ep01 vocals segments. CPU-only — GPU may produce different numerical precision but drift stats are device-independent for this measurement."})
- argparse with `--smoke-only N=3` for fast Wave 1 verification
- ALL exception paths via `_safe_error` (T-10-01 mitigation — WhisperX may emit HF errors if it falls back to HF hub for the wav2vec2 model)

**Step 4 — Smoke run (3 segments, A1/A2 validation):**
```
/tmp/whisperx-spike-venv/bin/python spike/audio/run_whisperx_align.py --smoke-only 3
```
Expect: align model downloads `jonatasgrosman/wav2vec2-large-xlsr-53-chinese-zh-cn` (~1.2GB, first run only), runs align on 3 segments, writes results/whisperx_align_ep01.json with sample_size=3. Validates A1 (CPU works) + A2 (arbitrary segments accepted). If smoke fails, the spike records A1 or A2 as FAILED in the result JSON and exits cleanly with a clear message.
  </action>
  <verify>
    <automated>test -d /tmp/whisperx-spike-venv && \
      /tmp/whisperx-spike-venv/bin/python -c "import whisperx, torch; assert 'cu124' not in torch.__version__ and 'cu128' not in torch.__version__; print('[venv] whisperx', whisperx.__version__, 'torch', torch.__version__)" && \
      python3 -c "import torch; assert '+cu124' in torch.__version__; print('[canary] system torch intact')" && \
      python3 -c "import ast; ast.parse(open('spike/audio/run_whisperx_align.py').read()); print('[wx] parses')" && \
      grep -q "import whisperx" spike/audio/run_whisperx_align.py && \
      grep -q "language_code=\"zh\"" spike/audio/run_whisperx_align.py && \
      echo "[wx] Task 1 GREEN"</automated>
  </verify>
  <acceptance_criteria>
- /tmp/whisperx-spike-venv exists with whisperx 3.8.6 installed.
- Venv torch is CPU wheel (no `cu124`/`cu128` substring in version).
- System torch remains 2.6.0+cu124 (Pitfall 1 mitigation verified).
- run_whisperx_align.py exists, ≥100 lines, parses.
- Imports `whisperx` + common. Hardcodes `language_code="zh"` and `device="cpu"`.
- Smoke run completes (3 segments) and validates A1+A2; if smoke fails, the script writes a stub JSON recording which assumption failed.
  </acceptance_criteria>
  <done>WhisperX isolated-venv setup proven non-contaminating (Pitfall 1 gate). Spike script ready. A1/A2 validated via smoke run.</done>
</task>

<task type="auto">
  <name>Task 2: Run full WhisperX drift spike (30 segments), validate JSON, verify system torch intact, commit</name>
  <files>spike/audio/results/whisperx_align_ep01.json</files>
  <read_first>
    - spike/audio/run_whisperx_align.py (Task 1)
    - spike/audio/tests/results_schema_check.py
  </read_first>
  <action>
**Step 1 — Full run (30 segments):**
```
cd /data/workspace/kais-shot-timeline
/tmp/whisperx-spike-venv/bin/python spike/audio/run_whisperx_align.py
```
Wall-clock target: <15 min on CPU for 30 segments (wav2vec2 align is small; this is NOT re-transcription — Pitfall 5 mitigation).

**Step 2 — Validate JSON + verify canary + commit:**
```
python3 spike/audio/tests/results_schema_check.py && \
  python3 -c "
import json
d = json.load(open('spike/audio/results/whisperx_align_ep01.json'))
assert d['sample_size'] >= 30, f'expected >=30, got {d[\"sample_size\"]}'
assert 'pct_under_200ms' in d['drift_stats']
assert 'mean_drift_ms' in d['drift_stats']
print('[wx] full-run GREEN, pct_under_200ms:', d['drift_stats']['pct_under_200ms'])
" && \
  python3 -c "import torch; assert '+cu124' in torch.__version__; print('[canary] system torch intact post-run')" && \
  ! grep -qE "hf_[a-zA-Z0-9]{20,}" spike/audio/results/whisperx_align_ep01.json && \
  echo "[wx] no token leak" && \
  git add spike/audio/run_whisperx_align.py spike/audio/results/whisperx_align_ep01.json && \
  git commit -m "spike(wx): Phase 10 DIA-05 WhisperX drift on ep01 vocals (CPU isolated venv, n=30)"
```

**Step 3 — Document in SUMMARY (do NOT lock DIA-05 or CUDA decision — that's Plan 06):**
- A1 status: CPU mode worked / failed (and fallback path taken if failed)
- A2 status: align() accepted arbitrary segments / required fresh transcribe
- pct_under_200ms (the gating stat for DIA-05)
- mean_drift_ms, median_drift_ms
- Per-bucket breakdown: drift distribution by segment length (short/med/long/dense)
- System torch post-run version (canary)
- Venv torch version
- Caveat: CPU-only spike; GPU-derived numbers deferred per CONTEXT.md deferred list
- If A1 or A2 FAILED: explicit recommendation for DIA-05 = DEFER + CUDA path = STAY-ON-12.4 (because WhisperX is unusable); Plan 06 records this directly without ambiguity
  </action>
  <verify>
    <automated>python3 -c "
import json
d = json.load(open('spike/audio/results/whisperx_align_ep01.json'))
assert d['sample_size'] >= 30
assert 'pct_under_200ms' in d['drift_stats'] and 'mean_drift_ms' in d['drift_stats']
print('[wx] JSON valid, pct_under_200ms:', d['drift_stats']['pct_under_200ms'])
" && \
      python3 -c "import torch; assert '+cu124' in torch.__version__; print('[canary] cu124 intact')" && \
      ! grep -qE "hf_[a-zA-Z0-9]{20,}" spike/audio/results/whisperx_align_ep01.json && \
      git log --oneline -1 | grep -q "spike(wx)"</automated>
  </verify>
  <acceptance_criteria>
- whisperx_align_ep01.json committed, sample_size ≥30, drift_stats has pct_under_200ms + mean_drift_ms.
- System torch remains 2.6.0+cu124 post-run (Pitfall 1 mitigation durable through full spike).
- No hf_<20+> token patterns in the JSON.
- Commit message format `spike(wx): Phase 10 DIA-05 ...`.
- If A1 or A2 failed: SUMMARY explicitly says "DIA-05 recommendation: DEFER" + the result JSON records the failure mode.
- If A1+A2 both succeeded: SUMMARY records pct_under_200ms + the threshold judgment (≥80% → ship-experimental candidate / <80% → defer candidate) WITHOUT locking — that's Plan 06.
  </acceptance_criteria>
  <done>WhisperX drift spike complete. Plan 06 reads the JSON to compose the WhisperX section + lock DIA-05 + lock CUDA path in PROJECT.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI + download.pytorch.org → /tmp/whisperx-spike-venv | pip install crosses the network — WhisperX + CPU torch wheel downloaded at install time |
| /tmp/whisperx-spike-venv ↔ system python (/usr/bin/python3) | ISOLATION BOUNDARY — venv must NOT write to /usr/lib/python3.12, ~/.local/lib/python3.12, or overwrite system torch (Pitfall 1) |
| HF Hub → venv process | wav2vec2 model `jonatasgrosman/wav2vec2-large-xlsr-53-chinese-zh-cn` downloaded on first align_model load |
| ep01 vocals.wav + transcript.json → whisperx.align() | Read-only file inputs; deterministic segments; no injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | whisperx exception strings (HF auth echo) | mitigate | `_safe_error` regex `hf_*\|token=*\|Bearer *` on all exception paths; HF_TOKEN read from env (never hardcoded); if missing, the wav2vec2 model defaults to anonymous download (it's a public model) |
| T-10-02 | Information Disclosure | whisperx_align_ep01.json | mitigate | grep gate before commit; the JSON contains only drift numbers + segment metadata (no auth context) |
| T-10-03 | Supply Chain | wav2vec2 + WhisperX model weights | mitigate | WhisperX PyPI version pinned 3.8.6; wav2vec2 model is the documented default in DEFAULT_ALIGN_MODELS_HF (canonical ID, no local-checkpoint path) |
| T-10-05 | Tampering | /tmp/whisperx-spike-venv with system-write access | mitigate | venv in /tmp (ephemeral, world-writable but isolated from system python); pip install with --no-cache-dir to prevent cache poisoning; never `--user` install; verify post-install that NO files appeared under /usr/lib/python3.12 or ~/.local/lib/python3.12 (canary: `find ~/.local/lib/python3.12 -name 'whisperx*' -o -name 'torch*' \| head`) |
| T-10-SC | Supply Chain | whisperx==3.8.6 + CPU torch wheel install | mitigate | RESEARCH Package Legitimacy Audit [OK] for whisperx (correct canonical name); torch CPU wheel pinned via `--index-url https://download.pytorch.org/whl/cpu` (official PyTorch CDN); never use `m-bain/whisperx` (SLOP wrong-name per audit) |
| T-10-10 | Availability | network failure during model download | accept | spike fails gracefully with clear error message; can re-run after network restored |
</threat_model>

<verification>
- /tmp/whisperx-spike-venv exists; whisperx 3.8.6 installed; venv torch is CPU wheel (no `cu124`/`cu128`)
- System torch 2.6.0+cu124 intact pre-install, post-install, post-run (3-point canary)
- whisperx_align_ep01.json committed with sample_size ≥30, drift_stats present
- No hf_<20+> token patterns in the JSON
- `find ~/.local/lib/python3.12 -name 'whisperx*'` returns empty (no --user contamination)
</verification>

<success_criteria>
- WhisperX drift JSON is the empirical basis Plan 06 needs to (a) lock DIA-05 ship-experimental / defer AND (b) lock CUDA path (upgrade-to-12.8 vs stay-on-12.4) in PROJECT.md
- Pitfall 1 (torch poisoning) proven absent via 3-point canary
- A1 + A2 (CPU mode + arbitrary-segment align) validated; if either fails, the spike honestly records the failure and recommends defer (no fabrication)
- Isolated-venv pattern documented in SUMMARY for Phase 12+ route-host deployment (if CUDA 12.8 upgrade is chosen later, this venv approach can be retired; otherwise it becomes the production pattern)
</success_criteria>

<output>
Create `.planning/phases/10-risk-validation-spike-route-stub/10-05-SUMMARY.md`. Report: A1/A2 status, pct_under_200ms, mean/median drift, per-bucket drift distribution, system+venv torch versions, fallback path if A1/A2 failed.
</output>
