---
phase: 10-risk-validation-spike-route-stub
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - spike/audio/README.md
  - spike/audio/common.py
  - spike/audio/aggregate_report.py
  - spike/audio/tests/smoke_all.sh
  - spike/audio/tests/route_stub_smoke.sh
  - spike/audio/tests/results_schema_check.py
  - spike/audio/tests/staleness_check.sh
autonomous: true
requirements: [ROUTE-01]
tags: [spike, audio, wave0, smoke-harness]

must_haves:
  truths:
    - "common.py exposes EP01_DIR/EP01_VOCALS/EP01_DRUMS/EP01_BASS/EP01_OTHER path constants pointing at the verified ep01 cached layout (stems/htdemucs/<ep01-stem>/)"
    - "common.py:stratified_sample(segments, n=30, seed=10) returns a deterministic list of (idx, segment) tuples across short/medium/long/dense buckets"
    - "common.py:parse_sensevoice_tags(raw_text) extracts {language,emotion,events,clean_text} from raw SenseVoice output BEFORE rich_transcription_postprocess (Pitfall 2 prevention)"
    - "common.py:_safe_error(msg) redacts hf_/token=/Bearer patterns (Pitfall 6 prevention — T-10-01 mitigation)"
    - "common.py:write_result(model, fixture, payload) stamps {model,fixture,git_sha,timestamp_utc,device:'cpu'} and writes spike/audio/results/<model>_<fixture>.json"
    - "bash spike/audio/tests/smoke_all.sh runs all sub-checks and exits non-zero on any failure"
    - "python3 spike/audio/aggregate_report.py --check-staleness exits 0 when results/ is empty (skeleton; aggregation logic lands in Plan 06)"
  artifacts:
    - path: "spike/audio/common.py"
      provides: "Shared ep01 paths + stratified sample + SenseVoice tag parser + _safe_error regex + write_result stamper"
      min_lines: 80
    - path: "spike/audio/tests/smoke_all.sh"
      provides: "Top-level wave-merge gate runner"
      min_lines: 20
    - path: "spike/audio/tests/route_stub_smoke.sh"
      provides: "3 curl-based ROUTE-01 smoke checks (envelope, 400-on-missing, stub_mode env flip)"
      min_lines: 30
    - path: "spike/audio/tests/results_schema_check.py"
      provides: "Per-model JSON shape validator (asserts model/fixture/per_sample/sample_size fields)"
      min_lines: 40
    - path: "spike/audio/tests/staleness_check.sh"
      provides: "Wrapper around aggregate_report.py --check-staleness"
      min_lines: 10
    - path: "spike/audio/aggregate_report.py"
      provides: "Skeleton with --check-staleness flag; --aggregate stub that Plan 06 fleshes out"
      min_lines: 50
    - path: "spike/audio/README.md"
      provides: "States scripts are THROWAWAY (not pipeline code); how to reproduce the spike"
      min_lines: 30
  key_links:
    - from: "spike/audio/run_ser_sensevoice.py (Plan 03)"
      to: "spike/audio/common.py"
      via: "from common import EP01_VOCALS, stratified_sample, parse_sensevoice_tags, write_result, _safe_error"
      pattern: "from \\.{0,1}common import"
    - from: "spike/audio/tests/smoke_all.sh"
      to: "route_stub_smoke.sh + staleness_check.sh + results_schema_check.py"
      via: "bash sub-script invocation"
      pattern: "bash .*/(route_stub_smoke|staleness_check)\\.sh"
---

<objective>
Build the Wave 0 throwaway-spike foundation: shared `common.py` helper, smoke-check harness, and aggregate-report skeleton that all Wave 1 spike scripts (Plans 03/04/05) import and that the ROUTE-01 stub verification (Plan 02) calls.

Purpose: Phase 10 is a measurement phase, not a build phase. Nyquist Wave 0 requires the test harness exist BEFORE feature work. common.py fixes the ep01 paths + sample so all 4 model spikes run on the SAME 30 segments (Pitfall 9 head-to-head integrity). The smoke harness is the wave-merge gate per VALIDATION.md.

Output: 7 files under `spike/audio/`. NONE of these are pipeline code — `spike/audio/README.md` states they are throwaway reference scripts, NOT wired into `run_pipeline.py` (preserves the `analysis/` = producer-code convention).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-risk-validation-spike-route-stub/10-CONTEXT.md
@.planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md
@./CLAUDE.md

<interfaces>
<!-- Verified ep01 layout (this session, bash ls) -->
ep01 dir name (exact): "虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。"
ep01 dir absolute path: /data/workspace/kais-shot-timeline/output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。
stems layout: stems/htdemucs/<ep01-stem>/{vocals,drums,bass,other}.wav  (verified)
cached intermediates present: shots.json (155 segments), transcript.json, audio_analysis.json, frames.json

<!-- RESEARCH §Code Examples gives the canonical common.py body. Source: 10-RESEARCH.md lines 607-667. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write spike/audio/common.py with ep01 paths + stratified sample + SenseVoice tag parser + _safe_error + write_result</name>
  <files>spike/audio/common.py</files>
  <read_first>
    - .planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md (§Code Examples lines 607-686 — canonical common.py body + parse_sensevoice_tags reference)
    - .planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md (§Pitfalls 2, 6, 9 — tag-mis-parse, HF_TOKEN leak, head-to-head integrity)
    - analysis/call_shot_analysis.py (existing _safe_error regex pattern reference; lines ~115-130)
    - output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/ (ls to confirm path encoding works)
  </read_first>
  <behavior>
    - EP01_DIR: pathlib.Path at the verified absolute ep01 path (string-escape the parentheses + colon as needed)
    - EP01_VOCALS/EP01_DRUMS/EP01_BASS/EP01_OTHER: derived via EP01_DIR / "stems/htdemucs" / EP01_DIR.stem / "<stem>.wav"
    - EP01_SHOTS = EP01_DIR / "shots.json"; EP01_TRANSCRIPT = EP01_DIR / "transcript.json"; EP01_AUDIO_ANALYSIS = EP01_DIR / "audio_analysis.json"
    - stratified_sample(segments, n=30, seed=10): deterministic, 4 buckets (short<2s, medium 2-5s, long>5s, dense>10 chars/sec), max(1, n//4) per bucket, return sample[:n]
    - parse_sensevoice_tags(raw_text): regex `<\|([A-Z_]+)\|>` extracts language (zh/en/yue/ja/ko), emotion (HAPPY/SAD/ANGRY/NEUTRAL/FEARFUL/DISGUSTED/SURPRISED), events (BGM/Speech/Applause/Laughter/Cry/Sneeze/Breath/Cough), clean_text (tags stripped)
    - _safe_error(msg): regex-replace `hf_[a-zA-Z0-9_]+|token=\w+|Bearer\s+\w+` with `[REDACTED]` (extends call_shot_analysis.py:122 pattern)
    - write_result(model, fixture, payload): stamps model/fixture/git_sha/timestamp_utc/device="cpu", writes spike/audio/results/<model>_<fixture>.json with ensure_ascii=False + indent=2
    - git_sha(): subprocess.check_output(["git","rev-parse","--short","HEAD"]) — short-circuits to "unknown" on failure
  </behavior>
  <action>
Create spike/audio/common.py per the body in 10-RESEARCH.md §Code Examples (lines 607-667) with these refinements:
- Module docstring (Chinese per CLAUDE.md) explaining this is THROWAWAY Phase 10 spike code, NOT pipeline code; do not import from run_pipeline.py.
- EP01_DIR uses the exact ep01 dir name (full-width parentheses + colon). Use a raw triple-quoted string literal or explicit unicode escapes — the path must resolve via pathlib.Path.exists().
- stratified_sample: per research, n=30, seed=10 (NOT seed=42 — VALIDATION.md "Spike Reproducibility Invariants" fixes seed=10). Verify the function returns a list of (idx, segment_dict) tuples where idx is the index into the input segments list.
- parse_sensevoice_tags: include the EMOTIONS set (7) and EVENTS set (8) as module-level constants. Default emotion to "emo_unk" if no emotion tag matches.
- _safe_error: source-comment "T-10-01 mitigation — extends call_shot_analysis.py:122 regex with hf_/token=/Bearer patterns per Pitfall 6".
- write_result: ensure the results/ dir exists (mkdir parents=True exist_ok=True) before writing.
- All print statements use the bracketed-stage prefix convention per CLAUDE.md (`[common] ...`).
- Add `# noqa` for any unused import warnings; this is throwaway code.

Do NOT include model loading or inference code — those belong in Plans 03/04/05 spike scripts. common.py is pure helpers.

Validation via behavior tests below MUST pass before committing.
  </action>
  <verify>
    <automated>python3 -c "
import sys; sys.path.insert(0, 'spike/audio')
import common
from pathlib import Path
assert common.EP01_DIR.exists(), f'EP01_DIR not found: {common.EP01_DIR}'
assert common.EP01_VOCALS.exists(), f'vocals.wav missing: {common.EP01_VOCALS}'
assert common.EP01_DRUMS.exists() and common.EP01_BASS.exists() and common.EP01_OTHER.exists()
assert common.EP01_SHOTS.exists() and common.EP01_TRANSCRIPT.exists()
# stratified_sample deterministic + n=30
import json
segs = json.loads(common.EP01_TRANSCRIPT.read_text())['segments']
s1 = common.stratified_sample(segs, n=30, seed=10)
s2 = common.stratified_sample(segs, n=30, seed=10)
assert len(s1) == 30, f'expected 30 got {len(s1)}'
assert [i for i,_ in s1] == [i for i,_ in s2], 'non-deterministic'
# tag parser
tags = common.parse_sensevoice_tags('<|zh|><|HAPPY|><|Speech|><|woitn|>他有一百种方法')
assert tags['language']=='zh' and tags['emotion']=='HAPPY' and 'Speech' in tags['events']
assert tags['clean_text']=='他有一百种方法'
# _safe_error
redacted = common._safe_error('Auth failed: hf_abCdEfGhIjKlMnOpQrStVwXyZ1234567890 token=abc123 Bearer xyz')
assert 'hf_' not in redacted and 'abc123' not in redacted and 'Bearer' not in redacted, f'leak: {redacted}'
print('[common] all behavior checks GREEN')
"</automated>
  </verify>
  <acceptance_criteria>
- File spike/audio/common.py exists, ≥80 lines, Chinese module docstring stating "THROWAWAY Phase 10 spike — NOT pipeline code".
- EP01_DIR / EP01_VOCALS / EP01_DRUMS / EP01_BASS / EP01_OTHER / EP01_SHOTS / EP01_TRANSCRIPT constants all .exists()=True.
- stratified_sample returns exactly 30 deterministic (idx, seg) tuples (same seed → same list).
- parse_sensevoice_tags extracts all 4 tag categories from the test string and strips tags from clean_text.
- _safe_error regex catches hf_/token=/Bearer patterns (no substring survives).
- The behavior verification command above prints "[common] all behavior checks GREEN".
  </acceptance_criteria>
  <done>common.py committed with passing behavior checks. All 4 spike scripts (Plans 03/04/05) can `from common import ...` and rely on the deterministic sample.</done>
</task>

<task type="auto">
  <name>Task 2: Write spike/audio/tests/ smoke harness (4 files)</name>
  <files>spike/audio/tests/smoke_all.sh, spike/audio/tests/route_stub_smoke.sh, spike/audio/tests/results_schema_check.py, spike/audio/tests/staleness_check.sh</files>
  <read_first>
    - .planning/phases/10-risk-validation-spike-route-stub/10-VALIDATION.md (Per-Task Verification Map — defines all smoke checks)
    - .planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md (§Validation Architecture — Wave 0 gap list)
    - spike/audio/common.py (Task 1 output)
  </read_first>
  <action>
Create 4 smoke-check files. Each file uses `#!/usr/bin/env bash` (or python3 shebang) and `set -euo pipefail` (bash). Print `[smoke:<name>] ...` per CLAUDE.md bracketed-stage convention.

**spike/audio/tests/smoke_all.sh** — top-level runner that calls the other 3 in order; exits non-zero on first failure. Honors an env var SKIP_ROUTE_STUB=1 to skip the curl-based check when the route host is not running (Plan 02 task 2 will explicitly start the server and unset SKIP_ROUTE_STUB).

**spike/audio/tests/route_stub_smoke.sh** — 3 curl checks against `${AUDIO_ROUTE_URL:-http://localhost:3000}/api/production/audio-analysis`:
  (1) Happy path: POST `{"video":"/x","shots":"/y"}` Content-Type application/json → `jq '.code'` == 200 AND `jq '.data.stub_mode'` == true.
  (2) Validation: POST `{}` → `jq '.code'` == 400.
  (3) Stub-mode env flip: with AUDIO_ANALYSIS_STUB_MODE env injected server-side, stub_mode stays true; (optional check) document that flipping to "false" without ML loaded returns 501 (the executor skips this check unless the server is configured to flip).
  Exit 0 only if checks 1+2 pass; check 3 is informational (echo result, do not fail).

**spike/audio/tests/results_schema_check.py** — python script that validates every `spike/audio/results/*.json` blob against a per-model schema:
  - ser_sensevoice_*.json: must have model (str), fixture (str), sample_size (int ≥1), per_sample (list of {shot_id, start_sec, end_sec, predicted_emotion}), methodology (str), caveat (str non-empty)
  - mir_mert_*.json + mir_panns_*.json (BOTH use the SAME uniform per_sample entry shape — per-model slice per Plan 10-04 Task 2): must have sample_size (int ≥1), per_sample (list of {shot_id: int, predicted_instruments: list, metric_per_sample: number}), checkpoint (str top-level field). The per_sample entry shape is identical for both JSONs — only the values differ per model.
  - whisperx_align_*.json: must have sample_size (int ≥30), drift_stats ({pct_under_200ms: float, mean_drift_ms: float}), per_sample (list)
  - diarize_*.json (if present): must have DER (float), per_sample (list)
  Exit 0 if all matching files validate, 1 otherwise. If results/ is empty, exit 0 (Wave 0 baseline).

**spike/audio/tests/staleness_check.sh** — wraps `python3 spike/audio/aggregate_report.py --check-staleness` and exits with its status. Single line.

All files chmod +x via the Write tool's default. Smoke harness must NOT depend on any model being installed — that's the whole point of Wave 0.
  </action>
  <verify>
    <automated>bash spike/audio/tests/smoke_all.sh && echo "[smoke_all] PASS (Wave 0 baseline — results/ empty allowed)"</automated>
  </verify>
  <acceptance_criteria>
- 4 files exist under spike/audio/tests/ with executable bit.
- bash spike/audio/tests/smoke_all.sh exits 0 when results/ is empty AND SKIP_ROUTE_STUB=1 is set.
- python3 spike/audio/tests/results_schema_check.py exits 0 when results/ is empty.
- bash spike/audio/tests/staleness_check.sh exits 0 (delegates to aggregate_report.py --check-staleness).
- route_stub_smoke.sh references `${AUDIO_ROUTE_URL:-http://localhost:3000}/api/production/audio-analysis` (NO /v1/ per research note).
  </acceptance_criteria>
  <done>Wave 0 smoke harness committed. Plans 02-06 verification steps can invoke these scripts.</done>
</task>

<task type="auto">
  <name>Task 3: Write spike/audio/aggregate_report.py skeleton + spike/audio/README.md</name>
  <files>spike/audio/aggregate_report.py, spike/audio/README.md</files>
  <read_first>
    - .planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md (§Spike Script Architecture — aggregator role; §Pitfall 10 — staleness gate)
    - spike/audio/common.py (Task 1)
    - spike/audio/tests/smoke_all.sh (Task 2)
  </read_first>
  <action>
**spike/audio/aggregate_report.py** — Python CLI script with argparse:
- `--check-staleness` flag: walks `spike/audio/results/*.json`; for each, reads its `timestamp_utc` and `git_sha`; if ANY result file's git_sha differs from current HEAD (via common.git_sha()), print `WARNING: stale results: <file>` and exit 1. If results/ is empty, exit 0 (Wave 0 baseline). If all files match HEAD or HEAD-unavailable, exit 0.
- `--aggregate` flag (default behavior if no flag): reads all `spike/audio/results/*.json` and prints a placeholder message `"[aggregate] skeleton — Plan 06 fleshes this out; got N result files"` where N is the count. Exit 0.
- Module docstring (Chinese) noting this is the spike aggregator; Plan 06 fills in the markdown-section generation logic.
- Use only stdlib + common.py imports (no model deps).

**spike/audio/README.md** — Chinese-language readme (~30-50 lines):
- States explicitly: "Phase 10 throwaway spike scripts — NOT pipeline code. Do NOT import from run_pipeline.py. Do NOT add to any stage_* function. These exist only to produce .planning/research/audio-spike-report.md."
- Lists the 4 spike scripts that Plans 03/04/05 will add (run_ser_sensevoice.py, run_mir_head_to_head.py, run_whisperx_align.py).
- Documents the stratified_sample n=30 seed=10 invariant ("all 4 spikes run on the SAME 30 segments — head-to-head integrity, Pitfall 9").
- Documents WhisperX isolated-venv requirement ("/tmp/whisperx-spike-venv — Pitfall 1; do NOT install in project env").
- Quickstart: `bash spike/audio/tests/smoke_all.sh` runs all checks.

Both files committed.
  </action>
  <verify>
    <automated>python3 spike/audio/aggregate_report.py --check-staleness && python3 spike/audio/aggregate_report.py --aggregate && head -5 spike/audio/README.md | grep -c "Phase 10\|throwaway\|THROWAWAY"</automated>
  </verify>
  <acceptance_criteria>
- aggregate_report.py exists with argparse, supports --check-staleness and --aggregate flags.
- `python3 aggregate_report.py --check-staleness` exits 0 when results/ is empty.
- `python3 aggregate_report.py --aggregate` exits 0 with placeholder output mentioning Plan 06.
- README.md exists, first 5 lines contain "Phase 10" or "throwaway" (≥1 match).
- README.md mentions n=30 seed=10 + WhisperX isolated venv path.
  </acceptance_criteria>
  <done>Wave 0 foundation complete. Plan 06 will edit aggregate_report.py to add real aggregation logic; README documents the throwaway invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→spike scripts | ep01 cached files read by common.py path constants — paths are hardcoded absolutes, not user-supplied |
| spike results→committed git | results JSON blobs may contain model output that could include HF auth-token echo (Pitfall 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | common.py:_safe_error | mitigate | regex `hf_[a-zA-Z0-9_]+\|token=\w+\|Bearer\s+\w+` applied to all error messages before any print/write (extends call_shot_analysis.py:122 pattern) |
| T-10-02 | Information Disclosure | results JSON files | mitigate | write_result stamps device='cpu' + git_sha; commit-time grep gate in Plan 06 catches residual token patterns |
| T-10-05 | Tampering | common.py:git_sha subprocess | accept | `subprocess.check_output(["git","rev-parse",...])` — only invokes system git on repo HEAD; no untrusted input |
| T-10-SC | Supply Chain | (this plan installs no packages) | accept | Wave 0 introduces zero new deps; only stdlib + existing project deps used |
</threat_model>

<verification>
- `python3 -c "import sys; sys.path.insert(0,'spike/audio'); import common; common.stratified_sample([], n=30)"` does not raise
- `bash spike/audio/tests/smoke_all.sh` exits 0 with SKIP_ROUTE_STUB=1
- All 7 files under spike/audio/ exist and committed to git
- `git diff --stat HEAD~1 HEAD` shows only spike/audio/* additions (no pipeline files touched)
</verification>

<success_criteria>
- common.py exposes deterministic ep01 paths + stratified sample + SenseVoice tag parser + safe_error + result writer
- 4 smoke-check scripts runnable independently + as a suite via smoke_all.sh
- aggregate_report.py skeleton works; --check-staleness is the wave-merge gate per VALIDATION.md
- README.md makes the throwaway invariant unambiguous (no future contributor wires these into run_pipeline.py)
</success_criteria>

<output>
Create `.planning/phases/10-risk-validation-spike-route-stub/10-01-SUMMARY.md` when done. Report: file count, behavior-check pass status, any deviations from the planned helper signatures.
</output>
