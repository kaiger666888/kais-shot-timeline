---
phase: 12-producer-route-client-call-audio-analysis-py
verified: 2026-07-26T00:45:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: N/A
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 12: Producer Route Client (call_audio_analysis.py) Verification Report

**Phase Goal:** Producer-side thin httpx client hits the Phase 10 ROUTE-01 stub per-shot + normalizes into audio_semantic.json (Phase 11 schema) + per-shot 4-tuple cache + poisoned-cache invalidation + [audio] warnings sidecar merge + graceful-degrade + stub-only round-trip proof.
**Verified:** 2026-07-26T00:45:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (4 Success Criteria from ROADMAP.md)

| #   | Truth (ROADMAP SC)                                                                                                                                                                                                                                                                                     | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  1   | `call_audio_analysis.py` POSTs per-shot `{video, shots, shot_id_range, stems_dir, models, language}` to the documented route contract and normalizes the 3-modality response into `audio_semantic.json` (route contract documented per ROUTE-03)                                                         | ✓ VERIFIED | Smoke Scenario 1 PASS: route-up + nonempty fixture → `audio_semantic.json` written + schema-valid (Draft202012Validator) + cache file `shot_001.json` written + zero `[audio]` warnings. `ROUTE_PATH = "/api/production/audio-analysis"` (NO `/v1/`) hardcoded 1× (analysis/call_audio_analysis.py:120). `call_route` POSTs via `client.post(ROUTE_PATH, json=body)` (line 352). `normalize_audio_semantic` (line 162-326) projects dialogue/sfx/reproduction with isinstance guards. CLI surfaces all 6 documented body fields (lines 487-515). Nonempty fixture schema-validates when `data.shots[]` lifted to top-level.                                                                                                                                       |
  2   | Per-shot cache (4-tuple key: `video_content_hash`/`route_name`/`route_version`/implicit `shot_id`) hits on re-run; poisoned cache (schema-validate fail) auto-invalidates and re-fetches (mirror `call_reid.py:418-427`)                                                                                 | ✓ VERIFIED | Smoke Scenarios 2 + 3 PASS. Scenario 2: re-run `--offline` produces byte-identical `audio_semantic.json` (deterministic cache read); "shot 1: cache hit" logged. Scenario 3: cache corrupted (`word.start=-5` violates schema `minimum:0`) → "invalidated poisoned cache" logged + cache file unlinked + `[audio]` warning appended. `_cache_key` 3-tuple `{video_content_hash, route_name, route_version}` at analysis/call_audio_analysis.py:449-451 + shot_id in filename `shot_{sid:03d}.json` = 4-tuple per PIPE-02. Poisoned invalidation fires via `validator.iter_errors(probe_payload)` on cache hit (line 657) AND pre-write (line 760).                                                                                                                              |
  3   | Graceful-degrade: route unreachable / preflight fail / `--offline` → `audio_semantic.json` not written (byte-identical to v1.1 asset) AND `[audio]` warning appended to sidecar WITHOUT clobbering existing `[semantic]`/`[reid]` tags (read-merge-write per `call_reid.py:443-449`)                    | ✓ VERIFIED | Smoke Scenarios 4 + 5 PASS. Scenario 4: empty stub (`stub_mode:true`) + pre-populated `[semantic]`/`[reid]` sidecar → `audio_semantic.json` absent (CONTRACT-05) + `[audio]` appended + both prior tags preserved. Scenario 5: `--offline --force` + empty cache → `audio_semantic.json` absent + `[audio]` warning appended. `has_any_data` gate at line 734-738 drives `write_payload=None` (line 747); atomic write skipped when `write_payload is None` (line 759). Read-merge-write at lines 799-803: `prior = [w for w in existing_warnings if not w.startswith(STEP_TAG)]` preserves cross-step tags; `tagged_audio` prepends fresh `[audio]` warnings. `STEP_TAG = "[audio]"` self-dedup (1× match, line 799).                                                                              |
  4   | Stub-only round-trip (Phase 10 stub, models not loaded) confirms envelope parsing + cache write + warnings merge work BEFORE route ML is live                                                                                                                                                          | ✓ VERIFIED | `bash tests/run_audio_analysis_smoke.sh` exits 0 with `ALL_SCENARIOS_PASS` — all 5 scenarios green against a frozen-fixture stub mirroring the Phase 10 ROUTE-01 envelope byte-for-byte (empty fixture is byte-snapshot of 10-02-SUMMARY.md:166). Empty fixture `{"code":200,"data":{"shots":[],"count":0,"errors":[],"stub_mode":true,...}}` parses cleanly → graceful-degrade → no crash. Task 2 (12-02-SUMMARY.md:139-186) documents a LIVE cross-repo cross-check on PORT 10595 against `feat/audio-analysis-route` worktree — producer client ran cleanly, exit 0, audio_semantic absent, `[audio]` warning with `stub_mode:true` diagnostic. Local deterministic proof (Task 1) is the SC#4 baseline; Task 2 is supplementary confirmation. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact                                                    | Expected                                                                                                                                                                                | Status     | Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `analysis/call_audio_analysis.py`                           | Thin httpx producer client: per-shot POST + envelope normalize + schema-validate + 4-tuple cache + poisoned invalidation + graceful-degrade + [audio] warnings read-merge-write + CLI | ✓ VERIFIED | 827 lines (> min_lines:380). Contains `ROUTE_PATH = "/api/production/audio-analysis"` (1×), `ROUTE_NAME = "audio_analysis"`, `def normalize_audio_semantic`, `def call_route`, `STEP_TAG = "[audio]"` (1×), `Draft202012Validator` (5× uses). All 5 helpers + 3 constants + `main()` import cleanly. Syntax valid (`ast.parse`). Case-insensitive grep for `instrument` returns 0 matches (MUS-04 lock honored). Wired via run_pipeline.py:step_audio_semantic (Phase 14, deferred — not a Phase 12 gap). |
| `tests/audio_analysis_stub_server.py`                       | Minimal http.server stub returning frozen fixture on POST `/api/production/audio-analysis`                                                                                              | ✓ VERIFIED | 121 lines (> min_lines:35). Contains `do_POST`, `do_GET` (405), `audio-analysis` ROUTE_PATH, explicit `Content-Length`, `application/json`. HTTP/1.1 to avoid Python 3.12 send_error empty-reply bug (documented deviation in 12-02-SUMMARY). Wired via `tests/run_audio_analysis_smoke.sh`.                                                                                                                                                                                                                              |
| `tests/fixtures/audio_analysis_stub_response_empty.json`    | Byte-snapshot of Phase 10 stub response (shots:[], stub_mode:true)                                                                                                                       | ✓ VERIFIED | 1 line, byte-identical to 10-02-SUMMARY.md:166 snapshot. Contains `stub_mode`, `shots`, `code`. Verified by `diff` against Phase 10 stub envelope (per 12-02-SUMMARY).                                                                                                                                                                                                                                                                                                                                                    |
| `tests/fixtures/audio_analysis_stub_response_nonempty.json` | Shape-correct non-empty envelope (1 shot, dialogue/sfx/reproduction)                                                                                                                    | ✓ VERIFIED | 36 lines. Contains `dialogue`, `emotion`, `words`, `reproduction`. `data.shots[0]` lifted to top-level schema-validates against `audio_semantic.schema.json` (Draft202012Validator) — independently re-verified during this audit.                                                                                                                                                                                                                                                                                          |
| `tests/run_audio_analysis_smoke.sh`                         | Bash smoke orchestration: 5 scenarios + cleanup                                                                                                                                         | ✓ VERIFIED | 226 lines (> min_lines:80). Contains `audio_analysis_stub_server.py`, `audio_semantic.json`, `SCENARIO`, `exit 1`. Independently executed during this verification: `ALL_SCENARIOS_PASS` + exit 0. Links client ↔ stub_server ↔ fixtures via subprocess + fixture flag.                                                                                                                                                                                                                                                  |

### Key Link Verification

| From                                | To                                                    | Via                                                                            | Status     | Details                                                                                                                                                                                                                                                                                                                                                       |
| ----------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `analysis/call_audio_analysis.py`   | `POST /api/production/audio-analysis`                 | `httpx.Client.post(ROUTE_PATH, json=body)`                                     | ✓ WIRED   | Pattern `client\.post\(ROUTE_PATH` matched at line 352. Response handling: `raise_for_status` + `resp.json()` + envelope isinstance guards + `code!=200` guard + `data.shots[0]` extraction. Broadened except `(httpx.HTTPError, ValueError, AttributeError, TypeError, KeyError)` (line 386-394). Tested by Scenario 1.                                       |
| `analysis/call_audio_analysis.py`   | `spec/schemas/audio_semantic.schema.json`             | Draft202012Validator pre-write + poisoned-cache probe                          | ✓ WIRED   | Pattern `Draft202012Validator.*AUDIO_SEMANTIC_SCHEMA` resolved at lines 619-623 (validator construction). Used at line 657 (poisoned-cache probe on hit) + line 760 (pre-write payload validation). AUDIO_SEMANTIC_SCHEMA.exists() = True. Tested by Scenarios 1 (schema-valid) + 3 (schema-invalid → unlink).                                                  |
| `analysis/call_audio_analysis.py`   | `output/<video>/route_cache/warnings.json`            | Read-merge-write with `[audio]` STEP_TAG (preserves `[semantic]`/`[reid]`)     | ✓ WIRED   | Pattern `STEP_TAG = "\[audio\]"` matched 1× at line 799. Read at lines 585-592 (existing_warnings extracted from sidecar JSON). Write at lines 804-811 (`json.dump({"warnings": all_warnings}, ...)`. Self-dedup strips prior `[audio]` tags; cross-step tags preserved by `not w.startswith(STEP_TAG)` filter. Tested by Scenario 4 (cross-tag preservation). |
| `tests/run_audio_analysis_smoke.sh` | `analysis/call_audio_analysis.py`                     | subprocess invocation with `--route-url http://127.0.0.1:<port>/...`           | ✓ WIRED   | Pattern `call_audio_analysis\.py` resolved via `CLIENT="${REPO_ROOT}/analysis/call_audio_analysis.py"` + `python3 "${CLIENT}" ...` (lines 40, 50).                                                                                                                                                                                                            |
| `tests/run_audio_analysis_smoke.sh` | `tests/audio_analysis_stub_server.py`                 | background start with `--fixture` + `--port`; kill on EXIT trap                | ✓ WIRED   | Pattern `audio_analysis_stub_server\.py` resolved via `STUB="${REPO_ROOT}/tests/audio_analysis_stub_server.py"` + `python3 -u "${STUB}" --fixture ... --port ... &` (lines 41, 125, 196). Cleanup via `trap cleanup EXIT` (line 37).                                                                                                                            |
| `tests/audio_analysis_stub_server.py` | `tests/fixtures/audio_analysis_stub_response_*.json` | reads fixture file → returns as response body on POST                          | ✓ WIRED   | Pattern `fixture` resolved via `--fixture` argparse (line 91) + `_FIXTURE_BYTES = fixture_path.read_bytes()` (line 108) + `body = _FIXTURE_BYTES` served on POST (line 56).                                                                                                                                                                                   |

### Data-Flow Trace (Level 4)

| Artifact                            | Data Variable          | Source                                | Produces Real Data           | Status     |
| ----------------------------------- | ---------------------- | ------------------------------------- | ---------------------------- | ---------- |
| `analysis/call_audio_analysis.py`   | `shots_out`            | route response `data.shots[0]` via `call_route` → `normalize_audio_semantic` | Yes (route fixture) or skeleton (degrade) | ✓ FLOWING |
| `analysis/call_audio_analysis.py`   | `words_present`        | per-shot `dialogue.words[]` non-empty check (line 719-722)             | Yes — derived from normalized data        | ✓ FLOWING |
| `analysis/call_audio_analysis.py`   | `existing_warnings`    | `route_cache/warnings.json` (read at line 585-592)                     | Yes — read from sidecar                   | ✓ FLOWING |
| `analysis/call_audio_analysis.py`   | `SCHEMA_VERSION`       | lazy-import from `scripts/export_asset.py` via `importlib` (lines 537-555) | Yes (CONTRACT-03 single-source)           | ✓ FLOWING |
| `analysis/call_audio_analysis.py`   | `audio_warnings`       | per-shot error paths + preflight + degrade messages                    | Yes — generated from real error paths     | ✓ FLOWING |

No hardcoded empty data flowing to rendering. `shots_out` is built from real route responses (Scenario 1) or schema-valid skeletons (Scenarios 4/5 degrade — by design).

### Behavioral Spot-Checks

| Behavior                                                                        | Command                                                                                                                                                                    | Result                                                                              | Status  |
| ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------- |
| Smoke harness all 5 scenarios pass                                              | `bash tests/run_audio_analysis_smoke.sh`                                                                                                                                   | `ALL_SCENARIOS_PASS`, exit 0                                                        | ✓ PASS  |
| Client CLI parses cleanly                                                        | `python3 -c "import ast; ast.parse(open('analysis/call_audio_analysis.py').read())"`                                                                                       | `syntax OK` (no error)                                                              | ✓ PASS  |
| All public symbols importable                                                    | `python3 -c "from analysis.call_audio_analysis import main, normalize_audio_semantic, call_route, preflight, video_content_hash, _safe_error, ROUTE_PATH, ROUTE_NAME, AUDIO_SEMANTIC_SCHEMA"` | exit 0                                                                              | ✓ PASS  |
| ROUTE_PATH no /v1/ + hardcoded single source                                    | `grep -c 'ROUTE_PATH = "/api/production/audio-analysis"'`                                                                                                                  | `1`                                                                                 | ✓ PASS  |
| Instruments ABSENT (MUS-04 lock)                                                | `! grep -ni 'instrument' analysis/call_audio_analysis.py`                                                                                                                  | 0 matches                                                                           | ✓ PASS  |
| `_safe_error` redacts user:pass@ auth                                           | `_safe_error('connect to http://user:pass@host/path failed')`                                                                                                              | `connect to http://***@host/path failed` (no `user:pass` leak)                      | ✓ PASS  |
| `normalize_audio_semantic(None, shot_meta)` returns schema-valid skeleton        | direct call with None                                                                                                                                                       | `{'shot_id':5,'start_sec':1.0,'end_sec':2.0,'duration':1.0}`                        | ✓ PASS  |
| Nonempty fixture schema-validates (Draft202012Validator)                        | lift `data.shots[]` to top-level, validate against schema                                                                                                                  | exit 0 (no errors)                                                                  | ✓ PASS  |
| Empty fixture is byte-snapshot of Phase 10 stub                                 | inspect keys + stub_mode                                                                                                                                                   | `{'code':200,'data':{'shots':[], ..., 'stub_mode':True}}`                           | ✓ PASS  |

### Probe Execution

| Probe                              | Command                                  | Result                                    | Status |
| ---------------------------------- | ---------------------------------------- | ----------------------------------------- | ------ |
| `tests/run_audio_analysis_smoke.sh` | `bash tests/run_audio_analysis_smoke.sh` | exit 0, `ALL_SCENARIOS_PASS` (5/5 green) | PASS   |

### Requirements Coverage

| Requirement | Source Plan    | Description                                                                                                                                                                                                                       | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ROUTE-02    | 12-01-PLAN.md  | 路由托管 SenseVoice (SER+events) + WhisperX (transcribe+align+diarize) + MERT/librosa/PANNs (MIR)；**CUDA 12.8 升级是 Phase 1 前置**（WhisperX 硬需）                                                                                  | ✓ SATISFIED (producer slice) / DEFERRED (route-host ML) | **Producer-side slice delivered:** CLI `--models` flag documents the canonical SER/ASR/MIR model IDs (`iic/SenseVoiceSmall` / `large-v3` / `m-aap/MERT-v1-95M`), surfaced in the POST body per Phase 10 contract. **Route-host ML loading explicitly deferred** per CONTEXT.md `deferred` section: "Live ML round-trip (route host loading SenseVoice/WhisperX/MERT/PANNs) — Phase 12 proves the stub envelope round-trip; live ML is post-merge smoke." Task 2 live cross-check confirmed producer client integrates cleanly with the actual `feat/audio-analysis-route` stub host (PORT 10595, exit 0, byte-identical envelope). **Not a Phase 12 gap** — verification_focus explicitly acknowledges this deferral. |
| ROUTE-03    | 12-01-PLAN.md  | 路由 request/response 契约文档化（per-shot 输入：vocals→SER/diarize，drums+bass+other→MIR；输出：dialogue/music/sfx 子对象）                                                                                                            | ✓ SATISFIED | Producer-client docstring (analysis/call_audio_analysis.py:11-35) documents the full route contract: REQUEST body schema `{video, shots, shot_id_range, stems_dir, models, language}` + RESPONSE envelope `{code:200, data:{shots[], count, errors, stub_mode, message}, message}`. ROUTE-03 cross-repo documentation is the Phase 10 deliverable (10-02-SUMMARY.md); Phase 12 reflects the contract slice the producer depends on. Nonempty fixture (`tests/fixtures/audio_analysis_stub_response_nonempty.json`) is a frozen snapshot of the documented response shape.                                                                                                                                         |
| PIPE-02    | 12-01-PLAN.md, 12-02-PLAN.md | per-shot cache（4-tuple key: video_content_hash/route_name/route_version/+shot_id 隐含于文件名）+ poisoned-cache invalidation + read-merge-write `[audio]` warnings sidecar | ✓ SATISFIED | All three PIPE-02 sub-features verified end-to-end by smoke scenarios: (1) 4-tuple cache key — `_cache_key` 3-tuple in cache JSON + shot_id in `shot_{sid:03d}.json` filename; (2) poisoned-cache invalidation — Scenarios 3 + the pre-write schema gate unlink offending cache files (lines 657-667, 760-780); (3) read-merge-write `[audio]` warnings sidecar — STEP_TAG self-dedup + cross-step `[semantic]`/`[reid]` preservation proven by Scenario 4.                                                                                                                                                                                                                                                |

**Orphaned requirements:** None. REQUIREMENTS.md table maps exactly ROUTE-02, ROUTE-03, PIPE-02 to Phase 12 (lines 107-130). All three are claimed by 12-01-PLAN.md / 12-02-PLAN.md `requirements:` frontmatter and resolved above.

### Anti-Patterns Found

| File                                      | Line | Pattern                                                | Severity | Impact                                                                                                                                                                                                                                |
| ----------------------------------------- | ---- | ------------------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `analysis/call_audio_analysis.py`         | 70   | `shot_XXX.json` in docstring (cache filename narrative) | ℹ️ Info  | Not a debt marker — `XXX` here is prose shorthand for the `{sid:03d}` shot-number template (`shot_001.json`, `shot_002.json`, ...), not an `XXX` debt comment. No action needed.                                                       |
| `tests/audio_analysis_stub_server.py`     | —    | Test-only stub (no production stub flags)              | ℹ️ Info  | All stubs are intentional test fixtures (frozen snapshots of Phase 10 envelope), not production stubs. Empty array `shots:[]` in empty fixture is the Phase 10 stub_mode:true contract — by design, not a code stub. |

No `TBD`/`FIXME`/`TODO`/`HACK`/`PLACEHOLDER`/`coming soon`/`not yet implemented` markers in any Phase 12 file. No empty implementations, no console.log-only handlers, no hardcoded empty data flowing to render.

### Human Verification Required

None. Phase 12 is pure backend Python (httpx client + smoke harness) with deterministic,automatable behavior. All four Success Criteria are proven by:

1. **Deterministic smoke harness** (`tests/run_audio_analysis_smoke.sh`, 5/5 scenarios PASS) — covers every code path in the client (route-up, cache-hit, poisoned-invalidate, full-degrade, offline-empty).
2. **Behavioral spot-checks** (9/9 PASS above) — syntax, imports, ROUTE_PATH invariant, MUS-04 grep invariant, `_safe_error` auth redaction, skeleton normalize, schema-validity of nonempty fixture, byte-identity of empty fixture.
3. **Task 2 live cross-repo cross-check** (12-02-SUMMARY.md:139-186) — the only `checkpoint:human-verify` task; the executor already performed it LIVE (not graceful-skip). Producer client ran against the actual `feat/audio-analysis-route` stub on PORT 10595: exit 0, audio_semantic absent, `[audio]` warning with `stub_mode:true` diagnostic. The SUMMARY thoroughly documents the live cross-check (curl output + producer-client log + 5-criterion table). Since Task 1 already deterministically proves SC#4, the truth status does not depend on re-running Task 2.

The deferred ROUTE-02 route-host ML loading is acknowledged in CONTEXT.md `deferred` and is not a Phase 12 gap. No items remain that require human visual / real-time / external-service testing beyond what the executor already performed for Task 2.

### Gaps Summary

**No gaps found.** All 4 Success Criteria verified by deterministic smoke + behavioral spot-checks. All 3 requirements (ROUTE-02 producer-slice, ROUTE-03, PIPE-02) satisfied. ROUTE-02 route-host ML loading is an explicit CONTEXT.md deferral (post-merge smoke), not a Phase 12 gap — confirmed by Task 2's live cross-check proving the producer client integrates cleanly with the stub host today.

**Key strengths:**
- **Zero stubs in production code.** `audio_semantic.json` is built entirely from (a) cached route responses, (b) live route responses, or (c) absent entirely (byte-identical v1.1 asset on full degrade). The Phase 10 ROUTE-01 stub on the route host side is documented separately and is not a Phase 12 stub — the client correctly handles `stub_mode:true` as a graceful-degrade signal.
- **Defense-in-depth on every error path.** `_safe_error` regex on every warning string (T-12-04), broadened except clause covering httpx + ValueError + AttributeError + TypeError + KeyError (T-12-05), isinstance guards at every envelope level (CR-02), schema-validate both on cache hit AND pre-write (T-12-02).
- **CONTRACT-03 single-source honored.** `SCHEMA_VERSION` lazy-imported via `importlib.util.spec_from_file_location` (lines 537-555); the literal `"1.2"` appears only as a documented fallback with explanatory comment (line 553).
- **Non-destructive cross-step merge.** Scenario 4 empirically proves `[audio]` coexists with `[semantic]` + `[reid]` in `route_cache/warnings.json` after re-run — PIPE-02 read-merge-write contract honored.

**Deviations from plan (all auto-fixed, none scope-creep):**
1. Scenario 3 corruption changed from `emotion=123` (silently dropped by normalize isinstance guard → never triggers unlink) to `word.start=-5` (passes type guard, fails schema `minimum:0` → actually triggers unlink). Plan bug fix.
2. Stub server HTTP/1.1 + explicit Content-Length (Python 3.12 `send_error` empty-reply bug). Plan bug fix.
3. REPO_ROOT path depth off-by-one (`tests/` is 1 level deep, not 2). Plan bug fix.
4. Stub ports switched from 10591/10592 to 10593/10594 (pre-existing `kontext-service.py` PID 5574 on 10591). Rule 3 — do NOT kill unrelated service.

All 4 deviations are documented in `12-02-SUMMARY.md:198-264` with rule classifications + verification evidence.

---

_Verified: 2026-07-26T00:45:00Z_
_Verifier: Claude (gsd-verifier)_
