---
phase: 13-speaker-01-linkage-hitl
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - tests/fixtures/speaker_edits_phase13_smoke.json
  - scripts/verify_phase13_smoke.sh
autonomous: true
requirements: [SPEAKER-01, SPEAKER-02, DIA-02, DIA-03]
tags: [end-to-end-proof, sc5-round-trip, idempotency-proof, xss-regression, smoke-harness, fixture]

must_haves:
  truths:
    - "Running `bash scripts/verify_phase13_smoke.sh` proves the full SPEAKER-01 HITL round-trip on the v1.2 fixture set: audio_semantic.json + characters.json → gen_speaker_review.py HTML → frozen speaker-edits.json (operator decision surrogate) → link_speakers.py → speakers.json; ALL assertions pass; script prints PHASE13_ROUND_TRIP_PASS and exits 0"
    - "The round-trip output speakers.json schema-validates against spec/schemas/speakers.schema.json (Draft202012Validator) AND passes _producer_registry_integrity (Plan 01 Task 2 extension) — proving both the link_speakers gate AND the producer second-line defense accept the canonical shape"
    - "Idempotency proven: re-applying the same speaker-edits.json to the same inputs produces byte-identical speakers.json (sha256 match across 3 runs — Pitfall 5 deterministic-apply guard)"
    - "Confirmed-only gate proven: a 'reject_ids' edit on a real spk_id in audio_semantic.json produces speakers.json WITHOUT that spk_id in the confirmed set (the rejected ID stays in canonical ONLY if link_speakers' hard gate would include it — which it MUST NOT; reject_ids → review_state='rejected' → hard-gate skip)"
    - "DIA-02 + DIA-03 exercised end-to-end: input audio_semantic.json carries diarization output (dialogue.spk_id from Phase 12 WhisperX); operator HITL produces link_mappings (DIA-03 speaker→character); canonical speakers.json carries the resolved char_id per the operator's confirm+link decisions"
    - "XSS regression: poison a copy of audio_semantic.json with '<script>' payloads in spk_id / dialogue.text / char name; regen HTML; grep the output for raw '<script>' returns 0 matches (the _esc layer neutralizes all attack vectors)"
  artifacts:
    - path: "tests/fixtures/speaker_edits_phase13_smoke.json"
      provides: "Frozen representative speaker-edits.json documenting the operator's HITL decisions on the v1.2 fixture speakers (confirm spk_001 + spk_002; link spk_001→char_001; reject spk_003 if smoke augments a third). Schema-validates against speaker-edits.schema.json. Documents the SC#5 operator decision shape."
      contains: ["confirm_ids", "link_mappings", "spk_001", "char_001"]
    - path: "scripts/verify_phase13_smoke.sh"
      provides: "Bash smoke orchestration running the full SPEAKER-01 HITL round-trip on v1.2 fixtures (audio_semantic + characters → gen_speaker_review HTML → frozen speaker-edits → link_speakers → speakers.json) + idempotency proof + reject_ids gate proof + XSS regression + producer integrity extension proof. Prints PHASE13_ROUND_TRIP_PASS on success, exits non-zero with diff/grep evidence on any failure."
      min_lines: 130
      contains: ["gen_speaker_review.py", "link_speakers.py", "PHASE13_ROUND_TRIP_PASS", "_producer_registry_integrity", "SCENARIO", "exit 1"]
  key_links:
    - from: "scripts/verify_phase13_smoke.sh"
      to: "html/gen_speaker_review.py"
      via: "subprocess invocation on v1.2 audio_semantic + characters fixtures → generates HITL HTML"
      pattern: "gen_speaker_review\\.py"
    - from: "scripts/verify_phase13_smoke.sh"
      to: "registry/link_speakers.py"
      via: "subprocess invocation applying tests/fixtures/speaker_edits_phase13_smoke.json to v1.2 fixture inputs → canonical speakers.json"
      pattern: "link_speakers\\.py"
    - from: "scripts/verify_phase13_smoke.sh"
      to: "scripts/verify_contract.py:_producer_registry_integrity"
      via: "python -c invocation on the round-trip output dir → asserts producer-side second-line defense accepts canonical shape"
      pattern: "_producer_registry_integrity"
    - from: "scripts/verify_phase13_smoke.sh"
      to: "tests/fixtures/speaker_edits_phase13_smoke.json"
      via: "passes --edits flag to link_speakers.py"
      pattern: "speaker_edits_phase13_smoke\\.json"
---

<objective>
Prove SC#5 (ROADMAP Phase 13): the full SPEAKER-01 HITL round-trip works end-to-end on the v1.2 fixture set. This plan produces NO production code — it delivers a frozen operator-decision fixture + a bash smoke harness that exercises: audio_semantic.json + characters.json → gen_speaker_review.py HTML (Plan 02) → frozen speaker-edits.json (operator surrogate) → link_speakers.py (Plan 01) → speakers.json (canonical). Plus idempotency proof + confirmed-only gate proof + XSS regression + producer-side integrity acceptance proof.

Purpose: Phase 13 closes the v1.1 SPEAKER-01 deferral. Plan 01 built the gate (link_speakers.py + producer integrity extension); Plan 02 built the UI (gen_speaker_review.py); this plan proves they COMPOSE correctly on the canonical v1.2 fixture — the same composition Phase 14 will wire into run_pipeline.py. The smoke harness becomes the regression gate for future Phase 14/15/16 changes (any drift in the canonical shape or the schemas will break the assertions).

Output: 2 test artifacts (frozen speaker-edits fixture + smoke script). No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-speaker-01-linkage-hitl/13-CONTEXT.md
@.planning/phases/13-speaker-01-linkage-hitl/13-01-PLAN.md
@.planning/phases/13-speaker-01-linkage-hitl/13-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Plan 01 + 02 produced the production code; Plan 03 wires the e2e proof. -->

## Plan 01 deliverable: registry/link_speakers.py (confirmed-only apply gate)

CLI: `python3 registry/link_speakers.py --audio-semantic <a> --characters <c> --edits <e> --work-dir <w> --output <o>`
Exit codes: 0 success; non-zero = schema validation fail / confirmed-only gate violation / dangling char_id / orphan link_mapping.
Key behaviors:
  - Draft reconstruction: aggregates spk_ids from audio_semantic.json#shots[].dialogue.spk_id
  - Apply order: merge → split → confirm/reject → link_mapping (LOCKED)
  - HARD GATE: only confirmed speakers land in speakers.json
  - Idempotent: sorted iteration + _next_speaker_id max+1
  - char_id resolution: non-null MUST resolve to confirmed characters.json#id

## Plan 01 Task 2 deliverable: _producer_registry_integrity extension

Importable from scripts.verify_contract. Returns list[str] of failures ([] = clean). For Plan 03, the smoke calls:
    from scripts.verify_contract import _producer_registry_integrity
    from pathlib import Path
    failures = _producer_registry_integrity(Path(work_dir))
    assert failures == []

## Plan 02 deliverable: html/gen_speaker_review.py (HITL review HTML)

CLI: `python3 html/gen_speaker_review.py --audio-semantic <a> --characters <c> --shots <s> --output <o>`
Key behaviors:
  - Generates self-contained HTML with speaker cards sorted by shot_count desc
  - Character dropdown filtered to confirmed (Pitfall 7 upstream gate)
  - Export-edits button → speaker-edits.json (schema-validates against speaker-edits.schema.json)
  - _esc() XSS defense on all dynamic strings

## v1.2 fixture inputs (the round-trip source — Phase 11 frozen)

- spec/fixtures/v1.2/audio_semantic.json (57 lines) — 2 shots, dialogue.spk_id = spk_001 (shot 1) + spk_002 (shot 2)
- spec/fixtures/v1.2/characters.json (28 lines) — 2 confirmed chars (char_001 少女, char_002 路人)
- spec/fixtures/v1.2/shots.json (14 lines) — 2 shots
- spec/fixtures/v1.2/speakers.json (22 lines) — CANONICAL post-apply shape (reference for assertions: spk_001→char_001 confirmed, spk_002→null confirmed)

## Existing smoke pattern (the bash orchestration style to follow)

- spike/audio/tests/route_stub_smoke.sh (Phase 10) — bash pattern: background server, curl probes, trap cleanup, exit-on-assert-fail.
- tests/run_audio_analysis_smoke.sh (Phase 12 Plan 02) — closest analog: starts stub server, runs the producer client across 5 scenarios, ALL_SCENARIOS_PASS marker, trap cleanup.
- scripts/verify_phase8_smoke.py (Phase 8) — python smoke pattern calling _producer_registry_integrity on a synthetic workdir. Useful for the producer-integrity assertion block.

Plan 03's smoke is structurally simpler than Phase 12's (no server — both Plan 01 + Plan 02 CLIs are file-in / file-out). It is closer to a shell driver of subprocess.run + assertion greps.

## Frozen operator-decision shape (the fixture content)

The operator's HITL decisions on the v1.2 fixture speakers (represented as a frozen speaker-edits.json so the test is deterministic — the human review happened once, the smoke replays the apply):
  - spk_001 (少女 vocals, shot 1): CONFIRM + LINK to char_001 (少女)
  - spk_002 (shot 2): CONFIRM + NO LINK (旁白/群杂 → null char_id)
  - No merge / split (single-turn speakers)
  - Optional: spk_003 reject_ids proof — the smoke AUGMENTS audio_semantic.json with a synthetic third speaker to exercise the reject_ids gate (the canonical fixture has only 2 speakers). This keeps the canonical fixture untouched while proving reject semantics.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frozen speaker-edits fixture + smoke script exercising full HITL round-trip + idempotency + confirmed-only gate + XSS regression + producer integrity</name>
  <files>tests/fixtures/speaker_edits_phase13_smoke.json, scripts/verify_phase13_smoke.sh</files>
  <read_first>
    - tests/run_audio_analysis_smoke.sh (Phase 12 Plan 02, ~170 lines) — THE smoke script analog. Read line-by-line for the bash pattern: set -euo pipefail, REPO_ROOT computation, WORK=/tmp/p13-smoke-$$ with trap cleanup, helper function to wrap client invocation, scenario blocks with assertions, ALL_SCENARIOS_PASS marker on success.
    - tests/fixtures/audio_analysis_stub_response_empty.json + nonempty.json (Phase 12) — the pattern of a frozen JSON fixture under tests/fixtures/ (smoke-only, not canonical). speaker_edits_phase13_smoke.json follows this convention.
    - spec/fixtures/v1.2/audio_semantic.json (57 lines) + characters.json (28 lines) + shots.json (14 lines) + speakers.json (22 lines) — the round-trip source inputs + canonical reference shape.
    - registry/link_speakers.py (Plan 01 Task 1 output) — confirms CLI flags + happy-path behavior the smoke will exercise.
    - html/gen_speaker_review.py (Plan 02 Task 1 output) — confirms CLI flags + XSS _esc defense the regression will probe.
    - scripts/verify_phase8_smoke.py (Phase 8, lines around 392-441 calling _producer_registry_integrity) — the python pattern for invoking the producer integrity assert on a test workdir. Plan 03 smoke uses the same pattern via `python3 -c 'from pathlib import Path; from scripts.verify_contract import _producer_registry_integrity; ...'`.
    - spec/schemas/speaker-edits.schema.json + speakers.schema.json (Phase 11) — the schema-validate assertions to apply on the frozen fixture + round-trip output respectively.
  </read_first>
  <behavior>
    - `python3 -c "import json; json.load(open('tests/fixtures/speaker_edits_phase13_smoke.json'))"` parses cleanly.
    - The frozen fixture schema-validates against spec/schemas/speaker-edits.schema.json (Draft202012Validator).
    - `bash -n scripts/verify_phase13_smoke.sh` returns 0 (syntax valid).
    - `bash scripts/verify_phase13_smoke.sh` runs all scenarios, prints PHASE13_ROUND_TRIP_PASS on success, exits 0. On any failure, prints which scenario failed + the diff/grep evidence + exits 1.
    - The smoke cleans up: removes /tmp/p13-smoke-* workdir on exit via trap.
    - The smoke NEVER modifies spec/fixtures/v1.2/* (canonical fixtures are read-only inputs; any augmentation — e.g. adding a synthetic third speaker for reject_ids proof — copies to /tmp first).
  </behavior>
  <action>
    Create 2 artifacts. No production code changes.

    (1) tests/fixtures/speaker_edits_phase13_smoke.json — frozen operator decisions on the v1.2 fixture speakers. Concrete shape (representative happy-path; schema-validates against speaker-edits.schema.json):
        {
          "confirm_ids": ["spk_001", "spk_002"],
          "reject_ids": [],
          "link_mappings": {"spk_001": "char_001"},
          "merge_groups": [],
          "splits": {},
          "review_notes": "Phase 13 SC#5 smoke fixture: operator confirms both speakers; links spk_001 (少女 vocals, shot 1) to char_001; leaves spk_002 unlinked (旁白/群杂). Representative of a clean HITL pass on the v1.2 diarization output."
        }
      All keys schema-optional; the shape documents the SC#5 operator decision. Validation: `python3 -c "import json, jsonschema; s=json.load(open('spec/schemas/speaker-edits.schema.json')); d=json.load(open('tests/fixtures/speaker_edits_phase13_smoke.json')); jsonschema.Draft202012Validator(s).validate(d)"` passes.

    (2) scripts/verify_phase13_smoke.sh (~150-180 lines, mirror Phase 12 tests/run_audio_analysis_smoke.sh structure):
      - set -euo pipefail; set -o pipefail at top.
      - REPO_ROOT computed from $0 (cd $(dirname $0)/.. — note: scripts/ not tests/, so one level up not two).
      - WORK=/tmp/p13-smoke-$$ (unique per run; trap cleanup on EXIT: rm -rf $WORK).
      - Phase 13 fixture inputs (READ-ONLY — never modify): FIX_AUDIO=$REPO_ROOT/spec/fixtures/v1.2/audio_semantic.json, FIX_CHARS=$REPO_ROOT/spec/fixtures/v1.2/characters.json, FIX_SHOTS=$REPO_ROOT/spec/fixtures/v1.2/shots.json, FROZEN_EDITS=$REPO_ROOT/tests/fixtures/speaker_edits_phase13_smoke.json.
      - Helper function run_link(): wraps `python3 $REPO_ROOT/registry/link_speakers.py "$@"` — passes --audio-semantic / --characters / --edits / --work-dir / --output flags.
      - Helper function run_review(): wraps `python3 $REPO_ROOT/html/gen_speaker_review.py "$@"` — passes --audio-semantic / --characters / --shots / --output flags.
      - Helper function assert_schema(): wraps python3 -c jsonschema validation — takes schema path + instance path, exits 1 on validation fail with the errors printed.

      SCENARIO 1 (SC#5 main round-trip — happy path):
        # Step 1: gen HTML on canonical fixture inputs (proves Plan 02 produces the review UI)
        run_review --audio-semantic $FIX_AUDIO --characters $FIX_CHARS --shots $FIX_SHOTS --output $WORK/review.html
        Assert: test -s $WORK/review.html (non-empty)
        Assert: grep -q 'data-speaker-id="spk_001"' $WORK/review.html (spk_001 card present)
        Assert: grep -q '<option value="char_001">少女</option>' $WORK/review.html (confirmed dropdown populated)
        Assert: ! grep -q 'review_state.*proposed\|review_state.*rejected' $WORK/review.html (Pitfall 7 upstream gate — no unconfirmed chars in dropdown)

        # Step 2: link_speakers applies the frozen operator decisions
        run_link --audio-semantic $FIX_AUDIO --characters $FIX_CHARS --edits $FROZEN_EDITS --work-dir $WORK --output $WORK/speakers.json
        Assert: test -f $WORK/speakers.json
        Assert: assert_schema $REPO_ROOT/spec/schemas/speakers.schema.json $WORK/speakers.json (schema-valid)
        Assert: python3 -c "import json; d=json.load(open('$WORK/speakers.json')); assert all(s['review_state']=='confirmed' for s in d['speakers']), 'non-confirmed leaked'; assert len(d['speakers'])==2, f'expected 2, got {len(d[\"speakers\"])}'; spk1=next(s for s in d['speakers'] if s['spk_id']=='spk_001'); assert spk1['char_id']=='char_001', f'spk_001 char_id: {spk1[\"char_id\"]}'; spk2=next(s for s in d['speakers'] if s['spk_id']=='spk_002'); assert spk2.get('char_id') is None, f'spk_002 should have null char_id'"
        echo "SCENARIO 1 (happy path round-trip) PASS"

      SCENARIO 2 (idempotency — re-apply produces byte-identical output):
        # Snapshot sha256 of scenario 1 output
        H1=$(sha256sum $WORK/speakers.json | awk '{print $1}')
        # Apply 2 more times to different output paths (same inputs)
        run_link --audio-semantic $FIX_AUDIO --characters $FIX_CHARS --edits $FROZEN_EDITS --work-dir $WORK --output $WORK/speakers_run2.json
        run_link --audio-semantic $FIX_AUDIO --characters $FIX_CHARS --edits $FROZEN_EDITS --work-dir $WORK --output $WORK/speakers_run3.json
        H2=$(sha256sum $WORK/speakers_run2.json | awk '{print $1}')
        H3=$(sha256sum $WORK/speakers_run3.json | awk '{print $1}')
        Assert: [ "$H1" == "$H2" ] && [ "$H2" == "$H3" ] (byte-identical across 3 runs)
        echo "SCENARIO 2 (idempotency) PASS"

      SCENARIO 3 (confirmed-only gate — reject_ids excludes speaker from canonical):
        # Augment audio_semantic with a synthetic 3rd speaker (spk_003 in shot 1) — copy to /tmp, do NOT touch canonical fixture
        python3 -c "
import json, copy
src = json.load(open('$FIX_AUDIO'))
# Synthesize a 3rd shot/segment with spk_003
augmented = copy.deepcopy(src)
# Add spk_003 as additional dialogue in shot 1 (multi-speaker shot — common in diarization)
# Simpler: append a synthetic shot 3 with spk_003
augmented['shots'].append({'shot_id': 3, 'start_sec': 3.0, 'end_sec': 4.5, 'duration': 1.5, 'dialogue': {'text': 'third speaker', 'spk_id': 'spk_003'}})
json.dump(augmented, open('$WORK/audio_augmented.json', 'w'), ensure_ascii=False, indent=2)
"
        # Edits: confirm spk_001 + spk_002, REJECT spk_003, link spk_001
        python3 -c "
import json
edits = {'confirm_ids': ['spk_001', 'spk_002'], 'reject_ids': ['spk_003'], 'link_mappings': {'spk_001': 'char_001'}}
json.dump(edits, open('$WORK/edits_with_reject.json', 'w'), ensure_ascii=False, indent=2)
"
        run_link --audio-semantic $WORK/audio_augmented.json --characters $FIX_CHARS --edits $WORK/edits_with_reject.json --work-dir $WORK --output $WORK/speakers_with_reject.json
        Assert: python3 -c "import json; d=json.load(open('$WORK/speakers_with_reject.json')); ids={s['spk_id'] for s in d['speakers']}; assert 'spk_003' not in ids, f'spk_003 leaked into canonical: {ids}'; assert ids == {'spk_001', 'spk_002'}, f'unexpected ids: {ids}'"
        echo "SCENARIO 3 (confirmed-only gate via reject_ids) PASS"

      SCENARIO 4 (XSS regression — _esc neutralizes poison payloads):
        # Poison copies of the inputs with <script> payloads in every dynamic string field
        python3 -c "
import json
# Poison audio_semantic: <script> in spk_id (will be skipped by SPK_PATTERN — defense-in-depth)
audio_poison = {'shots':[{'shot_id':1,'start_sec':0.0,'end_sec':1.0,'duration':1.0,'dialogue':{'text':'<script>alert(1)</script>','spk_id':'<script>x</script>'}}]}
json.dump(audio_poison, open('$WORK/audio_poison.json', 'w'), ensure_ascii=False)
# Poison characters: <script> in name field (will be _esc'd in dropdown options)
chars_poison = [{'id':'char_001','name':'<script>alert(2)</script>','review_state':'confirmed','appearance_shots':[]}]
json.dump(chars_poison, open('$WORK/chars_poison.json', 'w'), ensure_ascii=False)
"
        run_review --audio-semantic $WORK/audio_poison.json --characters $WORK/chars_poison.json --shots $FIX_SHOTS --output $WORK/review_poison.html || true  # may exit non-zero on aggregate producing 0 speakers — that's fine
        # The raw <script> payload MUST NOT appear in the HTML output (either skipped or escaped)
        Assert: ! grep -q '<script>alert(1)</script>' $WORK/review_poison.html || test ! -s $WORK/review_poison.html
        Assert: ! grep -q '<script>alert(2)</script>' $WORK/review_poison.html || test ! -s $WORK/review_poison.html
        # If HTML was generated, confirm _esc transformed any <script> in char name into &lt;script&gt;
        if [ -s $WORK/review_poison.html ]; then
          grep -q '&lt;script&gt;' $WORK/review_poison.html && echo "(confirmed _esc active on poison)"
        fi
        echo "SCENARIO 4 (XSS regression) PASS"

      SCENARIO 5 (producer integrity extension accepts canonical shape — Plan 01 Task 2 proof):
        # Place the scenario-1 speakers.json in a work_dir with sibling shots.json + characters.json
        # (mirror the producer asset_dir layout) → _producer_registry_integrity returns []
        python3 -c "
import shutil, json, os
W = '$WORK/producer_integrity'
os.makedirs(W, exist_ok=True)
shutil.copy('$WORK/speakers.json', os.path.join(W, 'speakers.json'))
shutil.copy('$FIX_CHARS', os.path.join(W, 'characters.json'))
shutil.copy('$FIX_SHOTS', os.path.join(W, 'shots.json'))
"
        Assert: python3 -c "
from pathlib import Path
from scripts.verify_contract import _producer_registry_integrity
import sys
sys.path.insert(0, '$REPO_ROOT')
failures = _producer_registry_integrity(Path('$WORK/producer_integrity'))
assert failures == [], f'producer integrity failures on canonical shape: {failures}'
"
        # Negative test: corrupt speakers.json with a non-confirmed entry → failures surfaces
        python3 -c "
import json, os
W = '$WORK/producer_integrity_bad'
os.makedirs(W, exist_ok=True)
json.dump({'speakers':[{'spk_id':'spk_001','review_state':'proposed'}]}, open(os.path.join(W, 'speakers.json'), 'w'))
import shutil
shutil.copy('$FIX_CHARS', os.path.join(W, 'characters.json'))
shutil.copy('$FIX_SHOTS', os.path.join(W, 'shots.json'))
"
        Assert: python3 -c "
from pathlib import Path
from scripts.verify_contract import _producer_registry_integrity
import sys
sys.path.insert(0, '$REPO_ROOT')
failures = _producer_registry_integrity(Path('$WORK/producer_integrity_bad'))
assert any('confirmed' in m.lower() or 'pitfall 7' in m.lower() for m in failures), f'expected Pitfall 7 failure, got: {failures}'
"
        echo "SCENARIO 5 (producer integrity extension) PASS"

      On all 5 scenarios passing: echo "PHASE13_ROUND_TRIP_PASS" && exit 0. Trap cleans up $WORK.

      Add inline comment near the top of the script: "# Phase 13 SC#5 end-to-end HITL round-trip proof. Mirrors Phase 12 tests/run_audio_analysis_smoke.sh structure but simpler (no stub server — both Plan 01 + Plan 02 CLIs are file-in/file-out). Canonical v1.2 fixtures are READ-ONLY inputs; augmentations go to /tmp workdir."
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "import json; json.load(open('tests/fixtures/speaker_edits_phase13_smoke.json'))" && python3 -c "
import json, jsonschema
schema = json.load(open('spec/schemas/speaker-edits.schema.json'))
data = json.load(open('tests/fixtures/speaker_edits_phase13_smoke.json'))
jsonschema.Draft202012Validator(schema).validate(data)
print('frozen fixture schema-valid OK')
" && bash -n scripts/verify_phase13_smoke.sh && echo 'smoke script syntax OK' && bash scripts/verify_phase13_smoke.sh 2>&1 | tail -25</automated>
  </verify>
  <done>
    - tests/fixtures/speaker_edits_phase13_smoke.json exists, parses, schema-validates against speaker-edits.schema.json.
    - scripts/verify_phase13_smoke.sh exists, syntax-valid (bash -n).
    - `bash scripts/verify_phase13_smoke.sh` prints PHASE13_ROUND_TRIP_PASS and exits 0.
    - All 5 scenarios pass: (1) happy-path round-trip; (2) idempotency (byte-identical across 3 runs); (3) confirmed-only gate via reject_ids; (4) XSS regression (raw <script> never in HTML); (5) producer integrity extension (accepts canonical + rejects leak).
    - Canonical v1.2 fixtures (audio_semantic / characters / shots / speakers) are NOT modified — all augmentations go to /tmp workdir.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → production code (file-in/file-out) | Frozen JSON fixture + canonical v1.2 fixtures → Plan 01 link_speakers.py + Plan 02 gen_speaker_review.py CLI invocations |
| Test workdir → filesystem | All outputs under /tmp/p13-smoke-* (test isolation; canonical fixtures untouched) |
| Poison fixture → HTML output | Scenario 4 deliberately injects <script> payloads to probe the _esc defense layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Tampering | Test fixture deviates from schema (false positive e2e pass) | mitigate | Frozen fixture schema-validated at smoke start (assert_schema call). Any future drift in speaker-edits.schema.json (Phase 14+) that breaks the fixture will fail loudly here. |
| T-13-14 | Repudiation | Smoke modifies canonical v1.2 fixtures (cross-test contamination) | mitigate | Trap cleanup + READ-ONLY discipline: canonical fixtures accessed via FIX_* constants only; augmentations (synthetic 3rd speaker, poison payloads) go to $WORK (which is rm-rf'd on exit). Assert ! -w checks could be added but YAGNI — operator can verify via git status post-smoke. |
| T-13-15 | Tampering | XSS regression false-pass (poison not actually rendered) | mitigate | Scenario 4 generates HTML on the poison inputs; if HTML is empty (aggregator skipped all poison), the test still passes (skipped == neutralized) BUT the assertion `! grep -q '<script>alert(1)</script>'` is satisfied either way. To prove _esc is active, the secondary assertion `grep -q '&lt;script&gt;' $WORK/review_poison.html` confirms the escape layer actually fired on the poisoned char_name (the spk_id poison is skipped by SPK_PATTERN before _esc). |
| Reuse T-13-01..12 | — | Production code trust boundaries | (see Plans 01 + 02) | The smoke does not alter the production code's attack surface; it exercises the existing mitigations. |
| T-13-SC | Supply Chain | No new packages | accept | Zero pip/npm installs. Smoke uses bash + python3 stdlib + jsonschema (already in v1.0 deps). |
</threat_model>

<verification>
- tests/fixtures/speaker_edits_phase13_smoke.json parses + schema-validates.
- scripts/verify_phase13_smoke.sh: bash -n clean.
- bash scripts/verify_phase13_smoke.sh: PHASE13_ROUND_TRIP_PASS + exit 0.
- All 5 scenarios produce expected assertions.
- git status post-smoke: zero changes to spec/fixtures/v1.2/* (canonical fixtures preserved).
</verification>

<success_criteria>
- SC#5 (ROADMAP): End-to-end HITL round-trip proven on fixture: audio_semantic.json + characters.json → review HTML → speaker-edits.json → speakers.json (confirmed-only); DIA-02 diarization + DIA-03 speaker↔character HITL both exercised. — Covered by Scenario 1 (happy path) + Scenario 3 (reject_ids gate) + Scenario 5 (producer integrity acceptance).
- Idempotency (Pitfall 5): byte-identical re-apply proven. — Covered by Scenario 2.
- Confirmed-only gate (Pitfall 7): reject_ids excludes speaker from canonical. — Covered by Scenario 3.
- XSS defense (T-13-01, mirror v1.1 Phase 8 PRESENT): _esc neutralizes all attack vectors. — Covered by Scenario 4.
- Producer integrity extension (SPEAKER-03, Plan 01 Task 2): accepts canonical shape + rejects leak. — Covered by Scenario 5.
- SPEAKER-01 + SPEAKER-02 + DIA-02 + DIA-03 all exercised end-to-end.
</success_criteria>

<output>
Create `.planning/phases/13-speaker-01-linkage-hitl/13-03-SUMMARY.md` when done
</output>
