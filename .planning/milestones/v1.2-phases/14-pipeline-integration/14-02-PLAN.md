---
phase: 14-pipeline-integration
plan: 02
type: execute
wave: 1
depends_on: [14-01]
files_modified:
  - scripts/verify_phase_audio_smoke.py
autonomous: true
requirements: [PIPE-03]
tags: [smoke-harness, regression-guard, route-up, route-down, cache-hit-offline, conditional-field-defer, stub-only, e2e-proof, asset-schema-1-2]

must_haves:
  truths:
    - "scripts/verify_phase_audio_smoke.py exists and runs 5 scenarios in order, each in an isolated tempfile.mkdtemp work_dir cleaned up in finally; mirror scripts/verify_phase6_smoke.py:54-505 structure (bracketed [phase14-smoke] prefix tags, sys.exit(0/1) exit-code contract, stdlib + jsonschema only)"
    - "Scenario 1 route-up: stub server returns non-empty envelope (reuse tests/fixtures/audio_analysis_stub_response_nonempty.json from Phase 12) → call_audio_analysis.py writes audio_semantic.json schema-valid + cache file shot_001.json + [audio] tag NOT in warnings sidecar"
    - "Scenario 2 route-down: unreachable URL → audio_semantic.json NOT written (byte-identical-absent CONTRACT-05) + warnings sidecar contains [audio] tag with 'preflight' or 'ConnectError' string"
    - "Scenario 3 cache-hit-offline: pre-populate route_cache/audio_analysis/shot_001.json with nonempty fixture + correct _cache_key → run call_audio_analysis.py --offline → audio_semantic.json byte-identical to scenario 1 + stdout contains 'cache hit' + 0 network calls (offline + cache hit double-bind)"
    - "Scenario 4 conditional-field-defer: stub returns shot with dialogue.emotion=null + emotion_confidence=null (DIA-04 ship-nullable+confidence) AND instruments field absent (MUS-04 deferred v1.3) → audio_semantic.json schema-valid + emotion fields nullable + no instrument field anywhere (case-insensitive grep on output JSON)"
    - "Scenario 5 stub-only: stub returns empty envelope (stub_mode:true, data.shots=[] — Phase 10 ROUTE-01 stub contract) → audio_semantic.json NOT written + [audio] warning sidecar explains stub_mode"
    - "End-to-end (SC#5): on a v1.2 fixture set (or synthesized minimal v1.2 work_dir with audio_semantic.json present), scripts/export_asset.py produces asset.json with schema_version=\"1.2\" AND data.audio_semantic + data.speakers conditionally emitted; on a v1.1 work_dir (no audio_semantic.json), asset.json still schema_version=\"1.2\" but data.audio_semantic/data.speakers OMITTED (byte-identical-absent CONTRACT-05 proof)"
  artifacts:
    - path: "scripts/verify_phase_audio_smoke.py"
      provides: "5-scenario smoke regression harness covering route-up / route-down / cache-hit-offline / conditional-field-defer / stub-only — mirror of scripts/verify_phase6_smoke.py:54-505 + Phase 12 tests/run_audio_analysis_smoke.sh scenario shape, ported to Python for cross-platform determinism + integrated e2e asset.json schema_version='1.2' proof"
      min_lines: 450
      contains: ["scenario_route_up", "scenario_route_down", "scenario_cache_hit_offline", "scenario_conditional_field_defer", "scenario_stub_only", "ALL_SCENARIOS_PASS", "schema_version", "1.2"]
  key_links:
    - from: "scripts/verify_phase_audio_smoke.py"
      to: "analysis/call_audio_analysis.py"
      via: "subprocess.run([sys.executable, 'analysis/call_audio_analysis.py', '--video', tiny_video, '--shots', shots_json, '--work-dir', work_dir, '--output', audio_semantic_json, '--stems-dir', stems_dir, '--route-url', stub_url, ...])"
      pattern: "call_audio_analysis\\.py"
    - from: "scripts/verify_phase_audio_smoke.py (e2e section)"
      to: "scripts/export_asset.py"
      via: "subprocess.run on minimal v1.2 work_dir → assert asset.json schema_version + conditional data.audio_semantic emission"
      pattern: "export_asset\\.py.*schema_version|schema_version.*1\\.2"

objective: Deliver the Phase 14 PIPE-03 5-scenario smoke regression harness AND prove SC#5 (end-to-end asset.json schema_version="1.2" with conditional audio_semantic/speakers emission). Mirror scripts/verify_phase6_smoke.py:54-505 + Phase 12 tests/run_audio_analysis_smoke.sh scenario shape — port to Python (single-file, stdlib + jsonschema only, cross-platform determinism, no bash dependency).

output: New scripts/verify_phase_audio_smoke.py (~500 lines, 5 scenarios + e2e). Single-task atomic commit.

execution_context:
  @$HOME/.claude/get-shit-done/workflows/execute-plan.md
  @$HOME/.claude/get-shit-done/templates/summary.md

context:
  @.planning/PROJECT.md
  @.planning/ROADMAP.md
  @.planning/STATE.md
  @.planning/phases/14-pipeline-integration/14-CONTEXT.md
  @CLAUDE.md

threat_model:
  threat: T-14
  enforcement: security_enforcement
  scope_files: [scripts/verify_phase_audio_smoke.py]
  dispositions:
    - id: T-14-04
      category: unsafeguarded-feature
      risk: "smoke harness could spawn stub server that lingers after harness exits (resource leak / port exhaustion on re-run)"
      mitigation: "trap-style finally cleanup in EVERY scenario: tempfile.mkdtemp captured in try, shutil.rmtree(ignore_errors=True) in finally; stub server started via subprocess with atexit + signal handling OR (preferred) use httpx directly in-process via ThreadingHTTPServer context manager"
      verify: "grep audit: each scenario_* function has try/finally with shutil.rmtree; stub server is either in-process (ThreadingHTTPServer in a with block) or has explicit .terminate() in finally"
    - id: T-14-05
      category: test-harness-false-positive
      risk: "scenario passes but assertion is vacuous (e.g., asserts file exists but not that content is correct; or asserts 'cache hit' in stdout but stub still got called)"
      mitigation: "every assertion has BOTH a structural check (file exists/specific field value) AND a behavioral check (stdout substring OR absence of FAIL). Scenario 3 specifically proves no-network via the UNREACHABLE_URL — if stub were called, ConnectError would land in warnings sidecar and fail the [audio] absence assertion."
      verify: "manual code review of each scenario's assertion tuple (≥3 conjunctive checks per scenario); no scenario has only one assertion"
---

<tasks>

<task type="auto">
  <name>Task 1: 5-scenario smoke harness + e2e asset.json schema_version=1.2 proof</name>
  <files>scripts/verify_phase_audio_smoke.py</files>
  <read_first>
    - scripts/verify_phase6_smoke.py:1-505 (THE pattern to mirror: docstring with scenario table, _tmp_work_dir helper, _write_synthetic_shots helper, _check_*_valid schema helper, _run wrapper, scenario_* functions returning (ok, detail), main() with bracketed [phaseN-smoke] tags, sys.exit(0/1))
    - scripts/verify_phase6_smoke.py:125-194 (scenario_route_down — model for our scenario_route_down + scenario_stub_only)
    - scripts/verify_phase6_smoke.py:257-355 (scenario_cache_hit_offline — model for our scenario 3)
    - tests/audio_analysis_stub_server.py (Phase 12 stub server — reuse for scenarios 1 + 4: starts ThreadingHTTPServer on configurable port, returns fixture bytes on POST to /api/production/audio-analysis, 405 on GET)
    - tests/fixtures/audio_analysis_stub_response_nonempty.json (Phase 12 frozen nonempty envelope — scenario 1 + 3 input)
    - tests/fixtures/audio_analysis_stub_response_empty.json (Phase 12 frozen empty envelope — scenario 5 input)
    - analysis/call_audio_analysis.py:99-159 (module-level constants ROUTE_NAME / ROUTE_VERSION / ROUTE_PATH for cache key construction)
    - analysis/call_audio_analysis.py:128-146 (video_content_hash function — needed to construct valid _cache_key for scenario 3 pre-population)
    - spec/schemas/audio_semantic.schema.json (the schema to validate scenario 1/3/4 outputs against)
    - spec/fixtures/v1.2/audio_semantic.json (canonical shape — scenario 4 conditional-field-deferred shot shape: nullable emotion fields + absent instruments)
    - scripts/export_asset.py:367-391 (build_asset_dict return shape — schema_version + data.audio_semantic conditional emission — what e2e asserts against)
    - CLAUDE.md:117-130 (Logging conventions: bracketed stage prefix for progress lines — mirror for [phase14-smoke] tags)
  </read_first>
  <behavior>
    After this task, all of the following hold:

    (a) **scripts/verify_phase_audio_smoke.py exists + is executable.** Module docstring (Chinese ~40 lines) describes the 5 scenarios + e2e section + exit code contract, mirror verify_phase6_smoke.py:1-53 structure.

    (b) **5 scenarios all pass when run via `python3 scripts/verify_phase_audio_smoke.py`:**

    Scenario 1 (route-up):
        - Start tests/audio_analysis_stub_server.py with tests/fixtures/audio_analysis_stub_response_nonempty.json on port 10593 (Phase 12 used 10591; Phase 14 smoke uses 10593 to avoid collision with lingering Phase 12 runs).
        - Invoke analysis/call_audio_analysis.py with --route-url http://127.0.0.1:10593/api/production/audio-analysis against a tiny test video (Path(__file__) like verify_phase6_smoke.py:80 TINY_VIDEO).
        - Assert: exit 0 + audio_semantic.json written + schema-valid (Draft202012Validator against spec/schemas/audio_semantic.schema.json) + cache file route_cache/audio_analysis/shot_001.json written + warnings sidecar does NOT contain "[audio]" tag (success path — no warning).

    Scenario 2 (route-down):
        - Invoke against unreachable URL http://127.0.0.1:1/api/production/audio-analysis with --analysis-timeout 2.
        - Assert: exit 0 (graceful-degrade) + audio_semantic.json NOT written (byte-identical-absent, CONTRACT-05) + warnings sidecar contains "[audio]" tag AND ("preflight" or "ConnectError") substring.

    Scenario 3 (cache-hit-offline):
        - Pre-populate route_cache/audio_analysis/shot_001.json with tests/fixtures/audio_analysis_stub_response_nonempty.json content + injected _cache_key matching TINY_VIDEO's video_content_hash (compute via analysis.call_audio_analysis.video_content_hash(TINY_VIDEO)).
        - Invoke with --offline against unreachable URL.
        - Assert: exit 0 + audio_semantic.json byte-identical to a pre-snapshot from scenario 1 OR (if scenarios are independent) byte-identical to a fresh nonempty-output snapshot + stdout contains "cache hit" + warnings sidecar does NOT contain "[audio]" with "FAIL" or "offline/cache-miss" substring (proves cache was used, not degraded).

    Scenario 4 (conditional-field-defer):
        - Use a stub fixture variant where shot 1's dialogue has emotion=null + emotion_confidence=null (DIA-04 ship-nullable+confidence per Phase 10 spike outcome) AND instruments field is OMITTED entirely (MUS-04 deferred v1.3).
        - Either: write a new fixture file tests/fixtures/audio_analysis_stub_response_nullable.json OR reuse tests/fixtures/audio_analysis_stub_response_nonempty.json if it already has the right shape. Inspect first; create variant only if needed.
        - Invoke against stub returning this fixture.
        - Assert: exit 0 + audio_semantic.json written + schema-valid + dialogue.emotion == null OR dialogue has emotion field with null value (nullable proof) + grep -i for the literal "instrument" substring on the audio_semantic.json file returns 0 matches (MUS-04 absent proof — Phase 11 schema $comment lock: 全文用中文「乐器」/ "MIR label" 指代，绝不出现英文 M-I 开头那个字面; case-insensitive grep must be empty).

    Scenario 5 (stub-only):
        - Start stub server with tests/fixtures/audio_analysis_stub_response_empty.json (the byte-identical Phase 10 stub envelope from 10-02-SUMMARY.md:166 — data.shots=[] + stub_mode:true).
        - Invoke against stub URL (no --offline; stub_mode:true is the integration target).
        - Assert: exit 0 + audio_semantic.json NOT written (zero shots with data → byte-identical-absent) + warnings sidecar contains "[audio]" AND ("stub_mode" OR "0 shots" substring — call_audio_analysis.py:386 message includes "(stub_mode:true — ML not loaded)").

    (c) **End-to-end (SC#5) section** — two sub-assertions in a single function:
        - e2e-with-audio_semantic: build a minimal v1.2 work_dir under tempfile.mkdtemp by copying spec/fixtures/v1.2/{shots.json, audio_analysis.json, transcript.json, frames.json, prompts.json, audio_semantic.json, asset.json} (all 7 canonical files) + creating a tiny video file + stems dir; invoke scripts/export_asset.py; assert asset.json#schema_version == "1.2" AND asset.json#data.audio_semantic == "audio_semantic.json" (conditional emission fires when file present).
        - e2e-without-audio_semantic: same fixture set but DELETE audio_semantic.json + speakers.json before invoking export; assert asset.json#schema_version == "1.2" (unchanged) AND "audio_semantic" NOT IN asset.json#data keys (byte-identical-absent CONTRACT-05) AND "speakers" NOT IN asset.json#data keys.

    (d) **All cleanup uses try/finally + shutil.rmtree(ignore_errors=True)** (T-14-04 mitigation). Stub servers started as background subprocesses have PID captured + killed in finally (mirror tests/run_audio_analysis_smoke.sh trap pattern). Alternatively, import tests.audio_analysis_stub_server as a module and start ThreadingHTTPServer in a with-block / contextmanager (preferred — no PID management).
  </behavior>
  <action>
    Create a single Python file scripts/verify_phase_audio_smoke.py.

    (1) Module docstring (Chinese, ~40 lines) — mirror verify_phase6_smoke.py:1-53: explain 5 scenarios + e2e section + exit code contract. Reference Phase 12 tests/run_audio_analysis_smoke.sh as the bash analog being ported to Python for determinism + Windows-compat.

    (2) Path constants (mirror verify_phase6_smoke.py:68-80):
        - REPO = Path(__file__).parent.parent.resolve()
        - SCHEMAS_DIR = REPO / "spec" / "schemas"
        - FIXTURES_DIR = REPO / "spec" / "fixtures" / "v1.2"
        - STUB_DIR = REPO / "tests"
        - STUB_SERVER = STUB_DIR / "audio_analysis_stub_server.py"
        - NONEMPTY_FIXTURE = STUB_DIR / "fixtures" / "audio_analysis_stub_response_nonempty.json"
        - EMPTY_FIXTURE = STUB_DIR / "fixtures" / "audio_analysis_stub_response_empty.json"
        - UNREACHABLE_URL = "http://127.0.0.1:1/api/production/audio-analysis"
        - TINY_VIDEO = Path(__file__).resolve()
        - BASE_PORT = 10593 (Phase 14 smoke; distinct from Phase 12's 10591)

    (3) Common helpers (mirror verify_phase6_smoke.py:83-122):
        - _tmp_work_dir() → tempfile.mkdtemp(prefix="phase14-smoke-")
        - _write_synthetic_shots(path, count=1) → writes [{"id":1,"start_sec":0.0,"end_sec":1.5,"duration":1.5}] (1 shot for cache scenarios)
        - _write_minimal_stems(work_dir) → mkdir stems/htdemucs/<basename>/ + touch empty vocals.wav/drums.wav/bass.wav/other.wav (call_audio_analysis.py reads the dir but stub ignores body)
        - _check_audio_semantic_valid(work_dir) → Draft202012Validator against audio_semantic.schema.json, returns sorted errors list
        - _run(cmd, **kw) → subprocess.run wrapper with capture_output=True, text=True defaults
        - _start_stub_server(fixture_path, port) → subprocess.Popen([sys.executable, str(STUB_SERVER), "--fixture", str(fixture_path), "--port", str(port)]), poll for "listening" on stdout up to 30×100ms, return proc

    (4) Five scenario functions — each returns (ok: bool, detail: str) — using try/finally cleanup pattern:
        - scenario_route_up(verbose) — port BASE_PORT, nonempty fixture
        - scenario_route_down(verbose) — unreachable URL, no stub
        - scenario_cache_hit_offline(verbose) — pre-populate cache, --offline, unreachable URL
        - scenario_conditional_field_defer(verbose) — port BASE_PORT+1, nullable fixture (write inline if new fixture needed, else reuse)
        - scenario_stub_only(verbose) — port BASE_PORT+2, empty fixture

    (5) End-to-end function scenario_e2e_asset_schema(verbose) — two sub-cases (with/without audio_semantic), each with own temp dir + cleanup. Reuse _run helper for export_asset subprocess.

    (6) main() — argparse with --verbose (mirror verify_phase6_smoke.py:466-501); iterate scenarios list; print [phase14-smoke] PASS/FAIL per scenario; print summary line; sys.exit(0) on all-pass else sys.exit(1).

    (7) For scenario 3 cache-hit-offline byte-identical proof: use `subprocess.run` to invoke call_audio_analysis.py once with nonempty stub to produce the baseline audio_semantic.json, copy it aside, then re-run with --offline + pre-populated cache, then `filecmp.cmp()` the two files.

    (8) For scenario 4 nullable-field proof: inspect tests/fixtures/audio_analysis_stub_response_nonempty.json; if dialogue.emotion is non-null, write a temporary variant under the temp work_dir (NOT into tests/fixtures/) with emotion=null + emotion_confidence=null. After call_audio_analysis runs, grep the output audio_semantic.json for "instrument" case-insensitive — must be 0 matches.

    (9) For e2e: use shutil.copy to assemble the v1.2 fixture set into a temp work_dir; the "video" can be a tiny file (the actual export uses ffprobe to get duration from transcript.json, not from the video bytes — so an empty .mp4 touch works as long as ffprobe doesn't fail catastrophically; if it does, use TINY_VIDEO which is a real file with bytes). Create stems dir structure with empty .wav files. Invoke export_asset.py with --work-dir, --video, --stems-source-dir, --output. Parse output asset.json + assert.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "import ast; ast.parse(open('scripts/verify_phase_audio_smoke.py').read()); print('syntax OK')" && test -x scripts/verify_phase_audio_smoke.py 2>/dev/null || chmod +x scripts/verify_phase_audio_smoke.py; python3 scripts/verify_phase_audio_smoke.py 2>&1 | tail -30</automated>
  </verify>
  <done>
    - scripts/verify_phase_audio_smoke.py exists, syntax-valid, executable.
    - Module docstring describes 5 scenarios + e2e + exit code contract.
    - All 5 scenarios print [phase14-smoke] PASS in the output.
    - E2E section prints PASS for both with/without-audio_semantic sub-cases.
    - Final line: "[phase14-smoke] OK: 6/6 scenarios green" (5 + e2e) — exit 0.
    - Scenario 1 audio_semantic.json schema-valid against spec/schemas/audio_semantic.schema.json.
    - Scenario 2 audio_semantic.json absent + [audio] warning sidecar.
    - Scenario 3 cache hit logged + byte-identical output (filecmp.cmp == True).
    - Scenario 4 audio_semantic.json written with nullable emotion fields + zero "instrument" matches (case-insensitive grep).
    - Scenario 5 audio_semantic.json absent + [audio] warning mentions stub_mode.
    - E2E asset.json schema_version == "1.2" in both sub-cases; data.audio_semantic present in sub-case 1, absent in sub-case 2.
    - No background stub server processes linger after harness exits (verify via `pgrep -f audio_analysis_stub_server` returning empty).
  </done>
</task>

</tasks>
