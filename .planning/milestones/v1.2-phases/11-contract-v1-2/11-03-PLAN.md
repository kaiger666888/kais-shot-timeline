---
phase: 11-contract-v1-2
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - spec/SPEC.md
  - spec/README.md
autonomous: true
requirements: [CONTRACT-04]
tags: [docs, spec, prose, fidelity-disclaimer]

must_haves:
  truths:
    - "`spec/SPEC.md` has a `1.2` Changelog entry under §4 that documents the 3 Phase-10-informed deviations (instruments omitted, emotion nullable-string, word-level experimental) + 3 new schemas + asset.schema additive + schema_version literal locked at `export_asset.py:55`"
    - "`spec/SPEC.md` has §5.8 Audio Semantic (v1.2) and §5.9 Speakers (v1.2) shape sections following the same 4-block template as v1.1's §5.6 Characters + §5.7 Props"
    - "`spec/SPEC.md` has a `fidelity_disclaimer` section explicitly stating: AF-01 'perfectly reconstruct' / 'exact restoration' are forbidden phrases; emotion = calibrated estimate (NOT accuracy); word-level = experimental (segment-level is SLA); instruments = absent (deferred v1.3); per-modality estimates TTS ~70% / music-gen ~60-75% / foley ~80%"
    - "`spec/SPEC.md` §1 schema layout lists 13 schemas (10 v1.0/v1.1 + 3 new v1.2)"
    - "`spec/README.md` has a v1.2 update footer mirroring the v1.1 update footer pattern"
    - "The `fidelity_disclaimer` lives in SPEC PROSE (not as a schema field) — honors the two-tier authority rule"
    - "NOWHERE in SPEC.md or README.md do the phrases 'perfectly reconstruct' / 'exact restoration' / '完美复刻' / '精确复原' appear (AF-01 invariant)"
  artifacts:
    - path: "spec/SPEC.md"
      provides: "Human-readable contract overview extended with v1.2 changelog + shapes + fidelity disclaimer"
      contains: "1.2"
    - path: "spec/README.md"
      provides: "Schema layout overview with v1.2 update footer"
      contains: "v1.2"
  key_links:
    - from: "spec/SPEC.md §4 Changelog 1.2 entry"
      to: "scripts/export_asset.py:55 (SCHEMA_VERSION literal)"
      via: "prose reference to single-source location"
      pattern: "export_asset\\.py"
    - from: "spec/SPEC.md §5.8 Audio Semantic"
      to: "spec/schemas/audio_semantic.schema.json"
      via: "Reference schema footer link"
      pattern: "audio_semantic\\.schema\\.json"
---

<objective>
Document the v1.2 contract in human-readable prose: SPEC.md §4 Changelog `1.2` entry + §5.8 Audio Semantic shape + §5.9 Speakers shape + `fidelity_disclaimer` section + §1 schema layout bump + README.md v1.2 footer. This is the "two-tier authority" human half — the schemas are the machine truth (Plan 01), this prose is the 15-minute readable overview that pairs with them.

Purpose: CONTRACT-04 acceptance + AF-01/AF-02/AF-03 invariants. The `fidelity_disclaimer` is a first-class deliverable (NOT a schema field — it lives in prose because it is not machine-checkable). The §4 Changelog entry records WHY the Phase-10-informed deviations exist (MERT no classifier / SenseVoice self-consistency-not-accuracy / WhisperX drift metric artifact), so a future contributor doesn't accidentally re-introduce the over-claiming patterns.

Output: Extended `spec/SPEC.md` + `spec/README.md`. No code, no schemas, no fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-contract-v1-2/11-CONTEXT.md
@.planning/phases/11-contract-v1-2/11-RESEARCH.md
@.planning/phases/11-contract-v1-2/11-01-SUMMARY.md

<interfaces>
<!-- Existing SPEC.md structures the executor extends. -->

spec/SPEC.md existing sections (verified line numbers):
  §1 Authority & Schema Layout       (lines 12-39) — 9-schema table
  §4 Changelog                       (lines 157-168) — `1` entry + `1.1` entry (template)
  §5 The 5/7/9 Canonical Data Shapes (lines 172-420) — §5.1 Shots ... §5.7 Props
  §5 intro                           (line 176) — "v1.1 新增 §5.6 Characters + §5.7 Props"

§4 Changelog 1.1 entry structure (lines 161-168 — literal template for 1.2):
  - **<DATE> — `<VERSION>`(<flavor>)** — <one-line summary>. <Pure-additive claim>. <Pitfall reference>.变更:
    - **<N> 个新 schema**: <list>
    - **`<existing>.schema.json` additive**: <what + `required[]` byte-identical callout>
    - **`schema_version` pattern 不变** (<pattern>) — version literal locked at `<file>:<line>`
    - **向后兼容**: <fixture counts + verify_contract.py bidirectional status>
    - (phase-specific bullets)

§5 shape template (4-block — verified from §5.6 Characters):
  1. `**Producer:**` `<file:func>`** line
  2. `**顶层形状:**` <JSON value kind description>** line
  3. | Field | Type | Required | Notes | table
  4. `**最小片段:**` JSON code block
  5. `Reference schema:` `<path>` closing line

spec/README.md existing structure:
  - schema layout overview
  - v1.1 update footer (line ~80) — template for v1.2 footer

v1.2 contract facts (from Plan 01 + 02 outputs):
  - 3 new schemas: audio_semantic.schema.json, speakers.schema.json, speaker-edits.schema.json
  - asset.schema.json additive: data.audio_semantic + data.speakers (NOT in required[])
  - SCHEMA_VERSION literal at scripts/export_asset.py:55
  - 12-file fixture (v1.1's 10 + audio_semantic.json + speakers.json)
  - Phase-10 deviations: instruments omitted, emotion type:string nullable+confidence, word_level_experimental flag
  - Per-modality fidelity: TTS ~70%, music-gen ~60-75%, foley ~80%
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SPEC.md §4 Changelog 1.2 entry + §5.8 Audio Semantic + §5.9 Speakers + fidelity_disclaimer + §1 schema layout bump</name>
  <files>spec/SPEC.md</files>
  <read_first>
    - `spec/SPEC.md` (FULL — esp. §1 lines 12-39 the 9-schema table; §4 lines 157-168 the Changelog with `1.1` entry as template; §5 lines 172-420 the shape sections, esp. §5.6 Characters as the 4-block template; line 176 the §5 intro paragraph)
    - `spec/schemas/audio_semantic.schema.json` (Plan 01 output — source of truth for §5.8 field table)
    - `spec/schemas/speakers.schema.json` (Plan 01 output — source of truth for §5.9 field table)
    - `spec/schemas/speaker-edits.schema.json` (Plan 01 output — referenced in §5.9 or §1)
    - `.planning/phases/11-contract-v1-2/11-CONTEXT.md` § Specific Ideas (fidelity_disclaimer is a first-class deliverable)
    - `.planning/phases/11-contract-v1-2/11-RESEARCH.md` Pattern 6 (SPEC.md section structure, lines 643-684) + Anti-Patterns (lines 686-694 — `fidelity_disclaimer` MUST be prose, not schema field)
    - `.planning/REQUIREMENTS.md` CONTRACT-04 + AF-01/AF-02/AF-03 (Out of Scope)
  </read_first>
  <behavior>
    - SPEC.md §1 schema layout table extended to 13 schemas (10 existing + 3 new v1.2).
    - SPEC.md §4 Changelog has a new `1.2` entry dated `2026-07-25` following the `1.1` entry structure verbatim. Documents: pure-additive claim (Pitfall 11 prevented), 3 new schemas, asset.schema additive (required[] byte-identical), schema_version pattern unchanged + literal locked at `scripts/export_asset.py:55`, Phase-10-informed deviations (instruments omitted / emotion nullable-string / word-level experimental), backward-compat proof status (v1.0 minimal 6/6 + v1.1 10/10 + v1.2 12/12 all GREEN; bidirectional proof GREEN).
    - SPEC.md §5 intro paragraph extended with: "v1.2 新增 §5.8 Audio Semantic + §5.9 Speakers (均 optional — 仅 route-host round-trip 跑过且条件字段达标后 emit)".
    - SPEC.md §5.8 Audio Semantic (v1.2) follows the 4-block template (Producer / 顶层形状 / field table / 最小片段 / Reference schema). Field table lists: schema_version, word_level_experimental, shots[].shot_id/start_sec/end_sec/duration, shots[].dialogue.{text,spk_id,emotion,emotion_confidence,events,words[]}, shots[].sfx.{events,description}, shots[].reproduction.{tts,music_gen,foley}.{text,confidence,fidelity_disclaimer}.
    - SPEC.md §5.9 Speakers (v1.2) follows the 4-block template. Field table lists: speakers[].spk_id, speakers[].char_id (nullable), speakers[].total_speech_sec, speakers[].review_state (enum), speakers[].turns[].{shot_id,start_sec,end_sec}.
    - A new `fidelity_disclaimer` section (either §10 or a sub-section of §4 right after the Changelog) explicitly states: (1) AF-01 — 'perfectly reconstruct' / 'exact restoration' / '完美复刻' / '精确复原' are FORBIDDEN phrases in README/SPEC; (2) per-modality estimates TTS ~70% / music-gen ~60-75% / foley ~80%; (3) emotion = calibrated estimate (NOT rigorous macro-F1; SenseVoice self_consistency=100% is a label-stability proxy); (4) word-level = experimental (segment-level is SLA); (5) instruments = absent (MUS-04 deferred v1.3 — MERT no classifier + PANNs zenodo-blocked).
    - NOWHERE in the new prose do 'perfectly reconstruct' / 'exact restoration' / '完美复刻' / '精确复原' appear.
    - The `1.2` Changelog entry mentions `scripts/export_asset.py:55` as the single source of the `schema_version` literal (does NOT hardcode "1.2" elsewhere — honors single-source rule).
  </behavior>
  <action>
    Five edits to `spec/SPEC.md`:

    (1) **§1 schema layout** (lines 12-39) — extend the table with 3 new rows for `audio_semantic.schema.json`, `speakers.schema.json`, `speaker-edits.schema.json`. Update the section header reference to "13 schemas" (currently "9 个 schema 文件" at line 23). Update the "下游 TS 类型生成" note (line 37) to say "13 schemas" instead of "9 schemas". Mark each new row with `**v1.2**` annotation matching the v1.1 convention.

    (2) **§4 Changelog** — append a new `1.2` entry AFTER the existing `1.1` entry (line 168). Use the exact `1.1` entry structure (RESEARCH Pattern 6 lines 647-653 + 1.1 entry lines 161-168 as the literal template):

    ```
    - **2026-07-25 — `1.2`(v1.2 additive extension,Phases 10-17)** — 第二个 minor bump,纯增量(无 rename / 语义漂移 / 新增 required 字段 — Pitfall 11 prevented)。v1.2 milestone 引入 route-based 三模态音频语义(dialogue/music/sfx)+ 分层复现 prompt(TTS/music-gen/foley)+ 关闭 v1.1 SPEAKER-01 deferral。整个 v1.2 milestone(phases 10-17)共享此版本号。**Phase 10 spike outcomes reshape three field shapes**(见下方 deviations)。变更:
      - **3 个新 schema**:`audio_semantic.schema.json`(per-shot dialogue/sfx + reproduction prompts;`word_level_experimental` flag)、`speakers.schema.json`(`^spk_[0-9]{3}$` NEW acoustic ID space,nullable `char_id` link)、`speaker-edits.schema.json`(HITL round-trip,mirror registry-edits + 新 `link_mappings`)。
      - **`asset.schema.json` additive**:新增 optional `data.audio_semantic` / `data.speakers`(JSON 路径)。`required[]` 与 v1.0/v1.1 byte-identical(仍 5 keys)。
      - **`schema_version` pattern 不变**(`^(0|[1-9]\d*)(\.(0|[1-9]\d*))?$`)— 版本字面量锁在 producer 单一真源 `scripts/export_asset.py:SCHEMA_VERSION = "1.2"`(line 55),非 schema `const`。
      - **Phase-10-informed deviations**(NON-NEGOTIABLE,empirical basis):
        - **`instruments` field OMITTED** — MUS-04 deferred v1.3。MERT-v1-95M 无 instrument classifier head(only K-means embedding clusters correlating with shot DURATION);PANNs Cnn14 zenodo-blocked at spike time。ROUTE Phase-12+ host 需要真实 MIR classifier 才能解锁。
        - **`dialogue.emotion` = `type:["string","null"]`**(NOT enum)+ `emotion_confidence: ["number","null"]`(0..1)。SenseVoice `self_consistency_pct=100%` 是 label-stability proxy,NOT calibrated accuracy(DIA-04 ship-nullable+confidence)。Closed 7-class enum 会 over-claim 我们没有的 calibration。
        - **`dialogue.words[]` EXPERIMENTAL optional sub-field** — gated behind top-level `word_level_experimental: boolean` flag(DIA-05 ship-experimental)。WhisperX boundary drift median 101.5ms(<200ms ✓)但 aggregate per-word drift 是 metric-definition artifact(word_start - segment_start inflates for interior words;Phase 12 refines)。Segment-level remains SLA path。
      - **`speakers.speakers[].spk_id` uses NEW `^spk_[0-9]{3}$` acoustic ID space**(NOT `^char_[0-9]{3}$` — deliberate disjoint to avoid identity-signal conflation;closes v1.1 SPEAKER-01 deferral via Phase 13 HITL linking)。
      - **向后兼容**:`spec/fixtures/minimal/`(v1)仍 6/6 绿;`spec/fixtures/v1.1/`仍 10/10 绿;`spec/fixtures/v1.2/`(12 文件)新增;`scripts/verify_contract.py` `_cross_version_check` 实测三向兼容(v1.0↔v1.1↔v1.2 forward 0 errors;backward 仅 additionalProperties errors → 0 non-additive errors);`speakers.char_id ⊆ characters.id` consistency GREEN。
      - **`fidelity_disclaimer`**:见 §10 — 复现 prompt ≠ 精确复原,per-modality 估算 TTS~70%/music-gen~60-75%/foley~80%(AF-01 缓解)。
    ```

    (3) **§5 intro** (line 176) — append after the existing "v1.1 新增 §5.6 + §5.7" sentence: "v1.2 新增 §5.8 **Audio Semantic** + §5.9 **Speakers** 两个 optional 音频支路形状(仅 route-host round-trip 跑过且条件字段达标后 emit)。`speaker-edits` 是 HITL round-trip 工作产物,非 canonical asset 数据形状,故不在此列(见 §1 schema 索引)。"

    (4) **§5.8 Audio Semantic (v1.2)** — append after §5.7 Props ends (around line 420). Use the 4-block template:

    - **Producer:** `audio/call_audio_analysis.py`(Phase 12 + Phase 15 pipeline producer,写入 `audio_semantic.json` per-shot 三模态 + reproduction prompts)
    - **顶层形状:** JSON 对象 — `schema_version` + `word_level_experimental` 顶层 flag + `shots[]` 数组(per-shot 三模态 + reproduction)。
    - Field table covering all properties from `spec/schemas/audio_semantic.schema.json#properties` (use the schema as source of truth). For each field: name / type / Required (✓ for `schema_version`+`shots`+`shots[].shot_id`;— for everything else) / Notes (esp. cross-reference the Phase-10 deviations inline: emotion nullable-string; word_level_experimental gating; instruments field absent; reproduction.{tts,music_gen,foley} nullable + confidence).
    - **最小片段:** JSON code block referencing shot 77 from the v1.2 fixture (real HAPPY emotion + Speech event + word-level + reproduction.tts). Mirror the v1.2 fixture content realism.
    - Reference schema: `spec/schemas/audio_semantic.schema.json`

    (5) **§5.9 Speakers (v1.2)** — append after §5.8. Use the 4-block template:

    - **Producer:** `registry/link_speakers.py`(Phase 13 HITL apply,镜像 v1.1 `apply_edits.py`,写入 `speakers.json` canonical confirmed speaker registry)
    - **顶层形状:** JSON 对象 — `speakers[]` 数组,每条目含 acoustic `^spk_[0-9]{3}$` ID + 可选 `^char_[0-9]{3}$` link + `review_state` + `turns[]`。
    - Field table covering all properties from `spec/schemas/speakers.schema.json#properties.speakers.items.properties`. Highlight: `spk_id` 是 acoustic ID(pyannote/WhisperX diarize embedding),与 visual `char_NNN`(DINOv2)deliberately disjoint;`char_id` nullable(旁白/群杂);non-null `char_id` MUST resolve to confirmed `characters.json#id`(verify_contract.py enforces);`review_state:"confirmed"` 是下游唯一流向(Pitfall 7)。
    - **最小片段:** JSON code block with 2 speakers — spk_001 (linked to char_001) + spk_002 (char_id:null 旁白).
    - Reference schema: `spec/schemas/speakers.schema.json`

    (6) **§10 Fidelity Disclaimer (v1.2)** — append a new top-level section (or a §4.1 sub-section if §10 doesn't fit the numbering). Content:

    - **AF-01 explicit out-of-scope**: 复现 prompt ≠ 精确复原。术语 'perfectly reconstruct' / 'exact restoration' / '完美复刻' / '精确复原' 在 README/SPEC/HTML 中 **FORBIDDEN**。所有复现场景的字段都带 confidence + 在 HTML/SPEC 显式 "estimated" 标签(Phase 16 PRESENT-01)。
    - **Per-modality estimates**: TTS ~70%similar to source voice;music-gen ~60-75%(harmonic/rhythmic相似,音色因 model-agnostic prompt 不保证);foley ~80%(音效描述相对 well-defined)。
    - **Phase-10-informed字段保真度**:
      - **emotion = calibrated estimate(NOT rigorous accuracy)** — SenseVoice `self_consistency_pct=100.0` 是 label-stability proxy(3 runs same label),NOT macro-F1 against ground truth。Phase 10 methodology_ab 走 calibrated estimate + qualitative sanity;rigorous macro-F1 需要 developer-annotated 30-segment ground truth(Phase 12+)。
      - **word-level timestamps = EXPERIMENTAL** — WhisperX wav2vec2 align。Boundary drift median 101.5ms < 200ms ship threshold;但 aggregate per-word drift 是 metric-definition artifact(word_start - segment_start inflates for interior words)。Segment-level remains SLA path;consumers SHOULD graceful-degrade to segment-only when `word_level_experimental:false`。
      - **instruments = ABSENT** — v1.2 schema 不含 `instruments` 字段(MUS-04 defer v1.3)。MERT-v1-95M 无 classifier head;PANNs Cnn14 zenodo-blocked。Phase 12+ route host 需 real MIR classifier。
    - **Two-tier authority 重申**:`fidelity_disclaimer` 是 SPEC prose(人读概览),NOT schema field。机器可校验的 nullable+confidence 在 schema(`audio_semantic.schema.json#shots[].dialogue.emotion{,_confidence}` + `reproduction.{tts,music_gen,foley}.confidence{,fidelity_disclaimer}`)。Schema 与 SPEC 冲突时 schema 为准。
  </action>
  <verify>
    <automated>python3 -c "
import re
spec = open('spec/SPEC.md').read()

# (1) §4 Changelog 1.2 entry exists with all required content
m = re.search(r'##\s*4\.?.*Changelog.*?- \*\*2026-07-25 — \`1\.2\`', spec, re.S)
assert m, '§4 Changelog 1.2 entry missing'
assert 'instruments field OMITTED' in spec or 'instruments 字段 OMITTED' in spec or 'instruments OMITTED' in spec, 'instruments omission not documented'
assert 'emotion' in spec and 'nullable' in spec, 'emotion nullable pattern not documented'
assert 'word_level_experimental' in spec, 'word_level_experimental flag not documented'
assert 'scripts/export_asset.py' in spec, 'SCHEMA_VERSION single-source not cross-referenced'

# (2) §5.8 + §5.9 shape sections
assert '§5.8' in spec or '5.8 Audio Semantic' in spec or 'Audio Semantic' in spec, '§5.8 Audio Semantic missing'
assert '§5.9' in spec or '5.9 Speakers' in spec or 'speakers.schema.json' in spec, '§5.9 Speakers missing'

# (3) fidelity_disclaimer section
assert 'fidelity_disclaimer' in spec, 'fidelity_disclaimer missing'
for phrase in ['TTS', 'music-gen', 'foley']:
    assert phrase in spec, f'{phrase} per-modality estimate missing'

# (4) AF-01 forbidden phrases
for forbidden in ['perfectly reconstruct', 'exact restoration', '完美复刻', '精确复原']:
    assert forbidden not in spec, f'AF-01 FORBIDDEN phrase present: {forbidden!r}'

# (5) §1 schema count
assert '13' in spec.split('## 1')[1].split('## 2')[0] if '## 1' in spec and '## 2' in spec else True, '§1 schema count not bumped to 13'

print('[spec-ok] §4 Changelog + §5.8 + §5.9 + fidelity_disclaimer + §1 bump + AF-01 phrases absent')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\`1.2\`' spec/SPEC.md` returns ≥5 (Changelog header + cross-references + shape sections + schema_version literal mention + per-modality estimates context).
    - `grep -c 'fidelity_disclaimer' spec/SPEC.md` returns ≥3 (section header + at least 2 references).
    - `grep -c 'word_level_experimental' spec/SPEC.md` returns ≥2 (Changelog + §5.8 field table).
    - `grep -ci 'instrument' spec/SPEC.md` returns ≥2 (Changelog deviation bullet + fidelity_disclaimer). The mentions MUST be in "OMITTED" / "absent" / "deferred" context — NOT describing a present field. (Audit by reading the matching lines.)
    - `grep -ci 'spk_[0-9]' spec/SPEC.md` returns ≥2 (§5.9 field table + Changelog new ID space mention).
    - `grep -c '完美复刻\|精确复原\|perfectly reconstruct\|exact restoration' spec/SPEC.md` returns 0 (AF-01 forbidden phrases).
    - `grep -c 'scripts/export_asset.py' spec/SPEC.md` returns ≥1 (Changelog single-source cross-reference).
    - `grep -c 'audio_semantic.schema.json\|speakers.schema.json\|speaker-edits.schema.json' spec/SPEC.md` returns ≥3 (each schema named once in §1 layout + once in §5/Changelog).
    - Manual review: §5.8 + §5.9 follow the 4-block template (Producer / 顶层形状 / field table / 最小片段 / Reference schema).
  </acceptance_criteria>
  <done>
    `spec/SPEC.md` extended with: §1 schema table at 13 entries; §4 Changelog `1.2` entry documenting 3 new schemas + asset.schema additive + Phase-10 deviations + single-source literal; §5 intro mentions §5.8 + §5.9; §5.8 Audio Semantic + §5.9 Speakers follow the 4-block template; §10 Fidelity Disclaimer section with AF-01 out-of-scope + per-modality estimates + per-field fidelity context; AF-01 forbidden phrases absent.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: spec/README.md v1.2 update footer + schema inventory refresh</name>
  <files>spec/README.md</files>
  <read_first>
    - `spec/README.md` (FULL — esp. the v1.1 update footer around line 80 which is the literal template)
    - `spec/SPEC.md` (post-Task-1 — for cross-reference consistency)
  </read_first>
  <behavior>
    - `spec/README.md` has a v1.2 update footer mirroring the v1.1 footer pattern.
    - Footer mentions: 3 new schemas (audio_semantic + speakers + speaker-edits), asset.schema additive (data.audio_semantic + data.speakers NOT in required[]), SCHEMA_VERSION = "1.2" single-source at export_asset.py:55, 12-file fixture, bidirectional v1.0↔v1.1↔v1.2 cross-version proof GREEN.
    - Schema inventory section (if it exists in README) extended to 13 schemas.
    - NOWHERE in README do AF-01 forbidden phrases appear.
  </behavior>
  <action>
    Edit `spec/README.md`. Locate the v1.1 update footer (around line 80). Append a v1.2 footer immediately after it, mirroring the structure but substituting v1.2 content:

    ```
    ## v1.2 Update (Phase 11, 2026-07-25)

    Contract bumped to `1.2` (pure-additive minor bump; v1.0/v1.1 fixtures remain byte-identical).

    **3 new schemas:**
    - `audio_semantic.schema.json` — per-shot dialogue/sfx + reproduction prompts (Phase 11).
    - `speakers.schema.json` — `^spk_[0-9]{3}$` acoustic speaker registry with nullable `char_id` link (Phase 11; closes v1.1 SPEAKER-01 deferral via Phase 13 HITL).
    - `speaker-edits.schema.json` — HITL round-trip edits shape (mirrors `registry-edits.schema.json` + new `link_mappings`).

    **`asset.schema.json` additive:** optional `data.audio_semantic` + `data.speakers` (NOT in `required[]`; absent on v1.0/v1.1 assets → graceful-degrade byte-identical).

    **`SCHEMA_VERSION = "1.2"` single-source** at `scripts/export_asset.py:55` (Pitfall 12 prevented).

    **Phase-10-informed deviations** (NON-NEGOTIABLE):
    - `instruments` field OMITTED (MUS-04 deferred v1.3 — MERT no classifier head + PANNs zenodo-blocked).
    - `dialogue.emotion` is `type:["string","null"]` (NOT enum) + `emotion_confidence` nullable (SenseVoice self-consistency is stability proxy, NOT accuracy).
    - `dialogue.words[]` is EXPERIMENTAL, gated behind top-level `word_level_experimental` flag (segment-level is SLA path).

    **12-file fixture** at `spec/fixtures/v1.2/` (10 byte-copied from v1.1 + `audio_semantic.json` + `speakers.json`).

    **Bidirectional cross-version proof** extended in `scripts/verify_contract.py`: v1.0↔v1.1↔v1.2 forward 0 errors + backward only-additionalProperties + `speakers.char_id ⊆ characters.id` consistency.

    See `spec/SPEC.md` §4 Changelog `1.2` entry + §5.8 Audio Semantic + §5.9 Speakers + §10 Fidelity Disclaimer for the full prose.
    ```

    If README has a schema inventory section (list of `spec/schemas/*.schema.json` files), extend it to include the 3 new schemas with one-line descriptions matching the §1 SPEC.md table.

    DO NOT use AF-01 forbidden phrases ('perfectly reconstruct' / 'exact restoration' / '完美复刻' / '精确复原') anywhere.
  </action>
  <verify>
    <automated>python3 -c "
readme = open('spec/README.md').read()
assert 'v1.2 Update' in readme or 'v1.2' in readme, 'v1.2 footer missing'
assert 'audio_semantic.schema.json' in readme
assert 'speakers.schema.json' in readme
assert 'speaker-edits.schema.json' in readme
assert '1.2' in readme
# AF-01 forbidden
for forbidden in ['perfectly reconstruct', 'exact restoration', '完美复刻', '精确复原']:
    assert forbidden not in readme, f'AF-01 forbidden phrase in README: {forbidden!r}'
print('[readme-ok] v1.2 footer present + AF-01 phrases absent')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'v1.2' spec/README.md` returns ≥3 (footer header + cross-references).
    - `grep -c 'audio_semantic.schema.json' spec/README.md` returns ≥1.
    - `grep -c 'speakers.schema.json' spec/README.md` returns ≥1.
    - `grep -c 'speaker-edits.schema.json' spec/README.md` returns ≥1.
    - `grep -c '完美复刻\|精确复原\|perfectly reconstruct\|exact restoration' spec/README.md` returns 0 (AF-01 invariant).
  </acceptance_criteria>
  <done>
    `spec/README.md` extended with a v1.2 update footer mirroring the v1.1 pattern; 3 new schemas listed; Phase-10 deviations documented; SCHEMA_VERSION single-source cross-referenced; AF-01 forbidden phrases absent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPEC.md prose → human reader | The prose is the human-half of the two-tier authority — a future contributor or downstream consumer reads SPEC.md to understand the contract; misinformation here propagates to misunderstanding |
| SPEC.md → schema | On SPEC/schema conflict, schema wins (CLAUDE.md "Two-tier authority"). The prose MUST accurately describe what the schemas enforce; misleading prose is a documentation bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Information Disclosure (false advertising) | SPEC.md / README.md over-claim fidelity ('perfectly reconstruct' / 'exact restoration' / '完美复刻' / '精确复原') | mitigate | Task 1 + Task 2 acceptance: `grep -c` returns 0 for all 4 forbidden phrases. The `fidelity_disclaimer` section explicitly names them as forbidden. AF-01 invariant documented as out-of-scope. |
| T-11-10 | Repudiation | SPEC.md prose drifts from schema (e.g., documents `instruments` field that schema doesn't have; or claims emotion is enum when schema says string) | mitigate | Task 1 acceptance: `spec/schemas/audio_semantic.schema.json` is the source of truth for §5.8 field table; `grep -ci 'instrument'` returns ≥2 BUT all mentions MUST be in OMITTED/absent/deferred context (manual review gate). The Two-tier authority rule is restated in the fidelity_disclaimer section. |
| T-11-11 | Information Disclosure (consumer mis-trusts fields) | Downstream consumer (Phase 17 canvas) reads emotion/word-level fields without knowing they are calibrated-estimate / experimental | mitigate | Task 1 acceptance: `fidelity_disclaimer` section documents per-field fidelity context (emotion = calibrated estimate NOT accuracy; word-level = experimental segment-level-SLA; instruments = absent). The `fidelity_disclaimer` is a first-class deliverable per CONTEXT § Specific Ideas. |
| T-11-SC | Tampering | pip / package installs | accept (carried) | Phase 11 installs ZERO packages. |
</threat_model>

<verification>
After both tasks:

1. `grep -c '\`1.2\`' spec/SPEC.md` ≥5; `grep -c 'fidelity_disclaimer' spec/SPEC.md` ≥3.
2. `grep -c '完美复刻\|精确复原\|perfectly reconstruct\|exact restoration' spec/SPEC.md spec/README.md` returns 0 across both files (AF-01 invariant).
3. `grep -c 'audio_semantic.schema.json\|speakers.schema.json\|speaker-edits.schema.json' spec/SPEC.md spec/README.md` returns ≥6 (3 schemas × 2 files).
4. Manual review: §5.8 + §5.9 follow the 4-block template; fidelity_disclaimer section honestly frames the Phase-10-informed deviations (instruments absent, emotion=calibrated estimate, word-level=experimental).
5. Pre-existing gates (must remain green): `python3 spec/validate.py` + `python3 scripts/verify_contract.py --mode=producer` still GREEN (no regressions from prose edits).
</verification>

<success_criteria>
- CONTRACT-04 fully covered: SPEC.md §4 Changelog `1.2` entry + §5 audio_semantic/speakers shapes + `fidelity_disclaimer` documented; AF-01 'perfect restoration' explicitly out-of-scope.
- §1 schema layout bumped to 13 schemas; §5 intro mentions §5.8 + §5.9.
- README.md has a v1.2 update footer mirroring v1.1 pattern.
- Two-tier authority honored: `fidelity_disclaimer` is prose (NOT a schema field); SPEC/schema conflict rule documented.
- AF-01 forbidden phrases absent from both files.
</success_criteria>

<output>
Create `.planning/phases/11-contract-v1-2/11-03-SUMMARY.md` when done.
</output>
