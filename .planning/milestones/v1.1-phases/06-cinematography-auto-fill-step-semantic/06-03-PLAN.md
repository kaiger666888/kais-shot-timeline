---
phase: 06-cinematography-auto-fill-step-semantic
plan: 03
type: execute
wave: 3
depends_on: ['06-01', '06-02']
files_modified:
  - run_pipeline.py
  - scripts/verify_phase6_smoke.py
autonomous: true
requirements: [CINEMA-02, CINEMA-03, CINEMA-06]
user_setup: []

must_haves:
  truths:
    - "run_pipeline.py exposes a step_semantic function that subprocess-calls analysis/call_shot_analysis.py with the 6 required flags + optional --offline"
    - "run_pipeline.py main() calls step_semantic between step_transcribe and step_timeline (slot 5 of 7)"
    - "Every existing [N/6] step label in run_pipeline.py is renumbered to [N/7] (17 occurrences across [1/6]..[6/6])"
    - "step_semantic labels use [5/7] (the new slot 5)"
    - "step_timeline (previously [5/6]) is renumbered to [6/7]; step_export (previously [6/6]) is renumbered to [7/7]"
    - "run_pipeline.py argparse exposes 4 new flags: --skip-semantic, --offline, --analysis-url (default http://127.0.0.1:8000/api/v1/production/shot-analysis), --analysis-timeout (type=float, default 960.0)"
    - "Each new flag has a Chinese help= string matching CLAUDE.md convention"
    - "run_pipeline.py --force clears the route_cache/ directory (rmtree) and prompts.json in addition to the existing cache list"
    - "scripts/verify_phase6_smoke.py exercises 3 scenarios (route-down, --skip-semantic, cache-hit) using a temp work_dir + captured fixture, exits 0 on all green"
    - "--skip-semantic causes step_semantic to return None without subprocess-calling analysis/call_shot_analysis.py"
    - "prompts.json cache check in step_semantic uses _safe_mtime helper (mirrors step_export TOCTOU-safe pattern from 02-REVIEW WR-07)"
  artifacts:
    - path: "run_pipeline.py"
      provides: "step_semantic function + main() integration + [N/7] renumber + 4 new flags + --force cache list extension"
      contains: "step_semantic"
    - path: "scripts/verify_phase6_smoke.py"
      provides: "Phase 6 end-to-end smoke regression (3 scenarios: route-down, --skip-semantic, cache-hit)"
      contains: "route_down"
  key_links:
    - from: "run_pipeline.py:main"
      to: "analysis/call_shot_analysis.py"
      via: "subprocess.run([sys.executable, str(HERE/'analysis'/'call_shot_analysis.py'), ...], check=True)"
      pattern: "analysis.*call_shot_analysis\\.py"
    - from: "run_pipeline.py:step_semantic"
      to: "output/<asset>/prompts.json"
      via: "step output (consumed by step_timeline + step_export)"
      pattern: "prompts\\.json"
    - from: "run_pipeline.py:main --force"
      to: "output/<asset>/route_cache/"
      via: "rmtree the cache dir on forced rerun"
      pattern: "route_cache"
    - from: "run_pipeline.py:step_export"
      to: "scripts/export_asset.py (Plan 01 warnings plumbing)"
      via: "subprocess call (unchanged) — export_asset picks up route_cache/warnings.json sidecar automatically"
      pattern: "export_asset\\.py"
---

<objective>
Wire the new `step_semantic` stage into `run_pipeline.py`: insert it between `step_transcribe` and `step_timeline`, add 4 CLI flags (`--skip-semantic`, `--offline`, `--analysis-url`, `--analysis-timeout`), renumber the existing 17 `[N/6]` step labels to `[N/7]` (with `step_semantic` taking slot 5, `step_timeline` moving to `[6/7]`, `step_export` to `[7/7]`), and extend the `--force` cache-clear list to include `prompts.json` + the `route_cache/` directory. Then build `scripts/verify_phase6_smoke.py` — a permanent regression harness that proves the 3 verifiable scenarios work end-to-end (route-down degrade, `--skip-semantic`, cache-hit) using a temp work_dir + the 7 captured fixtures from Plan 02.

Purpose: Close CINEMA-02 (step integration + counter), CINEMA-06 (CLI flags), and the integration half of CINEMA-03 (the `--skip-semantic` flag in run_pipeline; the analysis-side degrade logic landed in Plan 02). Live route round-trip remains deferred per STATE.md blocker — the smoke harness covers the 3 modes that ARE testable now.

Output: 1 modified pipeline orchestrator + 1 new regression script. After this plan, the full Phase 6 is shippable as a graceful-degrade producer; the post-merge live round-trip becomes a documented smoke check (not a Phase 6 gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cinematography-auto-fill-step-semantic/06-CONTEXT.md
@.planning/phases/06-cinematography-auto-fill-step-semantic/06-RESEARCH.md
@.planning/phases/06-cinematography-auto-fill-step-semantic/06-VALIDATION.md
@.planning/phases/06-cinematography-auto-fill-step-semantic/06-01-PLAN.md
@.planning/phases/06-cinematography-auto-fill-step-semantic/06-02-PLAN.md
@run_pipeline.py

<interfaces>
<!-- CLI contract of analysis/call_shot_analysis.py (Plan 02) — the script this plan's step_semantic subprocess-calls. -->

```
analysis/call_shot_analysis.py
  --video <abs path>             (required)
  --shots <abs path>             (required — shots.json)
  --work-dir <abs path>          (required — output/<asset>/)
  --output <abs path>            (required — prompts.json path; usually <work-dir>/prompts.json)
  --analysis-url <url>           (default http://127.0.0.1:8000/api/v1/production/shot-analysis)
  --analysis-timeout <float>     (default 960.0; > route-side 900s execFileSync ceiling)
  --offline                      (store_true; cache-only, no network)
```

Exit codes: 0 on success (including full graceful-degrade — empty facets + warnings sidecar still exits 0); non-zero only on schema-self-validation failure (prompts.json invalid per Draft202012Validator — fails loud).

Side effects: writes `<work-dir>/prompts.json` (atomic temp+replace); writes `<work-dir>/route_cache/warnings.json` (shape `{"warnings": [...]}` — consumed by scripts/export_asset.py per Plan 01); writes/reads `<work-dir>/route_cache/shot_analysis/shot_XXX.json` (per-shot cache with embedded `_cache_key`).

<!-- run_pipeline.py step pattern (load-bearing for CINEMA-02) — the shape step_semantic must mirror. -->

From run_pipeline.py:step_transcribe (the canonical reference — step_semantic slots right after it):
```python
def step_transcribe(video: str, transcript: str, skip: bool,
                    model: str, language: str, device: str,
                    backend: str) -> str:
    if skip:
        print("[4/6] --skip-transcribe: skipping Whisper")
        return transcript if os.path.exists(transcript) else None
    if os.path.exists(transcript):
        print(f"[4/6] cached transcript: {transcript}")
        return transcript
    cmd = [sys.executable, str(HERE / "audio" / "transcribe.py"),
           "--input", video, "--output", transcript,
           "--model", model, "--language", language,
           "--backend", backend]
    if device:
        cmd += ["--device", device]
    run_step(cmd, "[4/6] Whisper transcription")
    return transcript
```

step_semantic mirrors this: `skip` short-circuits (returns existing prompts.json if present else None); cache check via `_safe_mtime(prompts_json) > _safe_mtime(shots_json)` (mirrors step_export TOCTOU-safe pattern); subprocess to `analysis/call_shot_analysis.py`; thread `--offline` conditionally.

<!-- --force cache list (load-bearing for CINEMA-02 integration) — currently at run_pipeline.py:326-329. -->

Current `--force` block:
```python
for p in (shots_json, frames_json, audio_json, transcript, out_html,
          asset_json, asset_json + ".video-stamp"):
    if os.path.exists(p):
        os.unlink(p)
```
Plan 03 extends this tuple to ALSO clear: `prompts_json` (new producer in step_semantic) and the `route_cache/` directory (rmtree, not unlink — directory). The directory case needs an `os.path.isdir(p)` branch with `shutil.rmtree` (import shutil at top-of-block — lazy/local import like elsewhere).

<!-- [N/6] occurrences (load-bearing for CINEMA-02 counter bump) — grep verified. -->

EXACTLY 17 strings of the form `[N/6]` (where N is 1..6) exist in run_pipeline.py today, found via `grep -nE '\[[1-6]/6\]'`:
- [1/6]: lines 68, 72, 74 (codec / H264 cache / transcode banner — 3 occurrences)
- [2/6]: lines 92, 95, 102 (detect skip / cache / V3b banner — 3 occurrences)
- [3/6]: lines 110, 113, 121 (separate skip / cache / Demucs banner — 3 occurrences)
- [4/6]: lines 129, 132, 140 (transcribe skip / cache / Whisper banner — 3 occurrences)
- [5/6]: lines 152, 170 (timeline cache + banner — 2 occurrences)
- [6/6]: lines 217, 249, 258 (export skip / cache / banner — 3 occurrences)

Plan 3 renumbers: [1/6]→[1/7], [2/6]→[2/7], [3/6]→[3/7], [4/6]→[4/7], [5/6]→[6/7] (timeline moves to slot 6), [6/6]→[7/7] (export moves to slot 7). The new step_semantic adds 3 new [5/7] occurrences (skip / cache / banner). Final state: 20 occurrences of `[N/7]` (17 renumbered + 3 new). NOTE: research summary said "18" but the actual count is 17 (off-by-one in research's summary text — the table that follows lists groups summing to 17).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_pipeline.py — add step_semantic function + main() integration + 4 flags + [N/7] renumber + --force cache list</name>
  <files>run_pipeline.py</files>
  <read_first>
    - run_pipeline.py (the entire file — focus on step_transcribe lines 125-141 as the template; main() lines 271-368 for sequencing + argparse; --force block lines 324-330; step_export TOCTOU-safe mtime pattern lines 220-250 for the _safe_mtime reuse)
    - .planning/phases/06-cinematography-auto-fill-step-semantic/06-RESEARCH.md (§Architecture Patterns → "run_pipeline.py Integration" — the full step_semantic skeleton + argparse flags + --force extension; §Code Examples → Example 2 export_asset warnings sidecar read — confirms Plan 01 closed that half)
    - .planning/phases/06-cinematography-auto-fill-step-semantic/06-CONTEXT.md (lines 30-31: step counter [N/7] lock; lines 40-42: 4 flags lock)
    - analysis/call_shot_analysis.py (from Plan 02 — the CLI contract above is the interface)
  </read_first>
  <action>
**[N/7] renumber (CINEMA-02 counter):** Use search-and-replace to rename the existing 17 `[N/6]` strings. Replace `[1/6]`→`[1/7]`, `[2/6]`→`[2/7]`, `[3/6]`→`[3/7]`, `[4/6]`→`[4/7]`, `[5/6]`→`[6/7]` (timeline moves to slot 6 — DO NOT just change 6→7 blindly; the 5→6 shift is the slot move), `[6/6]`→`[7/7]` (export moves to slot 7). Order matters: do `[6/6]`→`[7/7]` and `[5/6]`→`[6/7]` BEFORE any global `/6]`→`/7]` (if attempted) to avoid double-shift. Safest: do literal-string replaces per token, in the order `[6/6]`, `[5/6]`, `[4/6]`, `[3/6]`, `[2/6]`, `[1/6]` (high-N first prevents `[5/6]` matching `[5/7]` artifacts). After this, the file has 17 occurrences of `[N/7]` and ZERO occurrences of `[N/6]` — verify via `grep -cE '\[[1-6]/6\]' run_pipeline.py` == 0.

**Update module docstring (lines 1-35):** Add step 5 to the ordered list — between "4. Whisper 转录（audio/transcribe.py）" and "5. 生成时间轴双面板 HTML" — insert a new line `5. 运镜语义分析路由调用（analysis/call_shot_analysis.py）` and renumber the remaining two: timeline becomes 6, export becomes 7. Update the output layout block (around lines 22-34) to note `prompts.json` is now produced by step 5 (not "外部步骤产出"), and that `route_cache/shot_analysis/shot_XXX.json` + `route_cache/warnings.json` are new runtime artifacts of step 5.

**Add `step_semantic` function** (place it between `step_transcribe` and `step_timeline` — physically between lines 141 and 144 in the post-renumber file). Mirror the step_transcribe shape:
```python
def step_semantic(video: str, work_dir: str, shots_json: str,
                  prompts_json: str, skip: bool, offline: bool,
                  analysis_url: str, analysis_timeout: float) -> str:
    """[5/7] 运镜语义分析 —— 调用 kais-aigc-platform shot-analysis 路由，填 prompts.json facets。

    首个网络依赖步骤。Graceful-degrade: 路由不可达时 call_shot_analysis.py 写空 facets
    + warnings sidecar，资产仍导出（CONTEXT D-XX 锁）。--skip-semantic 整步跳过；
    --offline 压制网络（仅读 route_cache/）。
    """
    if skip:
        print("[5/7] --skip-semantic: skipping cinematography analysis")
        return prompts_json if os.path.exists(prompts_json) else None
    # TOCTOU-safe mtime cache (mirror step_export 02-REVIEW WR-07)；offline 模式不跳过（仍读 cache）
    if (os.path.exists(prompts_json)
            and _safe_mtime(prompts_json) > _safe_mtime(shots_json)):
        print(f"[5/7] cached prompts: {prompts_json}")
        return prompts_json
    cmd = [sys.executable, str(HERE / "analysis" / "call_shot_analysis.py"),
           "--video", video, "--shots", shots_json,
           "--work-dir", work_dir, "--output", prompts_json,
           "--analysis-url", analysis_url,
           "--analysis-timeout", str(analysis_timeout)]
    if offline:
        cmd += ["--offline"]
    run_step(cmd, "[5/7] cinematography analysis (shot-analysis route)")
    return prompts_json if os.path.exists(prompts_json) else None
```

This adds 3 new `[5/7]` occurrences (skip label + cache label + run_step banner) — combined with the 17 renumbered, the file ends with 20 `[N/7]` occurrences total.

**Insert `step_semantic` call in main()** (between the existing `step_transcribe` call ~line 346-348 and the `step_timeline` call ~line 350-354, in the post-renumber file). Compute `prompts_json = os.path.join(work_dir, "prompts.json")` near the other path constants (around lines 315-320). Insert:
```python
# 5. 运镜语义分析（shot-analysis 路由 —— 首个网络依赖步骤）
step_semantic(video, work_dir, shots, prompts_json,
              args.skip_semantic, args.offline,
              args.analysis_url, args.analysis_timeout)
```

The return value is intentionally not captured — step_timeline + step_export already check `os.path.exists(prompts_json)` themselves. If step_semantic fails to produce prompts.json (e.g. schema validation failure causes sys.exit), `subprocess.run(check=True)` will raise CalledProcessError and the pipeline halts (fails loud — project convention). For graceful-degrade (route down), call_shot_analysis.py STILL writes prompts.json with empty facets + exits 0 — so the pipeline continues.

**Add 4 argparse flags** (insert into the argparse block ~lines 272-304, near the other `--skip-*` and config flags):
```python
ap.add_argument("--skip-semantic", action="store_true",
                help="跳过运镜语义分析（shot-analysis 路由调用）")
ap.add_argument("--offline", action="store_true",
                help="全局：仅读 route_cache 不联网（缓存命中即用，miss 则降级空 facets）")
ap.add_argument("--analysis-url",
                default="http://127.0.0.1:8000/api/v1/production/shot-analysis",
                help="shot-analysis 路由 URL（含 /api/v1/production/shot-analysis path；"
                     "默认端口 8000 — 路由 unmerged，首跑需 verify 实际端口）")
ap.add_argument("--analysis-timeout", type=float, default=960.0,
                help="单次路由调用 read 超时秒（默认 960，> 路由侧 900s execFileSync）")
```

All 4 carry Chinese `help=` per CLAUDE.md convention. The `--analysis-url` default notes "首跑需 verify 实际端口" (STATE.md blocker documentation — aigc-platform route port unverified until branch merges).

**Extend `--force` cache list** (current block lines 324-329): change the iteration tuple to ALSO include `prompts_json` and `os.path.join(work_dir, "route_cache")`. Because `route_cache/` is a directory, add an `isdir` branch with `shutil.rmtree`. Use a local `import shutil` at the top of the `if args.force:` block (lazy import — matches project convention). Shape:
```python
if args.force:
    import shutil
    for p in (shots_json, frames_json, audio_json, transcript, out_html,
              asset_json, asset_json + ".video-stamp",
              prompts_json,                                      # NEW (Phase 6)
              os.path.join(work_dir, "route_cache")):            # NEW (Phase 6)
        if os.path.isdir(p):
            shutil.rmtree(p, ignore_errors=True)
        elif os.path.exists(p):
            os.unlink(p)
    print(f"[force] cleared cache under {work_dir}")
```

`ignore_errors=True` on rmtree is intentional — if route_cache is partially corrupt or already gone, the force-rerun should still proceed (don't let a stale-cache-rmtree failure block a fresh run).

After edits, run `python3 run_pipeline.py --help` to confirm the 4 new flags appear with their Chinese help strings.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "
import ast, re
src = open('run_pipeline.py', encoding='utf-8').read()
# 1) [N/6] fully purged; [N/7] present
assert not re.search(r'\[[1-6]/6\]', src), 'residual [N/6] in run_pipeline.py'
n7_count = len(re.findall(r'\[[1-7]/7\]', src))
assert n7_count == 20, f'expected 20 [N/7] occurrences (17 renumbered + 3 new step_semantic), got {n7_count}'
# 2) step_semantic function + main() integration
assert 'def step_semantic(' in src, 'step_semantic function missing'
assert 'step_semantic(video, work_dir' in src, 'step_semantic not called in main()'
# 3) call_shot_analysis.py subprocess path
assert 'analysis' in src and 'call_shot_analysis.py' in src, 'subprocess to call_shot_analysis.py missing'
# 4) 4 new argparse flags
for flag in ('--skip-semantic', '--offline', '--analysis-url', '--analysis-timeout'):
    assert flag in src, f'{flag} missing from argparse'
# 5) --force cache list extended
assert 'route_cache' in src, 'route_cache not in --force cache list'
assert 'shutil.rmtree' in src or 'rmtree' in src, 'rmtree missing for route_cache dir clear'
# 6) module docstring updated (step 5 in the ordered list)
assert '5. 运镜语义分析' in src or 'analysis/call_shot_analysis.py' in src[:2000], 'module docstring not updated'
# 7) file parses as valid Python
ast.parse(src)
print(f'OK: run_pipeline.py integration complete ({n7_count} [N/7] occurrences)')
" && python3 run_pipeline.py --help 2>&1 | grep -cE '\-\-skip-semantic|\-\-offline|\-\-analysis-url|\-\-analysis-timeout' | grep -q '^4$' && echo OK: all 4 flags appear in --help</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\[[1-6]/6\]' run_pipeline.py` returns 0 (all old counters purged)
    - `grep -cE '\[[1-7]/7\]' run_pipeline.py` returns exactly 20 (17 renumbered + 3 new [5/7] in step_semantic)
    - `grep -c 'def step_semantic' run_pipeline.py` equals 1
    - `grep -c 'analysis.*call_shot_analysis.py' run_pipeline.py` ≥ 1 (subprocess invocation)
    - `python3 -c "import ast; ast.parse(open('run_pipeline.py').read())"` exits 0 (file is valid Python)
    - `python3 run_pipeline.py --help 2>&1 | grep -cE '\-\-skip-semantic|\-\-offline|\-\-analysis-url|\-\-analysis-timeout'` returns 4 (all flags surfaced)
    - `python3 run_pipeline.py --help 2>&1 | grep -c '跳过\|仅读\|超时' ` ≥ 3 (Chinese help strings present, CLAUDE.md convention)
    - `grep -c 'prompts_json' run_pipeline.py` ≥ 3 (path constant defined + used in step_semantic + in --force list)
    - `grep -c 'route_cache' run_pipeline.py` ≥ 2 (--force list + step_semantic reads/writes via subprocess)
    - Module docstring (lines 1-35) lists step 5 as `analysis/call_shot_analysis.py` between transcribe (4) and timeline (now 6); step counter on the docstring's ordered list reads 1-7
  </acceptance_criteria>
  <done>
run_pipeline.py exposes step_semantic (mirror of step_transcribe shape with skip/cache/subprocess); main() calls it between step_transcribe and step_timeline; 17 old [N/6] strings renamed to [N/7] (with timeline→[6/7], export→[7/7]); 3 new [5/7] labels in step_semantic; 4 new argparse flags with Chinese help; --force cache list clears prompts.json + route_cache/ dir (rmtree ignore_errors); module docstring updated to reflect 7-step pipeline.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create scripts/verify_phase6_smoke.py — 3-scenario regression (route-down, --skip-semantic, cache-hit)</name>
  <files>scripts/verify_phase6_smoke.py</files>
  <behavior>
    - Scenario 1 (route-down): run call_shot_analysis.py against unreachable URL (http://127.0.0.1:1/) on a temp work_dir + synthetic shots.json → prompts.json written, all 5 route-sourced facets empty across all shots, route_cache/warnings.json populated with ≥1 entry, prompts.json schema-valid → exit 0
    - Scenario 2 (--skip-semantic): run run_pipeline.py with --skip-semantic on a temp work_dir (with pre-existing empty prompts.json) → step_semantic returns without subprocess-calling call_shot_analysis.py (capture stdout, assert NO "[5/7] cinematography analysis" banner) → exit 0
    - Scenario 3 (cache-hit / --offline): pre-seed route_cache/shot_analysis/shot_001.json with captured fixture + correct _cache_key, run call_shot_analysis.py --offline → prompts.json uses cached facet values (camera="中景, follow, fast, pan_right" for shot 1 if seeded with shot_003 fixture), 0 httpx post calls (assert via stdout "[semantic] shot 1: cache hit") → exit 0
    - Overall: all 3 scenarios green → script prints "[phase6-smoke] OK: 3/3 scenarios green" and exits 0; any failure → exit 1 with diagnostic
  </behavior>
  <read_first>
    - scripts/verify_contract.py (the existing regression harness pattern — sys.exit(0/1) on pass/fail; clean stdout labels like [self-test] / [producer]; PHASE4_* env var convention; argparse + Chinese description)
    - analysis/call_shot_analysis.py (Plan 02 — the script under test in scenarios 1 + 3; its stdout labels confirm cache hit / failure)
    - run_pipeline.py (Plan 03 Task 1 — the script under test in scenario 2; its --skip-semantic path)
    - examples/shot_analysis/shot_003.json (Plan 02 Task 2 — the seed fixture for scenario 3)
    - .planning/phases/06-cinematography-auto-fill-step-semantic/06-VALIDATION.md (Manual-Only Verifications — confirms live round-trip is deferred, these 3 scenarios are the verifiable scope)
    - .planning/phases/06-cinematography-auto-fill-step-semantic/06-RESEARCH.md (§Validation Architecture — test commands and expected outputs)
  </read_first>
  <action>
Create a NEW standalone regression script `scripts/verify_phase6_smoke.py` (NOT pytest — VALIDATION.md confirms "Framework: None, standalone Python sys.exit(0/1); inline jsonschema"). Module docstring (Chinese) explains: purpose (Phase 6 graceful-degrade + cache regression), the 3 scenarios, that live round-trip is deferred (STATE.md blocker), and CLI usage `python3 scripts/verify_phase6_smoke.py`.

**Common helpers:**
- `_tmp_work_dir()` — `tempfile.mkdtemp(prefix="phase6-smoke-")`; returns the path; cleanup via atexit or try/finally.
- `_synthetic_shots_json(path, count=2)` — writes `[{"id":1,"start_sec":0.0,"end_sec":1.0,"duration":1.0}, {"id":2,"start_sec":1.0,"end_sec":2.0,"duration":1.0}]` (2 shots is enough to prove the loop + per-shot degrade).
- `_check_prompts_valid(work_dir)` — loads `<work_dir>/prompts.json`, runs `Draft202012Validator(prompts.schema.json).iter_errors`, returns the error list (empty = valid).
- `_run(cmd, **kw)` — wrapper around `subprocess.run(cmd, capture_output=True, text=True, **kw)`; returns the CompletedProcess.

**Scenario 1 — route-down (CINEMA-03):**
- Create temp work_dir; write synthetic shots.json (2 shots).
- Run: `[sys.executable, "analysis/call_shot_analysis.py", "--video", <any existing small file, e.g. scripts/verify_phase6_smoke.py itself or a tiny test fixture>, "--shots", shots_json_path, "--work-dir", work_dir, "--output", prompts_json_path, "--analysis-url", "http://127.0.0.1:1/api/v1/production/shot-analysis", "--analysis-timeout", "2"]`. (Port 1 is reserved/unroutable — ConnectError immediate. Timeout 2s keeps the test fast.)
- Assert: returncode == 0 (graceful-degrade does NOT exit non-zero); `prompts.json` exists; load it; assert `len(prompts) == 2`; for each entry, assert `entry["camera"] == ""` and `entry["action"] == ""` and `entry["lighting"] == ""` and `entry["style"] == ""` (all route-sourced facets empty); assert `_check_prompts_valid(work_dir) == []`; assert `route_cache/warnings.json` exists; load it; assert `len(warnings["warnings"]) >= 1` and `"preflight" in warnings["warnings"][0] or "ConnectError" in warnings["warnings"][0]`.

**Scenario 2 — --skip-semantic (CINEMA-06):**
- Create temp work_dir; write synthetic shots.json; pre-create an empty/trivial prompts.json (so step_export wouldn't fail on missing-file if it ever ran — though we won't get that far).
- Use `unittest.mock.patch` or a direct call to `run_pipeline.step_semantic` (import it as a Python function — faster than subprocess; avoids the whole pipeline). Call `step_semantic(video=<existing tiny file>, work_dir=work_dir, shots_json=shots_json_path, prompts_json=prompts_json_path, skip=True, offline=False, analysis_url="http://127.0.0.1:1/", analysis_timeout=2.0)` while capturing stdout (use `contextlib.redirect_stdout(io.StringIO())`).
- Assert: return value is `prompts_json_path` (because the pre-existing prompts.json was found) OR None (if you didn't pre-create it — either is acceptable per the function contract); captured stdout contains `"--skip-semantic: skipping"` AND does NOT contain `"cinematography analysis (shot-analysis route)"` (the run_step banner — proves no subprocess fired).

**Scenario 3 — cache-hit / --offline (CINEMA-04):**
- Create temp work_dir; write synthetic shots.json (1 shot, id=1).
- Pre-seed cache: copy `examples/shot_analysis/shot_003.json` to `<work_dir>/route_cache/shot_analysis/shot_001.json` BUT inject the correct `_cache_key` block — load the fixture, add `"_cache_key": {"video_content_hash": <precomputed hash of the tiny --video file>, "route_name": "shot_analysis", "route_version": "feat-shot-analysis-route-v1"}`, dump it back. (Use `analysis.call_shot_analysis.video_content_hash` to compute the hash — import the function.)
- Run: `[sys.executable, "analysis/call_shot_analysis.py", "--video", <same tiny file>, "--shots", shots_json_path, "--work-dir", work_dir, "--output", prompts_json_path, "--offline", "--analysis-url", "http://127.0.0.1:1/...", "--analysis-timeout", "2"]`. (URL is unreachable but should NOT be hit because of cache + --offline.)
- Assert: returncode == 0; `prompts.json` exists; load it; assert `len(prompts) == 1`; assert `prompts[0]["camera"] == "中景, follow, fast, pan_right"` (the cached shot_003 facet values — proves cache was used, not empty degrade); assert `_check_prompts_valid(work_dir) == []`; capture stdout of the subprocess; assert `"shot 1: cache hit"` in stdout (proves cache path triggered); assert `"FAIL"` not in stdout (no failure path).

**main() / CLI:**
- `ap = argparse.ArgumentParser(description="Phase 6 graceful-degrade + cache 回归校验")`; `--verbose` flag optional.
- Run all 3 scenarios in order; collect `(name, ok, detail)` tuples; print `[phase6-smoke] {name}: PASS` or `[phase6-smoke] {name}: FAIL — {detail}` per scenario.
- At end: if all 3 PASS, print `[phase6-smoke] OK: 3/3 scenarios green` and `sys.exit(0)`; else `sys.exit(1)`.
- Clean up temp dirs in a `finally` block.

Mirror `scripts/verify_contract.py` style: bracketed prefix tags, exit-code contract, no external deps beyond stdlib + jsonschema (already in env). Chinese comments where they aid clarity.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 scripts/verify_phase6_smoke.py && python3 -c "
import ast
src = open('scripts/verify_phase6_smoke.py', encoding='utf-8').read()
ast.parse(src)
# Contract: 3 scenarios explicitly defined
assert 'route' in src.lower() and 'skip' in src.lower() and 'cache' in src.lower()
assert 'sys.exit(0)' in src and 'sys.exit(1)' in src
print('OK: verify_phase6_smoke.py exists + 3 scenarios green + exit contract present')
"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/verify_phase6_smoke.py` exits 0
    - Output contains the literal string `[phase6-smoke] OK: 3/3 scenarios green`
    - Output contains 3 `PASS` lines (one per scenario)
    - `grep -c 'http://127.0.0.1:1' scripts/verify_phase6_smoke.py` ≥ 2 (scenarios 1 + 3 both use unroutable URL to prove graceful-degrade / cache-hit-doesn't-touch-network)
    - `grep -c '_cache_key' scripts/verify_phase6_smoke.py` ≥ 1 (scenario 3 pre-seeds cache correctly)
    - `python3 -c "import ast; ast.parse(open('scripts/verify_phase6_smoke.py').read())"` exits 0
    - `python3 spec/validate.py` still exits 0 (no schema regression — Plan 03 doesn't touch schemas)
    - `python3 scripts/verify_contract.py --mode=producer` still exits 0 (no contract regression)
  </acceptance_criteria>
  <done>
scripts/verify_phase6_smoke.py exists; 3 scenarios (route-down → empty facets + warnings sidecar schema-valid; --skip-semantic → step_semantic returns without subprocess; cache-hit/--offline → cached facet values used, no network) all green via `python3 scripts/verify_phase6_smoke.py`; output prints "[phase6-smoke] OK: 3/3 scenarios green"; spec/validate.py + verify_contract.py both still green (no regression).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator→Pipeline | Operator invokes `python3 run_pipeline.py --video ...` with CLI flags. The 4 new flags (`--skip-semantic` / `--offline` / `--analysis-url` / `--analysis-timeout`) cross this boundary. |
| Pipeline→Analysis stage | `run_pipeline.py:step_semantic` subprocess-calls `analysis/call_shot_analysis.py`. Same trust boundary (operator invoked both). |
| Regression harness→Temp FS | `scripts/verify_phase6_smoke.py` writes temp work_dirs under `/tmp/phase6-smoke-*`. Cleanup in finally block. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | `--force` rmtree scope | mitigate | `--force` clears only known paths under the operator-controlled `work_dir`: `shots.json`, `frames.json`, `audio_analysis.json`, `transcript.json`, `timeline.html`, `asset.json`, `asset.json.video-stamp`, `prompts.json`, and the `route_cache/` directory. No user-supplied paths fed to rmtree. `ignore_errors=True` on rmtree prevents partial-corrupt cache from blocking rerun. |
| T-06-12 | Denial of Service | Operator-forgets-timeout-flag → 960s × N hang | mitigate | `--analysis-timeout` default 960.0s is explicit (cannot be zero); preflight short-circuit (Plan 02) means route-down fails fast (5s preflight) not slow (960s × N). Pitfall 7 prevention inherited from Plan 02. |
| T-06-13 | Information Disclosure | Step banner prints `--analysis-url` | accept | The URL is operator-supplied via CLI; printing it back in the `[5/7] cinematography analysis (shot-analysis route)` banner is consistent with every other step's banner pattern (which prints full cmd). No secrets in URL (local dev; no auth). CONTEXT D-XX documents URL is operator-controlled. |
| T-06-14 | Repudiation | step_semantic silently skips on cached prompts.json | mitigate | Both the `--skip-semantic` and the cache-hit paths print explicit `[5/7]` labeled stdout (`--skip-semantic: skipping` / `cached prompts: ...`) — operator can always see what ran. Fails-loud path (schema validation error) raises CalledProcessError. |
| T-06-15 | Tampering | Regression harness temp-dir leak | mitigate | `tempfile.mkdtemp` + `finally: shutil.rmtree(tmp, ignore_errors=True)` — temp work_dirs always cleaned. `ignore_errors=True` ensures a corrupt temp dir never blocks the harness from running. |
| T-06-SC | Tampering | pip/npm/cargo installs | accept | Plan 03 adds ZERO new packages — uses only stdlib (subprocess/tempfile/argparse/contextlib/io/json) + existing project deps (jsonschema 4.26.0, httpx via Plan 02's script). No install tasks. |
</threat_model>

<verification>
After both tasks complete:
1. `python3 run_pipeline.py --help` shows all 4 new flags with Chinese help strings.
2. `grep -cE '\[[1-6]/6\]' run_pipeline.py` returns 0; `grep -cE '\[[1-7]/7\]' run_pipeline.py` returns 20.
3. `python3 scripts/verify_phase6_smoke.py` exits 0 and prints `[phase6-smoke] OK: 3/3 scenarios green`.
4. `python3 spec/validate.py` exits 0 (no schema regression — Plan 03 doesn't touch schemas).
5. `python3 scripts/verify_contract.py --mode=producer` exits 0 (no contract regression).
6. **Phase 6 gate (all 3 plans together):**
   - `python3 spec/validate.py` green (Plan 01 schema + fixture sync)
   - `python3 -c "from analysis.call_shot_analysis import compose_facets; ..."` (Plan 02 mapping check) green
   - `python3 scripts/verify_phase6_smoke.py` (Plan 03 3-scenario regression) green
   - Manual-only: live route round-trip (DEFERRED per STATE.md until feat/shot-analysis-route merges — documented in 06-VALIDATION.md).
</verification>

<success_criteria>
- `run_pipeline.py` exposes `step_semantic` (mirror of step_transcribe); main() calls it between step_transcribe (slot 4) and step_timeline (now slot 6); step_export moves to slot 7.
- All 17 old `[N/6]` step labels renamed to `[N/7]`; 3 new `[5/7]` labels added in step_semantic; total 20 `[N/7]` occurrences.
- 4 new argparse flags (`--skip-semantic`, `--offline`, `--analysis-url`, `--analysis-timeout`) with Chinese help.
- `--force` clears `prompts.json` + `route_cache/` directory (rmtree ignore_errors).
- `scripts/verify_phase6_smoke.py` exercises route-down + --skip-semantic + cache-hit scenarios; exits 0 on all green.
- No regression: `spec/validate.py` + `scripts/verify_contract.py --mode=producer` both still green.
- CINEMA-02 (step + counter), CINEMA-06 (flags), and the integration half of CINEMA-03 (--skip-semantic in run_pipeline) are closed. Live route round-trip explicitly deferred per STATE.md.
</success_criteria>

<output>
Create `.planning/phases/06-cinematography-auto-fill-step-semantic/06-03-SUMMARY.md` when done
</output>
