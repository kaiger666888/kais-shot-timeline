---
phase: 08-prompt-reference-system-shot-timeline-html-gallery
plan: 02
type: execute
wave: 2
depends_on: ['08-01']
files_modified:
  - prompts/attach_refs.py
  - spec/fixtures/v1.1/prompts.json
  - scripts/export_asset.py
  - scripts/verify_contract.py
autonomous: true
requirements: [PROMPT-01, PROMPT-02, PROMPT-03, PROMPT-04]

must_haves:
  truths:
    - "prompts/attach_refs.py is a standalone CLI (mirrors prompts/merge_prompts.py) that reads characters.json + props.json + prompts.json, attaches character_refs[]/prop_refs[] per appearance_shots[], recomposes prompt_text via the locked Pattern 2 template, schema-validates, atomic-writes prompts.json in place"
    - "attach_refs is idempotent: running it twice on the same inputs produces byte-identical prompts.json (sorted(set(...)) refs + fixed-order template; no timestamps)"
    - "attach_refs graceful-degrades: characters.json/props.json absent → refs empty, prompt_text recomposed from facets alone (no identity clause), schema-valid, exit 0"
    - "The v1.1 fixture prompts.json prompt_text is synced to match the Pattern 2 recompose output (one-time fixture oracle update per RESEARCH A6; fixture already has correct refs from Phase 5)"
    - "scripts/export_asset.py build_asset_dict emits generator.registry_snapshot (compact confirmed-only view) WHEN characters.json or props.json exists; absent otherwise (byte-identical to v1.0/v1.1-no-reid)"
    - "scripts/export_asset.py SCHEMA_VERSION stays '1.1' (NO bump — Pitfall 10 prevented)"
    - "scripts/verify_contract.py _producer_registry_integrity extended with prompts→registry direction: every character_refs[]/prop_refs[] ID in prompts.json MUST exist in confirmed characters.json/props.json (Pitfall 17 — no dangling refs)"
    - "spec/validate.py + scripts/verify_contract.py --mode=producer both still green after all 3 tasks (no regression)"
  artifacts:
    - path: "prompts/attach_refs.py"
      provides: "Standalone CLI post-processor: reads confirmed registry + prompts.json, attaches refs + recomposes prompt_text deterministically. Invoked by step_timeline as a pre-step (Plan 03)."
      contains: "def attach"
    - path: "spec/fixtures/v1.1/prompts.json"
      provides: "Fixture prompts.json with prompt_text synced to Pattern 2 recompose output (contract oracle for attach_refs)."
      contains: "角色:["
    - path: "scripts/export_asset.py"
      provides: "build_asset_dict conditional registry_snapshot emission (PROMPT-04 producer half). _build_registry_snapshot helper projects confirmed-only compact view."
      contains: "_build_registry_snapshot"
    - path: "scripts/verify_contract.py"
      provides: "Producer-side prompts↔registry ID integrity (Pitfall 17 second-line assert). Extends Phase 7 _producer_registry_integrity additively."
      contains: "Pitfall 17"
  key_links:
    - from: "prompts/attach_refs.py:attach"
      to: "characters.json#appearance_shots[] + props.json#appearance_shots[]"
      via: "invert appearance_shots → shot_id→[ids] map; sorted(set(...)) for determinism"
      pattern: "appearance_shots"
    - from: "prompts/attach_refs.py:_recompose"
      to: "Pattern 2 locked template (CONTEXT Q2)"
      via: "[style] · [scene] · 角色:[names] · 道具:[names] · [subject] · [action] · [camera] · [lighting]"
      pattern: "角色:\\["
    - from: "scripts/export_asset.py:build_asset_dict"
      to: "asset.json#generator.registry_snapshot"
      via: "_build_registry_snapshot(work_dir) → conditional emit when snapshot is not None"
      pattern: "registry_snapshot"
    - from: "scripts/verify_contract.py:_producer_registry_integrity"
      to: "prompts.json character_refs[]/prop_refs[] ⊆ confirmed characters.json/props.json IDs"
      via: "additive block after Phase 7 characters/props checks"
      pattern: "character_refs"
---

<objective>
Build the producer wiring for Phase 8: the prompt-ref attacher (`prompts/attach_refs.py` — PROMPT-01/02), the registry-snapshot emitter (`scripts/export_asset.py` — PROMPT-04 producer half), and the cross-file integrity gate (`scripts/verify_contract.py` — PROMPT-03). Three independent files, zero overlap, all depending only on the Plan 01 schema contract.

Purpose: PROMPT-01/02 wire the confirmed Phase-7 registry onto prompts (attach refs by inverting `appearance_shots[]` + recompose `prompt_text` referencing characters/props by name). PROMPT-04 freezes the registry into `asset.json#generator.registry_snapshot` at export time (Pitfall 18 prevention). PROMPT-03 extends the producer integrity gate to catch dangling prompt refs (Pitfall 17). All three are pure JSON post-processing — zero ML, zero network, zero new deps.

Output: 1 new standalone CLI script (`prompts/attach_refs.py`) + 1 fixture synced (`spec/fixtures/v1.1/prompts.json`) + 2 modified scripts (`scripts/export_asset.py`, `scripts/verify_contract.py`). After this plan, the producer can emit fully-referenced prompts + a frozen snapshot + integrity-gated assets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-CONTEXT.md
@.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-RESEARCH.md
@.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-VALIDATION.md
@.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-01-PLAN.md
@prompts/merge_prompts.py
@scripts/export_asset.py
@scripts/verify_contract.py
@spec/schemas/prompts.schema.json
@spec/fixtures/v1.1/prompts.json
@spec/fixtures/v1.1/characters.json
@spec/fixtures/v1.1/props.json

<interfaces>
<!-- prompts/attach_refs.py CLI contract (the NEW standalone script — Plan 03 step_timeline invokes it as a pre-step). -->

```
prompts/attach_refs.py
  --prompts <abs path>          (required — prompts.json; rewritten in-place by default)
  --work-dir <abs path>         (required — asset root containing characters.json + props.json)
  --output <abs path>           (optional — defaults to --prompts for in-place rewrite)
```
Exit 0 on success (including full graceful-degrade — no registry files → refs empty, prompt_text facets-only). Validates output against prompts.schema.json BEFORE atomic write (fails loud). Prints `[attach-refs] N shots + X char refs + Y prop refs → <path>`.

Module-level importable functions (for smoke harness unit tests):
- `attach(prompts: list, chars: list, props: list) -> list` — pure function; returns new list with character_refs/prop_refs/prompt_text on each entry. Idempotent.
- `_recompose(entry: dict, crefs: list, prefs: list, name_by_char: dict, name_by_prop: dict) -> str` — the Pattern 2 template. Pure function.
- `_load_registry(work_dir: str) -> tuple[list, list]` — reads characters.json + props.json; filters confirmed-only; graceful-degrade returns ([], []) when files absent.

<!-- PROMPT-02 recompose template (LOCKED — CONTEXT Q2 + RESEARCH Pattern 2 lines 402-476): -->

```
[style] · [scene] · 角色:[name1, name2] · 道具:[name1] · [subject] · [action] · [camera] · [lighting]
```
Separator: `" · "` (U+00B7 middle dot, space-separated). Identity clauses (`角色:[...]` / `道具:[...]`) omitted when refs empty. Facets skipped when empty (no trailing separator). Names resolved from characters.json/props.json `name` field (NOT id). Idempotent via `sorted(set(...))` + fixed order.

<!-- scripts/export_asset.py build_asset_dict (Phase 7 — the function Phase 8 extends with _build_registry_snapshot). -->

Current build_asset_dict (lines 136-259) returns the asset dict. Phase 7 added conditional data.characters/props + media.characters[]/props[] at lines 197-220 + the generator block at lines 246-256 (with conditional warnings). Phase 8 adds `registry_snapshot` to the generator block — same conditional-emission pattern as warnings (emit when non-None; omit when None).

<!-- scripts/verify_contract.py _producer_registry_integrity (Phase 7 — the function Phase 8 extends). -->

Current function (lines 492-590) checks: (a) appearance_shots ⊆ shots; (b) registry cluster members ⊆ shots; (c) canonical ID uniqueness + pattern; (d) no non-confirmed in canonical (Pitfall 7). Phase 8 ADDS: (e) prompts.character_refs[]/prop_refs[] ⊆ confirmed characters.json/props.json IDs (Pitfall 17 — dangling prompt ref detection). The extension goes AFTER the existing characters/props loop (which already builds `seen_ids` / confirmed ID sets), BEFORE the registry.draft.json check.

<!-- prompts.schema.json (Phase 5 CONTRACT-04) — ALREADY permits optional character_refs[]/prop_refs[]. -->

Verified at prompts.schema.json:70-85 (RESEARCH A1). No schema change needed for prompts — attach_refs output is schema-valid by construction (Phase 5 already added the optional fields).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: prompts/attach_refs.py (NEW — attach refs + deterministic recompose + idempotent) + fixture prompts.json prompt_text sync</name>
  <files>prompts/attach_refs.py, spec/fixtures/v1.1/prompts.json</files>
  <behavior>
    - attach() with confirmed chars/props inverts appearance_shots → correct character_refs[]/prop_refs[] per shot (sorted, deterministic)
    - attach() with NO registry files (graceful-degrade) → refs empty for every shot, prompt_text recomposed from facets alone (no identity clause), schema-valid, exit 0
    - _recompose() with refs produces identity clauses (角色:[names] / 道具:[names]); without refs omits them; empty facets skipped
    - Idempotency: running attach() twice on the same inputs → byte-identical output (sorted(set(...)) + fixed template order + no timestamps)
    - Running attach_refs.py against the fixture set (characters.json + props.json + prompts.json) produces prompt_text matching Pattern 2; fixture prompts.json is updated to this output
  </behavior>
  <read_first>
    - prompts/merge_prompts.py (the ENTIRE 80-line file — DIRECT template for attach_refs.py: standalone CLI, argparse with Chinese help, json I/O with ensure_ascii=False + indent=2, `[done]`/`[warn]` bracketed print tags, `if __name__=="__main__"` guard. attach_refs.py mirrors this structure but reads characters.json/props.json additionally and rewrites prompts.json in-place.)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-RESEARCH.md (§Architecture Patterns → Pattern 1 "attach_refs.py mirrors merge_prompts.py" lines 265-400 — the FULL attach_refs.py skeleton with _atomic_write / _load_registry / attach / _recompose / main; §Pattern 2 "prompt_text deterministic recompose template" lines 402-476 — the locked template with worked examples; §Pitfall 2 "recompose not idempotent" lines 913-918 — sorted(set(...)) enforcement)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-CONTEXT.md (§decisions → "Prompt-ref attachment + recompose Q1/Q2/Q3" — LOCKED: invert appearance_shots, deterministic template join, idempotent, graceful-degrade, NO LLM)
    - scripts/export_asset.py lines 400-405 (the atomic write pattern: temp file + os.replace — attach_refs._atomic_write mirrors this)
    - spec/schemas/prompts.schema.json (VERIFIED already has optional character_refs[]/prop_refs[] per Phase 5 CONTRACT-04 — attach_refs output is schema-valid by construction; load it for the pre-write Draft202012Validator check)
    - spec/fixtures/v1.1/prompts.json (the fixture to sync — currently has correct refs but OLD prompt_text not matching Pattern 2; running attach_refs against it produces the canonical recomposed prompt_text)
    - spec/fixtures/v1.1/characters.json + props.json (the confirmed registry — char_001 少女 [1,2] + char_002 路人 [1] + prop_001 落叶 [2]; attach_refs reads appearance_shots from these)
  </read_first>
  <action>
**Create `prompts/attach_refs.py`** — standalone CLI mirroring `prompts/merge_prompts.py` structure. Module docstring (Chinese) documents: purpose (attach character_refs/prop_refs + recompose prompt_text), the recompose template (Pattern 2), idempotency guarantee, graceful-degrade behavior, CLI usage.

Module-level constants:
- `REPO = Path(__file__).resolve().parent.parent`
- `PROMPTS_SCHEMA = REPO / "spec" / "schemas" / "prompts.schema.json"`
- `_SEP = " · "` (the U+00B7 middle-dot separator for recompose)

Functions (follow RESEARCH Pattern 1 lines 284-399 skeleton):

1. `_atomic_write(path, data)` — temp + `os.replace` (mirror export_asset.py:402-405). `json.dump(data, f, ensure_ascii=False, indent=2)`.

2. `_load_registry(work_dir)` → `(chars, props)` — reads `characters.json` + `props.json` from work_dir. For EACH file: if absent, return `[]` for that side (graceful-degrade). If present, load JSON; if list, filter to `review_state == "confirmed"` only (Pitfall 7 consistent — non-confirmed never participates in prompt refs). Swallow `(OSError, json.JSONDecodeError)` → return `[]` for that side (silent degrade, do not block timeline generation). Return tuple `(chars_list, props_list)`.

3. `attach(prompts, chars, props)` → new list. Build reverse indexes by inverting `appearance_shots[]`:
   - `char_by_shot`: dict[shot_id → list[char_id]] — iterate chars, for each `c`, for each `sid` in `c.get("appearance_shots") or []`, append `c["id"]` to `char_by_shot.setdefault(sid, [])`.
   - `name_by_char`: dict[char_id → name] — `name_by_char[c["id"]] = c.get("name", c["id"])`.
   - Same for props → `prop_by_shot` + `name_by_prop`.
   - For each entry in prompts: `sid = entry.get("shot_id")`; `crefs = sorted(set(char_by_shot.get(sid, [])))` (sorted = idempotent); `prefs = sorted(set(prop_by_shot.get(sid, [])))`. Shallow-copy entry (`dict(entry)`), set `character_refs=crefs`, `prop_refs=prefs`, `prompt_text=_recompose(entry, crefs, prefs, name_by_char, name_by_prop)`. Append to output.

4. `_recompose(entry, crefs, prefs, name_by_char, name_by_prop)` → str. Pattern 2 (LOCKED):
   - Build `parts` list. Fixed order: for facet in `("style", "scene")`: if `(entry.get(facet) or "").strip()` non-empty, append it. Then if `crefs` non-empty: `names = [name_by_char.get(cid, cid) for cid in crefs]`; append `f"角色:[{', '.join(names)}]"`. Then if `prefs` non-empty: append `f"道具:[{', '.join(names)}]"` (prop names). Then for facet in `("subject", "action", "camera", "lighting")`: same skip-empty logic. Return `self._SEP.join(parts)`.

5. `main()` — argparse with `--prompts` (required), `--work-dir` (required), `--output` (optional, default=None → in-place). Load prompts.json (assert it's a list). Call `_load_registry(work_dir)` → `(chars, props)`. Call `attach(prompts, chars, props)`. Pre-write validate: lazy-import `from jsonschema import Draft202012Validator`, load prompts.schema.json, `iter_errors(out)`; if errors, `sys.exit` with Chinese message listing first few error paths. Atomic-write to `args.output or args.prompts`. Print `[attach-refs] {len(out)} shots + {total_char_refs} char refs + {total_prop_refs} prop refs → {out_path}`.

**Sync the fixture prompts.json:** after creating attach_refs.py, run it against the fixture set to produce the canonical recomposed prompt_text:
`python3 prompts/attach_refs.py --prompts spec/fixtures/v1.1/prompts.json --work-dir spec/fixtures/v1.1/`
This rewrites the fixture prompts.json in-place with Pattern 2 prompt_text (the refs are already correct from Phase 5 — they stay idempotent; only prompt_text changes). The fixture becomes the contract oracle for the recompose output (RESEARCH A6 one-time sync). Verify shot 1 prompt_text contains `角色:[少女, 路人]` and shot 2 contains `道具:[落叶]`.

**Idempotency self-check:** after the fixture sync, run attach_refs.py AGAIN against the synced fixture. `git diff spec/fixtures/v1.1/prompts.json` should be EMPTY (byte-identical re-run — Pitfall 2 prevented).
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 prompts/attach_refs.py --prompts spec/fixtures/v1.1/prompts.json --work-dir spec/fixtures/v1.1/ && python3 -c "
import json, sys
sys.path.insert(0, '.')
from prompts.attach_refs import attach, _recompose, _load_registry

# 1) fixture synced: prompt_text matches Pattern 2
p = json.load(open('spec/fixtures/v1.1/prompts.json', encoding='utf-8'))
s1 = p[0]
assert '角色:[少女, 路人]' in s1['prompt_text'], f'shot 1 prompt_text missing identity clause: {s1[\"prompt_text\"]!r}'
assert ' · ' in s1['prompt_text'], f'shot 1 missing separator: {s1[\"prompt_text\"]!r}'
# shot 2 has prop ref
s2 = p[1]
assert '道具:[落叶]' in s2.get('prompt_text',''), f'shot 2 missing prop clause: {s2.get(\"prompt_text\")!r}'

# 2) idempotency: re-run produces byte-identical
chars, props = _load_registry('spec/fixtures/v1.1/')
out1 = attach(p, chars, props)
out2 = attach(out1, chars, props)
assert json.dumps(out1, ensure_ascii=False) == json.dumps(out2, ensure_ascii=False), 'attach not idempotent'

# 3) graceful-degrade: no registry → empty refs, facets-only prompt_text
bare = [{'shot_id':1, 'subject':'cat', 'action':'runs', 'camera':'wide', 'scene':'yard', 'lighting':'day', 'style':'photo', 'prompt_text':'old'}]
out = attach(bare, [], [])
assert out[0]['character_refs'] == [], 'graceful-degrade should produce empty refs'
assert out[0]['prop_refs'] == []
assert '角色' not in out[0]['prompt_text'], 'degrade should omit identity clause'
assert 'cat' in out[0]['prompt_text'], 'degrade should keep facets'

# 4) schema-valid output
from jsonschema import Draft202012Validator
schema = json.load(open('spec/schemas/prompts.schema.json', encoding='utf-8'))
errs = list(Draft202012Validator(schema).iter_errors(out))
assert not errs, f'attach output schema-invalid: {errs[0].message}'

# 5) fixture still passes _fixture_consistency_check (refs unchanged — only prompt_text synced)
print('OK: attach_refs.py + fixture sync complete (idempotent + graceful-degrade + schema-valid + Pattern 2 prompt_text)')
" && python3 spec/validate.py 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `python3 prompts/attach_refs.py --prompts spec/fixtures/v1.1/prompts.json --work-dir spec/fixtures/v1.1/` exits 0 and prints `[attach-refs]`
    - `grep -c 'def attach' prompts/attach_refs.py` equals 1
    - `grep -c 'def _recompose' prompts/attach_refs.py` equals 1
    - `grep -c 'sorted(set(' prompts/attach_refs.py` ≥ 2 (char + prop ref sorting — idempotency enforcement)
    - `grep -c '角色:\[' prompts/attach_refs.py` ≥ 1 (recompose identity clause)
    - `grep -c 'review_state.*confirmed' prompts/attach_refs.py` ≥ 1 (confirmed-only filter in _load_registry)
    - `grep -c 'Draft202012Validator' prompts/attach_refs.py` ≥ 1 (pre-write schema validate)
    - `grep -c 'os.replace' prompts/attach_refs.py` ≥ 1 (atomic write)
    - `python3 -c "import ast; ast.parse(open('prompts/attach_refs.py').read())"` exits 0 (valid Python)
    - `grep -c '角色:\[少女, 路人\]' spec/fixtures/v1.1/prompts.json` equals 1 (shot 1 recomposed)
    - `grep -c '道具:\[落叶\]' spec/fixtures/v1.1/prompts.json` equals 1 (shot 2 recomposed)
    - `python3 spec/validate.py` exits 0 (no schema regression — prompts fixture still schema-valid after prompt_text sync)
  </acceptance_criteria>
  <done>
prompts/attach_refs.py exists as a standalone CLI mirroring merge_prompts.py: reads confirmed registry, inverts appearance_shots → character_refs/prop_refs, recomposes prompt_text via Pattern 2 (locked template), pre-write schema-validates, atomic-writes. Idempotent (sorted(set(...)) + fixed order). Graceful-degrades (no registry → empty refs, facets-only prompt_text, exit 0). spec/fixtures/v1.1/prompts.json prompt_text synced to Pattern 2 output (fixture is now the recompose oracle).
  </done>
</task>

<task type="auto">
  <name>Task 2: scripts/export_asset.py — _build_registry_snapshot helper + conditional emit in build_asset_dict (PROMPT-04 producer)</name>
  <files>scripts/export_asset.py</files>
  <read_first>
    - scripts/export_asset.py (the ENTIRE 414-line file — focus on: build_asset_dict lines 136-259 which is the ONLY place to add snapshot emission; the generator block at lines 246-256 with conditional `**({"warnings": warnings} if warnings else {})` — Phase 8 adds `registry_snapshot` the same conditional way; the `data_block`/`media_block` local-dict pattern at lines 181-220 that Phase 7 used for conditional characters/props emission — Phase 8 mirrors this for the generator-level snapshot; the atomic write at lines 402-405; SCHEMA_VERSION constant at line 50 which MUST stay "1.1")
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-RESEARCH.md (§Architecture Patterns → Pattern 3 "registry_snapshot freeze shape + export emission" lines 478-566 — the FULL _build_registry_snapshot skeleton + the build_asset_dict generator-block extension. Copy _build_registry_snapshot from lines 507-552; the generator-block conditional emit from lines 555-566.)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-CONTEXT.md (§decisions → "Registry snapshot freeze Q1/Q2" — LOCKED: confirmed-only compact shape, additive optional emit when registry exists, NO schema_version bump)
    - spec/schemas/asset.schema.json (Plan 01 — the generator.registry_snapshot declaration that makes this emission schema-valid. _build_registry_snapshot output MUST conform to this shape.)
    - spec/fixtures/v1.1/asset.json (Plan 01 — the example registry_snapshot; export_asset.py on the fixture dir should reproduce this exact shape)
  </read_first>
  <action>
**Add `_build_registry_snapshot(work_dir)` helper** to scripts/export_asset.py (place it near `build_asset_dict`, before the function or as a module-level helper). Follow RESEARCH Pattern 3 lines 507-552 skeleton:

Signature: `def _build_registry_snapshot(work_dir: str) -> dict | None:`

Logic:
- `chars_path = os.path.join(work_dir, "characters.json")`; `props_path = os.path.join(work_dir, "props.json")`.
- If NEITHER file exists (`not (os.path.isfile(chars_path) or os.path.isfile(props_path))`): return `None` (snapshot OMITTED — graceful-degrade; asset byte-identical to v1.0/v1.1-no-reid).
- Define inner `_project(entries)` → list: iterate entries (if list); for each dict entry: if `entry.get("review_state") != "confirmed"`: `continue` (Pitfall 7 — confirmed-only hard filter, same gating as attach_refs._load_registry + apply_edits build-time gate). Build compact dict: `{"id": entry.get("id"), "name": entry.get("name"), "appearance_shots": entry.get("appearance_shots") or []}`. Additionally include `representative_image` ONLY if `entry.get("representative_image")` is truthy: `**({"representative_image": entry["representative_image"]} if entry.get("representative_image") else {})` (Phase 7 WARNING-2: apply_edits OMITS representative_image on ffmpeg failure — snapshot must not carry a dangling path). Append to output list. Non-dict entries → skip.
- Build `snapshot = {}`. If chars_path exists: try load + `_project` → `snapshot["characters"] = projected`; on `(OSError, json.JSONDecodeError)` → `snapshot["characters"] = []`. Same for props_path → `snapshot["props"]`.
- Return `snapshot`.

**Extend `build_asset_dict`** (lines 136-259): in the generator block of the return dict (lines 246-256), add the conditional snapshot emission AFTER the existing `warnings` conditional:
`**({"registry_snapshot": _build_registry_snapshot(work_dir)} if _build_registry_snapshot(work_dir) is not None else {})`

IMPORTANT — call `_build_registry_snapshot(work_dir)` ONCE and store in a local variable (`snapshot = _build_registry_snapshot(work_dir)`) before the return dict, to avoid calling it twice (it reads files). Then use the local in the conditional: `**({"registry_snapshot": snapshot} if snapshot is not None else {})`.

Do NOT change `SCHEMA_VERSION` (line 50 stays `"1.1"` — Pitfall 10). Do NOT touch the existing warnings conditional or data/media blocks. Do NOT add registry_snapshot to any `required` list. The snapshot is additive-optional, emitted conditionally.

**Pre-write validation** (the existing `validate_asset_json(asset)` call at line 398) automatically covers the new field — if the snapshot shape is wrong, Draft202012Validator catches it before write. No additional assert needed.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "
import json, os, sys, tempfile, shutil
sys.path.insert(0, '.')
from scripts.export_asset import build_asset_dict, _build_registry_snapshot, SCHEMA_VERSION

# 0) SCHEMA_VERSION unchanged (Pitfall 10)
assert SCHEMA_VERSION == '1.1', f'SCHEMA_VERSION bumped to {SCHEMA_VERSION!r} (Pitfall 10)'

# 1) WITHOUT registry → snapshot None → field OMITTED (byte-identical to v1.0)
work = tempfile.mkdtemp(prefix='phase8-export-no-reg-')
for name, content in [('transcript.json',{'source':'t.mp4','duration':2.0}),
                       ('shots.json',[{'id':1,'start_sec':0.0,'end_sec':1.0,'duration':1.0}]),
                       ('audio_analysis.json',{}),('frames.json',[]),('prompts.json',[])]:
    json.dump(content, open(os.path.join(work,name),'w'))
a = build_asset_dict(work, '/fake/v.mp4', warnings=None)
assert 'registry_snapshot' not in a['generator'], f'snapshot should be omitted without registry; got: {a[\"generator\"].keys()}'
print('OK (no registry): registry_snapshot OMITTED')

# 2) WITH confirmed registry → snapshot emitted, confirmed-only
json.dump([{'id':'char_001','name':'少女','review_state':'confirmed','appearance_shots':[1],'representative_image':'characters/char_001.png'},
           {'id':'char_002','name':'draft','review_state':'proposed','appearance_shots':[1]}],   # Pitfall 7 — must be filtered
          open(os.path.join(work,'characters.json'),'w'), ensure_ascii=False)
json.dump([{'id':'prop_001','name':'落叶','review_state':'confirmed','appearance_shots':[1]}],
          open(os.path.join(work,'props.json'),'w'), ensure_ascii=False)
snap = _build_registry_snapshot(work)
assert snap is not None
assert len(snap['characters']) == 1, f'Pitfall 7 leak: proposed entry in snapshot: {snap}'
assert snap['characters'][0]['id'] == 'char_001'
assert snap['characters'][0]['name'] == '少女'
assert len(snap['props']) == 1

# 3) build_asset_dict emits it in generator
a2 = build_asset_dict(work, '/fake/v.mp4', warnings=None)
assert 'registry_snapshot' in a2['generator'], 'registry_snapshot not emitted in build_asset_dict'
assert a2['generator']['registry_snapshot']['characters'][0]['name'] == '少女'

# 4) representative_image OMITTED when absent (WARNING-2 pattern)
json.dump([{'id':'char_003','name':'noimg','review_state':'confirmed','appearance_shots':[1]}],   # no representative_image
          open(os.path.join(work,'characters.json'),'w'), ensure_ascii=False)
snap2 = _build_registry_snapshot(work)
entry = snap2['characters'][0]
assert 'representative_image' not in entry, f'representative_image should be omitted when absent: {entry}'

shutil.rmtree(work, ignore_errors=True)
print('OK: _build_registry_snapshot confirmed-only + conditional emit + representative_image optional')
" && python3 spec/validate.py 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def _build_registry_snapshot' scripts/export_asset.py` equals 1
    - `grep -c 'review_state.*confirmed' scripts/export_asset.py` ≥ 1 (confirmed-only filter in _project)
    - `grep -c 'registry_snapshot' scripts/export_asset.py` ≥ 3 (helper def + conditional emit + comment)
    - `grep -c 'SCHEMA_VERSION = "1.1"' scripts/export_asset.py` equals 1 (unchanged — Pitfall 10)
    - `python3 -c "from scripts.export_asset import SCHEMA_VERSION; assert SCHEMA_VERSION=='1.1'"` exits 0
    - `python3 -c "import ast; ast.parse(open('scripts/export_asset.py').read())"` exits 0 (valid Python)
    - The inline test block (above automated) prints all 4 OK lines (OMITTED / confirmed-only / emitted / representative_image optional)
    - `python3 spec/validate.py` exits 0 (no schema regression)
  </acceptance_criteria>
  <done>
scripts/export_asset.py has _build_registry_snapshot(work_dir) helper (confirmed-only compact projection; representative_image optional; returns None when no registry files). build_asset_dict conditionally emits generator.registry_snapshot when snapshot is not None (same conditional pattern as warnings). SCHEMA_VERSION stays "1.1". Old assets without registry stay byte-identical (snapshot field omitted).
  </done>
</task>

<task type="auto">
  <name>Task 3: scripts/verify_contract.py — extend _producer_registry_integrity with prompts→registry ID integrity (PROMPT-03, Pitfall 17)</name>
  <files>scripts/verify_contract.py</files>
  <read_first>
    - scripts/verify_contract.py (focus on _producer_registry_integrity lines 492-590 — the Phase 7 function. It builds `shot_ids` from shots.json, iterates characters.json/props.json checking appearance_shots ⊆ shots + ID pattern + Pitfall 7 confirmed-only. The `seen_ids` set per file + the loop structure are the insertion context. Phase 8 ADDS a prompts→registry check AFTER the characters/props loop (which has already loaded + validated those files) and BEFORE the registry.draft.json check at line 571.)
    - scripts/verify_contract.py lines 440-447 (the existing FIXTURE-SIDE prompts→registry check in _fixture_consistency_check — this is the DIRECT analog. Phase 8 mirrors it on the producer side. Copy the loop structure: iterate prompts, for each character_ref check ⊆ char IDs, for each prop_ref check ⊆ prop IDs.)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-RESEARCH.md (§Architecture Patterns → Pattern 5 "verify_contract.py prompt↔registry integrity extension" lines 664-702 — the FULL extension skeleton with failure messages referencing Pitfall 17; §Pitfall 6 "Producer integrity check duplicates fixture check logic" lines 941-946 — extend _producer_registry_integrity, do NOT fork a new function)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-CONTEXT.md (§decisions → "Cross-file integrity Q1" — LOCKED: extend _producer_registry_integrity (additive), do NOT create a separate verifier)
  </read_first>
  <action>
**Extend `_producer_registry_integrity(asset_dir)`** (lines 492-590) with the prompts→registry direction. The extension is an ADDITIVE block inserted AFTER the existing characters/props loop (which ends around line 568) and BEFORE the registry.draft.json check (line 571).

The existing loop already loads characters.json + props.json and builds `seen_ids` per file. Phase 8 needs the CONFIRMED ID sets to check against. Refactor minimally: inside the existing characters/props loop, additionally populate `char_confirmed_ids` and `prop_confirmed_ids` sets (only entries with `review_state == "confirmed"` — aligns with the Pitfall 7 check at line 559). Initialize these sets as empty before the loop (`char_confirmed_ids: set = set()` / `prop_confirmed_ids: set = set()`). In the loop body, when `entry.get("review_state") == "confirmed"`: add `eid` to the appropriate set. (The existing loop already checks `!= "confirmed"` as a failure; this just additionally collects the confirmed IDs for the prompts check downstream.)

Then add the prompts→registry check block:
- `prompts_path = asset_dir / "prompts.json"`. If `not prompts_path.is_file()`: skip (no prompts → nothing to check; graceful).
- Else: try load prompts (`json.loads(prompts_path.read_text(encoding="utf-8"))`); on `json.JSONDecodeError` → append failure + continue. If prompts is a list: iterate entries; for each dict entry with a `shot_id`: for each `cref` in `entry.get("character_refs", []) or []`: if `cref not in char_confirmed_ids`: append failure `f"prompts.json shot {sid}: character_ref {cref!r} not in confirmed characters.json IDs (Pitfall 17 — prompt dangling ref)"`. Same for `prop_refs` → `prop_confirmed_ids`. Failure message MUST reference "Pitfall 17" for traceability (mirrors Phase 7's Pitfall 7 convention).

Do NOT create a new function (Pitfall 6 — DRY violation). Do NOT touch `_fixture_consistency_check` (it already does this for the fixture set at lines 440-447). Do NOT rename EIGHT_SHAPES. The extension is purely additive inside the existing `_producer_registry_integrity`.

The existing `run_producer_check` (line 593) already calls `_producer_registry_integrity(asset_dir)` at line 649 — no wiring change needed; the extended function automatically covers prompts→registry when prompts.json exists in the producer asset dir.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "
import json, os, sys, tempfile, shutil, pathlib
sys.path.insert(0, '.')
from scripts.verify_contract import _producer_registry_integrity

# 1) clean: no prompts.json → no-op (graceful)
work = tempfile.mkdtemp(prefix='phase8-verify-clean-')
json.dump([{'id':1,'start_sec':0.0,'end_sec':1.0,'duration':1.0}], open(os.path.join(work,'shots.json'),'w'))
json.dump([{'id':'char_001','name':'少女','review_state':'confirmed','appearance_shots':[1]}],
          open(os.path.join(work,'characters.json'),'w'), ensure_ascii=False)
assert _producer_registry_integrity(pathlib.Path(work)) == [], 'clean case should have 0 failures'
print('OK (clean): no failures')

# 2) dangling ref: prompts references char_999 not in registry → Pitfall 17 failure
json.dump([{'shot_id':1,'character_refs':['char_999'],'prompt_text':'x'}],
          open(os.path.join(work,'prompts.json'),'w'), ensure_ascii=False)
fails = _producer_registry_integrity(pathlib.Path(work))
assert any('char_999' in f and 'Pitfall 17' in f for f in fails), f'dangling ref not detected: {fails}'
print(f'OK (dangling): Pitfall 17 detected ({len(fails)} failure(s))')

# 3) valid ref: prompts references char_001 which IS confirmed → no failure
json.dump([{'shot_id':1,'character_refs':['char_001'],'prompt_text':'x'}],
          open(os.path.join(work,'prompts.json'),'w'), ensure_ascii=False)
fails2 = _producer_registry_integrity(pathlib.Path(work))
assert not any('Pitfall 17' in f for f in fails2), f'valid ref flagged: {fails2}'
print('OK (valid ref): no Pitfall 17 failure')

# 4) ref to proposed (non-confirmed) entry → failure (confirmed-only)
json.dump([{'id':'char_002','name':'draft','review_state':'proposed','appearance_shots':[1]}],
          open(os.path.join(work,'characters.json'),'w'), ensure_ascii=False)  # overwrite: now has char_001 confirmed + char_002 proposed
json.dump([{'shot_id':1,'character_refs':['char_002'],'prompt_text':'x'}],   # refs the proposed one
          open(os.path.join(work,'prompts.json'),'w'), ensure_ascii=False)
fails3 = _producer_registry_integrity(pathlib.Path(work))
assert any('char_002' in f and 'Pitfall 17' in f for f in fails3), f'ref to proposed not flagged: {fails3}'
print('OK (proposed ref): flagged as dangling (confirmed-only)')

shutil.rmtree(work, ignore_errors=True)
" && python3 scripts/verify_contract.py --mode=producer 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Pitfall 17' scripts/verify_contract.py` ≥ 1 (failure message references Pitfall 17)
    - `grep -c 'character_refs' scripts/verify_contract.py` ≥ 4 (existing _fixture_consistency_check at ~line 442 + new producer check + comments)
    - `grep -c 'prop_refs' scripts/verify_contract.py` ≥ 4 (same — fixture + producer)
    - `grep -c 'def _producer_registry_integrity' scripts/verify_contract.py` equals 1 (NOT forked into a new function — Pitfall 6 prevented)
    - `python3 -c "import ast; ast.parse(open('scripts/verify_contract.py').read())"` exits 0 (valid Python)
    - The inline test block (above automated) prints all 4 OK lines (clean / dangling / valid / proposed)
    - `python3 scripts/verify_contract.py --mode=producer` exits 0 (no regression — additive check; if ep01 has no prompts with refs, it's a no-op)
  </acceptance_criteria>
  <done>
scripts/verify_contract.py _producer_registry_integrity extended with prompts→registry direction (Pitfall 17): every character_refs[]/prop_refs[] ID in prompts.json MUST exist in confirmed characters.json/props.json IDs. Additive inside the existing function (no fork — Pitfall 6 prevented). Failure messages reference "Pitfall 17". run_producer_check automatically picks up the extension (no wiring change). Producer gate green on existing assets (no regression).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator→Pipeline | Operator runs `run_pipeline.py`; Plan 03's step_timeline invokes attach_refs.py as a pre-step. attach_refs reads characters.json/props.json (operator-curated via HITL review) + prompts.json. |
| Pipeline→prompts.json | attach_refs rewrites prompts.json in-place (atomic). Downstream consumers (gen_timeline_html.py, export_asset.py) read the rewritten file. |
| Producer asset dir→verify_contract.py | Producer gate reads prompts.json + characters.json/props.json. Prompts↔registry integrity check fires when both exist. |
| Producer→asset.json | export_asset.py emits generator.registry_snapshot (frozen registry view). Consumer reads asset.json as the export-time truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering / Integrity | Dangling prompt ref (character_refs:["char_999"] not in registry) ships in asset | mitigate | verify_contract.py `_producer_registry_integrity` extended with prompts→registry direction (Pitfall 17). Also: attach_refs.py attaches refs ONLY from confirmed registry inversion — it cannot produce a dangling ref by construction (refs come FROM the registry). The integrity check is defense-in-depth for hand-edited prompts.json. |
| T-08-05 | Tampering | Non-confirmed entry leaks into registry_snapshot (Pitfall 7 in new clothes) | mitigate | `_build_registry_snapshot._project` hard-filters `review_state != "confirmed": continue` — same gating as attach_refs._load_registry + apply_edits build-time gate. Smoke scenario 5 (Plan 03) asserts this. |
| T-08-06 | Tampering / Path Traversal | registry_snapshot carries a malicious representative_image path | mitigate | Schema pattern (Plan 01) enforces anti-traversal `^(?!.*\.\.)([^/]+/)*characters/[^:*?"<>|]+\.png$`. Additionally _build_registry_snapshot OMITS representative_image when absent (WARNING-2) — no dangling path. |
| T-08-07 | Tampering | attach_refs rewrites prompts.json non-atomically (partial read by downstream) | mitigate | `_atomic_write` (temp + os.replace, mirror export_asset.py:402-405). Cross-platform atomic; partial-write protection (Phase 2 lesson). |
| T-08-08 | Integrity / Repudiation | Snapshot mutation post-export invalidates old refs (Pitfall 18) | mitigate | Snapshot is frozen inside asset.json (a written file). Later registry edits (re-review, re-cluster) mutate characters.json/props.json on the producer filesystem but do NOT rewrite already-exported asset.json. Structural guarantee. |
| T-08-09 | Denial of Service | attach_refs non-idempotent → mtime cache thrash on every run | mitigate | `sorted(set(...))` for refs + fixed-order template + no timestamps. Smoke scenario 2 (Plan 03) asserts byte-identical re-run. |
| T-08-SC | Tampering | pip/npm/cargo installs | accept | Plan 02 adds ZERO packages — stdlib (json/pathlib/argparse/os/sys) + existing project deps (jsonschema 4.26.0). No install tasks. |

No HIGH severity threats (local CLI scripts; pure JSON post-processing; no network egress; all operator input schema-validated; atomic writes; deterministic output).
</threat_model>

<verification>
After all 3 tasks complete:
1. `python3 prompts/attach_refs.py --prompts spec/fixtures/v1.1/prompts.json --work-dir spec/fixtures/v1.1/` exits 0 (fixture self-consistent under attach_refs).
2. `python3 -c "from prompts.attach_refs import attach, _recompose, _load_registry; ..."` (inline idempotency + graceful-degrade + Pattern 2 checks) passes.
3. `python3 -c "from scripts.export_asset import _build_registry_snapshot, SCHEMA_VERSION; assert SCHEMA_VERSION=='1.1'; ..."` (confirmed-only + conditional emit + representative_image optional) passes.
4. `python3 -c "from scripts.verify_contract import _producer_registry_integrity; ..."` (clean / dangling / valid / proposed-ref checks) passes.
5. `python3 spec/validate.py` exits 0 (no schema regression — fixture prompts.json still schema-valid after prompt_text sync).
6. `python3 scripts/verify_contract.py --mode=producer` exits 0 (additive integrity check gated on file existence — no regression for assets without prompts refs).
7. **Phase 8 gate (Plans 01+02 together):** the v1.1 fixture set is internally consistent — registry_snapshot in asset.json mirrors characters.json/props.json; prompts.json prompt_text matches Pattern 2; refs ⊆ confirmed registry IDs.
</verification>

<success_criteria>
- `prompts/attach_refs.py` exists: standalone CLI, reads confirmed registry, attaches refs via appearance_shots inversion, recomposes prompt_text via Pattern 2 (locked), pre-write schema-validates, atomic-writes, idempotent, graceful-degrades.
- `spec/fixtures/v1.1/prompts.json` prompt_text synced to Pattern 2 recompose output (contract oracle).
- `scripts/export_asset.py` emits `generator.registry_snapshot` conditionally (confirmed-only compact view; None when no registry → omitted). SCHEMA_VERSION stays "1.1".
- `scripts/verify_contract.py` `_producer_registry_integrity` extended with prompts→registry integrity (Pitfall 17; additive, no fork).
- PROMPT-01 (attach), PROMPT-02 (recompose), PROMPT-03 (integrity), PROMPT-04 (snapshot emit) all closed at the producer level.
- No regression: spec/validate.py + verify_contract.py --mode=producer both green.
</success_criteria>

<output>
Create `.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-02-SUMMARY.md` when done
</output>
