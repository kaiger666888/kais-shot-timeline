---
phase: 08-prompt-reference-system-shot-timeline-html-gallery
plan: 03
type: execute
wave: 3
depends_on: ['08-02']
files_modified:
  - html/gen_timeline_html.py
  - run_pipeline.py
  - scripts/verify_phase8_smoke.py
autonomous: true
requirements: [PRESENT-01, PRESENT-02, PRESENT-03]

must_haves:
  truths:
    - "html/gen_timeline_html.py build_html renders a character/prop gallery section (cards: external PNG thumbnail + _esc(name) + ID + appearance count) above the two-panel layout when registry data is provided"
    - "html/gen_timeline_html.py per-shot row renders clickable reference chips (🧑 name / 🔧 name) as in-page #gallery-<id> anchors linking to gallery entries"
    - "html/gen_timeline_html.py per-shot row renders a semantic-fill chip: green '✓ 运镜' when ALL four cinematography facets non-empty, gray '○ 降级' when ANY empty"
    - "html/gen_timeline_html.py applies _esc() (Python-side, CR-04 carry) to EVERY operator-influenced name interpolation in gallery card HTML + a JS-side _esc() to ref-chip template literals"
    - "html/gen_timeline_html.py applies .replace('</', '<\\/') to EVERY json.dumps landing in a <script> block (shots + stems + transcript + characters + props — CR-04 JSON-in-script defense)"
    - "html/gen_timeline_html.py graceful-degrades: no registry data → gallery absent, chips absent, fill-chip still renders (reads prompts facets; gray when empty); HTML still schema-valid + self-contained"
    - "run_pipeline.py step_timeline invokes prompts/attach_refs.py as a pre-step BEFORE gen_timeline_html.py (under existing [7/8] banner; NO [N/9] counter bump — Pitfall 5 prevented)"
    - "run_pipeline.py step_timeline mtime cache includes prompts_json as an input (Pitfall 9 prevented — timeline regenerates after attach_refs rewrites prompts)"
    - "run_pipeline.py step_timeline passes --prompts + --characters/--props (or --asset-json) to gen_timeline_html.py so the HTML reflects attached refs + gallery"
    - "scripts/verify_phase8_smoke.py exercises 6 scenarios (attach_no_registry / attach_idempotent / snapshot_freeze / integrity_dangling_ref / snapshot_confirmed_only / html_xss_inert), exits 0 on all green"
    - "No [N/9] counter appears in run_pipeline.py (grep returns 0); [N/8] count unchanged from Phase 7 (24 occurrences)"
  artifacts:
    - path: "html/gen_timeline_html.py"
      provides: "Gallery section + reference chips + semantic-fill indicator + _esc() XSS defense + JSON-in-script defense. Extends build_html + build_shots_js + main() with registry/prompts data loading."
      contains: "gallery-card"
    - path: "run_pipeline.py"
      provides: "step_timeline invokes attach_refs.py as pre-step + passes --prompts/--characters to gen_timeline_html.py + mtime cache includes prompts_json (Pitfall 9)."
      contains: "attach_refs"
    - path: "scripts/verify_phase8_smoke.py"
      provides: "6-scenario Phase 8 regression (mirror verify_phase7_smoke.py structure): graceful-degrade + idempotency + snapshot freeze + integrity + confirmed-only + XSS inert."
      contains: "phase8-smoke"
  key_links:
    - from: "run_pipeline.py:step_timeline"
      to: "prompts/attach_refs.py (Plan 02) + html/gen_timeline_html.py"
      via: "subprocess.run([sys.executable, attach_refs.py, ...]) BEFORE subprocess.run([sys.executable, gen_timeline_html.py, --prompts, ...])"
      pattern: "attach_refs\\.py"
    - from: "html/gen_timeline_html.py:build_html gallery section"
      to: "characters.json/props.json OR asset.json#generator.registry_snapshot"
      via: "main() --characters/--props flags OR --asset-json flag; preferred: registry_snapshot when present"
      pattern: "gallery-card"
    - from: "html/gen_timeline_html.py ref-chip anchors"
      to: "gallery card #gallery-<id>"
      via: "in-page href=#gallery-{id} anchor + CSS scroll-behavior:smooth (line 132)"
      pattern: "ref-chip"
    - from: "scripts/verify_phase8_smoke.py scenario 6"
      to: "html/gen_timeline_html.py XSS defense"
      via: "seed name='</script>...' → generate HTML → assert raw payload absent"
      pattern: "xss_inert"
---

<objective>
Build the Phase 8 UI + pipeline integration + regression: extend `html/gen_timeline_html.py` with a character/prop gallery + clickable reference chips + semantic-fill indicator (PRESENT-01/02/03) while carrying the Phase 7 CR-04 XSS hardening verbatim; wire `run_pipeline.py:step_timeline` to invoke `prompts/attach_refs.py` as a pre-step + extend the mtime cache (Pitfall 9); ship `scripts/verify_phase8_smoke.py` as the 6-scenario permanent regression.

Purpose: This is the user-visible surface of Phase 8 — the gallery + chips + indicator let the operator SEE cross-shot narrative continuity + route-degrade status at a glance. The XSS defense is the primary security concern (operator-influenced `name` strings now flow into gallery cards, chips, and inlined JSON — Phase 7 CR-04 lesson applied verbatim). The pipeline wiring makes attach_refs run automatically within step_timeline (no counter bump — CONTEXT Q3).

Output: 1 modified HTML generator + 1 modified pipeline + 1 new smoke harness. After this plan, Phase 8 is shippable: the full v1.1 producer side (contract + registry + prompts + gallery) is wired end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-CONTEXT.md
@.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-RESEARCH.md
@.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-VALIDATION.md
@.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-01-PLAN.md
@.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-02-PLAN.md
@html/gen_timeline_html.py
@html/gen_registry_review.py
@run_pipeline.py
@scripts/verify_phase7_smoke.py

<interfaces>
<!-- html/gen_timeline_html.py extension points (line numbers verified via grep): -->

- `build_shots_js(shots, frames_by_id, audio_by_id, transcript_segments)` at line 24 — EXTEND signature with `prompts_by_id: dict|None = None`; add `"route_filled": bool` to each shot dict (True when ALL of camera/action/lighting/style non-empty strings in the prompts entry for that shot).
- `build_html(...)` at line 99 — EXTEND signature with `characters_data=None, props_data=None`; add JSON-in-script defense to shots/stems/transcript/characters/props json.dumps; add CHARACTERS/PROPS JS consts; add gallery section HTML before `.app`; add ref-chip + fill-chip CSS.
- Shot-row template #1 at line 365 (linear mode) + template #2 at line 818 (adaptive mode) — append ref-chips + fill-chip after the `.type-badge` span in BOTH templates.
- `main()` at line 1005 — add `--prompts`, `--characters`, `--props`, `--asset-json` CLI flags; load registry data (prefer registry_snapshot from --asset-json when present); pass to build_html.

<!-- html/gen_registry_review.py:79-91 _esc() — the CANONICAL XSS defense (Phase 7 CR-04 fix commit 336d04f). Copy VERBATIM into gen_timeline_html.py module top. -->
<!-- html/gen_registry_review.py:318 JSON-in-script defense — `json.dumps(...).replace("</", "<\\/")`. Copy pattern to gen_timeline_html.py for ALL inlined JSON. -->

<!-- run_pipeline.py:step_timeline (lines 316-343) — the function Phase 8 extends. Currently: mtime cache on shots/audio/transcript (lines 320-323); subprocess to gen_timeline_html.py (lines 326-342). Phase 8: invoke attach_refs BEFORE gen_timeline_html; add prompts_json to cache inputs; pass --prompts/--characters/--props to gen_timeline_html. -->
<!-- run_pipeline.py:step_semantic (lines 163-232) — the DIRECT template for video identity sidecar + mtime cache pattern (attach_refs does NOT need its own cache — it's idempotent + runs every time within step_timeline). -->

<!-- scripts/verify_phase7_smoke.py — the DIRECT structural template for verify_phase8_smoke.py (same helper functions, bracketed-tag print style, exit-code contract, temp-dir cleanup). 5 scenarios → 6 scenarios. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: html/gen_timeline_html.py — gallery + chips + indicator + _esc() + JSON-in-script defense (PRESENT-01/02/03 + XSS)</name>
  <files>html/gen_timeline_html.py</files>
  <read_first>
    - html/gen_timeline_html.py (the ENTIRE 1083-line file — focus on: build_shots_js lines 24-54; build_html signature + JSON inlining lines 99-113; CSS block lines 131-202; HTML body .app div line 233; SHOTS/STEMS/TRANSCRIPT JS consts lines 248-252; shot-row template #1 lines 360-372; shot-row template #2 lines 813-829; main() argparse + data loading lines 1005-1079)
    - html/gen_registry_review.py lines 79-91 (the _esc() helper — Phase 7 CR-04 fix commit 336d04f. Copy VERBATIM into gen_timeline_html.py at module top. This is the canonical XSS defense for this repo.)
    - html/gen_registry_review.py line 318 (the JSON-in-script `.replace("</", "<\\/")` defense — copy pattern to gen_timeline_html.py for ALL inlined JSON)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-RESEARCH.md (§Architecture Patterns → Pattern 4 "HTML XSS hardening — carry Phase 7 CR-04 fix verbatim" lines 623-657 — the _esc() source + JSON-in-script defense rationale; §Pattern 7 "gen_timeline_html.py gallery + chips + indicator" lines 738-860 — the FULL CSS + JS + Python gallery-card skeleton; §Pitfall 1 "HTML XSS via operator-influenced names" lines 906-911; §Pitfall 7 "Gallery hardcodes external PNG paths without onerror fallback" lines 948-953; §Pitfall 8 "Semantic-fill indicator false-negative" lines 955-960)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-CONTEXT.md (§decisions → "HTML gallery + chips + indicator Q1/Q2/Q3" — LOCKED: gallery section, clickable chips as in-page anchors, green/gray fill indicator; Claude's Discretion on CSS/card layout)
    - spec/fixtures/v1.1/characters.json + props.json (the registry data the gallery reads — id/name/representative_image/appearance_shots shape)
  </read_first>
  <action>
**Add `_esc(s)` helper** at module top (after the imports, before `build_shots_js`). Copy VERBATIM from `html/gen_registry_review.py:79-91` (Phase 7 CR-04 fix commit 336d04f). The function: `str(s).replace("&","&amp;").replace("<","&lt;").replace(">","&gt;").replace('"',"&quot;").replace("'","&#x27;")`. Chinese docstring documenting CR-04 carry-over + the namespace-package shadowing rationale (local `html/` dir shadows stdlib `html.escape`).

**Extend `build_shots_js`** (line 24): add optional param `prompts_by_id=None`. Inside the per-shot loop (line 40-53), after building the existing dict fields, add `"route_filled"`: look up `prompt_entry = prompts_by_id.get(shot["id"], {})` if prompts_by_id else `{}`; `route_filled = all((prompt_entry.get(f) or "").strip() for f in ("camera","action","lighting","style"))`. Add `"character_refs": prompt_entry.get("character_refs", [])` + `"prop_refs": prompt_entry.get("prop_refs", [])` to the shot dict (so JS can render chips without a separate lookup).

**Extend `build_html`** (line 99): add params `characters_data=None, props_data=None` to the signature. Apply JSON-in-script defense to ALL inlined JSON (currently lines 110-112 lack it):
- `shots_json = json.dumps(shots_js, ensure_ascii=False).replace("</", "<\\/")`
- `stems_json = json.dumps(stems_js).replace("</", "<\\/")`
- `transcript_json = json.dumps(transcript_segments or [], ensure_ascii=False).replace("</", "<\\/")`
- NEW: `chars_json = json.dumps(characters_data or [], ensure_ascii=False).replace("</", "<\\/")`
- NEW: `props_json = json.dumps(props_data or [], ensure_ascii=False).replace("</", "<\\/")`

**Add JS consts** near line 248 (after TRANSCRIPT_SEGMENTS): `const CHARACTERS = {chars_json};` + `const PROPS = {props_json};`. Also add a JS-side `_esc` function in the <script> block (mirrors Python _esc but in JS): `function _esc(s){return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#x27;');}` — used by ref-chip template literals.

**Add CSS** to the <style> block (after line 202, before `</style>`): copy the `.ref-chip` / `.char-chip` / `.prop-chip` / `.fill-chip` / `.fill-filled` / `.fill-degraded` / `.gallery` / `.gallery-card` rules from RESEARCH Pattern 7 lines 801-825. Reuse GitHub-dark palette tokens (#58a6ff char blue, #d29922 prop yellow, #3fb950 filled green, #8b949e degraded gray).

**Add gallery section HTML** before the `.app` div (line 233): build a `gallery_html` Python string (server-rendered). If `characters_data` or `props_data` is non-empty: for each character, emit `<div class="gallery-card" id="gallery-{_esc(c['id'])}">` with `<img src="{_esc(c.get('representative_image',''))}" onerror="this.style.display='none'">` (Pitfall 7 — missing PNG graceful-degrade) + `<div class="gname">{_esc(c.get('name'))}</div>` + `<div class="gid">{_esc(c.get('id'))}</div>` + `<div class="gcount">{len(c.get('appearance_shots',[]))} shots</div>`. Same for props (with 🔧 prefix on name). Wrap in `<div class="gallery">{cards}</div>`. If both empty: `gallery_html = ""` (gallery absent — graceful-degrade). Insert `{gallery_html}` between the header `</div>` and `<div class="app">`.

**Extend BOTH shot-row templates** (lines 365-371 and 818-824): after the `.type-badge` span and before the closing `</div>` of `.times`, append ref-chips + fill-chip. In JS (template literal):
- Ref-chips: `var refChips = (s.character_refs||[]).map(function(cid){var c=CHARACTERS.find(function(x){return x.id===cid;});var n=c?c.name:cid;return '<a href="#gallery-'+cid+'" class="ref-chip char-chip" title="'+_esc(n)+'">🧑 '+_esc(n)+'</a>';}).join('') + (s.prop_refs||[]).map(function(pid){var p=PROPS.find(function(x){return x.id===pid;});var n=p?p.name:pid;return '<a href="#gallery-'+pid+'" class="ref-chip prop-chip" title="'+_esc(n)+'">🔧 '+_esc(n)+'</a>';}).join('');`
- Fill-chip: `var fillChip = s.route_filled ? '<span class="fill-chip fill-filled" title="运镜分析已路由填充">✓ 运镜</span>' : '<span class="fill-chip fill-degraded" title="运镜分析 offline 降级">○ 降级</span>';`
- Append `${refChips}${fillChip}` after the type-badge span in the `.times` div, in BOTH templates.

**Extend `main()`** (line 1005): add CLI flags `--prompts` (optional path to prompts.json), `--characters` (optional path to characters.json), `--props` (optional path to props.json), `--asset-json` (optional path to asset.json). After loading existing data: if `--asset-json` exists and has `generator.registry_snapshot`: use that as the preferred source for characters_data/props_data (RESEARCH Open Question 2 resolution — snapshot is export-time truth). Else if `--characters` exists: load characters_data from it. Else: characters_data=None (graceful-degrade). Same for props. Load prompts_by_id from `--prompts` if given (shot_id → entry map). Pass characters_data + props_data + prompts_by_id to build_shots_js + build_html.

CRITICAL — do NOT introduce a new numbered step or change any `[N/8]` counter (that is run_pipeline.py's concern, Task 2). Do NOT modify the existing stem playback / video / caption logic. The gallery + chips + indicator are ADDITIVE.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "
import ast
src = open('html/gen_timeline_html.py', encoding='utf-8').read()
ast.parse(src)
# 1) _esc helper present (CR-04 carry)
assert 'def _esc(' in src, '_esc helper missing'
assert 'function _esc(' in src or 'function _esc (' in src, 'JS-side _esc missing'
# 2) JSON-in-script defense on ALL inlined JSON
import re
dumps_with_defense = len(re.findall(r'json\.dumps\([^)]+\)\.replace\(\"</\", \"<\\\\/\"\)', src))
assert dumps_with_defense >= 3, f'expected ≥3 json.dumps with </ replace defense, found {dumps_with_defense}'
# 3) gallery CSS + HTML
assert 'gallery-card' in src, 'gallery-card CSS/HTML missing'
assert 'ref-chip' in src, 'ref-chip missing'
assert 'fill-filled' in src and 'fill-degraded' in src, 'fill-chip classes missing'
# 4) onerror fallback (Pitfall 7)
assert 'onerror' in src, 'img onerror fallback missing (Pitfall 7)'
# 5) route_filled in build_shots_js
assert 'route_filled' in src, 'route_filled detection missing'
# 6) new CLI flags
for flag in ('--prompts', '--characters', '--props', '--asset-json'):
    assert flag in src, f'{flag} CLI flag missing'
# 7) CHARACTERS/PROPS JS consts
assert 'const CHARACTERS' in src or 'CHARACTERS =' in src, 'CHARACTERS JS const missing'
print('OK: gen_timeline_html.py gallery + chips + indicator + _esc + JSON-in-script defense present')
" && python3 html/gen_timeline_html.py --shots spec/fixtures/v1.1/shots.json --characters spec/fixtures/v1.1/characters.json --props spec/fixtures/v1.1/props.json --prompts spec/fixtures/v1.1/prompts.json --output /tmp/phase8_timeline.html 2>&1 | tail -3 && python3 -c "
html = open('/tmp/phase8_timeline.html', encoding='utf-8').read()
assert 'gallery-card' in html, 'gallery-card not in generated HTML'
assert 'ref-chip' in html or 'char-chip' in html, 'ref-chip CSS not in HTML'
assert 'fill-filled' in html or 'fill-degraded' in html, 'fill-chip not in HTML'
assert 'gallery-char_001' in html or 'gallery-' in html, 'gallery anchor IDs missing'
print('OK: generated timeline.html has gallery + chips + indicator')
"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('html/gen_timeline_html.py').read())"` exits 0 (valid Python)
    - `grep -c 'def _esc' html/gen_timeline_html.py` equals 1 (Python-side _esc)
    - `grep -c 'function _esc' html/gen_timeline_html.py` equals 1 (JS-side _esc)
    - `grep -c '\.replace("</", "<\\\\/' html/gen_timeline_html.py` ≥ 3 (JSON-in-script defense on shots + stems + transcript at minimum; + characters + props)
    - `grep -c 'gallery-card' html/gen_timeline_html.py` ≥ 2 (CSS rule + HTML template)
    - `grep -c 'ref-chip' html/gen_timeline_html.py` ≥ 2 (CSS + JS template)
    - `grep -c 'fill-filled\|fill-degraded' html/gen_timeline_html.py` ≥ 2 (CSS classes)
    - `grep -c 'onerror' html/gen_timeline_html.py` ≥ 1 (Pitfall 7 — PNG missing fallback)
    - `grep -c 'route_filled' html/gen_timeline_html.py` ≥ 2 (build_shots_js + JS chip logic)
    - `python3 html/gen_timeline_html.py --shots spec/fixtures/v1.1/shots.json --characters spec/fixtures/v1.1/characters.json --props spec/fixtures/v1.1/props.json --prompts spec/fixtures/v1.1/prompts.json --output /tmp/phase8_timeline.html` exits 0
    - Generated HTML contains `gallery-card`, `ref-chip`/`char-chip`, `fill-filled` or `fill-degraded`
  </acceptance_criteria>
  <done>
html/gen_timeline_html.py extended: _esc() (Python + JS) XSS defense carried from Phase 7 CR-04; JSON-in-script .replace("</","<\\/") on all inlined JSON; build_shots_js adds route_filled + character_refs/prop_refs; build_html renders gallery section (cards with _esc'd names + external PNG + onerror fallback) + ref-chips (in-page #gallery-<id> anchors with JS _esc) + fill-chip (green/gray per route_filled); main() gains --prompts/--characters/--props/--asset-json flags with registry_snapshot-preferred loading. Graceful-degrades when registry/prompts absent.
  </done>
</task>

<task type="auto">
  <name>Task 2: run_pipeline.py — step_timeline invokes attach_refs.py as pre-step + mtime cache includes prompts_json (Pitfall 9) + NO counter bump</name>
  <files>run_pipeline.py</files>
  <read_first>
    - run_pipeline.py (focus on: step_timeline lines 316-343 — the function Phase 8 modifies. Currently: mtime cache on shots/audio/transcript lines 320-323; subprocess to gen_timeline_html.py lines 326-342. Phase 8 inserts attach_refs invocation BEFORE the gen_timeline_html subprocess AND adds prompts_json to the cache inputs.)
    - run_pipeline.py lines 77-81 (run_step helper — takes a cmd list + label string; used for the attach_refs banner)
    - run_pipeline.py lines 101 (HERE constant — `HERE = Path(__file__).resolve().parent`; attach_refs invoked as `str(HERE / "prompts" / "attach_refs.py")`)
    - run_pipeline.py main() around lines 505-520 (where work_dir paths are computed: shots_json, prompts_json, registry_draft etc. — confirm prompts_json path constant exists)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-RESEARCH.md (§Architecture Patterns → Pattern 6 "step_timeline integration — NO counter bump" lines 704-736 — the full step_timeline extension skeleton; §Pitfall 5 "Step-counter phantom bump" lines 934-939 — NO [N/9]; §Pitfall 9 "attach_refs modifies prompts.json but cache thinks timeline unchanged" lines 962-967 — add prompts_json to mtime cache inputs)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-CONTEXT.md (§decisions → "pipeline integration Q3" — LOCKED: step_timeline invokes attach_refs as pre-step, NO new numbered step, NO [N/8]→[N/9])
    - prompts/attach_refs.py (Plan 02 Task 1 — the CLI contract: --prompts + --work-dir flags; in-place rewrite; exit 0 on success + graceful-degrade)
  </read_first>
  <action>
**Extend `step_timeline`** (lines 316-343) with TWO changes:

1. **Invoke attach_refs.py as a pre-step** — insert AFTER the function signature + BEFORE the mtime cache check. The invocation:
   - `if prompts_json and os.path.exists(prompts_json):` (guard — only run if prompts.json exists; step_semantic may have been skipped)
   - `cmd_refs = [sys.executable, str(HERE / "prompts" / "attach_refs.py"), "--prompts", prompts_json, "--work-dir", work_dir]`
   - `run_step(cmd_refs, "[7/8] prompt-ref attachment (attach_refs)")` — REUSES the existing `[7/8]` banner prefix (CONTEXT Q3 lock — NO new numbered step; the banner clearly labels it as part of timeline assembly). Do NOT use `[N/9]`.
   - This runs BEFORE the mtime cache check so that IF attach_refs rewrites prompts.json, the cache check sees the new prompts.json mtime → cache miss → timeline regenerates with new refs/chips (Pitfall 9 prevention, part 1).
   - Add `prompts_json: str` as a NEW parameter to step_timeline's signature (currently it takes shots_json/audio_json/transcript/frames_json/stems_dir/out_html/video_src/stem_basename — add prompts_json at the end).

2. **Add prompts_json to the mtime cache inputs** (lines 320-323) — Pitfall 9 prevention, part 2. Currently the cache checks:
   `os.path.getmtime(out_html) > max(getmtime(shots_json), getmtime(audio_json) or 0, getmtime(transcript) or 0)`
   Change to ALSO include prompts_json:
   `max(getmtime(shots_json), getmtime(audio_json) if audio_json else 0, getmtime(transcript) if transcript else 0, getmtime(prompts_json) if prompts_json and os.path.exists(prompts_json) else 0)`
   This ensures that if attach_refs rewrites prompts.json (new mtime), the cache misses → timeline regenerates. Use `_safe_mtime` (line 346) instead of bare `os.path.getmtime` for TOCTOU safety if practical, but the existing step_timeline uses bare getmtime — match the existing style for consistency (the cache-miss-on-error behavior is acceptable since a missing prompts.json means step_semantic was skipped).

3. **Pass --prompts + --characters/--props to gen_timeline_html.py** (lines 326-342) — extend the cmd construction:
   - After the existing flag additions: `if prompts_json and os.path.exists(prompts_json): cmd += ["--prompts", prompts_json]`
   - `chars_path = os.path.join(work_dir, "characters.json")`; `if os.path.exists(chars_path): cmd += ["--characters", chars_path]`
   - Same for `props_path` → `--props`.
   - `asset_json_path = os.path.join(work_dir, "asset.json")`; `if os.path.exists(asset_json_path): cmd += ["--asset-json", asset_json_path]` (preferred source — registry_snapshot; gen_timeline_html reads it if present).

4. **Update the step_timeline call in main()** — add `prompts_json` to the call arguments (around the existing step_timeline invocation). The `prompts_json` path constant already exists at line 510.

CRITICAL — do NOT add `[N/9]` anywhere. Do NOT renumber existing `[N/8]` counters. Do NOT add a new numbered step function. attach_refs is a pre-step WITHIN step_timeline, under the existing `[7/8]` banner. `grep -cE '\[[1-9]/9\]' run_pipeline.py` MUST return 0 after this task.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "
import ast, re
src = open('run_pipeline.py', encoding='utf-8').read()
ast.parse(src)
# 1) NO [N/9] counter (Pitfall 5)
assert not re.search(r'\[[1-9]/9\]', src), 'phantom [N/9] counter introduced (Pitfall 5)'
# 2) [N/8] count unchanged (still 24 from Phase 7)
n8 = len(re.findall(r'\[[1-8]/8\]', src))
assert n8 == 24, f'[N/8] count changed: expected 24, got {n8}'
# 3) attach_refs invocation in step_timeline
assert 'attach_refs.py' in src, 'attach_refs.py subprocess missing'
assert 'prompt-ref attachment' in src or 'attach_refs' in src, 'attach_refs banner missing'
# 4) prompts_json in mtime cache (Pitfall 9)
# Find step_timeline's cache check — it must reference prompts_json
assert 'prompts_json' in src, 'prompts_json not referenced'
# 5) --prompts / --characters / --props passed to gen_timeline_html
for flag in ('--prompts', '--characters', '--props'):
    assert flag in src, f'{flag} not passed to gen_timeline_html.py'
# 6) step_timeline signature extended with prompts_json
# (check the def line + the main() call)
assert src.count('prompts_json') >= 4, f'prompts_json references too few: {src.count(\"prompts_json\")}'
print(f'OK: step_timeline invokes attach_refs + cache includes prompts_json + [N/8]={n8} unchanged + no [N/9]')
" && python3 run_pipeline.py --help 2>&1 | grep -c 'skip\|timeline\|export' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\[[1-9]/9\]' run_pipeline.py` returns 0 (NO phantom counter — Pitfall 5)
    - `grep -cE '\[[1-8]/8\]' run_pipeline.py` returns 24 (unchanged from Phase 7)
    - `grep -c 'attach_refs' run_pipeline.py` ≥ 2 (subprocess invocation + banner label)
    - `grep -c 'prompts_json' run_pipeline.py` ≥ 4 (path constant + step_timeline param + cache check + main() call)
    - `grep -c '\-\-prompts' run_pipeline.py` ≥ 1 (passed to gen_timeline_html.py)
    - `grep -c '\-\-characters' run_pipeline.py` ≥ 1
    - `python3 -c "import ast; ast.parse(open('run_pipeline.py').read())"` exits 0 (valid Python)
    - `python3 run_pipeline.py --help` exits 0 (argparse not broken)
  </acceptance_criteria>
  <done>
run_pipeline.py step_timeline invokes prompts/attach_refs.py as a pre-step under the existing [7/8] banner BEFORE gen_timeline_html.py; mtime cache includes prompts_json (Pitfall 9 prevented); gen_timeline_html.py receives --prompts + --characters/--props/--asset-json; NO [N/9] counter (Pitfall 5 prevented); [N/8] count stays 24.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: scripts/verify_phase8_smoke.py (NEW — 6-scenario regression: graceful-degrade + idempotency + snapshot freeze + integrity + confirmed-only + XSS inert)</name>
  <files>scripts/verify_phase8_smoke.py</files>
  <behavior>
    - Scenario 1 (attach_no_registry): seed prompts.json (no refs) + NO characters.json/props.json → run attach_refs.py → exit 0 + refs empty + prompt_text facets-only (no identity clause) + schema-valid
    - Scenario 2 (attach_idempotent): seed fixture characters.json/props.json + prompts.json → run attach_refs.py TWICE → byte-diff the two outputs → assert identical; refs match fixture target
    - Scenario 3 (snapshot_freeze): build_asset_dict with confirmed registry → snapshot1 in generator; mutate characters.json; re-read the WRITTEN asset → snapshot unchanged (Pitfall 18 — frozen at export time)
    - Scenario 4 (integrity_dangling_ref): seed prompts.json with character_refs:["char_999"] + characters.json (no char_999) → run verify_contract _producer_registry_integrity → failure mentions "Pitfall 17"
    - Scenario 5 (snapshot_confirmed_only): seed characters.json with 1 confirmed + 1 proposed → _build_registry_snapshot → snapshot contains ONLY the confirmed entry (Pitfall 7)
    - Scenario 6 (html_xss_inert): seed characters.json with name="</script><script>alert(1)</script>" → run gen_timeline_html.py → read HTML → assert literal "</script><script>" does NOT appear (must be escaped or JSON-in-script defended)
    - Overall: all 6 green → "[phase8-smoke] OK: 6/6 scenarios green" + exit 0
  </behavior>
  <read_first>
    - scripts/verify_phase7_smoke.py (the ENTIRE file — DIRECT structural template. Mirror its helper functions: `_tmp_work_dir()` → tempfile.mkdtemp; `_write_synthetic_shots(path, count)`; `_run(cmd, **kw)` → subprocess.run wrapper; `REPO = Path(__file__).parent.parent.resolve()`; bracketed-tag print style `[phase7-smoke] PASS/FAIL`; exit-code contract; temp-dir cleanup in finally. Phase 8 mirror: 6 scenarios instead of 5.)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-RESEARCH.md (§Code Examples → Example 3 "verify_phase8_smoke.py scenario skeleton" lines 1055-1096 — the 6-scenario spec with each scenario's assertions; §Validation Architecture lines 1194-1248 — test framework decision: standalone sys.exit(0/1), NO pytest)
    - prompts/attach_refs.py (Plan 02 Task 1 — script under test in scenarios 1 + 2; importable as `from prompts.attach_refs import attach, _load_registry` for unit-level checks, OR invoke via subprocess for CLI-level checks)
    - scripts/export_asset.py (Plan 02 Task 2 — `_build_registry_snapshot` + `build_asset_dict` under test in scenarios 3 + 5; importable directly)
    - scripts/verify_contract.py (Plan 02 Task 3 — `_producer_registry_integrity` under test in scenario 4; importable directly)
    - html/gen_timeline_html.py (Task 1 — script under test in scenario 6; invoked via subprocess with seeded characters.json)
    - spec/fixtures/v1.1/characters.json + props.json + prompts.json (seed fixtures for scenario 2)
  </read_first>
  <action>
Create NEW standalone `scripts/verify_phase8_smoke.py`. Mirror `scripts/verify_phase7_smoke.py` structure VERBATIM (same helper functions, same bracketed-tag print style, same exit-code contract, same temp-dir-cleanup-in-finally pattern). NO pytest (VALIDATION.md confirms "Framework: None").

**Module docstring (Chinese):** purpose (Phase 8 graceful-degrade + idempotency + snapshot freeze + integrity + confirmed-only + XSS-inert regression), the 6 scenarios, CLI usage `python3 scripts/verify_phase8_smoke.py`.

**Common helpers** (COPY from verify_phase7_smoke.py): `_tmp_work_dir()`, `_write_synthetic_shots(path, count=2)`, `_run(cmd, **kw)`, `REPO = Path(__file__).parent.parent.resolve()`.

**Scenario 1 — attach_no_registry (PROMPT-01 graceful-degrade):**
- Create temp work_dir + synthetic shots.json (2 shots) + a prompts.json with facets but NO character_refs (e.g., `[{"shot_id":1,"subject":"cat","action":"runs","camera":"wide","scene":"yard","lighting":"day","style":"photo","prompt_text":"old"},...]`). Do NOT create characters.json/props.json.
- Run `[sys.executable, str(REPO/"prompts"/"attach_refs.py"), "--prompts", prompts_path, "--work-dir", work_dir]`.
- Assert: returncode == 0; re-read prompts.json; every entry has `character_refs == []` + `prop_refs == []`; `prompt_text` does NOT contain `角色` or `道具` (no identity clause); load prompts.schema.json + Draft202012Validator → 0 errors.

**Scenario 2 — attach_idempotent (PROMPT-01/02 determinism):**
- Create temp work_dir; copy `spec/fixtures/v1.1/characters.json` + `props.json` + a synthetic prompts.json (2 shots with facets matching fixture, but WITHOUT character_refs + with old prompt_text) into it.
- Run attach_refs.py TWICE (second run on the first run's output).
- Assert: returncode == 0 both times; byte-diff the two outputs (`json.dumps(run1, ensure_ascii=False) == json.dumps(run2, ensure_ascii=False)`) → identical; shot 1 `character_refs == ["char_001","char_002"]` (per fixture appearance_shots); shot 1 prompt_text contains `角色:[少女, 路人]`.

**Scenario 3 — snapshot_freeze (PROMPT-04 + Pitfall 18):**
- Create temp work_dir + seed transcript.json (for build_asset_dict) + shots.json + characters.json (1 confirmed char) + props.json (1 confirmed prop). (build_asset_dict needs transcript.json for duration.)
- Call `build_asset_dict(work_dir, '/fake/v.mp4', warnings=None)` → `asset1`. Assert `'registry_snapshot' in asset1['generator']`. Record `snapshot1 = asset1['generator']['registry_snapshot']`.
- Write asset1 to disk as asset.json (json.dump).
- Mutate characters.json on disk: rename char_001 name "少女" → "renamed"; add a new char_003.
- Re-read asset.json from disk → `snapshot_reread = asset['generator']['registry_snapshot']`. Assert `snapshot_reread == snapshot1` (unchanged — the WRITTEN file is frozen; live registry mutation does not retroactively change it). Alternatively: call `build_asset_dict` again → `asset2` with `snapshot2` that DIFFERS from snapshot1 (proving build_asset_dict reads LIVE files, but the frozen asset.json on disk does not change). The freeze guarantee is structural: once written, it doesn't change.

**Scenario 4 — integrity_dangling_ref (PROMPT-03 / Pitfall 17):**
- Create temp work_dir + shots.json (1 shot, id=1) + characters.json (`[{"id":"char_001","name":"少女","review_state":"confirmed","appearance_shots":[1]}]`) + prompts.json (`[{"shot_id":1,"character_refs":["char_999"],"prompt_text":"x"}]` — char_999 NOT in registry).
- Call `_producer_registry_integrity(Path(work_dir))`.
- Assert: at least one failure contains `"char_999"` AND `"Pitfall 17"`.

**Scenario 5 — snapshot_confirmed_only (Pitfall 7):**
- Create temp work_dir + characters.json with 2 entries: char_001 (confirmed) + char_002 (proposed). No props.json.
- Call `_build_registry_snapshot(work_dir)`.
- Assert: snapshot is not None; `len(snapshot['characters']) == 1`; `snapshot['characters'][0]['id'] == 'char_001'` (char_002 filtered — Pitfall 7).

**Scenario 6 — html_xss_inert (PRESENT-01/02 XSS + CR-04 carry):**
- Create temp work_dir + synthetic shots.json (1 shot) + characters.json with `name = "</script><script>alert(1)</script>"` for char_001 + representative_image path.
- Run `[sys.executable, str(REPO/"html"/"gen_timeline_html.py"), "--shots", shots_path, "--characters", chars_path, "--output", html_path]` (no stems — gen_timeline_html graceful-degrades to zero arrays).
- Assert: returncode == 0; read HTML; the literal substring `"</script><script>"` does NOT appear (it must be either `&lt;/script&gt;` in body context via _esc, or `<\/script>` in JSON-in-script context via .replace). The payload must not survive as executable HTML.

**main() / CLI:** argparse with `--verbose`; run all 6 scenarios; collect `(name, ok, detail)`; print `[phase8-smoke] PASS/FAIL {name}: {detail}`; all 6 PASS → `[phase8-smoke] OK: 6/6 scenarios green` + `sys.exit(0)`; else `sys.exit(1)`.

For scenarios that import project modules (attach, _build_registry_snapshot, _producer_registry_integrity): use `sys.path.insert(0, str(REPO))` + `from prompts.attach_refs import attach` etc. (namespace-package convention — no __init__.py).
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 scripts/verify_phase8_smoke.py && python3 -c "
import ast
src = open('scripts/verify_phase8_smoke.py', encoding='utf-8').read()
ast.parse(src)
for token in ('attach_no_registry', 'attach_idempotent', 'snapshot_freeze', 'integrity_dangling', 'snapshot_confirmed_only', 'html_xss_inert'):
    assert token in src, f'scenario {token} missing'
assert '6/6' in src or '6 scenarios' in src
assert 'sys.exit(0)' in src and 'sys.exit(1)' in src
print('OK: verify_phase8_smoke.py exists + 6 scenarios green + exit contract present')
" && python3 spec/validate.py 2>&1 | tail -2 && python3 scripts/verify_contract.py --mode=producer 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/verify_phase8_smoke.py` exits 0
    - Output contains the literal `[phase8-smoke] OK: 6/6 scenarios green`
    - Output contains 6 `PASS` lines (one per scenario)
    - `grep -c 'attach_no_registry\|attach_idempotent\|snapshot_freeze\|integrity_dangling\|snapshot_confirmed_only\|html_xss_inert' scripts/verify_phase8_smoke.py` ≥ 6
    - `grep -c 'Pitfall 17\|Pitfall 7\|Pitfall 18' scripts/verify_phase8_smoke.py` ≥ 2 (scenario assertions reference the pitfalls)
    - `python3 -c "import ast; ast.parse(open('scripts/verify_phase8_smoke.py').read())"` exits 0 (valid Python)
    - `python3 spec/validate.py` exits 0 (no schema regression)
    - `python3 scripts/verify_contract.py --mode=producer` exits 0 (no contract regression)
  </acceptance_criteria>
  <done>
scripts/verify_phase8_smoke.py exists; 6 scenarios (attach_no_registry / attach_idempotent / snapshot_freeze / integrity_dangling_ref / snapshot_confirmed_only / html_xss_inert) all green via `python3 scripts/verify_phase8_smoke.py`; prints "[phase8-smoke] OK: 6/6 scenarios green"; spec/validate.py + verify_contract.py both still green (no regression). Covers PROMPT-01 graceful-degrade + PROMPT-01/02 idempotency + PROMPT-04 freeze + PROMPT-03 integrity + Pitfall 7 confirmed-only + PRESENT XSS inert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator→gen_timeline_html.py | Operator-influenced character/prop `name` (registry-reviewer-editable per characters.schema.json:21) flows into gallery cards, chip badges, and inlined JSON. PRIMARY XSS surface. |
| Pipeline→step_timeline→attach_refs | step_timeline invokes attach_refs.py as a pre-step. attach_refs reads operator-curated registry + rewrites prompts.json. |
| Regression harness→Temp FS | verify_phase8_smoke.py writes temp work_dirs under /tmp/phase8-smoke-*. Cleanup in finally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Tampering / XSS | Operator-influenced `name` in gallery card HTML body (server-rendered Python f-string) | mitigate | Python-side `_esc()` (CR-04 carry from gen_registry_review.py:79-91) on EVERY name interpolation in gallery card HTML. Smoke scenario 6 asserts payload inert. |
| T-08-11 | Tampering / XSS | Operator-influenced `name` in ref-chip JS template literal (client-rendered) | mitigate | JS-side `_esc()` function in the <script> block, applied to every name in ref-chip `${_esc(name)}` interpolation. Mirrors Python _esc. |
| T-08-12 | Tampering / XSS | `</script>` in any string field breaks out of inlined JSON `<script>` block | mitigate | `.replace("</", "<\\/")` on EVERY json.dumps landing in `<script>` (shots + stems + transcript + characters + props). CR-04 fix `336d04f` verbatim. Smoke scenario 6 covers this. |
| T-08-13 | Tampering | Missing PNG shows broken icon (Pitfall 7 in gallery clothes) | mitigate | `onerror="this.style.display='none'"` on every gallery `<img>`. apply_edits.py may OMIT representative_image on ffmpeg failure (Phase 7 WARNING-2). |
| T-08-14 | Integrity | step_timeline cache stale after attach_refs rewrites prompts.json (Pitfall 9) | mitigate | prompts_json added to step_timeline mtime cache inputs. attach_refs runs BEFORE the cache check → new prompts.json mtime → cache miss → timeline regenerates. |
| T-08-15 | Tampering | Phantom [N/9] counter bump (Pitfall 5) | mitigate | attach_refs invoked under existing [7/8] banner within step_timeline. NO new step function. `grep [N/9]` returns 0. |
| T-08-16 | Information Disclosure | Fill-chip reveals route-degrade status to end user | accept | The indicator is operator-facing (producer HTML, not consumer-facing). Route-degrade status is already in generator.warnings (Phase 6). No new disclosure. |
| T-08-SC | Tampering | pip/npm/cargo installs | accept | Plan 03 adds ZERO packages — stdlib (subprocess/tempfile/argparse/contextlib/json/os/sys/pathlib) + existing project deps. No install tasks. |

No HIGH severity threats beyond T-08-10/11/12 (XSS via operator-influenced names — mitigated by _esc + JSON-in-script defense, asserted by smoke scenario 6). All other threats LOW (local tool, no network ingress, schema-validated input, deterministic rendering).
</threat_model>

<verification>
After all 3 tasks complete:
1. `python3 scripts/verify_phase8_smoke.py` exits 0 and prints `[phase8-smoke] OK: 6/6 scenarios green`.
2. `python3 html/gen_timeline_html.py --shots spec/fixtures/v1.1/shots.json --characters spec/fixtures/v1.1/characters.json --props spec/fixtures/v1.1/props.json --prompts spec/fixtures/v1.1/prompts.json --output /tmp/phase8_timeline.html` exits 0; generated HTML contains `gallery-card` + `ref-chip` + `fill-filled` or `fill-degraded`.
3. `grep -cE '\[[1-9]/9\]' run_pipeline.py` returns 0; `grep -cE '\[[1-8]/8\]' run_pipeline.py` returns 24 (unchanged).
4. `python3 spec/validate.py` exits 0 (no schema regression).
5. `python3 scripts/verify_contract.py --mode=producer` exits 0 (no contract regression).
6. **Phase 8 gate (all 3 plans together):**
   - Full suite: `python3 spec/validate.py && python3 scripts/verify_contract.py --mode=producer && python3 scripts/verify_phase8_smoke.py` — all green (~8s).
   - Manual-only (pilots, NOT blockers): gallery UX pilot (open timeline.html in browser, confirm cards render + chips click-scroll + indicator colors); recomposed prompt_text readability pilot (spot-check 3-5 strings).
</verification>

<success_criteria>
- `html/gen_timeline_html.py` renders gallery (character/prop cards with external PNG + _esc'd names + onerror fallback) + reference chips (in-page #gallery-<id> anchors with JS _esc) + semantic-fill indicator (green/gray per route_filled). XSS defense: Python _esc + JS _esc + JSON-in-script .replace("</","\\/") on ALL inlined JSON. Graceful-degrades when registry/prompts absent.
- `run_pipeline.py` step_timeline invokes attach_refs.py as a pre-step (existing [7/8] banner, NO [N/9]); mtime cache includes prompts_json (Pitfall 9); passes --prompts/--characters/--props/--asset-json to gen_timeline_html.py.
- `scripts/verify_phase8_smoke.py` 6 scenarios green (attach_no_registry / attach_idempotent / snapshot_freeze / integrity_dangling_ref / snapshot_confirmed_only / html_xss_inert).
- PRESENT-01 (gallery), PRESENT-02 (chips), PRESENT-03 (indicator) closed. XSS-inert (CR-04 carry) asserted by smoke scenario 6.
- No regression: spec/validate.py + verify_contract.py + [N/8] count all unchanged.
- Phase 8 complete: producer-side prompt references + snapshot freeze + integrity + gallery all wired end-to-end.
</success_criteria>

<output>
Create `.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-03-SUMMARY.md` when done — MUST document: (a) the 2 manual-only pilot items (gallery UX + recomposed prompt_text readability) as non-blocking per 08-VALIDATION.md; (b) that PRESENT-04/05/06 (canvas consumer) are Phase 9 cross-repo deferred.
</output>
