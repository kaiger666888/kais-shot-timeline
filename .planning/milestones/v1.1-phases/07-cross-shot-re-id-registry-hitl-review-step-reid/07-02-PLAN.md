---
phase: 07-cross-shot-re-id-registry-hitl-review-step-reid
plan: 02
type: execute
wave: 2
depends_on: ['07-01']
files_modified:
  - analysis/call_reid.py
autonomous: true
requirements: [CAST-05, CAST-09]
user_setup:
  - service: httpx
    why: "Sync HTTP client to call kais-aigc-platform character-reid route (DEFERRED — route does not exist yet; mirrors Phase 6 shot-analysis client). httpx 0.28.1 already installed + verified in Phase 6."
    env_vars: []
    dashboard_config: []
    precondition: "httpx 0.28.1 is already in the active Python env (verified Phase 6 via `python3 -c \"import httpx; print(httpx.__version__)\"` → 0.28.1). NO `pip install` runs in this plan. The character-reid ROUTE is DEFERRED cross-repo (does not exist in kais-aigc-platform today); graceful-degrade is fully testable without it (route-down → empty-draft + warnings sidecar)."

must_haves:
  truths:
    - "call_reid.py normalizes route response → registry.draft.json where every cluster has review_state='proposed' (CAST-05 lock — producer never emits confirmed/rejected)"
    - "call_reid.py is schema-valid against registry.schema.json (Draft202012Validator pre-write self-check, fails loud)"
    - "Cache is per-video (route_cache/character_reid/video_<vch>.json) NOT per-shot — re-id is cross-shot aggregation (Pitfall 4)"
    - "Route-down (--offline / preflight ConnectError / POST failure) → registry.draft.json written with empty clusters:[] + warnings sidecar populated; asset still exports (CAST-09 graceful-degrade)"
    - "Preflight runs ONCE before the single POST; failure short-circuits (no retry storm — Pitfall 7)"
    - "call_reid.py APPENDS re-id warnings to the existing route_cache/warnings.json sidecar (does NOT overwrite step_semantic warnings from Phase 6)"
    - "URL userinfo scrubbing via _safe_error (WR-05 reuse from call_shot_analysis.py — prevents user:pass@ leaking into warnings → asset.json)"
    - "normalize_clusters projects route response onto registry.schema.json#clusters[] allowed fields only (drops centroid_embedding/mask_bbox/etc — schema additionalProperties:false would reject them)"
    - "_tier_for(mean_cosine) returns auto_merge ≥0.85 / review 0.6-0.85 / auto_distinct <0.6 (CONTEXT Q2 locked three-tier defaults, advisory)"
    - "ROUTE_VERSION literal 'deferred-character-reid-route-v1' is a module-level constant (bump invalidates all caches — post-merge cache-clearing knob)"
  artifacts:
    - path: "analysis/call_reid.py"
      provides: "httpx sync client + normalize_clusters + _tier_for + video_content_hash (reuse) + per-video cache + preflight + main() CLI"
      contains: "ROUTE_VERSION"
  key_links:
    - from: "analysis/call_reid.py:main"
      to: "kais-aigc-platform POST /api/v1/production/character-reid (DEFERRED)"
      via: "httpx.Client(base_url=--reid-url).post(json=body)"
      pattern: "client\\.post"
    - from: "analysis/call_reid.py:main"
      to: "output/<asset>/route_cache/character_reid/video_<vch>.json"
      via: "per-video cache file with embedded _cache_key"
      pattern: "_cache_key"
    - from: "analysis/call_reid.py:main"
      to: "output/<asset>/route_cache/warnings.json"
      via: "APPEND re-id warnings to existing sidecar (read-merge-write, NOT overwrite)"
      pattern: "route_cache.*warnings"
    - from: "analysis/call_reid.py:normalize_clusters"
      to: "spec/schemas/registry.schema.json"
      via: "Draft202012Validator iter_errors pre-write"
      pattern: "Draft202012Validator"
---

<objective>
Build `analysis/call_reid.py` — a self-contained CLI stage that calls the (DEFERRED cross-repo) kais-aigc-platform `POST /api/v1/production/character-reid` route ONCE per video (re-id is cross-shot aggregation, NOT per-shot), normalizes the response into `registry.draft.json` (all clusters `review_state:"proposed"`), caches the whole-video response under `route_cache/character_reid/video_<vch>.json`, runs a single preflight probe, APPENDS re-id warnings to the existing `route_cache/warnings.json` sidecar (non-destructive merge with step_semantic's warnings), and degrades gracefully to an empty-clusters draft when the route is unreachable.

Purpose: This is the producer-side half of the re-id pipeline — it produces the `registry.draft.json` that `html/gen_registry_review.py` (Plan 03) turns into the HITL review HTML, which in turn produces `registry.edits.json`, which `registry/apply_edits.py` (Plan 03) applies to canonical `characters.json`/`props.json`. The route is DEFERRED (does not exist in kais-aigc-platform today — mirrors `feat/shot-analysis-route` pre-merge state in Phase 6). Graceful-degrade is fully testable NOW because no route is running.

Output: 1 new module (`analysis/call_reid.py`, ~200-250 LOC). It is a SIBLING of `analysis/call_shot_analysis.py` (Phase 6) — same structure, same patterns, different endpoint + payload + normalization. The module is invoked by `run_pipeline.py:step_reid` (Plan 04) as a subprocess.

**Direct template:** `analysis/call_shot_analysis.py` (425 lines) — copy its structure verbatim. The ONLY substantive differences are: (a) constants (ROUTE_NAME/ROUTE_VERSION/ROUTE_PATH); (b) cache is per-video (single file, not per-shot loop); (c) `normalize_clusters()` replaces `compose_facets()`; (d) output is `registry.draft.json` validated against `registry.schema.json` (not prompts.json/prompts.schema.json); (e) warnings sidecar is READ-MERGED-WRITTEN (not overwritten — step_semantic may have written warnings first).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cross-shot-re-id-registry-hitl-review-step-reid/07-CONTEXT.md
@.planning/phases/07-cross-shot-re-id-registry-hitl-review-step-reid/07-RESEARCH.md
@.planning/phases/07-cross-shot-re-id-registry-hitl-review-step-reid/07-VALIDATION.md
@.planning/phases/07-cross-shot-re-id-registry-hitl-review-step-reid/07-01-PLAN.md

<interfaces>
<!-- analysis/call_reid.py CLI contract — consumed by run_pipeline.py:step_reid (Plan 04 Task 1). -->

```
analysis/call_reid.py
  --video <abs path>             (required — original video with audio stream)
  --shots <abs path>             (required — shots.json, used for video_content_hash context + cache key)
  --work-dir <abs path>          (required — output/<asset>/; route_cache/ + registry.draft.json written here)
  --output <abs path>            (required — registry.draft.json path; usually <work-dir>/registry.draft.json)
  --reid-url <url>               (default http://127.0.0.1:8000/api/v1/production/character-reid)
  --reid-timeout <float>         (default 960.0; > route-side 900s execFileSync ceiling)
  --offline                      (store_true; cache-only, no network)
```

Exit codes: 0 on success (including full graceful-degrade — empty clusters + warnings sidecar still exits 0); non-zero only on schema-self-validation failure (registry.draft.json invalid per Draft202012Validator — fails loud).

Side effects:
- Writes `<work-dir>/registry.draft.json` (atomic temp+replace; shape per registry.schema.json).
- Writes/reads `<work-dir>/route_cache/character_reid/video_<vch>.json` (per-video cache with embedded `_cache_key`).
- READS then MERGES then WRITES `<work-dir>/route_cache/warnings.json` (appends re-id warnings to existing step_semantic warnings — non-destructive).

<!-- Route REQUEST/RESPONSE contract (DEFERRED — shape inferred from shot-analysis analog, 07-RESEARCH §Route REQUEST Contract lines 133-186). -->

Endpoint: `POST /api/v1/production/character-reid` (DEFERRED — does not exist today; will mirror shot-analysis THIN-wrapper pattern).

Predicted request body (sent by call_reid.py main):
```json
{
  "video":  "<host abs path>",
  "shots":  "<host abs path>/shots.json",
  "mask_samples": 3,
  "embedding_model": "facebook/dinov2-base",
  "tau": 0.30,
  "fps": 24
}
```
NOTE: re-id is cross-shot aggregation — NO `shot_id_range` (unlike shot-analysis per-shot isolation). The whole video is processed in one POST.

Predicted response envelope (`{code, data, message}` — same as shot-analysis):
```json
{
  "code": 200,
  "data": {
    "clusters": [<cluster>, ...],
    "count": N,
    "outputDir": "...",
    "crops": ["char_001.png", ...]
  },
  "message": "Character re-id complete"
}
```

Predicted cluster shape (route → producer normalize → registry.schema.json):
```json
{
  "cluster_id": "char_001",
  "mean_cosine": 0.92,
  "members": [
    {"shot_id": 1, "frame_pos": "first", "mask_quality": "high"},
    ...
  ]
}
```
normalize_clusters projects this onto registry.schema.json#clusters[] allowed fields: adds `review_state:"proposed"` (CAST-05), adds `tier` (from _tier_for(mean_cosine)), drops any extra route fields (centroid_embedding/mask_bbox — schema additionalProperties:false rejects them).

<!-- Established stage pattern (mirror analysis/call_shot_analysis.py verbatim — Phase 6 proven). -->

- Module docstring (Chinese; purpose + route contract + output schema + CLI usage) — mandatory per CLAUDE.md.
- Module-level constants: ROUTE_NAME, ROUTE_VERSION, ROUTE_PATH, REGISTRY_SCHEMA.
- `video_content_hash(video_path)` — REUSE from call_shot_analysis.py (same head+tail 1MB sha256[:16]).
- `_safe_error(msg)` — REUSE from call_shot_analysis.py (URL userinfo scrubbing, WR-05).
- Lazy `import httpx` inside functions that use it (WR-02 — offline mode works without httpx).
- `argparse` with Chinese `help=` strings.
- `main()` at bottom + `if __name__ == "__main__": sys.exit(main())`.
- JSON I/O: `indent=2`, `ensure_ascii=False`.
- Atomic write: temp + os.replace.

<!-- registry.schema.json (Phase 5 shipped — the frozen output shape). -->

registry.draft.json = `{generated_at, model, tau, clusters: [...]}`. clusters[].required = [cluster_id, review_state, tier, mean_cosine, members]. members[].required = [shot_id, frame_pos]. cluster_id pattern `^(char|prop)_[0-9]{3}$`. review_state enum [proposed, confirmed, rejected]. tier enum [auto_merge, review, auto_distinct]. mean_cosine range [-1, 1]. Schema $comment locks the three-tier advisory thresholds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create analysis/call_reid.py (httpx client + normalize_clusters + per-video cache + preflight + graceful-degrade + warnings sidecar merge)</name>
  <files>analysis/call_reid.py</files>
  <behavior>
    - normalize_clusters(route_data with 1 cluster char_001 mean_cosine 0.92 + 2 members) → list of 1 cluster with review_state="proposed", tier="auto_merge", mean_cosine=0.92, members projected to shot_id/frame_pos/mask_quality only
    - normalize_clusters(None) → [] (route down / non-dict → empty)
    - normalize_clusters({"clusters": "not_a_list"}) → [] (defensive — route bug returning wrong type)
    - normalize_clusters drops extra fields: route cluster with centroid_embedding/mask_bbox → normalized cluster has ONLY schema-allowed fields (cluster_id/review_state/tier/mean_cosine/members)
    - _tier_for(0.92) → "auto_merge"; _tier_for(0.72) → "review"; _tier_for(0.45) → "auto_distinct"; _tier_for(None) → "review"; _tier_for("not_a_number") → "review"
    - normalize_clusters skips clusters with empty members (schema minItems:1)
    - main() with unreachable --reid-url → registry.draft.json written with clusters:[] + warnings sidecar has ≥1 entry containing "preflight" or "ConnectError"; exit 0
    - main() with --offline + pre-seeded per-video cache matching _cache_key → 0 httpx post calls; registry.draft.json uses cached clusters; stdout contains "cache hit"
    - main() APPENDS to existing warnings sidecar: pre-existing step_semantic warnings preserved + re-id warnings appended (read-merge-write, non-destructive)
    - ROUTE_VERSION == 'deferred-character-reid-route-v1'; ROUTE_NAME == 'character_reid'; ROUTE_PATH == '/api/v1/production/character-reid'
  </behavior>
  <read_first>
    - analysis/call_shot_analysis.py (the ENTIRE 425-line file — DIRECT template; mirror its structure, docstring style, helper functions, main() flow, lazy imports, _safe_error, video_content_hash. Read it ALL in one pass — extract every pattern.)
    - .planning/phases/07-cross-shot-re-id-registry-hitl-review-step-reid/07-RESEARCH.md (§Architecture Patterns → Pattern 1 "call_reid.py mirrors call_shot_analysis.py" lines 313-370 — the normalize_clusters + _tier_for skeleton; §Code Examples → Example 2 "call_reid.py normalize + write" lines 906-939; §Common Pitfalls → Pitfall 4 "per-shot cache key (wrong granularity)" + Pitfall 7 "mean_cosine vs tau units" + Pitfall 8 "step_reid blocking" + Pitfall 9 "registry.draft.json in asset.json#data")
    - .planning/phases/07-cross-shot-re-id-registry-hitl-review-step-reid/07-CONTEXT.md (§decisions → ALL Q1/Q2/Q3 under "Cross-repo split" + "step integration & counter" — LOCKED: graceful-degrade shape, flags, per-video cache, 960s timeout, non-blocking)
    - spec/schemas/registry.schema.json (the OUTPUT schema — clusters[].required, cluster_id pattern, tier enum, mean_cosine range, members shape. Draft202012Validator pre-write checks against THIS.)
    - spec/fixtures/v1.1/registry.draft.json (the TARGET shape — 3 clusters, all proposed, generated_at/model/tau header. call_reid.py output must match this shape.)
  </read_first>
  <action>
**No `analysis/__init__.py`** — CLAUDE.md forbids package structure. `analysis/call_reid.py` is a sibling of `analysis/call_shot_analysis.py` (Phase 6 created the directory without `__init__.py`; namespace package convention).

**`analysis/call_reid.py`** structure (Chinese module docstring mandatory — mirror call_shot_analysis.py header style: purpose + route contract summary + output schema + CLI usage + graceful-degrade behavior):

1. **Module-level constants:**
   - `ROUTE_NAME = "character_reid"` (used in cache path `route_cache/character_reid/` + _cache_key)
   - `ROUTE_VERSION = "deferred-character-reid-route-v1"` (cache-invalidation knob — route merges → bump this → all caches miss)
   - `ROUTE_PATH = "/api/v1/production/character-reid"` (hardcoded path; --reid-url provides host root only)
   - `REGISTRY_SCHEMA = Path(__file__).resolve().parent.parent / "spec" / "schemas" / "registry.schema.json"` (absolute path for pre-write validation)

2. **`video_content_hash(video_path: str) -> str`** — COPY VERBATIM from `call_shot_analysis.py:91-108`. Same head+tail 1MB + size sha256[:16]. This is the per-video cache key component (NOT per-shot — re-id is cross-shot aggregation).

3. **`_safe_error(msg: str) -> str`** — COPY VERBATIM from `call_shot_analysis.py:111-122`. Same URL userinfo scrubbing regex `re.sub(r"(https?://)([^@/]+@)", r"\1***@", msg)`. WR-05 reuse.

4. **`_tier_for(mean_cosine: float | None) -> str`** (CONTEXT Q2 locked three-tier defaults, advisory):
   ```python
   def _tier_for(mean_cosine) -> str:
       """CONTEXT-locked 三档（advisory —— calibration deferred per Q2）。
       mean_cosine is cosine SIMILARITY (1.0 = identical; 0.0 = orthogonal).
       τ in registry.schema.json $comment is cosine DISTANCE (= 1 - similarity).
       """
       if not isinstance(mean_cosine, (int, float)):
           return "review"   # 未知 → 进 review 队列让人决定
       if mean_cosine >= 0.85:
           return "auto_merge"
       if mean_cosine >= 0.6:
           return "review"
       return "auto_distinct"
   ```

5. **`normalize_clusters(route_data: dict | None) -> list[dict]`** (CAST-05 — the projection from route response to registry.schema.json#clusters[] shape):
   - If `route_data` is not a dict → return `[]`.
   - Extract `raw = route_data.get("clusters")` if it's a list, else `[]`.
   - For each raw cluster `r` (skip if not a dict):
     - Build `members` list: for each `m` in `r.get("members") or []`, if `m` is a dict AND `m.get("shot_id")` is an int → append `{"shot_id": m["shot_id"], "frame_pos": m.get("frame_pos", "first"), **({"mask_quality": m["mask_quality"]} if m.get("mask_quality") else {})}`. Frame_pos accepts string ("first"/"last"/"25%"/"50%"/"75%") or number (schema allows both). mask_quality omitted if absent (schema optional).
     - If `members` is empty → skip this cluster (schema minItems:1).
     - Compute `mc = r.get("mean_cosine")` — if not int/float → default 0.0 (schema range [-1, 1]; 0.0 = orthogonal, neutral).
     - Append `{"cluster_id": r["cluster_id"], "review_state": "proposed", "tier": _tier_for(mc), "mean_cosine": float(mc) if isinstance(mc, (int, float)) else 0.0, "members": members}`.
   - cluster_id is NOT validated here (schema Draft202012Validator will catch malformed patterns pre-write). Route is assumed to emit schema-conforming IDs; if not, schema validation fails loud.
   - **CRITICAL:** drop ALL extra route fields (centroid_embedding, mask_bbox, etc). schema additionalProperties:false would reject them. This is the explicit projection per RESEARCH Pattern 1 line 326-357.

6. **`call_route(client, body: dict) -> tuple[dict | None, str | None]`** — COPY STRUCTURE from `call_shot_analysis.py:165-217`. Same try/except `(httpx.HTTPError, ValueError, AttributeError, TypeError, KeyError)` broad catch. Same `_safe_error` on all error paths. Same envelope check (`payload.get("code") != 200` → degrade). Difference: extract `data = payload.get("data")` and return `(data, None)` on success (the WHOLE data dict, not `data.shots[0]` — re-id returns `data.clusters` as a list, not per-shot). call_route returns the `data` dict (containing clusters + optional crops); normalize_clusters handles projection.

7. **`preflight(base_url: str, timeout: float = 5.0) -> tuple[bool, str | None]`** — COPY VERBATIM from `call_shot_analysis.py:220-243`. Same single short probe. Same `rsplit("/api/v1", 1)[0]` host extraction. Same ROUTE_PATH constant for the probe path. ANY HTTP response (even 404/405) = "up"; only httpx.HTTPError → down.

8. **`main()`** (the CLI stage — mirror call_shot_analysis.py main() shape but SIMPLER because no per-shot loop):
   - argparse: `--video` (required), `--shots` (required), `--work-dir` (required), `--output` (required — registry.draft.json), `--reid-url` (default `http://127.0.0.1:8000/api/v1/production/character-reid`), `--reid-timeout` (type=float, default 960.0), `--offline` (store_true). Chinese `help=` strings.
   - Step sequence:
     1. Load `shots_meta = json.load(open(args.shots))` (used for cache key context — shots.json path + mtime feeds the video identity). Compute `vch = video_content_hash(args.video)`.
     2. `cache_dir = os.path.join(args.work_dir, "route_cache", ROUTE_NAME)`; `os.makedirs(cache_dir, exist_ok=True)`. `cache_file = os.path.join(cache_dir, f"video_{vch}.json")` (per-video, NOT per-shot — Pitfall 4 prevention).
     3. `warnings_sidecar = os.path.join(args.work_dir, "route_cache", "warnings.json")`. **READ existing warnings** (step_semantic may have written here first): try `json.load`; if dict with `"warnings"` list of strings → `existing_warnings = that list`; else `existing_warnings = []`. OSError/JSONDecodeError → `existing_warnings = []` (don't let a corrupt sidecar block us).
     4. `reid_warnings = []` (new warnings from this step).
     5. Cache lookup: if `cache_file` exists → load it; check `_cache_key` matches `(vch, ROUTE_NAME, ROUTE_VERSION)` → cache hit → `route_data = cached`; print `[reid] cache hit: {cache_file}`. Else stale/missing → `route_data = None`.
     6. Preflight (non-offline only): `route_down = args.offline`; if not offline → `(ok, msg) = preflight(args.reid_url)`; if not ok → `route_down = True; reid_warnings.append(msg); print(f"[reid] preflight failed → route_down: {msg}")`.
     7. Cache miss + not route_down: open httpx.Client (lazy import; base = `args.reid_url.rsplit("/api/v1", 1)[0]`; timeout `httpx.Timeout(connect=5.0, read=args.reid_timeout, write=5.0, pool=5.0)`). Build body `{"video": os.path.abspath(args.video), "shots": os.path.abspath(args.shots), "mask_samples": 3, "embedding_model": "facebook/dinov2-base", "tau": 0.30, "fps": 24}`. Call `(route_data, err) = call_route(client, body)`. If err → `reid_warnings.append(f"character-reid: {err}")`; `route_data = None`. Else → write cache_file (json.dump with `_cache_key` embedded: `{"clusters": route_data.get("clusters", []), "_cache_key": {"video_content_hash": vch, "route_name": ROUTE_NAME, "route_version": ROUTE_VERSION}, **rest_of_route_data}`). Close client in finally.
     8. Cache miss + route_down: `reid_warnings.append("character-reid: offline/cache-miss → empty draft")` (Pitfall 4 — non-silent degrade).
     9. Normalize: `clusters = normalize_clusters(route_data if isinstance(route_data, dict) else (route_data.get("data") if ...))` — NOTE: route_data from call_route is the `data` dict directly (contains `clusters` key). normalize_clusters extracts `.get("clusters")`. If route_data is None (degrade) → normalize_clusters(None) → [].
     10. Compose `registry_draft = {"generated_at": datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ"), "model": "facebook/dinov2-base", "tau": 0.30, "clusters": clusters}`. (generated_at is fresh per run — this is fine, it's a pipeline-internal draft not a canonical asset.)
     11. **Pre-write schema self-validation** (fails loud): load registry.schema.json; `errors = list(Draft202012Validator(registry_schema).iter_errors(registry_draft))`; if errors → `sys.exit(f"registry.draft.json schema validation failed ({len(errors)} errors): ...")`.
     12. **Atomic write** registry.draft.json: `tmp = args.output + ".tmp"`; `json.dump(registry_draft, open(tmp, "w", encoding="utf-8"), ensure_ascii=False, indent=2)`; `os.replace(tmp, args.output)`.
     13. **Merge + write warnings sidecar** (NON-DESTRUCTIVE — append to step_semantic's warnings): `all_warnings = existing_warnings + reid_warnings`; `json.dump({"warnings": all_warnings}, open(warnings_sidecar, "w", encoding="utf-8"), ensure_ascii=False, indent=2)`.
     14. Print `[reid] wrote {args.output} ({len(clusters)} clusters, {len(reid_warnings)} new warnings)`; return 0.

   Lazy-import httpx INSIDE call_route + preflight + the client-open in main (WR-02 — offline mode works without httpx). Lazy-import jsonschema.Draft202012Validator inside the pre-write validation block. Lazy-import datetime/timezone at top of main (or inline).

   Add `if __name__ == "__main__": sys.exit(main())` at the bottom.

   The `tdd="true"` behavior block above is the contract — write the script to satisfy it. Inline assertions during development (a `python3 -c "..."` one-liner per behavior row) are the test loop; the formal regression harness lives in Plan 04 Task 3 (`scripts/verify_phase7_smoke.py`).
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "
import json, sys, tempfile, os, subprocess
sys.path.insert(0, '.')
from analysis.call_reid import (normalize_clusters, _tier_for, video_content_hash,
                                 ROUTE_NAME, ROUTE_VERSION, ROUTE_PATH, _safe_error)

# 1) constants
assert ROUTE_NAME == 'character_reid', ROUTE_NAME
assert ROUTE_VERSION == 'deferred-character-reid-route-v1', ROUTE_VERSION
assert ROUTE_PATH == '/api/v1/production/character-reid', ROUTE_PATH

# 2) _tier_for (CONTEXT Q2 locked three-tier)
assert _tier_for(0.92) == 'auto_merge'
assert _tier_for(0.85) == 'auto_merge'   # boundary inclusive
assert _tier_for(0.72) == 'review'
assert _tier_for(0.60) == 'review'       # boundary inclusive
assert _tier_for(0.45) == 'auto_distinct'
assert _tier_for(None) == 'review'
assert _tier_for('not_a_number') == 'review'

# 3) normalize_clusters — projection
route_data = {'clusters': [
    {'cluster_id': 'char_001', 'mean_cosine': 0.92, 'members': [
        {'shot_id': 1, 'frame_pos': 'first', 'mask_quality': 'high'},
        {'shot_id': 2, 'frame_pos': 'last', 'mask_quality': 'medium'}],
     'centroid_embedding': [0.1]*768,   # extra field — must be dropped
    }]}
clusters = normalize_clusters(route_data)
assert len(clusters) == 1, f'expected 1 cluster, got {len(clusters)}'
c = clusters[0]
assert c['cluster_id'] == 'char_001'
assert c['review_state'] == 'proposed'   # CAST-05 lock
assert c['tier'] == 'auto_merge'
assert c['mean_cosine'] == 0.92
assert len(c['members']) == 2
assert c['members'][0] == {'shot_id': 1, 'frame_pos': 'first', 'mask_quality': 'high'}
assert 'centroid_embedding' not in c   # extra field dropped (schema additionalProperties:false)

# 4) normalize_clusters — edge cases
assert normalize_clusters(None) == []
assert normalize_clusters({}) == []
assert normalize_clusters({'clusters': 'not_a_list'}) == []
assert normalize_clusters({'clusters': []}) == []
# cluster with empty members → skipped (schema minItems:1)
assert normalize_clusters({'clusters': [{'cluster_id': 'char_001', 'mean_cosine': 0.9, 'members': []}]}) == []

# 5) video_content_hash determinism + content-sensitivity
with tempfile.NamedTemporaryFile(delete=False) as t:
    t.write(b'X' * (3 * 1024 * 1024)); v1 = t.name
h1 = video_content_hash(v1); h2 = video_content_hash(v1)
assert h1 == h2 and len(h1) == 16, 'hash not deterministic or wrong length'
with open(v1, 'rb+') as f: f.seek(-1, 2); f.write(b'Y')
assert video_content_hash(v1) != h1, 'hash not content-sensitive'
os.unlink(v1)

# 6) _safe_error URL scrubbing
assert 'user:pass@' not in _safe_error('ConnectError: http://user:pass@host/path'), 'userinfo not scrubbed'
assert '***@' in _safe_error('ConnectError: http://user:pass@host/path')

print('OK: Task 1 behavior contracts green (normalize_clusters + _tier_for + video_content_hash + constants)')
" && echo '---route-down degrade test---' && python3 -c "
import json, sys, os, tempfile, subprocess
sys.path.insert(0, '.')
# route-down graceful degrade: unreachable URL → registry.draft.json with empty clusters + warnings
work = tempfile.mkdtemp(prefix='phase7-plan02-')
shots = os.path.join(work, 'shots.json')
json.dump([{'id': 1, 'start_sec': 0.0, 'end_sec': 1.0, 'duration': 1.0}], open(shots, 'w'))
draft = os.path.join(work, 'registry.draft.json')
r = subprocess.run([sys.executable, 'analysis/call_reid.py',
    # NOTE: __file__ is UNDEFINED inside `python3 -c` — use a stable repo file literal.
    '--video', 'run_pipeline.py', '--shots', shots, '--work-dir', work, '--output', draft,
    '--reid-url', 'http://127.0.0.1:1/api/v1/production/character-reid',
    '--reid-timeout', '2'], capture_output=True, text=True, timeout=30)
assert r.returncode == 0, f'expected exit 0 (degrade), got {r.returncode}; stderr: {r.stderr[:300]}'
assert os.path.isfile(draft), 'registry.draft.json not written'
d = json.load(open(draft))
assert d['clusters'] == [], f'expected empty clusters on degrade, got {len(d[\"clusters\"])}'
w = json.load(open(os.path.join(work, 'route_cache', 'warnings.json')))
assert any('preflight' in msg or 'ConnectError' in msg or 'character-reid' in msg for msg in w['warnings']), f'no reid warning; got: {w}'
print(f'OK: route-down degrade → empty draft + {len(w[\"warnings\"])} warning(s)')
import shutil; shutil.rmtree(work, ignore_errors=True)
" && echo '---cache-hit offline test (WARNING-4)---' && python3 -c "
import json, sys, os, tempfile, subprocess, shutil
sys.path.insert(0, '.')
from analysis.call_reid import video_content_hash, ROUTE_NAME, ROUTE_VERSION
# Pre-seed per-video cache with fixture clusters + correct _cache_key (matches main() lookup)
work = tempfile.mkdtemp(prefix='phase7-cache-')
video = 'run_pipeline.py'   # stable repo file (NOT __file__ — undefined in -c mode)
vch = video_content_hash(video)
cache_dir = os.path.join(work, 'route_cache', ROUTE_NAME)
os.makedirs(cache_dir, exist_ok=True)
cache_file = os.path.join(cache_dir, f'video_{vch}.json')
fixture = json.load(open('spec/fixtures/v1.1/registry.draft.json'))
cache_payload = {
    'clusters': fixture['clusters'],
    '_cache_key': {'video_content_hash': vch, 'route_name': ROUTE_NAME, 'route_version': ROUTE_VERSION},
}
json.dump(cache_payload, open(cache_file, 'w'), ensure_ascii=False)
shots = os.path.join(work, 'shots.json')
json.dump([{'id': 1, 'start_sec': 0.0, 'end_sec': 1.0, 'duration': 1.0}], open(shots, 'w'))
draft = os.path.join(work, 'registry.draft.json')
# --offline short-circuits BEFORE any httpx import/POST — proves 0 network calls + cache reuse
r = subprocess.run([sys.executable, 'analysis/call_reid.py',
    '--video', video, '--shots', shots, '--work-dir', work, '--output', draft,
    '--offline', '--reid-url', 'http://127.0.0.1:1/api/v1/production/character-reid'],
    capture_output=True, text=True, timeout=15)
assert r.returncode == 0, f'cache-hit run exit {r.returncode}; stderr: {r.stderr[:300]}'
assert 'cache hit' in r.stdout, f'no cache-hit log; stdout: {r.stdout[:300]}'
d = json.load(open(draft))
assert len(d[\"clusters\"]) == len(fixture[\"clusters\"]), f'cache values not used; got {len(d[\"clusters\"])} clusters'
assert 'preflight failed' not in r.stdout, 'offline should short-circuit before preflight (proves 0 httpx POST calls)'
print(f'OK: cache-hit offline → draft uses {len(d[\"clusters\"])} cached clusters, 0 network calls')
shutil.rmtree(work, ignore_errors=True)
"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "from analysis.call_reid import normalize_clusters, _tier_for, video_content_hash, call_route, preflight, ROUTE_NAME, ROUTE_VERSION, ROUTE_PATH; assert ROUTE_NAME=='character_reid'; assert ROUTE_VERSION=='deferred-character-reid-route-v1'; assert ROUTE_PATH=='/api/v1/production/character-reid'"` exits 0
    - `grep -c 'ROUTE_VERSION' analysis/call_reid.py` ≥ 2 (constant defined + used in cache key)
    - `grep -c 'httpx.HTTPError' analysis/call_reid.py` ≥ 2 (call_route + preflight catch — graceful-degrade)
    - `grep -c 'Draft202012Validator' analysis/call_reid.py` ≥ 1 (pre-write self-validation)
    - `grep -c 'review_state.*proposed' analysis/call_reid.py` ≥ 1 (CAST-05 lock — normalize_clusters sets proposed)
    - `grep -c 'video_' analysis/call_reid.py` ≥ 1 (per-video cache file naming — Pitfall 4 prevention)
    - `grep -c 'os.replace' analysis/call_reid.py` ≥ 1 (atomic write registry.draft.json)
    - `grep -c 'existing_warnings' analysis/call_reid.py` ≥ 2 (read-merge-write sidecar — non-destructive)
    - The route-down degrade test (above automated block) exits 0 with empty clusters + warning
    - Module docstring present (lines 1-30 contain a triple-quoted Chinese docstring describing route contract + output schema + CLI usage + graceful-degrade)
  </acceptance_criteria>
  <done>
analysis/call_reid.py implements normalize_clusters (CAST-05 projection with review_state="proposed" default + extra-field dropping + empty-members skip), _tier_for (CONTEXT Q2 three-tier locked), video_content_hash (deterministic + content-sensitive), call_route (HTTPError broad catch), preflight (single short-circuit), main (preflight → per-video cache → single POST → normalize → schema-validate → atomic write → warnings sidecar MERGE). All 10+ behavior rows pass via inline checks. Route-down degrade verified end-to-end (empty draft + warnings sidecar, exit 0).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline→External route | shot-timeline (operator CLI) calls kais-aigc-platform `POST /character-reid` over localhost HTTP. Route is DEFERRED (does not exist today). Identical trust boundary to Phase 6 shot-analysis. |
| Route response→Filesystem | Route response (per-video JSON) written to `route_cache/character_reid/video_<vch>.json` + read back as cache. Trust assumed (route is operator-controlled, same trust level as ffmpeg/Demucs subprocesses). |
| Filesystem→Producer | `route_cache/warnings.json` sidecar: this step APPENDS to existing (non-destructive merge with step_semantic warnings). |
| `--reid-url` flag→httpx | CLI flag controls route URL; only operator sets it (no untrusted user input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering / SSRF | `--reid-url` could point at arbitrary host | mitigate | CLI flag, NOT runtime user input. httpx validates URL syntax (`InvalidURL` on parse fail). Default `http://127.0.0.1:8000/...` (localhost). Same mitigation as Phase 6 T-06-05. CONTEXT documents operator-controlled. |
| T-07-05 | Denial of Service | Route down hangs pipeline (960s timeout) | mitigate | Preflight short-circuits on first ConnectError (Pitfall 7 — single probe, no retry). `--reid-timeout` default 960s is upper bound. `--skip-reid` (Plan 04) is escape hatch. Per-video failure is non-fatal (degrade to empty draft, asset still exports). |
| T-07-06 | Tampering | Cache poisoning (stale/malicious route_cache file) | mitigate | `_cache_key` embedded checks `(video_content_hash, route_name, route_version)`. Mismatch → miss. `video_content_hash` is sha256 head+tail 1MB + size (collision-resistant for cache). Same pattern as Phase 6 (proven). |
| T-07-07 | Information Disclosure | Secrets leaking via warnings | mitigate | `_safe_error` scrubs URL userinfo (WR-05 reuse). Warning strings carry ONLY: exception class + message OR route status code+message. NO request body, NO auth headers. Same as Phase 6 T-06-08. |
| T-07-08 | Spoofing | Route returns 200 with malformed body | mitigate | `call_route` checks `code==200` AND data is dict. `normalize_clusters` uses isinstance checks throughout — malformed → empty clusters (schema-valid degrade). Defense-in-depth. |
| T-07-09 | Tampering | Overwriting step_semantic warnings sidecar | mitigate | main() READS existing warnings.json before writing; APPENDS re-id warnings; writes merged list. Non-destructive. Inline test verifies pre-existing warnings preserved. |
| T-07-SC | Tampering | httpx package legitimacy | mitigate (conditional) | httpx 0.28.1 already in env (Phase 6 verified: Author Tom Christie, BSD-3-Clause, 9 downstream dependents). NO `pip install` task. `user_setup` precondition documents legitimacy. |

No HIGH severity threats (local analysis tool, localhost outbound only, schema-validated output, non-destructive sidecar merge).
</threat_model>

<verification>
After the task completes:
1. `python3 -c "from analysis.call_reid import normalize_clusters, _tier_for, video_content_hash, call_route, preflight, main, ROUTE_VERSION, ROUTE_NAME, ROUTE_PATH"` exits 0 (clean import).
2. The Task 1 inline automated blocks pass (constants + _tier_for + normalize_clusters projection + edge cases + video_content_hash + route-down degrade).
3. `python3 spec/validate.py` still exits 0 (Plan 02 adds no schema changes — registry.schema.json is Phase 5 frozen target).
4. The cache-hit + --skip-reid + empty-draft scenarios are exercised end-to-end in Plan 04 Task 3 (`scripts/verify_phase7_smoke.py`).
5. The warnings sidecar merge (read-existing + append + write-merged) is verified by the route-down degrade test block (step 6 of the automated block).
</verification>

<success_criteria>
- `analysis/call_reid.py` exists with Chinese module docstring, lazy-imported httpx, ROUTE_NAME/ROUTE_VERSION/ROUTE_PATH constants, video_content_hash (reused), _safe_error (reused), _tier_for (three-tier locked), normalize_clusters (CAST-05 projection: proposed default + extra-field drop + empty-member skip), call_route (HTTPError catch), preflight (single probe), main (per-video cache + single POST + normalize + schema-validate + atomic write + warnings sidecar MERGE).
- All behavior rows green via inline checks.
- Route-down degrade verified end-to-end (unreachable URL → registry.draft.json with clusters:[] + warnings sidecar populated, exit 0).
- Warnings sidecar is NON-DESTRUCTIVE (appends to existing step_semantic warnings, does not overwrite).
- CAST-05 (draft shape + proposed default) + CAST-09 (graceful-degrade component) implemented at the component level; CAST-09 step integration + flag wiring closes in Plan 04.
</success_criteria>

<output>
Create `.planning/phases/07-cross-shot-re-id-registry-hitl-review-step-reid/07-02-SUMMARY.md` when done
</output>
