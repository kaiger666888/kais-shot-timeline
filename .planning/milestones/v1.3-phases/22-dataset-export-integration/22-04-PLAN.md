---
phase: 22-dataset-export-integration
plan: 04
type: execute
wave: 3
depends_on: ["22-03"]
files_modified:
  - tests/test_phase22_e2e.sh
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/asset.json"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip_review.html"
  - "output/dataset/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/manifest.json"
autonomous: false
requirements: [PIPE-02, PIPE-01, RT-05]
must_haves:
  truths:
  - "tests/test_phase22_e2e.sh 四场景全绿输出 ALL_SCENARIOS_PASS（exit 0）：S1 ComfyUI down（fixture work_dir 上 5 数据 JSON + asset.json md5 前后等值 + roundtrip.json 缺席 + warnings comfyui_unreachable）/ S2 cache-hit 断点续跑（ep01 pipeline 外层 cache 命中 + sidecar sha256 不变 + 陈旧 asset 重导出）/ S3 抽样模式（--sample-shots 2 → 2/93 镜入样 + cache-hit=2 rendered=0 + sidecar 19 镜 READ-merge 不丢存量）/ S4 VRAM-guard 拒提交（--gpu-index 0 → 批开始 guard 拒绝 + vram_insufficient + exit 0 + cache 保留）"
  - "ep01 抽样端到端一次真跑（SC1 live 证明）：run_pipeline --sample-shots 2 后 asset.json 重导出 schema_version=='1.3' + data.roundtrip=={path, accepted_count:4, rejected_count:15}（现存陈旧 schema '1' 资产被 Pattern 4 强制刷新）+ roundtrip_review.html 生成 + output/dataset/<video-stem>/ 齐产（4 shot dir + manifest + accepted.txt 4 行 + rejected.txt 15 行）"
  - "ep01 roundtrip.json 全程 sha256 前后不变（19 镜冻结真值不被 e2e 触碰——rejected 永不删除红线的 pipeline 级复验）"
  - "harness 前置探测齐备：ComfyUI /system_stats 探活 / GPU1 free VRAM 探测（< 22528MiB 时 S3 走文档化降级分支而非误判失败）/ TTS 5110/5111 端口检查 / CJK work_dir 全程 glob 取路径零硬编码"
  - "Kai 浏览器走查 checkpoint 通过：serve.py 服务下 roundtrip_review.html 双 video 同步播放 + 三态覆盖 + export edits 下载 + apply CLI 演示（/tmp 副本或真实 edits）"
  artifacts:
  - path: tests/test_phase22_e2e.sh
    provides: "PIPE-02 四场景 smoke 回归 harness（mirror v1.2 Phase 14 / Phase 12 bash 形态）"
    min_lines: 180
    contains: "ALL_SCENARIOS_PASS"
  - path: "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip_review.html"
    provides: "ep01 真实审阅面板（SC1 产物 + 走查对象）"
  - path: "output/dataset/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/manifest.json"
    provides: "ep01 正式 dataset 导出（RT-05 e2e 半边）"
    contains: "faithful_below_tau"
  key_links:
  - from: "run_pipeline step_roundtrip 外层 cache"
    to: "S2/S3 断言锚 [9/10] cached roundtrip sidecar"
    via: "ep01 roundtrip.json(08-20) 新于 prompts.json(07-20)/shots.json(07-19) → 首跑即命中"
    pattern: "cached roundtrip sidecar"
  - from: "step_export Pattern 4 条件 input"
    to: "asset.json data.roundtrip 挂载"
    via: "陈旧 asset(07-20) 旧于 roundtrip.json → 强制 miss → 重导出 1.3"
    pattern: "accepted_count"
  - from: "S4 直调 h3_regen --gpu-index 0"
    to: "guard 拒提交日志 + vram_insufficient warning"
    via: "3060Ti 8GB 结构性 < 22528MiB（确定性触发，与 GPU1 余量无关）"
    pattern: "guard 拒绝"
---

<objective>
PIPE-02 + SC1 live 收口：(a) `tests/test_phase22_e2e.sh` 四场景 bash harness（mirror v1.2 Phase 14 / Phase 12 形态——scenario 头 + assert helpers + cleanup trap + ALL_SCENARIOS_PASS exit 契约 + 环境前置探测）；(b) ep01 `--sample-shots 2` 抽样端到端真跑作 SC1 live 证明（外层 cache 全命中秒级——roundtrip.json + asset.json 1.3 挂载 + review HTML + dataset 目录一次齐产）；(c)【checkpoint】Kai 浏览器走查审阅面板（PRESENT-01 人机闭环终验）。

Purpose: 五条 SC 的机器级 + 人工级终验；SC1 的「用户跑一次即拿到数据集」从代码事实变成运行事实。
Output: harness 脚本 + ep01 全套产物 + 走查 checkpoint 记录。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dataset-export-integration/22-CONTEXT.md
@.planning/phases/22-dataset-export-integration/22-RESEARCH.md
@.planning/phases/22-dataset-export-integration/22-PATTERNS.md
@.planning/phases/22-dataset-export-integration/22-03-SUMMARY.md
@tests/run_audio_analysis_smoke.sh
</context>

<interfaces>
<!-- 运行时事实（本 session 实测，e2e 预期直接对表）+ 断言锚全集 -->

ep01 运行时态（2026-08-20 实测）：
- EP01 = output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。（全角名——harness 内一律 `EP01=$(ls -d output/虫虫武侠*第01话*/ | head -1)` 取，禁硬编码）
- roundtrip.json 19 镜全冻结：accepted = shot 10/61/75/84 / rejected 15（faithful<τ=6 + diverged=9）；mtime 08-20 10:05 > prompts.json 07-20 > shots.json 07-19 → step_roundtrip 外层 cache 首跑即命中（h3_regen/scorer/judge 子进程整体跳过，批前 guard 不跑）
- asset.json 现存陈旧导出（schema_version "1"、mtime 07-20 22:56、无 data.roundtrip）→ Pattern 4 使其强制 miss 重导出为 "1.3" + data.roundtrip{accepted_count:4, rejected_count:15}——这正是 SC1 live 证明
- roundtrip_review.html / output/dataset/ 均尚未存在（首跑新生）
- GPU 现态：GPU0(3060Ti) free ~4.9GB（S4 确定性触发位）；GPU1(3090) free 余量薄（实测 22539 vs 阈值 22528——**S3 的直调 h3_regen 段必须 GPU 探测 + 文档化降级分支**）；TTS 5110/5111 未监听（guard kill no-op 安全）
- uniform_sample(93,2) = [1, 47] 双双 cache 命中（20-03 实证）→ --sample-shots 2 零重渲

四模块日志锚（RESEARCH §汇总 grep 锚表，全部实测在场）：
- down-degrade：`[roundtrip] ComfyUI 不可达（status=0）—— graceful-degrade 退出` + warnings `"code": "comfyui_unreachable"`
- VRAM：`[roundtrip] 批开始 guard 拒绝（reason=` + warnings `vram_insufficient`（exit 0）
- cache/抽样：`[roundtrip] 完成：rendered=N cache-hit=N failed=N sampled=N skipped=N` + `[roundtrip] --sample-shots 2: 2/93 镜入样` + scorer `全部 cache 命中` + judge `verdict 应用完成：applied=0 frozen=19`
- pipeline 层（22-03 新增）：`[9/10] cached roundtrip sidecar` / `[10/10] ShotTimelineAsset export` / `[roundtrip-review] wrote` / `[roundtrip-dataset]`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_phase22_e2e.sh 四场景 harness 编写 + 离线确定性场景（S1/S4）本地过</name>
  <files>tests/test_phase22_e2e.sh</files>
  <read_first>
  - tests/run_audio_analysis_smoke.sh（全文 227 行——assert helpers :59-99 / scenario 形态 :130-155 / byte-identical 先例 :153-167 / exit 契约 :224-226 / cleanup trap）
  - .planning/phases/22-dataset-export-integration/22-RESEARCH.md §Validation Architecture 全表 + §Pitfalls 3-7（GPU margin / guard 副作用 / CJK 路径 / ep01 现态不可用于 absent 场景 / 冻结预期）
  - .planning/phases/22-dataset-export-integration/22-PATTERNS.md §test_phase22_e2e.sh 模式指派
  - run_pipeline.py --help（既有 --skip-* flag 全集——S1 fixture 跑法用）
  </read_first>
  <behavior>
  - 头部：scenario 清单注释 + set -euo pipefail + WORK=/tmp/p22-smoke-$$ + cleanup() trap EXIT；EP01 全程 glob 取；helpers：assert_file_exists/absent、assert_grep/not_grep（[PASS]/[FAIL] + 失败 cat 证据 + exit 1）、md5_snapshot/compare
  - 前置探测函数：probe_comfyui（curl /system_stats，down 时 S2e/S3/S4 标 SKIP-with-reason）；probe_gpu1_free（nvidia-smi，< 22528 → S3 直调段走降级分支）；probe_tts（ss -tlnp | grep 511，有监听则打印警告——guard kill 副用）；探测失败输出诊断退出而非误判功能坏（Pitfall 3 fail-fast with reason）
  - S1 ComfyUI down（fixture，Pitfall 6——不能用 ep01 现态）：mkdtemp fixture work_dir——ffmpeg testsrc 生成 ~4s tiny mp4 + 手写最小 shots.json（2 镜全覆盖时长，key=id）+ prompts.json（2 条目含 prompt_text+六 facet）+ 其余数据 JSON 最小合成（timeline/export 缺席输入降级是既有架构行为）；子跑 a：run_pipeline --video tiny.mp4 --output-dir $WORK --comfy-url http://127.0.0.1:1 + 全部既有 --skip-*（语义/重识别/音序等，以 --help 实测为准）+ --skip-export → 断言 roundtrip.json 缺席 + warnings.json 含 comfyui_unreachable + [roundtrip] 降级日志 + 5 数据 JSON md5 前后等值 + asset.json 未被创建/未变；子跑 b：同 fixture 不带 --skip-export 重跑 → export fresh 跑 → asset.json 存在且无 data.roundtrip 键（absent-不挂载 pipeline 级证明）+ 二跑 export cache-hit（asset.json md5 等值）
  - S2 cache-hit 断点续跑（ep01 live）：sha256 前置快照 roundtrip.json → run_pipeline --video "$EP01/h264.mp4" --output-dir output --sample-shots 2 → 断言日志 `[9/10] cached roundtrip sidecar` + `[roundtrip-review] wrote` + `[10/10] ShotTimelineAsset export` + `[roundtrip-dataset]`；asset.json schema_version=="1.3" + data.roundtrip{accepted_count:4, rejected_count:15}；roundtrip.json sha256 前后等值；output/dataset/<stem>/{manifest.json, accepted.txt(4), rejected.txt(15), shot_010/061/075/084 目录}；roundtrip_review.html 存在且 19 卡
  - S3 抽样模式（ep01 live，GPU1-gated 双分支）：probe_gpu1_free ≥ 22528 → 直调 h3_regen --work-dir "$EP01" --sample-shots 2 --vram-wait-timeout 90 → 断言 `2/93 镜入样` + `cache-hit=2 rendered=0` + sidecar 仍 19 镜（READ-merge 不丢存量）+ sha256 等值； scorer 直调 → `全部 cache 命中`；judge 直调 --apply-verdict --tau-sim 0.9670 → `applied=0 frozen=19` + sha256 等值（Pitfall 7 冻结预期）；GPU1 不足 → 打印 `[SKIP] GPU1 free=... < 22528（环境余量，非功能失败；模块级 cache 已由 Phase 20 测试 + S2 外层 cache 覆盖）` 场景记 PASS-with-note（S4 仍独立证明 guard 语义）
  - S4 VRAM-guard 拒提交（确定性）：ComfyUI up 前提 → 直调 h3_regen --work-dir "$EP01" --gpu-index 0 --sample-shots 2 --vram-wait-timeout 90 → 断言 `批开始 guard 拒绝（reason=` + warnings vram_insufficient + exit 0 + roundtrip.json sha256 等值（cache 保留，二跑可续）
  - 每场景 timeout 包裹（mirror Phase 12 惯例，单场景上限 ~300s；S2 整管线含 export 放宽 ~600s）；收尾 `ALL_SCENARIOS_PASS` + exit 0
  </behavior>
  <action>
  1. 按 behavior 写全脚本（helpers → probes → S1→S4 → exit 契约）；S1 fixture 构造封装成函数（mk_fixture_workdir）。
  2. 本地先跑离线确定性部分：S1（ComfyUI down 不需要引擎）+ 前置探测函数——绿了再进 Task 2 live 段（S2/S3/S4 需 ComfyUI up，Task 2 执行）。
  3. 脚本内所有断言路径相对 $EP01/$WORK，零全角硬编码（Pitfall 5）；python3 -c 内嵌断言用于 JSON 字段级检查（asset/dataset/manifest）。
  </action>
  <verify>
    <automated>bash -n tests/test_phase22_e2e.sh &amp;&amp; grep -c "assert_grep\|assert_file" tests/test_phase22_e2e.sh | grep -qv "^0$" &amp;&amp; grep -q "ALL_SCENARIOS_PASS" tests/test_phase22_e2e.sh &amp;&amp; grep -q "comfyui_unreachable" tests/test_phase22_e2e.sh &amp;&amp; grep -q "gpu-index 0" tests/test_phase22_e2e.sh &amp;&amp; grep -q "22528" tests/test_phase22_e2e.sh &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - bash -n 语法过；四场景 + 双分支（S3）+ 前置探测齐在脚本内
  - S1 fixture 子跑 a/b 本地实跑绿（ComfyUI 无关）：byte-identical + absent + warnings + fresh-export-无挂载四组断言过
  - grep 锚全集落脚本（interfaces 锚表）；pytest 全套件零回归
  </acceptance_criteria>
  <done>PIPE-02 harness 成形且离线确定性半边（S1 + 探测）已绿——live 三场景待 Task 2 真跑。</done>
</task>

<task type="auto">
  <name>Task 2: ep01 抽样端到端真跑（SC1 live 证明）+ harness 全量四场景绿</name>
  <files>tests/test_phase22_e2e.sh, output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/asset.json, output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip_review.html, output/dataset/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/manifest.json</files>
  <read_first>
  - Task 1 产出的 tests/test_phase22_e2e.sh（live 段执行前通读——S3 降级分支判定）
  - .planning/phases/22-dataset-export-integration/22-RESEARCH.md §Pitfalls 3/4/7（GPU margin ~11MiB 级 / guard 副作用真执行 / applied=0 frozen=19 预期）+ §Environment Availability
  - 22-03-SUMMARY.md（step_roundtrip 实际 banner 文案——断言锚对齐用）
  </read_first>
  <behavior>
  - 跑前环境检查实录：ComfyUI /system_stats 状态、GPU0/GPU1 free、TTS 端口——写进执行记录（任一硬前置不满足：ComfyUI down 时拉起或如实报告；GPU1 不足只影响 S3 直调段，走降级分支）
  - S2 首跑同时就是 SC1 live 证明：一条 `run_pipeline --video "$EP01/h264.mp4" --output-dir output --sample-shots 2` 产出全部四类产物；断言集 = Task 1 behavior 所列（外层 cache 命中日志 + asset 1.3 挂载 4/15 + dataset 齐产 4 目录 + review HTML 19 卡 + sidecar sha 不变）
  - S3/S4 按 probe 分支执行；S4 必绿（GPU0 结构性触发）；guard 的 POST /free 副作用如实记录（对 ComfyUI cache 的清空是 guard 设计行为）
  - 全绿后 `bash tests/test_phase22_e2e.sh` 端到端再跑一次整体（含 S1）确认 ALL_SCENARIOS_PASS + exit 0；wall time 记录（S2 应分钟级——cache 全命中）
  - 产物证据收集进执行记录：asset.json data.roundtrip 块、dataset 目录树、manifest 分桶、review HTML 卡数、sidecar sha 双值
  </behavior>
  <action>
  1. 环境探测实录（命令 + 输出贴执行记录）。
  2. bash tests/test_phase22_e2e.sh 2>&1 | tee /tmp/p22_e2e_run.log（首跑；逐场景核对 [PASS]）。
  3. 失败处置：断言红 → 先判环境（probe 诊断段）再判功能；S3 降级分支触发时确认其 PASS-with-note 语义如实。
  4. 收集证据 + 复跑一遍全绿确认（幂等：二跑 S2 的 asset.json 已 1.3 → export cache-hit → asset 不再变——顺带验证条件 input 的稳定态）。
  </action>
  <verify>
    <automated>bash tests/test_phase22_e2e.sh 2&gt;&amp;1 | tee /tmp/p22_e2e_final.log &amp;&amp; grep -q "ALL_SCENARIOS_PASS" /tmp/p22_e2e_final.log &amp;&amp; EP01=$(ls -d output/虫虫武侠*第01话*/ | head -1) &amp;&amp; python3 -c "
import json
from pathlib import Path
ep01 = Path('$(ls -d output/虫虫武侠*第01话*/ | head -1)')
a = json.loads((ep01 / 'asset.json').read_text(encoding='utf-8'))
assert a['schema_version'] == '1.3', a['schema_version']
rt = a['data']['roundtrip']
assert rt['accepted_count'] == 4 and rt['rejected_count'] == 15, rt
ds = Path('output/dataset') / ep01.name
dirs = sorted(p.name for p in ds.glob('shot_*'))
assert dirs == ['shot_010', 'shot_061', 'shot_075', 'shot_084'], dirs
m = json.loads((ds / 'manifest.json').read_text(encoding='utf-8'))
assert m['rejected_buckets']['faithful_below_tau'] == 6 and m['rejected_buckets']['diverged'] == 9
h = (ep01 / 'roundtrip_review.html').read_text(encoding='utf-8')
assert h.count('id=\"card-shot_') == 19
print('22-04 T2 OK — SC1 live proof complete')
" &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - ALL_SCENARIOS_PASS + exit 0（S3 允许 documented PASS-with-note；S1/S2/S4 必须硬绿）
  - asset.json == schema "1.3" + data.roundtrip{4,15}；dataset 4 目录 + manifest 分桶 6/9 + 两清单 4/15 行；review HTML 19 卡
  - roundtrip.json sha256 全程不变（跑前/跑后/复跑后三点）
  - 复跑整体仍 ALL_SCENARIOS_PASS（S2 稳定态：1.3 asset export cache-hit）
  </acceptance_criteria>
  <done>SC1 live 证明 + SC5 四场景机器级全绿——「用户一条命令拿到数据集」成为运行事实；剩 Kai 人机走查（Task 3）。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Kai 浏览器走查 —— 审阅面板人机闭环终验（PRESENT-01/SC2）</name>
  <files>output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip_review.html</files>
  <what-built>ep01 真实审阅面板（19 镜冻结态：4 accepted·auto / 15 rejected·auto）+ 完整 HITL 环：三态覆盖按钮 → roundtrip-edits.json 导出 → apply CLI confirmed-only 回写（22-02 已测 + /tmp 副本已演示）。面板由 pipeline e2e 自动生成（SC1 产物之一）。</what-built>
  <how-to-verify>
  1. Claude 起 serve.py：`python3 scripts/serve.py "<EP01 绝对路径>" 8765`（Range-aware，video seek 依赖它）；给 Kai 本地 http://localhost:8765/roundtrip_review.html（如 Kai 不在本机，改用 tailscale serve/URL 转发——按其默认展示偏好）。
  2. 双 video 同步：任选一卡点「▶ 同步播放」——原片段时窗与 regen 同时起播；「⏸ 同步暂停」「⏮ 重置」生效；两侧原生 controls 可独立拖拽（设计如此，非从属锁）。
  3. 信息面：sim bar 与 τ=0.9670 虚线 tick 的相对位置、归因三色 badge（绿 faithful/黄 diverged/灰 underspecified）、judge reason 文案、Prompt 快照折叠展开（六 facet + refs (无)）。
  4. 三态与计数：点 ✓ Accept / ✗ Reject / ⟲ 维持 auto——互斥高亮、reject 卡 opacity 0.45、header `⏳ 已复核 R/N` 与 export bar `已复核 R/N · 覆盖 A/R` 实时变。
  5. Export edits：点 `📥 Export edits (roundtrip-edits.json)`——下载文件含所选覆盖（升序 int、维持 auto 不进）。
  6. （可选，真实覆盖时）把下载的 edits 交给 Claude 跑 apply CLI 回写 ep01 正本并重跑 dataset 导出；无覆盖则维持现状（/tmp 副本演示已在 22-02 留档）。
  </how-to-verify>
  <resume-signal>回复：走查=approved，或列出问题（卡号 + 现象）；可选附上要 apply 的 edits 文件</resume-signal>
  <action>Claude 已自动化全部可自动化部分（面板生成 + e2e 齐产 + serve 拉起 + apply 演示路径就绪）；本 task 暂停等待 Kai 浏览器走查——若 Kai 提交真实 edits，apply 后重跑 `python3 analysis/roundtrip/export_dataset.py --work-dir "$EP01"` 使 dataset 子集随 human 覆盖更新（闭环最后一环），并把 apply 审计行与 dataset 变化记进 SUMMARY。</action>
  <verify>
    <human-check>双 video 同步播放/暂停/重置可用；三态按钮互斥且计数联动；edits 文件下载成功且形状正确（只含显式覆盖）；归因/分数/prompt 快照与 sidecar 数据一致（抽 1-2 卡目视）</human-check>
  </verify>
  <done>Kai 回复 approved（或问题清单已闭环）——PRESENT-01 人机终验通过，Phase 22 五条 SC 全闭合。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 运行环境（ComfyUI/GPU/TTS） → e2e 判定 | 环境漂移（GPU 余量 11MiB 级波动）会被误读为功能回归 |
| 真实 ep01 work_dir → 生产数据 | e2e 在真实冻结数据上跑——任何误写都是真值损毁 |
| 面板 → 人机裁决 | 走查是 SC2 终验；误导性呈现会让 human 覆盖打错方向 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-14 | Spoofing（误报） | 环境余量不足被误判为功能失败 | mitigate | 前置探测函数 + S3 文档化降级分支（PASS-with-note 语义显式）+ fail-fast with reason（Pitfall 3） |
| T-22-15 | Destruction | e2e 误写 ep01 冻结 sidecar / 误删 dataset 兄弟目录 | mitigate | roundtrip.json sha256 三点断言（跑前/跑后/复跑）；export_dataset prune 仅限自身 shot 目录（22-02 已测）；apply 演示默认走 /tmp 副本 |
| T-22-16 | Tampering | guard 副作用（TTS kill + POST /free 真执行）伤及无关服务 | mitigate | probe_tts 端口前置检查 + 输出警告；S4 用 --gpu-index 0 确定性触发；副作用如实记录（guard 设计行为） |
| T-22-17 | Tampering | CJK 全角路径 quoting 错误导致断言打到错误目录 | mitigate | EP01 全程 glob 取 + 断言相对 work_dir（Pitfall 5）；bash -n 语法门 |
| T-22-SC | Tampering | 包安装 | accept | 零新增包 |
</threat_model>

<verification>
- Task 1：bash -n + S1 本地绿 + 锚全集在脚本 + pytest 零回归
- Task 2：ALL_SCENARIOS_PASS + asset 1.3 挂载 + dataset 齐产 + sha 三点不变 + 复跑绿
- Task 3：checkpoint 走查 approved（blocking）
</verification>

<success_criteria>
- SC1 live：ep01 一条命令 → roundtrip.json + regen mp4 + review HTML + dataset 目录 + asset 1.3 挂载齐产（分钟级）
- SC2 终验：Kai 走查 approved（人机闭环全链：面板→edits→apply→dataset 更新可选演示）
- SC5：四场景 harness 全绿（S3 降级分支语义显式非静默）
- RT-01 红线 pipeline 级复验：S1 byte-identical-absent + 全程 sidecar sha 不变
</success_criteria>

<output>
Create `.planning/phases/22-dataset-export-integration/22-04-SUMMARY.md` when done（必须含：四场景逐项 PASS 证据、SC1 产物清单、sidecar sha 双/三点值、GPU/ComfyUI 探测实录、Kai 走查回复原文、若有 apply 则审计行）
</output>
