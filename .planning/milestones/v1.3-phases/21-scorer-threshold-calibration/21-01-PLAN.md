---
phase: 21-scorer-threshold-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analysis/roundtrip/scorer.py
  - analysis/roundtrip/judge.py
  - tests/test_scorer.py
  - tests/test_judge.py
autonomous: true
requirements: [SCORE-01, SCORE-02, DATASET-01]
must_haves:
  truths:
    - "frame_ts 产出的 8 个时间戳全部落在 [25%·dur, 75%·dur] 内（t=0/t=end 结构性不可能出现）；dur-0.2s 端点 clamp 在短镜上生效（Pitfall 3）"
    - "FakeSigLIP（固定向量替身）注入下 per-position cosine 与 mean 分数和手工期望精确一致（打分数学正确性，SCORE-01 半边）"
    - "scorer 写 scores.midframe_sim / judge 写 scores.judge 时浅合并不丢对侧子对象、不丢已有 verdict（DATASET-01 merge 语义）"
    - "--apply-verdict：accepted ⇔ midframe_sim ≥ τ_sim ∧ attribution == prompt_faithful（硬合取）；已有 verdict（auto 或 human）的镜原样冻结永不覆盖"
    - "SigLIP 加载失败 → scorer_model_missing warning（dict 形）+ rc=0 + sidecar 原样（RT-04 degrade）"
    - "judge 解析容错：7 类畸形答案各归各的 verdict 码；失败重问 ≤2 且错误信息回喂 prompt"
  artifacts:
    - path: "analysis/roundtrip/scorer.py"
      provides: "SCORE-01 SigLIP 中段帧轨迹相似度（帧窗数学 + ffmpeg 提取 + 离线加载 + cache + scores 半边 merge）"
      contains: "pooler_output"
    - path: "analysis/roundtrip/judge.py"
      provides: "SCORE-02 三分类归因（grid + qwen-eye + 解析容错）+ --apply-verdict 冻结应用器（SCORE-03/DATASET-01）"
      contains: "apply-verdict"
    - path: "tests/test_scorer.py"
      provides: "FakeSigLIP + 帧窗数学 + cache + degrade 离线单测"
      min_lines: 250
    - path: "tests/test_judge.py"
      provides: "解析容错矩阵 + FakeEye 重问 + grid 布局 + verdict 冻结/合并/schema 离线单测"
      min_lines: 350
  key_links:
    - from: "analysis/roundtrip/scorer.py + judge.py"
      to: "analysis/roundtrip/h3_regen.py 共享件"
      via: "importlib.util.spec_from_file_location 文件加载（单源不漂移；不改 h3_regen 成包）"
      pattern: "spec_from_file_location"
    - from: "analysis/roundtrip/judge.py"
      to: "analysis/engine_clients/qwen_eye_client.py"
      via: "sys.path.insert(parent.parent) 后 import QwenEye（sanctioned 唯一跨模块 import；深一层 off-by-one）"
      pattern: "from engine_clients.qwen_eye_client import"
    - from: "analysis/roundtrip/scorer.py"
      to: "route_cache/scorer/shot_XXX.json"
      via: "帧时间戳清单（frames.orig/regen 各 {j,t_pct,t_sec,path}）+ per_position_cos 落盘（SC2 审计面）"
      pattern: "per_position_cos"
    - from: "analysis/roundtrip/judge.py --apply-verdict"
      to: "output/<work-dir>/roundtrip.json"
      via: "READ-merge 冻结写入（scores 浅合并 / verdict 只补缺）+ 写前两层自校验"
      pattern: "tau"
---

<objective>
Phase 21 的全部代码半边，全离线零 GPU：`analysis/roundtrip/scorer.py`（SCORE-01 SigLIP so400m 中段帧轨迹相似度——N=8 @25%-75% 时窗、per-position cosine、帧清单审计 cache、scores.midframe_sim 半边 merge、scorer_model_missing degrade）+ `analysis/roundtrip/judge.py`（SCORE-02 qwen-eye 三分类归因——2×4 标注 grid、解析容错 + 重问 ≤2、scores.judge 半边 merge；SCORE-03/DATASET-01 的 --apply-verdict 冻结应用器）+ 两份离线单测（FakeSigLIP/FakeEye 替身，帧窗数学/解析矩阵/冻结幂等全断言）。

Purpose: SC1/SC2/SC5 的机器可证半边；SC3/SC4 的工具链本体。GPU 真跑（smoke + 19 镜批）在 21-02/21-03 复用同一 requirement IDs（mirror 18-01/19-01/20-01 先例——离线半边先交付，真机半边共享 IDs）。
Output: 2 个新模块 + 2 份新测试；全套件 pytest 绿（基线 119 passed 上只增不减）。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-scorer-threshold-calibration/21-CONTEXT.md
@.planning/phases/21-scorer-threshold-calibration/21-RESEARCH.md
@.planning/phases/21-scorer-threshold-calibration/21-PATTERNS.md
@analysis/roundtrip/h3_regen.py
</context>

<interfaces>
<!-- 已存在、本 plan 要消费的接点（全部 VERIFIED，行号=当前 HEAD） -->

h3_regen.py 可复用件（importlib 文件加载为 h3s，模块级无副作用、main 有 guard）：
- h3s.append_roundtrip_warnings(work_dir, new_entries)（L685）——双形保留 + 上一轮 [roundtrip] strip；degrade 三码 closed enum {comfyui_unreachable, vram_insufficient, scorer_model_missing}
- h3s._atomic_write_json(path, data)（L654）——tmp 带 PID + os.replace
- h3s.probe_duration_sec(path)（L724）、h3s.gpu_mem_mib(gpu_index)（L918，fail-open）、h3s.comfy_free(comfy_url)（L1005，POST {unload_models:true, free_memory:true}）
- h3s._iter_sidecar_errors(payload)（L770，Draft202012Validator 全量 iter_errors）+ h3s.write_roundtrip_sidecar(work_dir, entries)（L779，kept-keys = 除 regen/status 全保留——scores/verdict 原样；Pitfall 8 已核对）
- h3s.COMFY_URL_DEFAULT = "http://127.0.0.1:8188"（L142）

engine_clients.qwen_eye_client（judge.py 唯一新 import）：
- QwenEye().ensure_ready(timeout_s=120) → (healthy: bool, owned: bool)（L176）；stop_if_owned()（L243，KAP lease 配对释放）
- observe_single(image_path: Path, question: str, max_tokens=2000) → str（L306）——单图豁免多图丢弃 bug；_call_llm 内部恒传 enable_thinking:false + 温度 0.1 + 瞬态重试 1 次；串行调用 thread-unsafe by design
- ENGINE_NAME / ENGINE_VERSION 模块级常量

roundtrip.schema.json 形状（写前校验目标）：
- scores.midframe_sim{score: number[0,1], model: string minLength 1}（required score+model）
- scores.judge{attribution: enum[prompt_faithful, model_diverged, prompt_underspecified], confidence: number[0,1], reason: string 1..2000}（required 三件）
- verdict{decision: enum[accepted, rejected], source: enum[auto, human], decided_at: string}（required decision+source）
- 条目顶层：shot_id(int ≥1) required，regen/scores/verdict 均 optional（degrade 中间态合法）

数据输入（ep01 实测形状）：
- shots.json = list[{id: int, start_sec, end_sec, duration}]（93 条）
- prompts.json = list[{shot_id, …, prompt_text: str}]（93 条，顶层 list）
- roundtrip.json 现存 2 条 @896×512：regen{path: "roundtrip/shot_XXX_regen.mp4", video_content_hash, engine_version: "fl2va-int8/euler+simple/15/896x512", duration_sec, width, height}
- 原片 = work_dir/h264.mp4（1920×1080 30fps）

transformers 5.6.2 实测锚（RESEARCH Pattern 1 探针）：HF_HUB_OFFLINE=1 + TRANSFORMERS_OFFLINE=1 先设 env 再 import；AutoModel.from_pretrained(..., dtype=torch.float16)；get_image_features(pixel_values=...).pooler_output → (B, 1152)；参数名是 dtype= 不是 torch_dtype=。
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scorer.py —— SCORE-01 SigLIP 中段帧轨迹相似度模块</name>
  <files>analysis/roundtrip/scorer.py</files>
  <read_first>
  - .planning/phases/21-scorer-threshold-calibration/21-PATTERNS.md §analysis/roundtrip/scorer.py A-G（模块骨架/cache/ffmpeg fail-loud/sidecar merge/degrade gate/三层 parent off-by-one/GPU 选择）
  - .planning/phases/21-scorer-threshold-calibration/21-RESEARCH.md §Pattern 1（探针实测 SigLIP 加载代码）+ §Code Examples「scorer cache 元数据形状」+ §Common Pitfalls 1/2/3/4
  - analysis/roundtrip/h3_regen.py L387-417（extract_endpoint_frames：先删旧帧 + rc/存在/大小三重 fail-loud）、L591-671（cache key + _atomic_write_json）、L770-824（_iter_sidecar_errors + write_roundtrip_sidecar 的 kept-keys merge + 两层自校验）、L1233-1239（degrade gate 形状）
  </read_first>
  <behavior>
  - frame_ts(dur, j, n=8, guard=0.2)：ts = dur·(0.25 + 0.5·j/(n-1))；返回 min(ts, max(dur-guard, 0.0))。不变式：n=8 时 t_pct 序列 = 25.0, 32.14…, 75.0（步长 50/7），t=0/t=end 结构性不可达；dur=0.5 时 j=7 clamp 到 0.3
  - 帧提取对每镜 16 帧（orig 8 + regen 8）：orig 侧 ts = shot.start_sec + frame_ts(end-start, j)；regen 侧 ts = frame_ts(regen.duration_sec, j)；dest 先删再提；ffmpeg rc!=0 / 文件不存在 / 大小为 0 → RuntimeError 含 stderr 摘录（WR-05 形状）
  - SigLIP fp16 离线加载（HF_HUB_OFFLINE=1 + TRANSFORMERS_OFFLINE=1 在 import transformers 前设好）；get_image_features(...).pooler_output 取 (B,1152)；batch=每镜 16 帧一次前向；L2 归一后 per-position cosine → 8 元数组 → mean 即 score（round 4 位）
  - cache key = {video_content_hash, regen_mp4_sha256_16, model, n_frames, window}；regen_mp4_sha256_16 = regen mp4 sha256 前 16 hex——896×512 与 1344×768 regen 天然分离（Pitfall 7）
  - cache 文件含 frames.orig/regen 各 8 条 {j, t_pct, t_sec, path}（SC2 审计硬要求）+ per_position_cos + score + device + scored_at
  - sidecar merge：scores 子对象浅合并（只 update midframe_sim，绝不整体替换 scores——不丢 judge 半边）；kept-keys 除 scores 外全保留（verdict/regen/status 原样）；写前两层自校验 mirror h3_regen WR-04（本批坏 fail-loud；预存坏剔除 + .bak-<ts> 备份 + str warning）
  - degrade：SigLIP 加载抛任何异常 → append_roundtrip_warnings {code: "scorer_model_missing", detail: 异常类型+消息} + return 0，sidecar 不动，绝不 sys.exit 炸批
  - 设备：--device 默认 cuda:0（GPU0 3060Ti 零竞争，Pitfall 4）；加载/OOM 失败 → 自动降 cpu 重试（日志可见），不碰 GPU1
  </behavior>
  <action>
  新建 analysis/roundtrip/scorer.py（中文五段式 docstring：背景/算法步骤/degrade 语义/cache 惯例/argv 用法；STEP_TAG="[roundtrip]"；SCHEMA_PATH 三层 parent parent.parent.parent——PATTERNS §F off-by-one）。
  常量：MODEL_ID="google/siglip-so400m-patch14-384"、MODEL_LABEL="siglip-so400m-patch14-384"（sidecar model 字段与 cache key 用）、N_FRAMES=8、WINDOW_PCT=(25.0, 75.0)、ENDPOINT_GUARD_SEC=0.2、SCORER_CACHE_SUBDIR="route_cache/scorer"、FRAMES_SUBDIR="route_cache/scorer/frames"。
  h3_regen 共享件经 importlib.util.spec_from_file_location 加载为 h3s（spec 名 "h3_regen_shared"，mirror h3_regen.py L708-712 加载 export_asset 的方式），用 h3s.append_roundtrip_warnings / h3s._atomic_write_json / h3s.probe_duration_sec / h3s._iter_sidecar_errors / h3s.gpu_mem_mib；绝不复制字面量实现。
  函数群（全部无 OOP，mirror repo 惯例）：
  - frame_ts(duration_sec, j, n=N_FRAMES, guard=ENDPOINT_GUARD_SEC) → float
  - plan_frames(duration_sec, n) → list[{j, t_pct, t_sec}]（t_pct = (0.25 + 0.5·j/(n-1))·100）
  - extract_frame(video, ts_sec, dest) → None：先 os.unlink(dest)（isfile 守卫）、subprocess.run list-form ["ffmpeg","-y","-ss",f"{ts:.3f}","-i",video,"-frames:v","1","-q:v","2",dest,"-loglevel","error"]，capture_output=True, timeout=60；rc/存在/大小三重检查 fail-loud（_stderr_snip mirror h3_regen L378-384）
  - regen_sha16(path) → str：分块读 sha256 前 16 hex
  - load_siglip(device) → (model, processor, device)：函数内先 os.environ.setdefault 两离线开关再 import transformers；AutoModel.from_pretrained(MODEL_ID, dtype=torch.float16)（cpu 时 float32）；cuda 失败/OOM 捕获后降 cpu 重试一次并 print 降级日志；返回对象供 embed_frames 使用。延迟 import 使单测可 monkeypatch 此函数整体
  - embed_frames(model, processor, device, frames: list[PIL.Image]) → numpy (B,1152) L2 归一：proc(images=frames, return_tensors="pt")["pixel_values"] → .to(device)（cuda 时 .half()）→ model.get_image_features(pixel_values=pv).pooler_output → .float().cpu().numpy() → L2 归一
  - score_shot(...) → dict：16 帧提取 + embed + 前 8/后 8 切分 + per-position cosine（逐位 dot）+ mean；返回 cache 完整 payload（frames 清单带 path、per_position_cos、score、model=MODEL_LABEL、device、scored_at UTC ISO）
  - write_scores_sidecar(work_dir, entries) → list[str]：READ roundtrip.json → merged kept-keys 除 "scores" 全保留 → merged_scores = dict(prev.get("scores") or {}) 后 update 新 midframe_sim 子对象 → 写前 h3s._iter_sidecar_errors 两层自校验（本批坏 raise SystemExit；预存坏剔除 + 备份 .bak-<ts> + str warning）→ h3s._atomic_write_json（mirror h3_regen L805-863 逐行形状）
  - main(argv)：argparse kebab-case 中文 help——--work-dir（required）、--device（default "cuda:0"）、--shots（default ""，逗号分隔 int 子集，空=全部有 regen 的镜）。流程：读 roundtrip.json 取有 regen.path 的条目（status/failed 条目跳过 + str warning）→ --shots 过滤 → 逐镜 cache_read（key 字段全等才 hit，缺席/损坏/字段不等一律 miss 绝不部分复用）→ miss 则提取+打分+cache_write → 汇总 print [roundtrip] 行（hit/miss 计数）→ 有新分数则 write_scores_sidecar；SigLIP 加载仅在存在 miss 时发生（全 hit 零加载）；load_siglip 异常 → scorer_model_missing degrade return 0
  cache_read/cache_write：文件 = SCORER_CACHE_SUBDIR/shot_%03d.json；hit 判定 = key 五字段（video_content_hash, regen_mp4_sha256_16, model, n_frames, window）与 payload 内字段全等。
  </action>
  <verify>
    <automated>python3 -m py_compile analysis/roundtrip/scorer.py &amp;&amp; python3 -c "
import importlib.util
spec = importlib.util.spec_from_file_location('scorer', 'analysis/roundtrip/scorer.py')
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
f = m.frame_ts
# 窗口数学锚点：dur=6.73, n=8
ts = [round(f(6.73, j), 4) for j in range(8)]
assert abs(ts[0] - 1.6825) &lt; 1e-3 and abs(ts[7] - 5.0475) &lt; 1e-3, ts
assert all(6.73*0.25 - 1e-9 &lt;= t &lt;= 6.73*0.75 + 1e-9 for t in ts)
# 端点 clamp：短镜
assert abs(f(0.5, 7) - 0.3) &lt; 1e-9          # min(0.375, 0.5-0.2)
assert f(0.15, 7) &gt;= 0.0                      # max(dur-0.2, 0) 下限
p = m.plan_frames(6.73, 8)
assert [e['t_pct'] for e in p][0] == 25.0 and [e['t_pct'] for e in p][-1] == 75.0
src = open('analysis/roundtrip/scorer.py', encoding='utf-8').read()
assert 'pooler_output' in src and 'HF_HUB_OFFLINE' in src
assert 'scorer_model_missing' in src and 'spec_from_file_location' in src
assert 'torch_dtype' not in src               # transformers 5.x 参数名
assert src.count('-frames:v') &gt;= 1
print('21-01 T1 OK')
"</automated>
  </verify>
  <acceptance_criteria>
  - frame_ts 锚点值过（1.6825/5.0475；短镜 clamp 0.3）；t_pct 序列 25→75，t=0/t=end 结构性不可达
  - pooler_output / HF_HUB_OFFLINE / dtype=（torch_dtype 绝不出现）/ scorer_model_missing / importlib 加载 h3_regen 全部在源码
  - ffmpeg 全 list-form；dest 先删后提；三重 fail-loud
  - cache key 五字段含 regen_mp4_sha256_16（分辨率/引擎身份联动）
  </acceptance_criteria>
  <done>SCORE-01 离线半边：帧窗数学、SigLIP 加载（5.x API）、cache 审计清单、scores 浅合并、degrade 全部落地（单测在 Task 3）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: judge.py —— SCORE-02 三分类归因 + --apply-verdict 冻结应用器</name>
  <files>analysis/roundtrip/judge.py</files>
  <read_first>
  - .planning/phases/21-scorer-threshold-calibration/21-PATTERNS.md §analysis/roundtrip/judge.py A-F（import 纪律深一层/生命周期/observe_single 调用面/解析容错/verdict 应用器/grid 拼图）+ §Shared Patterns（STEP_TAG / Import 纪律 / 原子写 / 两层自校验 / GPU 串行链）
  - .planning/phases/21-scorer-threshold-calibration/21-RESEARCH.md §Pattern 2（parse_judge_answer 探针 5/5 实测）、§Pattern 3（2×4 grid 标注设计）、§Pattern 4（verdict 冻结 merge 骨架）、§Common Pitfalls 3/4/6/8
  - analysis/engine_clients/qwen_eye_client.py L176-243（ensure_ready/stop_if_owned 生命周期）、L273-318（_call_llm 契约 + observe_single）
  - analysis/vision_seq_facets.py L564-567（全 cache 命中零实例化预判）+ L631-668（try/finally 生命周期形状）
  - analysis/roundtrip/h3_regen.py L1005-1011（comfy_free payload）
  </read_first>
  <behavior>
  - grid：4 时位 {0, 1/3, 2/3, 1} ×2 侧（orig 左 / regen 右）→ 8 帧提取（两端 clamp min(ts, max(dur-0.2, 0))——t=100% 帧同理，Pitfall 3）→ PIL canvas 1370×1476（cell 640×360）→ 列头 "ORIGINAL"（#58a6ff）/ "REGEN (h3)"（#3fb950）+ 行标签 t=0%/33%/66%/100% 竖排进图 → 存 roundtrip/_judge_grids/shot_XXX.jpg
  - 提示词含三分类定义逐字（prompt_faithful = prompt 描述了X且渲染出X / model_diverged = 描述了X渲染成Y（prompt 好）/ prompt_underspecified = 欠约束，h3 自行脑补）+ JSON 模板 {"attribution": …, "confidence": …, "reason": …} + 指令「reason 必须引用 prompt 原文具体短语作证据、以 t=33%/66% 中段行为为主要证据」+ 本镜 prompt_text（prompt_text 只进 HTTP JSON body，绝不进 argv）
  - parse_judge_answer：fence 剥离 → 花括号截取 → json.loads → enum 三值闭集 / confidence ∈[0,1] / reason ≥10 字符 → (obj|None, code)；写 sidecar 前 reason 截 2000（schema T-18-02 上界）
  - 失败重问 ≤2 次，每次把错误码+消息回喂进 question（retry-with-feedback）；attempts 全记录进 cache
  - 引擎编排：全 cache 命中 → 零实例化；有 miss → comfy_free(comfy_url) best-effort（腾 GPU1，Pitfall 4）→ QwenEye() → ensure_ready 返回 (False, ·) → 等 ≥30s 显式重试一次（Pitfall 6，120s 贴上限）→ 仍败 → str warning + rc=0 degrade；try/finally stop_if_owned（13.4GB lease 永不泄漏）
  - --apply-verdict --tau-sim τ：读现存 roundtrip.json（不重跑引擎）——verdict 已存在的镜跳过（冻结：auto/human 一视同仁）；双信号齐备的镜 accepted ⇔ midframe_sim.score ≥ τ ∧ judge.attribution == "prompt_faithful"（硬合取，无 confidence 第二门槛）；verdict = {decision, source: "auto", decided_at: UTC ISO}；信号缺一的镜跳过 + str warning 列 shot_id
  - --apply-verdict 的 merge = Pattern 4 冻结骨架：kept-keys 除 scores 全保留 + 新条目 verdict 先 pop（预存即冻结）+ scores 子对象浅合并（保 midframe_sim 不丢、保 judge 不丢）；写前两层自校验同 Task 1
  - --summarize：读 sidecar → (shot_id, sim, attribution, confidence) 表 → sim 分位数 p10/25/50/75/90 + attribution 三桶计数 + τ 候选预演（各候选下 accepted/rejected 计数 + rejected 按归因分桶）→ print 可粘贴进报告的文本块
  </behavior>
  <action>
  新建 analysis/roundtrip/judge.py（中文五段式 docstring；STEP_TAG="[roundtrip]"）。
  import 纪律：sys.path.insert(0, str(Path(__file__).resolve().parent.parent)) 后 from engine_clients.qwen_eye_client import ENGINE_NAME, ENGINE_VERSION, QwenEye（深一层 off-by-one——PATTERNS §A）；h3_regen 共享件 importlib 加载为 h3s（同 Task 1 的 spec 名可复用 "h3_regen_shared"），用 h3s.append_roundtrip_warnings / h3s._atomic_write_json / h3s.comfy_free / h3s.probe_duration_sec / h3s._iter_sidecar_errors / h3s.COMFY_URL_DEFAULT。
  常量：GRID_T_PCT=(0.0, 1/3, 2/3, 1.0)、GRID_ROW_LABELS=("t=0%","t=33%","t=66%","t=100%")、CELL_W=640、CELL_H=360、HDR_H 与 ROWLBL_W 按 RESEARCH Pattern 3 探针值（canvas 总尺寸 1370×1476）、COL_ORIG=(88,166,255)、COL_REGEN=(63,185,80)、BG=(13,17,23)、JUDGE_CACHE_SUBDIR="route_cache/judge"、GRIDS_SUBDIR="roundtrip/_judge_grids"、ATTRIBUTIONS=("prompt_faithful","model_diverged","prompt_underspecified")。
  函数群：
  - grid_ts(duration_sec) → list[float]：四时位 ts=dur·pct，clamp min(ts, max(dur-ENDPOINT_GUARD_SEC, 0.0))（guard 常量与 scorer 同值 0.2，本地声明不 import scorer——两模块不互相 import，PATTERNS 惯例）
  - build_grid(orig_frames: list[Path], regen_frames: list[Path], dest: str) → (w, h)：Image.new RGB canvas + ImageDraw 列头/行标签（默认 PIL 位图字体即可，标签必须进图）+ paste 8 cell + save JPEG quality 90
  - build_question(prompt_text: str, parse_err: str | None) → str：三分类定义逐字 + JSON 模板 + reason 证据指令 + prompt_text + （parse_err 非空时）「上一次回答无效：{parse_err}，请严格只输出符合模板的 JSON」
  - parse_judge_answer(txt: str) → (dict | None, str)：逐字落地 RESEARCH Pattern 2 探针函数（no-brace / json:<msg> / enum / conf-range / reason-short / ok 六码）
  - run_judge_shot(engine, grid_path, prompt_text) → (parsed | None, attempts: list[dict])：最多 1+2 次调用 observe_single（max_tokens 2000），每次 parse 后记 {parse: code, raw_len}；成功即停
  - write_judge_sidecar(work_dir, entries) → list[str]：scores 浅合并只 update judge 子对象（mirror Task 1 的 write_scores_sidecar 结构，reason 先 str()[:2000]）
  - apply_verdict(work_dir, tau_sim: float) → dict：读 sidecar → 逐条判定（behavior 规则）→ Pattern 4 冻结 merge → 两层自校验 + h3s._iter_sidecar_errors → 原子写 → 返回 {applied, frozen, skipped_missing: [ids], accepted, rejected}
  - summarize_scores(entries) → dict：{n, quantiles: {p10..p90}, buckets: {faithful/diverged/underspecified}, tau_preview: [{tau, accepted, rejected, rejected_by_bucket}]}（quantile 用线性插值 statistics.quantiles(data, n=100) 或手写同义实现——19 值小样本直接排序索引即可，写明方法）
  - main(argv)：argparse——--work-dir（required）、--comfy-url（default h3s.COMFY_URL_DEFAULT）、--shots（default ""）、--apply-verdict（store_true 模式开关）、--tau-sim（float，--apply-verdict 时 required，absent 则中文报错退出）、--summarize（store_true，只读打印不写盘）。普通模式 = judge 批：条目选择同 scorer（有 regen.path 的条目）→ cache 判定（key = {video_content_hash, regen_mp4_sha256_16, engine_name, engine_version}，parsed 缺席也 miss）→ miss 镜提取 8 帧 + build_grid + run_judge_shot → cache_write（含 grid{path,w,h} + attempts + parsed + judged_at）→ write_judge_sidecar；needs_engine = 存在 miss，编排按 behavior（comfy_free → ensure_ready → (False) 时 time.sleep(30) 重试一次 → 仍败 degrade str warning + return 0；finally stop_if_owned 仅在实例化过时）；--apply-verdict 模式跳过全部引擎路径直接 apply_verdict + print 汇总
  reason 截断与 attempts 审计是 cache 与 sidecar 双写面（cache 存 raw_len，sidecar 存截断后 reason）。
  </action>
  <verify>
    <automated>python3 -m py_compile analysis/roundtrip/judge.py &amp;&amp; python3 -c "
import importlib.util
spec = importlib.util.spec_from_file_location('judge', 'analysis/roundtrip/judge.py')
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
ok, code = m.parse_judge_answer('{\"attribution\": \"prompt_faithful\", \"confidence\": 0.9, \"reason\": \"prompt 说 X 且 REGEN 渲染出 X\"}')
assert code == 'ok' and ok['attribution'] == 'prompt_faithful'
_, c1 = m.parse_judge_answer('前言\`\`\`json\n{\"attribution\": \"bad\", \"confidence\": 0.5, \"reason\": \"aaaaaaaaaa\"}\n\`\`\`后记')
assert c1 == 'enum', c1
_, c2 = m.parse_judge_answer('{\"attribution\": \"model_diverged\", \"confidence\": 1.5, \"reason\": \"aaaaaaaaaa\"}')
assert c2 == 'conf-range', c2
_, c3 = m.parse_judge_answer('{\"attribution\": \"model_diverged\", \"confidence\": 0.5, \"reason\": \"短\"}')
assert c3 == 'reason-short', c3
_, c4 = m.parse_judge_answer('完全不是 JSON')
assert c4 == 'no-brace', c4
gt = m.grid_ts(7.2917)
assert abs(gt[3] - 7.0917) &lt; 1e-6, gt   # t=100% clamp dur-0.2
src = open('analysis/roundtrip/judge.py', encoding='utf-8').read()
assert 'from engine_clients.qwen_eye_client import' in src
assert 'parent.parent' in src                     # 深一层 import 纪律
assert 'prompt_underspecified' in src and 'prompt_faithful' in src
assert 'apply-verdict' in src and 'tau-sim' in src and 'summarize' in src
assert 'stop_if_owned' in src and 'comfy_free' in src
assert '[:2000]' in src                            # reason 截断
print('21-01 T2 OK')
"</automated>
  </verify>
  <acceptance_criteria>
  - parse_judge_answer 六码矩阵锚点全过（干净/fence+enum/conf 越界/reason 短/no-brace）
  - grid_ts 的 t=100% 在 dur=7.2917 上 clamp 到 7.0917（Pitfall 3 实测值的机器锚）
  - engine_clients import 走 parent.parent 深一层；--apply-verdict/--tau-sim/--summarize CLI 齐
  - reason [:2000] 截断、stop_if_owned + comfy_free 生命周期、冻结 merge 骨架在源码
  </acceptance_criteria>
  <done>SCORE-02 归因半边 + SCORE-03/DATASET-01 应用器与汇总器落地（单测在 Task 3）。</done>
</task>

<task type="auto">
  <name>Task 3: 离线单测 —— FakeSigLIP/FakeEye + 帧窗 + 解析矩阵 + 冻结幂等</name>
  <files>tests/test_scorer.py, tests/test_judge.py</files>
  <read_first>
  - tests/test_h3_regen.py L61-95（importlib 加载被测模块 + FakeHTTP + FakeClock）、L153-242（make_workdir fixture + patch_pipeline 分发器——cmd[0] 分发 ffmpeg/nvidia-smi/ffprobe）、L830-907（read_sidecar/validate_sidecar/sidecar_result helpers + merge 保留测试模板）、L927-957（预存坏条目剔除模板）
  - 本 plan Task 1/2 交付的 scorer.py / judge.py（被测面）
  </read_first>
  <behavior>
  - test_scorer.py：帧窗锚点（dur=6.73 t 序列 + dur=0.5 clamp 0.3 + t=0/t=end 结构性不可达）；FakeSigLIP 固定向量（orig_j = e_j，regen_j = 已知角度变换）→ per_position_cos 与 mean 精确断言（tol 1e-6）；cache miss/hit（不同 regen sha16 二跑必重打分、同 key 二跑零加载——monkeypatch load_siglip 计数）；cache 清单字段齐（frames.orig 8 条 {j,t_pct,t_sec,path} + per_position_cos + score + model）；sidecar 浅合并（预存 scores.judge + verdict → scorer 写后两者原样、midframe_sim 更新）；scorer_model_missing degrade（load_siglip 抛 → rc 0 + dict warning + sidecar 不动）；ffmpeg fail-loud（fake_run rc=1 → RuntimeError；dest 预置旧文件先被删）
  - test_judge.py：解析矩阵 7 用例（干净 / fence 包裹 / 前后散文 / 尾逗号坏 JSON→json: 码 / 非法 enum / conf 越界 / reason 过短）；FakeEye（observe_single 回放预设文本序列）重问——首答坏次答好 → attempts len 2 + 第二问含错误回喂标记；三连坏 → parsed None + str warning + rc 0；grid 纯 PIL 布局（dest 尺寸 (1370,1476) + 列头区域采样像素 ≠ BG 色）；全 cache 命中零实例化（monkeypatch QwenEye 构造器 raise，main 仍 rc 0）；--apply-verdict 硬合取三态（0.95+faithful→accepted；0.95+model_diverged→rejected；0.80+faithful@τ=0.9→rejected）；冻结幂等（预存 rejected verdict + τ 变为会 accept → 原样；缺 verdict 镜补写）；信号缺一跳过 + warning 列 shot_id；Pitfall 8 双向用例（--apply-verdict 后跑 h3s.write_roundtrip_sidecar 重写 regen 半边 → verdict + scores 原样）；预存坏条目剔除 + .bak 备份；schema 全字段条目（regen+scores 双半+verdict）过 h3s._iter_sidecar_errors == []；summarize_scores 已知 19 值 → 精确分位数 + 桶计数 + τ 预演计数
  </behavior>
  <action>
  新建 tests/test_scorer.py + tests/test_judge.py（平铺 tests/ 惯例；importlib 文件位置加载被测模块；fake 基建从 test_h3_regen.py 复制改名——make_workdir 需扩展：预置 roundtrip/shot_XXX_regen.mp4 假字节文件 + roundtrip.json 预存条目 + 可选 prompts.json prompt_text）。
  分发器扩展：fake subprocess.run 按 cmd[0]=="ffmpeg" 分支写假 jpg 字节到 cmd 输出路径（从 argv 提取 dest）；monkeypatch time.sleep → no-op；monkeypatch scorer.load_siglip / judge 模块的 QwenEye 符号（FakeEye 类注入 judge 模块命名空间）。
  FakeSigLIP：embed_frames 的替身——monkeypatch scorer.embed_frames 整体，返回按输入帧路径序构造的固定 numpy 向量（用路径里的 side/j 标记区分 orig/regen 与位置，返回对应预置单位向量；regen_j = orig_j 旋转已知 cos 的向量——例如 regen_j = cos(θ_j)·e_j + sin(θ_j)·u，θ_j 预置 → 期望 per-position cos == cos(θ_j)）。
  FakeEye：__init__/ensure_ready→(True, True)/stop_if_owned 记录调用/observe_single pop 预设答案序列（类属性 answers 列表）。
  verdict 用例走 run_main 形状（临时 sys.argv 注入 main()），断言 rc 与 sidecar JSON 内容；.bak 备份断言用 glob("*.bak-*")。
  summarize 用例：手写 19 个 (sim, attribution) 对（覆盖三桶 + 已知排序），断言 quantiles/buckets/tau_preview 逐值相等。
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_scorer.py tests/test_judge.py -q &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - 两文件合计 ≥20 个新用例全绿；全套件 tests/ 绿（基线 119 passed 只增不减），总耗时 <20s（sleep 已 patch）
  - 帧窗数学 / clamp / 解析六码 / 硬合取三态 / 冻结幂等 / Pitfall 8 双向 / schema gate 各有专门用例（RESEARCH Validation 表逐行对应）
  - FakeEye 重问用例断言 attempts 审计与错误回喂；FakeSigLIP 用例断言数值精确（非仅"有值"）
  </acceptance_criteria>
  <done>SCORE-01/02/DATASET-01 离线证据闭合：探针验证过的数学与容错全部有回归锚，21-02 真 GPU 跑前可全量本地验证。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 模型输出文本（judge reason / SigLIP 无关） → sidecar/未来 HTML | qwen-eye 产出 NL 文本跨入持久层（Phase 22 会再入 HTML 渲染） |
| prompt_text / 视频路径 → 子进程与 HTTP | 自家 JSON 数据进入 ffmpeg argv（list-form）与 qwen-eye HTTP body |
| SigLIP 权重 → 进程内存 | 盘上 HF cache 模型文件（离线加载，无网络面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | judge.reason 模型文本入 sidecar | mitigate | schema maxLength 2000 上界（T-18-02 契约已立）+ 写前 str()[:2000] 截断；HTML _esc() 留 Phase 22 PRESENT-01 |
| T-21-02 | Injection | prompt_text/路径经 subprocess | mitigate | ffmpeg/ffprobe 全 list-form；-ss 数值只来自自家 shots.json/ffprobe；prompt_text 只进 HTTP JSON body 绝不进 argv（T-20-01 延续） |
| T-21-03 | Spoofing | grid 列序/标签误导 judge | mitigate | 列序固定 orig 左 regen 右 + 标签进图（ImageDraw）+ Task 3 布局单测（尺寸 + 列头像素采样断言） |
| T-21-04 | Supply Chain | SigLIP 权重来源 | mitigate | HF_HUB_OFFLINE=1 + TRANSFORMERS_OFFLINE=1 离线加载（只读盘 cache，无网络取包面）；权重已由 research 经 hf-mirror 补全并校验 |
| T-21-05 | Repudiation | 打分数据可信度 | mitigate | 帧时间戳清单进 cache（SC2 审计可回放）+ model 字段强制（schema）+ per_position_cos 留档 |
| T-21-SC | Tampering | 包安装 | accept | 本 plan 零新包（transformers/torch/Pillow/jsonschema/matplotlib 全在位实测）；无 npm/pip install 步骤 |
</threat_model>

<verification>
- python3 -m pytest tests/ -q 全绿（119 基线 + ≥20 新增）
- 两个 python -c 锚点脚本过（帧窗数学 / 解析矩阵 / clamp / CLI 面）
- grep：pooler_output、HF_HUB_OFFLINE、scorer_model_missing、spec_from_file_location、apply-verdict、stop_if_owned、[:2000] 均在位
- repo 红线：本 plan 不改 h3_regen.py / 不动 spec/——git status 只有 4 个新文件
</verification>

<success_criteria>
- SCORE-01 离线半边：25%-75% 时窗 + t=0/t=end 结构性排除 + 帧清单审计 cache + SigLIP 5.x API + degrade——全有单测锚（SC1/SC2 代码半边）
- SCORE-02 离线半边：三分类提示词 + 结构化输出容错 ≤2 重问 + grid 标注 + 引擎生命周期——全有 Fake 替身锚（SC3 工具半边）
- DATASET-01 离线半边：verdict 冻结 merge + 硬合取 + 浅合并 + 两层自校验 + Pitfall 8 双向用例（SC5 代码半边）
- 真 GPU 跑（SC1 live / SC3 抽检 / SC4 校准）由 21-02/21-03 共享同 requirement IDs 收口
</success_criteria>

<output>
Create `.planning/phases/21-scorer-threshold-calibration/21-01-SUMMARY.md` when done
</output>
