---
phase: 21-scorer-threshold-calibration
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - .planning/research/roundtrip-threshold-calibration.md
  - .planning/PROJECT.md
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip.json"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/route_cache/scorer/"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/route_cache/judge/"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip/_judge_grids/"
autonomous: false
requirements: [SCORE-03, DATASET-01]
must_haves:
  truths:
  - "19 镜 @1344×768 双信号全量落 roundtrip.json（19 条 scores.midframe_sim + scores.judge），全量 schema 校验 0 errors"
  - "校准报告草稿含：分位数表（p10/25/50/75/90）+ attribution 三桶分布 + τ 候选预演（各候选 accepted/rejected + rejected 按归因分桶）+ 反循环论证声明（τ 只能由 Kai 看分布裁决，机器指标仅参考）"
  - "Kai 裁决 τ_sim + 抽检 5 镜 judge 归因（一致率 ≥4/5 可接受；不一致镜进报告）——同一 checkpoint 完成（SC3+SC4）"
  - "--apply-verdict --tau-sim <锁定值> 后：全部双信号齐备镜有 verdict{decision, source:auto, decided_at}；accepted ⇔ sim ≥ τ ∧ attribution == prompt_faithful 硬合取"
  - "重跑 --apply-verdict：roundtrip.json byte 级不变（冻结幂等机器证明，SC5——rejected 永不删除/永不翻转）"
  - "PROJECT.md Key Decisions 新增 τ_sim 决策行（值 + 双门槛定义 + decided_at + 证据指针）；报告终稿含 rejected 占比按归因分桶（SC4 防偏向审计）"
  artifacts:
    - path: ".planning/research/roundtrip-threshold-calibration.md"
      provides: "SCORE-03 校准报告（草稿→Kai 裁决→终稿）"
      min_lines: 120
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions 表新增 τ_sim 锁定行"
      contains: "tau"
  key_links:
    - from: "19 镜双信号（roundtrip.json scores）"
      to: ".planning/research/roundtrip-threshold-calibration.md"
      via: "judge.py --summarize 分位数/分桶/τ 预演 + 散点呈现"
      pattern: "tau"
    - from: "checkpoint 裁决值 τ_sim"
      to: "roundtrip.json verdict 半边"
      via: "judge.py --apply-verdict --tau-sim（硬合取 + 冻结 merge）"
      pattern: "apply-verdict"
    - from: "裁决记录"
      to: ".planning/PROJECT.md Key Decisions"
      via: "append-only 追加行（mirror 表内既有行格式）"
      pattern: "decided_at"
---

<objective>
收口 Phase 21：overnight 批完成后对 19 镜 @1344×768 全量跑 scorer + judge → 汇编双信号分布材料（分位数表 + attribution 分桶 + τ 候选预演）→ 校准报告草稿 → 【checkpoint】Kai 裁决 τ_sim + 抽检 5 镜归因一致率 → τ 锁定 → --apply-verdict 写 verdict（冻结 merge）→ PROJECT.md Key Decisions 行 + 报告终稿（rejected 占比按归因分桶审计）+ 幂等重跑证明。

Purpose: SCORE-03（ep01 实测分布 → 双门槛锁定 + rejected 占比可审计）+ DATASET-01（verdict 合并写 + rejected 永不删除）+ SC3 人工抽检（归因不是信口开河）。verdict 本 phase 全部 source:"auto"（HITL 覆盖是 Phase 22 PRESENT-01）。
Output: 校准报告终稿 + PROJECT.md 决策行 + 19 镜 verdict 落 roundtrip.json（schema 合法）。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-scorer-threshold-calibration/21-CONTEXT.md
@.planning/phases/21-scorer-threshold-calibration/21-RESEARCH.md
@.planning/phases/21-scorer-threshold-calibration/21-PATTERNS.md
@.planning/phases/21-scorer-threshold-calibration/21-01-SUMMARY.md
@.planning/phases/21-scorer-threshold-calibration/21-02-SUMMARY.md
@.planning/research/vision-seq-spike-report.md
</context>

<interfaces>
<!-- 前置交付物 + 校准流程锚点 -->

工具面（21-01 落地，21-02 真机验证）：
- python3 analysis/roundtrip/judge.py --work-dir "$EP01" --summarize → 分位数 + 三桶 + τ 预演文本块（可直接粘报告）
- python3 analysis/roundtrip/judge.py --work-dir "$EP01" --apply-verdict --tau-sim <τ> → {applied, frozen, skipped_missing, accepted, rejected} 汇总
- scorer/judge 全量跑 = 不带 --shots（自动处理全部有 regen 的条目；1344×768 regen sha 变化 → cache 自然 miss 重打分，896×512 smoke cache 留档无害）

EP01 = output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。
批产物预期（21-02 SUMMARY 载 pidfile/日志路径）：roundtrip/shot_XXX_regen.mp4 ×19 @1344×768 + sidecar regen 半边（shots 1/47 被覆盖为新分辨率，scores 保留——Pitfall 7/8）；uniform-20 锚点 = [1,5,10,14,19,24,28,33,38,42,47,52,56,61,66,70,75,80,84,89] 减 shot 70 = 19 镜。

校准报告结构（PATTERNS §校准报告 映射表，analog = vision-seq-spike-report.md）：
头部元数据（19 镜清单 + 批时段 + raw 零改写声明）→ 烧录实录（批耗时 + scorer/judge 计时）→ Methodology（双门槛定义 + **反循环论证声明**——mirror vision-seq L63-68 措辞姿态）→ 机器可见观察（分位数表 + per-position 分布 + attribution 分桶——区分度不足时如实呈现，Pitfall 5/A4）→ Evidence（19 镜表 + 抽检 5 镜 grid 路径指针）→ Recommendations/裁决记录（τ 值 + 理由 + 抽检一致率 + rejected 分桶占比）→ Reproducibility（argv 全文 + cache 全命中重跑秒级证明）。

抽检 5 镜确定性选择规则（stratified）：按 attribution 桶分层取 sim 极值——prompt_faithful 桶 sim 最高 1 镜 + 最低 1 镜；model_diverged 桶 sim 中位 1 镜；prompt_underspecified 桶 sim 中位 1 镜；第 5 镜 = 全体 sim 中位镜。空桶顺延取全量 sim 次高/次低镜（规则优先级写进报告，不临场挑）。

PROJECT.md Key Decisions 行格式（PATTERNS §PROJECT.md）：| **决策标题** (Row N) | 理由 | 结论 + *(decided_at: YYYY-MM-DD; phase: 21; evidence: .planning/research/roundtrip-threshold-calibration.md#节锚)* |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 批完成确认 + 19 镜全量双信号 + 分布材料 + 校准报告草稿</name>
  <files>.planning/research/roundtrip-threshold-calibration.md</files>
  <read_first>
  - 21-02-SUMMARY.md（pidfile/日志路径/启动时间戳/预期完成窗口）
  - .planning/phases/21-scorer-threshold-calibration/21-RESEARCH.md §Common Pitfalls 5（SigLIP 窄带——τ 不能拍脑袋 + 区分度不足处置）+ §Assumptions A3/A4 + §Validation Architecture「校准 checkpoint 流程」
  - .planning/research/vision-seq-spike-report.md L1-100（报告结构 + 反循环论证声明措辞先例）
  </read_first>
  <behavior>
  - 批完成判定：pid 不存活 + 日志有批收尾行 + 19 个 @1344×768 regen mp4 在盘 + sidecar 19-20 条 regen 半边；提前死 → 读日志归因后重启批（断点续跑）再轮询（10min 间隔 sleep，批可能已在 overnight 完成——先查再等）
  - 全量 scorer（GPU0，19 镜 <5min 量级）+ 全量 judge（/free + 引擎 120s + 19 次调用）→ sidecar 19 条 scores 双半边 + 全量 schema 0 errors
  - 分布材料：--summarize 输出 + 19 镜 (sim, attribution) 排序表 + 散点（matplotlib PNG 可选，fallback markdown 表 + 文本直方图——RESEARCH Environment 表裁决）；per-position 分布纳入（区分度不足时的诊断面）
  - 报告草稿：按 interfaces 结构映射七节；τ 候选 = 分位数表关键档位（如 p25/p50/p75 + 中位±观察）各配 accepted/rejected 预演 + rejected 按归因分桶——**不预设结论**，供 checkpoint 裁决
  - 抽检 5 镜按 interfaces 确定性规则选出，grid 绝对路径 + sidecar reason 摘录进报告 Evidence 节
  </behavior>
  <action>
  1. 批检查：读 21-02-SUMMARY 的 pidfile/日志路径；kill -0 判活 → 活则 tail 日志看进度（每镜渲后 cache 写入可数），sleep 600 循环至批收尾（执行记录写明等待段）；死则核对 19 mp4 + 日志收尾行，缺件则重启批（同命令，断点续跑只补缺）。
  2. python3 analysis/roundtrip/scorer.py --work-dir "$EP01"（全量；观察 19 miss→cache 写入→sidecar merge）。
  3. python3 analysis/roundtrip/judge.py --work-dir "$EP01"（全量；引擎周期同 21-02 T1）。
  4. python3 analysis/roundtrip/judge.py --work-dir "$EP01" --summarize > /tmp/tau_summary.txt 捕获输出；python3 -c 全量 schema 断言（h3s._iter_sidecar_errors == []）+ 19 条双半边计数断言。
  5. 按确定性规则选 5 镜（python3 -c 读 sidecar 实现，规则代码进执行记录）。
  6. 写 .planning/research/roundtrip-threshold-calibration.md 草稿（七节；头部标注 status: DRAFT — 待 Kai 裁决；Reproducibility 节含本 task 实测的 cache 全命中重跑计时——scorer/judge 各再跑一次证明秒级返回）。
  散点呈现：优先 matplotlib（scatter: x=sim, y=attribution 抖动, 颜色分桶）存 .planning/research/ 附图 PNG + 报告内 markdown 表双保险；若 matplotlib 环境异常直接 fallback 文本直方图（不阻塞 checkpoint）。
  </action>
  <verify>
    <automated>python3 -c "
import json, importlib.util
from pathlib import Path
ep01 = Path('output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。')
d = json.load(open(ep01 / 'roundtrip.json'))
scored = [s for s in d['shots'] if s.get('scores', {}).get('midframe_sim') and s.get('scores', {}).get('judge')]
assert len(scored) == 19, f'expected 19 fully-scored shots, got {len(scored)}'
assert all(s['regen']['engine_version'].endswith('1344x768') for s in scored)
spec = importlib.util.spec_from_file_location('h3r', 'analysis/roundtrip/h3_regen.py')
h3s = importlib.util.module_from_spec(spec); spec.loader.exec_module(h3s)
assert list(h3s._iter_sidecar_errors(d)) == []
rep = Path('.planning/research/roundtrip-threshold-calibration.md')
src = rep.read_text(encoding='utf-8')
for tok in ('分位', 'prompt_faithful', 'model_diverged', 'prompt_underspecified', 'DRAFT', 'τ'):
    assert tok in src, tok
assert len(src.splitlines()) &gt;= 120
print('21-03 T1 OK — 19 scored, report draft ready for checkpoint')
" &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - sidecar 19 条双半边全量 schema 0 errors；engine_version 全部 1344x768（校准集纯度，Pitfall 7）
  - 报告草稿 ≥120 行、七节齐、含分位数表 + 三桶 + τ 预演 + 反循环论证声明 + 5 镜 grid 路径
  - 抽检 5 镜选择可复算（规则代码留执行记录）；cache 重跑秒级证明留档
  </acceptance_criteria>
  <done>SC4 前半 + SC3 素材就位：19 镜 @1344×768 双信号实测分布成文，checkpoint 材料齐备。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Kai 裁决 checkpoint —— τ_sim 定值 + judge 归因抽检 5 镜</name>
  <files>.planning/research/roundtrip-threshold-calibration.md</files>
  <what-built>19 镜 @1344×768 双信号实测分布（校准报告草稿：分位数表 + attribution 分桶 + τ 候选预演 + rejected 分桶预演）+ 5 镜抽检素材（judge grid 图 + 原片/regen 并排路径 + judge reason）。</what-built>
  <how-to-verify>
  1. 读报告草稿 .planning/research/roundtrip-threshold-calibration.md（重点：机器可见观察节的分位数表与三桶分布；τ 候选预演表——每个候选值下 accepted/rejected 计数与 rejected 按归因分桶）。
  2. τ_sim 裁决：基于分布选定一个 τ 值并给一句理由（SigLIP 高窄带特性见报告 Methodology——直觉阈值失效是预期内的；若分布区分度不足，报告已列处置选项：接受带 caveat / 调整 N·窗口·特征层的显式决策）。
  3. judge 抽检（SC3）：对报告 Evidence 节列出的 5 镜逐一查看 grid 图（roundtrip/_judge_grids/shot_XXX.jpg 绝对路径在报告内）——自己判断该镜归因应属三分类哪类，与 judge 的 attribution 对比；可另看原片段 vs regen（output/<ep01>/roundtrip/shot_XXX_regen.mp4 vs 原片对应时窗）辅助判断。
  4. 记录：τ 值 + 理由 + 5 镜逐镜 agree/disagree（一致率 ≥4/5 可接受；不一致镜会进报告终稿，不要求返工）。
  </how-to-verify>
  <resume-signal>回复：tau=&lt;float&gt;；抽检=&lt;approved 或列出不一致镜号与你认为的正确归因&gt;；若选了区分度处置选项请注明选项号</resume-signal>
  <action>Claude 已完成全部机器侧工作（19 镜分布汇编 + 报告草稿 + 5 镜抽检素材定位——grid 图与 reason 均在盘可看）；本 task 暂停等待 Kai 读报告裁决 τ_sim 与逐镜抽检归因——裁决值（tau=&lt;float&gt;）是 Task 3 --apply-verdict --tau-sim 的唯一输入，机器不代裁（SCORE-03 的 HITL 硬门）。</action>
  <verify>
    <human-check>报告草稿的分位数表/三桶分布/τ 预演可读且与 sidecar 一致；5 镜 grid 图打开可辨列（左 ORIGINAL 右 REGEN）与行时间标签；Kai 对 τ 值给出理由、对 5 镜归因给出 agree/disagree 判定（一致率 ≥4/5 可接受）</human-check>
  </verify>
  <done>Kai 回复含 tau=&lt;float&gt; 与抽检结论（approved 或不一致镜清单）；不一致镜与理由记录进报告终稿的裁决记录节。</done>
</task>

<task type="auto">
  <name>Task 3: verdict 应用 + 幂等证明 + PROJECT.md 决策行 + 报告终稿 + rejected 分桶审计</name>
  <files>.planning/PROJECT.md, .planning/research/roundtrip-threshold-calibration.md</files>
  <read_first>
  - .planning/PROJECT.md Key Decisions 表（行格式 mirror——PATTERNS §PROJECT.md L138 实样）
  - checkpoint 裁决回复（Task 2 的 resume-signal 内容——tau 值 + 抽检结果）
  - .planning/phases/21-scorer-threshold-calibration/21-CONTEXT.md §阈值校准与 verdict 写入（双门槛硬合取 + source:"auto" + 幂等合并锁定语义）
  </read_first>
  <behavior>
  - --apply-verdict --tau-sim <裁决值>：19 镜全获 verdict（decision 按 accepted ⇔ sim ≥ τ ∧ attribution == prompt_faithful）；汇总 {applied:19, accepted:N, rejected:M} 打印进执行记录
  - 幂等机器证明（SC5）：sha256(roundtrip.json) 前后比对——再跑一次 --apply-verdict（同 τ）→ byte 级不变；frozen 计数 = 19
  - PROJECT.md Key Decisions 追加一行：τ_sim 锁定值 + 双门槛定义 + decided_at + evidence 指针（报告裁决记录节锚）
  - 报告终稿：status Final；新增/回填「裁决记录（checkpoint —— 已收口）」节（τ 值 + Kai 理由 + 抽检一致率 + 不一致镜明细）+ rejected 占比按归因分桶表（机器可 grep：attribution enum × decision 计数矩阵）+ 最终 accepted/rejected 清单
  - 审计断言：rejected 镜数 > 0 时分桶表与 sidecar 实际计数一致（python 复算）；全 rejected（=0）也如实记录（不制造人为 rejected）
  </behavior>
  <action>
  1. python3 analysis/roundtrip/judge.py --work-dir "$EP01" --apply-verdict --tau-sim <τ>（τ = Task 2 裁决值；捕获汇总 print）。
  2. 幂等证明：sha256sum "$EP01/roundtrip.json" > /tmp/rt_a；重跑同命令；sha256sum > /tmp/rt_b；diff /tmp/rt_a /tmp/rt_b 必须为空；python3 -c 断言 19 条 verdict{decision, source:"auto", decided_at 非空} + schema 0 errors。
  3. .planning/PROJECT.md Key Decisions 表 append 一行（不动既有行）：| **roundtrip accepted 双门槛 τ_sim = <τ>（SigLIP so400m midframe_sim）** (Row N) | 19 镜 @1344×768 实测分布裁决；硬合取 midframe_sim ≥ τ ∧ attribution == prompt_faithful | accepted <N> 镜 / rejected <M> 镜；source:auto（HITL 覆盖留 Phase 22）+ *(decided_at: <checkpoint 日期>; phase: 21; evidence: .planning/research/roundtrip-threshold-calibration.md#裁决记录)* |（Row N 顺延表内序号）。
  4. 报告终稿：status: DRAFT → FINAL；裁决记录节回填（τ + 理由 + 抽检逐镜结果与一致率）；新增 rejected 分桶审计表（python3 -c 复算 attribution×decision 矩阵，输出贴进报告——机器可 grep 的 enum 计数，SC4 防偏向审计的落档形态）；accepted/rejected 镜号清单。
  5. 执行记录收尾：checkpoint 回复原文、幂等 sha256 双值、最终计数。
  </action>
  <verify>
    <automated>TAU_LOCKED=&lt;Task2 裁决值&gt; python3 -c "
import json, importlib.util, os
from pathlib import Path
tau = float(os.environ['TAU_LOCKED'])
ep01 = Path('output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。')
d = json.load(open(ep01 / 'roundtrip.json'))
vd = [s for s in d['shots'] if s.get('verdict')]
assert len(vd) == 19, f'verdict coverage {len(vd)}/19'
for s in vd:
    v = s['verdict']
    assert v['decision'] in ('accepted', 'rejected') and v['source'] == 'auto' and v.get('decided_at')
    ms = s['scores']['midframe_sim']['score']; at = s['scores']['judge']['attribution']
    expect = 'accepted' if (ms &gt;= tau and at == 'prompt_faithful') else 'rejected'
    assert v['decision'] == expect, (s['shot_id'], ms, at, v['decision'])
assert Path('/tmp/rt_a').read_text().split()[0] == Path('/tmp/rt_b').read_text().split()[0], 'idempotency'
spec = importlib.util.spec_from_file_location('h3r', 'analysis/roundtrip/h3_regen.py')
h3s = importlib.util.module_from_spec(spec); spec.loader.exec_module(h3s)
assert list(h3s._iter_sidecar_errors(d)) == []
rep = Path('.planning/research/roundtrip-threshold-calibration.md').read_text(encoding='utf-8')
assert 'FINAL' in rep and '裁决记录' in rep
proj = Path('.planning/PROJECT.md').read_text(encoding='utf-8')
assert ('tau_sim' in proj) or ('τ_sim' in proj)
print('21-03 T3 OK — tau =', tau)
" &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - 19 条 verdict 全在、判定与硬合取逐镜一致（脚本复算）、source 全 auto、decided_at 非空
  - 同 τ 重跑 sidecar byte 不变（/tmp/rt_a == /tmp/rt_b）——SC5 机器证明
  - PROJECT.md 恰好新增一行 τ 决策（evidence 指针可点）；报告 FINAL + 裁决记录 + attribution×decision 分桶审计表在位
  - 全套件 pytest 绿
  </acceptance_criteria>
  <done>SCORE-03 + DATASET-01 收口：双门槛锁定并落档、verdict 冻结写入、rejected 占比按归因分桶可审计——Phase 21 五条 SC 全闭合，Phase 22（dataset 导出 + HITL 面板）素材就绪。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 模型分数/归因 → 数据集准入门 | 双信号决定 accepted/rejected——校准偏差会系统性塑造 SFT 真值集（v1.3 立项顶级风险） |
| 模型产出 reason 文本 → 报告/未来 HTML | judge reason 进校准报告与 Phase 22 渲染面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09 | Repudiation | 校准数据静默偏向简单动作（v1.3 research Pitfall 5 of 立项） | mitigate | rejected 占比按 attribution closed enum 分桶进报告终稿（机器可 grep 矩阵）+ PROJECT.md 决策行 + 反循环论证声明（τ 人类裁决、机器指标仅参考）——SC4 审计三件套 |
| T-21-10 | Tampering | verdict 被后续重跑覆盖/删除 | mitigate | 冻结 merge（auto/human 一视同仁）+ 同 τ 重跑 byte 级不变证明 + Pitfall 8 双向单测（21-01 已锚）+ h3_regen 重渲 kept-keys 对称性（20-03 已核对） |
| T-21-11 | Spoofing | 区分度不足时静默调参凑数（A4） | mitigate | checkpoint 显式回 Kai 裁决（选项化：accept-with-caveat / 调 N·窗口·特征层），报告如实呈现 per-position 分布——绝不私调窗口/帧数 |
| T-21-12 | Tampering | reason 文本入报告 | mitigate | sidecar 侧 schema ≤2000 + 截断（21-01 已落）；报告内 reason 引用标注来源镜号可回溯 cache raw |
| T-21-SC | Tampering | 包安装 | accept | 零新包（matplotlib 可选且已装） |
</threat_model>

<verification>
- Task 1：19 条双半边 + schema 0 errors + 报告草稿断言 + pytest 绿
- Task 2：checkpoint 回复含 tau=<float> 与抽检结论（blocking——未回复不进 Task 3）
- Task 3：verdict 逐镜硬合取复算 + 幂等 sha256 + PROJECT.md/报告终稿断言 + pytest 绿
</verification>

<success_criteria>
- SC1 完整闭合：19 镜 scores{midframe_sim, judge} + verdict 全字段过 schema gate
- SC2：帧清单审计面在 cache（21-02 smoke + 本 plan 全量）
- SC3：抽检 5 镜一致率 ≥4/5 记录在案（不一致镜明细进报告）
- SC4：分布实测 → τ_sim 由 Kai 裁决 → PROJECT.md Key Decisions + rejected 分桶审计落档
- SC5：重跑 byte 级不变——rejected/hard negatives 永续保留
</success_criteria>

<output>
Create `.planning/phases/21-scorer-threshold-calibration/21-03-SUMMARY.md` when done（必须含：τ 锁定值、checkpoint 回复原文、accepted/rejected 计数与分桶、幂等 sha256 双值）
</output>
