---
phase: 21-scorer-threshold-calibration
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip.json"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/route_cache/scorer/shot_001.json"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/route_cache/scorer/shot_047.json"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/route_cache/judge/shot_001.json"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/route_cache/judge/shot_047.json"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip/_judge_grids/shot_001.jpg"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip/_judge_grids/shot_047.jpg"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/route_cache/h3_regen/overnight_20.log"
autonomous: true
requirements: [SCORE-01, SCORE-02]
must_haves:
  truths:
    - "既有 2 镜 896×512 regen（shots 1/47）上 scorer 真跑出 midframe_sim 分数（SigLIP 真权重真 embed），judge 真跑出三分类归因（真 grid + 真 qwen-eye 调用）——全链在 overnight 批之前先行验证"
    - "roundtrip.json 条目含 scores 双半边（midframe_sim{score,model} + judge{attribution,confidence,reason}）且全量过 Phase 18 schema gate"
    - "打分帧清单（8 帧 ×2 侧 ×2 镜）落 route_cache/scorer/*.json，t_pct/t_sec/path 逐帧可审计（SC2）"
    - "judge grid 图（列/行标注进图）落 roundtrip/_judge_grids/shot_00{1,47}.jpg 可目检（SC3 抽检素材先例）"
    - "uniform-20 @1344×768 overnight 批已 nohup 启动且守卫序列过线：guard 日志可见（TTS 审计/双 /free/22GB gate）、19 镜清单确认（shot 70 跳过）、首镜 /prompt 提交在日志可证、pid 存活可查"
  artifacts:
    - path: "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/route_cache/scorer/shot_001.json"
      provides: "SCORE-01 首个真机打分证据（帧清单 + per_position_cos + score）"
      contains: "per_position_cos"
    - path: "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/route_cache/judge/shot_001.json"
      provides: "SCORE-02 首个真机归因证据（grid + attempts + parsed 三件套）"
      contains: "attribution"
    - path: "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/route_cache/h3_regen/overnight_20.log"
      provides: "overnight 批运行日志（guard 序列 + 逐镜提交审计）"
  key_links:
    - from: "21-01 scorer.py/judge.py"
      to: "真 GPU 路径（GPU0 SigLIP / GPU1 qwen-eye）"
      via: "CLI 真跑（--shots 1,47）+ sidecar/schema 断言"
      pattern: "midframe_sim"
    - from: "h3_regen.py（Phase 20 既有，零修改）"
      to: "uniform-20 @1344×768 overnight 批"
      via: "nohup 后台 + 日志/ pidfile 进度协议"
      pattern: "sample-shots"
---

<objective>
21-01 交付的 scorer/judge 在真 GPU 上首跑验证（research 建议不等 overnight 批：既有 2 镜 896×512 smoke regen 已在盘，cache key 含 regen 身份，1344×768 批后自然重打分）——scorer 真权重 embed + judge 真 grid/真引擎归因 + sidecar 写入 + schema gate；随后启动 uniform-20 @1344×768 overnight 批（h3_regen 既有客户端，零代码修改），本 plan 以"批已启动且过守卫"收口，不等批完成。

Purpose: SC1 的 live 半边（"对抽样 regen 镜跑 scorer"）+ SC3 的素材先例（grid 留档）+ SC4 的前提（19-20 镜 @1344×768 regen 素材——overnight 批是门槛锁定的正式分辨率，896×512 只是验证模式）。
Output: 2 镜真机双信号 + schema 合法 sidecar + 后台运行中的 overnight 批（预计 3-4.5h，19 镜 2594 帧）。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-scorer-threshold-calibration/21-CONTEXT.md
@.planning/phases/21-scorer-threshold-calibration/21-RESEARCH.md
@.planning/phases/21-scorer-threshold-calibration/21-PATTERNS.md
@.planning/phases/21-scorer-threshold-calibration/21-01-SUMMARY.md
@analysis/roundtrip/h3_regen.py
</context>

<interfaces>
<!-- 21-01 交付物 + 环境锚点 -->

CLI 契约（21-01 落地）：
- python3 analysis/roundtrip/scorer.py --work-dir <ep01> [--shots 1,47] [--device cuda:0]
- python3 analysis/roundtrip/judge.py --work-dir <ep01> [--shots 1,47] [--comfy-url http://127.0.0.1:8188]
- python3 analysis/roundtrip/judge.py --work-dir <ep01> --summarize（只读）

EP01 = output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。
现存 regen：roundtrip/shot_001_regen.mp4（7.292s）+ shot_047_regen.mp4（5.167s），engine_version "fl2va-int8/euler+simple/15/896x512"；roundtrip.json 现存 2 条（regen 半边，无 scores）。

GPU 编排锚点（RESEARCH §Pitfall 4 + Environment 表）：
- scorer 默认 GPU0（3060Ti，~4.9GB free，SigLIP fp16 峰值 ~2.0GB，与 GPU1 引擎链零竞争）——批后不需要 /free
- judge 走 GPU1：启动前 comfy_free best-effort（ComfyUI 渲后 cache 驻留可达 ~21GB，post_render_free_mib=1321 实测）；qwen-eye 13.4GB lease，启动实测恰 120s 贴 LLM_START_TIMEOUT_S 上限——(False, ·) 时 ≥30s 重试一次（模块已内建）
- 串行链 = h3 批 → scorer → judge（CONTEXT 锁定）；本 plan 顺序 = scorer（GPU0，可与 GPU1 并行但串行更稳）→ judge（/free 后）→ h3 批启动（guard 自会再 kill TTS + 双 /free + eye 等待——judge 的 stop_if_owned 已释放 lease，不冲突）

overnight 批锚点（Phase 20 已验证 + research 外推）：
- 命令：python3 analysis/roundtrip/h3_regen.py --work-dir <ep01> --sample-shots 20（--regen-resolution 默认 1344x768）
- uniform-20 = [1,5,10,14,19,24,28,33,38,42,47,52,56,61,66,70,75,80,84,89]，shot 70（19.73s）被 --max-shot-sec 10 跳过 → 实渲 19 镜
- 896×512 旧 cache 对 1344×768 engine_version 全 miss（这是对的——校准集 = 19 条 @1344×768 新条目）
- 预计 3-4.5h（2594 h3 帧 × 每帧 2-3× smoke 耗时外推，A2）；断点续跑兜底
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 2 镜 896×512 真 GPU smoke —— scorer 真跑 + judge 真跑 + schema gate</name>
  <files>analysis/roundtrip/scorer.py, analysis/roundtrip/judge.py</files>
  <read_first>
  - .planning/phases/21-scorer-threshold-calibration/21-RESEARCH.md §Validation Architecture「GPU smoke」节 + §Common Pitfalls 1/2/3/4/6（SigLIP 报错形态 / 5.x API / 端点越界 / 批后驻留 / ensure_ready 超时）
  - .planning/phases/20-h3-regen-client/20-03-SUMMARY.md（smoke 计时先例 + _compare/ 目视抽检先例）
  - 21-01-SUMMARY.md（CLI 面 + 已知行为）
  </read_first>
  <behavior>
  - scorer 真跑：--shots 1,47 --device cuda:0 → rc 0；两 cache 文件落地（frames.orig/regen 各 8 条、per_position_cos 8 元、score ∈ [0,1]、model 字段非空）；sidecar 两镜各得 scores.midframe_sim
  - 预期量级：smoke regen 是 896×512——真实分应落 RESEARCH 实测窄带量级（~0.85-0.98；随机基线 ~0.99 的 SigLIP 高区特性）；分数只记录不判阈（τ 未锁）
  - judge 真跑：--shots 1,47 → 日志可见 comfy_free → 引擎拉起（~120s 属正常，偶发贴上限走模块内重试）→ 2 grid 生成 → 2 次调用；cache 含 attempts（探针先例 5/5 一次过，重问路径是保险）+ parsed 三件套；sidecar 两镜各得 scores.judge（reason ≤2000）
  - grid 目检文件存在 + 尺寸 1370×1476 量级 + 非零字节
  - schema gate：roundtrip.json 全量过 Draft202012Validator（h3s._iter_sidecar_errors 或 spec/validate.py 等价路径）0 errors
  - smoke 发现 bug → 就地修 scorer/judge（这正是 smoke 的目的），重跑至过；修复不回退 21-01 单测（全套件保持绿）
  </behavior>
  <action>
  执行序列（每步失败即停排查，不盲目重试）：
  1. 环境预检：python3 -c 探测 torch.cuda device_count 与 GPU0 free（nvidia-smi -i 0）；确认 ComfyUI 可达（curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8188/system_stats——judge 的 /free 需要）。
  2. scorer：python3 analysis/roundtrip/scorer.py --work-dir "$EP01" --shots 1,47（EP01 为完整 work-dir 绝对路径，含中文与全角括号——引号包裹）。观察 [roundtrip] 日志：模型加载（首次 ~10-30s）、16 帧×2 镜提取、cache 写入、sidecar merge。
  3. judge：python3 analysis/roundtrip/judge.py --work-dir "$EP01" --shots 1,47。观察 comfy_free、ensure_ready（120s 量级耐心等）、grid 落盘、observe_single 调用与 attempts。若 ensure_ready 两次失败：查 qwen-eye server.log（RESEARCH Pitfall 6 预警），修复后重跑（cache 已 hit 的镜自动跳过）。
  4. 机器断言（写进执行记录）：两镜 cache JSON 字段断言 + sidecar scores 双半边断言 + schema 0 errors + grid 文件尺寸断言。参考断言骨架：python3 -c 读 $EP01/roundtrip.json，assert shots[] 中 id∈{1,47} 条目 scores.midframe_sim.score 是 0..1 float 且 model 非空、scores.judge.attribution ∈ 三值闭集、len(reason) ≤ 2000；jsonschema Draft202012Validator 对全文件 iter_errors 列表为空。
  5. 把 2 镜真机分数/归因记进执行记录（21-03 校准报告的 896×512 对照数据点）。
  本 task 允许修改 files_modified 中的两个模块（smoke 修复回路）；任何修复后必须 python3 -m pytest tests/ -q 全绿再继续。
  </action>
  <verify>
    <automated>python3 -c "
import json, importlib.util
from pathlib import Path
ep01 = Path('output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。')
d = json.load(open(ep01 / 'roundtrip.json'))
by = {s['shot_id']: s for s in d['shots']}
for sid in (1, 47):
    e = by[sid]
    ms = e['scores']['midframe_sim']; jd = e['scores']['judge']
    assert isinstance(ms['score'], float) and 0.0 &lt;= ms['score'] &lt;= 1.0, ms
    assert ms['model']
    assert jd['attribution'] in ('prompt_faithful', 'model_diverged', 'prompt_underspecified'), jd
    assert 0.0 &lt;= jd['confidence'] &lt;= 1.0 and 1 &lt;= len(jd['reason']) &lt;= 2000
    sc = json.load(open(ep01 / 'route_cache' / 'scorer' / f'shot_{sid:03d}.json'))
    assert len(sc['frames']['orig']) == 8 and len(sc['frames']['regen']) == 8
    assert len(sc['per_position_cos']) == 8
    assert all(25.0 &lt;= f['t_pct'] &lt;= 75.0 for f in sc['frames']['orig'])
    jc = json.load(open(ep01 / 'route_cache' / 'judge' / f'shot_{sid:03d}.json'))
    assert jc['parsed']['attribution'] and len(jc['attempts']) &gt;= 1
    g = ep01 / 'roundtrip' / '_judge_grids' / f'shot_{sid:03d}.jpg'
    assert g.is_file() and g.stat().st_size &gt; 100_000, g
spec = importlib.util.spec_from_file_location('h3r', 'analysis/roundtrip/h3_regen.py')
h3s = importlib.util.module_from_spec(spec); spec.loader.exec_module(h3s)
assert list(h3s._iter_sidecar_errors(d)) == [], 'schema gate'
print('21-02 T1 OK')
" &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - rc 0 全链完成；sidecar 两镜 scores 双半边齐且 schema 0 errors
  - scorer cache 帧清单 8+8 且 t_pct 全落 [25,75]（SC2 真机证据）
  - judge cache attempts 审计在位；grid ≥100KB 可目检
  - 全套件 pytest 仍绿（smoke 修复不破坏离线证据）
  </acceptance_criteria>
  <done>SCORE-01/02 的 live 半边闭合：2 镜真机双信号 + schema 合法——overnight 批前全链已验证，3-4.5h 批不会早晨才发现断链。</done>
</task>

<task type="auto">
  <name>Task 2: uniform-20 @1344×768 overnight 批 nohup 启动 + 守卫过线确认</name>
  <files>analysis/roundtrip/h3_regen.py</files>
  <read_first>
  - .planning/phases/21-scorer-threshold-calibration/21-CONTEXT.md §打分对象与批渲染编排（overnight 批锁定决策）
  - .planning/phases/20-h3-regen-client/20-02-PLAN.md Task 1（guard 五步固定序——日志行形态）+ Task 2（uniform-20 锚点清单 + shot 70 跳过）
  - .planning/phases/21-scorer-threshold-calibration/21-RESEARCH.md §Summary（3-4.5h 外推）+ §Common Pitfalls 4（批后驻留）
  </read_first>
  <behavior>
  - 启动前态：judge 引擎已 stop_if_owned（Task 1 同 plan 内先跑完）；TTS 若在跑由 h3_regen guard 自处理（kill + 审计 warning）
  - nohup 启动：日志落 route_cache/h3_regen/overnight_20.log + pidfile；进程 detach 存活
  - 3-5 分钟内日志可证：guard 序列（TTS 探测/审计、双 /free、free gate 过线）→ uniform-20 选择 20 镜 + shot 70 跳过日志 → 首镜 /prompt 提交 → cache miss（engine_version 含 1344x768）
  - 本 plan 不等批完成（预计 3-4.5h）；21-03 Task 1 负责完成度检查与断点续跑
  </behavior>
  <action>
  1. 预检：kill -0 检查无遗留 judge/eye 进程占 GPU1（Task 1 的 finally 已释放；nvidia-smi -i 1 确认 used 未含 13.4GB lease）。
  2. 启动（bash，路径含全角字符需引号）：
     nohup python3 analysis/roundtrip/h3_regen.py --work-dir "$EP01" --sample-shots 20 > "$EP01/route_cache/h3_regen/overnight_20.log" 2>&1 &
     echo $! > "$EP01/route_cache/h3_regen/overnight_20.pid"
  3. 轮询日志（sleep 30 × ≤10 次）：确认出现 guard 相关 [roundtrip] 行、镜清单行（sampled=20 / skipped=1 形态）、首镜提交行；grep -c "1344x768\|1344" 日志与 route_cache/h3_regen/ 新 cache 文件佐证 engine_version 切换。
  4. 记录：pidfile 路径、日志路径、启动时间戳、预期完成窗口（+3-4.5h）、预期产物 = 19 个 roundtrip/shot_XXX_regen.mp4 @1344×768 + sidecar 19-20 条 regen 半边（shots 1/47 的 regen 半边被 READ-merge 覆盖为 1344×768，scores 保留——Pitfall 7/8 已验证语义）。
  5. 若 guard 拒绝（vram_insufficient）：读日志 top 占用者，处理（等待或人工协调 GPU），重启批（断点续跑安全）；此情况记进 SUMMARY。
  h3_regen.py 零修改（files_modified 列它只为 smoke 修复回路的完整性——本 task 无代码变更）。
  </action>
  <verify>
    <automated>EP01="output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。" &amp;&amp; PID=$(cat "$EP01/route_cache/h3_regen/overnight_20.pid") &amp;&amp; kill -0 "$PID" &amp;&amp; python3 -c "
from pathlib import Path
log = Path('$EP01/route_cache/h3_regen/overnight_20.log').read_text(encoding='utf-8', errors='replace')
assert '[roundtrip]' in log, 'no step-tagged lines yet'
assert '--sample-shots 20' in log, 'sampling line missing'
assert 'guard 拒绝' not in log and 'ComfyUI 不可达' not in log, 'guard refused / comfy down'
assert ('渲染中' in log) or ('rendered' in log) or ('cache hit' in log), 'no per-shot activity yet'
print('21-02 T2 OK — batch running (guard passed, sampling logged, per-shot activity visible)')
"</automated>
  </verify>
  <acceptance_criteria>
  - 进程存活（kill -0）+ 日志含 [roundtrip] 守卫/选择/提交行
  - pidfile + 日志路径已记录进 SUMMARY（21-03 的完成度检查入口）
  - 本 plan 收口时批在后台运行；不等待、不阻塞 plan 完成
  </acceptance_criteria>
  <done>SC4 前提就位：19 镜 @1344×768 regen 素材在产；门槛锁定将对正式分辨率（CONTEXT 锁定决策）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 执行器 → 本地 GPU/引擎（ComfyUI :8188 / qwen-eye :8125 / SigLIP 权重） | 环回管理端点 + 盘上模型；长时批任务占用共享硬件 |
| overnight 后台进程 → work-dir 文件树 | nohup detach 进程在 plan 结束后继续写 cache/sidecar/mp4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Denial of Service | GPU OOM 级联（黑屏先例） | mitigate | scorer 固定 GPU0 + CPU 降级；judge 启动前 comfy_free（Pitfall 4）；h3 批 guard 五步序 + 22GB gate（Phase 20 已验证，零新代码） |
| T-21-07 | Tampering | 后台批无人值守写 sidecar | mitigate | h3_regen 两层自校验 + 原子写 + 19-03/SUMMARY 可查的 READ-merge 语义；21-03 Task 1 批后全量 schema gate 复验 |
| T-21-08 | Repudiation | smoke 证据可信度 | mitigate | cache 帧清单 + attempts 审计 + 执行记录含具体分数/归因值（进 21-03 报告的 896×512 对照点） |
| T-21-SC | Tampering | 包安装 | accept | 零新包；SigLIP 权重离线加载（同 T-21-04） |
</threat_model>

<verification>
- Task 1 的 python -c 断言块全过（sidecar 双半边 + cache 清单 + schema 0 errors + grid 文件）+ 全套件 pytest 绿
- Task 2 的存活/日志断言过；pidfile 与日志路径进 SUMMARY
- 2 镜真机分数/归因值记进 SUMMARY（896×512 对照数据点）
</verification>

<success_criteria>
- SC1 live 半边：真 scorer 分数 + 真 judge 归因 + schema 合法 sidecar（896×512 smoke 证据）
- SC2 真机证据：帧清单 t_pct 全落 [25,75] 的 cache 文件在盘
- overnight 批 @1344×768 运行中——21-03 校准的素材前提就位（SC4 前半）
</success_criteria>

<output>
Create `.planning/phases/21-scorer-threshold-calibration/21-02-SUMMARY.md` when done（必须含：pidfile/日志绝对路径、启动时间戳、预期完成窗口、2 镜 smoke 分数与归因值）
</output>
