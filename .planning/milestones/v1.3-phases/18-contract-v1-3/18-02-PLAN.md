---
phase: 18-contract-v1-3
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - spec/fixtures/v1.3/asset.json
  - spec/fixtures/v1.3/roundtrip.json
  - spec/fixtures/v1.3/shots.json
  - spec/fixtures/v1.3/audio_analysis.json
  - spec/fixtures/v1.3/transcript.json
  - spec/fixtures/v1.3/frames.json
  - spec/fixtures/v1.3/prompts.json
  - spec/fixtures/v1.3/characters.json
  - spec/fixtures/v1.3/props.json
  - spec/fixtures/v1.3/registry.draft.json
  - spec/fixtures/v1.3/registry.edits.json
  - spec/fixtures/v1.3/audio_semantic.json
  - spec/fixtures/v1.3/speakers.json
  - spec/validate.py
  - scripts/verify_contract.py
autonomous: true
requirements: [RT-01, RT-02, RT-04]
tags: [fixture, validation, bidirectional-proof, cross-version, backward-filter, eight-shapes]

must_haves:
  truths:
    - "`spec/fixtures/v1.3/` contains exactly 13 JSON files: 11 byte-copied from v1.2 (all non-asset files) + edited `asset.json` + new `roundtrip.json`"
    - "The 11 non-asset v1.3 fixture files are byte-identical to `spec/fixtures/v1.2/` (`diff` clean) — byte-identical-absent 红线的 substrate 半边证明"
    - "v1.3 fixture asset.json: `schema_version: \"1.3\"` + `generator.version: \"0.3.0-spec-fixture-v1.3\"` + `data.roundtrip` = `{path, accepted_count: 1, rejected_count: 1}` object + warnings 双形并存 (≥1 legacy string + ≥1 structured `{code, detail}`)"
    - "v1.3 fixture roundtrip.json validates against roundtrip.schema.json with shot 1 = full (regen 5-tuple + duration/width/height + scores{midframe_sim + judge} + verdict{accepted, auto}) and shot 2 = degrade+human (regen + midframe_sim only + verdict{rejected, human, decided_at}); shot_id ∈ {1, 2} only"
    - "`python3 spec/validate.py` runs a 4-tier gate (minimal 6 / v1.1 10 / v1.2 12 / v1.3 13 shapes) — all tiers 0 failures, exit 0, summary line contains `v1.3 failures=0`"
    - "`verify_contract.py` pass (e) FORWARD v1.2→v1.3: v1.2 fixture asset.json × current schema → 0 errors"
    - "`verify_contract.py` pass (f) BACKWARD v1.3→v1.2: v1.3 fixture asset.json × `_recover_v12_schema` → 0 non-additive errors EXCLUDING the two documented v1.3 deltas (additionalProperties at data + type/anyOf under generator/warnings) — 过滤规则扩展后注入真实 shared-field drift（如 `asset_type: \"other\"`）仍必须 FAIL"
    - "`_recover_v12_schema` primary = `git show v1.2:spec/schemas/<shape>.schema.json`; strip fallback pops `data.properties.roundtrip` AND restores `generator.properties.warnings.items` to `{\"type\": \"string\"}`"
    - "EIGHT_SHAPES includes `\"roundtrip\"` AND `validate_eight_shapes` handles object-valued data.roundtrip (takes `.get(\"path\")`) — 同 plan 成对落地，Phase 20 producer 挂载不误报"
    - "`_fixture_consistency_check` v1.3 block: roundtrip.shots[].shot_id ⊆ v1.3 fixture shots.json ids (v1.3 目录自己的 shots.json 为 source-of-truth)"
    - "全字段覆盖证明：status.failed 条目、全部三个 attribution enum 值、scorer_model_missing warning code、decided_at 缺席、width/height 缺席 — direct Draft202012Validator 实例检查全绿（fixture 2-shot 装不下的形状走此通道）"
  artifacts:
    - path: "spec/fixtures/v1.3/roundtrip.json"
      provides: "v1.3 round-trip sidecar fixture (full + degrade 两态 2-shot)"
      contains: "prompt_faithful"
    - path: "spec/fixtures/v1.3/asset.json"
      provides: "v1.3 manifest fixture (schema_version 1.3 + data.roundtrip object + warnings 双形)"
      contains: "0.3.0-spec-fixture-v1.3"
    - path: "spec/validate.py"
      provides: "4-tier shape gate (minimal + v1.1 + v1.2 + v1.3)"
      contains: "V13_ORDER"
    - path: "scripts/verify_contract.py"
      provides: "v1.0↔v1.1↔v1.2↔v1.3 bidirectional proof + _recover_v12_schema + v1.3 fixture consistency + EIGHT_SHAPES object 特判"
      contains: "_recover_v12_schema"
  key_links:
    - from: "spec/fixtures/v1.3/asset.json#data.roundtrip.path"
      to: "spec/fixtures/v1.3/roundtrip.json"
      via: "relative path string"
      pattern: "roundtrip\\.json"
    - from: "scripts/verify_contract.py:_cross_version_check pass (f)"
      to: "scripts/verify_contract.py:_recover_v12_schema"
      via: "function call in v1.3→v1.2 backward pass"
      pattern: "_recover_v12_schema"
    - from: "spec/validate.py:validate_v13"
      to: "spec/schemas/roundtrip.schema.json"
      via: "load_validator('roundtrip') against fixture"
      pattern: "validate_v13"
---

<objective>
Build the empirical proof layer for the v1.3 contract: 13-file fixture (11 byte-copied substrate + edited asset.json + new roundtrip.json), the 4-tier validate.py gate, and the verify_contract.py v1.2↔v1.3 bidirectional cross-version proof — including the two novel sub-mechanisms that make this NOT a blind copy of Phase 11: the backward-filter extension (first items-type widening in v1.x history produces `type` errors that must be exempted as documented deltas while real drift still fails) and the validate_eight_shapes object special-case (first object-valued data.* mount).

Purpose: RT-02's fixture + gate + bidirectional proof, RT-01's byte-identical-absent substrate proof, RT-04's fixture-level 双形 demonstration + machine-checked enum coverage. The backward-filter negative test (A3) is the highest-risk item of the phase — a filter that exempted too much would silently blind the proof harness.

Output: `spec/fixtures/v1.3/` (13 files) + `spec/validate.py` V13 tier + `scripts/verify_contract.py` (5 surgical edits).

Scope note (files_modified=15, checker-flagged, Phase 11 precedent): 11 of the 15 files are mechanical byte-copies (`cp` + `diff` gate — near-zero context cost); only 4 carry substantive work (fixture asset.json edits, roundtrip.json authoring, validate.py, verify_contract.py) across 3 tasks. No split required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-contract-v1-3/18-CONTEXT.md
@.planning/phases/18-contract-v1-3/18-RESEARCH.md
@.planning/phases/18-contract-v1-3/18-PATTERNS.md
@.planning/phases/18-contract-v1-3/18-01-SUMMARY.md

<interfaces>
<!-- Existing structures the executor extends. All line numbers verified 2026-08-19 against post-18-01 state where relevant. -->

spec/fixtures/v1.2/ (12 files — the substrate):
  asset.json: schema_version "1.2", generator.version "0.3.0-spec-fixture-v1.2", generated_at "2026-07-24T00:00:00Z", warnings = 2 plain strings ("preflight route unreachable: ConnectError…" / "shot 3: route code=500: SHOT_ANALYSIS_DRIVER_FAILED"), data has 9 keys
  shots.json: ids = [1, 2] (runtime-verified) — roundtrip.json fixture entries MUST stay within {1, 2}

spec/validate.py (314 lines):
  V12 block ends at line 98 (V12_FIXTURE_DIR/MAP/ORDER); validate_v12() ends at line 214; main() aggregation lines 293-304: `total_strict_failures = minimal_failures + v11_failures + v12_failures` + summary print `f"v1.2 failures={v12_failures}, "`
  Per-tier function pattern: loop ORDER + FIXTURE_MAP, `[FAIL-vXX]`/`[valid-vXX]` prefixes, reuse load_validator/_format_errors

scripts/verify_contract.py (1428 lines):
  EIGHT_SHAPES (lines 82-88): currently 11 elements ending "audio_semantic", "speakers" — append "roundtrip" with Phase-18 comment
  validate_eight_shapes string assumption (lines 253-261): `rel = data_field.get(shape); if not isinstance(rel, str): … failures.append(f"{shape}: data.{shape} is not a string: {rel!r}")` ← object 特判 point
  _recover_v11_schema (lines 326-365): git-show-primary + deep-copy-strip-fallback template — _recover_v12_schema clones this with tag v1.2 + the extra warnings-items restore
  _cross_version_check (lines 368-480): pass (c) forward v1.1→v1.2 at 428-445, pass (d) backward v1.2→v1.1 at 447-472 with filter `non_addprop = [e for e in errs if e.validator != "additionalProperties"]`, summary lines 474-480 — pass (e)/(f) insert before the return; summary message updated to v1.0↔v1.1↔v1.2↔v1.3 with honest exclusion wording
  _fixture_consistency_check v1.2 speakers block (lines 578-632), success message line 639 — v1.3 block inserts after line 632 (gated on spec/fixtures/v1.3/ is_dir; shots source-of-truth = v1.3 目录自己的 shots.json, mirror WR-01 lesson)
  CLI: `--mode` choices ["producer","consumer","e2e","all"], default "all" — proof checks run in producer mode; self-test via PHASE4_SELF_TEST=1 env

Plan 18-01 outputs (contract layer this plan proves):
  spec/schemas/roundtrip.schema.json — regen 5-tuple required, attribution enum {prompt_faithful, model_diverged, prompt_underspecified}, verdict {decision, source, decided_at?}, status {state: "failed", error}, midframe_sim {score 0..1, model}
  spec/schemas/asset.schema.json — data.roundtrip {path, accepted_count, rejected_count} optional object; warnings items anyOf string | {code enum(3), detail}
  scripts/export_asset.py — SCHEMA_VERSION "1.3"; mount counts; _valid_warnings_list

Runtime facts (verified): git tag v1.2 exists and `git show v1.2:spec/schemas/asset.schema.json` yields the pre-v1.3 state (9 data props, warnings items {"type":"string"}); ep01 producer asset dir on disk for --mode=producer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build 13-file v1.3 fixture (11 byte-copied + roundtrip.json + edited asset.json)</name>
  <files>spec/fixtures/v1.3/ (13 files: asset.json, roundtrip.json, shots.json, audio_analysis.json, transcript.json, frames.json, prompts.json, characters.json, props.json, registry.draft.json, registry.edits.json, audio_semantic.json, speakers.json)</files>
  <read_first>
    - `spec/fixtures/v1.2/asset.json` (FULL — the substrate to copy and edit; note warnings 双 string 现状 + generator.version 字串风格)
    - `spec/fixtures/v1.2/shots.json` (ids [1,2] — roundtrip entries 的硬约束)
    - `spec/fixtures/v1.2/audio_semantic.json` (2-shot full/degrade fixture 结构模板 — roundtrip.json mirror 此模式)
    - `spec/schemas/roundtrip.schema.json` (18-01 output — the schema this fixture must satisfy)
    - `spec/schemas/asset.schema.json` (18-01 output — data.roundtrip object mount + warnings 双形)
    - `.planning/phases/18-contract-v1-3/18-RESEARCH.md` §Open Questions Q1 (fixture 全字段覆盖 vs 2-shot 上限的解法) + Assumptions A10
    - `.planning/phases/18-contract-v1-3/18-PATTERNS.md` §spec/fixtures/v1.3/ (asset.json 编辑点 + roundtrip.json 形状)
  </read_first>
  <behavior>
    - Directory contains exactly 13 files; the 11 non-asset files are byte-identical copies of v1.2 (`cp` then `diff` clean — 覆盖 byte-identical-absent 红线的 substrate 半边).
    - asset.json edits (ONLY these four points vs the v1.2 copy): `schema_version` → `"1.3"`; `generator.version` → `"0.3.0-spec-fixture-v1.3"`; `generator.warnings` → 双形并存 (keep ONE legacy string, e.g. the existing "preflight route unreachable…" line; drop or keep the second string at discretion; add TWO structured entries: `{"code": "comfyui_unreachable", "detail": "…ConnectError…"}` and `{"code": "vram_insufficient", "detail": "…<22GB free refused submit…"}`); `data` gains `"roundtrip": {"path": "roundtrip.json", "accepted_count": 1, "rejected_count": 1}` after `speakers`. `generated_at` and all other fields unchanged.
    - roundtrip.json: top-level `{"schema_version": "1.3", "shots": [ …2 entries… ]}`.
      - Shot 1 (full / auto-accept): `regen` full 8-field (path `"roundtrip/shot_001.mp4"`, video_content_hash 16-hex e.g. `"a1b2c3d4e5f60718"`, engine_name `"MiniMaxH3ImageToVideo"`, engine_version + prompt_version short non-empty strings, duration_sec ~4.8, width 832, height 480) + `scores` both (`midframe_sim` {score 0.87, model "clip-vit-l14-336"}; `judge` {attribution "prompt_faithful", confidence 0.8, reason 中文短句}) + `verdict` {decision "accepted", source "auto"} (no decided_at).
      - Shot 2 (degrade + human-reject): `regen` 5-tuple + duration_sec only (no width/height — optional 字段缺席示例) + `scores` with `midframe_sim` ONLY ({score 0.42, model same}) + `verdict` {decision "rejected", source "human", decided_at "2026-08-19T00:00:00Z"}.
    - Both files validate against their schemas; Chinese content written with ensure_ascii=False, indent=2.
  </behavior>
  <action>
    Step 1: `mkdir -p spec/fixtures/v1.3` then byte-copy the 11 non-asset files from v1.2 (shots / audio_analysis / transcript / frames / prompts / characters / props / registry.draft / registry.edits / audio_semantic / speakers) + copy asset.json as the edit substrate. Verify the 11 with `diff` (must be empty).

    Step 2: Edit the copied asset.json at exactly the four points in <behavior>. Structured warning entries carry realistic Chinese-free-text details mirroring the existing legacy string content (ConnectError / VRAM guard refusal — Phase 20 REGEN-03 semantics). data.roundtrip counts MUST equal the roundtrip.json verdict counts (1 accepted, 1 rejected) — 统计与 sidecar 一致是 18-01 挂载语义的 fixture 级体现.

    Step 3: Author roundtrip.json per <behavior> (Open Q1 recommendation: 2 entries cover the two main paths; remaining shapes covered by Task 1's direct-validator verify below, NOT by stuffing more entries — 一 shot 一条目的结果集语义).

    Write both authored files via `json.dump(..., indent=2, ensure_ascii=False)` or byte-equivalent hand-editing.
  </action>
  <verify>
    <automated>python3 - <<'PY'
import filecmp, json, os
from jsonschema import Draft202012Validator as V
d = 'spec/fixtures/v1.3'
files = sorted(os.listdir(d))
assert len(files) == 13, f'expected 13 files, got {len(files)}: {files}'
SUBSTRATE = ['shots.json','audio_analysis.json','transcript.json','frames.json','prompts.json',
             'characters.json','props.json','registry.draft.json','registry.edits.json',
             'audio_semantic.json','speakers.json']
for f in SUBSTRATE:
    assert filecmp.cmp(f'spec/fixtures/v1.2/{f}', f'{d}/{f}', shallow=False), f'{f} drifted from v1.2'
asset = json.load(open(f'{d}/asset.json'))
assert asset['schema_version'] == '1.3'
assert asset['generator']['version'] == '0.3.0-spec-fixture-v1.3'
assert asset['data']['roundtrip'] == {'path': 'roundtrip.json', 'accepted_count': 1, 'rejected_count': 1}
w = asset['generator']['warnings']
assert any(isinstance(x, str) for x in w) and any(isinstance(x, dict) for x in w), 'warnings not dual-form'
V(json.load(open('spec/schemas/asset.schema.json'))).validate(asset)
rt = json.load(open(f'{d}/roundtrip.json'))
V(json.load(open('spec/schemas/roundtrip.schema.json'))).validate(rt)
ids = {s['id'] for s in json.load(open(f'{d}/shots.json'))}
assert all(e['shot_id'] in ids for e in rt['shots']), 'shot_id outside substrate ids'
counts = {'accepted': 0, 'rejected': 0}
for e in rt['shots']:
    v = (e.get('verdict') or {}).get('decision')
    if v in counts: counts[v] += 1
assert counts == {'accepted': 1, 'rejected': 1}
# 全字段 direct-validator coverage (shapes the 2-entry fixture cannot carry — Open Q1)
rs = json.load(open('spec/schemas/roundtrip.schema.json'))
base = json.loads(json.dumps(rt))
def ok(mut):
    inst = json.loads(json.dumps(base)); mut(inst); V(rs).validate(inst)
ok(lambda s: s['shots'].append({'shot_id': 2, 'status': {'state': 'failed', 'error': 'ComfyUI 429 timeout'}}))
for attr in ('prompt_faithful', 'model_diverged', 'prompt_underspecified'):
    ok(lambda s, a=attr: s['shots'][0]['scores']['judge'].update(attribution=a))
ok(lambda s: s['shots'][0].pop('verdict'))
ok(lambda s: s['shots'][1]['regen'].pop('duration_sec'))
for code in ('comfyui_unreachable', 'vram_insufficient', 'scorer_model_missing'):
    ws = json.load(open('spec/schemas/asset.schema.json'))
    V(ws).validate({**asset, 'generator': {**asset['generator'], 'warnings': [{'code': code, 'detail': 'x'}]}})
print('[fixture-ok] 13 files + 11 diff-clean + schemas green + counts consistent + full-field coverage (status.failed / 3 attributions / 3 warning codes / optional absences)')
PY
</automated>
  </verify>
  <acceptance_criteria>
    - `ls spec/fixtures/v1.3/ | wc -l` returns 13.
    - `diff spec/fixtures/v1.2/shots.json spec/fixtures/v1.3/shots.json` exits 0; same for the other 10 substrate files.
    - `python3 -c "import json; a=json.load(open('spec/fixtures/v1.3/asset.json')); print(a['schema_version'], a['data']['roundtrip'])"` prints `1.3 {'path': 'roundtrip.json', 'accepted_count': 1, 'rejected_count': 1}`.
    - `python3 -c "import json; w=json.load(open('spec/fixtures/v1.3/asset.json'))['generator']['warnings']; print(sum(isinstance(x,str) for x in w), sum(isinstance(x,dict) for x in w))"` prints `1 2` (one legacy string + two structured).
    - Both authored files validate against their 18-01 schemas; roundtrip shot_ids ⊆ {1,2}; verdict counts 1/1 match the mount.
    - Direct-validator coverage prints `[fixture-ok] …` (status.failed entry + all three attribution values + verdict-absent + optional-absence + all three warning codes as solo structured entries all validate).
  </acceptance_criteria>
  <done>
    13-file v1.3 fixture in place: 11 substrate files byte-identical to v1.2; asset.json carries schema_version 1.3 + data.roundtrip object with consistent counts + dual-form warnings; roundtrip.json validates with full/degrade 2-shot coverage within substrate ids; every remaining field shape proven via direct-validator instances.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend spec/validate.py with V13 constants + validate_v13() + 4-tier exit-code gate</name>
  <files>spec/validate.py</files>
  <read_first>
    - `spec/validate.py` (FULL 314 lines — esp. V12 block lines 73-98, validate_v12 lines 181-214, main aggregation lines 279-310)
    - `.planning/phases/18-contract-v1-3/18-RESEARCH.md` §Code Examples B (V13 wiring, lines 432-459)
  </read_first>
  <behavior>
    - New constants after line 98: `V13_FIXTURE_DIR = SPEC_DIR / "fixtures" / "v1.3"`, `V13_FIXTURE_MAP` (12 v1.2 entries verbatim + `"roundtrip": "roundtrip.json"`), `V13_ORDER = V12_ORDER + ["roundtrip"]` (13 entries).
    - New `validate_v13() -> int`: line-for-line mirror of `validate_v12()` — only the docstring (11 个非 asset 文件 byte-copied + asset.json 编辑 + roundtrip.json 新增), the constants it references, and the print prefixes (`[valid-v13]` / `[FAIL-v13]`) differ. Reuses load_validator + _format_errors.
    - `main()`: after the v1.2 block, `print(f"[validate] v1.3 fixture = {V13_FIXTURE_DIR}")` + `v13_failures = validate_v13()`; aggregation becomes `total_strict_failures = minimal_failures + v11_failures + v12_failures + v13_failures`; summary line gains `v1.3 failures={v13_failures}`.
    - Chinese comment above the constants noting Phase 18 additive 第四阶.
    - Existing minimal/v1.1/v1.2 tiers byte-identical in behavior (no regression).
  </behavior>
  <action>
    Three surgical edits mirroring the V12 pattern exactly (RESEARCH Code Ex. B): (1) constants block after line 98; (2) `validate_v13()` after `validate_v12()` ends (line 214); (3) main() wiring between the v1.2 block (line 280) and the smoke block (line 282) + aggregation (line 293) + summary print (lines 298-304). No other edits — the smoke pass and strict-smoke flag are untouched.
  </action>
  <verify>
    <automated>python3 spec/validate.py 2>&1 | tee /tmp/validate_v13.out
    python3 -c "
out = open('/tmp/validate_v13.out').read()
assert out.count('[valid-v13]') == 13, f\"expected 13 [valid-v13] lines, got {out.count('[valid-v13]')}\"
assert '[FAIL-v13]' not in out
assert 'v1.3 failures=0' in out
assert 'minimal failures=0' in out and 'v1.1 failures=0' in out and 'v1.2 failures=0' in out
print('[validate.py] 4-tier gate GREEN')
"
    python3 spec/validate.py >/dev/null 2>&1 && echo "exit 0 OK" || echo "FAILED non-zero exit"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 spec/validate.py` exits 0; output contains exactly 13 `[valid-v13]` lines (including `[valid-v13] asset` and `[valid-v13] roundtrip`) and the summary `minimal failures=0, v1.1 failures=0, v1.2 failures=0, v1.3 failures=0`.
    - `grep -c 'V13_FIXTURE_DIR\|V13_FIXTURE_MAP\|V13_ORDER\|validate_v13' spec/validate.py` returns ≥5 (all new symbols defined and used).
    - `python3 -c "import ast; ast.parse(open('spec/validate.py').read())"` exits 0.
    - `python3 -c "import importlib.util; s=importlib.util.spec_from_file_location('v','spec/validate.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert len(m.V13_ORDER)==13 and m.V13_ORDER[-1]=='roundtrip' and set(m.V12_ORDER) < set(m.V13_ORDER)"` exits 0.
  </acceptance_criteria>
  <done>
    validate.py runs the 4-tier gate; v1.3 tier validates all 13 shapes green; exit code aggregates all four tiers; old tiers regression-free.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend verify_contract.py — _recover_v12_schema + pass (e)/(f) with filter extension + consistency block + EIGHT_SHAPES object 特判 + backward negative test</name>
  <files>scripts/verify_contract.py</files>
  <read_first>
    - `scripts/verify_contract.py:60-90` (constants + EIGHT_SHAPES), `:240-278` (validate_eight_shapes string assumption at 253-261), `:326-365` (_recover_v11_schema — clone template), `:368-480` (_cross_version_check passes + filter + summary), `:578-639` (_fixture_consistency_check v1.2 block + success message)
    - `.planning/phases/18-contract-v1-3/18-RESEARCH.md` §Code Examples C (lines 461-482) + §Wrinkle 1 (过滤规则交互 — the highest-risk mechanism) + Pitfall 1/2
    - `.planning/phases/18-contract-v1-3/18-PATTERNS.md` §verify_contract (Analog A-D + §No Analog Found #1/#2)
    - `git show v1.2:spec/schemas/asset.schema.json` (run it — the recovered-schema baseline: 9 data props, warnings items {"type":"string"})
  </read_first>
  <behavior>
    - `_recover_v12_schema(shape)`: primary `git show v1.2:spec/schemas/{shape}.schema.json` (subprocess, timeout 5, mirror _recover_v11_schema); fallback deep-copies the current schema and for asset pops `data.properties.roundtrip` AND sets `generator.properties.warnings.items = {"type": "string"}` (首个非 property-delta 必须一并还原 — Wrinkle 1 连锁 3). Returns None only when both paths fail.
    - Pass (e) FORWARD v1.2→v1.3: v1.2 fixture asset.json × current asset.schema.json → assert 0 errors (v1.2 的 string warnings 对加宽后 items 天然合法).
    - Pass (f) BACKWARD v1.3→v1.2: v1.3 fixture asset.json × `_recover_v12_schema("asset")` → exempt exactly: `e.validator == "additionalProperties"` OR (`e.validator in ("type", "anyOf")` AND `tuple(e.absolute_path[:2]) == ("generator", "warnings")`); ANY other error → failure entry `backward v1.3→v1.2 asset: N non-additive error(s) (shared fields drifted)…`.
    - Summary message becomes `v1.0↔v1.1↔v1.2↔v1.3 cross-version bidirectional compat proven (forward 0 errors; backward 0 non-additive errors, excluding documented v1.3 deltas: data.roundtrip + warnings items widening)`.
    - EIGHT_SHAPES gains `"roundtrip"` (comment: Phase 18 additive, gated on data.roundtrip existence); `validate_eight_shapes` unwraps dict values via `rel.get("path")` before the string check — a dict whose path is missing/non-string records a failure naming `data.roundtrip.path`.
    - `_fixture_consistency_check` v1.3 block (gated on `spec/fixtures/v1.3/` is_dir + roundtrip.json is_file): every `roundtrip.shots[].shot_id` ∈ v1.3 目录自己 shots.json 的 ids (WR-01 lesson — 不复用 v1.2 ids); success message extended to mention v1.3.
    - Negative test (A3 — MUST hold): injecting a real shared-field drift (`asset_type: "other"`, a `const` violation) into the v1.3 fixture instance makes the backward check FAIL (filter not blind).
    - Existing passes (a)-(d) + v1.1/v1.2 consistency + self-test untouched and still green.
  </behavior>
  <action>
    Five surgical edits:

    (1) EIGHT_SHAPES (line 87): append `"roundtrip"` with a Phase 18 comment mirroring the Phase 11 comment style (gated on data.roundtrip existence).

    (2) validate_eight_shapes (lines 253-261): before the `isinstance(rel, str)` check, add `if isinstance(rel, dict): rel = rel.get("path")` with a Chinese comment (Phase 18: data.roundtrip 是首个 object 值挂载——取 path 再走文件加载；与 EIGHT_SHAPES 追加必须同 plan 成对, Pitfall 2). Adjust the failure message to name `data.{shape}.path is not a string` for the unwrapped case.

    (3) Insert `_recover_v12_schema` after `_recover_v11_schema` (line 365) — clone structure, tag `v1.2`, docstring documents both strip operations (pop data.roundtrip + restore warnings items), 中文注释 citing Wrinkle 1.

    (4) In `_cross_version_check`, insert passes (e)/(f) after pass (d) (line 472) and before the failures check (line 474), mirroring pass (c)/(d) structure verbatim (v12_dir → forward; `v13_dir = REPO / "spec" / "fixtures" / "v1.3"` → backward with the extended filter from <behavior>). Update the success return string per <behavior> (诚实化 excluding-deltas 话术). The filter exemption conditions are expressed as a helper predicate or inline list comprehension — keep it in the same function (no new module-level proof machinery).

    (5) In `_fixture_consistency_check`, insert the v1.3 block after the v1.2 speakers block (line 632): load v1.3 roundtrip.json + v1.3 shots.json (own source-of-truth), flag `roundtrip shot_id {id} unknown` style failures; extend the success message (line 639) to `v1.1 + v1.2 + v1.3 fixture set cross-file IDs consistent (0 dangling)`.

    After the edits run the full producer mode; then run the negative test from <verify>.
  </action>
  <verify>
    <automated>python3 scripts/verify_contract.py --mode=producer 2>&1 | tee /tmp/verify_v13.out
    python3 - <<'PY'
import importlib.util, json, shutil, subprocess
out = open('/tmp/verify_v13.out').read()
assert 'v1.0↔v1.1↔v1.2↔v1.3 cross-version bidirectional compat proven' in out, out[-800:]
assert 'v1.3 fixture set cross-file IDs consistent' in out, out[-800:]
s = importlib.util.spec_from_file_location('vc', 'scripts/verify_contract.py')
vc = importlib.util.module_from_spec(s); s.loader.exec_module(vc)
# _recover_v12_schema: git-show primary yields pre-v1.3 state
rec = vc._recover_v12_schema('asset')
assert 'roundtrip' not in rec['properties']['data']['properties']
assert rec['properties']['generator']['properties']['warnings']['items'] == {'type': 'string'}
# strip fallback also restores (simulate by calling with the tag path unavailable is intrusive; assert the code contains the restore)
src = open('scripts/verify_contract.py').read()
assert src.count('_recover_v12_schema') >= 2
assert '"roundtrip"' in src.split('EIGHT_SHAPES = [')[1].split(']')[0]
# A3 negative test: real shared-field drift MUST still fail the backward pass
p = 'spec/fixtures/v1.3/asset.json'
backup = open(p, encoding='utf-8').read()
try:
    inst = json.loads(backup); inst['asset_type'] = 'other'
    open(p, 'w', encoding='utf-8').write(json.dumps(inst, indent=2, ensure_ascii=False))
    ok, detail = vc._cross_version_check()
    assert not ok and 'backward v1.3→v1.2' in detail and 'non-additive' in detail, f'drift NOT caught: {detail}'
    print('[negative] injected asset_type drift correctly FAILS backward v1.3→v1.2 (filter not blind)')
finally:
    open(p, 'w', encoding='utf-8').write(backup)
r = subprocess.run(['python3', 'scripts/verify_contract.py', '--mode=producer'], capture_output=True, text=True)
assert r.returncode == 0, r.stdout[-1000:]
print('[verify_contract] v1.0↔v1.1↔v1.2↔v1.3 bidirectional + consistency + EIGHT_SHAPES object pass GREEN')
PY
PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer >/dev/null 2>&1 && echo "self-test GREEN" || echo "self-test FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/verify_contract.py --mode=producer` exits 0; output contains `v1.0↔v1.1↔v1.2↔v1.3 cross-version bidirectional compat proven (forward 0 errors; backward 0 non-additive errors, excluding documented v1.3 deltas: data.roundtrip + warnings items widening)` and `v1.1 + v1.2 + v1.3 fixture set cross-file IDs consistent (0 dangling)`.
    - `grep -c '_recover_v12_schema' scripts/verify_contract.py` returns ≥2 (definition + call site).
    - `python3 -c` (see automated): `_recover_v12_schema('asset')` output has no `roundtrip` data property AND warnings items == `{'type': 'string'}` (both the git-show primary content and the strip-fallback restore verified in source).
    - Negative test prints `[negative] injected asset_type drift correctly FAILS backward v1.3→v1.2 (filter not blind)` — the extended filter does NOT swallow a real `const` violation.
    - `PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer` exits 0 (fail-loud self-test intact).
    - `python3 spec/validate.py` still exits 0 (Task 2 gate undisturbed).
  </acceptance_criteria>
  <done>
    verify_contract.py proves v1.2↔v1.3 bidirectional compatibility with the documented-deltas filter (data.roundtrip addProp + generator/warnings type errors exempted, everything else — including injected const drift — fails); _recover_v12_schema restores the true v1.2 schema via git tag with a correct strip fallback; EIGHT_SHAPES + object 特判 landed as a pair; v1.3 fixture consistency enforced; passes (a)-(d) and self-test regression-free.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture dir → schema validator | validate.py + verify_contract.py load spec/fixtures/v1.3/*.json into Draft202012Validator — trusted dev artifacts, no untrusted input |
| git history → schema recovery | `_recover_v12_schema` subprocess-invokes `git show v1.2:spec/schemas/...` to read the immutable tag; the strip fallback is the shallow-clone/CI insurance path |
| proof harness → phase gate | the backward filter is the trust anchor of the entire cross-version proof — an over-broad exemption would silently convert breaking changes into "proven compatible" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Tampering/Repudiation | backward pass (f) filter extension exempts too much (proof goes blind on real shared-field drift) | mitigate | Filter scope is exactly two conditions (addProp anywhere; type/anyOf ONLY when `absolute_path[:2] == ("generator","warnings")`); Task 3 negative test injects `asset_type: "other"` (const violation) and asserts FAIL with backup/restore in finally. This is the A3 highest-risk assumption made machine-checked. |
| T-18-08 | Tampering | `_recover_v12_schema` fallback yields a v1.3-contaminated "v1.2" schema (false-positive backward GREEN) | mitigate | Primary path is the immutable git tag (verified present); fallback restores BOTH strip operations (pop data.roundtrip + warnings items back to string); Task 3 acceptance asserts the recovered schema's actual content (no roundtrip prop, items == {"type":"string"}). |
| T-18-09 | Tampering | EIGHT_SHAPES gains roundtrip without the object 特判 (or vice versa) → Phase 20 producer mode false-fails on real assets | mitigate | Both changes are specified in the SAME task with an explicit pairing comment (Pitfall 2 "勿只做一半"); producer-mode rerun on ep01 (which lacks roundtrip.json — dormant gate) plus the fixture-based checks confirm no misfire. |
| T-18-10 | Tampering | fixture roundtrip entries reference shot_ids absent from substrate (dangling cross-refs) | mitigate | Task 1 acceptance (shot_ids ⊆ {1,2}) + the new `_fixture_consistency_check` v1.3 block using the v1.3 directory's own shots.json (WR-01 lesson). |
| T-18-11 | DoS/Information Disclosure | old-tier regression from asset.schema edits or harness changes | mitigate | 4-tier validate gate (all old tiers count toward exit code) + passes (a)-(d) untouched + PHASE4_SELF_TEST fail-loud + `python3 -m pytest tests/ -x -q` in wave merge. |
| T-18-SC | Tampering | package installs | accept (carried) | Zero installs this plan. |
</threat_model>

<verification>
After all 3 tasks:

1. `python3 spec/validate.py` exits 0 — summary `minimal failures=0, v1.1 failures=0, v1.2 failures=0, v1.3 failures=0`.
2. `python3 scripts/verify_contract.py --mode=producer` exits 0 — v1.0↔v1.1↔v1.2↔v1.3 bidirectional line + v1.3 consistency line present.
3. `ls spec/fixtures/v1.3/ | wc -l` returns 13; the 11 substrate files `diff` clean against v1.2 (byte-identical-absent substrate proof — 契约级断言证据，连同 18-01 的 data-keys smoke 一起写进 phase VERIFICATION).
4. Negative test output `[negative] … filter not blind`.
5. Wave regression: `python3 -m pytest tests/ -x -q` green.
</verification>

<success_criteria>
- RT-01 (proof half): byte-identical-absent 红线 proven at fixture level (11-file diff-clean) on top of 18-01's data-keys smoke; full-field schema acceptance demonstrated (fixture + direct-validator instances).
- RT-02 (gate + proof half): SCHEMA_VERSION gated through the 4-tier validate gate; v1.2↔v1.3 bidirectional cross-version proof GREEN with honest documented-deltas wording and a machine-checked non-blind filter.
- RT-04 (proof half): warnings 双形 demonstrated in fixture (1 string + 2 structured) + all three degrade codes proven schema-expressible; roundtrip-absent degrade already proven in 18-01 smoke.
- No regression: passes (a)-(d), old fixture tiers, self-test, pytest all green.
</success_criteria>

<output>
Create `.planning/phases/18-contract-v1-3/18-02-SUMMARY.md` when done.
</output>
