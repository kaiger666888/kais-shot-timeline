---
phase: 18-contract-v1-3
plan: 03
type: execute
wave: 3
depends_on: [18-01, 18-02]
files_modified:
  - spec/SPEC.md
  - spec/README.md
autonomous: false
requirements: [RT-03]
tags: [docs, spec, prose, fidelity-disclaimer, changelog]

must_haves:
  truths:
    - "`spec/SPEC.md` §4 Changelog has a `1.3` entry (dated 2026-08-19, Phases 18-22) that honestly records the TWO non-pure-property deltas — `generator.warnings.items` 加宽 (string | {code, detail}) 与 `data.roundtrip` object 挂载 — 对旧数据 additive 但非 property-delta"
    - "`spec/SPEC.md` §1 lists 14 schema files (heading + 表内计数 + TS note) with a roundtrip.schema.json row; header Version line says 1.3 and Status line mentions Phase 18"
    - "`spec/SPEC.md` has a `### Round-trip (\\`1.3\\`)` shape section (after Speakers) following the 4-block template: Producer/Consumers 行 → 顶层形状段 → 字段表 (Field/Type/Required/Notes, enum 值与 schema 逐字一致) → 最小片段 (摘自 spec/fixtures/v1.3/roundtrip.json) → Reference schema 行"
    - "`spec/SPEC.md` §10 carries the v1.3 three-layer fidelity disclaimer: ① accepted = 「h3 可复现」≠「prompt 完美」(fl2va 条件渲染幸存者偏差) ② rejected 是 hard negatives + h3 能力边界测绘、非垃圾 ③ judge attribution 是模型判断带 confidence 不是 ground truth"
    - "阈值数值只出现在 SPEC 散文 (§5/§10, 标注 Phase 21 校准后才存在) — schema 与本 phase 交付物零阈值"
    - "`spec/README.md` has a v1.3 update section (schemas + asset.schema 两处诚实 delta + SCHEMA_VERSION 单源位置【grep 复核行号】+ 13-file fixture + 双向证明话术) and a `*Updated: … (Phase 18 — v1.3 additive extension: …)*` footer line; layout diagram counts 13→14 + roundtrip.schema.json + fixtures/v1.3/ 行"
    - "NOWHERE in SPEC.md or README.md do the AF-01 forbidden phrases appear (完美复刻 / 精确复原 / perfectly reconstruct / exact restoration / 完美重建 / 绝对复现)"
    - "一次人类审阅通过 (ROADMAP SC#5): 用户读过 §4/§5.10/§10 v1.3 变更 diff 后 approve"
  artifacts:
    - path: "spec/SPEC.md"
      provides: "Human-readable contract extended with v1.3 changelog + Round-trip shape + three-layer fidelity disclaimer"
      contains: "Round-trip"
    - path: "spec/README.md"
      provides: "Schema layout overview + v1.3 update section + footer"
      contains: "v1.3"
  key_links:
    - from: "spec/SPEC.md §4 Changelog 1.3 entry"
      to: "scripts/export_asset.py (SCHEMA_VERSION literal)"
      via: "prose reference to single-source location (line number grep-verified at write time)"
      pattern: "export_asset\\.py"
    - from: "spec/SPEC.md Round-trip section"
      to: "spec/schemas/roundtrip.schema.json"
      via: "Reference schema footer line"
      pattern: "roundtrip\\.schema\\.json"
    - from: "spec/SPEC.md Round-trip 最小片段"
      to: "spec/fixtures/v1.3/roundtrip.json"
      via: "excerpt (摘自 fixture)"
      pattern: "roundtrip\\.json"
---

<objective>
Document the v1.3 contract in human-readable prose: SPEC.md §1 schema-layout bump (13→14), §4 Changelog `1.3` entry, §5 Round-trip shape section (4-block template), §10 v1.3 three-layer fidelity disclaimer, header Version/Status, footer; plus README.md v1.3 update section + footer. Then ONE human review pass (ROADMAP SC#5: 「一次人类审阅通过」).

Purpose: RT-03 — the two-tier authority human half. The schemas (18-01) are machine truth; this prose is the 15-minute overview. The fidelity disclaimer is a first-class deliverable: it prevents dataset consumers from over-trusting accepted verdicts (accepted = h3-reproducible, NOT prompt-perfect) and frames rejected entries as hard negatives, not garbage. The changelog must honestly record that v1.3 contains the first items-widening and first object-mount in v1.x history (对旧数据 additive 但非 property-delta) — 这是不盲目 copy Phase 11 changelog 模板的关键差异.

Output: Extended spec/SPEC.md + spec/README.md + human approval. No code, no schemas, no fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-contract-v1-3/18-CONTEXT.md
@.planning/phases/18-contract-v1-3/18-RESEARCH.md
@.planning/phases/18-contract-v1-3/18-01-SUMMARY.md
@.planning/phases/18-contract-v1-3/18-02-SUMMARY.md

<interfaces>
<!-- Existing SPEC.md / README.md structures the executor extends. Line numbers verified 2026-08-19. -->

spec/SPEC.md (728 lines):
  Header: line 3 Version (currently stale "1.1 (active…)" — bump to 1.3 and fix), line 6 Status (append Phase 18)
  §1 (lines 12-43): authority table line 18 "(13 份)", heading line 23 "### 13 个 schema 文件(按 filename 索引)", table rows lines 25-38 (append roundtrip row after speaker-edits line 38), TS note line 40 "13 个 schema"
  §4 Changelog (line 160): entries `1` / `1.1` / `1.2` (v1.2 entry lines 173-183 — the structural template: 日期—版本—纯增量声明 → 新 schema bullet → asset.schema additive bullet → schema_version pattern 不变 + 单源位置 bullet → phase-informed deviations → 向后兼容 bullet)
  §5 (line 187 起): intro blockquote lines 191-193 (append v1.3 sentence); shape sections are `### <Name> (\\`<ver>\\`)` style — §5.8 Audio Semantic at line 439, §5.9 Speakers at line 503 (ends ~line 539) — insert `### Round-trip (\\`1.3\\`)` after it
  4-block shape template (from §5.8): **Producer:**/**Consumers:** lines → **顶层形状:** paragraph → | Field | Type | Required | Notes | table → **最小片段**(摘自 fixture…) JSON block → "Reference schema:" line
  §10 Fidelity Disclaimer (`1.2` — AF-01…) lines 664-723 with subsections 10.1-10.4 — v1.3 layers append as new subsections (10.5+ or equivalent)
  Footer lines 726-728: append v1.3 line after the Phase 11 line

spec/README.md (122 lines):
  Layout diagram line 11 "(13 个机可校验的 JSON Schema 文件…" → 14 + add roundtrip.schema.json line + spec/fixtures/v1.3/ line in the tree
  v1.2 Update section lines 80-111 — structural template for the v1.3 section (insert after it, before "## Origin / Provenance")
  Footer lines 121-122: Created + Updated (Phase 05) — append a new Updated (Phase 18) line

Facts from 18-01/18-02 (cite these; grep-verify line numbers before writing them into prose):
  14th schema = spec/schemas/roundtrip.schema.json; asset.schema.json two deltas (data.roundtrip object {path, accepted_count, rejected_count}; warnings items anyOf string|{code enum(3), detail}); SCHEMA_VERSION = "1.3" single source in scripts/export_asset.py (line 56 — grep at write time); 13-file fixture at spec/fixtures/v1.3/ (11 byte-copied + 2 authored); 4-tier validate gate; verify_contract v1.0↔v1.1↔v1.2↔v1.3 bidirectional proof with documented-deltas wording; warnings codes comfyui_unreachable/vram_insufficient/scorer_model_missing; attribution enum prompt_faithful/model_diverged/prompt_underspecified; verdict decision accepted/rejected + source auto/human; regen 5-tuple (path/video_content_hash/engine_name/engine_version/prompt_version) + optional duration_sec/width/height; status {state: failed, error}; shots[] = 结果集 (未尝试 = 缺席).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SPEC.md — §1 bump + §4 Changelog 1.3 + §5 Round-trip section + §10 v1.3 三层 disclaimer + header/footer</name>
  <files>spec/SPEC.md</files>
  <read_first>
    - `spec/SPEC.md` (FULL — esp. header lines 1-8; §1 lines 12-43; §4 Changelog lines 160-186 with the `1.2` entry as literal template; §5 intro blockquote lines 191-193; §5.8 Audio Semantic lines 439-501 as the 4-block template; §10 lines 664-723; footer lines 726-728)
    - `spec/schemas/roundtrip.schema.json` (18-01 output — source of truth for the field table; enum 值逐字对齐)
    - `spec/schemas/asset.schema.json` (18-01 output — data.roundtrip mount + warnings 双形描述)
    - `spec/fixtures/v1.3/roundtrip.json` + `spec/fixtures/v1.3/asset.json` (18-02 output — 最小片段的摘录来源)
    - `.planning/phases/18-contract-v1-3/18-CONTEXT.md` §SPEC/fixture/gate 交付物 (fidelity disclaimer 三层 + 阈值不进 schema 锁定)
    - `grep -n 'SCHEMA_VERSION' scripts/export_asset.py` (run it — cite the verified line number, do not copy the stale line 55 from the v1.2 README section)
  </read_first>
  <behavior>
    - §1: heading "14 个 schema 文件(按 filename 索引)"; authority table "(14 份)"; TS note "14 个 schema"; new table row for roundtrip.schema.json (顶层对象 — per-shot h3 重生成 ref + scores{midframe_sim, judge} + verdict;**`1.3`** style matching siblings).
    - Header: Version line → 1.3 (active; `schema_version: "1.3"`) — also fix the stale v1.1 remnant; Status line appends Phase 18 v1.3 additive extension (2026-08-19).
    - §4 Changelog `1.3` entry (after the `1.2` entry): mirror the v1.2 entry structure with these bullets — 1 个新 schema (roundtrip.schema.json); `asset.schema.json` additive with the TWO honest deltas called out (warnings items 加宽 + data.roundtrip 首个 object 挂载 — 对旧数据 additive 但非 property-delta,backward 证明过滤规则同步扩展); schema_version pattern 不变 + 字面量单源位置 (grep-verified line); CONTEXT-locked decisions 段 (attribution closed enum / midframe_sim 必带 model / judge 无连续分 / shots[] 结果集语义 no pending/rendering / fps-seed-workflow 不收); 向后兼容 bullet citing validate.py 四阶 + verify_contract v1.0↔v1.1↔v1.2↔v1.3 双向证明 (backward 0 non-additive excluding documented v1.3 deltas) + 11 substrate 文件 byte-identical.
    - §5 intro: append sentence "v1.3 新增 §5.10 **Round-trip**(optional — 仅 Phase 20/21 regen+scoring 往返后 emit)".
    - `### Round-trip (\\`1.3\\`)` section, 4-block template: Producer = Phase 20 (h3 复现客户端) + Phase 21 (scorer/verdict 合并) — 均 pending, mirror v1.2 当时的 pending 措辞; Consumers = Phase 22 (gallery round-trip 审阅面板 + dataset 导出) — pending; 顶层形状段 (schema_version + shots[] 结果集语义 — 只有有产物的 shot,失败=status{state:failed},未尝试=缺席); field table covering schema_version / shots[].shot_id / regen.{path, video_content_hash, engine_name, engine_version, prompt_version, duration_sec?, width?, height?} / status.{state, error} / scores.midframe_sim.{score, model} / scores.judge.{attribution, confidence, reason} / verdict.{decision, source, decided_at?} — enum 值与 schema 逐字一致, Notes 列注明 regen 互斥、midframe model 标识理由、verdict.source=human 是 PRESENT-01 HITL 覆盖; 最小片段 = 摘自 spec/fixtures/v1.3/roundtrip.json (2-shot 简化); Reference schema: spec/schemas/roundtrip.schema.json. 加一段: judge.reason / status.error 是模型/引擎产出文本,Phase 22 HTML 渲染 MUST `_esc()` (PRESENT-01 XSS 硬化义务预告) + asset.json#data.roundtrip object 挂载说明 (path + accepted_count/rejected_count 统计,首个 object 值挂载).
    - §10 v1.3 三层 disclaimer (new subsections after 10.4): ① accepted = 「h3 可复现」≠「prompt 完美」— fl2va 首尾帧条件渲染的幸存者偏差,prompt 的未渲染维度不可由此 verdict 证伪; ② rejected = hard negatives + h3 能力边界测绘数据,非垃圾 — SCORE-03 的 rejected 占比审计依赖此语义; ③ judge attribution 是模型判断带 confidence,非 ground truth — 三分类是自有分类学,confidence 未校准. 附: accepted 双阈值 (midframe_sim + judge) 由 Phase 21 ep01 ≤20 镜抽样校准后锁定 — 校准前 SPEC 不给数值 (two-tier authority: 阈值是散文+校准报告,永不进 schema).
    - Footer: append "* 2026-08-19 (Phase 18 Plan 03 — v1.3 additive extension: §4 Changelog `1.3` + §5.10 Round-trip + §10 v1.3 三层 Fidelity Disclaimer). schema_version \\"1.3\\".*"
    - AF-01 forbidden phrases absent everywhere (including the new prose).
  </behavior>
  <action>
    Seven edits per <behavior>, using the `1.2` Changelog entry (lines 173-183) and §5.8 (lines 439-501) as literal structural templates. Before writing the 单源位置 citation, run `grep -n 'SCHEMA_VERSION = ' scripts/export_asset.py` and cite the actual line number (the v1.2 README's "line 55" is stale — PATTERNS warns about this). In the field table Notes column, keep threshold talk out (「阈值见 §10 / Phase 21」pointer only). Write the 最小片段 by excerpting the actual fixture file (18-02 output) — values must match it byte-for-byte in structure. Do NOT renumber existing sections; the new shape section lands after Speakers with heading style `### Round-trip (\\`1.3\\`)` and prose may refer to it as §5.10 (mirror how v1.2 prose refers to §5.8/§5.9).
  </action>
  <verify>
    <automated>python3 - <<'PY'
import re
spec = open('spec/SPEC.md', encoding='utf-8').read()
assert re.search(r'### 14 个 schema 文件', spec), '§1 heading not bumped to 14'
assert 'roundtrip.schema.json' in spec
assert re.search(r'- \*\*2026-08-19 — `1\.3`', spec) or re.search(r'`1\.3`\(v1\.3 additive', spec), '§4 Changelog 1.3 entry missing'
assert '### Round-trip (`1.3`)' in spec, 'Round-trip shape section missing'
for must in ['prompt_faithful', 'model_diverged', 'prompt_underspecified', 'comfyui_unreachable',
             'vram_insufficient', 'scorer_model_missing', 'accepted_count', 'rejected_count',
             'hard negatives', '幸存者偏差', 'video_content_hash', 'decided_at']:
    assert must in spec, f'missing: {must}'
assert re.search(r'_hard.?escaped|_esc\(\)', spec) or '_esc()' in spec, 'XSS 预告 missing'
for forbidden in ['完美复刻', '精确复原', 'perfectly reconstruct', 'exact restoration', '完美重建', '绝对复现']:
    assert forbidden not in spec, f'AF-01 forbidden phrase present: {forbidden}'
assert 'export_asset.py' in spec
# fixture excerpt consistency: fixture shot 1 judge attribution appears in the 最小片段 block
rt = open('spec/fixtures/v1.3/roundtrip.json', encoding='utf-8').read()
m = re.search(r'### Round-trip \(`1\.3`\)(.*?)(?=### |\n## )', spec, re.S)
assert m and ('roundtrip/shot_001.mp4' in m.group(1)) and ('clip-vit' in m.group(1) or 'midframe_sim' in m.group(1)), '最小片段 not fixture-consistent'
print('[spec-ok] §1 14 + §4 1.3 + Round-trip section + §10 三层 + AF-01 clean + fixture-consistent excerpt')
PY
python3 spec/validate.py >/dev/null 2>&1 && echo "validate GREEN" || echo "validate FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Round-trip' spec/SPEC.md` returns ≥3 (§5 intro mention + section heading + changelog/footer references).
    - `grep -c 'prompt_faithful\|model_diverged\|prompt_underspecified' spec/SPEC.md` returns ≥3 (changelog + field table + §10).
    - `grep -c 'comfyui_unreachable' spec/SPEC.md` returns ≥1 (changelog or field table).
    - `grep -c '完美复刻\|精确复原\|perfectly reconstruct\|exact restoration\|完美重建\|绝对复现' spec/SPEC.md` returns 0.
    - `head -6 spec/SPEC.md` shows Version 1.3 and a Status line mentioning Phase 18.
    - The 单源位置 line number cited in §4 matches `grep -n 'SCHEMA_VERSION = ' scripts/export_asset.py` output at review time.
    - The 最小片段 block is structurally identical to spec/fixtures/v1.3/roundtrip.json (field names + enum values match; automated check passes).
    - `python3 spec/validate.py` still exits 0 (prose edits regress nothing).
  </acceptance_criteria>
  <done>
    SPEC.md at v1.3: §1 counts 14 schemas with roundtrip row; §4 Changelog honestly records the two non-property deltas; Round-trip shape section follows the 4-block template with fixture-excerpted 最小片段; §10 carries the three-layer fidelity disclaimer + Phase 21 threshold pointer; header/footer updated; AF-01 clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: spec/README.md — v1.3 update section + layout counts + footer</name>
  <files>spec/README.md</files>
  <read_first>
    - `spec/README.md` (FULL — esp. layout diagram line 11 with the "13 个" count + tree; v1.2 Update section lines 80-111 as the literal template; footer lines 121-122)
    - `spec/SPEC.md` (post-Task-1 — cross-reference consistency)
  </read_first>
  <behavior>
    - Layout diagram: "13 个" → "14 个"; tree gains `roundtrip.schema.json` under schemas/ and `v1.3/` under fixtures/ (mirror existing fixture-dir lines).
    - New `## v1.3 Update (Phase 18, 2026-08-19)` section inserted after the v1.2 section, before `## Origin / Provenance`, mirroring the v1.2 section structure: contract bumped to `1.3`; **1 个新 schema** (roundtrip.schema.json 一句话); **`asset.schema.json` 两处 delta(诚实记录)** — optional `data.roundtrip` object `{path, accepted_count, rejected_count}` (v1.x 首个 object 值挂载) + `generator.warnings.items` 加宽 `string | {code, detail}` with code enum `{comfyui_unreachable, vram_insufficient, scorer_model_missing}` (v1.x 首个 items 类型加宽;对旧数据 additive); **`SCHEMA_VERSION = \"1.3\"` 单一真源** at scripts/export_asset.py (grep-verified line number); **CONTEXT-locked decisions** bullets (attribution closed enum 三分类 / midframe_sim 必带 model 标识 / judge 无连续分 / shots[] 结果集语义 — 无 pending/rendering / regen 元数据 minimal — fps/seed/workflow 不收); **13-file fixture** (11 byte-copied from v1.2 + asset.json 编辑 + roundtrip.json 新增); **双向证明** (validate.py 四阶 gate + verify_contract v1.0↔v1.1↔v1.2↔v1.3 forward 0 errors / backward 0 non-additive excluding documented v1.3 deltas); pointer line to SPEC §4/§5.10/§10.
    - Footer: append `*Updated: 2026-08-19 (Phase 18 — v1.3 additive extension: +1 schema (roundtrip), asset.schema data.roundtrip object mount + warnings items widening, 13-file v1.3 fixture, validate.py 4-tier gate, verify_contract 4-way bidirectional proof)*`.
    - AF-01 forbidden phrases absent.
  </behavior>
  <action>
    Three edits per <behavior>, using the v1.2 Update section (lines 80-111) as the literal structural template. Cite the grep-verified SCHEMA_VERSION line number (not the stale "line 55" the v1.2 section carries). Keep the section compact — README is the index, SPEC carries the prose.
  </action>
  <verify>
    <automated>python3 - <<'PY'
r = open('spec/README.md', encoding='utf-8').read()
assert '## v1.3 Update (Phase 18, 2026-08-19)' in r
assert 'roundtrip.schema.json' in r and 'v1.3' in r
assert 'comfyui_unreachable' in r
assert 'accepted_count' in r
assert '13-file' in r or '13 文件' in r
assert r.count('14 个') >= 1, 'layout schema count not bumped to 14'
assert 'Updated: 2026-08-19 (Phase 18' in r
for forbidden in ['完美复刻', '精确复原', 'perfectly reconstruct', 'exact restoration', '完美重建', '绝对复现']:
    assert forbidden not in r, f'AF-01 forbidden phrase: {forbidden}'
print('[readme-ok] v1.3 section + layout 14 + footer + AF-01 clean')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'v1.3' spec/README.md` returns ≥5 (section header + cross-references + footer).
    - `grep -c 'roundtrip.schema.json' spec/README.md` returns ≥2 (layout tree + update section).
    - `grep -c '完美复刻\|精确复原\|perfectly reconstruct\|exact restoration\|完美重建\|绝对复现' spec/README.md` returns 0.
    - `tail -3 spec/README.md` shows the new Phase 18 Updated footer line.
    - Layout diagram contains `fixtures/v1.3/` (or equivalent v1.3 fixture line).
  </acceptance_criteria>
  <done>
    README.md extended with the v1.3 update section (honest two-delta record + single-source citation + fixture/proof summary), layout bumped to 14 schemas + v1.3 fixture dir, Phase 18 footer appended, AF-01 clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of SPEC v1.3 changes (RT-03 「一次人类审阅通过」)</name>
  <files>spec/SPEC.md, spec/README.md</files>
  <read_first>
    - `git diff spec/SPEC.md spec/README.md` (the full v1.3 prose delta this plan produced)
    - `.planning/phases/18-contract-v1-3/18-CONTEXT.md` §SPEC/fixture/gate 交付物 (disclaimer 三层 + 阈值散文锁定的验收措辞)
  </read_first>
  <what-built>
    SPEC.md v1.3 prose layer (§1 14-schema layout, §4 Changelog `1.3` entry with honest two-delta record, §5.10 Round-trip shape section with fixture-excerpted 最小片段, §10 three-layer fidelity disclaimer, header/footer) + README.md v1.3 section/footer. All automated gates green (validate.py 4-tier, verify_contract producer mode, AF-01 grep 0).
  </what-built>
  <how-to-verify>
    1. `git diff spec/SPEC.md` — review the §4 `1.3` entry: the two non-pure-property deltas (warnings items 加宽 + data.roundtrip object 挂载) are recorded honestly, CONTEXT-locked decisions bullets match the schema (18-01), 向后兼容 bullet matches actual proof output (18-02).
    2. Read the `### Round-trip (`1.3`)` section — field table enums match `spec/schemas/roundtrip.schema.json` verbatim; 最小片段 matches `spec/fixtures/v1.3/roundtrip.json`; the Phase 22 `_esc()` obligation preview is present.
    3. Read the §10 v1.3 three-layer disclaimer — ① accepted = 「h3 可复现」≠「prompt 完美」② rejected = hard negatives 非垃圾 ③ attribution = 模型判断带 confidence 非 ground truth; thresholds explicitly deferred to Phase 21 校准 (no numbers anywhere).
    4. Confirm no absolute-reconstruction phrasing anywhere (`grep -c '完美复刻\|精确复原\|perfectly reconstruct\|exact restoration\|完美重建\|绝对复现' spec/SPEC.md spec/README.md` → 0).
    5. Optional: `python3 spec/validate.py && python3 scripts/verify_contract.py --mode=producer` — both exit 0.
  </how-to-verify>
  <verify>
    <human-check>Review steps 1-4 above; approve or list wording issues for Task 1/2 revision.</human-check>
  </verify>
  <acceptance_criteria>
    - User replies "approved" (or equivalent) — RT-03's 「一次人类审阅通过」success criterion is met.
    - Any wording issues raised are fixed in SPEC.md/README.md and the AF-01 + fixture-consistency greps re-run green before plan close.
  </acceptance_criteria>
  <action>Pause execution and present the Task 3 how-to-verify steps (git diff of spec/SPEC.md + spec/README.md, the three-layer disclaimer wording, the AF-01 grep, and the optional gate re-runs) to the user. This is a review-only checkpoint: Claude makes NO further edits unless the user reports wording issues, in which case fix them in Task 1/2 scope and re-run the AF-01 + fixture-consistency + validate/verify gates before re-presenting. Record the approval outcome (verbatim "approved" or issue list + resolution) in 18-03-SUMMARY.md.</action>
  <done>User has replied "approved" (or all reported wording issues are fixed and gates re-run green) — RT-03's 「一次人类审阅通过」 criterion is satisfied and recorded in the SUMMARY.</done>
  <resume-signal>Type "approved" or describe the wording issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPEC prose → human reader | the prose is the human half of two-tier authority — dataset consumers and future contributors read it to decide how much to trust accepted/rejected verdicts; over-claiming here propagates directly into dataset misuse |
| SPEC → schema | on conflict, schema wins; prose that mis-describes the schema (wrong enum, wrong required set, invented thresholds) is a documentation bug that silently misleads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09 | Information Disclosure (false advertising) | SPEC/README over-claim round-trip fidelity (绝对化复现措辞) | mitigate | Task 1/2 acceptance: grep 0 matches for the six forbidden phrases; §10 layer ① states accepted = h3-reproducible ≠ prompt-perfect explicitly (AF-01 invariant carried from v1.2 §10). |
| T-18-10 | Repudiation | prose drifts from schema (wrong enum values, wrong required fields, stale line numbers) | mitigate | Field table sourced from roundtrip.schema.json (read_first); 最小片段 machine-checked against the actual fixture; SCHEMA_VERSION line number grep-verified at write time (PATTERNS flags the stale v1.2 "line 55" precedent); human review checkpoint is the final gate. |
| T-18-11 | Information Disclosure (consumer over-trust) | dataset consumer treats judge attribution as ground truth or rejected entries as garbage | mitigate | §10 layers ②③ state rejected = hard negatives + capability mapping (non-garbage) and attribution = model judgment with uncalibrated confidence; thresholds explicitly deferred to Phase 21 (two-tier authority restated). |
| T-18-SC | Tampering | package installs | accept (carried) | Zero installs this plan. |
</threat_model>

<verification>
After all 3 tasks:

1. Task 1 + Task 2 automated checks pass (`[spec-ok] …` / `[readme-ok] …`).
2. AF-01: `grep -c '完美复刻\|精确复原\|perfectly reconstruct\|exact restoration\|完美重建\|绝对复现' spec/SPEC.md spec/README.md` returns 0 for both files.
3. `python3 spec/validate.py && python3 scripts/verify_contract.py --mode=producer` both exit 0 (prose edits regressed nothing — 18-02 gates still green).
4. Human review approved (Task 3) — RT-03 「一次人类审阅通过」satisfied.
</verification>

<success_criteria>
- RT-03 fully covered: §4 changelog 1.2→1.3 (honest two-delta record) + §5.10 Round-trip 形状文档 (4-block template, fixture-consistent) + §10 三层 fidelity disclaimer (accepted≠perfect / rejected=hard-negatives / attribution=model-judgment) + §1/README/footer 同步 + 一次人类审阅通过.
- Two-tier authority honored: thresholds exist only as §5/§10 prose with a Phase 21 pointer — never as numbers, never in schema.
- AF-01 invariant holds across both files.
</success_criteria>

<output>
Create `.planning/phases/18-contract-v1-3/18-03-SUMMARY.md` when done.
</output>
