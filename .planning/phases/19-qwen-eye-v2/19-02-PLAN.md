---
phase: 19-qwen-eye-v2
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - spike/vision_seq/README.md
  - spike/vision_seq/build_sandbox.py
  - spike/vision_seq/run_spike.py
  - .planning/research/vision-seq-spike-report.md
  - analysis/vision_seq_facets.py
autonomous: false
requirements: [VISION-01, VISION-02]
must_haves:
  truths:
    - "spike 在 sandbox（facet 置空副本，非 live 目录）完成 GPU 双跑：no-ear 6 镜 + ear 3 镜，cache 存 RAW 逐帧/逐对答案且可断点续跑"
    - "三策略（A 时序拼接 / B LLM 二次合并 / baseline 最长）同镜产物 + 客观辅助指标 + 匿名盲评表落 .planning/research/vision-seq-spike-report.md"
    - "Kai 盲评 checkpoint 后合并策略锁定为模块默认值，报告去 DRAFT 定稿"
    - "live ep01 prompts.json 全程 sha256 前后不变（SC1 不覆盖负测试，命令与哈希记录在报告）"
    - "ear on/off 同镜 diff 可见（SC3）；三策略共用同一份 RAW cache 证据（换策略零 GPU）"
  artifacts:
    - path: "spike/vision_seq/README.md"
      provides: "THROWAWAY 声明 + 交付物指向"
      contains: ["THROWAWAY"]
    - path: ".planning/research/vision-seq-spike-report.md"
      provides: "spike 证据 + 锁定结论 + 样例摘录（mirror audio-spike-report 先例）"
      min_lines: 120
    - path: "spike/vision_seq/sandbox/prompts.json"
      provides: "facet 置空副本（v2 填充对象）"
  key_links:
    - from: "spike/vision_seq/run_spike.py"
      to: "analysis/vision_seq_facets.py"
      via: "subprocess 按路径调用（--merge-strategy/--no-ear/--audio-semantic）"
      pattern: "vision_seq_facets.py"
    - from: "spike/vision_seq/sandbox/route_cache/vision_seq/"
      to: "断点续跑"
      via: "重跑同命令 → 已完成镜 cache 命中跳过"
      pattern: "shot_"
---

<objective>
ep01 小样本 spike（≤10 镜，实选 6 镜）：在 sandbox 副本目录真实烧 GPU 逐帧/逐对问答，产出 A/B/baseline 三策略同镜产物与客观辅助指标，ear on/off 双跑 diff，落 `.planning/research/vision-seq-spike-report.md`；Kai 人工并排盲评（blocking checkpoint）后锁定合并策略为模块默认值。

Purpose: Q3 27B 帧序列动作描述质量是 research Pitfall 3 明示的未验证假设（ROADMAP SC2）；机器指标循环论证，盲评是主判据。SC1 的「diff 可见」因 live 集 0% 空缺只能在 sandbox 实证（Pitfall 1），live 文件 sha 不变本身就是不覆盖的最强证明。
Output: spike 脚手架（THROWAWAY）+ sandbox 双跑 cache + 三策略产物 + 定稿 spike report + 模块默认策略锁定。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-qwen-eye-v2/19-CONTEXT.md
@.planning/phases/19-qwen-eye-v2/19-RESEARCH.md
@.planning/phases/19-qwen-eye-v2/19-PATTERNS.md
@.planning/phases/19-qwen-eye-v2/19-01-SUMMARY.md

<interfaces>
From 19-01（本 plan 消费的被测模块）:
```
python3 analysis/vision_seq_facets.py --shots <shots.json> --frames-dir <frames_5fps/> \
  --work-dir <work_dir> --output <prompts.json> --video <video> \
  [--audio-semantic <audio_semantic.json>] [--no-ear] [--merge-strategy {temporal,llm,longest}]
```
- cache 落 `<work-dir>/route_cache/vision_seq/shot_XXX.json`（ear_on/ear_off 双信封存 RAW answers + merged_B）
- 重跑同命令：已完成镜 cache 命中跳过（断点续跑零重烧）
- 全 cache 命中时不启动引擎（预判生命周期）；模块 --help 可查全部 flags

ep01 实测数据（RESEARCH §ep01 数据盘点，2026-08-19 probe）:
- work_dir: `output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/`（目录名含全角括号与中文冒号 —— 脚本内一律 pathlib.Path，禁 shell 字符串拼接）
- shots.json 93 镜；frames_5fps f000001-f001542.jpg（+11 张 _ds1280 变体须忽略）；prompts.json 为 93 元素 list，action/camera 空缺率 0/93
- transcript.json 155 段真实对白（demo audio 构建用）
- spike 候选镜（研究扫出，实选此 6 个）：#1（6.7s 递浆果动作链，v1 样例镜）、#46（5.7s 蜈蚣猛扑快动作）、#66（6.9s 低角度拉升）、#70（19.7s 最长镜长动作链）、#88（13.0s 推镜+低头闭目）、#91（15.1s 大特写拉远至大远景）
- ear 子集（3 镜）：#1、#88、#91
- 引擎 :8125 研究时 down（GPU1 free 22539MiB ≥ 14000 门槛）；首跑由模块 ensure_ready 自动拉起，执行前复探 :8125/:10588

audio_semantic.json schema 最低要求（RESEARCH 已验证）: 顶层 `schema_version` + `shots[].shot_id`；shot 结构参照 spec/fixtures/v1.2/audio_semantic.json。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: spike 脚手架（THROWAWAY README + sandbox 构建 + 双跑脚本）</name>
  <files>spike/vision_seq/README.md, spike/vision_seq/build_sandbox.py, spike/vision_seq/run_spike.py</files>
  <read_first>
  - spike/audio/README.md（THROWAWAY banner 先例原文，含 :71-72 全角括号路径警告）
  - tests/test_local_vision_facets.py:36-58（make_workdir —— shots+prompts+假帧三件套最小构造写法，放大到 sandbox 规模）
  - spec/fixtures/v1.2/audio_semantic.json 与 spec/schemas/audio_semantic.schema.json（demo 构建的结构与校验目标）
  - analysis/vision_seq_facets.py（19-01 产物的 CLI 与 cache 契约）
  - ep01 work_dir 实况（shots.json / transcript.json / frames_5fps/）
  </read_first>
  <action>
  1. `spike/vision_seq/README.md`：mirror spike/audio 的 THROWAWAY banner（「不要 import 进 run_pipeline / 不接 analysis/* 客户端；只服务 Phase 19 spike，最终交付物是 .planning/research/vision-seq-spike-report.md」）+ sandbox 布局说明 + 全角括号路径 pathlib 警告。
  2. `build_sandbox.py`（离线、幂等、只读 live 只写 spike/vision_seq/ 下——脚本内 assert 输出路径前缀）：
     - `sandbox/`：复制 ep01 shots.json 与 prompts.json（副本），把 shot id ∈ {1,46,66,70,88,91} 六镜的 action 与 camera 置 ""；symlink frames_5fps 与原视频（video_content_hash 需要真视频）；生成 demo 级 audio_semantic.json：schema_version + 六镜 shots[]（shot_id/start_sec/end_sec 从 shots.json 取），dialogue.text 用 transcript.json 落在该镜时窗内的真实段落文本（截断 200 字），sfx 手工示范——#91 events=["雨声"] description="持续的中雨声，伴随远雷"、#1 events=["脚步声","鸟鸣"] description="由远及近的脚步声"、#88 events=["呼吸声","衣料摩擦"] description="紧张的近距离呼吸与衣料摩擦"；产物过 Draft202012Validator(audio_semantic.schema.json) 自检后原子写。
     - `sandbox_ear/`：同构但只置空 {1,88,91} 三镜（ear 双跑子集，控预算）。
     - `--reset` 模式：从 live 副本重新置空既有 sandbox 的 facets（不动 sandbox 的 route_cache）——供 19-03 复用。
  3. `run_spike.py`：subprocess 按路径调 analysis/vision_seq_facets.py，四步序列（每步独立 CLI 调用、进度打印 mirror [vision-seq] 前缀、日志 tee 到 spike/vision_seq/results/run.log）：
     a. `sandbox/ --no-ear`（6 镜 × ~15 calls RAW burn）
     b. `sandbox_ear/` 带 --audio-semantic（3 镜 × ~15 calls，ear=true 信封）
     c. `sandbox/ --merge-strategy llm`（B 合并：2 ask_text/镜，merged_B 落 cache；sandbox prompts.json 已被 a 填过则用 --reset 先重置再跑，确保三策略从同一 RAW 证据出发产出可对比文本）
     d. 产物导出：从 cache RAW answers + merged_B 生成三策略文本对比（temporal/longest 纯 Python 归约，零引擎）→ results/ 下 per-shot markdown 片段
     设计为 tmux 后台可跑：无交互输入、每步幂等（cache 断点续跑）、中断后重跑同命令即续。
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-shot-timeline && python3 spike/vision_seq/build_sandbox.py && python3 -c "
import json, pathlib
sb = pathlib.Path('spike/vision_seq/sandbox')
p = json.load(open(sb/'prompts.json'))
blank = [s['shot_id'] if isinstance(s, dict) and 'shot_id' in s else i for i, s in enumerate(p) if not s.get('action') and not s.get('camera')]
assert len(blank) == 6, blank
a = json.load(open(sb/'audio_semantic.json'))
assert a['schema_version'] and all('shot_id' in sh for sh in a['shots'])
" && grep -q THROWAWAY spike/vision_seq/README.md && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - sandbox/ 与 sandbox_ear/ 存在：prompts.json 副本分别恰有 6 / 3 镜 action=="" 且 camera==""；其余 87/90 镜与 live 逐字节同值
  - sandbox/audio_semantic.json 过 schema 校验且 #91 含「雨声」、#1 含「脚步声」示范事件
  - frames_5fps 与视频为 symlink（不复制大文件）；build 脚本零 live 写入（git status 显示 output/ 无变化）
  - grep -c "THROWAWAY" spike/vision_seq/README.md ≥ 1；python3 -m pytest tests/ -q 仍全绿
  </acceptance_criteria>
  <done>spike 脚手架就绪：sandbox 双目录 + demo audio + 可后台断点续跑的四步 run 脚本，live 目录零污染。</done>
</task>

<task type="auto">
  <name>Task 2: GPU 双跑（tmux 后台）+ 三策略产物 + 客观指标 + 报告 DRAFT</name>
  <files>.planning/research/vision-seq-spike-report.md, spike/vision_seq/results/run.log</files>
  <read_first>
  - .planning/phases/19-qwen-eye-v2/19-PATTERNS.md §「vision-seq-spike-report.md」（audio-spike-report 章节映射表：头部元数据/Methodology/分节 evidence/Recommendations/Reproducibility）
  - .planning/phases/19-qwen-eye-v2/19-RESEARCH.md §Pitfall 4（反循环论证声明）、§Pitfall 6（调用量预算 ~135-150 calls，A1 估 1-3h）、§Open Questions Q2（tmux 后台建议）
  - spike/vision_seq/README.md 与 run_spike.py（Task 1 产物）
  </read_first>
  <action>
  1. 执行前复探：`curl -s -m 3 http://127.0.0.1:8125/health || true` 与 `nvidia-smi --query-gpu=index,memory.free --format=csv`；GPU1 free < 14000MiB 则停止并作为 blocker 上报（不强行拉起）。
  2. tmux 后台跑：`tmux new-session -d -s vision_seq_spike 'python3 spike/vision_seq/run_spike.py 2>&1 | tee -a spike/vision_seq/results/run.log'`；用 cache 文件增量（`ls sandbox/route_cache/vision_seq/shot_*.json | wc -l`）与 run.log 监控进度；中断/超时重跑同命令即续（cache 幂等）。预估 1-3h [ASSUMED]，期间不阻塞本 plan 其他读作类工作（报告骨架可先写）。
  3. 烧完后产出对比材料（全部从 cache RAW + merged_B 归约，除 b 步已烧的 llm 合并外零额外 GPU）：
     - 三策略同镜文本：6 镜 × {temporal, llm, longest} + v1 现行值参照（live prompts.json 的 action/camera 原文）并排。
     - 客观辅助指标（Claude's discretion 区，按此实现）：时序连接词密度（答案中 然后/接着/随后/此时/最终 等连接词计数/百字）、实体与动作动词覆盖率（v1 现行值中的名词/动词在产物中出现的比例，分词用简单字符匹配即可）、字符长度。
     - ear diff：#1/#88/#91 同镜 ear_on vs ear_off 的 action 提问产物并排（预期示范：雨声→场景雨天语义、脚步→动作链补「走近」类修正）。
     - 盲评表：同镜三策略匿名编号 甲/乙/丙（随机映射记录到 results/strategy_mapping.txt，盲表内不出现 A/B 字样）+ v1 参照列。
  4. SC1 负测试：跑前跑后 `sha256sum` live ep01 prompts.json（命令 + 两个哈希记录进报告；全程 sandbox-only，理论必等，实测为证）。
  5. SC4 runtime：重跑 a 步同命令计时（cache 全命中）→ wall-clock 秒数 + run.log 中零 observe 调用记录进报告。
  6. 写 `.planning/research/vision-seq-spike-report.md`（DRAFT，mirror audio-spike-report 结构）：头部元数据（ep01 目录全路径反引号包裹、spike 镜清单、fakes/demo 声明：audio_semantic 为 demo 级、live 文件未动）/ Methodology（三策略定义 + ear 双跑设计 + 「客观指标是辅助、人工盲评是主判据」反循环论证声明）/ 每镜 evidence 节（三策略原文 + v1 参照 + 指标）/ ear diff 节 / Recommendations（DRAFT——待盲评，盲评表置于其中）/ Reproducibility（重跑命令 + sha 证明 + cache 断点续跑说明 + 调用量与耗时实测）。
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-shot-timeline && test -f .planning/research/vision-seq-spike-report.md && test $(ls spike/vision_seq/sandbox/route_cache/vision_seq/shot_*.json 2>/dev/null | wc -l) -ge 6 && test -f spike/vision_seq/sandbox_ear/route_cache/vision_seq/shot_00{1,88,91}.json && grep -q "DRAFT" .planning/research/vision-seq-spike-report.md && grep -q "sha256" .planning/research/vision-seq-spike-report.md && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - sandbox cache ≥ 6 个 shot_XXX.json 且 ear 信封在位（ear_off 6 镜 + sandbox_ear 的 ear_on 3 镜）
  - 报告含 DRAFT 标记、盲评表（甲/乙/丙匿名 + v1 参照）、ear diff 节、live sha256 前后哈希且两值相等、SC4 秒级实测数字
  - run.log 记录每步进度；三策略产物在 results/ 可查
  - python3 -m pytest tests/ -q 全绿（repo 代码未动）
  </acceptance_criteria>
  <done>GPU 证据烧完且落档：三策略产物 + 指标 + ear diff + 盲评材料齐备，live 文件哈希不变证明在案，报告 DRAFT 就绪待盲评。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Kai 盲评 checkpoint → 锁定合并策略 → 定稿报告 + 模块默认值</name>
  <files>.planning/research/vision-seq-spike-report.md, analysis/vision_seq_facets.py</files>
  <read_first>
  - .planning/research/vision-seq-spike-report.md（Task 2 产出的 DRAFT 全文）
  - spike/vision_seq/results/strategy_mapping.txt（甲/乙/丙 ↔ 策略映射）
  - analysis/vision_seq_facets.py（MERGE_STRATEGY_DEFAULT 常量位置）
  </read_first>
  <what-built>
  ep01 六镜 spike 的三策略（时序拼接 / LLM 二次合并 / 最长参照）同镜产物 + 客观指标 + ear on/off 双跑 diff，全部落 DRAFT 报告。合并策略需要你人工并排盲评后锁定（Q3 27B 动作描述质量无机器可信判据——research Pitfall 4）。
  </what-built>
  <how-to-verify>
  1. 打开 .planning/research/vision-seq-spike-report.md，逐镜对比盲评表中甲/乙/丙与 v1 现行值：动作链完整性、运镜描述准确性、有无幻觉/啰嗦。
  2. 看 ear diff 节确认 #1/#88/#91 的 ear 版产物体现了音频修正（预期：雨声→雨天场景语义、脚步→动作链补「走近」）。
  3. 看 Reproducibility 节确认 live prompts.json sha256 前后一致（不覆盖证明）与 SC4 秒级重跑实测。
  4. 裁决：锁定合并策略（甲/乙/丙对应策略见 results/strategy_mapping.txt，裁决后映射写回报告）；如三策略均不合格，裁决改用何种降级（如 A + 人工抽检）。
  </how-to-verify>
  <resume-signal>回复「锁定 <甲/乙/丙 或 策略名>」或描述需要补跑/修正的问题</resume-signal>
  <action>
  （checkpoint 后执行）1. 把锁定的策略与理由写进报告 Recommendations 节（含证据指针），头部去 DRAFT 改 FINAL，盲评映射表回写。2. 若锁定策略 ≠ temporal：把 analysis/vision_seq_facets.py 的 MERGE_STRATEGY_DEFAULT 改为锁定值（llm 时确认 cache merged_B 已全量在位，否则生产路径首跑会补烧 ask_text——报告 Reproducibility 注明）。3. 重跑 python3 -m pytest tests/ -q 确认默认值变更未破测试（若有测试断言默认值则同步更新该断言）。4. commit。
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-shot-timeline && ! grep -q "DRAFT" .planning/research/vision-seq-spike-report.md && grep -q "锁定" .planning/research/vision-seq-spike-report.md && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - 报告无 DRAFT 标记、含「锁定结论」节（策略 + 理由 + 证据指针 + 盲评映射）
  - vision_seq_facets.py 默认策略 = 锁定值（grep MERGE_STRATEGY_DEFAULT 与报告结论一致）
  - python3 -m pytest tests/ -q 全绿
  </acceptance_criteria>
  <done>SC2 收口：合并策略经人工盲评实证锁定并成为模块默认，结论与证据落档 spike report（FINAL）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo audio_semantic.json → LLM prompt | 手工构造输入进提问上下文（只影响 spike sandbox，不进 live） |
| 模型产出文本 → spike report / sandbox prompts.json | 模型文本成为报告内容与 sandbox facet 值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Tampering | live ep01 work_dir 被误写 | mitigate | spike 只写 spike/vision_seq/（build 脚本 assert 输出前缀）+ live prompts.json sha256 前后比对负测试记录在报告 |
| T-19-07 | Repudiation | 盲评结论不可追溯 | mitigate | 甲/乙/丙映射落 results/strategy_mapping.txt + 报告回写映射与裁决；spike report 为归档交付物 |
| T-19-08 | DoS | spike 调用量失控烧 GPU | mitigate | 规模锁 ≤10 镜（实选 6+3），每镜 ~15 calls 封顶 ~150；cache 断点续跑；ep01 全量 93 镜明确不在本 phase SC（Pitfall 6 警告线） |
| T-19-SC | Tampering | pip/pip installs | accept | 本 plan 零包安装（零新 pip 依赖、零新引擎、零模型下载）——不适用 |
</threat_model>

<verification>
- `test -f .planning/research/vision-seq-spike-report.md` 且无 DRAFT（Task 3 后）
- live ep01 prompts.json sha256 与报告记录值一致（可随时复验）
- `python3 -m pytest tests/ -q` 全绿；`git diff --stat` 不含 output/ 下任何文件
- 模块 MERGE_STRATEGY_DEFAULT 与报告锁定结论一致
</verification>

<success_criteria>
- SC2：三策略 A/B 对比 + baseline 参照在 ep01 小样本盲评后锁定，结论记录在案（Q3 27B 质量有实证）
- SC1（半边）：sandbox 空缺镜被跨帧合并产物填充、与 v1 产物差异可见进报告；live 文件 byte-identical 负测试在案
- SC3：ear on/off 双跑 diff 进报告；demo audio_semantic.json 路径落档
- SC4（半边）：cache 断点续跑实测 + 秒级重跑 wall-clock 记录
</success_criteria>

<output>
Create `.planning/phases/19-qwen-eye-v2/19-02-SUMMARY.md` when done
</output>
