---
phase: 19-qwen-eye-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analysis/engine_clients/qwen_eye_client.py
  - tests/test_qwen_eye_client.py
  - analysis/vision_seq_facets.py
  - tests/test_vision_seq_facets.py
autonomous: true
requirements: [VISION-01, VISION-02]
must_haves:
  truths:
    - "qwen_eye_client 具备 observe_pair（两条 user 消息各一图，规避 llama.cpp 多图丢弃 bug）与 ask_text（纯文本零图）入口，均走 _call_llm 恒禁 thinking"
    - "vision_seq_facets.py 离线可加载：均匀 ≤8 帧采样、action 逐帧问、camera 相邻帧对问、cache 存 RAW 逐帧/逐对答案（4-tuple + ear 第 5 维）、只填空缺永不覆盖、ear 白名单注入 + --no-ear、graceful-degrade [vision-seq]"
    - "全部离线单测绿，既有 27+2 测试零回归（v1 local_vision_facets.py 零改动）"
  artifacts:
    - path: "analysis/vision_seq_facets.py"
      provides: "v2 帧序列逐帧问答 facet 升级模块（mirror v1 全套惯例）"
      contains: ["MAX_SEQ_FRAMES_PER_SHOT = 8", "PROMPT_VERSION = \"vision-seq-v1\"", "STEP_TAG = \"[vision-seq]\""]
    - path: "analysis/engine_clients/qwen_eye_client.py"
      provides: "observe_pair / ask_text 本地扩展"
      contains: ["def observe_pair", "def ask_text"]
    - path: "tests/test_vision_seq_facets.py"
      provides: "v2 离线单测矩阵（FakeEngine 零网络）"
      min_lines: 250
  key_links:
    - from: "analysis/vision_seq_facets.py"
      to: "analysis/engine_clients/qwen_eye_client.py"
      via: "sys.path 注入 + import（QwenEye/ENGINE_NAME/ENGINE_VERSION）"
      pattern: "from engine_clients.qwen_eye_client import"
    - from: "analysis/vision_seq_facets.py"
      to: "route_cache/vision_seq/shot_XXX.json"
      via: "per-shot cache（ear_on/ear_off 双信封存 RAW answers）"
      pattern: "vision_seq"
---

<objective>
交付 v2 核心模块与客户端扩展（全部离线可测，零 GPU）：`qwen_eye_client.py` 加 `observe_pair`/`ask_text` 两个本地扩展方法；新建 `analysis/vision_seq_facets.py`（mirror v1 `local_vision_facets.py` 全套惯例，换 facet=action/camera、换帧策略=均匀 ≤8 帧 + 相邻帧对、加 ear 注入与合并层）；配套离线单测矩阵。

Purpose: Phase 19 的全部「难」都在 v1 已解决的层（生命周期/cache/degrade/写保护）；本 plan 把 v2 真正的新逻辑（均匀采样、对问、合并、ear 注入）做成纯函数 + 可单测的模块，为 19-02 spike（GPU 实证）与 19-03 wiring 提供被测对象。
Output: 2 个源文件（1 新 1 改）+ 2 个测试文件（1 新 1 扩展），全套 `python3 -m pytest tests/ -q` 离线绿。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-qwen-eye-v2/19-CONTEXT.md
@.planning/phases/19-qwen-eye-v2/19-RESEARCH.md
@.planning/phases/19-qwen-eye-v2/19-PATTERNS.md

<interfaces>
<!-- 执行者直接使用这些契约，无需再探索代码库 -->

From analysis/engine_clients/qwen_eye_client.py（本 plan Task 1 扩展前的现状）:
```python
ENGINE_NAME / ENGINE_VERSION  # 模块级导出（:58-62 区域，禁改）
class QwenEye:
    def ensure_ready(self) -> tuple[bool, bool]          # (healthy, owned)；Guard 链勿重写
    def stop_if_owned(self) -> None                       # 只停自己拉起的 server
    def observe_single(self, image_path: Path, question: str,
                       max_tokens: int = 2000) -> str      # :267-279，v2 action 逐帧复用
    def _call_llm(self, messages, max_tokens) -> str      # :234-261 私有；恒传 enable_thinking:false（:243）
```
禁改区：`_call_llm`、生命周期（:159-231）、`ENGINE_NAME`/`ENGINE_VERSION`。新方法必须走 `_call_llm`。

From analysis/local_vision_facets.py（v1 mirror 真源，本 plan 零改动）:
```python
CACHE_NAME = "local_vision"            # :70 → v2 换 "vision_seq"
PROMPT_VERSION = "local-vision-v1"     # :71 → v2 换 "vision-seq-v1"
MAX_FRAMES_PER_SHOT = 3                # :78 → v2 不动它，新增 MAX_SEQ_FRAMES_PER_SHOT = 8
STEP_TAG = "[vision]"                  # :81 → v2 换 "[vision-seq]"
def video_content_hash(video_path)     # :99-110 首尾 1MB+size，原样复制勿改
def select_frames(...)                 # :113-137 时窗过滤 + "_ds" not in p.stem，v2 换均匀索引
```

v2 模块 CLI 契约（本 plan 定义；19-02 spike 与 19-03 wiring 消费）:
```
python3 analysis/vision_seq_facets.py \
  --shots <shots.json> --frames-dir <frames_5fps/> --work-dir <work_dir> \
  --output <prompts.json> --video <video> \
  [--audio-semantic <audio_semantic.json>] [--no-ear] \
  [--merge-strategy {temporal,llm,longest}]   # 默认 temporal（零 GPU 安全默认）
```

cache 文件契约（route_cache/vision_seq/shot_XXX.json，XXX 为 shot id 零填充 3 位）:
```json
{
  "ear_on":  {"_cache_key": {video_content_hash, engine_name, engine_version, prompt_version, "ear": true},
              "answers": {"action_frame_1": "...", "camera_pair_1": "...", ...}, "merged_B": "..."},
  "ear_off": {"_cache_key": {...ear: false}, "answers": {...}}
}
```
查找只读本 ear 信封；写入 read-merge-write 只更新本 ear 信封（ear on/off 双跑证据共存，切换不丢数据）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: qwen_eye_client 扩展 observe_pair / ask_text + shape 单测</name>
  <files>analysis/engine_clients/qwen_eye_client.py, tests/test_qwen_eye_client.py</files>
  <read_first>
  - analysis/engine_clients/qwen_eye_client.py（全文 279 行；重点 :9-11 头部裁剪注释、:29-33 硬约束注释、:234-261 _call_llm、:267-279 observe_single 锚点）
  - tests/test_qwen_eye_client.py（重点 :142-163 test_observe_single_request_shape 的 monkeypatch _call_llm 捕获写法）
  - .planning/phases/19-qwen-eye-v2/19-PATTERNS.md §「qwen_eye_client.py (MODIFY)」（含成品方法形态与必改头部注释）
  </read_first>
  <behavior>
  - Test 1 (test_observe_pair_request_shape): observe_pair(Path("a.jpg"), Path("b.jpg"), "问句") 构造的 messages 恰为 2 条 role=user；每条恰含 1 个 type=image_url part + 1 个 type=text part；问句子串只出现在第 2 条消息的 text；第 1 条消息 text 含「前一帧」标记；两张图 base64 互不相同且分别对应 a/b。
  - Test 2 (test_ask_text_request_shape): ask_text("问句") 构造单条 role=user 消息、纯 type=text part、零 image_url part。
  </behavior>
  <action>
  在 observe_single（:267-279）之后紧邻插入两个公开方法（形态直接用 19-PATTERNS.md 给出的成品，签名对齐 observe_single 风格）：
  1. `observe_pair(self, img_a: Path, img_b: Path, question: str, max_tokens: int = 2000) -> str` —— 恰两条 user 消息：第 1 条 = img_a 的 image_url + text「(第1帧/前一帧)」；第 2 条 = img_b 的 image_url + text「(第2帧/当前帧)」+ question 拼接。两条 user 各一图是对 llama.cpp「单条 user 带 N 图只算 ceil(N/2) 张」bug 的规避（头部注释写明）。
  2. `ask_text(self, question: str, max_tokens: int = 2000) -> str` —— 单条 user 纯 text 消息；docstring 注明「纯文本调用（策略 B 合并用）—— 无图 = 豁免多图丢弃 bug」。
  两方法都必须经 `self._call_llm(messages, max_tokens=max_tokens)`（继承 enable_thinking:false 恒传与超时/重试语义），绝不直接调 `_http_json`。
  同步更新文件头部 :9-11 裁剪清单注释为「裁剪 logging；本地扩展 observe_pair/ask_text（mirror 上游 observe() 多 user 消息 shape，question 版）」，并更新模块 docstring 的用法示例（:36-44 区域）加入两方法。
  禁改：`_call_llm`、生命周期方法（:159-231）、`ENGINE_NAME`/`ENGINE_VERSION`（:58-62）。全中文注释。
  tests/test_qwen_eye_client.py 追加上述 2 个测试（mirror :142-163 的 monkeypatch 捕获写法，不新增网络调用）。
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-shot-timeline && python3 -m pytest tests/test_qwen_eye_client.py -q && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - grep -c "def observe_pair\|def ask_text" analysis/engine_clients/qwen_eye_client.py 输出 2
  - python3 -m pytest tests/ -q 全绿，用例数 = 既有 27 + 新增 2 = 29（v1 零回归）
  - git diff --stat 不含 analysis/local_vision_facets.py（v1 红线零改动）
  - grep -n "enable_thinking" analysis/engine_clients/qwen_eye_client.py 仍只在 _call_llm 内出现一次设置（新方法未绕过）
  </acceptance_criteria>
  <done>客户端具备相邻帧对问与纯文本两个受控入口，shape 测试锁定消息结构，既有套件零回归。</done>
</task>

<task type="auto">
  <name>Task 2: 新建 analysis/vision_seq_facets.py v2 模块</name>
  <files>analysis/vision_seq_facets.py</files>
  <read_first>
  - analysis/local_vision_facets.py（全文 373 行 —— v1 mirror 真源：docstring/imports/sys.path 注入/常量/hash/采样/main 骨架/cache/引擎调用/写保护逐段对照）
  - .planning/phases/19-qwen-eye-v2/19-PATTERNS.md §「vision_seq_facets.py」A-J 项逐条
  - .planning/phases/19-qwen-eye-v2/19-RESEARCH.md Pattern 2/3/6 + Pitfall 5/8 + Code Examples（ear prompt 组装、cache 命中零引擎调用）
  - spec/schemas/prompts.schema.json（action/camera = required type:string）
  - spec/fixtures/v1.2/audio_semantic.json（ear 白名单字段结构实况）
  </read_first>
  <action>
  以 local_vision_facets.py 为模板逐段 mirror，按 PATTERNS A-J 项替换为 v2 语义。模块级要素全部落死值：
  - 常量：`CACHE_NAME = "vision_seq"`、`PROMPT_VERSION = "vision-seq-v1"`、`MAX_SEQ_FRAMES_PER_SHOT = 8`（新增；绝不触碰 v1 的 MAX_FRAMES_PER_SHOT=3）、`STEP_TAG = "[vision-seq]"`、`MERGE_STRATEGY_DEFAULT = "temporal"`。
  - 提问文案（CONTEXT 属 Claude's discretion，用此具体措辞，mirror v1 SCENE_PROMPT 一句中文指令 + 「只输出…不要…」结构）：
    ACTION_PROMPT = 「请用一句简洁中文描述这一帧画面中正在发生的动作：人物在做什么、肢体动作与交互、动作进行到哪一步。只输出动作描述本身，不要描述场景环境，不要描述镜头运动，不要编号或前缀。」
    CAMERA_PAIR_PROMPT = 「这两帧是同一镜头中相邻的两帧（第1帧在前、第2帧在后）。请用一句简洁中文描述：相对第1帧，第2帧的镜头怎么运动了（推、拉、摇、移、升降、跟随）或画面主体怎么位移、景别怎么变化。只输出运镜与相对变化描述本身，不要编号或前缀。」
  - imports + sys.path 注入照抄 v1（PATTERNS B 段原样），import QwenEye/ENGINE_NAME/ENGINE_VERSION。
  - `video_content_hash` 原样复制（首尾 1MB + size）。
  - `select_uniform_frames(frames_dir, start_sec, end_sec, max_frames=MAX_SEQ_FRAMES_PER_SHOT)`：时窗过滤 + `"_ds" not in p.stem` 忽略变体（Pitfall 8，mirror v1 :122-125）+ f%06d.jpg 编号解析 mirror v1 _frame_number；窗口 ≤ max_frames 全取，否则均匀索引 `idx = {round(i * (len(window) - 1) / (max_frames - 1)) for i in range(max_frames)}` 取排序后帧（RESEARCH Pattern 6）。
  - `_cache_key(vch, ear)` 返回 5 字段 dict（4-tuple + `"ear": ear`，PATTERNS G 段成品）。
  - per-shot cache 读写：`route_cache/vision_seq/shot_XXX.json` 双信封结构（本 plan frontmatter interfaces 中的 JSON 契约）——查找只读本 ear 信封、key 任一字段不匹配即 miss、写入 read-merge-write 只更新本 ear 信封；`answers` 存 RAW 逐帧/逐对答案（`action_frame_1..N`、`camera_pair_1..M`，N=帧数 M=对数，由实际采样决定，上限 8/7），绝不存合并产物（B 策略例外：`merged_B` 键持久化 LLM 合并结果）。
  - 引擎调用层：action = 对每个采样帧 `observe_single(frame, ear_question)`；camera = 对每相邻对 `observe_pair(f_i, f_{i+1}, ear_question)`；ear_question = `f"该镜音频：{audio_ctx}。{基础提问}"`（audio_ctx 非空时）。per-shot 异常 `except (RuntimeError, OSError)` → [vision-seq] warning + continue（mirror v1 :360-363）。绝不直调 engine._call_llm（Pitfall 2 警告线）。
  - ear 层：`build_audio_context(shot_audio, max_text=200)`（RESEARCH Code Examples 成品函数原样）：白名单仅 `dialogue.text`（截断 200 字）+ `dialogue.emotion` + `sfx.events` + `sfx.description`；words/reproduction/spk_id/speakers 永不进。CLI `--audio-semantic` 文件缺席 = 自动无 ear 且**零 warning**（CONTEXT 锁定，静默 degrade）；`--no-ear` 显式关；缺字段/类型错一律跳过该字段（V5 语义）。
  - 合并层（写出时执行，读 cache RAW answers）：`--merge-strategy {temporal,llm,longest}` 默认 temporal。temporal = 按帧序/对序以「→」join 非空答案（确定性纯归约）；longest = 取最长单条（baseline 参照）；llm = ask_text 二次合并（输入 = 编号的逐帧/逐对答案清单，输出一条连贯中文描述），结果写回 cache `merged_B` 键（下次零 GPU）。
  - cache 覆盖预判（本 plan 对 SC4「重跑秒级」的加强，v1 无此层）：per-shot 循环前先算 pending =（action 或 camera 为 "" 且本 ear 信封 cache miss）的镜集合；pending 为空 → 完全不进入引擎生命周期（不 ensure_ready、不启动 server），直接走 cache 合并写出。pending 非空才 `engine = QwenEye(); try: ensure_ready() ... finally: stop_if_owned()`（Shared Pattern 1 原样）。
  - graceful-degrade：引擎不可用 → pending 镜 facet 保持 "" + `[vision-seq]` warning + exit 0（mirror v1）。
  - 写出保护：读入 prompts.json → 只改 action/camera 两键（`if p.get(facet): continue` 已有值永不覆盖，**不做更短替换**——CONTEXT 锁定）→ Draft202012Validator(prompts.schema.json) 写前自校验 → tmp + os.replace 原子写（indent=2, ensure_ascii=False）。**零修改短路**：本次运行没有任何 facet 被改动 → 完全不重写 --output 文件（保 byte-identical）；warnings 为空且 sidecar 不存在 → 不创建 sidecar 文件。
  - warnings sidecar：READ-merge-write，strip 本 step `[vision-seq]` 前缀旧条目防 self-accumulate，他 step 条目保留（Shared Pattern 3）。
  - 进度：`print(f"[vision-seq] {done}/{total} shots processed")` 每 10 镜一行（mirror v1 :268-269）。
  - module docstring 全中文，必含：用途/算法步骤（采样→逐帧/对问→RAW cache→合并→写保护）/输出 schema/CLI 用法 + **Pitfall 5 时序后果**：「ear 激活发生在 audio_semantic.json 就位后的第二次管线跑（5.6 在 step 7 之前）；ear 进 cache key，切换 ear 会重烧一次；想跳过重烧保持 --no-ear」。
  - CLI flags（kebab-case + 中文 help）：--shots/--frames-dir/--work-dir/--output/--video（mirror v1 参数集）+ --audio-semantic + --no-ear（action="store_true"）+ --merge-strategy（choices，默认 MERGE_STRATEGY_DEFAULT）。
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-shot-timeline && python3 -c "import importlib.util; s=importlib.util.spec_from_file_location('vsf','analysis/vision_seq_facets.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert m.MAX_SEQ_FRAMES_PER_SHOT==8 and m.PROMPT_VERSION=='vision-seq-v1' and m.STEP_TAG=='[vision-seq]'" && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - 模块 importlib 加载成功且三常量断言通过（上列命令 exit 0）
  - grep -c "_call_llm" analysis/vision_seq_facets.py == 0（未越私有 API）
  - grep -c "local_vision_facets" analysis/vision_seq_facets.py == 0（不 import v1）
  - grep -c "MAX_FRAMES_PER_SHOT" analysis/vision_seq_facets.py == 0（只出现 MAX_SEQ_FRAMES_PER_SHOT）
  - git diff --stat analysis/local_vision_facets.py 为空（v1 零改动红线）
  - python3 analysis/vision_seq_facets.py --help 正常打印（argparse 冒烟）
  </acceptance_criteria>
  <done>v2 模块完整落地：均匀采样/对问/RAW-answer 双信封 cache/ear 白名单注入/三策略合并/只填空缺/预判生命周期/degrade/写保护全套，且 v1 文件零字节改动。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_vision_seq_facets.py 离线单测矩阵</name>
  <files>tests/test_vision_seq_facets.py</files>
  <read_first>
  - tests/test_local_vision_facets.py（全文 312 行 —— 骨架真源：importlib spec 加载 :24-33、make_workdir :36-58、FakeEngine :61-77、run_main/patch_engine :80-99、8 个 mirror 测试的行号见 PATTERNS 对照表）
  - analysis/vision_seq_facets.py（Task 2 刚写的本模块）
  - .planning/phases/19-qwen-eye-v2/19-RESEARCH.md §Validation Architecture「Phase Requirements → Test Map」
  </read_first>
  <behavior>
  - 采样：窗口 >8 帧恰取 ≤8 帧且首尾帧在列；窗口 ≤8 全取；`_ds1280` 变体被忽略。
  - 只填空缺：action/camera 已有值的镜零改动；空缺镜被填充。
  - cache 幂等：第二跑 FakeEngine2.call_count == 0；改 cache 里 prompt_version → miss 重拉；ear_on 信封不服务 ear_off 运行（miss 重拉）。
  - 合并确定性：temporal/longest 同输入同输出、零引擎调用。
  - degrade：引擎不可用 → [vision-seq] warning + exit 0 + prompts.json schema 合法 + facet 保持 ""。
  - 生命周期：observe 中途抛异常的引擎也被 stop_if_owned。
  - sidecar：他 step 条目保留、重跑不 self-accumulate。
  - ear：有 audio → 提问含音频摘要子串；--no-ear / 无文件 → 不含；words/reproduction/spk_id 字段值永不出现在提问串。
  - 无 audio 文件运行与显式 --no-ear 运行产出的 prompts.json byte-identical。
  - 零修改短路：全满 prompts.json 跑后文件 sha/mtime 不变。
  - 预判生命周期：全 cache 命中时 QwenEye 类从未被实例化。
  </behavior>
  <action>
  复制 v1 测试骨架（importlib 加载 vsf / make_workdir 改为 10 帧假 jpg + prompts.json 的 action=""/camera="" 两键 / FakeEngine 增加 observe_pair(img_a, img_b, question, max_tokens=2000) 与 ask_text(question, max_tokens=2000) 计数方法 / run_main argv 注入 + monkeypatch.setattr(vsf, "QwenEye", lambda: fake)），按上方 behavior 清单逐条落测试（v1 的 8 个 mirror 测试行号对照 PATTERNS「必 mirror 的测试」表，v2 新增 ear/信封/合并/短路/预判 7 类）。全离线零网络。中文注释。
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-shot-timeline && python3 -m pytest tests/test_vision_seq_facets.py -q && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - python3 -m pytest tests/test_vision_seq_facets.py -q 全绿，用例数 ≥ 15（覆盖 behavior 清单每条至少 1 例）
  - python3 -m pytest tests/ -q 全套绿（29 + 新增，v1/v1.2 全部既有测试零回归）
  - 测试文件无网络调用（grep -c "8125\|10588\|requests\|httpx" tests/test_vision_seq_facets.py == 0）
  </acceptance_criteria>
  <done>v2 全部新逻辑（采样/对问/合并/ear/cache 幂等/写保护/degrade/预判）有离线机器证明，SC3 单元级断言与 SC4 零引擎调用断言在档。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 视频帧内容 → 模型 → facet 文本 | 帧来自被分析视频（不可信输入源）；模型回答成为 prompts.json 内容 |
| audio_semantic.json → 提问文本 | 外部 JSON 输入进 LLM prompt 上下文 |
| cache 文件 → facet 产物 | 本地 JSON，理论可被篡改 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | 模型产出文本 → prompts.json | mitigate | 写前 Draft202012Validator(prompts.schema.json) 自校验（type:string 全字段约束）；渲染侧 _esc() 是 Phase 22 PRESENT-01 义务，本 phase 不产 HTML |
| T-19-02 | Tampering | route_cache/vision_seq/shot_XXX.json 投毒 | mitigate | 5 字段 cache key 强绑定（video_content_hash 参与）；读失败/key 不匹配一律 miss 重拉 |
| T-19-03 | Spoofing | 引擎端点错配 | accept | 常量 URL（127.0.0.1:8125/:10588），无用户可控 URL 输入，本地 CLI 无新端点 |
| T-19-04 | Injection (prompt) | 帧内画面内容诱导模型产出越权文本 | accept | facet 只进 schema-validated string 字段；不执行模型输出；身份归 re-id（char_XXX 永不写） |
| T-19-05 | Tampering | ear 输入字段滥用（非白名单字段进 prompt） | mitigate | build_audio_context 白名单提取（dialogue.text 截断/emotion/sfx.events/description），缺字段/类型错跳过；words/reproduction/spk_id 永不进（单测 grep 负测试） |
</threat_model>

<verification>
- `python3 -m pytest tests/ -q` 全套绿（既有 29 + 新增 ≥15，总耗时 <2s，全离线）
- `git diff --stat` 范围恰为 4 个 files_modified 文件；analysis/local_vision_facets.py 与 v1 常量零改动（零回归红线）
- 零新 pip 依赖：git diff 中无 requirements/pyproject/新 import 的第三方包
</verification>

<success_criteria>
- observe_pair/ask_text 落地且 shape 测试锁定两条 user 各一图 / 纯文本零图
- vision_seq_facets.py 具备 CONTEXT 全部锁定决策：均匀 ≤8 帧（MAX_SEQ_FRAMES_PER_SHOT=8 新常量）、action/camera 分开问、相邻帧对问 camera、只填空缺永不覆盖（无更短替换）、route_cache/vision_seq/ + PROMPT_VERSION="vision-seq-v1" + ear 进 cache key、ear 生成时注入白名单 + 默认开 + --no-ear + 缺席静默 degrade
- cache 存 RAW 逐帧/逐对答案（合并纯归约，策略切换零 GPU 重烧）
- SC3 单元级（ear 含/不含子串断言）与 SC4 单元级（二跑零引擎调用断言）在测试矩阵内
</success_criteria>

<output>
Create `.planning/phases/19-qwen-eye-v2/19-01-SUMMARY.md` when done
</output>
