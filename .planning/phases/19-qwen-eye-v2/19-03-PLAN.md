---
phase: 19-qwen-eye-v2
plan: 03
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - run_pipeline.py
  - tests/test_pipeline_vision_seq_wiring.py
  - .planning/research/vision-seq-spike-report.md
autonomous: true
requirements: [VISION-01, VISION-02]
must_haves:
  truths:
    - "run_pipeline 挂载无编号 pre-step 5.6（local_vision 之后、step_reid 之前），banner 为 plain label 无 [5.6/9] 数字前缀，step counter 不 bump"
    - "--no-vision-seq 跳过整段；--no-ear 直通模块；--audio-semantic 传 step 7 产物路径（存在性由模块自判）"
    - "SC1 集成证据：sandbox 置空镜经锁定默认策略填充（与 v1 现行值差异可见，进 spike report 证据节）；live ep01 prompts.json sha256 运行前后不变（负测试）"
    - "SC4 runtime：sandbox cache 全命中重跑 wall-clock 秒级、零 GPU（预判生命周期不启动引擎），实测数字进报告"
  artifacts:
    - path: "run_pipeline.py"
      provides: "pre-step 5.6 挂载 + --vision-seq/--no-vision-seq/--no-ear flags"
      contains: ["vision seq facets (qwen-eye v2 pre-step)", "--no-vision-seq"]
    - path: "tests/test_pipeline_vision_seq_wiring.py"
      provides: "wiring 四件套（help 冒烟 + argparse spy + 静态顺序 + banner 锁）"
      min_lines: 90
  key_links:
    - from: "run_pipeline.py 5.6 块"
      to: "analysis/vision_seq_facets.py"
      via: "subprocess [sys.executable, HERE/analysis/vision_seq_facets.py, ...]"
      pattern: "vision_seq_facets\\.py"
    - from: "run_pipeline.py 5.6 块"
      to: "audio_semantic.json（step 7 产物路径变量）"
      via: "--audio-semantic 参数直传（文件存在性模块自判）"
      pattern: "--audio-semantic"
    - from: "5.5 local_vision 块"
      to: "5.6 块"
      to_2: "step_reid 调用"
      via: "源码顺序 index(5.5) < index(5.6) < index(step_reid)"
      pattern: "local_vision_facets\\.py.*vision_seq_facets\\.py.*step_reid"
---

<objective>
把 v2 模块挂进 run_pipeline（无编号 pre-step 5.6，mirror 5.5 local_vision 先例），配齐 wiring 静态/argparse 测试（banner grep 锁不破），并用锁定后的默认策略补齐 SC1/SC4 集成证据：sandbox 置空镜被填充（可见 diff）+ live ep01 prompts.json sha 不变负测试 + cache 命中秒级重跑实测，全部追加进 spike report。

Purpose: SC1/SC4 的最后半边证据（生产路径形态：经 wiring 的默认策略 + 真实 work_dir 布局），并保证 v1.x 全部既有 pipeline 测试零回归。
Output: run_pipeline.py 两处 delta（flags + 5.6 块）+ wiring 测试文件 + spike report 证据节追加。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-qwen-eye-v2/19-CONTEXT.md
@.planning/phases/19-qwen-eye-v2/19-PATTERNS.md
@.planning/phases/19-qwen-eye-v2/19-01-SUMMARY.md
@.planning/phases/19-qwen-eye-v2/19-02-SUMMARY.md

<interfaces>
From run_pipeline.py（本 plan 修改对象，行号 @ 2026-08-19 实读）:
```python
# :684-690 flag 惯例（dest + store_true/store_false 双 flag）—— 5.5 的 --local-vision/--no-local-vision
# :736 附近  audio_semantic_json 变量（step 7 产物路径，main() 作用域内先于 5.6/step7 使用点定义）
# :801-822  5.5 pre-step 块（条件四连 + subprocess 按路径 + plain-label banner）—— 5.6 的 mirror 真源
# :824 起   "# 6. 跨镜 re-id" 注释 + step_reid 调用 —— 5.6 插入点右边界
# :741-773  --force 段：route_cache_dir 整目录 rmtree 已天然覆盖 route_cache/vision_seq/（无需改）
def run_step(cmd, label)  # :122-126 banner 打印；label 为 plain 字符串
```

From analysis/vision_seq_facets.py（19-01/19-02 产物）:
```
CLI: --shots --frames-dir --work-dir --output --video [--audio-semantic] [--no-ear] [--merge-strategy {temporal,llm,longest}]
默认策略 = MERGE_STRATEGY_DEFAULT（19-02 锁定值）
行为: 零修改短路（无 facet 改动不重写 --output）；全 cache 命中不启动引擎；ear 信封隔离
```

From tests/test_pipeline_vision_wiring.py（wiring 测试 mirror 真源）:
```python
# :30-38 --help subprocess 冒烟; :41-78 argparse spy（_StopMain 异常短路）
# :85-95 静态源码顺序断言（src.index）; :98-105 banner count 锁（assert "[5.5/9]" not in src）
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_pipeline flags + pre-step 5.6 挂载</name>
  <files>run_pipeline.py</files>
  <read_first>
  - run_pipeline.py:680-700（--local-vision 双 flag 惯例段）、:730-780（audio_semantic_json 定义 + --force 段）、:795-830（5.5 块全文 + step 6 注释边界）
  - tests/test_pipeline_vision_wiring.py:98-105（banner grep 锁——本任务不可破的既有断言）
  - analysis/vision_seq_facets.py --help 输出（模块 CLI 参数名逐一核对）
  - .planning/phases/19-qwen-eye-v2/19-PATTERNS.md §「run_pipeline.py (MODIFY)」（含成品 flag 与块形态）
  </read_first>
  <action>
  1. argparse 段（--no-local-vision 组之后 ~:690 加）：
     - `--vision-seq` / `--no-vision-seq`（dest="vision_seq"，store_true default=True / store_false；中文 help：「启用帧序列逐帧问答升级 action/camera facets（默认启用；5.5 之后的无编号 pre-step）」/「禁用帧序列 v2（action/camera 保持现有值）」）
     - `--no-ear`（action="store_true" default=False；help：「禁用 vision-seq 的 audio_semantic ear 融合（audio_semantic.json 存在时默认开）」）
  2. main() 体：在 5.5 块 `run_step(cmd_vision, "local vision facets (qwen-eye pre-step)")` 行（~:822）之后、`# 6. 跨镜 re-id` 注释（~:824）之前插入无编号 pre-step 块（mirror 5.5 注释结构，注明 ear 时序后果：5.6 在 step 7 前，首跑 audio_semantic.json 必然缺席 → ear 自动关且零 warning，第二次管线跑才激活）：
     - 条件四连：`if (args.vision_seq and not args.skip_semantic and os.path.exists(prompts_json) and os.path.isdir(frames_dir)):`
     - cmd：`[sys.executable, str(HERE / "analysis" / "vision_seq_facets.py"), "--shots", shots, "--frames-dir", frames_dir, "--work-dir", work_dir, "--output", prompts_json, "--video", video, "--audio-semantic", audio_semantic_json]`；`if args.no_ear: cmd += ["--no-ear"]`
     - `run_step(cmd, "vision seq facets (qwen-eye v2 pre-step)")` —— banner 是 plain label，**绝不带 `[5.6/9]` 类数字前缀**（破既有 grep 锁）。
  3. --force 段零改动（route_cache 整目录 rmtree 已覆盖 vision_seq/ 子目录，加注释说明即可）。
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-shot-timeline && grep -q '"vision seq facets (qwen-eye v2 pre-step)"' run_pipeline.py && ! grep -q '\[5\.6/9\]' run_pipeline.py && python3 run_pipeline.py --help | grep -e '--vision-seq' -e '--no-vision-seq' -e '--no-ear' && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - grep -c "vision_seq_facets.py" run_pipeline.py == 1（恰一处 subprocess 调用）
  - grep '\[5\.6/9\]' run_pipeline.py 零命中；既有 test_step_banner_count_unchanged 类断言不破（pytest 全绿）
  - --help 三 flag 可见；静态顺序：local_vision_facets.py 的 index < vision_seq_facets.py 的 index < step_reid 调用 index（Task 2 测试锁定）
  - "--audio-semantic" 字符串在 5.6 块内出现且引用 audio_semantic_json 变量
  </acceptance_criteria>
  <done>pre-step 5.6 按 CONTEXT 锁定位置挂载（5.5 后 step_reid 前、无编号、--no-vision-seq 可跳过、ear 直通），既有管线测试零回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_pipeline_vision_seq_wiring.py 四件套</name>
  <files>tests/test_pipeline_vision_seq_wiring.py</files>
  <read_first>
  - tests/test_pipeline_vision_wiring.py（全文 106 行——四件套逐段 mirror：--help 冒烟 / argparse spy / 静态顺序 / banner 锁）
  - run_pipeline.py 新 5.6 块与 flags 段（Task 1 产物）
  </read_first>
  <behavior>
  - --help 冒烟：stdout 含 --vision-seq、--no-vision-seq、--no-ear。
  - argparse spy（_StopMain 短路）：默认 vision_seq is True；--no-vision-seq → False；--no-ear 默认 False、传入后 True。
  - 静态顺序：src.index("local_vision_facets.py") < src.index("vision_seq_facets.py") < src.index("step_reid(video, work_dir")；src 含 '"--audio-semantic"' 且出现在 vision_seq_facets.py 调用构造内；src 含 '"vision seq facets (qwen-eye v2 pre-step)"'。
  - banner 锁：保留 v1 文件全部既有断言形态并加 assert "[5.6/9]" not in src（"[5.5/9]" not in src 断言继续在位）。
  </behavior>
  <action>
  复制 test_pipeline_vision_wiring.py 四件套骨架，断言对象换成上方 behavior 清单（flag 名、5.6 块、新 banner 字符串、--audio-semantic 传递）。subprocess --help 冒烟与静态源码读全部离线，零 GPU 零网络。中文注释。
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-shot-timeline && python3 -m pytest tests/test_pipeline_vision_seq_wiring.py -q && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - python3 -m pytest tests/test_pipeline_vision_seq_wiring.py -q 全绿（≥4 测试，覆盖 behavior 四条）
  - python3 -m pytest tests/ -q 全套绿（含 test_pipeline_vision_wiring.py 与 test_local_vision_facets.py —— v1 零回归机器证明）
  </acceptance_criteria>
  <done>wiring 有静态 + argparse 双重机器锁：顺序、flag 语义、banner plain-label、ear 直通全部断言在档。</done>
</task>

<task type="auto">
  <name>Task 3: SC1/SC4 集成证据（sandbox 填充 + live 负测试 + 秒级重跑）</name>
  <files>.planning/research/vision-seq-spike-report.md, spike/vision_seq/results/sc1_evidence.txt</files>
  <read_first>
  - .planning/research/vision-seq-spike-report.md（19-02 定稿全文——本任务追加「SC1/SC4 集成证据」节）
  - spike/vision_seq/README.md 与 build_sandbox.py --reset 模式（19-02 Task 1 产物）
  - .planning/phases/19-qwen-eye-v2/19-RESEARCH.md §Pitfall 1（sandbox 方案与 sha 负测试设计）
  - analysis/vision_seq_facets.py（零修改短路 / 预判生命周期 / --work-dir 语义）
  </read_first>
  <action>
  1. SC1 sandbox 填充（生产路径形态）：`python3 spike/vision_seq/build_sandbox.py --reset`（重置 sandbox 六镜 facets，route_cache 保留）→ 以锁定后的默认策略重跑模块（不带 --merge-strategy，即用 19-02 锁定的 MERGE_STRATEGY_DEFAULT；ear 用 --no-ear 对齐 spike 烧的 ear_off 信封）→ 全 cache 命中（同视频 hash + ear=false + 19-02 已含 merged_B），秒级零 GPU 完成填充。用 python3 提取填充前后六镜 action/camera 值并排（vs v1 现行值），写入 spike/vision_seq/results/sc1_evidence.txt。
  2. SC1 负测试（live 不覆盖）：记录 `sha256sum "<live ep01>/prompts.json"`；运行 `python3 analysis/vision_seq_facets.py --shots <live>/shots.json --frames-dir <live>/frames_5fps --work-dir /tmp/vision_seq_sc1_scratch --output <live>/prompts.json --video <原视频> --no-ear`（--work-dir 指向 scratch，cache/warnings 不落 live；live 93 镜全满 → 只填空缺守卫全跳过 → 零修改短路不重写文件）；再取 sha256，两值必须相等（不等 = 立即停止排查序列化差异，禁止放过）。
  3. SC4 runtime：对第 1 步 sandbox 重跑计时（`time` 命令包住），wall-clock 秒数 + 「引擎未启动」（预判生命周期：run 输出无引擎 banner/health 行）记录进 sc1_evidence.txt。
  4. 把三组证据（sandbox 填充并排 diff、live sha 前后等值、SC4 秒级实测）追加为 spike report 新节「SC1/SC4 集成证据（post-wiring）」，含全部可复现命令原文。
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-shot-timeline && test -f spike/vision_seq/results/sc1_evidence.txt && grep -q "sha256" .planning/research/vision-seq-spike-report.md && python3 -c "
import json, pathlib
p = json.load(open('spike/vision_seq/sandbox/prompts.json'))
filled = [i for i, s in enumerate(p) if s.get('action') and s.get('camera')]
assert len(filled) >= 6, 'sandbox blanked shots must be filled'
" && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - sandbox 六置空镜的 action/camera 非空且来自锁定策略合并产物（sc1_evidence.txt 有填充前后并排 + v1 参照）
  - live ep01 prompts.json 运行前后 sha256 相等（两哈希与命令原文都在 sc1_evidence.txt + 报告节内；可复验：重跑 sha256sum 与记录值一致）
  - SC4 wall-clock 数字在档且为秒级（<60s，零引擎启动）
  - 报告含「SC1/SC4 集成证据」节；python3 -m pytest tests/ -q 全绿
  </acceptance_criteria>
  <done>SC1/SC4 收口：置空镜被生产路径填充（diff 可见）+ live 文件 byte-identical 负测试 + cache 命中秒级重跑，三组证据可复现地落档。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_pipeline CLI flags → 子进程参数 | 操作员输入直通 subprocess argv |
| live prompts.json | 生产数据文件（负测试保护对象） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | Tampering | live prompts.json 被 wiring 路径意外改写 | mitigate | 只填空缺守卫 + 零修改短路（无改动不重写）；本 plan 负测试强制 sha256 前后等值；--work-dir 可指向 scratch 隔离 cache/warnings 落点 |
| T-19-10 | Tampering | flag 注入子进程参数 | accept | argv 列表传参（无 shell 拼接）；flag 均为 argparse 布尔/枚举，无自由文本进 cmd |
| T-19-11 | Repudiation | 证据不可复现 | mitigate | sc1_evidence.txt + 报告节含全部命令原文与哈希，可独立复验 |
</threat_model>

<verification>
- `python3 -m pytest tests/ -q` 全套绿（v1 + v1.2 既有 wiring/vision 测试零回归 = v1 路径零改动的机器证明）
- `grep -q '"vision seq facets (qwen-eye v2 pre-step)"' run_pipeline.py` 且 `! grep -q '\[5\.6/9\]' run_pipeline.py`
- spike report 含 SC1/SC4 集成证据节；live sha 可复验等值
- `git diff --stat` 范围 = run_pipeline.py + 新 wiring 测试 + spike report（+ spike/vision_seq/results 证据文件）；output/ 零变化
</verification>

<success_criteria>
- ROADMAP SC1 全量收口：v2 产物填充可见（sandbox diff）+ 只填空缺/永不覆盖证明（live sha 负测试）+ 兼容 aw2-fast 防覆盖守卫（字段级语义互补，Pitfall 7）
- SC4 全量收口：cache 命中零引擎调用（19-01 单测）+ 秒级重跑 runtime 实测（本 plan）；零新引擎/零模型下载（git diff 无 engine_clients 新模块、无 HF 调用）
- pre-step 5.6 挂载形态与 CONTEXT 锁定逐字一致（无编号、--no-vision-seq、local_vision 后 step_reid 前、ear 生成时注入默认开）
</success_criteria>

<output>
Create `.planning/phases/19-qwen-eye-v2/19-03-SUMMARY.md` when done
</output>
