---
phase: 05-contract-v1-1
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-03]
files_modified:
  - scripts/verify_contract.py
  - spec/validate.py
  - spec/SPEC.md
  - spec/README.md
autonomous: true
requirements: [CONTRACT-07, CONTRACT-08, CONTRACT-09]
user_setup: []

must_haves:
  truths:
    - "scripts/verify_contract.py SIX_SHAPES constant renamed to EIGHT_SHAPES (adds characters/props/registry)"
    - "verify_contract.py validates the v1.1 fixture set against all 8 schemas via the producer mode"
    - "verify_contract.py _cross_version_check proves forward-compat (v1 minimal fixture passes v1.1 schemas, 0 errors)"
    - "verify_contract.py _cross_version_check proves backward-compat modulo additive fields (v1.1 fixture against v1 schema yields ONLY additionalProperties errors, 0 non-addprop errors)"
    - "verify_contract.py _fixture_consistency_check catches dangling prompt↔registry IDs (zero in the v1.1 fixture)"
    - "PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer still catches the injected schema_version='v1' drift (fail-loud unchanged)"
    - "spec/validate.py discovers + validates the v1.1 fixture set (shape maps extended; minimal fixture still gates exit code)"
    - "spec/SPEC.md §4 Changelog has a 1.1 entry + new §5.6 Characters + §5.7 Props + §1 schema-file index lists 9 files"
    - "spec/README.md layout block + schema index updated to reflect the 3 new schemas + v1.1 fixture dir"
  artifacts:
    - path: "scripts/verify_contract.py"
      provides: "EIGHT_SHAPES + _cross_version_check + _fixture_consistency_check (the v1.1 regression harness)"
      contains: "EIGHT_SHAPES"
    - path: "spec/validate.py"
      provides: "Extended shape maps (v1.1 fixture set pass; minimal fixture unchanged)"
      contains: "v1.1"
    - path: "spec/SPEC.md"
      provides: "§4 Changelog 1.1 entry + §5.6 Characters + §5.7 Props + §1 schema index"
      contains: "1.1"
    - path: "spec/README.md"
      provides: "Updated layout block + 9-file schema index"
      contains: "characters"
  key_links:
    - from: "scripts/verify_contract.py (EIGHT_SHAPES constant)"
      to: "spec/schemas/{characters,props,registry}.schema.json"
      via: "shape-name → schema-file convention"
      pattern: "characters|props|registry"
    - from: "scripts/verify_contract.py (_cross_version_check git show v1.0:spec/schemas/...)"
      to: "git tag v1.0"
      via: "subprocess git-show recovering the v1 schema"
      pattern: "git.*show.*v1\\.0"
    - from: "spec/validate.py (shape discovery for v1.1 dir)"
      to: "spec/fixtures/v1.1/*.json"
      via: "path constants + fixture iteration"
      pattern: "v1\\.1"
    - from: "spec/SPEC.md §5.6/§5.7 field tables"
      to: "spec/schemas/characters.schema.json + props.schema.json"
      via: "prose mirrors schema authority"
      pattern: "char_[0-9]{3}"
---

<objective>
Wire the v1.1 contract into the regression harness and document it in prose. Three concerns: (1) extend scripts/verify_contract.py with EIGHT_SHAPES + a schema-layer _cross_version_check (bidirectional v1↔v1.1 compat proof) + a _fixture_consistency_check (zero dangling IDs); (2) extend spec/validate.py to discover + validate the v1.1 fixture set alongside the minimal one; (3) update spec/SPEC.md (§4 Changelog + new §5.6/§5.7 + §1 index) and spec/README.md (layout block + index) to mirror the frozen schemas.

Purpose: This plan closes the contract loop — schemas + fixtures exist (Plans 01+03), but without the harness extension they're unverified claims, and without the SPEC prose future contributors have no human-readable entry point. The cross-version check is the single most important deliverable: it PROVES empirically that the v1→v1.1 bump is pure-additive (forward-compat) and that shared fields stay type-aligned (backward-compat modulo additive optional fields). This is the schema-layer realization of "ignored-not-crashed" without needing a fake Python consumer (CONTEXT D-XX lock; real TS consumer warn-behavior is Phase 9).

Output: 4 files modified. No new schemas or fixtures — those are frozen in Plans 01/03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-contract-v1-1/05-CONTEXT.md
@.planning/phases/05-contract-v1-1/05-RESEARCH.md
@.planning/phases/05-contract-v1-1/05-VALIDATION.md
@scripts/export_asset.py

<interfaces>
<!-- The existing harness primitives to extend IN-PLACE (no rewrite). Read each file once to capture current line numbers; line numbers below are from research and may have drifted. -->

From scripts/verify_contract.py:75 (the constant to rename + extend):
```python
SIX_SHAPES = ["asset", "shots", "audio_analysis", "transcript", "frames", "prompts"]
```
EIGHT_SHAPES adds: "characters", "props", "registry" — total 9 entries (the name "EIGHT" is historical/branding from when there were 6 + the asset shape counted differently; CONTEXT research notes this — keep the name EIGHT_SHAPES for v1.0 backward-compat with the existing fail-loud self-test references, just spell it with 9 elements).

From scripts/verify_contract.py:197-246 (validate_six_shapes — the function to extend):
```python
def validate_six_shapes(asset_dir: Path, manifest: dict) -> list:
    # iterates SIX_SHAPES, validates each against spec/schemas/<shape>.schema.json
    # CR-01 pattern: data_field.get() chain with type guard
```
Extend to validate characters/props/registry WHEN PRESENT (graceful-degrade: absence is NOT failure — mirrors v1.0 philosophy). The registry shape is NOT in asset.json#data — discover by canonical filename "registry.draft.json" in the asset dir. characters/props ARE in asset.json#data — use manifest["data"]["characters"] etc. Rename to `validate_eight_shapes` OR keep `validate_six_shapes` and rebind (research recommends rename for clarity).

From scripts/verify_contract.py:369-442 (run_self_test — MUST remain working after extension):
```python
def run_self_test(args) -> tuple:
    # injects schema_version='v1' into a copy of ep01 asset.json
    # asserts validate_asset_json returns >= 1 error
```
The fail-loud path MUST stay green: PHASE4_SELF_TEST=1 ... still catches drift after the EIGHT_SHAPES rename.

From scripts/verify_contract.py:177-194 (validate_asset_json — the inline validator reused by _cross_version_check):
```python
def validate_asset_json(asset_path) -> list:
    schema = json.loads((SCHEMAS_DIR / "asset.schema.json").read_text(encoding="utf-8"))
    instance = json.loads(asset_path.read_text(encoding="utf-8"))
    errors = sorted(Draft202012Validator(schema).iter_errors(instance), ...)
    return errors
```

From spec/validate.py:32-49 (the shape maps to extend):
```python
FIXTURE_DIR = SPEC_DIR / "fixtures" / "minimal"
SHAPE_TO_FIXTURE = {"shots": "shots.json", ..., "asset": "asset.json"}  # 6 entries
MINIMAL_ORDER = ["asset", "shots", "audio_analysis", "transcript", "frames", "prompts"]
SMOKE_SHAPES = ["shots", "audio_analysis", "transcript", "frames", "prompts"]
```
Add a parallel V11_FIXTURE_DIR + V11_ORDER + a validate_v11() function. The minimal fixture still gates the exit code (CONTRACT-09); the v1.1 pass is additive (failures counted into total_strict_failures).

From spec/SPEC.md (the prose to extend):
- §1 (lines ~23-34): schema-file index table — add 3 rows (characters/props/registry).
- §4 Changelog (lines ~148-150): single `1` entry — add a `1.1` entry mirroring the format.
- §5 (lines ~154-337): 5 canonical data shapes — add §5.6 Characters + §5.7 Props after §5.5 Prompts.
- §6 (lines ~341-389): media conventions — add a sub-section on characters/props external png convention.
- §8 Out of Scope — no change (v1.1 work is in other phases).

The `v1.0` git tag exists (verified during planning) — `git show v1.0:spec/schemas/asset.schema.json` recovers the v1 schema for the backward-compat check.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend verify_contract.py (EIGHT_SHAPES + _cross_version_check + _fixture_consistency_check) + validate.py (v1.1 fixture pass)</name>
  <files>scripts/verify_contract.py, spec/validate.py</files>
  <read_first>
    - scripts/verify_contract.py (read in FULL — the file being modified; capture SIX_SHAPES line 75, validate_six_shapes L197-246, run_producer_check L249-300, run_self_test L369-442, validate_asset_json L177-194, SCHEMAS_DIR constant L62)
    - spec/validate.py (the file being modified; capture FIXTURE_DIR L32, SHAPE_TO_FIXTURE L36-43, MINIMAL_ORDER L46, SMOKE_SHAPES L49, validate_minimal L69-94, main L141-180)
    - .planning/phases/05-contract-v1-1/05-RESEARCH.md (§Architecture Patterns → Pattern 7 contains the _cross_version_check reference implementation + empirically verified mechanics; §Code Examples for cross-version filtering)
    - .planning/phases/05-contract-v1-1/05-VALIDATION.md (Test Map — every automated command listed by requirement ID)
    - spec/schemas/characters.schema.json + props.schema.json + registry.schema.json (Plan 01 output — confirm shape names + the cluster_id pattern for the consistency check)
    - spec/fixtures/v1.1/*.json (Plan 03 output — confirm fixture paths)
  </read_first>
  <action>
Extend both files in-place. No rewrite of existing logic — additive only.

**scripts/verify_contract.py** — 4 changes:

1. **Rename SIX_SHAPES → EIGHT_SHAPES** (line 75) and add the 3 new shape names: `EIGHT_SHAPES = ["asset", "shots", "audio_analysis", "transcript", "frames", "prompts", "characters", "props", "registry"]`. Keep a `SIX_SHAPES = EIGHT_SHAPES[:6]` alias if any other code references the old name (grep first — likely only run_self_test cares about the asset shape, which is still at index 0). Update the comment to explain "9 shapes, name kept as EIGHT for v1.0 historic call-site compat".

2. **Extend validate_six_shapes → validate_eight_shapes** (rename the function; add the 3 new shapes as validate-when-present per RESEARCH Pattern 7). The v1.0 6 shapes stay always-required; the 3 new shapes are gated:
   - `characters`: if `manifest["data"]["characters"]` is a string and the file exists → validate against characters.schema.json. Absent → skip (graceful-degrade).
   - `props`: same pattern.
   - `registry`: NOT in asset.json#data — check `(asset_dir / "registry.draft.json").is_file()`; if present, validate against registry.schema.json; absent → skip.
   Update run_producer_check to call validate_eight_shapes.

3. **Add `_cross_version_check() -> tuple[bool, str]`** helper (place near run_self_test for thematic grouping). Per RESEARCH Pattern 7 + empirically verified mechanics:
   - (a) FORWARD: for each of the 6 v1.0 shapes (asset/shots/audio_analysis/transcript/frames/prompts), load `spec/fixtures/minimal/<shape>.json` (or asset.json for "asset" shape), load the CURRENT (v1.1 extended) schema `spec/schemas/<shape>.schema.json`, validate via Draft202012Validator. Assert 0 errors. Skip shapes that have no v1.0 fixture counterpart (characters/props/registry are NEW — they have no v1 instance to test forward-compat on).
   - (b) BACKWARD: for each of the 2 EXTENDED schemas (asset, prompts), load `spec/fixtures/v1.1/<shape>.json`, load the V1 schema via `git show v1.0:spec/schemas/<shape>.schema.json` (subprocess, capture_output=True, text=True, check=False — if returncode != 0, fall back to programmatic property-strip: deep-copy the v1.1 schema, delete the known-added keys `properties.characters`, `properties.props` for asset; `items.properties.character_refs`, `items.properties.prop_refs` for prompts), validate v1.1 instance against v1 schema, collect all errors, FILTER OUT errors where `e.validator == "additionalProperties"`, assert the remaining list is empty (proves shared fields type-aligned; the sole delta is added optional fields).
   - Return `(True, "v1↔v1.1 bidirectional compat proven; ...")` on success; `(False, "; ".join(failures))` on failure.
   - Thread `_cross_version_check` into `run_producer_check` (or run it as a separate mode called from main after producer check passes). The exact slot is Claude's discretion per CONTEXT.md — recommend running it inside run_producer_check after validate_eight_shapes returns no failures, so producer mode = schema validity + cross-version compat in one green-bar.

4. **Add `_fixture_consistency_check() -> tuple[bool, str]`** helper (per RESEARCH Open Question 2 + Pitfall C prevention). Loads the v1.1 fixture set (or the producer asset dir's characters/props/prompts files when present) and verifies:
   - Every `prompts[].character_refs[]` ID exists in `characters[].id`.
   - Every `prompts[].prop_refs[]` ID exists in `props[].id`.
   - Every `characters[].appearance_shots[]` and `props[].appearance_shots[]` shot ID exists in `shots[].id`.
   - Every `registry.draft.json clusters[].cluster_id` matches the `(char|prop)_[0-9]{3}` pattern AND exists in the union of characters + props IDs.
   - Every `registry.draft.json clusters[].members[].shot_id` exists in shots IDs.
   Return tuple; thread into run_producer_check. This is distinct from Phase 8 PROMPT-03 (which is the full producer-emitted integrity check); Phase 5's check is "the fixtures we ship don't ship broken".

CRITICAL: do NOT break run_self_test. After all changes, `PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer` MUST still inject schema_version='v1' into a copy of ep01 asset.json and catch the drift via validate_asset_json. Grep the diff for any change to run_self_test body — it should be untouched (or only have its call to validate_six_shapes renamed to validate_eight_shapes if you renamed the function).

**spec/validate.py** — add a v1.1 fixture pass alongside the existing minimal pass:
- Add `V11_FIXTURE_DIR = SPEC_DIR / "fixtures" / "v1.1"` near FIXTURE_DIR (line 32).
- Add `V11_SHAPES = ["asset", "shots", "audio_analysis", "transcript", "frames", "prompts", "characters", "props", "registry"]` and `V11_FIXTURE_MAP = {"asset": "asset.json", ..., "registry": "registry.draft.json"}` (9 entries; registry maps to the .draft filename).
- Add `validate_v11() -> int` function mirroring validate_minimal's structure (load schema → load fixture → Draft202012Validator → print [valid-v11] / [FAIL-v11]). Reuse the existing `load_validator` + `_format_errors` helpers — do not duplicate.
- In `main()`: after `validate_minimal()`, call `validate_v11()`. The minimal fixture still gates the exit code (if minimal fails, exit 1 regardless of v1.1 status — CONTRACT-09 priority). The v1.1 pass failures ALSO count into `total_strict_failures` (a v1.1 fixture failure is a real regression — the schemas from Plan 01 must accept the fixtures from Plan 03).
- Keep the existing smoke pass unchanged.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 spec/validate.py && python3 scripts/verify_contract.py --mode=producer && PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer</automated>
  </verify>
  <acceptance_criteria>
    - `python3 spec/validate.py` exits 0 AND stdout contains 6 lines of `[valid] <shape>` (minimal) + 9 lines of `[valid-v11] <shape>` (v1.1) + at least 5 `[smoke-valid|smoke-FAIL]` lines if output/ has a producer fixture
    - `grep -c 'EIGHT_SHAPES' scripts/verify_contract.py` >= 2 (constant definition + at least one call site)
    - `grep -c 'SIX_SHAPES' scripts/verify_contract.py` is either 0 (fully renamed) OR references only an alias line `SIX_SHAPES = EIGHT_SHAPES[:6]` for backward-compat
    - `grep -c '_cross_version_check' scripts/verify_contract.py` >= 2 (definition + call from run_producer_check)
    - `grep -c '_fixture_consistency_check' scripts/verify_contract.py` >= 2 (definition + call)
    - `python3 scripts/verify_contract.py --mode=producer` exits 0 AND summary table shows `producer OK` AND output contains a line mentioning "cross-version" / "v1↔v1.1" compat (proves _cross_version_check ran)
    - `PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer` exits 0 AND output contains `[self-test] PASS` (fail-loud still catches the injected schema_version='v1' drift — run_self_test unbroken)
    - `python3 -c "import ast; t=ast.parse(open('scripts/verify_contract.py').read()); fns={n.name for n in ast.walk(t) if isinstance(n, ast.FunctionDef)}; assert '_cross_version_check' in fns; assert '_fixture_consistency_check' in fns; assert 'validate_eight_shapes' in fns or 'validate_six_shapes' in fns"` exits 0
    - Negative test: temporarily corrupt spec/fixtures/v1.1/characters.json (add an invalid char_99 ID), run `python3 spec/validate.py`, confirm it reports `[FAIL-v11] characters` and exits non-zero. Restore the fixture. (This proves the v1.1 pass is actually gating, not silently skipping.)
  </acceptance_criteria>
  <done>
scripts/verify_contract.py runs EIGHT_SHAPES through validate_eight_shapes (3 new shapes validate-when-present); _cross_version_check empirically proves v1↔v1.1 bidirectional compat (forward 0 errors; backward 0 non-additionalProperties errors); _fixture_consistency_check catches dangling prompt↔registry IDs (0 in the v1.1 fixture); run_self_test fail-loud path unchanged; spec/validate.py reports both minimal (6/6) AND v1.1 (9/9) green with both gating exit code.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend spec/SPEC.md (§4 Changelog 1.1 + §5.6 Characters + §5.7 Props + §1 index + §6 media convention)</name>
  <files>spec/SPEC.md</files>
  <read_first>
    - spec/SPEC.md (read in FULL — the file being modified; capture §1 schema-file index L23-34, §4 Changelog L148-150, §5 Prompts L307-337, §6 media refs L341-389)
    - spec/schemas/characters.schema.json + props.schema.json + registry.schema.json (Plan 01 output — the schema authority the prose MUST mirror exactly)
    - spec/schemas/asset.schema.json + prompts.schema.json (Plan 01 extended output — confirm the additive optional fields)
    - .planning/phases/05-contract-v1-1/05-CONTEXT.md (lines 19-47: schema_version lock decision;Claude's discretion on exact wording of §4/§5.6/§5.7)
    - spec/fixtures/v1.1/characters.json + props.json + registry.draft.json (Plan 03 output — the canonical example to cite in §5.6/§5.7 minimal snippets)
  </read_first>
  <action>
Extend spec/SPEC.md per RESEARCH Pattern 8 + the §4/§5.6/§5.7/§1/§6 update plan. Two-tier authority (CLAUDE.md): schemas are machine-checkable truth, SPEC.md is human-readable overview — on conflict, schema wins; this prose must match the frozen schemas EXACTLY (field names, ID patterns, enum values).

**§1 Schema-file index table** (around lines 23-34): the current table lists 6 schema files. Add 3 rows (characters / props / registry), each with a one-line Chinese overview matching the schema's title/description. Update the section heading "6 个 schema 文件" → "9 个 schema 文件". Update the "Phase 3 消费端可从这 6 个 schema 生成 TS 类型" line to "9 个 schema".

**§3 asset.json field table** (around lines 73-96): add 4 rows for the new optional fields:
- `data.characters` | string (relative path) | (no ✓) | Optional relative path to characters.json (asset-local registry). Absent on v1 producer assets.
- `data.props` | string (relative path) | (no ✓) | Optional relative path to props.json.
- `media.characters` | array<string> (png path) | (no ✓) | Optional canonical character png inventory (external files, NOT base64).
- `media.props` | array<string> (png path) | (no ✓) | Optional canonical prop png inventory.

**§4 Changelog** (around lines 148-150): add a new bullet entry AFTER the existing 2026-07-20 `1` entry. Mirror the format. The entry MUST cover: (a) Phase 5 minor bump date (use 2026-07-XX — fill in actual phase completion date); (b) added 3 new schemas + 3 new optional data files; (c) prompts.schema additive character_refs[]/prop_refs[]; (d) asset.schema additive data/media characters+props; (e) external png (NOT base64) decision rationale; (f) schema_version pattern unchanged (lock is producer-side via SCHEMA_VERSION constant); (g) v1 minimal fixture stays green; (h) cross-version self-test in verify_contract.py.

**§5 — add §5.6 Characters and §5.7 Props AFTER §5.5 Prompts** (around line 337). Mirror the existing §5.1-§5.5 pattern: Producer line, Consumers line, field table, enum table if applicable, minimal JSON snippet (cite the v1.1 fixture), "Reference schema:" footer.
- **§5.6 Characters**: Producer = registry/apply_edits.py (Phase 7/8, pending); Consumers = gen_timeline_html.py gallery (Phase 8) + canvas character nodes (Phase 9). Field table: id (^char_[0-9]{3}$, immutable), name, representative_image (external png, anti-traversal), appearance_shots[], review_state (enum proposed/confirmed/rejected — only confirmed flows downstream, Pitfall 7), looks[] (OPTIONAL — label, image_ref, appearance_shots[]). Minimal snippet: a 1-character slice of spec/fixtures/v1.1/characters.json (the char_001 少女 entry).
- **§5.7 Props**: mirror §5.6 structure but with prop_ pattern + states[] (NOT looks[]) — call out the semantic distinction (props vary by state open/closed/intact/broken, not by costume). Minimal snippet: prop_001 落叶 entry.

**§6 Media References** (around lines 341-389): add a sub-section (e.g. §6.4 or extend §6.1) documenting the characters/props external png convention. Call out: (a) paths follow `characters/<id>.png` / `props/<id>.png` canonical naming; (b) external png NOT base64 (asset bloat prevention — opposite of frames.json which IS inlined); (c) served by scripts/serve.py (Range-aware not required for pngs — small enough for 200 OK, but serve.py handles them transparently); (d) anti-traversal pattern enforced at schema level (asset.schema.json media.characters[].pattern + characters.schema.json representative_image.pattern).

Add a single-line note at the top of §1 or §4 noting "v1.1 (Phase 5) — see §4 Changelog for the additive extension". Update the SPEC header "Version: 1" → "Version: 1.1 (active) / 1 (v1.0 archived)" or similar.

Do NOT touch §7 Validation (the commands are extended in spec/validate.py — leave the prose pointing at validate.py, optionally add a note about the v1.1 pass). Do NOT touch §8 Out of Scope (those items remain out of scope). Do NOT touch §9 References list except to add the 3 new schema file paths.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "
text = open('spec/SPEC.md').read()
# §4 Changelog has BOTH 1 and 1.1 entries
assert '`1`(initial contract)' in text or '1\`(initial' in text, 'missing v1.0 changelog entry'
assert '`1.1`' in text or '1.1' in text, 'missing v1.1 changelog entry'
# §5.6 + §5.7 exist
assert '### Characters' in text or '## 5.6 Characters' in text or '5.6 Characters' in text, 'no §5.6 Characters section'
assert '### Props' in text or '## 5.7 Props' in text or '5.7 Props' in text, 'no §5.7 Props section'
# §1 schema index has 9 files (characters + props + registry added)
for name in ['characters.schema.json', 'props.schema.json', 'registry.schema.json']:
    assert name in text, f'§1/§9 missing reference to {name}'
# ID patterns mirrored in prose
assert 'char_[0-9]{3}' in text or 'char_001' in text, 'no char_XXX pattern/example in prose'
assert 'prop_[0-9]{3}' in text or 'prop_001' in text, 'no prop_XXX pattern/example in prose'
# schema_version pattern note (lock is producer-side)
assert 'SCHEMA_VERSION' in text or 'producer-side' in text.lower() or 'pattern' in text.lower(), 'no schema_version lock explanation'
# external png not base64
assert 'external' in text.lower() and 'base64' in text.lower(), 'no external-png-not-base64 rationale'
print('OK: SPEC.md §4/§5.6/§5.7/§1/§6 extended + consistent with schemas')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '1.1' spec/SPEC.md` >= 5 (header version + §4 changelog entry + at least 3 references in §5.6/§5.7/§6)
    - `grep -c 'Characters' spec/SPEC.md` >= 2 (§5.6 heading + at least one body reference)
    - `grep -c 'Props' spec/SPEC.md` >= 2 (§5.7 heading + at least one body reference)
    - `grep -E 'char_\[0-9\]\{3\}\$|char_001' spec/SPEC.md` exits 0 (ID pattern mirrored verbatim from schema)
    - `grep -E 'prop_\[0-9\]\{3\}\$|prop_001' spec/SPEC.md` exits 0
    - `grep -c 'states\[\]' spec/SPEC.md` >= 1 AND `grep -c 'looks\[\]' spec/SPEC.md` >= 1 (semantic distinction documented)
    - `grep -cE 'characters\.schema\.json|props\.schema\.json|registry\.schema\.json' spec/SPEC.md` >= 3 (all 3 new schemas referenced)
    - §5.6 + §5.7 each contain a minimal JSON snippet (verify via `grep -A 10 '5.6 Characters' spec/SPEC.md | grep -c '{.*id.*char_001'` >= 1)
    - Manual spot-check: every ID pattern, enum value, and field name in §5.6/§5.7 prose EXACTLY matches the frozen characters.schema.json/props.schema.json (no prose-induced drift — two-tier authority)
  </acceptance_criteria>
  <done>
spec/SPEC.md §4 Changelog has a dated 1.1 entry; §5.6 Characters + §5.7 Props sections added with producer/consumer/field-table/enum/minimal-snippet/reference-schema-footer mirroring the §5.1-§5.5 pattern; §1 schema-file index lists 9 files; §3 asset.json field table shows the 4 new optional fields; §6 media conventions documents the external-png-not-base64 decision; all prose consistent with frozen schemas (two-tier authority preserved).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update spec/README.md layout block + schema index for the 3 new schemas + v1.1 fixture dir</name>
  <files>spec/README.md</files>
  <read_first>
    - spec/README.md (the file being modified — capture the layout block L9-28, "6 个" / "6 schema" mentions)
    - spec/SPEC.md (Task 2 output — README must mirror SPEC §1 schema-file index)
    - spec/schemas/ (confirm the 9 schema filenames)
    - spec/fixtures/ (confirm minimal/ + v1.1/ dirs)
  </read_first>
  <action>
Update spec/README.md to mirror the new directory structure. The README layout block (lines 9-28) currently shows `6 个 schema` + `fixtures/minimal/` only.

**Layout block** (the ASCII tree): add the 3 new schemas to the `schemas/` listing (characters.schema.json, props.schema.json, registry.schema.json — keep alphabetical or by-concern order, your discretion). Add a new entry under `fixtures/` for `v1.1/` showing the 9 files (asset/shots/audio_analysis/transcript/frames/prompts/characters/props/registry.draft). Update the comment "# 6 个机可校验的 JSON Schema 文件" → "# 9 个机可校验的 JSON Schema 文件 (draft 2020-12)".

**Prose body**: update the "## How to validate" section to mention both fixture sets — minimal (6/6) AND v1.1 (9/9). The command stays `python3 spec/validate.py` (Task 1 extended validate.py to do both passes). Update expected output description: "预期输出:6 行 `[valid]` ... + 9 行 `[valid-v11]` ... + smoke 行".

**"## Canonical asset layout"**: optionally add a note about the v1.1 characters/ + props/ external png sub-directories (mirrors §6.4 in SPEC.md from Task 2). Keep it brief — the authoritative reference is SPEC.md §6; README is the directory-level overview.

**"## Schema 版本与 graceful-degrade 规则"**: append a one-line note "v1.1 (Phase 5) 是首个 minor bump — 新增 3 个 optional schema + asset/prompts additive 扩展,见 SPEC.md §4 Changelog"。

Update the "Origin / Provenance" footer timestamp to reflect Phase 5 (2026-07-XX) — keep the original 2026-07-20 line as Phase 01 Plan 01 origin; add a new "Updated: 2026-07-XX (Phase 05 — v1.1 additive extension)" line.

Do NOT rewrite the README from scratch — additive edits only, mirroring the v1.0 style (Chinese prose, ASCII tree, code-fence bash commands).
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "
text = open('spec/README.md').read()
# Layout block lists 9 schemas
for name in ['characters.schema.json', 'props.schema.json', 'registry.schema.json']:
    assert name in text, f'README missing schema reference: {name}'
# v1.1 fixture dir mentioned
assert 'fixtures/v1.1' in text or 'v1.1/' in text, 'no v1.1 fixture dir in layout'
# 9 schemas mentioned somewhere (replaces 6)
import re
six_matches = re.findall(r'\\b6 个', text)
nine_matches = re.findall(r'\\b9 个', text)
assert len(nine_matches) >= 1, 'no 9-个 mention'
# validate command still documented
assert 'python3 spec/validate.py' in text
# v1.1 changelog note added
assert 'v1.1' in text and 'Phase 5' in text, 'no v1.1 Phase 5 note'
print('OK: README.md layout + index + prose updated for v1.1')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'characters.schema.json' spec/README.md` >= 1
    - `grep -c 'props.schema.json' spec/README.md` >= 1
    - `grep -c 'registry.schema.json' spec/README.md` >= 1
    - `grep -c 'v1.1' spec/README.md` >= 2 (fixture dir mention + Phase 5 note)
    - `grep -cE '9 个|9个' spec/README.md` >= 1 (replaces "6 个")
    - `grep -c 'python3 spec/validate.py' spec/README.md` >= 1 (validate command intact)
    - The README layout ASCII tree shows BOTH `fixtures/minimal/` AND `fixtures/v1.1/` directories (manual spot check)
  </acceptance_criteria>
  <done>
spec/README.md layout block lists 9 schema files + 2 fixture dirs (minimal + v1.1); prose body mentions the dual-fixture-set validation; canonical-asset-layout section notes the characters/ + props/ png sub-dirs; graceful-degrade section mentions Phase 5 v1.1 as the first minor bump; "Updated: 2026-07-XX (Phase 05)" timestamp appended.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema-authority → Harness | The harness (verify_contract.py + validate.py) loads schemas as the validation truth; any schema change flows through the harness to producer/consumer verification. |
| Harness → Producer | verify_contract.py producer mode is the regression net catching producer-emitted drift; _cross_version_check + _fixture_consistency_check extend the net to v1.1 + cross-file integrity. |
| SPEC.md prose ↔ Schema | Two-tier authority: schemas are machine-checkable truth, SPEC.md is human-readable overview. Prose MUST mirror schema exactly; on conflict, schema wins. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | Cross-version check uses wrong direction (tautology — v1 fixture vs v1 schema, v1.1 vs v1.1; never tests the actual invariant) | mitigate | _cross_version_check is EXPLICIT per RESEARCH Pitfall D: (a) v1 minimal fixture → v1.1 schema (forward, 0 errors); (b) v1.1 fixture → v1 schema, FILTER to non-additionalProperties errors (backward, 0 errors). The filtering step is the key — additionalProperties errors are EXPECTED and don't count as drift. Acceptance criteria verify the check actually runs (output mentions "cross-version"). |
| T-05-11 | Repudiation | Producer ships v1.1 asset with dangling prompt↔registry IDs (Pitfall C/17) and no automated catch | mitigate | _fixture_consistency_check runs in producer mode alongside EIGHT_SHAPES; catches prompt refs not in characters/props, registry cluster_ids not in characters+props, appearance_shots not in shots. Distinct from Phase 8 PROMPT-03 (full producer integrity); Phase 5 is "fixtures we ship don't ship broken". |
| T-05-12 | Tampering | run_self_test fail-loud path broken by EIGHT_SHAPES rename | mitigate | Acceptance criteria include `PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer` exits 0 with [self-test] PASS in output — empirically confirms fail-loud still catches injected schema_version='v1' drift after the rename. |
| T-05-13 | Information Disclosure | SPEC.md prose drifts from schema (field rename, wrong pattern) → consumers implement against stale prose | mitigate | Task 2 acceptance criteria include exact-string grep checks (char_[0-9]{3}, prop_[0-9]{3}, looks[], states[]) + manual two-tier-authority spot check. Schema remains machine-checkable truth; prose deviations become Phase 5 defects. |
| T-05-14 | Denial of Service | _cross_version_check subprocess git-show hangs or fails on missing tag | mitigate | Primary path `git show v1.0:spec/schemas/<name>.schema.json` uses subprocess.run with check=False (no raise); if returncode != 0, falls back to programmatic property-strip (deep-copy v1.1 schema, delete known-added keys). Planner confirmed v1.0 tag exists. |
| T-05-SC | Tampering | pip/npm/cargo package installs | accept | Zero new packages — harness extension uses only stdlib + jsonschema (already vetted). |
</threat_model>

<verification>
Phase gate (run before /gsd:verify-work):
1. `python3 spec/validate.py` exits 0 — minimal 6/6 + v1.1 9/9 + smoke green.
2. `python3 scripts/verify_contract.py --mode=producer` exits 0 — EIGHT_SHAPES all green + cross-version compat proven + fixture consistency 0 dangling.
3. `PHASE4_SELF_TEST=1 python3 scripts/verify_contract.py --mode=producer` exits 0 — fail-loud still catches drift.
4. `python3 -c "import json; [json.load(open(f'spec/schemas/{s}.schema.json')) for s in ['characters','props','registry','prompts','asset','shots','audio_analysis','transcript','frames']]"` exits 0 — all 9 schemas are valid JSON.
5. Manual review: SPEC.md §5.6/§5.7 prose matches characters/props schemas field-for-field.
6. `git diff --stat` shows 4 modified files (verify_contract.py, validate.py, SPEC.md, README.md) — no collateral.
</verification>

<success_criteria>
- EIGHT_SHAPES + _cross_version_check + _fixture_consistency_check all green in producer mode.
- v1 minimal fixture stays 6/6 green (CONTRACT-09 regression).
- v1.1 fixture set 9/9 green (CONTRACT-01..05 exercised).
- Cross-version compat empirically proven both directions (CONTRACT-07).
- Fail-loud self-test still catches drift (no regression in harness integrity).
- SPEC.md + README.md prose consistent with frozen schemas (CONTRACT-08).
- Phase 5 ready for /gsd:verify-work — contract is locked, documented, and regression-protected.
</success_criteria>

<output>
Create `.planning/phases/05-contract-v1-1/05-04-SUMMARY.md` when done
</output>
