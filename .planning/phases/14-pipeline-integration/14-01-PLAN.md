---
phase: 14-pipeline-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - run_pipeline.py
autonomous: true
requirements: [PIPE-01]
tags: [pipeline-wiring, step-audio-semantic, banner-renumber, cli-flags, force-extension, mtime-cache, subprocess-shellout]

must_haves:
  truths:
    - "run_pipeline.py defines step_audio_semantic(video, work_dir, shots_json, audio_semantic_json, skip, offline, audio_url, audio_timeout) that shells out to analysis/call_audio_analysis.py via subprocess.run([sys.executable, ...], check=True) — mirror step_reid:288-296 subprocess pattern"
    - "main() invokes step_audio_semantic AFTER step_reid (line ~625) and BEFORE step_timeline (line ~630); docstring step list updated to slot audio-semantic as 7/9 (step_timeline becomes 8/9, step_export becomes 9/9)"
    - "ALL 24 `[N/8]` banner instances in run_pipeline.py renumbered to `[N/9]` — `grep -c '\\[.*/8\\]' run_pipeline.py` returns 0 after; `grep -c '\\[.*/9\\]' run_pipeline.py` reflects new count"
    - "5 new CLI flags wired in main() argparse with Chinese help= per CLAUDE.md: --skip-audio-semantic, --audio-url (default http://127.0.0.1:8000/api/production/audio-analysis), --audio-timeout (default 900.0), --skip-speaker-link (controls hint message only; link_speakers NOT auto-invoked), --offline (already exists as global — REUSE, do not re-add)"
    - "step_audio_semantic prints `[7/9]` banners consistent with step_reid/step_semantic (skip label, cache label, run_step label) — grep audit `grep -c '\\[7/9\\]' run_pipeline.py` ≥ 3"
    - "--force explicit-list extended (mirror run_pipeline.py:580-587 pattern): clears audio_semantic.json + speakers.json + route_cache/audio_analysis/ subdirectory. NEVER glob/rmtree entire route_cache (project convention). Existing Phase 6/7 entries preserved."
    - "step_timeline mtime-cache inputs list extended additively with audio_semantic.json + speakers.json (mirror Phase 8 prompts_json addition at run_pipeline.py:347-353). Conditional append (if file exists) — byte-identical to v1.1 when audio_semantic absent."
    - "--skip-speaker-link controls a hint print ONLY (link_speakers.py is non-blocking standalone CLI per CONTEXT decision — operator runs it manually after HITL review, mirror apply_edits.py pattern); step_audio_semantic NEVER subprocess-runs link_speakers"
  artifacts:
    - path: "run_pipeline.py"
      provides: "End-to-end pipeline with step_audio_semantic wired as slot 7/9 + 5 CLI flags + --force extension + step_timeline mtime-cache extension. Mirror of v1.1 Phase 6/7 wiring patterns."
      min_lines: 720
      contains: ["step_audio_semantic", "[7/9]", "[8/9]", "[9/9]", "--skip-audio-semantic", "--audio-url", "--audio-timeout", "--skip-speaker-link", "audio_semantic.json", "speakers.json", "route_cache/audio_analysis"]
  key_links:
    - from: "run_pipeline.py:step_audio_semantic"
      to: "analysis/call_audio_analysis.py"
      via: "subprocess.run([sys.executable, str(HERE/'analysis'/'call_audio_analysis.py'), '--video', video, '--shots', shots_json, '--work-dir', work_dir, '--output', audio_semantic_json, '--stems-dir', stems_dir, '--route-url', audio_url, '--route-timeout', str(audio_timeout)] + (['--offline'] if offline else []), check=True)"
      pattern: "call_audio_analysis\\.py"
    - from: "run_pipeline.py:step_timeline inputs list"
      to: "audio_semantic.json + speakers.json"
      via: "additive conditional append (if os.path.exists) — Pitfall 9 prevention, mirror Phase 8 prompts_json addition"
      pattern: "audio_semantic.*prompts|prompts.*audio_semantic"

objective: Wire `step_audio_semantic` into run_pipeline.py as slot 7/9 (between step_reid[6] and step_timeline[8]) — the central Phase 14 deliverable. Mirror v1.1 step_reid:235-313 subprocess + mtime-cache pattern; add 4 new CLI flags + reuse global --offline; extend --force explicit-list with audio_semantic.json + speakers.json + route_cache/audio_analysis/; extend step_timeline mtime-cache inputs additively. Renumber ALL 24 `[N/8]` banner instances → `[N/9]` (Pitfall: phase-counter drift).

output: Modified run_pipeline.py (~720+ lines, +70 net). Single-task atomic commit.

execution_context:
  @$HOME/.claude/get-shit-done/workflows/execute-plan.md
  @$HOME/.claude/get-shit-done/templates/summary.md

context:
  @.planning/PROJECT.md
  @.planning/ROADMAP.md
  @.planning/STATE.md
  @.planning/phases/14-pipeline-integration/14-CONTEXT.md
  @CLAUDE.md

threat_model:
  threat: T-14
  enforcement: security_enforcement
  scope_files: [run_pipeline.py]
  dispositions:
    - id: T-14-01
      category: destructive-op
      risk: "--force rmtree on whole route_cache would nuke sibling step caches (shot_analysis, character_reid) silently — degrades pipeline correctness without warning. OR: glob-based deletion could expand to operator files outside work_dir."
      mitigation: "EXPLICIT LIST only (mirror run_pipeline.py:580-587 Phase 6/7 pattern). Additive delete list: `audio_semantic.json + speakers.json + route_cache/audio_analysis/ (single subdirectory rmtree, NOT route_cache/ parent)`."
      verify: "grep audit: run_pipeline.py --force block uses literal filenames in a tuple, never glob.glob / shutil.rmtree on route_cache_dir itself (only on specific route_cache/audio_analysis/ subdirectory)"
    - id: T-14-02
      category: injection
      risk: "subprocess shell-out with user-controlled --audio-url could enable argument injection if URL contains shell metacharacters"
      mitigation: "subprocess.run([sys.executable, ...], check=True) — list form (NO shell=True), args are passed as argv not shell-parsed (mirror step_reid:288-296 pattern)"
      verify: "grep audit: step_audio_semantic uses list-form subprocess.run, no `shell=True`, no string concatenation of cmd"
    - id: T-14-03
      category: unsafeguarded-feature
      risk: "auto-invoke link_speakers.py would bypass HITL gate (AF-05 violation: 全自动 speaker→character 映射 必须 HITL)"
      mitigation: "link_speakers is NON-BLOCKING standalone CLI (mirror apply_edits.py); step_audio_semantic NEVER subprocess-runs it; --skip-speaker-link controls only a hint message"
      verify: "grep audit: `grep -n 'link_speakers' run_pipeline.py` returns ONLY comment/docstring references (no subprocess.run call to registry/link_speakers.py)"
---

<tasks>

<task type="auto">
  <name>Task 1: Wire step_audio_semantic + renumber [N/8]→[N/9] + CLI flags + --force extension + step_timeline mtime-cache extension</name>
  <files>run_pipeline.py</files>
  <read_first>
    - run_pipeline.py:235-313 (step_reid — THE pattern to mirror: subprocess shell-out to analysis/call_reid.py, --force explicit-list cache clear, mtime-cache with _safe_mtime + _video_identity, video-stamp sidecar)
    - run_pipeline.py:163-232 (step_semantic — secondary mirror: video-stamp sidecar pattern, --offline forwarding to subprocess, mtime-cache inputs list)
    - run_pipeline.py:316-393 (step_timeline — Phase 8 prompts_json mtime-cache addition pattern at lines 347-353; the inputs list to extend with audio_semantic.json + speakers.json)
    - run_pipeline.py:424-490 (step_export — mirror for mtime-cache inputs list pattern at lines 442-449)
    - run_pipeline.py:493-545 (main() argparse — the location for the 5 new CLI flags; existing --offline at line 509 is global and reused)
    - run_pipeline.py:567-592 (--force explicit-list block at lines 580-587 — the tuple to extend additively)
    - run_pipeline.py:612-632 (main() invocation order — step_reid at 623, step_timeline at 630 — the slot for step_audio_semantic between them)
    - analysis/call_audio_analysis.py:486-518 (the CLI flags the subprocess expects: --video --shots --work-dir --output --stems-dir --route-url --route-timeout --models --language --offline --force)
    - registry/link_speakers.py:1-50 (the docstring confirms it is "独立 standalone CLI" — never invoked from run_pipeline)
    - CLAUDE.md:154-176 (CLI Argument Conventions: kebab-case, Chinese help=, action="store_true" for boolean flags)
  </read_first>
  <behavior>
    After this task, all of the following hold:

    (a) **step_audio_semantic defined.** New function `step_audio_semantic(video, work_dir, shots_json, stems_dir, audio_semantic_json, skip, offline, audio_url, audio_timeout)` that:
        - Prints `[7/9] --skip-audio-semantic: skipping audio semantic analysis` and returns existing-or-None path when `skip=True` (mirror step_reid:267-268)
        - Implements TOCTOU-safe mtime cache (mirror step_reid:269-286): reads `<audio_semantic_json>.video-stamp` sidecar, compares `_video_identity(video)` to cached_video_id, skips subprocess only when (audio_semantic.json exists) AND (mtime > shots.json mtime) AND (cached_video_id == current_video_id). Prints `[7/9] cached audio_semantic: <path>` on cache hit.
        - Shells out to `analysis/call_audio_analysis.py` via `subprocess.run([sys.executable, str(HERE / "analysis" / "call_audio_analysis.py"), "--video", video, "--shots", shots_json, "--work-dir", work_dir, "--output", audio_semantic_json, "--stems-dir", stems_dir, "--route-url", audio_url, "--route-timeout", str(audio_timeout)] + (["--offline"] if offline else []), check=True)` with run_step label `[7/9] audio semantic analysis (audio-analysis route)`
        - Writes video-stamp sidecar best-effort after subprocess (mirror step_reid:296-302)
        - Returns audio_semantic_json path if file exists else None
        - Docstring (Chinese) explains: slot 7/9, third network-dependent step, graceful-degrade contract (route-down → audio_semantic.json absent + [audio] warning sidecar written by call_audio_analysis.py), --offline semantics

    (b) **[N/8] → [N/9] banner renumber.** All 24 `[N/8]` instances renumbered:
        - `[1/8]` → `[1/9]` (3 instances in ensure_h264 at :87, :91, :93)
        - `[2/8]` → `[2/9]` (3 instances in step_detect at :111, :114, :121)
        - `[3/8]` → `[3/9]` (3 instances in step_separate at :129, :132, :140)
        - `[4/8]` → `[4/9]` (3 instances in step_transcribe at :148, :151, :159)
        - `[5/8]` → `[5/9]` (3 instances in step_semantic at :192, :214, :223)
        - `[6/8]` → `[6/9]` (4 instances in step_reid at :267, :285, :295, :312)
        - `[7/8]` → `[8/9]` (2 instances in step_timeline at :357, :392) — step_timeline becomes slot 8 of 9
        - `[8/8]` → `[9/9]` (3 instances in step_export at :439, :471, :480) — step_export becomes slot 9 of 9
        - Total: 24 old instances → 24 renumbered + 3 new `[7/9]` instances in step_audio_semantic = 27 `[N/9]` instances after this task

    (c) **5 new CLI flags wired in main() argparse** (Chinese help= per CLAUDE.md):
        - `--skip-audio-semantic` (action="store_true", help="跳过音频语义分析（audio-analysis 路由调用）")
        - `--audio-url` (default="http://127.0.0.1:8000/api/production/audio-analysis", help="audio-analysis 路由 URL（含 /api/production/audio-analysis path；NO /v1/ —— 10-02-SUMMARY mount-path flag）")
        - `--audio-timeout` (type=float, default=900.0, help="单次 audio-analysis 路由调用 read 超时秒（默认 900，= 路由侧 execFileSync 硬超时；mirror Phase 6 Pitfall 1）")
        - `--skip-speaker-link` (action="store_true", help="跳过 link_speakers 提示（link_speakers.py 本身是独立 CLI —— 操作员手动运行；本 flag 仅控制提示输出）")
        - `--offline` is **already global** at line 509 — REUSE, do not re-add (CONTEXT decision: global, audio-semantic inherits)

    (d) **--force explicit-list extended** (mirror run_pipeline.py:580-587 Phase 6/7 pattern; NEVER glob/rmtree parent route_cache):
        - Add to the delete tuple: `audio_semantic.json`, `speakers.json`, `audio_semantic_json + ".video-stamp"` (Phase 14 WR-01 analog sidecar)
        - Add a SINGLE subdirectory rmtree: `os.path.join(work_dir, "route_cache", "audio_analysis")` (NOT the parent `route_cache/` — that would nuke sibling shot_analysis/character_reid caches silently)
        - Comment updated to reference Phase 14 + the audio_analysis subdirectory (mirror Phase 6/7 comment block at :570-577)

    (e) **step_timeline mtime-cache inputs list extended additively** (mirror Phase 8 prompts_json addition at run_pipeline.py:347-353):
        - step_timeline signature extended with two optional params: `audio_semantic_json: str = None`, `speakers_json: str = None`
        - In step_timeline inputs list construction (currently lines 347-353), conditional append: `if audio_semantic_json and os.path.exists(audio_semantic_json): inputs.append(audio_semantic_json)` + same for speakers_json
        - Pitfall 9 prevention: attach_refs-like regeneration when audio_semantic/speakers mtime changes
        - main() invocation of step_timeline updated to pass `audio_semantic_json=audio_semantic_json, speakers_json=speakers_json`

    (f) **step_audio_semantic invoked between step_reid and step_timeline in main()** (insert at ~line 627, after step_reid call at :623, before stem_basename/video_src setup at :628):
        - Define `audio_semantic_json = os.path.join(work_dir, "audio_semantic.json")` near other path constants (~line 564)
        - Define `speakers_json = os.path.join(work_dir, "speakers.json")` likewise
        - Call: `step_audio_semantic(video, work_dir, shots, stems_dir, audio_semantic_json, args.skip_audio_semantic, args.offline, args.audio_url, args.audio_timeout)`
        - Comment block (mirror step_reid comment at :620-622): explain slot 7/9, third network-dependent step, link_speakers non-blocking standalone CLI hint controlled by --skip-speaker-link
        - When `not args.skip_speaker_link`, print a hint line: `[hint] SPEAKER-01 HITL: after reviewing speaker-review.html, run: python registry/link_speakers.py --audio-semantic {audio_semantic_json} --characters {chars_path} --edits {edits_path} --work-dir {work_dir} --output {speakers_json}` (this is the ONLY behavior of --skip-speaker-link — it's a hint suppressor, not a subprocess invocation)

    (g) **run_pipeline.py module docstring step list updated** (lines 4-21):
        - Insert step 7 entry between current step 6 (reid) and step 7 (timeline) — describe audio-analysis route call + audio_semantic.json product + non-blocking link_speakers
        - Renumber subsequent steps (timeline 7→8, export 8→9)
        - Update usage block at lines 23-35 to mention `--skip-audio-semantic` / `--audio-url` / `--audio-timeout` / `--skip-speaker-link`
        - Update output layout at lines 36-53 to include `audio_semantic.json`, `speakers.json`, `route_cache/audio_analysis/`
  </behavior>
  <action>
    Single cohesive edit to run_pipeline.py (single Python module, all changes mutually-dependent — no benefit to splitting). Apply in order:

    (1) Module docstring step list + usage + output layout (lines 4-53): renumber steps 7-8 → 8-9, insert step 7 audio-semantic, add CLI flag hints, add audio_semantic.json/speakers.json/route_cache/audio_analysis/ to output layout.

    (2) Renumber ALL 24 `[N/8]` → `[N/9]` (use Edit replace_all where the literal is unique per location; for ambiguous ones use the surrounding context as anchor). Verify after: `grep -c '\[.*/8\]' run_pipeline.py` returns 0.

    (3) Define step_audio_semantic function (insert between step_reid ending at :313 and step_timeline starting at :316). Follow step_reid's structure exactly: signature, docstring (Chinese, ~25 lines), skip short-circuit, video-stamp mtime-cache check, subprocess call via run_step helper, video-stamp write, return.

    (4) Extend step_timeline signature with `audio_semantic_json: str = None, speakers_json: str = None` params; extend inputs list conditional append (mirror prompts_json pattern at :352-353).

    (5) Add 5 new argparse flags in main() (after the existing --reid-timeout block at line 525, before --sample-fps at line 526).

    (6) Define `audio_semantic_json` + `speakers_json` path constants near `registry_draft` definition (line 564).

    (7) Extend --force explicit-list tuple (lines 580-587): append `audio_semantic_json`, `speakers_json`, `audio_semantic_json + ".video-stamp"`, and add `os.path.join(work_dir, "route_cache", "audio_analysis")` as a separate subdirectory rmtree entry. Update comment block.

    (8) Insert step_audio_semantic call + --skip-speaker-link hint in main() between step_reid (:623-625) and stem_basename setup (:627-628). Include the conditional hint print.

    (9) Update step_timeline invocation in main() (line 630-632) to pass `audio_semantic_json=audio_semantic_json, speakers_json=speakers_json`.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "import ast; ast.parse(open('run_pipeline.py').read()); print('syntax OK')" && echo '---grep audits---' && echo "old [N/8] count (must be 0): $(grep -c '\[.*/8\]' run_pipeline.py)" && echo "new [N/9] count (must be ≥27): $(grep -c '\[.*/9\]' run_pipeline.py)" && echo "[7/9] count (must be ≥3): $(grep -c '\[7/9\]' run_pipeline.py)" && echo "[8/9] count (must be ≥2): $(grep -c '\[8/9\]' run_pipeline.py)" && echo "[9/9] count (must be ≥3): $(grep -c '\[9/9\]' run_pipeline.py)" && echo "step_audio_semantic defn (must be ≥1): $(grep -c 'def step_audio_semantic' run_pipeline.py)" && echo "CLI flag --skip-audio-semantic (must be ≥2): $(grep -c -- '--skip-audio-semantic' run_pipeline.py)" && echo "CLI flag --audio-url (must be ≥2): $(grep -c -- '--audio-url' run_pipeline.py)" && echo "CLI flag --audio-timeout (must be ≥2): $(grep -c -- '--audio-timeout' run_pipeline.py)" && echo "CLI flag --skip-speaker-link (must be ≥2): $(grep -c -- '--skip-speaker-link' run_pipeline.py)" && echo "link_speakers.py subprocess invocation (must be 0 — non-blocking standalone): $(grep -c 'registry.*link_speakers' run_pipeline.py)" && echo "force glob/rmtree parent route_cache (must be 0): $(grep -E 'shutil\\.rmtree.*(route_cache[^/]|route_cache\\s*\\)|route_cache[\"'\\''\\)]' run_pipeline.py | wc -l)" && echo "audio_analysis subdirectory rmtree (must be ≥1): $(grep -c 'route_cache.*audio_analysis' run_pipeline.py)" && echo "mtime-cache extension inputs (audio_semantic + speakers in step_timeline): $(grep -E '(audio_semantic_json|speakers_json).*inputs\\.append|inputs\\.append.*(audio_semantic_json|speakers_json)' run_pipeline.py | wc -l)"</automated>
  </verify>
  <done>
    - run_pipeline.py parses cleanly (ast.parse OK)
    - `grep -c '\[.*/8\]' run_pipeline.py` == 0 (zero stragglers — Pitfall prevention)
    - `grep -c '\[.*/9\]' run_pipeline.py` ≥ 27 (24 renumbered + 3 new in step_audio_semantic)
    - `grep -c '\[7/9\]' run_pipeline.py` ≥ 3 (skip + cache + run_step labels in step_audio_semantic)
    - `grep -c '\[8/9\]' run_pipeline.py` ≥ 2 (step_timeline cache + run_step labels)
    - `grep -c '\[9/9\]' run_pipeline.py` ≥ 3 (step_export skip + cache + run_step labels)
    - `grep -c 'def step_audio_semantic' run_pipeline.py` == 1
    - 4 new CLI flags present (--skip-audio-semantic, --audio-url, --audio-timeout, --skip-speaker-link)
    - `grep 'registry.*link_speakers' run_pipeline.py` returns ONLY comment/docstring refs (no subprocess.run call — T-14-03 mitigation verified)
    - `grep 'shutil.rmtree.*(route_cache[^/]|route_cache\)|route_cache[")]'` returns 0 (no parent-route_cache rmtree — T-14-01 mitigation verified; the existing line `route_cache_dir = os.path.join(work_dir, "route_cache")` followed by `shutil.rmtree(p, ignore_errors=True)` inside the `if os.path.isdir(p)` branch is OK because that branch only triggers when the tuple entry IS route_cache_dir — for Phase 14 we replace that with the audio_analysis subdirectory; do NOT remove the existing route_cache_dir entry because Phase 6/7 cache-clear semantics must remain)
    - Audio_analysis subdirectory rmtree appears in --force block (T-14-01: explicit-list verified)
    - step_timeline inputs list contains both audio_semantic.json + speakers.json conditional appends (Pitfall 9 prevention)
    - `python3 run_pipeline.py --help` shows new flags with Chinese help text
  </done>
</task>

</tasks>
