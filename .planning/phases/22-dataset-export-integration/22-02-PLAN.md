---
phase: 22-dataset-export-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/schemas/roundtrip-edits.schema.json
  - analysis/roundtrip/apply_edits.py
  - analysis/roundtrip/export_dataset.py
  - tests/test_roundtrip_apply_edits.py
  - tests/test_export_dataset.py
autonomous: true
requirements: [RT-05, DATASET-02, PRESENT-01]
must_haves:
  truths:
  - "roundtrip-edits.schema.json（Draft 2020-12 / additionalProperties:false / 全字段 optional / 空对象 valid；accept_overrides 与 reject_overrides = integer minimum 1 数组 + review_notes string）对 22-01 面板导出的 payload 形状（UI-SPEC §5）合法、对坏 edits（字符串 id / 未知属性 / 负数）非法"
  - "apply CLI confirmed-only：edits 先过 Draft202012Validator 预校验（坏 edits sys.exit 非零、sidecar 零改动）；合法 edits 逐条写 verdict{decision, source:\"human\", decided_at} 并打印审计行 [roundtrip-apply] shot_007 auto→human/accepted + 收尾计数汇总"
  - "human 覆盖是唯一可替换已冻结 verdict 的路径（语义反转点 vs judge.apply_verdict frozen-skip）——被覆盖镜的 regen/scores 半边与其余全部镜 byte 级保留（READ-merge）"
  - "idempotent：同 edits 重放第二遍是 no-op diff——已 human 且同 decision 跳过（汇总 same_decision_replay 计数；accept/reject 两清单交集与非现存 shot_id 均 fail-loud sys.exit）"
  - "export_dataset 对 ep01 真实 work_dir 产 output/dataset/<video-stem>/{shot_010,061,075,084}/{first_frame.jpg,last_frame.jpg,prompt.json} + manifest.json + accepted.txt(4 行) + rejected.txt(15 行，每行 shot_id sim attribution reason摘要可 grep)"
  - "prompt.json 自含可独立消费：prompt_text + 六 facet + character_refs/prop_refs（缺席空列表）+ scores + attribution + regen{engine_name,engine_version,prompt_version,video_content_hash}——dataset 目录内零 asset.json/roundtrip/ 路径引用、帧为拷贝非 symlink（RT-05 消费端独立性）"
  - "manifest 含 τ_sim=0.9670 + 引擎版本 + accepted/rejected 计数 + rejected 分桶（ep01 实测 faithful_below_tau=6 / diverged=9）——DATASET-02 hard-negative 索引可审计"
  artifacts:
  - path: spec/schemas/roundtrip-edits.schema.json
    provides: "roundtrip-edits edits 契约（apply 安全门的一半）"
    contains: "accept_overrides"
  - path: analysis/roundtrip/apply_edits.py
    provides: "confirmed-only HITL 回写 CLI（PRESENT-01 回写半边）"
    min_lines: 200
    contains: ["roundtrip-apply", "source.*human", "Draft202012Validator"]
  - path: analysis/roundtrip/export_dataset.py
    provides: "accepted 子集独立 dataset 目录导出（RT-05 + DATASET-02）"
    min_lines: 200
    contains: ["dataset", "manifest"]
  - path: tests/test_roundtrip_apply_edits.py
    provides: "confirmed-only/idempotent/schema 拒坏单测"
    min_lines: 120
  - path: tests/test_export_dataset.py
    provides: "dataset 目录/字段/分桶/消费端独立性单测"
    min_lines: 120
  key_links:
  - from: "roundtrip_review.html 导出的 roundtrip-edits.json"
    to: "roundtrip.json verdict 半边"
    via: "apply_edits.py schema 预校验 + READ-merge + source:human"
    pattern: "roundtrip-apply"
  - from: "roundtrip.json 冻结 verdict（accepted 4 镜）"
    to: "output/dataset/<video-stem>/shot_NNN/"
    via: "export_dataset.py 按 decision 分桶 + route_cache 帧直拷"
    pattern: "first_frame.jpg"
  - from: "route_cache/h3_regen/frames/kst_{vch}_shot{NNN}_{ff,lf}.jpg"
    to: "dataset/<video-stem>/shot_NNN/{first,last}_frame.jpg"
    via: "shutil.copy2 直拷优先；缺席回落 h3s.extract_endpoint_frames 后改名"
    pattern: "extract_endpoint_frames"
---

<objective>
落地 PRESENT-01 回写半边 + RT-05/DATASET-02 全部：(a) `spec/schemas/roundtrip-edits.schema.json`（registry-edits 直系变体，integer shot_id 是唯一结构差异）+ `analysis/roundtrip/apply_edits.py` confirmed-only 回写 CLI（mirror `registry/apply_edits.py` 硬门骨架；verdict 半边可替换是唯一语义反转）；(b) `analysis/roundtrip/export_dataset.py` accepted 子集独立 dataset 目录导出（首尾帧复用 h3_regen 全分辨率提帧——直拷优先、manifest + accepted/rejected 分清单）。

Purpose: 闭环 HITL 环（面板→edits→apply→sidecar→dataset 随之更新）+ 把 accepted 真值变成消费端零契约依赖的 SFT-grade 数据集目录。
Output: 2 个新模块 + 1 个 schema + 2 个测试文件（全离线零 GPU；ep01 真实数据可作只读烟测）。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dataset-export-integration/22-CONTEXT.md
@.planning/phases/22-dataset-export-integration/22-RESEARCH.md
@.planning/phases/22-dataset-export-integration/22-PATTERNS.md
@registry/apply_edits.py
@analysis/roundtrip/judge.py
</context>

<interfaces>
<!-- 与 22-01 的共享契约 + 可复用共享件（全部本 session 实证） -->

edits payload 契约（22-01 面板产出 / UI-SPEC §5 锁定）：
{"accept_overrides": [7, 12], "reject_overrides": [3], "review_notes": "Exported from roundtrip review HTML on <ISO-8601>"}
- 只收显式覆盖（维持 auto 不进）；与 auto 同向的显式覆盖照导；shot_id 升序；空对象 `{}` = 操作员无改动，valid

importlib 共享件装载（mirror judge.py:100-103，copy 这块）：
importlib.util.spec_from_file_location("h3_regen_shared", Path(__file__).resolve().parent / "h3_regen.py") → h3s
可复用：h3s.extract_endpoint_frames / h3s.resolve_source_video（h264.mp4 → video.mp4 序）/ h3s.video_content_hash / h3s.load_shot_prompts / h3s._atomic_write_json（PID-tmp + os.replace）/ h3s._iter_sidecar_errors（写前自校验）——audit 注释列明用了哪些（judge.py:96-99 惯例）。

verdict 写入形状（judge.py:547-551 同字段，source 换 human）：
{"shot_id": sid, "verdict": {"decision": "accepted"|"rejected", "source": "human", "decided_at": time.strftime("%Y-%m-%dT%H:%M:%S", time.gmtime())}}

READ-merge 循环模板 = scorer.py:347-453 write_scores_sidecar（kept-keys merge + shallow-merge 子对象 + 两层校验 + 坏条目 .bak-<ts> 备份 + 原子写）。**关键语义差**：judge 的 merge 对已冻结 verdict pop 掉（judge.py:371-372）——apply CLI 恰相反，verdict 半边在 edits 命中时**替换**（human 覆盖唯一路径）。

帧 cache 实测形状：route_cache/h3_regen/frames/kst_{vch}_shot{NNN:03d}_{ff,lf}.jpg（ep01 19 对全在）；extract_endpoint_frames 产物名固定 kst_..._ff.jpg——落 dataset 需改名 first_frame.jpg/last_frame.jpg。

ep01 冻结态（本 plan 烟测基准）：19 镜 verdict 全在——accepted = shot 10/61/75/84、rejected 15（faithful<τ=6 + diverged=9）；regen.path 相对 work_dir；prompts.json 平铺 list（shot_id 对齐，character_refs/prop_refs 缺席）。
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: roundtrip-edits.schema.json + apply_edits.py confirmed-only 回写 CLI + 单测</name>
  <files>spec/schemas/roundtrip-edits.schema.json, analysis/roundtrip/apply_edits.py, tests/test_roundtrip_apply_edits.py</files>
  <read_first>
  - registry/apply_edits.py（全文 547 行——直接模板：standalone-CLI docstring/硬门语义 :1-35、_validate schema 预校验 :239-259、固定序 apply 流程 :275-509、_atomic_write :262-268）
  - spec/schemas/registry-edits.schema.json + speaker-edits.schema.json（edits schema 骨架：additionalProperties:false / 全 optional / 空 {} valid）
  - analysis/roundtrip/scorer.py:347-453（READ-merge 循环模板）+ judge.py:503-567（apply_verdict frozen-skip 语义——本 CLI 的反转对象）+ judge.py:321-323（_utc_now_iso）
  - .planning/phases/22-dataset-export-integration/22-PATTERNS.md §apply CLI 模式指派（idempotency guard / 审计行格式）
  </read_first>
  <behavior>
  - schema：Draft 2020-12 / $id https://kais.shot-timeline/spec/schemas/roundtrip-edits.schema.json / title+description+$comment 载明 producer=gen_roundtrip_review.py、consumer=本 CLI、HITL+idempotency 理由 / additionalProperties:false / accept_overrides+reject_overrides: array of integer minimum 1 / review_notes: string / 全字段 optional、空 {} valid
  - apply CLI（analysis/roundtrip/apply_edits.py，discretion 定名——与 roundtrip 三件套同目录便于 importlib）：--work-dir + --edits 两 required flag；独立 standalone（run_pipeline 永不调用，docstring 明文 mirror registry 先例）
  - 流程固定序：读 edits → Draft202012Validator 预校验（iter_errors 前 10 条 + sys.exit 非零）→ 交集检查（同一 shot 在两清单 → sys.exit fail-loud）→ 读 sidecar（缺席 sys.exit）→ 逐条检查 shot_id 存在（未知 id → sys.exit 列出，typo 防护）→ READ-merge 写 verdict{decision, source:"human", decided_at}（verdict 半边替换、其余半边与镜 byte 保留；坏既有条目 .bak-<ts> 备份 mirror scorer 惯例）→ 合并后 payload 过 h3s._iter_sidecar_errors 两层校验 → h3s._atomic_write_json
  - idempotent guard（A3 采纳）：prev.verdict.source == "human" 且 decision 相同 → 跳过不写（decided_at 不漂移）；审计行 `[roundtrip-apply] shot_{sid:03d} {prev_source}→human/{decision}`（跳过镜打 `已 human 同 decision 跳过`）；收尾 `[roundtrip-apply] 完成：applied=N same_decision_replay=K skipped=M`
  - 单测（tmp_path 合成 sidecar，mirror test_judge.py apply/frozen 用例风格）：schema 拒坏三态（字符串 id / 未知属性 / 负数）且 sidecar 零改动；auto→human 覆盖写回 + regen/scores 半边 byte 保留；frozen(auto) 镜被覆盖成功（反转点正测）；同 edits 重放第二遍 byte 级 no-op diff；交集/未知 shot_id fail-loud；空 {} edits no-op 退出 0
  </behavior>
  <action>
  1. 先写 schema（对照 registry-edits 骨架逐字段翻译，integer items 是唯一结构差异）；写 tests/test_roundtrip_apply_edits.py 骨架（上述七断言组）。
  2. 写 analysis/roundtrip/apply_edits.py：importlib 装载 h3s（audit 注释列复用符号）+ jsonschema lazy-import（mirror apply_edits.py :239-259）；merge 循环抄 scorer.py:347-453 骨架但 verdict 半边按 behavior 语义替换/跳过；不复制 "1.3" 等字面量。
  3. ep01 只读烟测：cp roundtrip.json 到 /tmp 后对副本跑一次真 apply（edits={accept_overrides:[3], reject_overrides:[10]}）+ 重放第二遍 diff 为空——证明真实数据形状兼容（ep01 正本零改动）。
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_roundtrip_apply_edits.py -x -q &amp;&amp; EP01=$(ls -d output/虫虫武侠*第01话*/ | head -1) &amp;&amp; rm -rf /tmp/rt_apply_demo_dir &amp;&amp; mkdir -p /tmp/rt_apply_demo_dir &amp;&amp; cp "$EP01/roundtrip.json" /tmp/rt_apply_demo_dir/roundtrip.json &amp;&amp; sha256sum "$EP01/roundtrip.json" | cut -d" " -f1 &gt; /tmp/rt_ep01_before.sha &amp;&amp; printf '{"accept_overrides":[3],"reject_overrides":[10],"review_notes":"demo"}' &gt; /tmp/rt_edits_demo.json &amp;&amp; python3 -c "
import subprocess, sys
r1 = subprocess.run([sys.executable, 'analysis/roundtrip/apply_edits.py', '--work-dir', '/tmp/rt_apply_demo_dir', '--edits', '/tmp/rt_edits_demo.json'], capture_output=True, text=True)
assert r1.returncode == 0, r1.stdout + r1.stderr
assert 'shot_003 auto→human/accepted' in r1.stdout, r1.stdout
assert 'shot_010 auto→human/rejected' in r1.stdout, r1.stdout
before = open('/tmp/rt_apply_demo_dir/roundtrip.json','rb').read()
r2 = subprocess.run([sys.executable, 'analysis/roundtrip/apply_edits.py', '--work-dir', '/tmp/rt_apply_demo_dir', '--edits', '/tmp/rt_edits_demo.json'], capture_output=True, text=True)
assert r2.returncode == 0, r2.stdout + r2.stderr
after = open('/tmp/rt_apply_demo_dir/roundtrip.json','rb').read()
assert before == after, 'replay must be byte-idempotent'
print('22-02 T1 OK — apply CLI live demo + idempotent replay')
" &amp;&amp; sha256sum "$EP01/roundtrip.json" | cut -d" " -f1 &gt; /tmp/rt_ep01_after.sha &amp;&amp; diff /tmp/rt_ep01_before.sha /tmp/rt_ep01_after.sha &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - schema 空 {} valid / 面板 payload 形状 valid / 坏 edits 三态 invalid（测试断言）
  - 坏 edits 或交集或未知 id → 非零退出 + sidecar byte 不动（confirmed-only 硬门）
  - 合法覆盖：verdict{source:human, decided_at} 写回 + 审计行 + 非 verdict 半边保留；重放第二遍 byte 级 no-op
  - ep01 副本演示跑通（auto→human 双向各一条 + 重放幂等），ep01 正本 sha256 前后不变
  </acceptance_criteria>
  <done>PRESENT-01 回写半边收口：面板导出的 edits 有了 schema + confirmed-only + idempotent 的唯一冻结覆盖路径（22-01 测试锁定的 payload 形状被本 task 的 schema 合法性覆盖）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: export_dataset.py 独立 dataset 目录导出 + 单测</name>
  <files>analysis/roundtrip/export_dataset.py, tests/test_export_dataset.py</files>
  <read_first>
  - .planning/phases/22-dataset-export-integration/22-PATTERNS.md §export_dataset 六个 Analog（A importlib 块 / B extract_endpoint_frames 禁手搓 / C shots-prompts join / D 分桶 / E 原子写 / F defensive read）
  - analysis/roundtrip/h3_regen.py:387-417（extract_endpoint_frames——LAST_FRAME_GUARD_SEC/删旧再提/fail-loud）+ :297-335（load_shot_prompts + resolve_source_video）
  - scripts/export_asset.py:370-404（读 sidecar → 计数 → emit 的结构形状；SCHEMA_VERSION 单源 :59 禁复制字面量）
  - .planning/phases/22-dataset-export-integration/22-CONTEXT.md §dataset 导出（RT-05/DATASET-02 锁定决策）
  </read_first>
  <behavior>
  - CLI：--work-dir（required）/ --dataset-root（默认空 → work_dir.parent/"dataset"，即 output/dataset/——Q1 收口：跨视频可累积集合根）/ --tau-sim（float 默认 0.9670，manifest 记录用）
  - 目标目录 = <dataset-root>/<video-stem>/（video-stem = work_dir.name，全角名原样）；shot_NNN 三位补零
  - 遍历 sidecar 冻结 verdict：accepted 镜建 shot_NNN/{first_frame.jpg, last_frame.jpg, prompt.json}；帧来源两级——route_cache/h3_regen/frames/kst_{vch}_shot{NNN:03d}_{ff,lf}.jpg 直拷（shutil.copy2，字节即 h3 实喂帧）→ 缺席回落 h3s.extract_endpoint_frames 提取后改名（同一实现同分辨率确定性）；vch 从该镜 regen.video_content_hash 取
  - prompt.json 自含字段（flat，mirror prompts.json 平键风格 + sidecar 合并）：shot_id / start_sec / end_sec / duration / prompt_text / subject / action / camera / scene / lighting / style / character_refs / prop_refs（缺席空列表）/ scores（原样整块）/ attribution（= scores.judge.attribution 冗余直取，消费端免解包）/ regen{engine_name, engine_version, prompt_version, video_content_hash}
  - manifest.json：video_stem / tau_sim / generated（UTC ISO）/ 引擎版本集合 / accepted_count / rejected_count / rejected 分桶（faithful_below_tau / diverged / underspecified，从冻结 verdict+scores 直接统计不重算）/ shots 索引（shot_id → dir 名）
  - accepted.txt：每行一个 shot_NNN；rejected.txt：每行 `shot_NNN sim={:.4f} {attribution} {reason 前 80 字符}`（可 grep 可审计）
  - 幂等重建：重跑整目录自重建——不在当前 accepted 集的 shot_NNN 目录被清（显式清单删自身目录，绝不 glob/rmtree 父级，T-14-01）；dataset-root 下其它 video-stem 目录不碰
  - roundtrip.json 缺席 → 打印 warning 退出 0（graceful，post-step 语义）；sidecar 有坏条目 → h3s._iter_sidecar_errors 过滤 + warning 跳过，不炸整批
  - **禁**：为 dataset 产物建 schema（RESEARCH Pattern 6——dataset 不是契约面）；symlink（帧必须拷贝）；dataset 文件内引用 asset.json 或 roundtrip/ 相对路径（消费端独立性）
  - 单测（tmp_path 合成 work_dir：手写小 sidecar 2 accepted + 1 rejected + 假帧文件 + prompts.json）：目录/文件名布局、prompt.json 字段集、manifest 分桶、两 txt 行格式、直拷路径命中（假帧字节相等）、回落路径（删假帧后 monkeypatch h3s.extract_endpoint_frames 记录调用）、独立性（manifest+prompt.json 全文无 "asset.json"/"roundtrip/" 依赖）、缺席 sidecar graceful、陈旧 shot 目录 prune
  </behavior>
  <action>
  1. 先写 tests/test_export_dataset.py 骨架（上述九断言组，fixture 全合成零 GPU）。
  2. 写 analysis/roundtrip/export_dataset.py：importlib 装 h3s（audit 注释）→ argparse → defensive 读 sidecar/prompts → 分桶遍历 → 帧两级来源 → prompt.json/manifest/两清单 → _atomic_write_json（jpg 用 copy2 直落）。中文 bracketed print（[roundtrip-dataset] 前缀）+ 收尾计数汇总。
  3. ep01 只读烟测：python3 analysis/roundtrip/export_dataset.py --work-dir "$EP01" --dataset-root /tmp/p22-ds-demo → 断言 4 shot 目录（010/061/075/084）+ accepted.txt 4 行 + rejected.txt 15 行 + manifest faithful_below_tau=6/diverged=9（/tmp 下演示，output/dataset 正式产出留给 22-04 e2e）。
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_export_dataset.py -x -q &amp;&amp; EP01=$(ls -d output/虫虫武侠*第01话*/ | head -1) &amp;&amp; rm -rf /tmp/p22-ds-demo &amp;&amp; python3 analysis/roundtrip/export_dataset.py --work-dir "$EP01" --dataset-root /tmp/p22-ds-demo &amp;&amp; python3 -c "
import json
from pathlib import Path
root = Path('/tmp/p22-ds-demo')
stem = next(root.iterdir())
dirs = sorted(p.name for p in stem.glob('shot_*'))
assert dirs == ['shot_010', 'shot_061', 'shot_075', 'shot_084'], dirs
for d in stem.glob('shot_*'):
    assert (d / 'first_frame.jpg').stat().st_size &gt; 0 and (d / 'last_frame.jpg').stat().st_size &gt; 0
    pj = json.loads((d / 'prompt.json').read_text(encoding='utf-8'))
    for k in ('prompt_text','subject','action','camera','scene','lighting','style','character_refs','prop_refs','scores','attribution','regen'):
        assert k in pj, k
m = json.loads((stem / 'manifest.json').read_text(encoding='utf-8'))
assert m['accepted_count'] == 4 and m['rejected_count'] == 15
assert m['rejected_buckets']['faithful_below_tau'] == 6 and m['rejected_buckets']['diverged'] == 9
assert len((stem / 'accepted.txt').read_text().strip().splitlines()) == 4
assert len((stem / 'rejected.txt').read_text().strip().splitlines()) == 15
print('22-02 T2 OK — ep01 dataset demo complete')
" &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - 合成 fixture 九断言组全绿（布局/字段/分桶/直拷/回落/独立性/graceful/prune/行格式）
  - ep01 只读演示：4 shot 目录 + jpg 非空 + prompt.json 字段齐 + manifest 分桶 6/9 + 两清单 4/15 行
  - dataset 目录内零 asset.json/roundtrip/ 引用、零 symlink；ep01 work_dir 零写入（除 route_cache 只读）
  </acceptance_criteria>
  <done>RT-05 + DATASET-02 模块半边收口：accepted 真值可导出为消费端零契约依赖的独立 dataset 目录——pipeline 挂载（22-03 post-step）与 e2e 齐产证明（22-04）就绪。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edits 文件（浏览器下载/手改） → sidecar verdict | 人机接口：伪造/误输 edits 直接改真值集准入 |
| sidecar 冻结数据 → dataset 真值集 | export 是纯派生，但派生错误会污染 SFT 消费 |
| work_dir 路径 → 文件系统写 | dataset 目录重建涉及删除旧 shot 目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering | 伪造 accept_overrides 篡改 verdict | mitigate | edits schema 预校验（integer minimum 1 / additionalProperties:false 拒自由结构）+ confirmed-only 显式运行 + 逐条审计行 + 交集/未知 id fail-loud |
| T-22-06 | Tampering/Destruction | apply 覆盖时误伤其它半边/其它镜 | mitigate | READ-merge kept-keys（scorer.py:347-453 模板）+ 合并后 _iter_sidecar_errors 两层校验 + .bak-<ts> 备份 + 重放 byte-idempotent 测试 |
| T-22-07 | Tampering | shot_id 路径穿越（dataset 路径构造） | mitigate | edits schema integer 天然拒字符串；dataset 路径全部由 int shot_id `:03d` 格式化构造，无用户字符串进路径 |
| T-22-08 | Tampering | dataset 消费端隐式依赖 asset 契约 | mitigate | 帧 copy2 拷贝非 symlink + manifest/prompt.json 零 asset.json/roundtrip/ 引用 grep 断言（RT-05 独立性机器证明） |
| T-22-09 | Destruction | dataset 重建误删父级/兄弟目录 | mitigate | prune 仅显式清单删自身 shot_NNN 目录（T-14-01 惯例，不 glob/rmtree 父级）+ 单测覆盖 |
| T-22-SC | Tampering | 包安装 | accept | 零新增包（jsonschema/pytest 已在库） |
</threat_model>

<verification>
- Task 1：apply 七断言组 + ep01 副本 live demo（审计行 + 重放 byte 幂等）+ 全套件零回归
- Task 2：export 九断言组 + ep01 只读演示（4 目录/分桶 6:9/清单 4:15 行）+ 全套件零回归
</verification>

<success_criteria>
- SC2 回写半边：edits → confirmed-only apply → source:human verdict，重放安全
- SC4 模块半边：dataset/&lt;video-stem&gt;/ 布局 + manifest hard-negative 索引 + 消费端独立性全部测试锁定
- RT-05/DATASET-02 代码侧完成（pipeline 触发在 22-03，e2e 齐产在 22-04）
</success_criteria>

<output>
Create `.planning/phases/22-dataset-export-integration/22-02-SUMMARY.md` when done（必须含：ep01 副本 apply 演示输出、dataset 演示目录树、两个 discretion 定名记录）
</output>
