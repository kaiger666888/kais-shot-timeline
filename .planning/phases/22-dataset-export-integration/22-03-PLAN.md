---
phase: 22-dataset-export-integration
plan: 03
type: execute
wave: 2
depends_on: ["22-01", "22-02"]
files_modified:
  - run_pipeline.py
  - tests/test_pipeline_roundtrip_wiring.py
  - tests/test_pipeline_vision_wiring.py
  - tests/test_pipeline_vision_seq_wiring.py
autonomous: true
requirements: [PIPE-01]
must_haves:
  truths:
  - "run_pipeline --help 含六新 flag（--skip-roundtrip / --comfy-url / --sample-shots / --regen-resolution / --max-shot-sec / --tau-sim）；parse 默认 tau_sim==0.9670、skip_roundtrip is False、comfy_url=='http://127.0.0.1:8188'"
  - "step_roundtrip 是编号 step [9/10]，位于 step_timeline 与 step_export 之间；subprocess 串 h3_regen → scorer → judge(--apply-verdict --tau-sim 显式透传) → gen_roundtrip_review(四模块 list-form argv)"
  - "外层 mtime+video-stamp cache（roundtrip.json vs shots.json+prompts.json，mirror step_reid :290-307）：命中时跳过 h3_regen/scorer/judge 全部子进程（连带跳过批前 guard）但 roundtrip.json 存在时仍补生成 review HTML；skip/降级短路径打印 [9/10] 前缀日志"
  - "banner 重编号到 [N/10]：27 处 [N/9] 字面量 + :35/:921 两处散文 + 模块 docstring 步骤清单同步；grep '/9]' run_pipeline.py 零存活；[9/10] roundtrip 与 [10/10] ShotTimelineAsset export 两锚在场"
  - "step_export cache inputs 条件性 append roundtrip.json（存在才加——mirror :471-474；绝不无条件 append）；dataset 导出 = export 后 plain-label post-step（keyed on roundtrip.json 存在，自写 check=False graceful，mirror canvas-import :926-953）"
  - "--force 清单不新增 roundtrip 人工数据清理（WR-01 红线保持；注释理由改述 verdict/scores 冻结人工数据）"
  - "两个既有 wiring 测试同步更新后绿 + 新 test_pipeline_roundtrip_wiring.py 绿（regex \\[\\d+/10\\] 形态）"
  artifacts:
  - path: run_pipeline.py
    provides: "step_roundtrip 编排 + 六 flag 透传 + banner [N/10] + step_export 条件 input + dataset post-step"
    contains: ["[9/10]", "[10/10]", "skip-roundtrip"]
  - path: tests/test_pipeline_roundtrip_wiring.py
    provides: "SC1 静态断言（banner/flags/顺序/条件 input/post-step）"
    min_lines: 90
    contains: "\\[\\d+/10\\]"
  - path: tests/test_pipeline_vision_wiring.py
    provides: "重编号同步（regex + 子串锁 [6/10）"
    contains: "[6/10"
  - path: tests/test_pipeline_vision_seq_wiring.py
    provides: "重编号同步（+ 5.5/5.6 plain-label 锁保持）"
    contains: "[5.6/10]"
  key_links:
  - from: "run_pipeline step_roundtrip"
    to: "analysis/roundtrip/h3_regen.py + scorer.py + judge.py + html/gen_roundtrip_review.py"
    via: "run_step subprocess 串（list-form argv；--tau-sim 总是显式；scorer 不透传 --device）"
    pattern: "apply-verdict"
  - from: "run_pipeline step_export"
    to: "work_dir/roundtrip.json"
    via: "条件性 mtime input append（存在才进 inputs）"
    pattern: "roundtrip_json_path"
  - from: "run_pipeline dataset post-step"
    to: "analysis/roundtrip/export_dataset.py"
    via: "plain-label check=False graceful（roundtrip.json 缺席 warning 跳过）"
    pattern: "roundtrip-dataset"
---

<objective>
PIPE-01 落地：`step_roundtrip` 进 run_pipeline 成为编号 step [9/10]（timeline 与 export 之间）——外层 cache 短路 + 四 subprocess 串 + 六 CLI flag 全透传（τ_sim=0.9670 进默认）；banner 重编号 27 处 + 2 散文 + docstring 同步；**Pattern 4 关键修补**：step_export mtime inputs 条件性加 roundtrip.json（不加则 ep01 e2e 陈旧 asset.json cache-hit、SC1 挂载断言必失败）；dataset 导出挂 export 后 plain-label post-step；既有两个 wiring 测试同步更新 + 新 wiring 测试。

Purpose: 闭环进流水线——用户一条命令跑出 roundtrip.json + regen mp4 + review HTML + dataset 目录（SC1 代码半边；ep01 live 证明在 22-04）。
Output: run_pipeline.py 修改 + 3 个 wiring 测试文件（新 1 改 2）。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-dataset-export-integration/22-CONTEXT.md
@.planning/phases/22-dataset-export-integration/22-RESEARCH.md
@.planning/phases/22-dataset-export-integration/22-PATTERNS.md
@.planning/phases/22-dataset-export-integration/22-01-SUMMARY.md
@.planning/phases/22-dataset-export-integration/22-02-SUMMARY.md
@run_pipeline.py
</context>

<interfaces>
<!-- 22-01/22-02 交付的调用面（本 plan 编排对象）+ 编排锚点（全部实证） -->

四模块 CLI（本 plan 只构造 argv，不实现）：
- analysis/roundtrip/h3_regen.py：--work-dir（req）/ --comfy-url（默认 http://127.0.0.1:8188）/ --sample-shots（int 默认 0=全量）/ --regen-resolution（默认 1344x768）/ --max-shot-sec（float 默认 10.0）
- analysis/roundtrip/scorer.py：--work-dir（req）——**不透传 --device**（cuda:0=3060Ti 零竞争是刻意默认 scorer.py:512-514）
- analysis/roundtrip/judge.py：--work-dir（req）/ --comfy-url / --apply-verdict / --tau-sim（自身 default=None 且 apply 无 τ 时 sys.exit :712-716——**不改 judge**，pipeline 默认 0.9670 并总是显式透传）
- html/gen_roundtrip_review.py：--roundtrip / --video / --shots / --prompts / --tau-sim（默认 0.9670）/ --output
- analysis/roundtrip/export_dataset.py：--work-dir / --dataset-root（默认 work_dir.parent/dataset）/ --tau-sim

run_pipeline.py 编排锚点（实测行号）：
- run_step(cmd, label) :122-126（check=True banner helper）；step_reid 外层 cache + video-stamp :290-307（roundtrip 版直接 mirror）；step_reid 第二 subprocess 先例 :327-333
- step_export inputs 列表 :573-580（Pattern 4 修补插入点：列表之后、mtime 计算之前）；条件 append 先例 step_timeline :460-474
- canvas post-step（dataset post-step 模板）:926-953（keyed-on-file + 自写 check=False）
- --force 清单 :753-793（**保持不动**；:768-773 注释理由改述）；HITL hint 先例 :893-904
- skip 短路先例 :212-214；flag 定义区 :696-706（dest/store_false 对 + type=float/int 惯例）

banner 现状（22-PATTERNS 修正后实数）：27 处字面量 = [1..5/9]×3 each + [6/9]×4 + [7/9]×3 + [8/9]×2 + [9/9]×3（export 3 处 → [10/10] 双位数）；散文 [N/9] 两处 :35/:921；docstring 步骤清单 :4-35。
既有测试锁（必须同 task 改，否则 pytest 红）：test_pipeline_vision_wiring.py:102-104（regex r"\[\d/9\]" + "[5.5/9]" not in + "[6/9" in）；test_pipeline_vision_seq_wiring.py:103-106（同 + "[5.6/9]" not in）。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 六 flag + step_roundtrip 编号 step（外层 cache + 四 subprocess）+ banner 重编号 + 既有 wiring 测试同步</name>
  <files>run_pipeline.py, tests/test_pipeline_vision_wiring.py, tests/test_pipeline_vision_seq_wiring.py</files>
  <read_first>
  - run_pipeline.py 全文（965 行——step 序列/banner/flag 区/--force/main 编排全貌）
  - .planning/phases/22-dataset-export-integration/22-PATTERNS.md §run_pipeline.py（MODIFY）模式指派——step_reid cache 块/第二 subprocess/skip 短路/flag 惯例逐段摘录
  - tests/test_pipeline_vision_wiring.py + tests/test_pipeline_vision_seq_wiring.py 全文（regex/子串锁形态 + _capture_namespace spy）
  - .planning/phases/22-dataset-export-integration/22-RESEARCH.md §Pattern 1（插入点论证 + 重编号波及面）+ §Pitfall 1（单位数字 regex 假定）
  </read_first>
  <behavior>
  - 插入点 = step_timeline 之后、step_export 之前（gen_timeline_html 零 roundtrip 引用已证——timeline 不动，只 export 改号 9→10）
  - step_roundtrip(work_dir, video, shots_json, prompts_json, ...)：skip 短路 `[9/10] --skip-roundtrip: skipping ...`；外层 cache = os.path.exists(rt) ∧ _safe_mtime(rt) > _safe_mtime(shots_json) ∧ _safe_mtime(rt) > _safe_mtime(prompts_json) ∧ video-stamp 匹配（mirror :290-307，_safe_mtime 缺席 +inf、_video_identity = path|size|mtime_ns）——命中打印 `[9/10] cached roundtrip sidecar: ...` 后**仍跑** gen_roundtrip_review subprocess（HTML 可能不存在，纯 Python 毫秒级，A2）再 return
  - cache miss 路径四 subprocess（run_step check=True，每模块自带降级 exit 0）：[9/10] roundtrip regen (h3 fl2va) → h3_regen argv（--work-dir/--comfy-url/--sample-shots>0 时/--regen-resolution/--max-shot-sec）；h3_regen 后 roundtrip.json 仍缺席 → 打印降级说明 + 跳过 scorer/judge/HTML（防止无 regen 时空转模型加载）；否则 [9/10] roundtrip scoring → scorer argv（--work-dir only）；[9/10] roundtrip judge (verdict) → judge argv（--work-dir/--comfy-url/--apply-verdict/--tau-sim str(args.tau_sim) 总是显式）；roundtrip.json 存在 → [9/10] roundtrip review HTML → gen argv 六 flag 齐透传（--output work_dir/roundtrip_review.html）
  - 成功链尾写 video-stamp（mirror step_reid）
  - 六 flag 进 argparse（中文 help）：--skip-roundtrip store_true / --comfy-url 默认 http://127.0.0.1:8188 / --sample-shots int 默认 0 / --regen-resolution 默认 1344x768 / --max-shot-sec float 默认 10.0 / --tau-sim float 默认 0.9670
  - banner 重编号：27 处字面量分母 9→10（export 3 处 [9/9]→[10/10]）+ :35/:921 散文 + docstring 步骤清单插 roundtrip 条目并重排 post-step 编号；新 step 自身 banner 用 [9/10]
  - 两个既有 wiring 测试同 commit 改：regex → r"\[\d+/10\]"；"[5.5/9]"→"[5.5/10]"、"[6/9"→"[6/10"、"[5.6/9]"→"[5.6/10]"（plain-label 锁语义保持）
  </behavior>
  <action>
  1. 改 argparse（六 flag，紧邻既有 --sample-fps 数值 flag 区）。
  2. 写 step_roundtrip 函数（插在 step_timeline 与 step_export 函数定义之间），逐段 mirror PATTERNS 摘录的 step_reid 形态；subprocess argv 全 list-form；scorer 不透传 --device；judge 的 --tau-sim 无条件透传。
  3. main() step 调用序列插入 step_roundtrip 调用（timeline 后、export 前），传参对齐。
  4. banner 重编号（27 字面量 + 2 散文 + docstring）；grep 自检 `grep -c "/9\]" run_pipeline.py` == 0。
  5. 同 commit 改两个既有 wiring 测试的三处锁点。
  </action>
  <verify>
    <automated>grep -c "/9\]" run_pipeline.py | grep -qx "0" &amp;&amp; grep -q "\[9/10\] roundtrip" run_pipeline.py &amp;&amp; grep -q "\[10/10\] ShotTimelineAsset export" run_pipeline.py &amp;&amp; grep -q "skip-roundtrip" run_pipeline.py &amp;&amp; grep -q "tau-sim" run_pipeline.py &amp;&amp; python3 run_pipeline.py --help | grep -c -e --skip-roundtrip -e --comfy-url -e --sample-shots -e --regen-resolution -e --max-shot-sec -e --tau-sim | grep -qx "6" &amp;&amp; python3 -m pytest tests/test_pipeline_vision_wiring.py tests/test_pipeline_vision_seq_wiring.py -x -q &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "/9\]" run_pipeline.py` 输出 0；[9/10]/[10/10] 两锚在场；--help 六新 flag 全显
  - 源码序：src.index("step_roundtrip") 落在 timeline 与 export 之间（Task 2 wiring 测试将锁死）
  - 外层 cache 三条件 + video-stamp 在场；cache 命中路径含 review HTML 补生成；sidecar 缺席跳过 scorer/judge 分支在场
  - 两个既有 wiring 测试更新后绿；全套件零回归
  </acceptance_criteria>
  <done>PIPE-01 主体：step_roundtrip 编号挂载 + flags + 重编号完成，pipeline 一条命令可跑 roundtrip 全链（export 挂载修补与 post-step 在 Task 2）。</done>
</task>

<task type="auto">
  <name>Task 2: step_export 条件性 roundtrip input（Pattern 4）+ dataset post-step + 新 wiring 测试</name>
  <files>run_pipeline.py, tests/test_pipeline_roundtrip_wiring.py</files>
  <read_first>
  - run_pipeline.py:560-600（step_export 全函数——inputs 列表/mtime 判定/cached 分支）+ :460-474（条件 append 先例）
  - run_pipeline.py:920-965（canvas post-step 形态 + main 收尾区——dataset post-step 插入点）
  - .planning/phases/22-dataset-export-integration/22-RESEARCH.md §Pattern 2/4 + §Pitfall 2（陈旧 cache 命中——本 phase 最重要一行修补）
  - tests/test_pipeline_vision_seq_wiring.py 全文（新 wiring 测试的四测结构模板：help/defaults/static-index/banner-count）
  </read_first>
  <behavior>
  - Pattern 4（step_export inputs 列表 :573-580 之后、mtime 计算之前插入）：roundtrip_json_path = os.path.join(work_dir, "roundtrip.json"); if os.path.exists(roundtrip_json_path): inputs.append(roundtrip_json_path)——带注释说明「存在且新 → miss 重导出挂载 data.roundtrip；缺席 → 不入 inputs 保 byte-identical-absent；绝不无条件 append（_safe_mtime 缺席=+inf 永久 miss）」
  - dataset post-step（main 内 step_export 调用之后）：rt_json 缺席 → `[roundtrip-dataset] warning: roundtrip.json 不存在（step_roundtrip 被跳过/降级），跳过 dataset 导出`；在场 → cmd = [sys.executable, HERE/"analysis"/"roundtrip"/"export_dataset.py", "--work-dir", work_dir, "--tau-sim", str(args.tau_sim)]，自写 check=False + returncode 判断 + warning（**不用 run_step**——post-step 要求 graceful-degrade，mirror canvas :926-953）；plain-label 打印（无 [N/M] 前缀）
  - 可选 HITL hint（mirror speaker-link hint :893-904）：roundtrip_review.html 生成后打印 apply CLI 用法一行
  - 新 tests/test_pipeline_roundtrip_wiring.py 四测（mirror seq_wiring 结构）：(1) --help 六 flag 在场；(2) _capture_namespace spy 断言默认值（tau_sim==0.9670 / skip_roundtrip is False / comfy_url=='http://127.0.0.1:8188' / sample_shots==0 / regen_resolution=='1344x768' / max_shot_sec==10.0）；(3) 静态 wiring——src.index("step_roundtrip") < src.index("step_export(work_dir")；'"--apply-verdict"' 与 '"--tau-sim"' argv 构造在 roundtrip/export 索引窗内；'"[9/10] roundtrip' 与 '[10/10] ShotTimelineAsset export' 字符串在场；条件 input 锁——step_export 函数源段内 "roundtrip.json" 与 os.path.exists 共现且无无条件 append 形态；post-step 锁——"export_dataset.py" 与 check=False 共现；(4) banner 计数——re.findall(r"\[\d+/10\]", src) 数量 ≥ 30；assert not re.findall(r"/9\]", src)；"[5.5/10]" not in src；"[5.6/10]" not in src（plain-label 锁语义延续）
  </behavior>
  <action>
  1. step_export 内插 Pattern 4 三行（连注释）。
  2. main 的 step_export 调用后插 dataset post-step 块（keyed on rt_json；自写 subprocess.run check=False + OSError 捕获双防线，mirror canvas）+ review HTML hint 打印。
  3. --force 注释 :768-773 理由改述（「roundtrip verdict/scores 是冻结人工数据（human 覆盖走 apply CLI）；route_cache rmtree 已覆盖三模块 cache」）——清单本身零改动。
  4. 写 tests/test_pipeline_roundtrip_wiring.py（四测如 behavior；_capture_namespace 直接拷 seq_wiring :20-46 实现）。
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_pipeline_roundtrip_wiring.py -x -q &amp;&amp; python3 -m pytest tests/test_pipeline_vision_wiring.py tests/test_pipeline_vision_seq_wiring.py -q &amp;&amp; grep -n "inputs.append(roundtrip_json_path)" run_pipeline.py &amp;&amp; grep -n "export_dataset.py" run_pipeline.py &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - Pattern 4 条件 append 在场且被 wiring 测试锁死（存在才 append）
  - dataset post-step：roundtrip.json 缺席 → warning 跳过不 raise；在场 → subprocess check=False；两形态均有静态锁
  - 新 wiring 四测绿；全套件（171+ 既有 + 22-01/22-02 新增）零回归
  - --force 清单 diff 为零（仅注释文字变化）
  </acceptance_criteria>
  <done>PIPE-01 全部收口：编号 step + flags + 重编号 + 条件挂载 + post-step 五件套齐——22-04 ep01 e2e 的全部管线侧前置就绪。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags → 子进程 argv | 编排层构造 argv 串四个模块（flag 拼错/漏传 = 静默错行为） |
| mtime cache → 挂载正确性 | export cache 判定决定 data.roundtrip 是否进 asset.json |
| --force → 冻结人工数据 | 误清 roundtrip.json/roundtrip/ = 毁 verdict 真值（WR-01 红线） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-09 | Tampering | 无条件 roundtrip.json 进 export inputs → 永久 cache miss 每跑重写 asset.json（破坏 byte-identical-absent） | mitigate | Pattern 4 条件 append（os.path.exists 守卫）+ wiring 静态锁 + 22-04 S1 场景 md5 复验 |
| T-22-10 | Destruction | --force 清单误含 roundtrip 人工数据 | mitigate | 清单零改动 + 注释理由改述 + T-14-01 显式清单惯例（不 glob/rmtree 父级） |
| T-22-11 | Tampering | subprocess argv 注入（flag 值含 shell 元字符） | mitigate | 全 list-form argv（T-14-02）；prompt_text 绝不进 argv（h3_regen 先例）；wiring 测试锁 argv 构造形态 |
| T-22-12 | Tampering | 陈旧 asset.json cache-hit 导致 data.roundtrip 挂载缺席（静默错） | mitigate | Pattern 4 使 roundtrip.json 新于 asset.json 时强制 miss；22-04 e2e 断言 schema "1.3" + accepted_count 4 |
| T-22-13 | DoS | 无外层 cache 时每跑都过 h3_regen 批前 guard（TTS kill + /free + 最长 1800s eye-wait 阻塞） | mitigate | 外层 mtime+video-stamp cache 短路整个 subprocess 链（mirror step_reid） |
| T-22-SC | Tampering | 包安装 | accept | 零新增包 |
</threat_model>

<verification>
- Task 1：grep 零 `/9]` 存活 + 两锚在场 + --help 六 flag + 既有 wiring 测试更新后绿 + 全套件绿
- Task 2：Pattern 4 + post-step 在场 + 新 wiring 四测绿 + 全套件绿
</verification>

<success_criteria>
- SC1 代码半边全闭合：编号 step / flags / 重编号 / 条件挂载 / post-step 五件套 + 三层测试锁（两个既有更新 + 一个新建）
- 反模式全避开：不无条件 append / post-step 不用 run_step / scorer 不吃 --device / --force 不动人工数据
</success_criteria>

<output>
Create `.planning/phases/22-dataset-export-integration/22-03-SUMMARY.md` when done（必须含：banner 重编号前后计数、step_roundtrip 源码索引窗、Pattern 4 插入行号、--force 清单 diff=0 证明）
</output>
