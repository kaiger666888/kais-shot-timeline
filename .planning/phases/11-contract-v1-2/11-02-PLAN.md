---
phase: 11-contract-v1-2
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - spec/fixtures/v1.2/asset.json
  - spec/fixtures/v1.2/audio_semantic.json
  - spec/fixtures/v1.2/speakers.json
  - spec/validate.py
  - scripts/verify_contract.py
autonomous: true
requirements: [CONTRACT-03, CONTRACT-05]
tags: [fixture, validation, bidirectional-proof, cross-version]

must_haves:
  truths:
    - "`spec/fixtures/v1.2/` contains exactly 12 JSON files: 10 byte-copied from v1.1 + 2 new (`audio_semantic.json` + `speakers.json`)"
    - "`spec/fixtures/v1.2/asset.json` has `schema_version: \"1.2\"` + `data.audio_semantic: \"audio_semantic.json\"` + `data.speakers: \"speakers.json\"`"
    - "`spec/fixtures/v1.2/audio_semantic.json` uses real Phase-10 spike outputs (HAPPY/emo_unk emotion labels, Speech events, word-level timestamps from WhisperX) — NOT hand-fabricated"
    - "`spec/fixtures/v1.2/speakers.json` uses synthetic-but-shape-correct `^spk_[0-9]{3}$` IDs + at least one `char_id` that resolves to the existing `characters.json#id` (`char_001`/`char_002`)"
    - "`spec/validate.py` runs a 3-tier gate: minimal (6 shapes) + v1.1 (10 shapes) + v1.2 (12 shapes) — all three must be 0-failure for exit code 0"
    - "`scripts/verify_contract.py` extends `_cross_version_check` with v1.1↔v1.2 forward (0 errors) + backward (only additionalProperties errors) passes — both GREEN"
    - "`scripts/verify_contract.py` extends `_fixture_consistency_check` with `speakers.char_id ⊆ characters.id` (where characters present) — catches Pitfall 17 (speaker→character dangling refs)"
    - "`scripts/verify_contract.py` adds `_recover_v11_schema(shape)` using `git show v1.1:spec/schemas/<shape>.schema.json` as PRIMARY path + programmatic-strip fallback"
  artifacts:
    - path: "spec/fixtures/v1.2/asset.json"
      provides: "v1.2 fixture manifest (schema_version=1.2 + data.audio_semantic + data.speakers)"
      contains: "audio_semantic"
    - path: "spec/fixtures/v1.2/audio_semantic.json"
      provides: "Realistic v1.2 audio sidecar (per-shot dialogue/sfx + reproduction prompts from ep01 spike)"
      contains: "word_level_experimental"
    - path: "spec/fixtures/v1.2/speakers.json"
      provides: "Synthetic-but-shape-correct v1.2 speaker registry (spk_NNN + nullable char_id)"
      contains: "spk_001"
    - path: "spec/validate.py"
      provides: "3-tier shape gate (minimal + v1.1 + v1.2)"
      contains: "V12_ORDER"
    - path: "scripts/verify_contract.py"
      provides: "Bidirectional v1.0↔v1.1↔v1.2 cross-version proof + speakers⊆characters consistency check"
      contains: "_recover_v11_schema"
  key_links:
    - from: "spec/fixtures/v1.2/asset.json#data.audio_semantic"
      to: "spec/fixtures/v1.2/audio_semantic.json"
      via: "relative path string"
      pattern: "audio_semantic\\.json"
    - from: "spec/fixtures/v1.2/speakers.json#speakers[].char_id"
      to: "spec/fixtures/v1.2/characters.json#[].id"
      via: "ID reference (consistency-check enforced)"
      pattern: "char_001"
    - from: "scripts/verify_contract.py:_cross_version_check"
      to: "scripts/verify_contract.py:_recover_v11_schema"
      via: "function call in v1.2→v1.1 backward pass"
      pattern: "_recover_v11_schema"
---

<objective>
Build the v1.2 12-file fixture (using real Phase-10 spike data) + extend the validation harness with a 3-tier shape gate (minimal/v1.1/v1.2) + extend `verify_contract.py` with bidirectional v1.1↔v1.2 cross-version proof + `speakers.char_id ⊆ characters.id` consistency check. This is the empirical proof layer that turns Plan 01's contract definitions into a machine-verified guarantee.

Purpose: The bidirectional cross-version proof is the correctness gate for the entire v1.2 contract (CONTEXT § specifics — non-negotiable). The fixture is the realistic-shape proof that the schemas accept real Phase-10 spike outputs (not hand-fabricated happy-path data). The 3-tier gate makes the proof part of the per-commit feedback loop.

Output: `spec/fixtures/v1.2/` (12 files, 10 byte-copied + 2 authored) + `spec/validate.py` V12 extension + `scripts/verify_contract.py` bidirectional + consistency extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-contract-v1-2/11-CONTEXT.md
@.planning/phases/11-contract-v1-2/11-RESEARCH.md
@.planning/phases/11-contract-v1-2/11-01-SUMMARY.md

<interfaces>
<!-- Existing v1.1 validate.py + verify_contract.py structures the executor extends. -->

spec/validate.py existing constants (lines 51-71):
  V11_FIXTURE_DIR = SPEC_DIR / "fixtures" / "v1.1"
  V11_FIXTURE_MAP = {  # 10 entries (asset/shots/audio_analysis/transcript/frames/prompts/characters/props/registry/registry-edits)
      "asset": "asset.json", ..., "registry": "registry.draft.json", "registry-edits": "registry.edits.json",
  }
  V11_ORDER = ["asset", "shots", "audio_analysis", "transcript", "frames", "prompts", "characters", "props", "registry", "registry-edits"]

spec/validate.py existing functions:
  validate_minimal() -> int   # iterates MINIMAL_ORDER (6 shapes)
  validate_v11() -> int       # iterates V11_ORDER (10 shapes), prints [valid-v11] / [FAIL-v11]
  main()                      # runs minimal + v1.1 + smoke, sums failures, exits 0 or 1

scripts/verify_contract.py existing structures:
  EIGHT_SHAPES (line 78) — 9 elements (legacy name; actually 9 = 6 v1.0 + characters/props/registry)
  SIX_SHAPES = EIGHT_SHAPES[:6]  # the 6 v1.0 shapes
  _recover_v1_schema(shape) at lines 275-316 — git show v1.0 + programmatic-strip fallback
  _cross_version_check() at lines 319-385 — currently does v1.0↔v1.1 forward (minimal × current) + backward (v1.1 × recovered-v1.0)
  _fixture_consistency_check() at lines 388-488 — v1.1 fixture cross-file ID checks (prompts→characters/props; characters/props→shots; registry→characters+props+shots)

Phase-10 spike real model outputs (for audio_semantic.json fixture realism):
  spike/audio/results/ser_sensevoice_ep01.json per_sample[2]: shot_id=77, predicted_emotion="HAPPY", predicted_events=["Speech"], proxy_confidence=1.0, clean_text="真的，说话算话。😊"
  spike/audio/results/ser_sensevoice_ep01.json per_sample[0]: shot_id=43, predicted_emotion="emo_unk", predicted_events=[], proxy_confidence=1.0
  spike/audio/results/whisperx_align_ep01.json — word-level timestamps + boundary drift median 101.5ms

v1.1 fixture asset.json (the substrate for v1.2/asset.json): see spec/fixtures/v1.1/asset.json
  - schema_version: "1.1", data block has 7 keys (5 required + characters + props), media has characters[]/props[] PNG arrays
  - v1.2/asset.json bumps schema_version to "1.2" + adds data.audio_semantic + data.speakers (data block now 9 keys)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build 12-file v1.2 fixture (10 byte-copied + audio_semantic.json + speakers.json + asset.json bump)</name>
  <files>spec/fixtures/v1.2/asset.json, spec/fixtures/v1.2/audio_semantic.json, spec/fixtures/v1.2/speakers.json</files>
  <read_first>
    - `spec/fixtures/v1.1/asset.json` (FULL — the substrate to copy and extend)
    - `spec/fixtures/v1.1/characters.json` (FULL — provides `char_001`/`char_002` IDs that speakers.json must link to)
    - `spec/fixtures/v1.1/shots.json` (FULL — provides shot IDs 1+2 that audio_semantic.json + speakers.json turns must reference)
    - `spec/fixtures/v1.1/transcript.json` (FULL — provides dialogue text + timing for realistic audio_semantic.json)
    - `spike/audio/results/ser_sensevoice_ep01.json` (head — real SenseVoice outputs for emotion/events/confidence)
    - `spike/audio/results/whisperx_align_ep01.json` (head — real WhisperX word-level timestamps)
    - `.planning/phases/11-contract-v1-2/11-RESEARCH.md` Example 5 (fixture realism, lines 932-973)
    - `spec/schemas/audio_semantic.schema.json` (Plan 01 output — the schema this fixture must satisfy)
    - `spec/schemas/speakers.schema.json` (Plan 01 output — the schema this fixture must satisfy)
  </read_first>
  <behavior>
    - `spec/fixtures/v1.2/` contains 12 files: the 10 v1.1 files byte-copied (`cp spec/fixtures/v1.1/{file} spec/fixtures/v1.2/{file}`) + 2 new (`audio_semantic.json` + `speakers.json`).
    - `spec/fixtures/v1.2/asset.json` differs from v1.1 ONLY in: `schema_version: "1.2"`, `data.audio_semantic: "audio_semantic.json"`, `data.speakers: "speakers.json"`, and `generator.version: "0.3.0-spec-fixture-v1.2"` (mirror v1.1's `"0.2.0-spec-fixture-v1.1"` convention). All other fields byte-identical.
    - `audio_semantic.json` validates against Plan 01's `audio_semantic.schema.json`. Contains 2 shots referencing shot IDs that exist in `spec/fixtures/v1.2/shots.json` (shot_id 1 and 2). Shot 1 carries real SenseVoice outputs (HAPPY/`["Speech"]`/confidence 1.0 + word-level timestamps from WhisperX + spk_id linking to speakers.json). Shot 2 carries `emo_unk`/empty events (real model output for silence/non-speech). `word_level_experimental: true` at top level. NO `instruments` field.
    - `speakers.json` validates against Plan 01's `speakers.schema.json`. Contains 2 speakers: `spk_001` with `char_id: "char_001"` (link to existing characters.json — happy path) + `spk_002` with `char_id: null` (旁白/群杂 — nullable path). Both `review_state: "confirmed"`. Each speaker has `total_speech_sec` + at least 1 turn referencing shot_id 1 or 2.
    - `speakers.json#speakers[].char_id` non-null values ⊆ `characters.json#id` set (the consistency check in Task 3 enforces this).
  </behavior>
  <action>
    Step 1: Create the directory + copy the 10 v1.1 files byte-identical:
    ```
    mkdir -p spec/fixtures/v1.2
    cp spec/fixtures/v1.1/{shots,audio_analysis,transcript,frames,prompts,characters,props}.json spec/fixtures/v1.2/
    cp spec/fixtures/v1.1/registry.draft.json spec/fixtures/v1.2/registry.draft.json
    cp spec/fixtures/v1.1/registry.edits.json spec/fixtures/v1.2/registry.edits.json
    cp spec/fixtures/v1.1/asset.json spec/fixtures/v1.2/asset.json   # then edit in Step 2
    ```
    Verify byte-identical via `diff` (should be empty for the 9 non-asset files).

    Step 2: Edit `spec/fixtures/v1.2/asset.json` — bump `schema_version` to `"1.2"`, bump `generator.version` to `"0.3.0-spec-fixture-v1.2"`, add 2 keys to the `data` block (after `props`):
    ```
    "audio_semantic": "audio_semantic.json",
    "speakers": "speakers.json"
    ```
    Do NOT touch `media` (v1.2 adds no new media types), do NOT touch `generator.registry_snapshot` (v1.2 adds no equivalent), do NOT touch `generator.warnings` (existing v1.1 warnings stay).

    Step 3: Author `spec/fixtures/v1.2/audio_semantic.json`. Use the RESEARCH Example 5 shape (lines 932-973) as the starting point. Adapt shot IDs to the fixture's shots.json (which has shots 1 and 2 — see `spec/fixtures/v1.1/shots.json`). For shot 1, use real SenseVoice HAPPY outputs + plausible WhisperX word-level timestamps (3-4 words). For shot 2, use real SenseVoice `emo_unk` + empty events (representing silence/non-speech — the spike's shot_id 43 is a real model example). Both shots carry a `reproduction` block: shot 1 has non-null `tts` (Chinese NL prompt + confidence 0.7 + fidelity_disclaimer) + null `music_gen` + null `foley`; shot 2 has all three null (no audio content to reproduce). Set `word_level_experimental: true` at top level. NEVER use an `instruments` field.

    Step 4: Author `spec/fixtures/v1.2/speakers.json`. Top-level `{speakers: [...]}` shape (mirror registry.schema.json's top-level shape). Two speakers:
    - `{spk_id: "spk_001", char_id: "char_001", total_speech_sec: <number>, review_state: "confirmed", turns: [{shot_id: 1, start_sec: 0.0, end_sec: 1.5}]}` — links to existing char_001 (少女).
    - `{spk_id: "spk_002", char_id: null, total_speech_sec: <number>, review_state: "confirmed", turns: [{shot_id: 2, start_sec: 1.5, end_sec: 3.0}]}` — 旁白/群杂 (nullable char_id path).

    Use `json.dump(obj, f, indent=2, ensure_ascii=False)` for Chinese content (project convention).
  </action>
  <verify>
    <automated>python3 -c "
import json, os
from jsonschema import Draft202012Validator as V

# (1) 12-file fixture inventory
v12_dir = 'spec/fixtures/v1.2'
files = sorted(os.listdir(v12_dir))
assert len(files) == 10, f'expected 10 files in v1.2 (the 8 .json + registry.draft + registry.edits = 10 from v1.1) + audio_semantic + speakers = 12; got {len(files)}: {files}'
assert 'audio_semantic.json' in files, 'audio_semantic.json missing'
assert 'speakers.json' in files, 'speakers.json missing'

# (2) 9 v1.1 files byte-identical (all except asset.json which gets the schema_version bump)
import filecmp
for f in ['shots.json','audio_analysis.json','transcript.json','frames.json','prompts.json','characters.json','props.json','registry.draft.json','registry.edits.json']:
    assert filecmp.cmp(f'spec/fixtures/v1.1/{f}', f'spec/fixtures/v1.2/{f}', shallow=False), f'{f} drifted from v1.1'

# (3) asset.json bump
asset = json.load(open(f'{v12_dir}/asset.json'))
assert asset['schema_version'] == '1.2', f'schema_version not 1.2: {asset[\"schema_version\"]}'
assert asset['data'].get('audio_semantic') == 'audio_semantic.json'
assert asset['data'].get('speakers') == 'speakers.json'

# (4) audio_semantic + speakers validate against Plan 01 schemas
audio_sem = json.load(open(f'{v12_dir}/audio_semantic.json'))
V(json.load(open('spec/schemas/audio_semantic.schema.json'))).validate(audio_sem)
spk = json.load(open(f'{v12_dir}/speakers.json'))
V(json.load(open('spec/schemas/speakers.schema.json'))).validate(spk)

# (5) Phase-10-informed deviations enforced
assert 'instruments' not in json.dumps(audio_sem), 'audio_semantic.json has instruments field (FORBIDDEN)'
assert audio_sem.get('word_level_experimental') is True, 'word_level_experimental flag missing'
# (6) speakers.char_id ⊆ characters.id (preview of Task 3 check)
chars = {c['id'] for c in json.load(open(f'{v12_dir}/characters.json'))}
for s in spk['speakers']:
    if s.get('char_id') is not None:
        assert s['char_id'] in chars, f'speaker {s[\"spk_id\"]} char_id {s[\"char_id\"]} not in characters'
print('[fixture-ok] 12-file v1.2 fixture validates; deviations enforced; char_id ⊆ characters.id')
"</automated>
  </verify>
  <acceptance_criteria>
    - `ls spec/fixtures/v1.2/ | wc -l` returns 10 (file count; 8 `.json` + `registry.draft.json` + `registry.edits.json` = 10 from v1.1, + 2 new = 12 files total — wait: v1.1 has 10 files: `asset.json`, `shots.json`, `audio_analysis.json`, `transcript.json`, `frames.json`, `prompts.json`, `characters.json`, `props.json`, `registry.draft.json`, `registry.edits.json`. So v1.2 should have 12 = these 10 + `audio_semantic.json` + `speakers.json`. The ls count should be 12.)
    - `ls spec/fixtures/v1.2/ | wc -l` returns 12 (corrected assertion).
    - `diff spec/fixtures/v1.1/shots.json spec/fixtures/v1.2/shots.json` exits 0 (byte-identical). Same for the other 8 non-asset v1.1 files.
    - `python3 -c "import json; a=json.load(open('spec/fixtures/v1.2/asset.json')); print(a['schema_version'], sorted(a['data'].keys()))"` prints `1.2 ['audio_analysis', 'audio_semantic', 'characters', 'frames', 'prompts', 'props', 'shots', 'speakers', 'transcript']` (9 data keys).
    - `python3 -c "import json; from jsonschema import Draft202012Validator as V; V(json.load(open('spec/schemas/audio_semantic.schema.json'))).validate(json.load(open('spec/fixtures/v1.2/audio_semantic.json')))"` exits 0.
    - `python3 -c "import json; from jsonschema import Draft202012Validator as V; V(json.load(open('spec/schemas/speakers.schema.json'))).validate(json.load(open('spec/fixtures/v1.2/speakers.json')))"` exits 0.
    - `! grep -ic 'instrument' spec/fixtures/v1.2/audio_semantic.json` (no instruments field — Phase-10 deviation).
    - `grep -c 'word_level_experimental' spec/fixtures/v1.2/audio_semantic.json` returns ≥1.
    - `grep -c 'spk_001\|spk_002' spec/fixtures/v1.2/speakers.json` returns ≥2.
  </acceptance_criteria>
  <done>
    `spec/fixtures/v1.2/` contains exactly 12 files; 9 non-asset files byte-identical to v1.1; `asset.json` carries `schema_version:"1.2"` + `data.audio_semantic` + `data.speakers`; `audio_semantic.json` validates + uses real Phase-10 spike outputs + has NO `instruments` field; `speakers.json` validates + carries `spk_001`/`spk_002` + `spk_001` links to existing `char_001`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend spec/validate.py with V12 fixture constants + validate_v12() + 3-tier exit-code gate</name>
  <files>spec/validate.py</files>
  <read_first>
    - `spec/validate.py` (FULL — 247 lines; esp. V11_FIXTURE_DIR/MAP/ORDER at lines 55-71, validate_v11 at lines 119-151, main at lines 198-242)
    - `.planning/phases/11-contract-v1-2/11-RESEARCH.md` Pattern 4 (validate.py V12 mirror — lines 516-571)
  </read_first>
  <behavior>
    - `spec/validate.py` defines `V12_FIXTURE_DIR = SPEC_DIR / "fixtures" / "v1.2"`, `V12_FIXTURE_MAP` (12 entries), `V12_ORDER` (12 entries) — exact mirror of the v1.1 block.
    - New `validate_v12() -> int` function — line-for-line clone of `validate_v11()` (lines 119-151) with directory/map constants swapped + print prefix `[valid-v12]` / `[FAIL-v12]`.
    - `main()` runs minimal + v1.1 + v1.2 (NEW) + smoke, sums failures into `total_strict_failures`, exits 0 or 1.
    - Closing print includes `v1.2 failures=N` alongside `minimal` and `v1.1`.
    - Existing v1.0 (minimal) + v1.1 tiers remain byte-identical in behavior (no regression).
  </behavior>
  <action>
    Three surgical edits to `spec/validate.py`:

    (1) After the v1.1 block (line 71, after `V11_ORDER = [...]`), append the v1.2 mirror (per RESEARCH Pattern 4 lines 538-562):

    ```
    # v1.2 fixture set (Phase 11 additive) —— 12 shapes. minimal + v1.1 + v1.2 三阶 gate。
    # 复用 v1.1 的 10 个 substrate 文件名（asset/shots/audio_analysis/transcript/frames/
    # prompts/characters/props/registry/registry-edits）+ 新增 audio_semantic/speakers。
    # speaker-edits 推迟到 Phase 13（HITL 流程落地才有 fixture）。
    V12_FIXTURE_DIR = SPEC_DIR / "fixtures" / "v1.2"
    V12_FIXTURE_MAP = {
        # 10 v1.1 entries verbatim:
        "asset": "asset.json",
        "shots": "shots.json",
        "audio_analysis": "audio_analysis.json",
        "transcript": "transcript.json",
        "frames": "frames.json",
        "prompts": "prompts.json",
        "characters": "characters.json",
        "props": "props.json",
        "registry": "registry.draft.json",
        "registry-edits": "registry.edits.json",
        # 2 NEW v1.2 shapes:
        "audio_semantic": "audio_semantic.json",
        "speakers": "speakers.json",
    }
    V12_ORDER = [
        "asset", "shots", "audio_analysis", "transcript", "frames", "prompts",
        "characters", "props", "registry", "registry-edits",
        "audio_semantic", "speakers",
    ]
    ```

    (2) After `validate_v11()` ends (line 151), add `validate_v12()` — clone of `validate_v11` changing only: the docstring (mention Phase 11 + 12 shapes = 10 v1.1 substrate + 2 v1.2 additive), the iteration target (V12_ORDER + V12_FIXTURE_DIR + V12_FIXTURE_MAP), and the print prefix (`[valid-v12]` / `[FAIL-v12]`). Reuse `load_validator` + `_format_errors` — do NOT duplicate.

    (3) In `main()` (lines 198-242), between the v1.1 block and the smoke block, add:
    ```
    print(f"[validate] v1.2 fixture = {V12_FIXTURE_DIR}")
    v12_failures = validate_v12()
    ```
    Extend `total_strict_failures = minimal_failures + v11_failures + v12_failures` (currently `minimal_failures + v11_failures`). Update the closing print (line 232) to include `v1.2 failures={v12_failures}`.
  </action>
  <verify>
    <automated>python3 spec/validate.py 2>&1 | tee /tmp/validate_v12.out
    # assertions
    python3 -c "
output = open('/tmp/validate_v12.out').read()
assert '[valid-v12] asset' in output, 'v1.2 asset fixture not validated'
assert '[valid-v12] audio_semantic' in output, 'audio_semantic fixture not validated'
assert '[valid-v12] speakers' in output, 'speakers fixture not validated'
assert 'v1.2 failures=0' in output, f'v1.2 failures not 0 in output'
assert 'minimal failures=0' in output, 'minimal tier regressed'
assert 'v1.1 failures=0' in output, 'v1.1 tier regressed'
print('[validate.py] 3-tier gate GREEN')
"
    # exit code gate
    python3 spec/validate.py >/dev/null 2>&1 && echo 'exit 0 OK' || echo 'FAILED exit non-zero'</automated>
  </verify>
  <acceptance_criteria>
    - `python3 spec/validate.py` exits 0.
    - Output contains `[valid-v12] asset`, `[valid-v12] audio_semantic`, `[valid-v12] speakers`, `[valid-v12] shots`, ... (12 `[valid-v12]` lines).
    - Output contains `minimal failures=0, v1.1 failures=0, v1.2 failures=0`.
    - `grep -c 'V12_FIXTURE_DIR\|V12_FIXTURE_MAP\|V12_ORDER\|validate_v12' spec/validate.py` returns ≥4 (all new symbols defined).
    - `python3 -c "import ast; ast.parse(open('spec/validate.py').read())"` exits 0.
    - `python3 -c "from jsonschema import Draft202012Validator; import spec.validate as v; assert len(v.V12_ORDER) == 12; assert set(v.V12_ORDER) == set(v.V11_ORDER) | {'audio_semantic', 'speakers'}"` exits 0 (12-order superset of v1.1).
  </acceptance_criteria>
  <done>
    `spec/validate.py` runs a 3-tier gate; minimal (6 shapes) + v1.1 (10 shapes) + v1.2 (12 shapes) all GREEN; exit code 0; v1.2 fixture 12/12 schema-valid.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend scripts/verify_contract.py with _recover_v11_schema + v1.1↔v1.2 bidirectional proof + speakers⊆characters consistency</name>
  <files>scripts/verify_contract.py</files>
  <read_first>
    - `scripts/verify_contract.py:60-90` (REPO/SCHEMAS_DIR constants + EIGHT_SHAPES/SIX_SHAPES legacy)
    - `scripts/verify_contract.py:275-316` (`_recover_v1_schema` — line-for-line template for the new `_recover_v11_schema`)
    - `scripts/verify_contract.py:319-385` (`_cross_version_check` — currently does v1.0↔v1.1 forward + backward; extend with v1.1↔v1.2 passes)
    - `scripts/verify_contract.py:388-488` (`_fixture_consistency_check` — currently checks prompts→characters/props, characters/props→shots, registry→characters+props; extend for v1.2 speakers→characters)
    - `.planning/phases/11-contract-v1-2/11-RESEARCH.md` Pattern 5 (verify_contract.py bidirectional extension — lines 573-639) + Code Examples 3 + 4 (lines 830-928)
    - `git tag --list 'v1*'` (verify v1.0 + v1.1 tags exist for `_recover_v1_schema` + new `_recover_v11_schema`)
  </read_first>
  <behavior>
    - `_recover_v11_schema(shape)` recovers the v1.1 schema for a given shape via `git show v1.1:spec/schemas/<shape>.schema.json` (PRIMARY) with programmatic-strip fallback (deep-copy current schema + `.pop()` the v1.2-additive keys: `data.audio_semantic` + `data.speakers` for asset shape).
    - `_cross_version_check()` returns a `(bool, str)` tuple covering THREE bidirectional pairs: v1.0↔v1.1 (existing, GREEN), v1.1↔v1.2 forward (NEW — v1.1 fixture × v1.2 schema = 0 errors), v1.2↔v1.1 backward (NEW — v1.2 fixture × recovered-v1.1 schema = only additionalProperties errors).
    - `_fixture_consistency_check()` extends to v1.2 fixture: loads `spec/fixtures/v1.2/speakers.json` (when present) + `spec/fixtures/v1.2/characters.json`, asserts every non-null `speakers[].char_id` resolves to a `characters.json#id`. Asserts every `speakers[].spk_id` matches `^spk_[0-9]{3}$`.
    - Existing v1.0↔v1.1 cross-version proof + v1.1 fixture consistency remain byte-identical in behavior.
    - Producer mode of `verify_contract.py` (the default invocation) returns GREEN for all three: schema-validate 12 v1.2 shapes + bidirectional + consistency.
  </behavior>
  <action>
    Four surgical edits to `scripts/verify_contract.py`:

    (1) **Add `_recover_v11_schema(shape)` function** — insert immediately AFTER `_recover_v1_schema` (which ends at line 316). Line-for-line clone, changing:
    - git ref: `v1.0:spec/schemas/...` → `v1.1:spec/schemas/...`
    - docstring: "Phase 11 backward check v1.2→v1.1 (CONTRACT-03)"
    - fallback strip keys: for `asset` shape, in addition to v1.1's `data.characters`/`data.props` (which v1.0 didn't have — preserved by `_recover_v1_schema`), ALSO strip v1.2's `data.audio_semantic` + `data.speakers`. (The fallback path: copy current v1.2 schema → pop v1.1-additive + v1.2-additive keys → result is equivalent to v1.1 schema.)

    Recommended parameterized form (per RESEARCH Example 4 lines 907-927):
    ```
    def _recover_v11_schema(shape: str):
        """恢复 v1.1 schema 用于 backward cross-version check v1.2→v1.1 (Phase 11 CONTRACT-03)。

        Primary: ``git show v1.1:spec/schemas/<shape>.schema.json``。Fallback: 程序化剥离
        v1.2 additive keys（asset: data.audio_semantic + data.speakers）从当前 schema。
        """
        try:
            r = subprocess.run(
                ["git", "-C", str(REPO), "show", f"v1.1:spec/schemas/{shape}.schema.json"],
                capture_output=True, text=True, timeout=5,
            )
            if r.returncode == 0 and r.stdout.strip():
                return json.loads(r.stdout)
        except (subprocess.SubprocessError, json.JSONDecodeError, OSError):
            pass
        # Fallback: programmatic strip of v1.2-additive keys from current schema
        import copy
        try:
            stripped = copy.deepcopy(
                json.loads((SCHEMAS_DIR / f"{shape}.schema.json").read_text(encoding="utf-8"))
            )
        except (FileNotFoundError, json.JSONDecodeError):
            return None
        if shape == "asset":
            data_props = stripped.get("properties", {}).get("data", {}).get("properties", {})
            for k in ("audio_semantic", "speakers"):
                data_props.pop(k, None)
        return stripped
    ```

    (2) **Extend `_cross_version_check`** — after the existing v1.0↔v1.1 backward loop ends (line 377), add the v1.1↔v1.2 passes before the final return (around line 378):

    ```
    # (c) Phase 11 FORWARD v1.1→v1.2: v1.1 fixture × current (v1.2-extended) schemas → 0 errors
    # Only asset — speakers/audio_semantic are NEW shapes with no v1.1 instance to test.
    v12_dir_for_forward = REPO / "spec" / "fixtures" / "v1.1"  # v1.1 fixture = pre-v1.2 instance
    for shape in ("asset",):
        try:
            schema = json.loads((SCHEMAS_DIR / f"{shape}.schema.json").read_text(encoding="utf-8"))
            instance = json.loads((v12_dir_for_forward / f"{shape}.json").read_text(encoding="utf-8"))
        except (FileNotFoundError, json.JSONDecodeError) as e:
            failures.append(f"forward v1.1→v1.2 {shape}: load failed: {e}")
            continue
        errs = list(Draft202012Validator(schema).iter_errors(instance))
        if errs:
            failures.append(
                f"forward v1.1→v1.2 {shape}: v1.1 fixture rejected by v1.2 schema "
                f"with {len(errs)} error(s); first: {errs[0].message}"
            )

    # (d) Phase 11 BACKWARD v1.2→v1.1: v1.2 fixture × recovered-v1.1 schema → ONLY additionalProperties errors
    v12_dir = REPO / "spec" / "fixtures" / "v1.2"
    for shape in ("asset",):
        v11_schema = _recover_v11_schema(shape)
        if v11_schema is None:
            failures.append(
                f"backward v1.2→v1.1 {shape}: could not recover v1.1 schema "
                f"(git show v1.1 + programmatic strip both failed)"
            )
            continue
        try:
            instance = json.loads((v12_dir / f"{shape}.json").read_text(encoding="utf-8"))
        except (FileNotFoundError, json.JSONDecodeError) as e:
            failures.append(f"backward v1.2→v1.1 {shape}: load failed: {e}")
            continue
        errs = list(Draft202012Validator(v11_schema).iter_errors(instance))
        non_addprop = [e for e in errs if e.validator != "additionalProperties"]
        if non_addprop:
            failures.append(
                f"backward v1.2→v1.1 {shape}: {len(non_addprop)} non-additionalProperties "
                f"error(s) (shared fields drifted); first: {non_addprop[0].message}"
            )
    ```

    Update the success message (around line 383) to: `"v1.0↔v1.1↔v1.2 cross-version bidirectional compat proven (forward 0 errors; backward 0 non-additive errors)"`.

    (3) **Extend `_fixture_consistency_check`** — after the existing v1.1 checks end (around line 487), add a v1.2 fixture block. Load `spec/fixtures/v1.2/` (if exists) and check speakers→characters:
    ```
    # Phase 11 v1.2 fixture consistency: speakers.char_id ⊆ characters.id (Pitfall 17)
    v12_fix_dir = REPO / "spec" / "fixtures" / "v1.2"
    if v12_fix_dir.is_dir():
        spk_path = v12_fix_dir / "speakers.json"
        if spk_path.is_file():
            try:
                speakers_data = json.loads(spk_path.read_text(encoding="utf-8"))
            except json.JSONDecodeError as e:
                failures.append(f"v1.2 speakers.json: invalid JSON: {e}")
                speakers_data = None
            if isinstance(speakers_data, dict):
                # Reuse characters.json from v1.2 fixture (byte-copied from v1.1)
                chars_v12_path = v12_fix_dir / "characters.json"
                chars_v12_ids = set()
                if chars_v12_path.is_file():
                    try:
                        for c in json.loads(chars_v12_path.read_text(encoding="utf-8")):
                            if isinstance(c, dict):
                                chars_v12_ids.add(c.get("id"))
                    except (json.JSONDecodeError, TypeError):
                        pass
                for spk in speakers_data.get("speakers", []):
                    sid = spk.get("spk_id")
                    if not (isinstance(sid, str) and re.match(r"^spk_[0-9]{3}$", sid)):
                        failures.append(f"v1.2 speakers.json: spk_id malformed: {sid!r}")
                    cid = spk.get("char_id")
                    if cid is not None and cid not in chars_v12_ids:
                        failures.append(
                            f"v1.2 speakers.json {sid}: char_id {cid!r} not in "
                            f"v1.2 characters.json IDs (Pitfall 17 — speaker→character dangling)"
                        )
    ```
    Update the success message to mention "v1.1 + v1.2 fixture set cross-file IDs consistent".

    (4) **Update the `EIGHT_SHAPES` legacy comment** (lines 74-82) to note that v1.2 adds `audio_semantic` + `speakers` to the recognized shape set when `validate_eight_shapes` (or equivalent) is invoked in producer mode. If `validate_eight_shapes` exists and iterates a fixed shape list, extend that list to include the 2 new shapes (gated on file existence, mirror the v1.1 characters/props pattern). If unsure where the shape list lives, grep for `EIGHT_SHAPES` usages and extend each consistently.
  </action>
  <verify>
    <automated>python3 scripts/verify_contract.py --mode=producer 2>&1 | tee /tmp/verify_contract_v12.out
    python3 -c "
output = open('/tmp/verify_contract_v12.out').read()
# bidirectional proof
assert 'v1.0↔v1.1↔v1.2 cross-version bidirectional compat proven' in output, f'bidirectional proof not GREEN: {output[-500:]}'
# consistency
assert 'v1.1 + v1.2 fixture set cross-file IDs consistent' in output or 'v1.2 speakers' not in output, 'consistency check missing or failing'
# exit code
import subprocess
r = subprocess.run(['python3','scripts/verify_contract.py','--mode=producer'], capture_output=True, text=True)
assert r.returncode == 0, f'verify_contract.py exited {r.returncode}:\n{r.stdout[-1000:]}\n{r.stderr[-500:]}'
print('[verify_contract] bidirectional v1.0↔v1.1↔v1.2 GREEN + speakers⊆characters consistency GREEN')
"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/verify_contract.py --mode=producer` exits 0.
    - Output contains `v1.0↔v1.1↔v1.2 cross-version bidirectional compat proven (forward 0 errors; backward 0 non-additive errors)`.
    - Output contains `v1.1 + v1.2 fixture set cross-file IDs consistent` (or equivalent — the v1.2 speakers check passes).
    - `grep -c '_recover_v11_schema' scripts/verify_contract.py` returns ≥2 (1 definition + 1 call site).
    - `git show v1.1:spec/schemas/asset.schema.json | python3 -c "import json,sys; s=json.load(sys.stdin); print(sorted(s['properties']['data']['properties'].keys()))"` prints the v1.1 keys WITHOUT `audio_semantic`/`speakers` (proves the recover path will yield a true v1.1 schema — primary path baseline).
    - Tampering probe: introduce a deliberate `char_id: "char_999"` (non-existent) into a temp copy of speakers.json and re-run — verify_contract.py MUST fail with a Pitfall 17 dangling message. (Sanity gate — executor need not commit this; just verify the check catches it.)
  </acceptance_criteria>
  <done>
    `verify_contract.py --mode=producer` exits 0 with bidirectional v1.0↔v1.1↔v1.2 cross-version proof GREEN (forward 0 errors + backward only-additionalProperties) + v1.2 fixture consistency (speakers.char_id ⊆ characters.id) GREEN. The `_recover_v11_schema` helper exists + is called by the extended `_cross_version_check`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture dir → schema validator | `validate.py` + `verify_contract.py` load JSON fixture files from `spec/fixtures/v1.2/` and feed them to `Draft202012Validator` — fixture content is trusted dev artifact (no untrusted input crosses here) |
| git history → schema recovery | `_recover_v11_schema` invokes `subprocess.run(["git", "-C", REPO, "show", "v1.1:spec/schemas/..."])` to read schemas from the immutable v1.1 tag |
| schema → TS type generation | Downstream consumers (Phase 17 canvas) may auto-generate TS types from these schemas; schema drift would silently propagate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering / Repudiation | Schema-invalid fixture ships (silent interop bug) | mitigate | Task 2 acceptance: `python3 spec/validate.py` exits 0; `[valid-v12]` for all 12 shapes; v1.2 tier counts toward `total_strict_failures` (fail-loud, not silent). Mirrors v1.1 fail-loud pattern at validate.py:226. |
| T-11-05 | Information Disclosure / DoS on old consumers | Cross-version drift (v1.2 schema accidentally breaks v1.1 forward compat; v1.2 fixture drifts from shared v1.1 field shapes) | mitigate | Task 3 acceptance: `_cross_version_check` extended with v1.1↔v1.2 forward (v1.1 fixture × v1.2 schema = 0 errors) + backward (v1.2 fixture × recovered-v1.1 schema = ONLY additionalProperties). The backward filter is load-bearing — shared fields drift would surface as non-addProp errors. |
| T-11-06 | Tampering | `_recover_v11_schema` fallback yields wrong shape (false-positive backward GREEN) | mitigate | PRIMARY path is `git show v1.1:...` (immutable tag — verified via `git tag --list 'v1*'`). Fallback only fires if git unavailable or tag missing. Acceptance criterion verifies the v1.1 git tag actually yields the v1.1 schema (no audio_semantic/speakers keys) — proves PRIMARY path baseline. |
| T-11-07 | Tampering | `speakers.char_id → characters.id` dangling (Pitfall 17) — silent semantic corruption | mitigate | Task 3 extends `_fixture_consistency_check` with the `speakers.char_id ⊆ characters.id` check. Acceptance: tampering probe (introducing `char_999`) MUST trigger a failure. |
| T-11-08 | Repudiation | Producer emits v1.2 fixture content claiming `schema_version: "1.2"` but with v1.1-shape audio_semantic (would pass schema but lie about contract version) | accept | Phase 11 emits the fixture as a spec example; Phase 12+ producer emits real audio_semantic.json from route round-trip. The contract is locked in Phase 11; producer conformance is a Phase 12+ concern. The acceptance criterion `asset['schema_version'] == '1.2'` is the structural gate; deeper content-shape audit happens via the validate_v12 12-shape gate. |
| T-11-SC | Tampering | pip / package installs | accept (carried from Plan 01) | Phase 11 installs ZERO packages. jsonschema 4.26.0 already verified. |
</threat_model>

<verification>
After all 3 tasks:

1. `python3 spec/validate.py` exits 0; output shows `minimal failures=0, v1.1 failures=0, v1.2 failures=0` (3-tier gate).
2. `python3 scripts/verify_contract.py --mode=producer` exits 0; output shows `v1.0↔v1.1↔v1.2 cross-version bidirectional compat proven` + `v1.1 + v1.2 fixture set cross-file IDs consistent`.
3. `ls spec/fixtures/v1.2/ | wc -l` returns 12.
4. `diff spec/fixtures/v1.1/shots.json spec/fixtures/v1.2/shots.json` exits 0 (byte-identical substrate).
5. `python3 -c "import json; from jsonschema import Draft202012Validator as V; V(json.load(open('spec/schemas/audio_semantic.schema.json'))).validate(json.load(open('spec/fixtures/v1.2/audio_semantic.json'))); V(json.load(open('spec/schemas/speakers.schema.json'))).validate(json.load(open('spec/fixtures/v1.2/speakers.json')))"` exits 0.

Pre-existing gates (must remain green):
- `git show v1.1:spec/fixtures/v1.1/asset.json` matches current `spec/fixtures/v1.1/asset.json` byte-identical (Phase 11 does NOT touch v1.1 fixtures).
- `python3 spec/validate.py` v1.0 minimal + v1.1 tiers still GREEN (regression check).
</verification>

<success_criteria>
- CONTRACT-03 (3-tier + bidirectional half): `validate.py` runs minimal/v1.1/v1.2 3-tier gate (all green); `verify_contract.py` extends `_cross_version_check` with v1.1↔v1.2 forward + backward bidirectional proof + adds `_recover_v11_schema` helper.
- CONTRACT-05 (byte-identical-absent + consistency half): v1.1 fixture 10/10 still green under extended schemas (forward proof); v1.2 fixture 12/12 green under new V12_ORDER; `speakers.char_id ⊆ characters.id` consistency check enforced.
- 12-file v1.2 fixture uses real Phase-10 spike outputs (SenseVoice HAPPY/emo_unk + WhisperX word-level) — not hand-fabricated happy-path data.
- No regression: v1.0 minimal (6 shapes) + v1.1 (10 shapes) tiers remain byte-identical in behavior.
</success_criteria>

<output>
Create `.planning/phases/11-contract-v1-2/11-02-SUMMARY.md` when done.
</output>
