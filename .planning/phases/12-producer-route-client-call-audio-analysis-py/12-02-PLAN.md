---
phase: 12-producer-route-client-call-audio-analysis-py
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - tests/audio_analysis_stub_server.py
  - tests/fixtures/audio_analysis_stub_response_empty.json
  - tests/fixtures/audio_analysis_stub_response_nonempty.json
  - tests/run_audio_analysis_smoke.sh
autonomous: false
requirements: [PIPE-02]
tags: [stub-round-trip, sc4-proof, smoke-harness, graceful-degrade-proof, cache-proof]

must_haves:
  truths:
    - "Running `bash tests/run_audio_analysis_smoke.sh` exercises the client against a local stub returning the Phase 10 stub envelope shape and ALL 5 scenarios pass (route-up-write, cache-hit, poisoned-cache-invalidate, full-degrade-empty-stub, offline-empty-cache)"
    - "Scenario 1 proves: route up + non-empty data → audio_semantic.json written + schema-valid + cache file created + zero [audio] warnings"
    - "Scenario 2 proves: re-run --offline → cache hit logged + audio_semantic.json byte-identical to scenario 1 (deterministic)"
    - "Scenario 3 proves: corrupted cache file (schema-invalid shot) → auto-invalidated + unlinked + client degrades + [audio] warning appended"
    - "Scenario 4 proves: stub returning empty shots[] (Phase 10 stub_mode:true shape) + --force → audio_semantic.json NOT written + [audio] warning present + pre-existing [semantic]/[reid] tags in warnings.json preserved (read-merge-write non-destructive)"
    - "Scenario 5 proves: --offline --force + empty cache → audio_semantic.json NOT written + [audio] warning appended (byte-identical v1.1 asset CONTRACT-05)"
    - "Live cross-repo stub (if running on localhost:10589 per 10-02-SUMMARY) round-trip is best-effort cross-checked; graceful-skip with documented rationale if Connection refused (mirror v1.1 CAST deferred pattern)"
  artifacts:
    - path: "tests/audio_analysis_stub_server.py"
      provides: "Minimal http.server stub returning a frozen audio-analysis envelope on POST /api/production/audio-analysis (configurable via --fixture + --port)"
      min_lines: 35
      contains: ["do_POST", "audio-analysis", "Content-Length", "application/json"]
    - path: "tests/fixtures/audio_analysis_stub_response_empty.json"
      provides: "Frozen snapshot of the Phase 10 stub response (shots:[], stub_mode:true) — the SC#4 stub-mode integration target"
      contains: ["stub_mode", "shots", "code"]
    - path: "tests/fixtures/audio_analysis_stub_response_nonempty.json"
      provides: "Shape-correct non-empty route response (1 shot with dialogue/sfx/reproduction) built from Phase 10 spike results shape — exercises cache write + normalize + schema-validate paths"
      contains: ["dialogue", "emotion", "words", "reproduction"]
    - path: "tests/run_audio_analysis_smoke.sh"
      provides: "Bash smoke orchestration running 5 scenarios (route-up/cache-hit/poisoned/full-degrade/offline-empty) + optional live cross-repo stub cross-check"
      min_lines: 80
      contains: ["stub_server", "audio_semantic.json", "SCENARIO", "exit 1"]
  key_links:
    - from: "tests/run_audio_analysis_smoke.sh"
      to: "analysis/call_audio_analysis.py"
      via: "subprocess invocation with --route-url http://127.0.0.1:<port>/api/production/audio-analysis"
      pattern: "call_audio_analysis\\.py"
    - from: "tests/run_audio_analysis_smoke.sh"
      to: "tests/audio_analysis_stub_server.py"
      via: "background start with --fixture + --port; kill on exit"
      pattern: "audio_analysis_stub_server\\.py"
    - from: "tests/audio_analysis_stub_server.py"
      to: "tests/fixtures/audio_analysis_stub_response_*.json"
      via: "reads fixture file → returns as response body on POST"
      pattern: "fixture"
---

<objective>
Prove SC#4 (Phase 12 ROADMAP): the producer client `call_audio_analysis.py` correctly parses the Phase 10 stub envelope, writes/reads the per-shot cache, and merges `[audio]` warnings — all BEFORE route ML goes live. This is the deterministic, fixture-based proof that the client integrates with the documented stub contract the moment the route host is up.

Purpose: Phase 12's contract is "stub-only round-trip" (CONTEXT.md specifics). The actual Phase 10 stub lives cross-repo (`kais-aigc-platform:feat/audio-analysis-route`) and may not be running. This plan delivers a LOCAL deterministic stub harness that exercises every code path in the client (route-up, cache-hit, poisoned-cache, full-degrade, offline-empty) — plus a best-effort live cross-check against the cross-repo stub if it happens to be reachable. Mirrors the Phase 10 ROUTE-01 smoke pattern (`spike/audio/tests/route_stub_smoke.sh`) applied to the producer side.

Output: 4 test artifacts (stub server + 2 frozen fixtures + bash smoke script). No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-producer-route-client-call-audio-analysis-py/12-CONTEXT.md
@.planning/phases/12-producer-route-client-call-audio-analysis-py/12-01-PLAN.md
@.planning/phases/10-risk-validation-spike-route-stub/10-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- The client under test (from Plan 01) + the Phase 10 stub envelope (the integration target). -->

## Client under test (analysis/call_audio_analysis.py — produced by Plan 01)

CLI: `python3 analysis/call_audio_analysis.py --video <v> --shots <s> --work-dir <w> --output <o> --stems-dir <d> [--route-url <u>] [--offline] [--force]`
Key behaviors:
  - POSTs {video, shots, shot_id_range:[N,N], stems_dir, models, language} to ROUTE_PATH
  - Cache: route_cache/audio_analysis/shot_{sid:03d}.json with _cache_key {video_content_hash, route_name, route_version}
  - Poisoned cache: schema-validate on hit; unlink + re-fetch on fail
  - Full degrade (zero shots with data): audio_semantic.json NOT written
  - Warnings sidecar: route_cache/warnings.json read-merge-write with STEP_TAG="[audio]"

## Phase 10 stub envelope (10-02-SUMMARY.md:166 — the SC#4 target)

Empty stub (models unloaded — what the live cross-repo stub returns today):
    {"code":200,
     "data":{"shots":[],"count":0,"errors":[],"stub_mode":true,
             "message":"Phase 10 stub: ML models not loaded. Producer client envelope round-trip proven."},
     "message":"Audio analysis stub"}

Non-empty shape (when ML lands — shape reference for the nonempty fixture):
    {"code":200,
     "data":{"shots":[{<audio_semantic.schema.json#shots[]>-shaped payload>}],
     "count":1,"errors":[],"stub_mode":false},
     "message":"Audio analysis complete"}

## Spike result shapes (for the nonempty fixture — realistic data)

- spike/audio/results/ser_sensevoice_ep01.json — emotion + events shape (HAPPY/ANGRY/NEUTRAL/emo_unk + Speech/BGM/Applause tags; self_consistency_pct as emotion_confidence source).
- spike/audio/results/whisperx_align_ep01.json — word-level timestamps shape ({start, end, text, score} per word; boundary drift median 101.5ms).
- spec/fixtures/v1.2/audio_semantic.json (57 lines) — the CANONICAL shape-correct example (2 shots, one full one sparse). The nonempty fixture should mirror this shape.

## Smoke script analog (the pattern to follow)

spike/audio/tests/route_stub_smoke.sh (Phase 10 ROUTE-01 stub smoke) — bash orchestration pattern: start server in background → curl probes → assert response markers → kill server on exit. Plan 02 adapts this to the PRODUCER side: start stub_server → invoke the client → assert file outputs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local stub server + frozen fixtures + 5-scenario smoke script</name>
  <files>tests/audio_analysis_stub_server.py, tests/fixtures/audio_analysis_stub_response_empty.json, tests/fixtures/audio_analysis_stub_response_nonempty.json, tests/run_audio_analysis_smoke.sh</files>
  <read_first>
    - scripts/serve.py (117 lines) — the project's existing http.server pattern (RangeRequestHandler, ThreadingHTTPServer, simple arg parsing). The stub server follows this minimal style (~40 lines, no Range support needed — just POST echo).
    - spike/audio/tests/route_stub_smoke.sh (Phase 10 smoke analog — bash pattern: background server start, curl probes, trap cleanup, exit-on-assert-fail).
    - .planning/phases/10-risk-validation-spike-route-stub/10-02-SUMMARY.md (lines 156-186) — the EXACT Phase 10 stub response bytes (line 166 is the empty-stub envelope to snapshot into the empty fixture).
    - spec/fixtures/v1.2/audio_semantic.json (57 lines) — the CANONICAL non-empty shape. The nonempty fixture's data.shots[0] should mirror this shape (use shot_id:1 with dialogue.text/emotion/words + sfx + reproduction).
    - spike/audio/results/ser_sensevoice_ep01.json + whisperx_align_ep01.json — realistic Phase 10 spike values to weave into the nonempty fixture (real emotion labels, real drift-shaped word timestamps — keeps the fixture honest, not synthetic).
    - analysis/call_audio_analysis.py (the Plan 01 client) — confirms the route_url + cache + warnings paths the smoke script will exercise.
  </read_first>
  <behavior>
    - `python3 tests/audio_analysis_stub_server.py --fixture tests/fixtures/audio_analysis_stub_response_nonempty.json --port 10591 &` starts a background HTTP server that returns the fixture body (with status 200 + Content-Type: application/json) on POST to /api/production/audio-analysis.
    - The empty fixture is byte-identical to the Phase 10 stub response (10-02-SUMMARY.md:166) — snapshot, not retyped.
    - The nonempty fixture has data.shots[0] mirroring spec/fixtures/v1.2/audio_semantic.json shot 1 (dialogue with emotion/words, sfx, reproduction) — schema-valid against audio_semantic.schema.json.
    - `bash tests/run_audio_analysis_smoke.sh` runs all 5 scenarios, prints "ALL_SCENARIOS_PASS" on success, exits 0; on any failure, prints which scenario failed + the diff/grep evidence, exits 1.
    - Smoke script cleans up: kills stub_server background PID on exit via trap, removes /tmp/p12-smoke-* work dirs.
  </behavior>
  <action>
    Create 4 artifacts. No production code changes. Mirror project conventions (Python 3, argparse + main(), Chinese docstrings where appropriate, http.server.BaseHTTPRequestHandler).

    (1) tests/fixtures/audio_analysis_stub_response_empty.json — snapshot the Phase 10 stub envelope byte-for-byte from 10-02-SUMMARY.md:166:
        {"code":200,
         "data":{"shots":[],"count":0,"errors":[],"stub_mode":true,
                 "message":"Phase 10 stub: ML models not loaded. Producer client envelope round-trip proven."},
         "message":"Audio analysis stub"}
        This is the SC#4 contract: the client must handle this envelope WITHOUT crashing (returning 0 shots → graceful-degrade → audio_semantic.json absent + [audio] warning).

    (2) tests/fixtures/audio_analysis_stub_response_nonempty.json — shape-correct non-empty envelope. Use the Phase 10 spike result shapes (ser_sensevoice_ep01.json emotion/events, whisperx_align_ep01.json word timestamps) woven into spec/fixtures/v1.2/audio_semantic.json shot 1 structure. Concrete content (the executor fills exact text from spike results, shape MUST match audio_semantic.schema.json):
        {"code":200,
         "data":{"shots":[{"shot_id":1,
                           "dialogue":{"text":"你好世界","spk_id":"spk_001",
                                        "emotion":"HAPPY","emotion_confidence":1.0,
                                        "events":["Speech"],
                                        "words":[{"start":0.0,"end":0.5,"text":"你","score":0.99},...]},
                           "sfx":{"events":[],"description":""},
                           "reproduction":{"tts":{"text":"成年女性说话者，语气开心愉悦","confidence":0.7,"fidelity_disclaimer":"TTS ~70% similarity"},
                                            "music_gen":null,"foley":null}}],
          "count":1,"errors":[],"stub_mode":false},
         "message":"Audio analysis complete"}

    (3) tests/audio_analysis_stub_server.py (~40 lines, mirror scripts/serve.py minimal style):
        - Module docstring (Chinese): explain it's a test-only stub for Phase 12 SC#4 smoke (NOT for production; returns a frozen fixture on POST).
        - argparse: --fixture <path> (required), --port <int> (default 10591), --host <str> (default "127.0.0.1").
        - Handler class (mirror scripts/serve.py:RangeRequestHandler structure but simpler): on do_POST, if path == "/api/production/audio-analysis": read Content-Length, ignore body, respond 200 with the fixture bytes (Content-Type: application/json). Else: 404. (do_GET returns 405 Method Not Allowed — the real route defines POST only; preflight probe expects this.)
        - main(): parse args, load fixture bytes once at startup (not per-request — deterministic + fast), ThreadingHTTPServer((host, port), Handler).serve_forever().
        - Print "[stub] listening on http://{host}:{port}/api/production/audio-analysis (fixture: {fixture_path})" on startup so the smoke script can confirm ready.

    (4) tests/run_audio_analysis_smoke.sh (~100 lines, mirror spike/audio/tests/route_stub_smoke.sh pattern):
        - set -euo pipefail at top.
        - REPO_ROOT computed from $0 (cd $(dirname $0)/../..).
        - WORK=/tmp/p12-smoke-$$ (unique per run; trap cleanup on EXIT: kill stub PID if set, rm -rf $WORK).
        - Helper function run_client(): wraps `python3 $REPO_ROOT/analysis/call_audio_analysis.py "$@"` with predictable --video/--shots/--work-dir/--output/--stems-dir pointing at $WORK.
        - Prepare $WORK: mkdir -p; write a minimal shots.json ([{"id":1,"start_sec":0,"end_sec":1.5,"duration":1.5}]); write a minimal "video" file (touch $WORK/video.mp4 — content-hash reads head/tail bytes, empty file is fine since the stub ignores the body).
        - Start stub_server with the NONEMPTY fixture on port 10591 in background; capture PID; poll up to 20×100ms until "listening" appears or curl probe succeeds.

        SCENARIO 1 (route-up + non-empty → write):
          run_client --route-url http://127.0.0.1:10591/api/production/audio-analysis (no --offline, no --force)
          Assert: test -f $WORK/audio_semantic.json (written)
          Assert: python3 -c "import jsonschema, json; jsonschema.Draft202012Validator(json.load(open('$REPO_ROOT/spec/schemas/audio_semantic.schema.json'))).validate(json.load(open('$WORK/audio_semantic.json')))" (schema-valid)
          Assert: test -f $WORK/route_cache/audio_analysis/shot_001.json (cache written)
          Assert: ! grep -q '\[audio\]' $WORK/route_cache/warnings.json (no audio warning on success)
          cp $WORK/audio_semantic.json $WORK/scenario1.json (snapshot for scenario 2 diff)

        SCENARIO 2 (cache-hit → byte-identical):
          run_client --offline (cache populated from scenario 1)
          Assert: diff $WORK/scenario1.json $WORK/audio_semantic.json (byte-identical)
          Assert: run log contains "cache hit" (grep the client's stdout for "shot 1: cache hit" or equivalent)

        SCENARIO 3 (poisoned cache → auto-invalidate):
          Corrupt the cache: python3 -c "import json; d=json.load(open('$WORK/route_cache/audio_analysis/shot_001.json')); d['dialogue']={'emotion':123}; json.dump(d, open('$WORK/route_cache/audio_analysis/shot_001.json','w'))"  (inject a type error so normalize/schema-validate fails)
          run_client --offline (cache is poisoned)
          Assert: run log shows "invalidated poisoned cache" (mirror call_reid.py:425 message)
          Assert: the cache file is unlinked OR re-fetched (test ! -f OR a fresh mtime > scenario-2 mtime)
          Assert: $WORK/route_cache/warnings.json now contains "[audio]" (degrade warning appended)

        SCENARIO 4 (full-degrade empty stub + read-merge-write preservation):
          Kill the nonempty stub_server; restart with the EMPTY fixture on port 10592 (different port to avoid TIME_WAIT).
          Pre-populate $WORK/route_cache/warnings.json with a fake prior [semantic] + [reid] tag:
            echo '{"warnings":["[semantic] shot 1: prefill","[reid] cluster 0: review"]}' > $WORK/route_cache/warnings.json
          run_client --route-url http://127.0.0.1:10592/api/production/audio-analysis --force
          Assert: test ! -f $WORK/audio_semantic.json (byte-identical-absent, CONTRACT-05)
          Assert: grep -q '\[audio\]' $WORK/route_cache/warnings.json (audio warning appended)
          Assert: grep -q '\[semantic\]' $WORK/route_cache/warnings.json (prior tag preserved)
          Assert: grep -q '\[reid\]' $WORK/route_cache/warnings.json (prior tag preserved)
          (This is the non-destructive read-merge-write proof.)

        SCENARIO 5 (offline + empty cache → byte-identical-absent):
          rm -rf $WORK/route_cache/audio_analysis (clear cache)
          run_client --offline --force
          Assert: test ! -f $WORK/audio_semantic.json
          Assert: grep -q '\[audio\]' $WORK/route_cache/warnings.json

        On all 5 passing: echo "ALL_SCENARIOS_PASS" && exit 0. Trap cleans up background stubs + /tmp workdir.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "import ast; ast.parse(open('tests/audio_analysis_stub_server.py').read()); print('stub_server syntax OK')" && test -f tests/fixtures/audio_analysis_stub_response_empty.json && test -f tests/fixtures/audio_analysis_stub_response_nonempty.json && python3 -c "import json; json.load(open('tests/fixtures/audio_analysis_stub_response_nonempty.json'))" && bash -n tests/run_audio_analysis_smoke.sh && echo 'smoke script syntax OK' && bash tests/run_audio_analysis_smoke.sh 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 4 artifacts exist + syntax-valid.
    - Empty fixture byte-identical to Phase 10 stub response (10-02-SUMMARY.md:166).
    - Nonempty fixture schema-valid against audio_semantic.schema.json (the data.shots[0] payload lifted into the top-level shots[] shape passes Draft202012Validator).
    - `bash tests/run_audio_analysis_smoke.sh` prints "ALL_SCENARIOS_PASS" and exits 0.
    - All 5 scenarios produce the expected assertions (audio_semantic.json written/absent per contract; cache hit/miss/poisoned-invalidation per PIPE-02; [audio]/[semantic]/[reid] coexistence per read-merge-write).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live cross-repo Phase 10 stub cross-check (best-effort, graceful-skip)</name>
  <files>(no file changes — verification only, mirror 10-02 Task 2 pattern)</files>
  <read_first>
    - .planning/phases/10-risk-validation-spike-route-stub/10-02-SUMMARY.md (lines 156-201) — Phase 10 stub provenance: branch feat/audio-analysis-route, worktree at /tmp/kais-aigc-platform-audio-route, smoke harness port 10589, exact curl commands that proved the envelope round-trip.
    - tests/run_audio_analysis_smoke.sh (Task 1 output) — the local stub harness already proved all 5 paths deterministically; this task is a SUPPLEMENTARY live cross-check.
  </read_first>
  <action>
    Context: Plan 01 built `analysis/call_audio_analysis.py` (the producer client). Task 1 of this plan built a LOCAL deterministic stub harness that proved all 5 code paths (route-up/cache-hit/poisoned/full-degrade/offline-empty) against a frozen snapshot of the Phase 10 stub envelope. SC#4 is ALREADY satisfied deterministically by Task 1. This Task 2 is a best-effort live cross-check against the ACTUAL cross-repo Phase 10 stub host (if it happens to be running) — a secondary confirmation that the local fixture matches the live stub byte-for-byte.

    Procedure (human executes + records outcome):
    Step 1 — Probe the cross-repo stub host (10-02-SUMMARY.md:159 used port 10589): `curl -sS -m 3 -X POST http://localhost:10589/api/production/audio-analysis -H 'Content-Type: application/json' -d '{"video":"/x","shots":"/y"}' 2>&1 | head -5`
    Step 2 — Interpret: if response is `{"code":200,"data":{"shots":[],"stub_mode":true,...}}` → live stub UP, proceed to Step 3. If `curl: (7) Failed to connect` / `Connection refused` → live stub DOWN (expected — worktree not running; mirror v1.1 Phase 7 CAST deferred pattern); skip to Step 4.
    Step 3 (live stub UP only) — Run the producer client: `mkdir -p /tmp/p12-live && echo '[{"id":1,"start_sec":0,"end_sec":1,"duration":1}]' > /tmp/p12-live/shots.json` then `python3 analysis/call_audio_analysis.py --video /tmp/p12-live/shots.json --shots /tmp/p12-live/shots.json --work-dir /tmp/p12-live --output /tmp/p12-live/audio_semantic.json --stems-dir /tmp/p12-live --route-url http://localhost:10589/api/production/audio-analysis`. Expected: exit 0; audio_semantic.json NOT written (stub returns shots:[] → full-degrade); /tmp/p12-live/route_cache/warnings.json contains "[audio]" tag; client did NOT crash on stub_mode:true.
    Step 4 (live stub DOWN — graceful-skip) — Document in 12-02-SUMMARY.md: record curl error evidence; confirm Task 1 local stub harness already proved SC#4 deterministically (reference ALL_SCENARIOS_PASS); note live cross-repo round-trip deferred to post-merge smoke (mirror v1.1 Phase 7 CAST-01..04/08 deferral, STATE.md:130).
    Step 5 — Record outcome (whichever path) in Task 2 section of 12-02-SUMMARY.md.
  </action>
  <verify>Human confirms: EITHER (a) live stub UP + producer-client round-trip exit code 0 + audio_semantic.json absent + [audio] warning present, OR (b) live stub DOWN + graceful-skip documented with curl error evidence + reference to Task 1 deterministic SC#4 proof.</verify>
  <done>12-02-SUMMARY.md Task 2 section records the outcome. Either path satisfies SC#4 (Task 1 is the deterministic proof; Task 2 is supplementary). No file changes required either way (verification-only task).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test stub → client (loopback) | Local http.server stub on 127.0.0.1:<port> — test-only, operator-controlled fixture, not a production trust boundary |
| Client → filesystem (test workdir) | All writes under /tmp/p12-smoke-* (test isolation; no production output paths touched) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | Stub server returns malicious/shape-incorrect payload | accept + mitigate | Test-only stub (operator-controlled fixture, not internet-exposed). Defense-in-depth: the client schema-validates the normalized payload pre-write anyway (T-12-02 in Plan 01), so a malformed fixture would be caught at the schema gate, not flow into audio_semantic.json. |
| T-12-07 | Denial of Service | Stub server crash / port conflict | accept | Test isolation: unique /tmp workdir per run, configurable --port, trap-based cleanup of background PID. A crashed stub manifests as a failed scenario (clear grep evidence), not a silent false-pass. |
| T-12-08 | Info Disclosure | Test fixture contains sensitive paths/tokens | accept | Fixtures contain only synthetic shape-correct data (no real video paths, no tokens, no PII). The empty fixture is a byte-snapshot of the Phase 10 stub message — already public in 10-02-SUMMARY.md. |
| T-12-SC | Supply Chain | No new packages installed | accept | Zero pip/npm installs. Stub server uses only Python stdlib (http.server, argparse, json). |
| Reuse T-12-01..05 | — | Client trust boundaries | (see Plan 01) | The client under test carries its own threat model (T-12-01 through T-12-05 + T-12-SC); this plan does not alter the client's attack surface. |
</threat_model>

<verification>
- All 4 artifacts exist + syntax-valid (python3 ast.parse + bash -n).
- Empty fixture byte-identical to Phase 10 stub response (grep -c 'stub_mode.*true' returns 1; diff against 10-02-SUMMARY.md:166 snapshot).
- Nonempty fixture schema-validates against audio_semantic.schema.json when data.shots[] is lifted to top-level.
- `bash tests/run_audio_analysis_smoke.sh` prints ALL_SCENARIOS_PASS + exits 0.
- Live cross-repo stub cross-check: either (a) round-trip exit 0 against localhost:10589 with audio_semantic.json absent + [audio] warning present, OR (b) graceful-skip with curl evidence + reference to Task 1 deterministic proof.
</verification>

<success_criteria>
- SC#4 (ROADMAP Phase 12): stub-only round-trip (Phase 10 stub, models not loaded) confirms envelope parsing + cache write + warnings merge work BEFORE route ML is live. — Covered by Task 1 (deterministic local stub harness exercising all 5 paths) + Task 2 (best-effort live cross-repo cross-check).
- The stub_mode:true envelope (Phase 10 default) is parsed by the client without crashing (Scenario 4 proves this: empty shots[] → graceful-degrade, NOT a crash).
- The nonempty shape (when ML eventually lands) is normalized + schema-validated correctly (Scenario 1 proves this against the canonical fixture shape).
- Cross-feature non-destructive merge proven (Scenario 4): [audio] coexists with [semantic] + [reid] in warnings.json.
</success_criteria>

<output>
Create `.planning/phases/12-producer-route-client-call-audio-analysis-py/12-02-SUMMARY.md` when done (include Task 2 outcome: live-stub-UP round-trip OR live-stub-DOWN graceful-skip with evidence)
</output>
