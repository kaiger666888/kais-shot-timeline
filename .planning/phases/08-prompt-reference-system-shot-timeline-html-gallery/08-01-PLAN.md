---
phase: 08-prompt-reference-system-shot-timeline-html-gallery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/schemas/asset.schema.json
  - spec/fixtures/v1.1/asset.json
  - spec/SPEC.md
autonomous: true
requirements: [PROMPT-04]

must_haves:
  truths:
    - "asset.schema.json#generator declares optional `registry_snapshot` property (additive within v1.1; `required[]` byte-identical to Phase 6 — tool/version/generated_at only)"
    - "Old assets WITHOUT registry_snapshot (v1.0 + route-down degrade) still schema-validate green (graceful-degrade promise preserved)"
    - "spec/fixtures/v1.1/asset.json carries an example `generator.registry_snapshot` reflecting the confirmed fixture registry (char_001 少女 + char_002 路人 + prop_001 落叶) and validates green against the extended schema"
    - "SCHEMA_VERSION in scripts/export_asset.py stays '1.1' (NO bump — STATE.md milestone lock; Pitfall 10 prevented) — this plan does NOT touch export_asset.py"
    - "spec/SPEC.md §3 field table documents the new `generator.registry_snapshot` row; §4 Changelog gains a Phase 8 bullet"
    - "minimal v1.0 fixture (6 shapes) still validates green (CONTRACT-09 backward-compat not broken by the additive property)"
  artifacts:
    - path: "spec/schemas/asset.schema.json"
      provides: "Strict-schema declaration of generator.registry_snapshot (additive optional; additionalProperties:false preserved). Producers MAY emit it when registry exists; old assets omit it and still validate."
      contains: "registry_snapshot"
    - path: "spec/fixtures/v1.1/asset.json"
      provides: "Example registry_snapshot embedded in generator — the frozen-at-export-time confirmed-registry view (char_001/char_002/prop_001). Contract oracle for Plan 02's export_asset.py emission."
      contains: "registry_snapshot"
    - path: "spec/SPEC.md"
      provides: "Human-readable §3 generator field table row + §4 Changelog Phase 8 bullet (additive, no version bump, freeze semantics)."
      contains: "registry_snapshot"
  key_links:
    - from: "spec/schemas/asset.schema.json#generator.properties.registry_snapshot"
      to: "spec/fixtures/v1.1/asset.json#generator.registry_snapshot"
      via: "Draft202012Validator (spec/validate.py validates fixture against schema)"
      pattern: "registry_snapshot"
    - from: "spec/fixtures/v1.1/asset.json#generator.registry_snapshot.characters[]"
      to: "spec/fixtures/v1.1/characters.json confirmed entries"
      via: "id + name + representative_image + appearance_shots mirror the canonical confirmed registry"
      pattern: "char_001|prop_001"
---

<objective>
Create the contract layer for the `generator.registry_snapshot` freeze (PROMPT-04 schema half): declare the additive-optional property in `asset.schema.json#generator.properties`, ship an example in `spec/fixtures/v1.1/asset.json`, and document it in `spec/SPEC.md` §3 + §4 Changelog.

Purpose: Phase 8 is contract-first (mirrors Phase 5/6/7). `additionalProperties:false` on `asset.schema.json#generator` means the schema MUST declare `registry_snapshot` BEFORE the producer (Plan 02 `export_asset.py`) can emit it — otherwise strict-schema validation rejects the asset. This plan freezes the shape so Plan 02 builds against a stable contract. CONTEXT Q2 LOCK: NO `schema_version` bump (stays `"1.1"` per STATE.md milestone lock; Pitfall 10).

Output: 1 schema modified (additive property) + 1 fixture modified (example field) + 1 spec doc modified (§3 row + Changelog). Zero new files. Zero code execution — pure contract layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-CONTEXT.md
@.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-RESEARCH.md
@.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-VALIDATION.md
@spec/schemas/asset.schema.json
@spec/fixtures/v1.1/asset.json
@spec/SPEC.md

<interfaces>
<!-- The registry_snapshot shape — LOCKED in CONTEXT Q1 + RESEARCH Pattern 3 (lines 478-619). -->

`generator.registry_snapshot` is an OPTIONAL object (absent on old assets / route-down degrade — schema-valid either way). When present, `required: ["characters", "props"]` (both arrays; may be empty). Each entry is a compact 4-field projection of a confirmed registry entry:

- `id`: string, pattern `^char_[0-9]{3}$` (characters) or `^prop_[0-9]{3}$` (props) — same ID contract as characters.schema.json / props.schema.json.
- `name`: string, minLength 1 — the display name (registry-reviewer-editable per characters.schema.json:21).
- `representative_image`: OPTIONAL string, anti-traversal pattern `^(?!.*\.\.)([^/]+/)*characters/[^:*?"<>|]+\.png$` (or `props/` variant) — same pattern as asset.schema.json#media.characters[]. Schema-OPTIONAL because apply_edits.py may omit it on ffmpeg failure (Phase 7 WARNING-2).
- `appearance_shots`: array of integers (≥1) — the shot IDs where this entity appears. Phase 8 attach_refs.py inverts this to attach character_refs[]/prop_refs[].

Confirmed-only hard filter (Pitfall 7): the snapshot contains ONLY `review_state:"confirmed"` entries. Non-confirmed (proposed/rejected) NEVER leak into the snapshot. Plan 02's `_build_registry_snapshot` enforces this at emission time.

Freeze semantics (Pitfall 18): once asset.json is written, the snapshot is the export-time truth. Later registry mutations (re-review, re-cluster, rename) do NOT mutate already-exported asset.json. This is structural — asset.json is a written file, not a live view.

<!-- Phase 6 precedent: generator.warnings was added the same way (additive optional, additionalProperties:false preserved, required[] unchanged). asset.schema.json:56-60 is the DIRECT template. -->

<!-- The existing v1.1 fixture asset.json (spec/fixtures/v1.1/asset.json) already has generator.warnings + data.characters/props + media.characters[]/props[] (Phase 6/7). Plan 01 adds registry_snapshot as a new sibling under generator. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: asset.schema.json — declare additive generator.registry_snapshot property</name>
  <files>spec/schemas/asset.schema.json</files>
  <read_first>
    - spec/schemas/asset.schema.json (the ENTIRE 159-line file — focus on the `generator` block lines 38-62: it has `additionalProperties:false`, `required:["tool","version","generated_at"]`, and properties tool/version/generated_at/warnings. Phase 6 added `warnings` as additive-optional (lines 56-60) — that is the DIRECT precedent for how `registry_snapshot` should be declared. The new property goes AFTER `warnings`, BEFORE the closing `}` of `generator.properties`.)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-RESEARCH.md (§Architecture Patterns → Pattern 3 "registry_snapshot freeze shape + export emission" lines 478-619 — the FULL schema declaration skeleton with every field, pattern, description. Copy the `registry_snapshot` property block verbatim from lines 569-618.)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-CONTEXT.md (§decisions → "Registry snapshot freeze Q1/Q2" — LOCKED: compact confirmed-only shape, additive optional, NO schema_version bump, strict additionalProperties:false means MUST declare)
    - spec/schemas/characters.schema.json (the `id` pattern `^char_[0-9]{3}$` + `representative_image` anti-traversal pattern — the snapshot's nested character items reuse these exact patterns)
    - spec/schemas/props.schema.json (the `id` pattern `^prop_[0-9]{3}$` + `representative_image` pattern — snapshot's nested prop items reuse these)
  </read_first>
  <action>
Add a new property `registry_snapshot` to `asset.schema.json#generator.properties` (currently lines 43-61, ending with the `warnings` property at lines 56-60). Insert it AFTER `warnings` and BEFORE the closing brace of `generator.properties`.

The property declaration (copy from RESEARCH Pattern 3 lines 569-618, adjusting only the description to reference Phase 8):
- `"type": "object"` with `"additionalProperties": false`
- `"required": ["characters", "props"]` (both arrays present even when empty — lets a consumer branch without nil-check)
- `"description"`: document it as v1.1 additive (OPTIONAL — Phase 8), frozen-at-export-time compact view of confirmed registry entries; consumer resolves prompts.character_refs[]/prop_refs[] + renders gallery WITHOUT re-reading external characters.json/props.json; later registry mutations cannot invalidate already-exported prompt references (Pitfall 18 prevented); older assets omit it and still validate (graceful-degrade); producer emits only when characters.json or props.json exists at export time.
- Nested `characters` array: items object with `additionalProperties:false`, `required:["id","name","appearance_shots"]`, properties `id` (pattern `^char_[0-9]{3}$`), `name` (string minLength 1), `representative_image` (OPTIONAL string, anti-traversal pattern `^(?!.*\.\.)([^/]+/)*characters/[^:*?"<>|]+\.png$`), `appearance_shots` (array of integer ≥1).
- Nested `props` array: identical shape but `id` pattern `^prop_[0-9]{3}$` and `representative_image` pattern with `props/` segment.

Critical constraints to preserve:
- Do NOT add `registry_snapshot` to `generator.required` (line 41 stays `["tool","version","generated_at"]`). It is OPTIONAL — absent on old assets.
- Do NOT change `generator.additionalProperties` (stays `false` at line 40). The declaration itself is what makes emission legal under strict schema.
- Do NOT touch `schema_version` pattern (line 14) or any other property. This is a single-property additive insert.
- Do NOT bump SCHEMA_VERSION in scripts/export_asset.py (that is Plan 02's concern; this plan touches schemas + fixtures + SPEC only).
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "
import json
from jsonschema import Draft202012Validator

schema = json.load(open('spec/schemas/asset.schema.json', encoding='utf-8'))
gen = schema['properties']['generator']

# 1) registry_snapshot declared
assert 'registry_snapshot' in gen['properties'], 'registry_snapshot not in generator.properties'
rs = gen['properties']['registry_snapshot']
assert rs['type'] == 'object', f'registry_snapshot type wrong: {rs[\"type\"]}'
assert rs.get('additionalProperties') is False, 'registry_snapshot additionalProperties not false'
assert rs['required'] == ['characters', 'props'], f'required wrong: {rs.get(\"required\")}'

# 2) character item shape
char_item = rs['properties']['characters']['items']
assert char_item['required'] == ['id', 'name', 'appearance_shots'], f'char item required wrong: {char_item[\"required\"]}'
assert '^char_[0-9]{3}\$' in char_item['properties']['id']['pattern'], 'char id pattern wrong'
assert char_item['properties']['name'].get('minLength') == 1, 'name minLength wrong'
assert 'representative_image' in char_item['properties'], 'representative_image missing (should be optional)'

# 3) prop item shape
prop_item = rs['properties']['props']['items']
assert prop_item['properties']['id']['pattern'] == '^prop_[0-9]{3}\$', 'prop id pattern wrong'

# 4) required[] UNCHANGED (still tool/version/generated_at — registry_snapshot NOT required)
assert gen['required'] == ['tool', 'version', 'generated_at'], f'required changed: {gen[\"required\"]}'

# 5) generator.additionalProperties still false
assert gen.get('additionalProperties') is False, 'generator additionalProperties drifted'

# 6) schema_version pattern unchanged (no version bump encoded in schema)
assert '^(' not in schema['properties']['schema_version'].get('const', ''), 'schema_version should NOT be const-locked'

print('OK: asset.schema.json generator.registry_snapshot declared (additive optional, strict, confirmed-only shape)')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'registry_snapshot' spec/schemas/asset.schema.json` ≥ 3 (property key + description reference + nested references)
    - `python3 -c "import json; s=json.load(open('spec/schemas/asset.schema.json')); g=s['properties']['generator']; assert 'registry_snapshot' in g['properties']; assert 'registry_snapshot' not in g.get('required',[]); assert g['additionalProperties']==False"` exits 0
    - `grep -c 'additionalProperties.*false' spec/schemas/asset.schema.json` ≥ 5 (top-level + source + generator + registry_snapshot + at least one nested item object — strict-schema defense-in-depth)
    - `python3 -c "import json; s=json.load(open('spec/schemas/asset.schema.json')); print(s['properties']['generator']['required'])"` outputs exactly `['tool', 'version', 'generated_at']` (unchanged — registry_snapshot is NOT required)
  </acceptance_criteria>
  <done>
asset.schema.json#generator.properties declares `registry_snapshot` as an additive-optional object (required: characters+props arrays; each item has id/name/representative_image?/appearance_shots with strict patterns). `generator.required` unchanged (tool/version/generated_at only). `generator.additionalProperties` stays false. Old assets without the field still validate.
  </done>
</task>

<task type="auto">
  <name>Task 2: spec/fixtures/v1.1/asset.json example registry_snapshot + SPEC.md §3 row + Changelog Phase 8 bullet</name>
  <files>spec/fixtures/v1.1/asset.json, spec/SPEC.md</files>
  <read_first>
    - spec/fixtures/v1.1/asset.json (the ENTIRE 41-line fixture — currently has generator.warnings + data.characters/props + media.characters[]/props[]. The new `registry_snapshot` goes inside the `generator` object, AFTER `warnings`.)
    - spec/fixtures/v1.1/characters.json (the confirmed fixture registry — char_001 少女 appearance_shots [1,2] + char_002 路人 appearance_shots [1]; the snapshot MUST mirror these exact id/name/representative_image/appearance_shots values)
    - spec/fixtures/v1.1/props.json (prop_001 落叶 appearance_shots [2])
    - spec/SPEC.md (focus on §3 lines 70-104 — the manifest field table. The `generator.warnings` row at line 85 is the DIRECT precedent for how to document an additive-optional generator property. §4 Changelog lines 156-166 has the v1.1 entry + Phase 6 bullet — append a Phase 8 bullet after line 166.)
    - .planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-RESEARCH.md (§Code Examples → Example 2 "registry_snapshot in asset.json" lines 1009-1053 — the TARGET fixture shape with the exact registry_snapshot content to copy)
  </read_first>
  <action>
**spec/fixtures/v1.1/asset.json — add `registry_snapshot` to the `generator` object** (currently lines 8-15). Insert AFTER the `warnings` array (line 15), before the closing `}` of `generator`:

The content mirrors the confirmed fixture registry (from characters.json + props.json):
- `characters`: array with 2 entries — `{id:"char_001", name:"少女", representative_image:"characters/char_001.png", appearance_shots:[1,2]}` and `{id:"char_002", name:"路人", representative_image:"characters/char_002.png", appearance_shots:[1]}`.
- `props`: array with 1 entry — `{id:"prop_001", name:"落叶", representative_image:"props/prop_001.png", appearance_shots:[2]}`.

Copy the exact shape from RESEARCH Example 2 (lines 1025-1048). The values MUST match characters.json/props.json fixtures exactly (same id, name, representative_image, appearance_shots) — the fixture set must be internally consistent (registry_snapshot is the frozen view of those canonical files).

**spec/SPEC.md — §3 manifest field table (lines 76-99):** add a new row after the `generator.warnings` row (line 85) documenting `generator.registry_snapshot`:
- Field: `generator.registry_snapshot`
- Type: `object {characters[], props[]}`
- Required: `— (v1.1 Phase 8)`
- Description: frozen-at-export-time compact view of confirmed registry entries (characters + props). Embeds id/name/representative_image/appearance_shots per entry — enough for a consumer to resolve prompts.character_refs[]/prop_refs[] and render a gallery WITHOUT re-reading external characters.json/props.json. Producer emits ONLY when characters.json or props.json exists at export time; absent on assets produced without re-id (v1.0 byte-identical). Later registry mutations (re-review, re-cluster) cannot invalidate already-exported prompt references.

**spec/SPEC.md — §4 Changelog (after line 166, the Phase 6 bullet):** add a Phase 8 bullet:
- `Phase 8 (prompt reference system)`: `asset.schema.json#generator` 新增 optional `registry_snapshot: {characters[], props[]}`(Plan 01)。Producer(`scripts/export_asset.py` Plan 02)在 characters.json/props.json 存在时 emit 确认态 registry 的冻结快照;无 registry 时缺省(byte-identical to v1.0/v1.1-no-reid)。仍是 v1.1 纯增量(无 schema_version bump、`required[]` 不变、`additionalProperties:false` 保留)。Pitfall 18 prevented: snapshot 是 export-time truth,后续 registry 变动不回写已导出的 asset.json。

Do NOT modify the §5 data shapes section or any other section — registry_snapshot is a generator-level field, not a data shape.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 spec/validate.py 2>&1 | tee /tmp/v8_validate.txt; echo "---"; python3 -c "
import json
from jsonschema import Draft202012Validator

# 1) fixture asset.json has registry_snapshot + schema-valid
fixture = json.load(open('spec/fixtures/v1.1/asset.json', encoding='utf-8'))
assert 'registry_snapshot' in fixture['generator'], 'fixture missing generator.registry_snapshot'
rs = fixture['generator']['registry_snapshot']
assert len(rs['characters']) == 2, f'expected 2 characters in snapshot, got {len(rs[\"characters\"])}'
assert len(rs['props']) == 1, f'expected 1 prop in snapshot, got {len(rs[\"props\"])}'

# 2) snapshot mirrors canonical fixture registry exactly
chars = json.load(open('spec/fixtures/v1.1/characters.json', encoding='utf-8'))
props = json.load(open('spec/fixtures/v1.1/props.json', encoding='utf-8'))
for snap_c in rs['characters']:
    canon = next(c for c in chars if c['id'] == snap_c['id'])
    assert snap_c['name'] == canon['name'], f'{snap_c[\"id\"]} name mismatch: {snap_c[\"name\"]} != {canon[\"name\"]}'
    assert snap_c['appearance_shots'] == canon['appearance_shots'], f'{snap_c[\"id\"]} shots mismatch'
for snap_p in rs['props']:
    canon = next(p for p in props if p['id'] == snap_p['id'])
    assert snap_p['name'] == canon['name']
    assert snap_p['appearance_shots'] == canon['appearance_shots']

# 3) fixture validates against the extended schema
schema = json.load(open('spec/schemas/asset.schema.json', encoding='utf-8'))
errs = list(Draft202012Validator(schema).iter_errors(fixture))
assert not errs, f'{len(errs)} schema errors; first: {errs[0].message}'

# 4) spec/validate.py exit 0 + no regression (10 v1.1 shapes + 6 minimal)
out = open('/tmp/v8_validate.txt').read()
assert out.count('[valid-v11]') == 10, f'expected 10 [valid-v11], got {out.count(\"[valid-v11]\")}'
assert out.count('[valid]') == 6, f'minimal shapes regressed: expected 6 [valid]'

# 5) SPEC.md documents the new field
spec = open('spec/SPEC.md', encoding='utf-8').read()
assert 'registry_snapshot' in spec, 'SPEC.md missing registry_snapshot'
assert 'Phase 8' in spec or 'phase 8' in spec.lower(), 'SPEC.md missing Phase 8 changelog bullet'
print('OK: fixture asset.json has schema-valid registry_snapshot mirroring canonical registry; SPEC.md updated; no regression')
"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 spec/validate.py` exits 0
    - `grep -c 'registry_snapshot' spec/fixtures/v1.1/asset.json` ≥ 1 (the field is present in generator)
    - `grep -c 'char_001' spec/fixtures/v1.1/asset.json` ≥ 2 (already in media.characters[] + now in registry_snapshot.characters[])
    - `grep -c 'registry_snapshot' spec/SPEC.md` ≥ 2 (§3 field table row + §4 Changelog bullet)
    - `python3 -c "import json; a=json.load(open('spec/fixtures/v1.1/asset.json')); assert a['generator']['registry_snapshot']['characters'][0]['name']=='少女'; assert a['generator']['registry_snapshot']['props'][0]['name']=='落叶'"` exits 0 (snapshot mirrors canonical names)
    - Output of `python3 spec/validate.py` contains exactly 10 `[valid-v11]` lines + 6 `[valid]` lines (no regression)
  </acceptance_criteria>
  <done>
spec/fixtures/v1.1/asset.json generator block carries a schema-valid `registry_snapshot` (2 characters + 1 prop) mirroring the canonical fixture characters.json/props.json exactly. spec/SPEC.md §3 documents the new generator field row; §4 Changelog has a Phase 8 additive bullet (no version bump). spec/validate.py green: 10 v1.1 shapes + 6 minimal shapes unaffected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema→Validator | asset.schema.json is the trust gate: any producer emission of `registry_snapshot` is validated by Draft202012Validator before the asset ships. |
| Fixture→Contract oracle | spec/fixtures/v1.1/asset.json is the frozen example future Plans (02/03) validate against. Drift between fixture and schema = silent contract bug. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | registry_snapshot contains non-confirmed entry (Pitfall 7 leak in new clothes) | mitigate | Schema declares the shape; Plan 02 `_build_registry_snapshot` enforces `review_state != "confirmed": continue` at emission time. Fixture example contains ONLY confirmed entries (char_001/char_002/prop_001 all review_state:"confirmed" in canonical fixtures). |
| T-08-02 | Tampering | Path traversal via `representative_image` in snapshot | mitigate | `representative_image` pattern uses anti-traversal negative-lookahead `^(?!.*\.\.)([^/]+/)*characters/[^:*?"<>|]+\.png$` — rejects `../`, absolute paths, Windows reserved chars. Inherited from Phase 5 characters.schema.json + asset.schema.json#media.characters[].pattern. |
| T-08-03 | Information Disclosure | registry_snapshot embeds PII via `name` field | accept | Registry names are character/prop display names (e.g. "少女", "落叶") — visual analysis output, not PII. Same data already in characters.json/props.json (Phase 5 baseline). No new disclosure surface. |
| T-08-SC | Tampering | pip/npm/cargo installs | accept | Plan 01 adds ZERO packages — pure JSON schema + fixture + Markdown. No install tasks. jsonschema 4.26.0 is v1.0 baseline. |

No HIGH severity threats (contract-layer plan — schema/fixture/doc files only; no code execution, no network, no filesystem writes beyond spec/).
</threat_model>

<verification>
After both tasks complete:
1. `python3 spec/validate.py` exits 0 with 10 `[valid-v11]` lines (including the extended asset fixture) + 6 `[valid]` lines (minimal v1.0 shapes — CONTRACT-09 backward-compat).
2. `python3 -c "from jsonschema import Draft202012Validator; import json; s=json.load(open('spec/schemas/asset.schema.json')); a=json.load(open('spec/fixtures/v1.1/asset.json')); assert not list(Draft202012Validator(s).iter_errors(a))"` exits 0 (fixture schema-valid with the new field).
3. `python3 -c "from jsonschema import Draft202012Validator; import json; s=json.load(open('spec/schemas/asset.schema.json')); old={'schema_version':'1','asset_type':'shottimeline','source':{'video_filename':'x.mp4','duration_sec':1.0},'generator':{'tool':'t','version':'v','generated_at':'2026-01-01T00:00:00Z'},'data':{'shots':'shots.json','audio_analysis':'a.json','transcript':'t.json','frames':'f.json','prompts':'p.json'},'media':{'video':'video.mp4','stems':{'vocals':'stems/vocals.wav','drums':'stems/drums.wav','other':'stems/other.wav'}}}; assert not list(Draft202012Validator(s).iter_errors(old))"` exits 0 (old asset WITHOUT registry_snapshot still validates — graceful-degrade promise).
4. `grep -c 'registry_snapshot' spec/SPEC.md` ≥ 2 (§3 + §4 Changelog).
</verification>

<success_criteria>
- `asset.schema.json#generator.properties.registry_snapshot` declared as additive-optional (strict object with characters[]+props[] arrays; each item has id/name/representative_image?/appearance_shots with anti-traversal patterns).
- `generator.required` unchanged (tool/version/generated_at — registry_snapshot is NOT required).
- `spec/fixtures/v1.1/asset.json` carries a schema-valid example snapshot mirroring the confirmed fixture registry.
- `spec/SPEC.md` §3 + §4 Changelog document the Phase 8 additive field (no version bump).
- CONTRACT-09 preserved: minimal v1.0 fixture (6 shapes) still green.
- PROMPT-04 schema half frozen; Plan 02 can build the producer emission against this contract.
</success_criteria>

<output>
Create `.planning/phases/08-prompt-reference-system-shot-timeline-html-gallery/08-01-SUMMARY.md` when done
</output>
