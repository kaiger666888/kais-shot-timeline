---
phase: 10-risk-validation-spike-route-stub
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  # Cross-repo: kais-aigc-platform on branch feat/audio-analysis-route
  - /data/workspace/kais-aigc-platform/src/routes/production/audio-analysis/index.ts
  - /data/workspace/kais-aigc-platform/src/routes/production/audio-analysis/_shared/config.ts
  - /data/workspace/kais-aigc-platform/src/router.ts
autonomous: true
requirements: [ROUTE-01]
tags: [route-stub, cross-repo, kais-aigc-platform]

must_haves:
  truths:
    - "Branch feat/audio-analysis-route exists in /data/workspace/kais-aigc-platform, branched from develop, NOT from feat/flowgraph-v3-canvas"
    - "POST /api/production/audio-analysis with valid body returns {code:200, data:{shots:[], count:0, errors:[], stub_mode:true, message:string}, message:string} — byte-identical envelope shape to shot-analysis (Pitfall 7 prevention)"
    - "POST /api/production/audio-analysis with missing required field returns {code:400, ...} via zod error path"
    - "audio-analysis/index.ts imports success+error from @/lib/responseFormat (plan-checker grep gate)"
    - "router.ts has 1 new import + 1 new app.use mount at /api/production/audio-analysis (NO /v1/ prefix per research note)"
    - "System torch 2.6.0+cu124 UNAFFECTED after task completion (this plan does NOT touch the project venv)"
  artifacts:
    - path: "/data/workspace/kais-aigc-platform/src/routes/production/audio-analysis/index.ts"
      provides: "Express route handler — zod body validation + STUB_MODE empty-data return + 501 placeholder for ML fan-out"
      min_lines: 60
      contains: "import { success, error } from \"@/lib/responseFormat\""
    - path: "/data/workspace/kais-aigc-platform/src/routes/production/audio-analysis/_shared/config.ts"
      provides: "AUDIO_ANALYSIS_CONFIG env-driven config object"
      min_lines: 10
      contains: "AUDIO_ANALYSIS_CONFIG"
    - path: "/data/workspace/kais-aigc-platform/src/router.ts"
      provides: "Route47 import + mount at /api/production/audio-analysis"
      contains: "audio-analysis"
  key_links:
    - from: "src/routes/production/audio-analysis/index.ts"
      to: "@/lib/responseFormat.ts:success"
      via: "import { success, error } from '@/lib/responseFormat'"
      pattern: "import \\{ success, error \\} from \"@/lib/responseFormat\""
    - from: "src/router.ts"
      to: "src/routes/production/audio-analysis/index.ts"
      via: "import route47 from './routes/production/audio-analysis/index'; app.use('/api/production/audio-analysis', route47)"
      pattern: "app\\.use\\(\"/api/production/audio-analysis\""
---

<objective>
Deliver ROUTE-01: a thin Express stub route in the cross-repo `kais-aigc-platform` that gives the Phase 12 producer client (`call_audio_analysis.py`) a POST target with the byte-identical envelope `{code:200, data:{...}, message:"成功"}` BEFORE any ML lands. Mirror v1.1 Phase 7 CAST deferred pattern: the stub proves the envelope round-trips, live ML is Phase 12+ responsibility.

Purpose: Phase 10 ROUTE-01 is the ONLY production-shaped artifact of this phase (spike scripts are throwaway). Without the stub, Phase 12 cannot integration-test the producer client.

Output: 3 files in kais-aigc-platform on a NEW branch `feat/audio-analysis-route` (NOT pushed — cross-repo PR is post-Phase 10). Plus 1 commit on that branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-risk-validation-spike-route-stub/10-CONTEXT.md
@.planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md

<interfaces>
<!-- Verified cross-repo state this session via bash:
     - current branch: feat/flowgraph-v3-canvas
     - develop branch exists, last commit: 86334e56 Merge PR #142
     - shot-analysis route ALREADY in develop (route46 at /api/production/shot-analysis)
     - router.ts highest route number = route46 (shot-analysis); next = route47
     - Path mount pattern: app.use("/api/production/<name>", routeNN) — NO /v1/ prefix
     - Existing shot-analysis/_shared/config.ts uses SHOT_ANALYSIS_CONFIG env-driven pattern (mirror this)
     - responseFormat.ts:success(data, message) returns {code:200, data, message:"成功"} (verified by research)
-->

<!-- Source-of-truth: kais-aigc-platform/src/routes/production/shot-analysis/index.ts (line-for-line structural template) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-repo — branch from develop, write 2 TS files, wire router.ts, commit on feat/audio-analysis-route</name>
  <files>/data/workspace/kais-aigc-platform/src/routes/production/audio-analysis/index.ts, /data/workspace/kais-aigc-platform/src/routes/production/audio-analysis/_shared/config.ts, /data/workspace/kais-aigc-platform/src/router.ts</files>
  <read_first>
    - /data/workspace/kais-aigc-platform/src/routes/production/shot-analysis/index.ts (line-for-line structural template; note zod schema shape, success/error envelope, STUB pattern absent here — audio-analysis stub is a NEW pattern)
    - /data/workspace/kais-aigc-platform/src/routes/production/shot-analysis/_shared/config.ts (env-driven config pattern to mirror)
    - /data/workspace/kais-aigc-platform/src/lib/responseFormat.ts (confirm success/error signatures)
    - /data/workspace/kais-aigc-platform/src/router.ts (find route46 line; insert route47 after)
    - .planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md §ROUTE-01 Stub Structure (lines 246-345 — exact TS body reference)
  </read_first>
  <action>
**CWD NOTE:** All file paths in this task are in `/data/workspace/kais-aigc-platform`, NOT in kais-shot-timeline. Use absolute paths or `cd /data/workspace/kais-aigc-platform && ...` per command.

**Step 1 — Branch setup (do NOT push):**
```
cd /data/workspace/kais-aigc-platform
git fetch origin develop
git checkout develop
git checkout -b feat/audio-analysis-route   # from develop, NOT from feat/flowgraph-v3-canvas
```
Verify `git branch --show-current` == `feat/audio-analysis-route` AND `git log --oneline -1` shows the develop HEAD before branching.

**Step 2 — Write src/routes/production/audio-analysis/_shared/config.ts:**
Mirror the shot-analysis/_shared/config.ts pattern (read in read_first). Export `AUDIO_ANALYSIS_CONFIG` object with these keys:
- `stubMode: process.env.AUDIO_ANALYSIS_STUB_MODE !== "false"` (default true — Phase 10 returns empty data unless explicitly disabled)
- `goldTeamUrl: process.env.GOLD_TEAM_URL || "http://gold-team:8002"`
- `perShotDeadlineMs: Number(process.env.AUDIO_ANALYSIS_PER_SHOT_TIMEOUT_MS || 900_000)` (15min, matches shot-analysis)
- `senseVoiceModel: "iic/SenseVoiceSmall"` (canonical ModelScope ID, fixed)
- `whisperxModel: "large-v3"`
- ` mertModel: "m-a-p/MERT-v1-95M"` (canonical HF ID, fixed — supply-chain T-10-03 mitigation)
- `pannsCheckpoint: null` (null = default Cnn14_mAP=0.431.pth, no local-checkpoint path — T-10-03 mitigation)
- `log: process.env.AUDIO_ANALYSIS_LOG === "true"` (debug flag)

**Step 3 — Write src/routes/production/audio-analysis/index.ts:**
Copy the ROUTE-01 Stub Structure body from 10-RESEARCH.md §lines 250-317 verbatim, with these refinements:
- Add Chinese docstring header explaining "Phase 10 STUB — ML unloaded returns empty data; Phase 12+ replaces STUB block with fan-out to SenseVoice/WhisperX/MERT/PANNs/pyannote via gold-team tasks (mirror shot-analysis gold-team fan-out pattern)."
- `import express from "express";`
- `import { z } from "zod";`
- `import { success, error } from "@/lib/responseFormat";` (byte-identical to shot-analysis line)
- `import { AUDIO_ANALYSIS_CONFIG } from "./_shared/config";`
- bodySchema = z.object({ video: z.string().min(1), shots: z.string().min(1), audio: z.string().optional(), transcript: z.string().optional(), shot_id_range: z.tuple([z.number().int(), z.number().int()]).optional() })
- router.post("/", async (req, res) => { ... }) with:
  - try bodySchema.parse(req.body) catch ZodError → 400 error("VALIDATION_ERROR", err.errors); generic → 500 error("AUDIO_ANALYSIS_FAILED", err.message)
  - if AUDIO_ANALYSIS_CONFIG.stubMode || !process.env.AUDIO_ANALYSIS_ML_LOADED → return res.json(success({ shots: [], count: 0, errors: [], stub_mode: true, message: "Phase 10 stub: ML models not loaded. Producer client envelope round-trip proven." }, "Audio analysis stub"))
  - else → return res.status(501).json(error("NOT_IMPLEMENTED", "ML path not yet wired — Phase 12+ responsibility"))
- `export default router;`

Do NOT add any file reads of `video` or `shots` paths (T-10-04 mitigation: Phase 10 stub validates type only; Phase 12 adds path sandboxing when it reads files).

**Step 4 — Edit src/router.ts:**
Add 1 import line near the existing route imports (after the route46 shot-analysis import line): `import route47 from "./routes/production/audio-analysis/index";`
Add 1 mount line after line 189 (the shot-analysis mount): `app.use("/api/production/audio-analysis", route47);`
NO `/v1/` prefix (research verified router.ts mounts shot-analysis without `/v1/`; the ROADMAP SC #1 mention of `/v1/` is a doc oversight — follow research).

**Step 5 — Compile-check + commit:**
- Run TypeScript compile: `cd /data/workspace/kais-aigc-platform && npx tsc --noEmit 2>&1 | head -20` (must have zero errors in audio-analysis files; pre-existing errors elsewhere are acceptable)
- `git add src/routes/production/audio-analysis/ src/router.ts`
- `git commit -m "feat(audio-analysis-route): Phase 10 ROUTE-01 stub — envelope mirrors shot-analysis"`

Do NOT push. Do NOT PR. The cross-repo PR is post-Phase 10 per CONTEXT.md deferred list.

Verify `import torch` in the shot-timeline repo (cwd `/data/workspace/kais-shot-timeline`) is unchanged after this task: `cd /data/workspace/kais-shot-timeline && python3 -c "import torch; assert '+cu124' in torch.__version__"` — this task should not touch it but verifies no collateral damage.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && \
      git branch --show-current | grep -q "^feat/audio-analysis-route$" && \
      git log --oneline -1 | grep -q "feat(audio-analysis-route)" && \
      test -f src/routes/production/audio-analysis/index.ts && \
      test -f src/routes/production/audio-analysis/_shared/config.ts && \
      grep -q "import { success, error } from \"@/lib/responseFormat\"" src/routes/production/audio-analysis/index.ts && \
      grep -q "app.use(\"/api/production/audio-analysis\", route47)" src/router.ts && \
      grep -v '^//' src/routes/production/audio-analysis/index.ts | grep -v '^ \*' | grep -cE "fs\.|readFile|path\.join.*video|path\.join.*shots" | grep -q "^0$" && \
      echo "[route-stub] all checks PASS" && \
      cd /data/workspace/kais-shot-timeline && python3 -c "import torch; assert '+cu124' in torch.__version__; print('[torch] cu124 intact')"</automated>
  </verify>
  <acceptance_criteria>
- Branch feat/audio-analysis-route exists, current, branched from develop (verify: `git merge-base HEAD develop == git rev-parse develop`)
- 2 TS files exist at the specified paths.
- index.ts contains `import { success, error } from "@/lib/responseFormat"` (byte-identical to shot-analysis).
- index.ts stub-mode return shape: `success({ shots: [], count: 0, errors: [], stub_mode: true, message: ... }, "Audio analysis stub")` — the `message` arg is the OUTER envelope message; the inner `message` field documents the stub state.
- index.ts contains NO `fs.read*` or `path.join` calls referencing req.body fields (Phase 10 stub does not read files; Phase 12 adds sandboxing).
- router.ts has `app.use("/api/production/audio-analysis", route47)` — NO `/v1/`.
- npx tsc --noEmit reports 0 NEW errors in audio-analysis files (pre-existing errors tolerated).
- Commit on feat/audio-analysis-route exists with the specified message format.
- System torch 2.6.0+cu124 unchanged (no project-env poisoning).
  </acceptance_criteria>
  <done>ROUTE-01 stub branch committed in kais-aigc-platform; envelope ready for Phase 12 producer client integration. The shot-timeline repo is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Start the route host, run route_stub_smoke.sh, document any baseline-discrepancy findings</name>
  <files>(no file changes — verification only; optionally spike/audio/tests/route_stub_smoke.sh run-log captured in SUMMARY)</files>
  <read_first>
    - /data/workspace/kais-aigc-platform/src/router.ts (confirm mount)
    - /data/workspace/kais-aigc-platform/package.json (find dev-server start script)
    - spike/audio/tests/route_stub_smoke.sh (from Plan 01 Task 2)
  </read_first>
  <action>
This task verifies the stub round-trips correctly via curl smoke checks. If the route host requires external services (ComfyUI, gold-team, Postgres) that are not running, the executor falls back to TypeScript-level verification (below).

**Step 1 — Attempt to start the route host (background):**
```
cd /data/workspace/kais-aigc-platform
# Try common dev-script names; do NOT fail the task if these don't start cleanly
npm run dev > /tmp/route-stub-dev.log 2>&1 &
DEV_PID=$!
sleep 10  # generous startup window
```
Check `/tmp/route-stub-dev.log` for "Listening" or "ready" or a port number. If startup fails (missing Postgres, missing GPU service, etc.), proceed to Step 3 (fallback).

**Step 2 — If host started, run smoke checks:**
```
cd /data/workspace/kais-shot-timeline  # smoke script lives here
unset SKIP_ROUTE_STUB
export AUDIO_ROUTE_URL=http://localhost:3000   # adjust if log shows different port
bash spike/audio/tests/route_stub_smoke.sh
```
Expected: 2 of 3 curl checks PASS (happy path → code:200 + stub_mode:true; validation → code:400). Check 3 (env-flip) is informational. Kill DEV_PID after.

**Step 3 — Fallback if host cannot start in dev sandbox:**
Run a TypeScript-only smoke via a 5-line inline script:
```
cd /data/workspace/kais-aigc-platform && npx tsx -e "
  import('./src/routes/production/audio-analysis/index.ts').then(m => {
    console.log('[fallback] module loaded, default export type:', typeof m.default);
  });
"
```
Record in SUMMARY that the curl-based envelope round-trip is deferred to the post-merge smoke check (mirroring v1.1 Phase 7 CAST-01..04/08 deferred pattern per CONTEXT.md `deferred` list — the stub-only round-trip can be proven when the route host is in a runnable state, e.g. post-reboot with ComfyUI available).

**Step 4 — Document outcome in SUMMARY:** which path (Step 2 or Step 3) ran, what the result was, and any baseline discrepancies (e.g. if the route host port differed from 3000, or ifAUDIO_ANALYSIS_STUB_MODE env flip requires server restart).

Do NOT push or PR the cross-repo branch. Do NOT modify any production deployment config.
  </action>
  <verify>
    <automated>cd /data/workspace/kais-aigc-platform && \
      git log --oneline feat/audio-analysis-route ^develop | head -3 && \
      echo "---" && \
      grep -c "stub_mode: true" src/routes/production/audio-analysis/index.ts | grep -qE "^[1-9]" && \
      echo "[route-stub] commit + stub_mode field present"</automated>
  </verify>
  <acceptance_criteria>
- Either: route_stub_smoke.sh happy-path + validation checks both pass (echo "[route-stub] PASS"), OR: Step 3 fallback is taken and the SUMMARY documents the deferral.
- feat/audio-analysis-route has 1 commit ahead of develop, NOT pushed.
- If curl-based check ran, results JSON-equivalent output documented in SUMMARY (code:200 + stub_mode:true on happy path; code:400 on {} body).
- No process leak: the background dev server is killed via `kill $DEV_PID 2>/dev/null || true` at end of task.
  </acceptance_criteria>
  <done>ROUTE-01 envelope round-trip either proven via curl OR deferred-with-explanation per the v1.1 CAST pattern. Summary records which path was taken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → route stub | Producer httpx client (Phase 12) crosses this boundary with the JSON body; zod is the only validator in Phase 10 |
| route stub → filesystem | SEALED in Phase 10 — stub does NOT read `video`/`shots` files even though they're in the body; Phase 12 must add path sandboxing before any fs.access/readFile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering / Path Traversal | audio-analysis/index.ts body fields (video, shots) | mitigate (Phase 10) + transfer (Phase 12) | Phase 10 stub uses zod string-type validation only; no fs reads. Inline comment in index.ts flags "Phase 12 MUST add path sandboxing before reading any body-supplied path". |
| T-10-06 | Information Disclosure | error() responses echo zod errors / err.message | mitigate | error envelope limited to VALIDATION_ERROR/AUDIO_ANALYSIS_FAILED/NOT_IMPLEMENTED — no auth headers, no internal paths echoed |
| T-10-SC | Supply Chain | (no npm install in this plan) | accept | Reuses existing express + zod already on the route host; no new deps |
</threat_model>

<verification>
- Branch feat/audio-analysis-route exists, 1 commit ahead of develop
- router.ts grep `app.use.*audio-analysis` returns exactly 1 match
- index.ts grep `success(...)` returns ≥1 match (stub-mode path) and `error(...)` returns ≥2 matches (zod + 501 paths)
- No `fs.read*` or `path.join.*req.body` references in index.ts (Phase 10 boundary)
- npx tsc --noEmit adds zero new errors
- System torch in shot-timeline repo remains 2.6.0+cu124
</verification>

<success_criteria>
- ROUTE-01 stub envelope `{code:200, data:{...}, message}` proven via curl OR documented deferral per v1.1 CAST pattern
- Stub branch committed (not pushed) in kais-aigc-platform on feat/audio-analysis-route from develop
- Phase 12 producer client has an integration target — the "audio-analysis route does not exist" STATE.md blocker is dissolved for Phase 12 planning
</success_criteria>

<output>
Create `.planning/phases/10-risk-validation-spike-route-stub/10-02-SUMMARY.md` when done. Report: branch SHA, commit SHA, curl-or-fallback path taken, any port/env discrepancies, residual Phase 12 contract notes (e.g. confirmed mount path /api/production/audio-analysis, expected body schema).
</output>
