---
phase: 10-risk-validation-spike-route-stub
plan: 04
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - spike/audio/run_mir_head_to_head.py
  - spike/audio/results/sample_mir_ep01.json
  - spike/audio/results/mir_mert_ep01.json
  - spike/audio/results/mir_panns_ep01.json
autonomous: false
requirements: [MUS-04]
tags: [spike, mir, mert, panns, mus-04, head-to-head, checkpoint]

must_haves:
  truths:
    - "MERT-v1-95M (m-a-p/MERT-v1-95M) AND PANNs Cnn14 (panns-inference default checkpoint) both load on CPU"
    - "Both models run on the SAME 30 stratified ep01 drums+bass+other mix segments — head-to-head integrity (Pitfall 9)"
    - "sample_mir_ep01.json commits the shared shot_id list BEFORE either model runs (audit trail)"
    - "Both result JSONs have identical shot_id lists (results_schema_check.py asserts this)"
    - "MERT uses librosa.load(sr=24000) (Pitfall 3 — 24kHz training rate); PANNs uses librosa.load(sr=32000)"
    - "Plan 06 report has a head-to-head mAP comparison table — both models' scores on identical segments + identical ground-truth annotation"
  artifacts:
    - path: "spike/audio/run_mir_head_to_head.py"
      provides: "Combined MERT+PANNs head-to-head MIR spike — drums+bass+other mix on 30 segments"
      min_lines: 150
      contains: "m-a-p/MERT-v1-95M"
    - path: "spike/audio/results/sample_mir_ep01.json"
      provides: "Pre-committed shared shot_id list — head-to-head integrity audit trail"
      contains: "shot_ids"
    - path: "spike/audio/results/mir_mert_ep01.json"
      provides: "MERT predicted instruments per segment"
      contains: "per_sample"
    - path: "spike/audio/results/mir_panns_ep01.json"
      provides: "PANNs Cnn14 predicted instruments per segment + AudioSet class probabilities"
      contains: "per_sample"
  key_links:
    - from: "spike/audio/run_mir_head_to_head.py"
      to: "spike/audio/common.py"
      via: "from common import EP01_DRUMS, EP01_BASS, EP01_OTHER, EP01_SHOTS, stratified_sample, write_result, _safe_error"
      pattern: "from \\.{0,1}common import"
    - from: "spike/audio/run_mir_head_to_head.py"
      to: "m-a-p/MERT-v1-95M via transformers AutoModel"
      via: "AutoModel.from_pretrained('m-a-p/MERT-v1-95M', trust_remote_code=True)"
      pattern: "m-a-p/MERT-v1-95M"
    - from: "spike/audio/run_mir_head_to_head.py"
      to: "PANNs Cnn14 via panns_inference.AudioTagging"
      via: "AudioTagging(checkpoint_path=None, device='cpu')"
      pattern: "panns_inference"
---

<objective>
Run the MERT-v1-95M vs PANNs Cnn14 head-to-head MIR spike on 30 stratified ep01 drums+bass+other segments. Produce 2 result JSONs on IDENTICAL segments (Pitfall 9 head-to-head integrity) so Plan 06 can compute a head-to-head mAP — the empirical basis for the MUS-04 ship (mAP≥0.30) / ship-nullable (0.20-0.30) / defer (<0.20) decision AND the MERT-vs-PANNs model pick for the route.

Purpose: research SUMMARY explicitly defers the MERT-vs-PANNs pick to this spike. Chinese folk instruments (erhu/pipa/guzheng/dizi) have no published benchmark on either model. This is the SECOND-highest ML unknown of v1.2 (STATE.md "polyphonic instrument recognition on Chinese folk").

Output: 4 files — run_mir_head_to_head.py (throwaway script) + sample_mir_ep01.json (shared sample audit) + 2 model result JSONs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-risk-validation-spike-route-stub/10-CONTEXT.md
@.planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md
@./CLAUDE.md

<interfaces>
<!-- From Plan 01 common.py -->
EP01_DRUMS, EP01_BASS, EP01_OTHER: pathlib.Path -> ep01 Demucs stems
EP01_SHOTS: pathlib.Path -> shots.json
stratified_sample(segments, n=30, seed=10) -> list[(idx, seg_dict)]   # SAME seed as SER + WhisperX plans
write_result(model, fixture, payload)
_safe_error(msg)

<!-- MERT-v1-95M interface (research §Pattern 2, lines 386-415) -->
from transformers import AutoModel, Wav2Vec2FeatureExtractor
model = AutoModel.from_pretrained("m-a-p/MERT-v1-95M", trust_remote_code=True)
processor = Wav2Vec2FeatureExtractor.from_pretrained("m-a-p/MERT-v1-95M", trust_remote_code=True)
model.eval()
audio_24k, _ = librosa.load(path, sr=24000, mono=True)   # 24kHz training rate
inputs = processor(audio_24k, sampling_rate=24000, return_tensors="pt")
with torch.no_grad(): outputs = model(**inputs, output_hidden_states=True)
all_layers = torch.stack(outputs.hidden_states).squeeze()  # [13, T, 768]
embedding = all_layers.mean(-2)  # layer-averaged time reduction

<!-- PANNs Cnn14 interface (research §Pattern 3, lines 418-441) -->
from panns_inference import AudioTagging, labels
at = AudioTagging(checkpoint_path=None, device="cpu")   # None = default Cnn14_mAP=0.431.pth
audio_32k, _ = librosa.load(path, sr=32000, mono=True)  # 32kHz training rate
audio_32k = audio_32k[None, :]  # (batch=1, samples)
clipwise_output, embedding = at.inference(audio_32k)
# clipwise_output: shape (1, 527) — probabilities over AudioSet 527 classes
# labels: list of 527 AudioSet class names — lookup by index
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: User confirms MIR ground-truth methodology for head-to-head mAP</name>
  <decision>How should the spike compute mAP for the MERT-vs-PANNs head-to-head on 30 ep01 segments, given that no canonical Chinese-folk instrument ground truth exists?</decision>
  <context>Unlike SER (which has the self-consistency fallback), mAP REQUIRES ground-truth instrument labels. The choice determines whether the spike produces a comparable number per the MUS-04 threshold (mAP ≥0.30) or falls back to a qualitative comparison.</context>
  <options>
    <option id="mir-a">
      <name>(a) Developer-annotated 30-segment ground truth (~1.5hr labor, MOST rigorous)</name>
      <pros>True mAP against human labels for the same 30 segments. Annotation set: {erhu, pipa, guzheng, dizi, percussion, piano, strings, wind, synth, bass, drums, other}. Multi-label per segment. Produces a defensible mAP for the ≥0.30/&lt;0.20 threshold.</pros>
      <cons>~1.5 hours manual labor. Annotator may not reliably distinguish erhu vs pipa vs dizi on polyphonic mixes. 30 segments × ~5 instruments/segment = 150 labels; manageable but not trivial.</cons>
    </option>
    <option id="mir-b">
      <name>(b) AudioSet-canonical subset proxy (cheap, semi-rigorous)</name>
      <pros>Filter PANNs's 527-class output to a 12-class instrument subset (matching the annotation set in (a)). For PANNs, compute precision@5 against developer-annotated top-5 (still needs (a)'s annotation, but lighter — 5 labels/segment). For MERT, compute nearest-neighbor match against a fixed embedding bank. Report precision@5 instead of mAP.</pros>
      <cons>Still needs developer annotation. Precision@5 is a weaker metric than mAP (doesn't penalize false negatives). Threshold comparison becomes precision@5 ≥0.50 (proxy) rather than the contracted mAP ≥0.30.</cons>
    </option>
    <option id="mir-c">
      <name>(c) Qualitative-only — SKIP rigorous mAP, report top-5 predictions + developer sanity rating (cheapest)</name>
      <pros>Zero rigorous-metric labor. Developer eyeballs each segment's top-5 predictions from both models, rates "sensible / partially sensible / wrong" (3-class). Report records MERT vs PANNs sensible-rate.</pros>
      <cons>Cannot compute mAP. Cannot apply the MUS-04 ≥0.30/&lt;0.20 threshold literally. Decision becomes qualitative ("both models produce sensible folk-instrument labels" → ship-nullable OR "predictions are noise" → defer). Report must explicitly note threshold-applicability gap.</cons>
    </option>
    <option id="mir-ab">
      <name>(a+b) Annotate 30 segments (multi-label) THEN compute mAP (a) AND precision@5 (b) — full evidence</name>
      <pros>Strongest evidence; allows Plan 06 to lock MUS-04 with maximum confidence. Same annotation serves both metrics.</pros>
      <cons>~1.5 hours labor (same as (a) alone). Marginal extra compute for the second metric.</cons>
    </option>
  </options>
  <resume-signal>Reply with: mir-a | mir-b | mir-c | mir-ab (the spike implements the chosen path; the report records the choice)</resume-signal>
  <files>(none — user-decision checkpoint; choice recorded in SUMMARY)</files>
  <action>Block plan execution until the user replies with one of: mir-a | mir-b | mir-c | mir-ab. Record the chosen methodology literal in the plan SUMMARY so Task 2 can read it as an input. Do NOT proceed with install/script-write until the user's choice is captured.</action>
  <verify>
    <automated>test -f .planning/phases/10-risk-validation-spike-route-stub/10-04-SUMMARY.md && grep -qE "mir[_-](a|b|c|ab)" .planning/phases/10-risk-validation-spike-route-stub/10-04-SUMMARY.md</automated>
  </verify>
  <done>SUMMARY records the user's chosen MIR methodology literal (mir-a/b/c/ab). Task 2 reads this value to branch the MIR script's metric computation path.</done>
</task>

<task type="auto">
  <name>Task 2: Install transformers/panns-inference + write run_mir_head_to_head.py</name>
  <files>spike/audio/run_mir_head_to_head.py, spike/audio/results/sample_mir_ep01.json</files>
  <read_first>
    - spike/audio/common.py
    - .planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md §Pattern 2 + §Pattern 3 (model loading code)
    - .planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md §Pitfalls 3, 9 (sample rate, head-to-head integrity)
    - output/虫虫武侠小故事《小江湖》第01话…/audio_analysis.json (per-shot stem energies — useful context for which shots have prominent music)
  </read_first>
  <action>
**Step 1 — Install MIR packages (verified [OK] in research Package Legitimacy Audit):**
```
pip install --break-system-packages panns-inference==0.1.1
# transformers 5.6.2 + torch 2.6.0+cu124 already present per research Environment Availability
python3 -c "import panns_inference; from transformers import AutoModel; print('[mir] imports OK')"
python3 -c "import torch; assert '+cu124' in torch.__version__; print('[torch] cu124 intact')"
```
librosa is already transitively present — no install needed.

**Step 2 — Compute + commit shared sample FIRST (Pitfall 9 head-to-head integrity):**
Before writing the script logic, compute the 30-segment sample and write its shot_id list as a standalone audit artifact:
```
python3 -c "
import sys; sys.path.insert(0,'spike/audio')
from common import EP01_SHOTS, stratified_sample
import json
shots = json.loads(EP01_SHOTS.read_text())
sample = stratified_sample(shots, n=30, seed=10)
shot_ids = [s[1]['id'] for s in sample]
from pathlib import Path
Path('spike/audio/results').mkdir(exist_ok=True)
Path('spike/audio/results/sample_mir_ep01.json').write_text(json.dumps({
  'shot_ids': shot_ids,
  'segments': [{'shot_id': s[1]['id'], 'start_sec': s[1]['start_sec'], 'end_sec': s[1]['end_sec']} for s in sample],
  'seed': 10, 'n': 30, 'fixture': 'ep01'
}, ensure_ascii=False, indent=2))
print('[mir] sample committed with', len(shot_ids), 'shot_ids')
"
```
This sample_mir_ep01.json is the AUDIT TRAIL — Plan 06 + plan-checker greps it to verify both model JSONs use the same shot_id list.

**Step 3 — Write spike/audio/run_mir_head_to_head.py** (≥150 lines, Chinese docstring):
- `from transformers import AutoModel, Wav2Vec2FeatureExtractor`
- `from panns_inference import AudioTagging, labels as panns_labels`
- `import librosa, torch, numpy as np`
- `from common import EP01_DRUMS, EP01_BASS, EP01_OTHER, EP01_SHOTS, stratified_sample, write_result, _safe_error`
- Load shots.json; recompute stratified_sample(shots, n=30, seed=10) — assert shot_ids match sample_mir_ep01.json (fail loud if not)
- For each shot in the sample:
  - Slice drums/bass/other stems via ffmpeg to [start_sec, end_sec] into tmp drums_tmp.wav / bass_tmp.wav / other_tmp.wav
  - Load each tmp at native rate (44.1kHz via librosa with sr=None); sum waveforms (after length-align via numpy.pad to shortest)
  - **MERT path**: librosa.load(mix, sr=24000) → processor → model → hidden_states → mean-pool → embedding
  - **PANNs path**: librosa.load(mix, sr=32000) → at.inference → clipwise_output (527 probs)
  - For each model, derive top-K (K=5) predicted instruments:
    - PANNs: argsort clipwise_output descending, lookup panns_labels[idx] for top-5; filter to instrument-relevant subset via a hardcoded AudioSet instrument-class allowlist (piano, guitar, violin, erhu, pipa, guzheng, dizi, flute, drums, bass, strings, synthesizer — map AudioSet class names to this set)
    - MERT: nearest-neighbor against a small fixed embedding bank (one reference clip per instrument class — for the spike, use a 0.5s slice of a canonical instrument sample from a public corpus if available, OR skip and report embedding-only + free-text clustering)
  - Record per_sample entry: {shot_id, mert_top5: [...], panns_top5: [...], mert_embedding_norm, panns_top1_prob}
  - Print bracketed progress: `[mir] 5/30 shot_id=12 panns_top1=Piano mert_top1=Strings`
- Compute metric PER USER'S METHODOLOGY CHOICE:
  - **mir-a**: load `spike/audio/results/mir_ground_truth_ep01.json` (developer-annotated); compute per-class precision/recall; mAP = mean(avg_precision_per_class); output both models' mAP
  - **mir-b**: precision@5 against ground truth (lighter annotation); output both models' p@5
  - **mir-c**: no metric, just record per_sample top-5 + developer-sensible rating (loaded from a separate JSON if provided)
  - **mir-ab**: compute mAP + p@5 both
- write_result("mir_mert", "ep01", {...}) and write_result("mir_panns", "ep01", {...}) — TWO separate JSONs
- Methodology field + caveat (e.g. "Polyphonic MIR on Chinese folk instruments has no canonical benchmark; mAP is computed against developer-annotated 30-segment ground truth, not an external standard. Results are a calibrated estimate, not a publishable F1.")
- argparse with `--smoke-only N=3`, `--model {mert,panns,both}` flags

Use `_safe_error()` on all exception paths. Use canonical model IDs only (`m-a-p/MERT-v1-95M`, PANNs default Cnn14_mAP=0.431 via checkpoint_path=None — NEVER a local path per T-10-03).
  </action>
  <verify>
    <automated>python3 -c "import panns_inference; from transformers import AutoModel; print('[mir] imports OK')" && \
      python3 -c "import torch; assert '+cu124' in torch.__version__; print('[torch] cu124 intact')" && \
      python3 -c "import ast; ast.parse(open('spike/audio/run_mir_head_to_head.py').read()); print('[mir] parses')" && \
      grep -q "m-a-p/MERT-v1-95M" spike/audio/run_mir_head_to_head.py && \
      grep -q "from panns_inference import" spike/audio/run_mir_head_to_head.py && \
      grep -qE "sr=24000" spike/audio/run_mir_head_to_head.py && \
      grep -qE "sr=32000" spike/audio/run_mir_head_to_head.py && \
      test -f spike/audio/results/sample_mir_ep01.json && \
      python3 -c "import json; d=json.load(open('spike/audio/results/sample_mir_ep01.json')); assert len(d['shot_ids'])==30" && \
      echo "[mir] Task 2 GREEN"</automated>
  </verify>
  <acceptance_criteria>
- panns-inference 0.1.1 installed; transformers 5.6.2 + torch 2.6.0+cu124 intact.
- run_mir_head_to_head.py exists, ≥150 lines, parses.
- Imports transformers AutoModel + panns_inference AudioTagging + common.
- Hardcodes 24kHz for MERT and 32kHz for PANNs (Pitfall 3 mitigation).
- Uses m-a-p/MERT-v1-95M (canonical HF ID) and PANNs Cnn14 (checkpoint_path=None — T-10-03 supply-chain mitigation).
- sample_mir_ep01.json committed with exactly 30 shot_ids BEFORE either model runs.
- argparse supports --smoke-only, --model flags.
  </acceptance_criteria>
  <done>MIR spike script ready; sample audit artifact committed (Pitfall 9 integrity gate). Wave 1 plan-checker greps the script for 24kHz/32kHz + canonical model IDs.</done>
</task>

<task type="auto">
  <name>Task 3: Run MIR head-to-head spike (smoke then full), validate JSONs, commit results</name>
  <files>spike/audio/results/mir_mert_ep01.json, spike/audio/results/mir_panns_ep01.json</files>
  <read_first>
    - spike/audio/run_mir_head_to_head.py (Task 2)
    - spike/audio/tests/results_schema_check.py
    - spike/audio/results/sample_mir_ep01.json (Task 2 — audit trail)
  </read_first>
  <action>
**Step 1 — Smoke run (3 segments, BOTH models, verifies models download + AudioTagging + tag/instrument parsing):**
```
cd /data/workspace/kais-shot-timeline
python3 spike/audio/run_mir_head_to_head.py --smoke-only 3 --model both
```
Expect: MERT-v1-95M downloads (~350MB), PANNs Cnn14 downloads (~250MB), 3 segments processed through both models, 2 JSONs written with sample_size=3. If model download fails (HF auth), the spike records this in the SUMMARY and falls back to a single-model run (the other model gets noted as "blocked at spike time").

**Step 2 — Full run (30 segments, both models):**
```
python3 spike/audio/run_mir_head_to_head.py --model both
```
Wall-clock target: <30 min on CPU for both models × 30 segments (MERT 95M is small; PANNs Cnn14 is ~80MB).

**Step 3 — Validate head-to-head integrity + commit:**
```
python3 spike/audio/tests/results_schema_check.py && \
  python3 -c "
import json
sample = json.load(open('spike/audio/results/sample_mir_ep01.json'))['shot_ids']
mert = json.load(open('spike/audio/results/mir_mert_ep01.json'))
panns = json.load(open('spike/audio/results/mir_panns_ep01.json'))
assert mert['sample_size']==30 and panns['sample_size']==30
mert_ids = [s['shot_id'] for s in mert['per_sample']]
panns_ids = [s['shot_id'] for s in panns['per_sample']]
assert mert_ids == panns_ids == sample, 'Pitfall 9 violation: shot_id lists diverge'
print('[mir] head-to-head integrity GREEN; mAP MERT:', mert.get('metric_value'), 'PANNs:', panns.get('metric_value'))
" && \
  ! grep -qE "hf_[a-zA-Z0-9]{20,}" spike/audio/results/mir_mert_ep01.json spike/audio/results/mir_panns_ep01.json && \
  echo "[mir] no token leak" && \
  git add spike/audio/run_mir_head_to_head.py spike/audio/results/sample_mir_ep01.json spike/audio/results/mir_mert_ep01.json spike/audio/results/mir_panns_ep01.json && \
  git commit -m "spike(mir): Phase 10 MUS-04 MERT vs PANNs head-to-head on ep01 drums+bass+other (methodology=<from result>)"
```

**Step 4 — Document results in SUMMARY** (do NOT lock MUS-04 decision yet — that's Plan 06):
- methodology used (Task 1 choice)
- Both models' metric_value (mAP or p@5 or qualitative-rating-%)
- Per-class confusion highlights: which instruments did each model get right/wrong on Chinese folk (erhu/pipa/guzheng/dizi specifically)?
- Winner of the head-to-head for the route-host model pick
- Sanity observations: are the top-5 predictions sensible for the dialogue context (e.g. a shot with prominent BGM should produce instrument predictions; a dialogue-only shot should produce noise/silence predictions)
  </action>
  <verify>
    <automated>python3 -c "
import json
sample = json.load(open('spike/audio/results/sample_mir_ep01.json'))['shot_ids']
mert = json.load(open('spike/audio/results/mir_mert_ep01.json'))
panns = json.load(open('spike/audio/results/mir_panns_ep01.json'))
assert mert['sample_size']==30 and panns['sample_size']==30
mert_ids = [s['shot_id'] for s in mert['per_sample']]
panns_ids = [s['shot_id'] for s in panns['per_sample']]
assert mert_ids == panns_ids == sample, 'Pitfall 9 violation'
print('[mir] full-run JSON GREEN, MERT metric:', mert.get('metric_value'), 'PANNs metric:', panns.get('metric_value'))
" && \
      ! grep -qE "hf_[a-zA-Z0-9]{20,}" spike/audio/results/mir_mert_ep01.json && \
      ! grep -qE "hf_[a-zA-Z0-9]{20,}" spike/audio/results/mir_panns_ep01.json && \
      git log --oneline -1 | grep -q "spike(mir)"</automated>
  </verify>
  <acceptance_criteria>
- mir_mert_ep01.json + mir_panns_ep01.json committed, both with sample_size=30.
- shot_id lists in both JSONs are IDENTICAL and match sample_mir_ep01.json (Pitfall 9 integrity).
- Methodology field names Task 1 choice; caveat non-empty.
- No hf_<20+> token patterns in either JSON.
- Commit message format `spike(mir): Phase 10 MUS-04 ...`.
- SUMMARY records head-to-head winner + per-class confusion highlights + Chinese-folk-instrument observations.
  </acceptance_criteria>
  <done>MIR head-to-head spike complete. Plan 06 reads both JSONs to compose the MIR section + lock MUS-04 outcome + pick route-host MIR model in PROJECT.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HF Hub → local process | transformers downloads m-a-p/MERT-v1-95M (~350MB) + panns_inference downloads Cnn14_mAP=0.431.pth (~250MB) on first run |
| Demucs stems → librosa/ffmpeg | drums/bass/other .wav files sliced via ffmpeg subprocess; path is hardcoded from common.py EP01_DRUMS/BASS/OTHER (no untrusted input) |
| Model output → results JSON → git | Model predictions are deterministic numbers/strings — low leakage risk, but exception strings from HF libraries get _safe_error-redacted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | transformers/panns_inference exception strings | mitigate | `_safe_error` regex on all exception paths |
| T-10-02 | Information Disclosure | mir_mert_ep01.json + mir_panns_ep01.json | mitigate | grep gate before commit; results contain only model predictions (no auth context) |
| T-10-03 | Supply Chain | MERT + PANNs model weights | mitigate | canonical IDs `m-a-p/MERT-v1-95M` + PANNs default `checkpoint_path=None` (resolves to Cnn14_mAP=0.431.pth on PyPI-hosted CDN); no local-checkpoint paths allowed |
| T-10-08 | Tampering | ffmpeg subprocess slicing | mitigate | subprocess args constructed from int/float shot timing values only (no shell expansion); no untrusted string reaches subprocess argv |
| T-10-SC | Supply Chain | panns-inference==0.1.1 install | mitigate | RESEARCH Package Legitimacy Audit [OK] verdict — qiuqiangkong/panns_inference is the official PANNs Python interface, mature (11 versions since 2020); pin version |
| T-10-09 | Denial of Service | librosa.load on long audio | accept | spike slices to shot-length clips (<30s typically); bounded by sample n=30 |
</threat_model>

<verification>
- run_mir_head_to_head.py committed with both model canonical IDs + 24kHz + 32kHz sample-rate hardcodes
- sample_mir_ep01.json committed with 30 shot_ids BEFORE model JSONs
- mir_mert_ep01.json + mir_panns_ep01.json have identical shot_id lists (Pitfall 9 grep gate)
- No hf_<20+> token patterns in any results JSON
- panns-inference 0.1.1 installed; system torch remains 2.6.0+cu124
</verification>

<success_criteria>
- MIR head-to-head JSONs are the empirical basis Plan 06 needs to (a) lock MUS-04 ship/defer per mAP threshold AND (b) pick MERT or PANNs as the route-host MIR model
- Head-to-head integrity (Pitfall 9) proven via shared sample JSON audit
- Chinese-folk-instrument coverage explicitly addressed in SUMMARY (the highest-value finding for v1.2)
</success_criteria>

<output>
Create `.planning/phases/10-risk-validation-spike-route-stub/10-04-SUMMARY.md`. Report: Task 1 methodology, both models' metric_value, head-to-head winner, per-class confusion highlights (esp. folk instruments), qualitative sanity check.
</output>
