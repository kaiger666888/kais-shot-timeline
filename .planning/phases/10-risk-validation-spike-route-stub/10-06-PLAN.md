---
phase: 10-risk-validation-spike-route-stub
plan: 06
type: execute
wave: 2
depends_on: ["10-01", "10-03", "10-04", "10-05"]
files_modified:
  - spike/audio/aggregate_report.py
  - .planning/research/audio-spike-report.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: false
requirements: [DIA-04, DIA-05, MUS-04]
tags: [aggregate, report, project-decisions, checkpoint, lock-outcomes]

must_haves:
  truths:
    - "Spike report .planning/research/audio-spike-report.md exists with 4 named sections (SER / MIR head-to-head / WhisperX drift / CUDA path) + methodology section + AF-02/AF-03 caveats"
    - "Report contains the literal phrase 'calibrated estimate' (AF-02/AF-03 anti-fabrication grep gate)"
    - "Report contains a head-to-head mAP comparison table with MERT and PANNs side-by-side on identical segments (Pitfall 9 audit)"
    - "Report records which WhisperX assumptions (A1 CPU-mode, A2 arbitrary-segment align) held and the resulting drift stat"
    - "PROJECT.md Key Decisions table has 4 NEW entries: models_used (per modality), CUDA path (12.8-upgrade vs stay-on-12.4), DIA-04 ship/defer, DIA-05 ship/defer, MUS-04 ship/defer (the 4 locked outcomes per ROADMAP SC #5)"
    - "STATE.md 'Pending Todos' Phase 10 entry + BLOCKER 1 entry updated to reflect resolution"
    - "No hf_<20+> token patterns anywhere in the report or committed JSONs (T-10-02 final grep gate)"
  artifacts:
    - path: ".planning/research/audio-spike-report.md"
      provides: "Phase 10 spike report — empirical basis for Phase 11 contract locks"
      min_lines: 150
      contains: "calibrated estimate"
    - path: ".planning/PROJECT.md"
      provides: "PROJECT.md Key Decisions updated with 4 locked outcomes"
      contains: "CUDA"
    - path: "spike/audio/aggregate_report.py"
      provides: "Fleshed-out aggregator: reads results/*.json + writes 4-section markdown"
      min_lines: 150
  key_links:
    - from: ".planning/research/audio-spike-report.md"
      to: "spike/audio/results/{ser_sensevoice,mir_mert,mir_panns,whisperx_align}_ep01.json"
      via: "embedded numeric citations (e.g. 'macro-F1=0.47 per results/ser_sensevoice_ep01.json')"
      pattern: "results/ser_sensevoice_ep01\\.json\\|results/mir_mert_ep01\\.json"
    - from: ".planning/PROJECT.md"
      to: ".planning/research/audio-spike-report.md"
      via: "Key Decisions entries cite the report section"
      pattern: "audio-spike-report\\.md"
---

<objective>
Aggregate the 4 spike result JSONs (SER, MERT, PANNs, WhisperX) into the Phase 10 spike report at `.planning/research/audio-spike-report.md`, then lock the 4 outcomes into PROJECT.md Key Decisions via a checkpoint task (user confirms threshold judgments per REQUIREMENTS tables). This is the empirical basis Phase 11 contract lock depends on — non-negotiable invariant #1.

Purpose: Phase 10 produces 5 success criteria; this plan delivers criteria #2-#5. Without these locks, Phase 11 cannot decide which CONDITIONAL fields go in the v1.2 schema (DIA-04 emotion? DIA-05 word-level? MUS-04 instruments?) nor the route-host CUDA baseline.

Output: 4 files — aggregate_report.py (fleshed out from Plan 01 skeleton), audio-spike-report.md (the deliverable), PROJECT.md (Key Decisions updated), STATE.md (todos + blockers updated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-risk-validation-spike-route-stub/10-CONTEXT.md
@.planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md
@.planning/phases/10-risk-validation-spike-route-stub/10-VALIDATION.md

<interfaces>
<!-- Inputs from Wave 1 plans (must exist before this plan runs) -->
spike/audio/results/ser_sensevoice_ep01.json   # from Plan 03 — keys: sample_size, per_sample, methodology, caveat, metric_value
spike/audio/results/sample_mir_ep01.json        # from Plan 04 — shot_ids audit list
spike/audio/results/mir_mert_ep01.json          # from Plan 04 — keys: sample_size, per_sample, metric_value
spike/audio/results/mir_panns_ep01.json         # from Plan 04 — keys: sample_size, per_sample, metric_value
spike/audio/results/whisperx_align_ep01.json    # from Plan 05 — keys: sample_size, drift_stats, per_sample, methodology

<!-- REQUIREMENTS.md threshold tables (verbatim) -->
DIA-04: "CONDITIONAL: Phase 1 SER macro-F1 ≥50% ship / <40% defer v1.3 / 40-50% ship nullable+confidence"
DIA-05: "CONDITIONAL: Phase 1 <200ms drift on ≥80% segments ship experimental / 否则段级 only"
MUS-04: "CONDITIONAL: Phase 1 mAP ≥0.30 ship / <0.20 defer / 0.20-0.30 ship nullable+confidence；MERT vs PANNs 对决 defer 到 Phase 1"

<!-- ROADMAP SC #5: PROJECT.md Key Decisions logs the 4 locked outcomes (models_used per modality; CUDA path; DIA-04/MUS-04/DIA-05 ship-or-defer per thresholds) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flesh out aggregate_report.py to read 4 JSONs + write 4-section audio-spike-report.md with methodology caveats</name>
  <files>spike/audio/aggregate_report.py, .planning/research/audio-spike-report.md</files>
  <read_first>
    - spike/audio/aggregate_report.py (Plan 01 skeleton)
    - spike/audio/common.py (git_sha helper)
    - spike/audio/results/ser_sensevoice_ep01.json (Plan 03 output)
    - spike/audio/results/sample_mir_ep01.json + mir_mert_ep01.json + mir_panns_ep01.json (Plan 04 output)
    - spike/audio/results/whisperx_align_ep01.json (Plan 05 output)
    - .planning/phases/10-risk-validation-spike-route-stub/10-RESEARCH.md (§Pitfalls 4, 9, 10 — methodology + head-to-head + staleness gates)
    - .planning/phases/10-risk-validation-spike-route-stub/10-VALIDATION.md (Manual-Only Verifications list)
  </read_first>
  <action>
**Step 1 — Pre-flight staleness + integrity gates:**
```
cd /data/workspace/kais-shot-timeline
python3 spike/audio/aggregate_report.py --check-staleness   # exits 1 if any result JSON is stale (Pitfall 10)
# Head-to-head integrity re-verify:
python3 -c "
import json
s = json.load(open('spike/audio/results/sample_mir_ep01.json'))['shot_ids']
m = [p['shot_id'] for p in json.load(open('spike/audio/results/mir_mert_ep01.json'))['per_sample']]
p = [p['shot_id'] for p in json.load(open('spike/audio/results/mir_panns_ep01.json'))['per_sample']]
assert m == p == s, f'Pitfall 9 violation: {len(m)=} {len(p)=} {len(s)=}'
print('[aggregate] head-to-head integrity OK (n=30)')
"
```
If staleness check fails, ABORT and re-run the relevant spike plan first.

**Step 2 — Flesh out spike/audio/aggregate_report.py:**
Replace the Plan 01 skeleton `--aggregate` placeholder with real logic:
- Read all 4 results JSONs (5 if diarize was run — but diarize is not in this plan set; skip gracefully)
- Build a markdown report structure:
  - `# Phase 10 Audio Spike Report — Empirical De-risking for v1.2`
  - Metadata: git_sha, generated_at, fixture (ep01), device (CPU), fakes disclaimer
  - `## Methodology` — Chinese-language section: explains AF-02/AF-03 anti-fabrication; explicit "calibrated estimate" language; cites the user's Plan 03 + Plan 04 methodology choices (read from result JSONs' `methodology` fields)
  - `## Section 1: SER (SenseVoice) — DIA-04 evidence` — table of per-class emotion distribution; the metric_value; the threshold table verbatim from REQUIREMENTS; the chosen methodology; caveat text containing "calibrated estimate"
  - `## Section 2: MIR head-to-head (MERT vs PANNs) — MUS-04 evidence` — table with MERT and PANNs side-by-side: metric_value per model, top-5 instruments detected on sample N (qualitative), head-to-head winner; explicit Pitfall 9 audit statement ("Both models ran on the same N=30 segments per sample_mir_ep01.json")
  - `## Section 3: WhisperX drift — DIA-05 evidence + CUDA path` — drift_stats (pct_under_200ms, mean, median); per-bucket breakdown; A1/A2 status; CUDA 12.8 vs stay-on-12.4 implication (if A1=OK then both paths viable; if A1=FAIL then stay-on-12.4 is the only working path)
  - `## Section 4: Recommendations` — table mapping each conditional req (DIA-04/MUS-04/DIA-05) to its ship/defer recommendation per the threshold table; CUDA path recommendation; MERT-vs-PANNs route-host pick
  - `## Reproducibility` — how to re-run (cite spike/audio/README.md from Plan 01); commit SHAs of result JSONs; stratified_sample seed=10 n=30 invariant
- `--aggregate` writes the report to `.planning/research/audio-spike-report.md` (NOT spike/audio/results/ — this is the deliverable, not a per-model blob)
- The report's `metric_value` citations reference each results JSON by relative path (e.g. "macro-F1=0.47 per `spike/audio/results/ser_sensevoice_ep01.json`")
- All Chinese sections use Chinese; English sections use English — match the project convention (Chinese for methodology / caveats, English for table headers is OK)
- `--check-staleness` extended to also verify each result JSON's `git_sha` matches current HEAD (Pitfall 10)

**Step 3 — Run aggregator to write report:**
```
python3 spike/audio/aggregate_report.py --aggregate
```

**Step 4 — Grep gates (T-10-02 + VALIDATION.md):**
```
grep -c "calibrated estimate" .planning/research/audio-spike-report.md   # expect ≥1
grep -cE "MERT.*PANNs|PANNs.*MERT" .planning/research/audio-spike-report.md   # expect ≥1 (head-to-head)
grep -c "sample_mir_ep01.json" .planning/research/audio-spike-report.md   # expect ≥1 (Pitfall 9 audit)
! grep -qE "hf_[a-zA-Z0-9]{20,}" .planning/research/audio-spike-report.md && echo "[aggregate] no token leak"
```

Do NOT lock PROJECT.md outcomes yet — that's Task 3 after the user's threshold-judgment checkpoint.
  </action>
  <verify>
    <automated>python3 spike/audio/aggregate_report.py --check-staleness && \
      python3 spike/audio/aggregate_report.py --aggregate && \
      test -f .planning/research/audio-spike-report.md && \
      grep -c "calibrated estimate" .planning/research/audio-spike-report.md | grep -qE "^[1-9]" && \
      grep -cE "MERT.*PANNs|PANNs.*MERT" .planning/research/audio-spike-report.md | grep -qE "^[1-9]" && \
      grep -c "sample_mir_ep01.json" .planning/research/audio-spike-report.md | grep -qE "^[1-9]" && \
      ! grep -qE "hf_[a-zA-Z0-9]{20,}" .planning/research/audio-spike-report.md && \
      echo "[aggregate] Task 1 GREEN"</automated>
  </verify>
  <acceptance_criteria>
- aggregate_report.py extended (≥150 lines), reads 4 JSONs, writes report.
- audio-spike-report.md exists (≥150 lines) with 4 named sections + Methodology + Recommendations + Reproducibility.
- Report contains literal "calibrated estimate" (≥1 match).
- Report contains MERT-vs-PANNs head-to-head table (≥1 match for the regex).
- Report references sample_mir_ep01.json (Pitfall 9 audit trail, ≥1 match).
- No hf_<20+> token patterns (T-10-02 final gate).
- `--check-staleness` exits 0 (no stale results).
  </acceptance_criteria>
  <done>Spike report drafted with all numbers from Wave 1 results. Ready for the user's threshold-judgment checkpoint (Task 2) before PROJECT.md locks.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: User confirms 4 ship/defer threshold judgments (DIA-04, DIA-05, MUS-04, CUDA path)</name>
  <decision>For each of the 4 outcomes below, confirm the spike recommendation OR override with a different choice. The decisions get locked into PROJECT.md Key Decisions by Task 3.</decision>
  <context>The spike report (Task 1 output) presents metric_values + the REQUIREMENTS.md threshold tables + the spike's recommendations. The user is the product owner; their judgment is final. Per AF-02/AF-03, when the metric is borderline or methodology is weak, the safe default is ship-nullable+confidence (not ship-rigorous) or defer-to-v1.3.</context>
  <options>
    <option id="decisions-accept-all">
      <name>Accept all spike recommendations (default — fastest)</name>
      <pros>The spike numbers map cleanly to threshold tables. Plan 06 Task 3 writes the recommendations verbatim into PROJECT.md.</pros>
      <cons>None if the user trusts the methodology.</cons>
    </option>
    <option id="decisions-override">
      <name>Override specific recommendations (user provides alternative judgment)</name>
      <pros>User exercises product-owner authority; e.g. "even though macro-F1 is 52%, ship-nullable because cross-domain risk is high" or "even though mAP is 0.18, ship because Chinese folk coverage matters most".</cons>
      <cons>Requires the user to articulate a rationale; Plan 06 Task 3 records both the spike's number AND the user's override reason.</cons>
    </option>
    <option id="decisions-revisit">
      <name>Revisit — re-run a spike with different methodology (e.g. developer annotates 30 segments if SER self-consistency was ambiguous)</name>
      <pros>Strongest evidence; defers Phase 10 close by ~1-2 hours.</pros>
      <cons>Re-opens a Wave 1 plan; Plan 06 pauses.</cons>
    </option>
  </options>
  <resume-signal>Reply with: decisions-accept-all | decisions-override (list the 4 overrides with rationale) | decisions-revisit (which spike + which methodology)</resume-signal>
  <files>(none — user-decision checkpoint; 4 outcomes recorded in SUMMARY)</files>
  <action>Block Task 3 until the user replies with their decision. Record the verbatim response (decisions-accept-all OR the 4 overrides with rationale OR decisions-revisit + which spike) in the plan SUMMARY. If decisions-revisit, abort this plan and re-open the relevant Wave 1 plan with the new methodology. Otherwise Task 3 uses the recorded decisions to write the 4 PROJECT.md Key Decisions rows.</action>
  <verify>
    <automated>test -f .planning/phases/10-risk-validation-spike-route-stub/10-06-SUMMARY.md && grep -qE "decisions[_-](accept-all|override|revisit)" .planning/phases/10-risk-validation-spike-route-stub/10-06-SUMMARY.md</automated>
  </verify>
  <done>SUMMARY records the user's decision literal verbatim. If accept-all: Task 3 writes the spike recommendations as-is. If override: Task 3 records both spike rec + user override + rationale. If revisit: Plan 06 aborts and the relevant Wave 1 plan re-runs.</done>
</task>

<task type="auto">
  <name>Task 3: Lock 4 outcomes into PROJECT.md Key Decisions + update STATE.md + final commit</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md (find the Key Decisions table; preserve all existing rows)
    - .planning/STATE.md (find Pending Todos + Blockers sections)
    - .planning/research/audio-spike-report.md (Task 1 output — the numbers to cite)
    - Task 2 user response (the locked decisions)
  </read_first>
  <action>
**Step 1 — Append 4 new rows to PROJECT.md Key Decisions table:**
The 4 rows correspond to ROADMAP SC #5 ("4 locked outcomes: models_used per modality; CUDA path; DIA-04/MUS-04/DIA-05 ship-or-defer per thresholds"):

Row 1 — `models_used` (per modality):
- Dialogue (SER + events): `iic/SenseVoiceSmall` via funasr (route-host)
- Dialogue (transcribe + word-align + diarize): `WhisperX large-v3 + wav2vec2-large-xlsr-53-chinese-zh-cn` (route-host)
- Music (MIR): `<MERT-v1-95M | PANNs Cnn14>` per Plan 04 head-to-head winner (route-host)
- SFX (audio events): folded into SenseVoice 8-event + PANNs 527-class (route-host)

Row 2 — `CUDA path`:
- Choice: `<upgrade-to-12.8 | stay-on-12.4>`
- If upgrade-to-12.8: "route-host upgrades to CUDA 12.8; WhisperX runs native on GPU; spike drift numbers from CPU are baseline, GPU expected similar precision"
- If stay-on-12.4: "route-host stays at cu124; WhisperX runs in isolated venv on CPU (per Plan 05 pattern); DIA-05 ships experimental at best; Phase 12+ documents this trade-off"

Row 3 — `DIA-04 ship-or-defer`:
- Value: `<ship | ship-nullable+confidence | defer-v1.3>` per Task 2 confirmation
- Cite: macro-F1 (or proxy) number + methodology + report section
- If ship-nullable+confidence: "field nullable in schema, confidence field populated, fidelity_disclaimer applies"

Row 4 — `MUS-04 + DIA-05 ship-or-defer`:
- MUS-04 value: `<ship | ship-nullable+confidence | defer-v1.3>` per Task 2
- DIA-05 value: `<ship-experimental | defer-segment-only>` per Task 2
- Cite: mAP number + drift pct_under_200ms + report sections

Each row includes a `decided_at: 2026-07-25` + `phase: 10` + `evidence: .planning/research/audio-spike-report.md#<section>` citation.

**Step 2 — Update STATE.md:**
- "Pending Todos" Phase 10 entry → mark DONE (the spike report exists at `.planning/research/audio-spike-report.md`; outcomes locked in PROJECT.md)
- "Blockers/Concerns" BLOCKER 1 (CUDA 12.8 upgrade) → mark RESOLVED with the chosen path + citation
- "Blockers/Concerns" Chinese SER cross-domain risk → mark RESOLVED (cite Section 1 of spike report)
- "Blockers/Concerns" Polyphonic instrument recognition on Chinese folk → mark RESOLVED (cite Section 2 of spike report)
- "Current Position" Phase: 10 → status "Spike complete; ready for /gsd:verify-work then /gsd:plan-phase 11"
- "Session Continuity" Last session note updated to mention spike report path + the 4 locked outcomes
- "Deferred Items" — add a row for any of DIA-04/DIA-05/MUS-04 that were marked defer-v1.3 in Row 3/4

**Step 3 — Final grep gates + commit:**
```
# Final T-10-02 grep across ALL Phase 10 outputs
! grep -qE "hf_[a-zA-Z0-9]{20,}" .planning/research/audio-spike-report.md spike/audio/results/*.json && echo "[lock] no token leak anywhere"

# PROJECT.md has 4 new entries (≥4 matches for the decided outcomes)
grep -cE "DIA-04|DIA-05|MUS-04|CUDA 12\.[48]|models_used" .planning/PROJECT.md | grep -qE "^[4-9]|[1-9][0-9]" && echo "[lock] PROJECT.md has the 4 outcomes"

# STATE.md updates
grep -q "audio-spike-report.md" .planning/STATE.md && echo "[lock] STATE.md cites spike report"

git add .planning/PROJECT.md .planning/STATE.md .planning/research/audio-spike-report.md spike/audio/aggregate_report.py && \
  git commit -m "docs(phase-10): lock 4 spike outcomes in PROJECT.md + spike report deliverable"
```
  </action>
  <verify>
    <automated>grep -cE "DIA-04|DIA-05|MUS-04|CUDA 12\.[48]" .planning/PROJECT.md | grep -qE "[4-9]|[1-9][0-9]" && \
      grep -q "audio-spike-report.md" .planning/STATE.md && \
      ! grep -qE "hf_[a-zA-Z0-9]{20,}" .planning/research/audio-spike-report.md && \
      ! grep -qE "hf_[a-zA-Z0-9]{20,}" spike/audio/results/ser_sensevoice_ep01.json && \
      ! grep -qE "hf_[a-zA-Z0-9]{20,}" spike/audio/results/mir_mert_ep01.json && \
      ! grep -qE "hf_[a-zA-Z0-9]{20,}" spike/audio/results/mir_panns_ep01.json && \
      ! grep -qE "hf_[a-zA-Z0-9]{20,}" spike/audio/results/whisperx_align_ep01.json && \
      git log --oneline -1 | grep -q "docs(phase-10)" && \
      echo "[lock] Task 3 + Phase 10 GREEN"</automated>
  </verify>
  <acceptance_criteria>
- PROJECT.md Key Decisions has 4 new entries with citations to audio-spike-report.md sections.
- STATE.md Pending Todos Phase 10 entry resolved; BLOCKER 1 marked RESOLVED with chosen path.
- STATE.md Session Continuity references the spike report + locked outcomes.
- No hf_<20+> patterns in any committed artifact (T-10-02 final gate across report + all 4 JSONs).
- Commit message format `docs(phase-10): lock 4 spike outcomes ...`.
- All 5 ROADMAP success criteria for Phase 10 are now empirically addressed (ROUTE-01 in Plan 02; SER/MIR/WhisperX in Plans 03/04/05; PROJECT.md locks in this task).
  </acceptance_criteria>
  <done>Phase 10 complete. The contract-first Phase 11 can now plan against empirical evidence + locked outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike results JSON → report markdown | Aggregator reads JSONs (trusted — produced by Wave 1 spike scripts) and writes markdown |
| report markdown → PROJECT.md / STATE.md | Decisions cross from spike evidence into project-authoritative docs — user checkpoint (Task 2) is the gate |
| Committed artifacts → public git history | Once committed, the report + JSONs are in the repo; T-10-02 grep gate is the last-chance secrets filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Information Disclosure | audio-spike-report.md + all 4 results JSONs | mitigate | final grep gate `grep -rE "hf_[a-zA-Z0-9]{20,}"` across .planning/research/audio-spike-report.md + spike/audio/results/*.json before commit; if ANY match, ABORT and re-run the relevant spike with _safe_error applied to the leaking path |
| T-10-11 | Repudiation | PROJECT.md Key Decisions | mitigate | each of the 4 new rows cites decided_at + phase + evidence: audio-spike-report.md#<section>; user checkpoint (Task 2) is recorded in SUMMARY as the lock event |
| T-10-12 | Information Integrity | aggregator output | mitigate | aggregator re-runs `--check-staleness` automatically before writing the report (Pitfall 10); head-to-head integrity (Pitfall 9) re-verified in pre-flight |
</threat_model>

<verification>
- audio-spike-report.md exists with all 4 sections + methodology caveats + head-to-head mAP + Pitfall 9 audit
- PROJECT.md Key Decisions table has 4 new rows (DIA-04, DIA-05, MUS-04, CUDA path / models_used)
- STATE.md BLOCKER 1 marked RESOLVED with the chosen CUDA path
- STATE.md Pending Todos Phase 10 entry marked DONE
- Final T-10-02 grep gate clean across report + 4 JSONs
- All 5 Phase 10 ROADMAP success criteria empirically addressed (audit table below)
</verification>

<success_criteria>
- **ROADMAP SC #1** (ROUTE-01 envelope): delivered by Plan 02 — cited in report §"ROUTE-01 stub"
- **ROADMAP SC #2** (Chinese SER macro-F1): delivered by Plan 03 + aggregated into report §1 — locked into PROJECT.md as DIA-04 row
- **ROADMAP SC #3** (polyphonic MIR mAP + MERT-vs-PANNs pick): delivered by Plan 04 + aggregated into report §2 — locked into PROJECT.md as MUS-04 row + models_used row
- **ROADMAP SC #4** (WhisperX drift + CUDA path): delivered by Plan 05 + aggregated into report §3 — locked into PROJECT.md as DIA-05 row + CUDA path row
- **ROADMAP SC #5** (4 locked outcomes in PROJECT.md): delivered by Task 3 of this plan
- Phase 10 is ready for `/gsd:verify-work` then `/gsd:plan-phase 11`
</success_criteria>

<output>
Create `.planning/phases/10-risk-validation-spike-route-stub/10-06-SUMMARY.md`. Report: Task 2 user-confirmed decisions (verbatim), 4 PROJECT.md row citations (line numbers), STATE.md updates applied, final grep gate status, any overrides with rationale.
</output>
