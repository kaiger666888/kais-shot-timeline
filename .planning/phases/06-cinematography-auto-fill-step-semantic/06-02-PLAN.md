---
phase: 06-cinematography-auto-fill-step-semantic
plan: 02
type: execute
wave: 2
depends_on: ['06-01']
files_modified:
  - analysis/__init__.py
  - analysis/call_shot_analysis.py
  - examples/shot_analysis/shot_001.json
  - examples/shot_analysis/shot_002.json
  - examples/shot_analysis/shot_003.json
  - examples/shot_analysis/shot_004.json
  - examples/shot_analysis/shot_005.json
  - examples/shot_analysis/shot_006.json
  - examples/shot_analysis/shot_007.json
  - README.md
autonomous: true
requirements: [CINEMA-01, CINEMA-03, CINEMA-04, CINEMA-05]
user_setup:
  - service: httpx
    why: "Sync HTTP client to call kais-aigc-platform shot-analysis route (Phase 6 introduces the project's first network dependency)"
    env_vars: []
    dashboard_config: []
    precondition: "httpx 0.28.1 is already installed in the active Python env (verified via `python3 -c \"import httpx; print(httpx.__version__)\"`). No `pip install` runs in this plan. If executing in a fresh env without httpx, human MUST verify package legitimacy at https://pypi.org/project/httpx/ (Author: Tom Christie; License: BSD-3-Clause; downstream dependents include openai/diffusers/mcp/huggingface_hub) BEFORE running `pip install \"httpx==0.28.1\"`. PyPI legitimacy audit was attempted during research but PyPI was network-unreachable — package treated as [ASSUMED] per the planner protocol; the in-env evidence (pip show + downstream dependents) makes actual risk LOW."

must_haves:
  truths:
    - "compose_facets(shot_003.json) returns camera='中景, follow, fast, pan_right', action='飞虫持刀向前飞行', lighting='雾气弥漫', style='normal', subject='', scene=''"
    - "compose_facets handles null/empty route fields without producing literal 'None' or leading ', ' (shot_002 shot_scale=null, shot_005 subject_motion='', shot_007 subject_motion=null)"
    - "compose_facets applied to all 7 captured fixtures → 7 prompts entries that pass Draft202012Validator(prompts.schema.json) with 0 errors"
    - "video_content_hash(video) is deterministic (same file → same hash) and content-sensitive (1-byte content change → different hash)"
    - "Route-down scenario (--analysis-url http://127.0.0.1:1/) → all 5 route-sourced facets are empty strings, prompts.json is schema-valid, route_cache/warnings.json is populated with the failure reason"
    - "Cache-hit scenario (--offline with pre-seeded route_cache/shot_analysis/shot_XXX.json matching _cache_key) → zero network calls, prompts.json uses cached data, schema-valid"
    - "Cache-stale scenario (cache file _cache_key.route_version mismatch) → treated as miss; in route-down mode degrades to empty facets with warning"
    - "Preflight runs ONCE before the per-shot loop; on failure sets route_down=True and short-circuits remaining shots (no per-shot retry storm)"
    - "ROUTE_VERSION literal 'feat-shot-analysis-route-v1' is a module-level constant in call_shot_analysis.py (bump invalidates all cache)"
    - "Per-shot POST body includes semantic:true and subject:false (CONTEXT D-XX lock) and shot_id_range:[N,N] (per-shot isolation)"
  artifacts:
    - path: "analysis/__init__.py"
      provides: "Empty package marker (project's first package-ized directory)"
      contains: ""
    - path: "analysis/call_shot_analysis.py"
      provides: "httpx sync client + compose_facets + video_content_hash + cache + preflight + main() CLI"
      contains: "ROUTE_VERSION"
    - path: "examples/shot_analysis/shot_003.json"
      provides: "Captured route fixture (mapping correctness oracle) — repo-local copy"
      contains: "semantic"
  key_links:
    - from: "analysis/call_shot_analysis.py:main"
      to: "kais-aigc-platform POST /api/v1/production/shot-analysis"
      via: "httpx.Client(base_url=--analysis-url).post(json=body)"
      pattern: "client\\.post"
    - from: "analysis/call_shot_analysis.py:main"
      to: "output/<asset>/route_cache/shot_analysis/shot_XXX.json"
      via: "per-shot cache files with embedded _cache_key"
      pattern: "_cache_key"
    - from: "analysis/call_shot_analysis.py:main"
      to: "output/<asset>/route_cache/warnings.json"
      via: "sidecar consumed by scripts/export_asset.py (Plan 01)"
      pattern: "route_cache.*warnings\\.json"
    - from: "analysis/call_shot_analysis.py:compose_facets"
      to: "spec/schemas/prompts.schema.json"
      via: "Draft202012Validator iter_errors pre-write"
      pattern: "Draft202012Validator"
---

<objective>
Build `analysis/call_shot_analysis.py` — a self-contained CLI stage that calls the kais-aigc-platform `shot-analysis` route per-shot, maps the response into `prompts.json` facets (camera/action/lighting/style), caches each shot's route response under `route_cache/`, runs a single preflight probe before the loop, writes a warnings sidecar on any failure, and degrades gracefully to schema-valid empty-string facets when the route is unreachable.

Purpose: This is the project's first network dependency and first real `prompts.json` producer inside `run_pipeline`. The mapping is verified offline against 7 captured route fixtures (shot_001..007.json — the correctness oracle; live round-trip is deferred per STATE.md blocker until `feat/shot-analysis-route` merges). Graceful-degrade is verifiable NOW because no route is running on any expected port.

Output: 1 new package directory (`analysis/`) with 2 files (`__init__.py` + `call_shot_analysis.py`, ~200-250 LOC), 7 captured fixtures copied into `examples/shot_analysis/` (repo-local to survive without `/mnt/agents/`), and a one-line README install note. The CLI contract established here is consumed by Plan 03's `run_pipeline.step_semantic` subprocess call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cinematography-auto-fill-step-semantic/06-CONTEXT.md
@.planning/phases/06-cinematography-auto-fill-step-semantic/06-RESEARCH.md
@.planning/phases/06-cinematography-auto-fill-step-semantic/06-VALIDATION.md
@.planning/phases/06-cinematography-auto-fill-step-semantic/06-01-PLAN.md

<interfaces>
<!-- Route contract (load-bearing for CINEMA-01) — extracted via `git -C /data/workspace/kais-aigc-platform show feat/shot-analysis-route:src/routes/production/shot-analysis/index.ts` during research. -->

Endpoint: `POST /api/v1/production/shot-analysis` (Zod-validated body).

**Request body** (Phase 6 MUST send these exact keys):
```
{
  "video":          "<host abs path>",          // REQUIRED min 1 char. Route does its own docker cp into comfyui-primary.
  "shots":          "<host abs path>/shots.json", // REQUIRED min 1 char.
  "shot_id_range":  [N, N],                     // OPTIONAL but Phase 6 ALWAYS sends [N,N] for per-shot isolation.
  "semantic":       true,                       // OPTIONAL default false. Phase 6 MUST send true (else semantic.* unfilled → mapping fails).
  "subject":        false,                      // OPTIONAL default false. Phase 6 sends false (CONTEXT D-XX — SAM3 layer unused).
  "grid_n":         20,                         // OPTIONAL 1-200 default 20.
  "fps":            24                          // OPTIONAL 1-120 default 24.
}
```

**Response envelope** (`@/lib/responseFormat.ts:success(data, message)`):
```
{
  "code": 200,
  "data": {
    "shots":        [<shot_00X.json content>, ...],  // 1 element when shot_id_range=[N,N]
    "count":        N,
    "outputDir":    "/mnt/agents/output/gpu1/shot_analysis",
    "driverStdout": "..."
  },
  "message": "Shot analysis complete"
}
```

**HTTP status codes:** 200 success; 400 Zod VALIDATION_ERROR; 500 SHOT_ANALYSIS_STAGING_FAILED / DRIVER_FAILED / OUTPUT_UNREADABLE.

**Route-side timeout:** `execFileSync(..., { timeout: 900_000, maxBuffer: 16*1024*1024 })` — route self-kills driver at 900s. Client `--analysis-timeout` default 960s (> 900s by 60s margin so route self-kills first and client gets a definite 500 to degrade on; Pitfall 1).

**Per-shot route fixture shape** (captured 2026-07-24, `/mnt/agents/output/gpu1/shot_analysis/shot_003.json` — copy to `examples/shot_analysis/shot_003.json`):
```json
{
  "shot_id": "shot_003",
  "geometry": {
    "primitive": "pan_right",
    "speed": "fast",
    "camera": { "pan_px_per_frame": 18.009, "tilt_px_per_frame": -2.853, ... },
    ...
  },
  "semantic": {
    "shot_scale": "中景",
    "camera_primitive": "follow",
    "camera_speed": "fast",
    "subject_motion": "飞虫持刀向前飞行",
    "lens_feel": "normal",
    "lighting": "雾气弥漫"
  },
  "conflict": [],
  "need_api_review": true
}
```

**Established stage pattern** (mirror `audio/transcribe.py` shape): module docstring (Chinese; purpose + algorithm steps + output schema + CLI usage); lazy imports for the heavy dep (httpx); argparse with Chinese `help=`; `main()` at bottom; `if __name__ == "__main__": main()`; JSON I/O with `ensure_ascii=False, indent=2`.

**Existing exporter sidecar contract (Plan 01 — load-bearing):** `scripts/export_asset.py:main()` reads `output/<asset>/route_cache/warnings.json` best-effort and expects shape `{"warnings": ["...", "..."]}`. Plan 02's main() writes EXACTLY this shape.

**Existing prompts schema** (spec/schemas/prompts.schema.json): items are objects with required `[shot_id, start_sec, end_sec, duration, subject, action, camera, scene, lighting, style, prompt_text]`; all 6 facets are `type: string` with NO `minLength` (so empty strings are schema-valid — graceful-degrade foundation). `character_refs[]`/`prop_refs[]` are optional (Phase 8 territory — Plan 02 does NOT emit them).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create analysis/call_shot_analysis.py (httpx client + mapping + cache + preflight + main)</name>
  <files>analysis/__init__.py, analysis/call_shot_analysis.py</files>
  <behavior>
    - compose_facets(shot_003 fixture) → {camera:"中景, follow, fast, pan_right", action:"飞虫持刀向前飞行", lighting:"雾气弥漫", style:"normal", subject:"", scene:""}
    - compose_facets(shot_002 shot_scale=null) → camera string contains NO "None" literal (filters falsy parts before join)
    - compose_facets(shot_005 subject_motion="") → action == "" (empty coerced, not "None")
    - compose_facets(None) → all 6 facets "" (route_shot is None when cache miss + route down)
    - video_content_hash(same file twice) → identical 16-char hex
    - video_content_hash(file with last byte changed) → different hash
    - 7 prompts entries composed from 7 fixtures → 0 Draft202012Validator errors
    - call_route(client, body) against http://127.0.0.1:1/ → (None, "<ConnectError/HTTPError class>: ...")
    - main() with unreachable --analysis-url → writes prompts.json with all-facet-empty entries + warnings sidecar with at least 1 entry; exit 0
    - main() with --offline + pre-seeded cache matching _cache_key → 0 httpx post calls (use a counter/wrapper); prompts.json uses cached data
  </behavior>
  <read_first>
    - /mnt/agents/output/gpu1/shot_analysis/shot_003.json (mapping oracle — semantic + geometry block shape)
    - /mnt/agents/output/gpu1/shot_analysis/shot_002.json (null boundary — shot_scale=null)
    - /mnt/agents/output/gpu1/shot_analysis/shot_005.json (empty boundary — subject_motion="")
    - .planning/phases/06-cinematography-auto-fill-step-semantic/06-RESEARCH.md (§Route REQUEST Contract; §Code Examples → Example 1 full skeleton; §Common Pitfalls ALL 7; §Pattern 1 cache key; §Pattern 2 HTTPError catch)
    - .planning/phases/06-cinematography-auto-fill-step-semantic/06-CONTEXT.md (ALL decisions LOCKED — esp. mapping table lines 19-27, hash design lines 33-34, graceful-degrade lines 36-38, flags lines 40-42)
    - audio/transcribe.py (the established stage pattern — argparse, lazy httpx-ish import, main() shape, Chinese docstring)
    - audio/separate_stems.py (per-shot loop + cache-on-existence pattern)
    - prompts/merge_prompts.py (the p.get(fld, "") empty-coercion idiom to mirror)
    - spec/schemas/prompts.schema.json (validate the produced list pre-write — required[] + facets type:string no minLength)
    - README.md (lines around 151-164 — install line to extend with httpx)
  </read_first>
  <action>
**`analysis/__init__.py`:** empty file (package marker — project's first package-ized directory). One line comment `# analysis package — pipeline stages that call external ML routes` is acceptable.

**`analysis/call_shot_analysis.py`** structure (Chinese module docstring mandatory; mirror audio/transcribe.py header style — purpose + route contract summary + output JSON schema + CLI usage):

1. **Module-level constants:**
   - `ROUTE_NAME = "shot_analysis"` (literal — used in cache path + _cache_key)
   - `ROUTE_VERSION = "feat-shot-analysis-route-v1"` (literal — bump invalidates ALL caches; documented as the cache-invalidation knob)
   - `PROMPTS_SCHEMA = Path(__file__).resolve().parent.parent / "spec" / "schemas" / "prompts.schema.json"` (absolute path to schema for pre-write validation)

2. **`video_content_hash(video_path: str) -> str`** (CINEMA-04): per CONTEXT line 33-34 + RESEARCH Pattern 1. `hashlib.sha256` of `head_1MB + tail_1MB + str(size).encode()`. Read first 1024*1024 bytes; if size > 2*1024*1024, seek to -1024*1024 from end and read tail 1024*1024. Update with `str(size).encode()`. Return `hexdigest()[:16]` (16 chars — collision-safe enough for cache invalidation).

3. **`compose_facets(route_shot: dict | None) -> dict`** (CINEMA-01 — the mapping): per CONTEXT line 19-27 (LOCKED). If `route_shot` is None → return all 6 facets as `""` (graceful-degrade foundation). Otherwise extract `sem = route_shot.get("semantic") or {}` and `geo = route_shot.get("geometry") or {}` (the `or {}` coerces None → empty dict). Define inner `join(*parts)` that returns `", ".join(str(p) for p in parts if p)` (the `if p` filter is the Pitfall 3 fix — drops None/""/0 before join, prevents "None" literal + leading ", "). Return the dict with:
   - `camera`: `join(sem.get("shot_scale"), sem.get("camera_primitive"), sem.get("camera_speed"), geo.get("primitive"))`
   - `action`: `sem.get("subject_motion") or ""` (Pitfall 3 — None coerced to "")
   - `lighting`: `sem.get("lighting") or ""`
   - `style`: `sem.get("lens_feel") or ""`
   - `subject`: `""` (CONTEXT D-XX — never fabricated; Phase 7 re-id handles identity)
   - `scene`: `""` (CONTEXT D-XX — never fabricated; future Qwen-VL extension)

4. **`call_route(client: httpx.Client, body: dict) -> tuple[dict | None, str | None]`** (CINEMA-01 route call + CINEMA-03 degrade): per RESEARCH Pattern 2. Try `resp = client.post("/api/v1/production/shot-analysis", json=body)`; `resp.raise_for_status()`; parse `payload = resp.json()`; if `payload.get("code") != 200` return `(None, f"route code={payload.get('code')}: {payload.get('message')}")`; extract `shots = payload.get("data", {}).get("shots", [])`; if empty return `(None, f"route returned 0 shots for shot_id_range={body.get('shot_id_range')}")`; else return `(shots[0], None)` (per-shot call always takes `[0]`). Catch `httpx.HTTPError` (root — covers ConnectError/TimeoutException/HTTPStatusError/NetworkError per in-env introspection) and return `(None, f"{type(e).__name__}: {e}")`. NO retry (per-shot failure non-fatal — CONTEXT D-XX).

5. **`preflight(base_url: str, timeout: float = 5.0) -> tuple[bool, str | None]`** (CINEMA-05): single short probe before the loop. Build a probe `httpx.Client(base_url=base_url.rsplit("/api/v1", 1)[0], timeout=httpx.Timeout(connect=5.0, read=5.0, write=5.0, pool=5.0))`; `probe.get("/api/v1/production/shot-analysis", timeout=timeout)`. ANY HTTP response (even 404/405 — route only defines POST /) counts as "up" (Assumption A3); only `httpx.HTTPError` (ConnectError etc.) → `(False, f"preflight route unreachable: {type(e).__name__}: {e}")`. Pitfall 7 prevention: this runs ONCE, result drives `route_down` flag short-circuiting remaining shots.

6. **`main()`** (the CLI stage — mirror audio/transcribe.py shape): argparse with `--video` (required), `--shots` (required), `--work-dir` (required), `--output` (required — prompts.json path), `--analysis-url` (default `http://127.0.0.1:8000/api/v1/production/shot-analysis`), `--analysis-timeout` (type=float, default 960.0), `--offline` (store_true). Chinese `help=` strings throughout.

   Step sequence:
   1. Load `shots_meta = json.load(open(args.shots, encoding="utf-8"))`; `vch = video_content_hash(args.video)`.
   2. `cache_dir = os.path.join(args.work_dir, "route_cache", ROUTE_NAME)`; `os.makedirs(cache_dir, exist_ok=True)`; `warnings_sidecar = os.path.join(args.work_dir, "route_cache", "warnings.json")`; `warnings = []`; `prompts = []`.
   3. Preflight: `route_down = args.offline`; if NOT offline, `(ok, msg) = preflight(args.analysis_url)`; if not ok: `route_down = True; warnings.append(msg); print(f"[semantic] preflight failed → route_down mode: {msg}")`.
   4. Open `httpx.Client(base_url=args.analysis_url, timeout=httpx.Timeout(connect=5.0, read=args.analysis_timeout, write=5.0, pool=5.0))` as `client` (the long-running client — read timeout is `--analysis-timeout`, deliberately > route-side 900s).
   5. Loop `for s in shots_meta:` per shot:
      - `sid = s["id"]`; `cache_file = os.path.join(cache_dir, f"shot_{sid:03d}.json")`.
      - Cache lookup: if file exists, `cached = json.load(...)`; check `cached.get("_cache_key", {}).get("video_content_hash") == vch AND cached.get("_cache_key", {}).get("route_version") == ROUTE_VERSION` → cache hit → `route_shot = cached`; print `[semantic] shot N: cache hit`. Else stale → `route_shot = None` (falls through to network/degrade; Pitfall 5 — stale cache doesn't poison).
      - Cache miss + NOT route_down: build body `{"video": os.path.abspath(args.video), "shots": os.path.abspath(args.shots), "shot_id_range": [sid, sid], "semantic": True, "subject": False, "grid_n": 20, "fps": 24}` (CONTEXT lock: semantic=True MUST, subject=False MUST). Call `(route_shot, err) = call_route(client, body)`. If err: `warnings.append(f"shot {sid}: {err}")`, `print(f"[semantic] shot {sid}: FAIL {err}")`, route_shot stays None. Else: write cache file `json.dump({**route_shot, "_cache_key": {"video_content_hash": vch, "route_name": ROUTE_NAME, "route_version": ROUTE_VERSION}}, f, ensure_ascii=False, indent=2)`.
      - Cache miss + route_down: per Pitfall 4 — append `warnings.append(f"shot {sid}: offline/cache-miss → empty facets")` so the user knows this shot wasn't filled.
      - Compose `facets = compose_facets(route_shot)` (None route_shot → all empty strings).
      - Build prompts entry: `{shot_id: sid, start_sec: s["start_sec"], end_sec: s["end_sec"], duration: s["duration"], subject: facets["subject"], action: facets["action"], camera: facets["camera"], scene: facets["scene"], lighting: facets["lighting"], style: facets["style"], prompt_text: ""}`. `prompt_text` left empty (Phase 8 owns recomposition — CONTEXT D-XX).
   6. **Pre-write schema self-validation** (Pitfall: "prompts.json 写后不 schema 自校验"): load `prompts_schema = json.load(open(PROMPTS_SCHEMA, encoding="utf-8"))`; `errors = list(Draft202012Validator(prompts_schema).iter_errors(prompts))`; if errors: `sys.exit(f"prompts.json schema validation failed ({len(errors)} errors): " + "; ".join(f"{'/'.join(map(str,e.absolute_path))}: {e.message}" for e in errors[:3]))` (fails loud — project convention).
   7. **Atomic write** prompts.json: `tmp = args.output + ".tmp"`; `json.dump(prompts, open(tmp, "w", encoding="utf-8"), ensure_ascii=False, indent=2)`; `os.replace(tmp, args.output)` (atomic — partial-write protection).
   8. **Write warnings sidecar** (Plan 01 contract — `{"warnings": [...]}`): `json.dump({"warnings": warnings}, open(warnings_sidecar, "w", encoding="utf-8"), ensure_ascii=False, indent=2)`.
   9. Print `[semantic] wrote {args.output} ({len(prompts)} shots, {len(warnings)} warnings)`; return 0.

   Lazy-import `httpx` INSIDE the functions that use it (`call_route`, `preflight`, and the client-open in main) — mirrors audio/transcribe.py's lazy import pattern; lets the script import cleanly even if httpx is somehow absent (the argparse + cache-read paths still work for `--offline` mode). Lazy-import `jsonschema.Draft202012Validator` inside the pre-write validation block.

   Add a short `if __name__ == "__main__": sys.exit(main())` at the bottom.

   The `tdd="true"` behavior block above is the contract — write the script to satisfy it. Inline assertions during development (a `python3 -c "..."` one-liner per behavior row) are the test loop; the formal regression harness lives in Plan 03 Task 2 (`scripts/verify_phase6_smoke.py`).

**README.md touch:** append `httpx` to the install command line (~line 160: `pip install scenedetect demucs faster-whisper pillow numpy opencv-python`) → add ` httpx` at the end. Note in a brief comment that Phase 6 uses httpx 0.28.1 (in-env; no install needed for current dev).
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && python3 -c "
import json, sys, tempfile, os
sys.path.insert(0, '.')
from analysis.call_shot_analysis import (compose_facets, video_content_hash,
                                          ROUTE_NAME, ROUTE_VERSION, call_route, preflight)
# 1) mapping correctness on shot_003
import pathlib
fix3 = json.load(open('/mnt/agents/output/gpu1/shot_analysis/shot_003.json' if os.path.exists('/mnt/agents/output/gpu1/shot_analysis/shot_003.json') else 'examples/shot_analysis/shot_003.json', encoding='utf-8'))
f = compose_facets(fix3)
assert f['camera'] == '中景, follow, fast, pan_right', f['camera']
assert f['action'] == '飞虫持刀向前飞行', f['action']
assert f['lighting'] == '雾气弥漫'
assert f['style'] == 'normal'
assert f['subject'] == '' and f['scene'] == ''
# 2) null/empty boundaries
fix2 = json.load(open('/mnt/agents/output/gpu1/shot_analysis/shot_002.json' if os.path.exists('/mnt/agents/output/gpu1/shot_analysis/shot_002.json') else 'examples/shot_analysis/shot_002.json', encoding='utf-8'))
assert 'None' not in compose_facets(fix2)['camera'], 'shot_002 leaked None literal'
fix5 = json.load(open('/mnt/agents/output/gpu1/shot_analysis/shot_005.json' if os.path.exists('/mnt/agents/output/gpu1/shot_analysis/shot_005.json') else 'examples/shot_analysis/shot_005.json', encoding='utf-8'))
assert compose_facets(fix5)['action'] == '', 'shot_005 action not empty'
# 3) None route_shot → all empty
empty = compose_facets(None)
assert all(v == '' for v in empty.values()), 'None route_shot did not degrade to empty'
# 4) video_content_hash determinism + content-sensitivity
with tempfile.NamedTemporaryFile(delete=False) as t:
    t.write(b'X' * (3 * 1024 * 1024)); v1 = t.name
h1 = video_content_hash(v1); h2 = video_content_hash(v1)
assert h1 == h2 and len(h1) == 16, 'hash not deterministic or wrong length'
with open(v1, 'r+b') as f: f.seek(-1, 2); f.write(b'Y')
assert video_content_hash(v1) != h1, 'hash not content-sensitive'
os.unlink(v1)
# 5) ROUTE_VERSION literal
assert ROUTE_VERSION == 'feat-shot-analysis-route-v1'
assert ROUTE_NAME == 'shot_analysis'
print('OK: Task 1 behavior contracts green')
"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "from analysis.call_shot_analysis import compose_facets, video_content_hash, call_route, preflight, ROUTE_VERSION, ROUTE_NAME; assert ROUTE_VERSION=='feat-shot-analysis-route-v1'; assert ROUTE_NAME=='shot_analysis'"` exits 0
    - `python3 -c "from analysis.call_shot_analysis import compose_facets; import json; f=json.load(open('examples/shot_analysis/shot_003.json')); r=compose_facets(f); assert r['camera']=='中景, follow, fast, pan_right'; assert r['action']=='飞虫持刀向前飞行'; assert r['subject']==''"` exits 0 (after Task 2 copies fixtures; or use /mnt/agents/ path before then)
    - `grep -c 'ROUTE_VERSION' analysis/call_shot_analysis.py` ≥ 2 (constant defined + used in cache key check)
    - `grep -c 'semantic.*True\|subject.*False' analysis/call_shot_analysis.py` ≥ 1 (CONTEXT D-XX lock honored in POST body)
    - `grep -c 'shot_id_range' analysis/call_shot_analysis.py` ≥ 1 (per-shot isolation — Pitfall 6 prevention)
    - `grep -c 'httpx.HTTPError' analysis/call_shot_analysis.py` ≥ 2 (call_route + preflight catch — Pitfall 2 graceful-degrade)
    - `grep -c 'Draft202012Validator' analysis/call_shot_analysis.py` ≥ 1 (pre-write self-validation)
    - `grep -c 'httpx' README.md` ≥ 1 (install line extended)
    - Module docstring present (`head -1 analysis/call_shot_analysis.py` is `#!/usr/bin/env python3` AND lines 2-20 contain a triple-quoted Chinese docstring)
  </acceptance_criteria>
  <done>
analysis/__init__.py exists (empty marker); analysis/call_shot_analysis.py implements compose_facets (verified against shot_003 + null/empty boundaries), video_content_hash (deterministic + content-sensitive), call_route (HTTPError catch), preflight (single short-circuit), and main (preflight → cache → per-shot POST → compose → schema-validate → atomic write → warnings sidecar); all 10 behavior rows pass via inline checks; README install line carries httpx.
  </done>
</task>

<task type="auto">
  <name>Task 2: Copy 7 captured fixtures into examples/shot_analysis/ + verify mapping produces 0 schema errors</name>
  <files>examples/shot_analysis/shot_001.json, examples/shot_analysis/shot_002.json, examples/shot_analysis/shot_003.json, examples/shot_analysis/shot_004.json, examples/shot_analysis/shot_005.json, examples/shot_analysis/shot_006.json, examples/shot_analysis/shot_007.json</files>
  <read_first>
    - /mnt/agents/output/gpu1/shot_analysis/ (the 7 captured route outputs — copy verbatim, do NOT edit content)
    - examples/ (existing examples dir convention — `examples/ep01_shots.json` etc.)
    - analysis/call_shot_analysis.py (Task 1 — the script that consumes these fixtures)
    - .planning/phases/06-cinematography-auto-fill-step-semantic/06-RESEARCH.md (§Code Examples → Example 3 "映射对 7 个真实 captured fixtures 的实测输出" — the expected camera/action/lighting/style table)
  </read_first>
  <action>
**Copy the 7 captured fixtures verbatim** from `/mnt/agents/output/gpu1/shot_analysis/shot_00{1..7}.json` to `/data/workspace/kais-shot-timeline/examples/shot_analysis/shot_00{1..7}.json`. Use `cp -p` (preserve mtime) or `cp` (the files are JSON; mtime is not load-bearing). Do NOT edit the content — these are the offline mapping oracle, their byte content is the correctness contract (RESEARCH Validation Architecture row 1).

The examples/ dir is the project's existing convention for repo-local sample data (CLAUDE.md mentions `examples/ep01_shots.json` + `examples/xiaojianghu_ep01_shots_v2.json`). The new `examples/shot_analysis/` subdir follows the same pattern.

After copying, run an inline check that proves the mapping + schema-validity round-trip closes for ALL 7 fixtures (this is the CINEMA-01 mapping correctness oracle — RESEARCH §Validation Architecture row 1+2 + row 3):
- Load each fixture
- Build a synthetic shots.json entry `{id: N, start_sec: 0.0, end_sec: 1.0, duration: 1.0}` (just to satisfy the prompts required fields)
- `compose_facets(fixture)` → attach to a prompts entry
- Collect all 7 prompts entries into a list
- Run `Draft202012Validator(prompts.schema.json).iter_errors(prompts_list)`
- Assert 0 errors

Also assert the expected mapping table from RESEARCH Example 3 for at least shot_003 (camera="中景, follow, fast, pan_right"; action="飞虫持刀向前飞行"; lighting="雾气弥漫"; style="normal") and spot-check shot_005 (action="" due to empty subject_motion).
  </action>
  <verify>
    <automated>cd /data/workspace/kais-shot-timeline && ls examples/shot_analysis/ && python3 -c "
import json, sys, glob
sys.path.insert(0, '.')
from analysis.call_shot_analysis import compose_facets
from jsonschema import Draft202012Validator
schema = json.load(open('spec/schemas/prompts.schema.json', encoding='utf-8'))
prompts_list = []
for path in sorted(glob.glob('examples/shot_analysis/shot_*.json')):
    fix = json.load(open(path, encoding='utf-8'))
    facets = compose_facets(fix)
    sid = int(path.rsplit('_', 1)[1].split('.')[0])  # shot_00X.json → X
    prompts_list.append({
        'shot_id': sid, 'start_sec': 0.0, 'end_sec': 1.0, 'duration': 1.0,
        'subject': facets['subject'], 'action': facets['action'],
        'camera': facets['camera'], 'scene': facets['scene'],
        'lighting': facets['lighting'], 'style': facets['style'],
        'prompt_text': '',
    })
errs = list(Draft202012Validator(schema).iter_errors(prompts_list))
assert not errs, f'{len(errs)} schema errors; first: {errs[0].message}'
assert len(prompts_list) == 7, f'expected 7 fixtures, got {len(prompts_list)}'
# spot-check the oracle values
shot3 = next(p for p in prompts_list if p['shot_id'] == 3)
assert shot3['camera'] == '中景, follow, fast, pan_right', shot3['camera']
assert shot3['action'] == '飞虫持刀向前飞行', shot3['action']
shot5 = next(p for p in prompts_list if p['shot_id'] == 5)
assert shot5['action'] == '', shot5['action']
print(f'OK: 7 fixtures mapped → 0 schema errors; oracle values verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - `ls examples/shot_analysis/` shows shot_001.json through shot_007.json (7 files)
    - `diff -q /mnt/agents/output/gpu1/shot_analysis/shot_003.json examples/shot_analysis/shot_003.json` reports identical (verbatim copy)
    - `python3 -c "import json; [json.load(open(f'examples/shot_analysis/shot_00{i}.json')) for i in range(1,8)]"` exits 0 (all 7 valid JSON)
    - The inline mapping check (above automated block) prints "OK: 7 fixtures mapped → 0 schema errors" — proves the CINEMA-01 mapping correctness oracle against ALL 7 captured outputs
    - `grep -rE 'shot_analysis' /data/workspace/kais-shot-timeline/examples/ 2>/dev/null | wc -l` ≥ 7 (all fixtures present)
  </acceptance_criteria>
  <done>
7 captured route fixtures copied verbatim into examples/shot_analysis/; compose_facets applied to all 7 yields schema-valid prompts entries (0 Draft202012Validator errors); shot_003 oracle values verified; shot_005 empty-subject_motion boundary verified. The mapping correctness oracle is now repo-local (survives without /mnt/agents/).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline→External route | shot-timeline (operator-run CLI) calls kais-aigc-platform `POST /api/v1/production/shot-analysis` over localhost HTTP. First network dependency in the project. |
| Route response→Filesystem | Route response (per-shot JSON) is written to `route_cache/shot_analysis/shot_XXX.json` and read back as cache. Trust assumed (route is operator-controlled, same trust level as ffmpeg/Demucs subprocesses). |
| Filesystem→Producer | `route_cache/warnings.json` sidecar (this plan writes; Plan 01's export_asset reads) lives in operator-controlled `work_dir`. |
| `--analysis-url` flag→httpx | CLI flag controls the route URL; only the operator sets it (no untrusted user input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering / SSRF | `--analysis-url` could point at arbitrary host | mitigate | `--analysis-url` is a developer CLI flag, NOT runtime user input. httpx validates URL syntax (`InvalidURL` on parse fail). Default `http://127.0.0.1:8000/...` (localhost). CONTEXT D-XX documents this is operator-controlled. No allowlist enforcement (YAGNI — single-user CLI; user owns the URL they pass). |
| T-06-06 | Denial of Service | Route down hangs pipeline (per-shot 960s × N shots) | mitigate | Preflight short-circuits the loop on first ConnectError (Pitfall 7 — no per-shot retry storm). `--analysis-timeout` default 960s is the upper bound per call. `--skip-semantic` (Plan 03) is the escape hatch. Per-shot failure is non-fatal (CONTEXT D-XX) — one bad shot doesn't sink the asset. |
| T-06-07 | Tampering | Cache poisoning (stale or malicious route_cache file) | mitigate | `_cache_key` embedded in each cache file checks `(video_content_hash, route_version)`. Mismatch → treated as miss (Pitfall 5). `video_content_hash` is sha256 of head+tail 1MB + size — collision-resistant for cache invalidation. Cache lives in `work_dir` filesystem trust boundary. |
| T-06-08 | Information Disclosure | Secrets leaking via warnings | mitigate | Warning strings carry ONLY: exception class name + message (e.g. `"ConnectError: [Errno 111] Connection refused"`) OR route status `code=N: message`. NO request body, NO auth headers, NO video path (path is in the request, not the warning). Plan 01 SPEC §3 row documents this. |
| T-06-09 | Spoofing | Route returns 200 with malformed body | mitigate | `call_route` checks `payload.get("code") == 200` AND `data.shots` non-empty; both must hold else degrade. `compose_facets` uses `.get() or {}` for ALL route fields — malformed/missing → empty facet (schema-valid). Belt-and-suspenders. |
| T-06-10 | Repudiation | Silent failure (cache miss + offline → empty prompts without warning) | mitigate | Per Pitfall 4 — `--offline` cache miss appends explicit warning `"shot N: offline/cache-miss → empty facets"` so the user knows that shot wasn't filled (no silent masking). |
| T-06-SC | Tampering | httpx package legitimacy | mitigate (conditional) | httpx 0.28.1 already in env (verified: Author Tom Christie, BSD-3-Clause, downstream dependents openai/diffusers/mcp/huggingface_hub). NO `pip install` task in this plan. `user_setup` precondition in frontmatter documents: if fresh env requires install, human MUST verify pypi.org/project/httpx legitimacy first (research's slopcheck was network-unreachable → `[ASSUMED]`). |
</threat_model>

<verification>
After both tasks complete:
1. `python3 -c "from analysis.call_shot_analysis import compose_facets, video_content_hash, call_route, preflight, main, ROUTE_VERSION, ROUTE_NAME"` exits 0 (clean import).
2. The Task 1 + Task 2 inline automated blocks both pass (mapping oracle + 7-fixture schema-validity).
3. `python3 spec/validate.py` still exits 0 (Plan 02 adds no schema changes — minimal + v1.1 fixtures stay green).
4. The route-down + cache-hit + --skip-semantic scenarios are exercised end-to-end in Plan 03 Task 2 (`scripts/verify_phase6_smoke.py`) — they are NOT run here because Plan 02 does not own the integration surface.
5. `grep -c 'httpx' README.md` ≥ 1 (install line extended).
</verification>

<success_criteria>
- `analysis/call_shot_analysis.py` exists with module docstring, lazy-imported httpx, ROUTE_NAME/ROUTE_VERSION constants, video_content_hash, compose_facets (LOCKED mapping per CONTEXT D-XX), call_route (HTTPError catch), preflight (single probe), main (per-shot cache + write prompts + write warnings sidecar + schema self-validate).
- All 10 behavior rows green via inline python checks.
- 7 captured fixtures copied verbatim into examples/shot_analysis/; the CINEMA-01 mapping oracle (compose_facets over all 7 → 0 schema errors) is repo-local and reproducible without /mnt/agents/.
- README install line carries httpx.
- CINEMA-01 (mapping + route call), CINEMA-03 (graceful-degrade logic), CINEMA-04 (cache + content-hash key), CINEMA-05 (preflight + sidecar write) are implemented at the component level; CINEMA-02 (run_pipeline integration) + CINEMA-06 (CLI flags surfaced in run_pipeline) close in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/06-cinematography-auto-fill-step-semantic/06-02-SUMMARY.md` when done
</output>
