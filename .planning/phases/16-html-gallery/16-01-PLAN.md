---
phase: 16-html-gallery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - html/gen_timeline_html.py
  - run_pipeline.py
  - scripts/verify_phase16_smoke.py
autonomous: true
requirements: [PRESENT-01]
tags: [html-gallery, v1.2-audio-semantic, dialogue-chip, music-chip, sfx-chip, speaker-to-character-chip, reproduction-panel, af-01-estimated-label, mus-04-omitted, xss-esc-hardening, graceful-omit-byte-identical, github-dark-palette, chinese-ui]

must_haves:
  truths:
    - "html/gen_timeline_html.py 仍是单一文件 monolithic HTML 生成器（CLAUDE.md god-script 反模式例外：本仓库约定 html/gen_*.py 都是 monolithic f-string template）。Phase 16 是 ADDITIVE 扩展 —— 不重写既有 timeline.html 渲染，只新增 v1.2 chips + reproduction panel + 加固新 attack surface"
    - "新增两个 CLI flag: `--audio-semantic <path>` + `--speakers <path>`（mirror Phase 8 `--prompts` / `--characters` flag 风格）；缺省（graceful-omit）→ byte-identical to v1.1 timeline.html（SC#4 graceful-degrade proof）"
    - "Per-shot 对白 chip (DIA-04 emotion nullable+confidence + 文本摘录): speaker label + 可选 emotion badge (emotion 为 null/emo_unk/缺席 → NO badge；NOT 'unknown') + dialogue.text 截断 200 字符显示。空 dialogue → 整 chip 省略"
    - "Per-shot 音乐 chip (MUS-02/03/05/06): 仅当 reproduction.music_gen 非 null（隐含 MUS-01 BGM 存在）→ 渲染 chip 显示 '🎵 音乐 (estimated)' + music_gen.text 摘录（tempo/mood/key/VA 已 embedded in NL text）。**MUS-04 instruments 字段 NEVER 渲染**（Phase 10 LOCKED defer v1.3；grep '\\binstruments\\b' 在 html/ 必须 0 匹配）"
    - "Per-shot SFX chip (SFX-01): 仅当 sfx.description 非空 OR sfx.events 非空 → 渲染 chip 显示 '🔊 音效' + SenseVoice 8-event tags (Speech/BGM/Applause/Laughter/Cry/Sneeze/Breath/Cough) + sfx.description 文本摘录。空 sfx → 整 chip 省略"
    - "Speaker→character chip (SPEAKER-01 + Phase 8 PRESENT-02 mirror): 当 dialogue.spk_id 解析到 speakers.json#speakers[].spk_id 且 speakers[i].char_id 非 null → 渲染 `<a href=\"#gallery-{char_id}\">🧑 {char_name}</a>` ref-chip，mirror Phase 8 PRESENT-02 char-chip 模式；char_id=null（旁白/群杂）→ 仅渲染 speaker label '🎤 spk_NNN'，no chip"
    - "Reproduction panel (AF-01): 3 个 reproduction strings (tts/music_gen/foley) 各自有可见的 'estimated' 文字标签（mirror SPEC §10.1 prose：「estimated」 prefix 是 Phase 16 PRESENT-01 MUST）；每个非 null 字段都显示 text + confidence + fidelity_disclaimer。Null 字段 gracefully omitted（NOT 'N/A'）。面板 default collapsed（CONTEXT.md discretion: default collapsed to avoid clutter）"
    - "XSS _esc() hardening (T-16-01 critical security gate): EVERY route-derived 字符串（dialogue.text / sfx.description / reproduction.{tts,music_gen,foley}.text/fidelity_disclaimer / speaker→character names）通过 Python _esc() (gen_timeline_html.py:24-41 已有) OR JS textContent/createTextNode OR JSON-in-script .replace(\"</\", \"<\\\\/\") 中和；NO innerHTML with route-derived content（DOM text APIs only）"
    - "XSS test payloads ALL neutralized (T-16-01 verify): `</script>`, `<script>alert`, `\"onerror=\"`, base64 `data:text/html`, `</textarea>` 全部不可执行 (raw breakout sequences absent from generated HTML)"
    - "run_pipeline.py step_timeline cmd 加 `--audio-semantic` + `--speakers` 透传（mirror Phase 8 `--prompts` / `--characters` 透传 at run_pipeline.py:489-496）；Phase 14 cache inputs 已就绪（run_pipeline.py:461-464）— Phase 16 仅加 cmd argv"
    - "scripts/verify_phase16_smoke.py 新增（mirror scripts/verify_phase8_smoke.py:490-633 scenario_html_xss_inert）—— 6-sink × multi-payload XSS matrix: dialogue.text / sfx.description / reproduction.tts.text / reproduction.music_gen.text / reproduction.foley.text / speakers.char_id (via char name lookup)；任一存活 = fail"
    - "Graceful-omit byte-identical proof: 在无 --audio-semantic AND 无 --speakers flags 时，gen_timeline_html.py 输出与 v1.1 byte-identical（diff 唯一允许差异是新增 const bootstraps 的 empty array，例如 `const AUDIO_SEMANTIC = [];`）—— 实际上设计要求完全 byte-identical，通过条件 emit（only when audio_semantic_json 或 speakers_json loaded）实现"
    - "Chinese UI (CLAUDE.md): 所有 chip labels / panel headers / estimated 前缀中文（如 '估算' / 'estimated（估算）' / '复现 prompt 面板'）；GitHub-dark 调色板（#0d1117/#161b22/#30363d/#58a6ff/#3fb950/#d29922/#f85149）"
  artifacts:
    - path: "html/gen_timeline_html.py"
      provides: "Extended timeline.html generator — 新增 --audio-semantic / --speakers CLI flags + per-shot dialogue/music/sfx chips + speaker→character ref-chip + collapsible reproduction panel ('estimated' labels) + _esc() hardening on every route-derived string + JSON-in-script .replace defense. Graceful-omit when both flags absent (byte-identical to v1.1 timeline.html). MUS-04 instruments NEVER rendered."
      min_lines: 1700
      contains: ["--audio-semantic", "--speakers", "estimated", "AUDIO_SEMANTIC", "SPEAKERS", "dialogue-chip", "music-chip", "sfx-chip", "reproduction-panel", "AF-01", "T-16-01"]
    - path: "run_pipeline.py"
      provides: "step_timeline cmd argv 增 --audio-semantic + --speakers flag pass-through (mirror --prompts at run_pipeline.py:489-496). Phase 14 cache inputs 已就绪 (run_pipeline.py:461-464); Phase 16 only adds CLI argv."
      contains: ["--audio-semantic", "--speakers", "audio_semantic_json", "speakers_json"]
    - path: "scripts/verify_phase16_smoke.py"
      provides: "Phase 16 graceful-omit byte-identical proof + 6-sink XSS payload matrix (dialogue.text / sfx.description / reproduction.{tts,music_gen,foley}.text / speaker→char name lookup) + chip-render grep assertions + 'estimated' label grep + MUS-04 instruments-absent grep. Mirrors scripts/verify_phase8_smoke.py scenario_html_xss_inert:490-633 structure."
      min_lines: 350
      contains: ["scenario_html_xss_inert_v12", "scenario_chips_rendered", "scenario_reproduction_estimated_labels", "scenario_mus_04_omitted", "scenario_graceful_omit_byte_identical"]
  key_links:
    - from: "html/gen_timeline_html.py:build_shots_js (lines 44-92)"
      to: "spec/fixtures/v1.2/audio_semantic.json (shots[].dialogue/sfx/reproduction) + spec/fixtures/v1.2/speakers.json (speakers[].spk_id/char_id)"
      via: "新增 audio_semantic_by_id + speakers_by_id 入参 (mirror frames_by_id / audio_by_id pattern at build_shots_js:44-92); 每个 js_shot 增 dialogue_chip / music_chip / sfx_chip / speaker_chip / reproduction 字段 (omitted when source absent → graceful-degrade)"
      pattern: "audio_semantic_by_id|speakers_by_id|dialogue_chip|music_chip|sfx_chip|speaker_chip|reproduction_panel"
    - from: "html/gen_timeline_html.py main() argparse + step_timeline cmd (run_pipeline.py:470-503)"
      to: "audio_semantic.json + speakers.json (per-asset files)"
      via: "新增 --audio-semantic/--speakers CLI flags (mirror --prompts/--characters at gen_timeline_html.py:1188-1196); loader graceful-degrades None when flag absent; step_timeline cmd 加 2 行 pass-through (audio_semantic_json + speakers_json)"
      pattern: "audio.semantic|audio_semantic|--speakers|speakers_json"
    - from: "html/gen_timeline_html.py 新 CSS classes (.dialogue-chip / .music-chip / .sfx-chip / .speaker-chip / .reproduction-panel / .repro-field.estimated)"
      to: "CLAUDE.md GitHub-dark palette + Phase 8 chip patterns (.ref-chip / .char-chip / .prop-chip / .fill-chip at gen_timeline_html.py:289-298)"
      via: "CSS additive (no removal); reuse existing color tokens; chip layout mirror Phase 8 inline-block 9px padding-0-4px radius-2px"
      pattern: "dialogue-chip|music-chip|sfx-chip|speaker-chip|reproduction-panel|estimated"

objective: >
  Extend html/gen_timeline_html.py (the 1294-line canonical timeline.html generator)
  ADDITIVELY to surface v1.2 audio semantics end-user-visible: per-shot dialogue/music/sfx
  chips + speaker→character chip + reproduction panel with VISIBLE "estimated" labels on
  every field (AF-01 fidelity-ceiling mitigation). Wire the new --audio-semantic /
  --speakers CLI flags through step_timeline (Phase 14 cache inputs already wired).
  Harden EVERY new route-derived string against XSS (T-16-01 critical security gate) —
  mirror Phase 8 PRESENT patterns + Phase 7 CR-04 fix 336d04f + Phase 8 carry-over +
  Phase 13 gen_speaker_review.py. Honor Phase 10 LOCKED CONDITIONAL gating: MUS-04
  instruments field NEVER rendered (deferred v1.3). Honor DIA-04 ship-nullable+confidence
  (null emotion → no badge). Honor graceful-omit (byte-identical to v1.1 timeline when
  both flags absent). Chinese UI per CLAUDE.md; GitHub-dark palette; mirror PRESENT-01/02/03
  chip patterns.

output: Three files modified (html/gen_timeline_html.py ~+400 net additive; run_pipeline.py
~+8 net wiring; scripts/verify_phase16_smoke.py ~+350 new). Four atomic commits (one per
task) + final docs commit.

execution_context:
  @$HOME/.claude/get-shit-done/workflows/execute-plan.md
  @$HOME/.claude/get-shit-done/templates/summary.md

context:
  @.planning/PROJECT.md
  @.planning/ROADMAP.md
  @.planning/STATE.md
  @.planning/phases/16-html-gallery/16-CONTEXT.md
  @CLAUDE.md
  @html/gen_timeline_html.py
  @html/gen_speaker_review.py
  @html/gen_registry_review.py
  @run_pipeline.py
  @spec/schemas/audio_semantic.schema.json
  @spec/schemas/speakers.schema.json
  @spec/fixtures/v1.2/audio_semantic.json
  @spec/fixtures/v1.2/speakers.json
  @spec/SPEC.md
  @scripts/verify_phase8_smoke.py

threat_model:
  threat: T-16
  enforcement: security_enforcement
  scope_files: [html/gen_timeline_html.py, run_pipeline.py, scripts/verify_phase16_smoke.py]
  dispositions:
    - id: T-16-01
      category: XSS-injection
      risk: "Route-derived NL strings (dialogue.text / sfx.description / reproduction.{tts,music_gen,foley}.text+ fidelity_disclaimer / resolved character names from speakers.json) flow into HTML body, attribute, and JSON-in-<script> sinks. Without _esc() + JSON-in-script .replace defense, a malicious or malformed route response (e.g. dialogue.text containing `</script><script>alert(1)</script>`) would break out of the inline JSON block and execute arbitrary JS in the reviewer's browser. This is a NEW ATTACK SURFACE in Phase 16 — Phase 8 chips only rendered short operator-influenced IDs/names; Phase 16 chips render long-form route-generated NL prose, materially expanding sink count and payload room. Mirror Phase 7 CR-04 fix 336d04f + Phase 8 carry-over + Phase 13 gen_speaker_review.py:79-101,340-347 _esc patterns."
      mitigation: |
        Three-layer defense (mirror Phase 8 PRESENT carry):

        **Layer 1 — Python _esc() on every f-string-interpolated route string:**
        Reuse existing `_esc()` (gen_timeline_html.py:24-41). Apply to:
          - dialogue.text excerpt (200-char substring)
          - sfx.description excerpt
          - reproduction.{tts,music_gen,foley}.{text, fidelity_disclaimer}
          - resolved speaker→character name (from CHARACTERS lookup)
          - any emotion label that flows to a Python-rendered HTML sink
        The escape order (& first, then < > \" ') prevents double-escape and
        neutralizes all 5 HTML-significant characters in both body and attribute
        contexts.

        **Layer 2 — JSON-in-<script> .replace("</", "<\\/"):**
        Apply to every new const bootstrap JSON dump (AUDIO_SEMANTIC_JSON,
        SPEAKERS_JSON). The HTML parser sees `</script>` as a terminator
        regardless of JS-string context; .replace deflects breakout payloads
        by turning the first `</` into the JS escape `<\/` (semantically
        identical for round-trip JSON.parse, but the parser no longer sees
        `</script>`).

        **Layer 3 — JS textContent/createTextNode for client-rendered chips:**
        Chips built in JS (buildShotChips at gen_timeline_html.py:394-413) use
        `'<tag>' + _esc(s) + '</tag>'` for direct template-literal composition
        (mirror Phase 8 PRESENT-02 JS _esc at gen_timeline_html.py:384-388).
        No raw `innerHTML = routeString` anywhere; the only innerHTML uses
        involve escaped fragments.

        **Verify (scripts/verify_phase16_smoke.py):** 6-sink × multi-payload
        matrix modeled on Phase 8 scenario_html_xss_inert (verify_phase8_smoke.py:
        490-633). Payloads: `</script><script>alert(1)</script>`, `<img src=x
        onerror=alert(1)>`, `</textarea><script>`, `data:text/html;base64,
        PHNjcmlwdD4=`, `\" onerror=\"alert(1)`, `</div><script>`. Sinks:
        dialogue.text, sfx.description, reproduction.tts.text,
        reproduction.music_gen.text, reproduction.foley.text, speakers-resolved
        character name. Forbidden raw breakout patterns MUST be absent from
        generated HTML; escaped forms MUST be present.
      verify: "scripts/verify_phase16_smoke.py exits 0 (all 5 scenarios GREEN); specifically scenario_html_xss_inert_v12 must confirm zero raw breakout sequences in generated HTML across all 6 sinks × 6 payloads"
    - id: T-16-02
      category: information-disclosure
      risk: "MUS-04 instruments field leaks into rendered HTML (Phase 11 schema $comment lock violation; Phase 10 LOCKED MUS-04 deferred v1.3). Any emitted 'instruments' label/key would falsely claim MIR classifier signal that does not exist (MERT has no classifier head; PANNs Cnn14 zenodo-blocked during spike). User-trust invariant."
      mitigation: "Source never references an instruments/instrument_labels/instruments_detected key in html/gen_timeline_html.py. Music chip displays ONLY: tempo/mood/key/VA embedded in music_gen reproduction text (NL form); never parses out or renders separate 'instruments' field. Task 4 acceptance: case-insensitive grep on html/gen_timeline_html.py for `\\binstruments\\b|instrument_labels|instruments_detected|乐器识别` returns 0 matches (mirror Phase 15-01 MUS-04 grep gate; the term '乐器' (general Chinese for instrument) is allowed in prose but NOT as a chip label/field name)."
      verify: "grep -riE '\\binstruments\\b|instrument_labels|instruments_detected' html/gen_timeline_html.py returns nothing"
    - id: T-16-03
      category: integrity
      risk: "AF-01 violation — reproduction panel omits 'estimated' label on any field, implying source-voice clone / exact restoration to content creators. SPEC §10.1 mandate + SC#3 lock."
      mitigation: "Every non-null reproduction.{tts,music_gen,foley} field rendered with a VISIBLE 'estimated（估算）' prefix/label in the panel HTML. Task 3 acceptance: `grep -c 'estimated\\|估算' html/gen_timeline_html.py` returns >= 3 (one per layer); scenario_reproduction_estimated_labels in verify_phase16_smoke.py asserts the generated HTML has 'estimated' on each rendered layer when audio_semantic.json with non-null reproduction is the input."
      verify: "scripts/verify_phase16_smoke.py:scenario_reproduction_estimated_labels GREEN"
    - id: T-16-04
      category: availability
      risk: "Graceful-omit regression: gen_timeline_html.py with no --audio-semantic AND no --speakers flags produces a different byte-stream than v1.1 baseline (e.g. new empty `const AUDIO_SEMANTIC = [];` line; new empty CSS class definitions; new HTML container divs). This breaks the SC#4 byte-identical proof and breaks v1.0/v1.1 asset backward-compat (byte-different timeline.html on cached v1.0/v1.1 assets triggers cache invalidation cascade)."
      mitigation: "Conditional emit pattern (mirror Phase 8 gallery_html at gen_timeline_html.py:206-208 `if gallery_cards:`). ALL new HTML/CSS/JS fragments gated on (audio_semantic_data is not None OR speakers_data is not None). New const bootstraps only emitted when at least one source loaded. New CSS rules + chip container divs + reproduction panel HTML all inside the same conditional. Task 4 acceptance: scenario_graceful_omit_byte_identical in verify_phase16_smoke.py runs gen_timeline_html.py with ONLY --shots/--characters (Phase 8 baseline) and with --shots/--characters/--audio-semantic/--speakers-pointing-to-nonexistent-paths, then diffs the two HTML outputs — MUST be byte-identical."
      verify: "scripts/verify_phase16_smoke.py:scenario_graceful_omit_byte_identical GREEN (diff returns empty)"

objective: >
  Plan 16-01 closes Phase 16 in a single cohesive plan (4 tasks): CLI flags + JSON loaders
  + bootstrap → per-shot chips (dialogue/music/sfx + speaker→character) + CSS → reproduction
  panel with "estimated" labels → step_timeline wiring + XSS + graceful-omit verification.
  Honors Phase 10 LOCKED CONDITIONAL gating (MUS-04 NEVER rendered; DIA-04 nullable+confidence)
  and v1.2 strict-schema × lenient-consumer (graceful-omit byte-identical). Closes PRESENT-01.

---

# Plan 16-01: HTML Gallery — v1.2 Audio Semantic Chips + Reproduction Panel + XSS Hardening

## Objective Recap

Extend `html/gen_timeline_html.py` (the canonical 1294-line timeline.html generator)
ADDITIVELY to surface v1.2 audio semantics end-user-visible, in a single cohesive plan
with 4 atomic tasks. NO new ML, NO new data — purely HTML rendering of existing Phase 11
schema + Phase 12/13/15 produced data (`audio_semantic.json` + `speakers.json`).

Phase 16 closes PRESENT-01 (the only Phase 16 req ID). SC#1-4 from ROADMAP.md:144-152 map
1:1 onto the 4 tasks below.

## What Success Looks Like

| SC | Verification | Task |
|----|--------------|------|
| SC#1: dialogue/music/sfx chips render; non-present gracefully omitted | scenario_chips_rendered + scenario_mus_04_omitted | Task 2 |
| SC#2: speaker→char_NNN chip links to gallery; unresolved → speaker label alone | scenario_chips_rendered (asserts char-chip anchor + spk-only fallback) | Task 2 |
| SC#3: reproduction panel with VISIBLE 'estimated' on EVERY field | scenario_reproduction_estimated_labels | Task 3 |
| SC#4: XSS _esc() on EVERY route string; test payloads pass | scenario_html_xss_inert_v12 (6-sink × 6-payload matrix) | Task 4 |
| (Implicit SC#5: graceful-omit byte-identical to v1.1 timeline) | scenario_graceful_omit_byte_identical | Task 4 |

---

## Task 1: CLI flags + JSON loaders + build_shots_js extension + JSON-in-script bootstrap

<task type="auto" id="16-01-T1">
<name>CLI flags + JSON loaders + build_shots_js extension + JSON-in-script bootstrap</name>

<read_first>
- html/gen_timeline_html.py:24-41 (_esc() definition — already complete; carry as-is)
- html/gen_timeline_html.py:44-92 (build_shots_js — the function to extend; note how prompts_by_id is plumbed as optional kwarg at line 75)
- html/gen_timeline_html.py:137-180 (build_html signature + JSON-in-script .replace("</", "<\\/") defense at lines 159-163 — mirror for new const bootstraps)
- html/gen_timeline_html.py:1162-1200 (main() argparse — where to add new flags; mirror --prompts/--characters/--props/--asset-json pattern at lines 1188-1199)
- html/gen_timeline_html.py:1216-1256 (loader blocks for prompts/characters/props — mirror for audio_semantic/speakers with graceful-degrade None when file unreadable)
- html/gen_timeline_html.py:1254-1290 (build_shots_js invocation + build_html invocation at line 1277-1287)
- spec/fixtures/v1.2/audio_semantic.json (the data shape — per-shot dialogue/sfx/reproduction)
- spec/fixtures/v1.2/speakers.json (the data shape — speakers[].spk_id/char_id/turns)
- spec/schemas/audio_semantic.schema.json (lenient-consumer: extra fields tolerated, missing fields OK)
- spec/schemas/speakers.schema.json (review_state gating: only 'confirmed' flows downstream)
</read_first>

<actions>
1. **Add two CLI flags** to `html/gen_timeline_html.py` argparse block (after line 1199):
   - `--audio-semantic <path>` (default None) — help string: "v1.2 audio_semantic.json 路径（per-shot 对白/音乐/音效 chips + 复现 prompt 数据源；缺省 → chips 省略 + reproduction panel 省略）"
   - `--speakers <path>` (default None) — help string: "v1.2 speakers.json 路径（spk_NNN → char_NNN 映射数据源；缺省 → speaker→character chip 省略）"
   - Mirror existing flag style: Chinese help strings, kebab-case multi-word, default=None.

2. **Add loader blocks** in main() (after the props_data loader at line 1252), modeled on the existing characters_data loader:
   - `audio_semantic_data = None` initial. If `args.audio_semantic and os.path.exists(args.audio_semantic)`: try open+json.load with `encoding="utf-8"`, except (OSError, json.JSONDecodeError) → print `[warn] audio_semantic.json unreadable, degrading to no chips: {e}` and leave None. Shape: `{"schema_version": "1.2", "shots": [{"shot_id":1, "dialogue":{...}, "sfx":{...}, "reproduction":{...}}, ...]}`.
   - `speakers_data = None` initial. Same pattern. Shape: `{"speakers":[{spk_id, char_id, ...}, ...]}`.
   - **Filter speakers to review_state="confirmed" only** (Pitfall 7 prevention per speakers.schema.json:36 — only confirmed flows downstream). Build a list `confirmed_speakers` of dicts; if no confirmed entries → speakers_data stays None.
   - Build lookup dicts: `audio_semantic_by_id = {s["shot_id"]: s for s in audio_semantic_data.get("shots", []) if "shot_id" in s}` (when loaded); `speakers_by_spk = {s["spk_id"]: s for s in confirmed_speakers}` (when loaded).

3. **Extend `build_shots_js()` signature** (line 44) to accept new optional kwargs (mirror `prompts_by_id` pattern at line 75):
   ```python
   def build_shots_js(shots, frames_by_id, audio_by_id, transcript_segments=None,
                      prompts_by_id=None,
                      audio_semantic_by_id=None, speakers_by_spk=None):
   ```
   For each shot, build new optional fields (omit key when source absent — graceful-degrade):
   - `dialogue_chip` (dict or omit): `{spk_id, emotion, emotion_confidence, text_excerpt}` from `audio_semantic_by_id[sid].dialogue`. `text_excerpt` = first 200 chars of `dialogue.text.strip()`. `spk_id` may be null.
   - `music_chip` (dict or omit): `{text_excerpt}` from `audio_semantic_by_id[sid].reproduction.music_gen` (when non-null; the music_gen NL text already embeds tempo/mood/key/VA per Phase 15 producer). NEVER reference an `instruments` field (T-16-02).
   - `sfx_chip` (dict or omit): `{events, description_excerpt}` from `audio_semantic_by_id[sid].sfx` when events OR description non-empty. `description_excerpt` = first 200 chars.
   - `speaker_chip` (dict or omit): when `dialogue.spk_id` is non-null AND `speakers_by_spk` has it:
     - `{spk_id, char_id}` — char_id may be null (旁白/群杂). When char_id is null, char_name is null too.
     - `char_name` is NOT resolved here (defer to JS — same as Phase 8 `character_refs` pattern at line 88, where JS does `CHARACTERS.find(...)` lookup at click time). Python only passes IDs.
   - `reproduction` (dict or omit): from `audio_semantic_by_id[sid].reproduction`. Pass through `tts`, `music_gen`, `foley` raw (each is null or `{text, confidence, fidelity_disclaimer}`).

4. **Add JSON-in-script const bootstraps** in `build_html()` (after `const PROPS = {props_json};` at line 371):
   - Apply the same `.replace("</", "<\\/")` defense as the existing 5 bootstraps at lines 159-163.
   - **Conditional emit**: only emit `const AUDIO_SEMANTIC = ...` and `const SPEAKERS = ...` lines when their respective data dicts are non-None (None → don't emit the const at all, since the JS code that consumes them will be conditionally emitted too in Task 2).
   - This means: when both args absent, the JS block is byte-identical to v1.1 (graceful-omit invariant T-16-04).

5. **Plumb new kwargs** through `build_html()` signature (line 137-140) and the `build_shots_js` invocation in `main()` (line 1255): pass `audio_semantic_by_id=audio_semantic_by_id, speakers_by_spk=speakers_by_spk`.

6. **Do NOT yet render chips/panel** — that's Task 2/3. This task only plumbs data through.

7. **Verify graceful-omit** at end of task: run `python3 html/gen_timeline_html.py --shots spec/fixtures/v1.2/shots.json --output /tmp/t1.html` (no v1.2 flags) and confirm HTML is byte-identical to v1.1 baseline (diff against a v1.1 run; only difference should be NONE — new consts are conditional). If you see ANY diff beyond what was already there, the conditional emit is wrong.
</actions>

<acceptance_criteria>
- `python3 html/gen_timeline_html.py --help` shows `--audio-semantic` and `--speakers` flags with Chinese help strings.
- `grep -n "audio-semantic\|--speakers" html/gen_timeline_html.py` returns >= 4 matches (2 add_argument + >=2 cmd usage).
- `grep -n "audio_semantic_by_id\|speakers_by_spk" html/gen_timeline_html.py` returns >= 6 matches (function signature + lookup build + invocation).
- `grep -n "AUDIO_SEMANTIC\|SPEAKERS" html/gen_timeline_html.py` returns matches in BOTH the const bootstrap block AND conditional-emit context.
- **Graceful-omit check**: `python3 html/gen_timeline_html.py --shots spec/fixtures/v1.2/shots.json --output /tmp/t1-noflags.html` succeeds; then `python3 html/gen_timeline_html.py --shots spec/fixtures/v1.2/shots.json --audio-semantic /nonexistent.json --speakers /nonexistent.json --output /tmp/t1-empty.html` succeeds (file-not-found → graceful degrade); `diff /tmp/t1-noflags.html /tmp/t1-empty.html` MUST be empty (byte-identical — both branches hit the None path).
- No new files created. No tests added yet (Task 4 adds the smoke test). build_shots_js invocation in main() plumbs new kwargs.
- Commit message: `feat(16-01): add --audio-semantic/--speakers CLI + JSON loaders + build_shots_js extension`
</acceptance_criteria>
</task>

---

## Task 2: Per-shot dialogue/music/sfx chips + speaker→character chip + CSS + JS rendering

<task type="auto" id="16-01-T2">
<name>Per-shot dialogue/music/sfx chips + speaker→character chip + CSS + JS rendering</name>

<read_first>
- html/gen_timeline_html.py:279-298 (Phase 8 PRESENT chip CSS — `.ref-chip` / `.char-chip` / `.prop-chip` / `.fill-chip` / `.fill-filled` / `.fill-degraded` — REUSE this style block, add new classes alongside)
- html/gen_timeline_html.py:384-413 (Phase 8 buildShotChips JS function — the EXACT pattern to extend; note `'<a href="#gallery-' + encodeURIComponent(cid) + '" class="ref-chip char-chip" title="' + _esc(n) + '">' + '🧑 ' + _esc(n) + '</a>'` is the speaker→character chip template — literally reuse with new CSS class `.speaker-char-chip`)
- html/gen_timeline_html.py:519-528 (the initial shot-row build — where chips are injected via `buildShotChips(s)`)
- html/gen_timeline_html.py:971-986 (rebuildAll shot-row build — second chip injection point in toggleMode path)
- spec/fixtures/v1.2/audio_semantic.json (shot 1: dialogue.text="你好世界", spk_id="spk_001", emotion="HAPPY", emotion_confidence=1.0, events=["Speech"], words[4]; sfx={events:["Laughter"], description:"观众轻笑声..."}; shot 2: dialogue with emotion="emo_unk" → no badge)
- spec/fixtures/v1.2/speakers.json (spk_001 → char_001 [resolved], spk_002 → null [unresolved 旁白/群杂])
</read_first>

<actions>
1. **Extend CSS block** (after line 298, BEFORE the `/* ===== Right: vertical timeline ===== */` comment at line 300) with new chip classes:
   ```css
   /* ===== Phase 16: v1.2 audio semantic chips (PRESENT-01 extension) ===== */
   .dlg-chip, .music-chip, .sfx-chip, .spk-chip, .spk-char-chip {{
       display:inline-block; font-size:9px; padding:0 4px; border-radius:2px;
       margin-left:3px; vertical-align:middle; max-width:280px;
       overflow:hidden; text-overflow:ellipsis; white-space:nowrap;
   }}
   .dlg-chip {{ background:#1a3a5e; color:#58a6ff; }}
   .music-chip {{ background:#1a3e1a; color:#3fb950; }}
   .sfx-chip {{ background:#3e351a; color:#d29922; }}
   .spk-chip {{ background:#2a2a3e; color:#8b949e; }}
   .spk-char-chip {{ background:#1a3a5e; color:#58a6ff; text-decoration:none; }}
   .spk-char-chip:hover {{ background:#264f78; }}
   .emo-badge {{ display:inline-block; font-size:8px; padding:0 2px; border-radius:1px;
                 margin-left:2px; background:#3e2a4a; color:#bc8cff; }}
   ```
   All colors drawn from CLAUDE.md GitHub-dark palette.

2. **Extend `buildShotChips(s)` JS function** (gen_timeline_html.py:394-413) to append new chips AFTER the existing charChips + propChips + fillChip. Pattern (mirror existing):
   - **Dialogue chip**: when `s.dialogue_chip` is truthy, render `<span class="dlg-chip" title="{_esc(text_excerpt)}">💬 {_esc(spk_or_'?')}{emotion_badge}{_esc(text_excerpt_truncated)}</span>`. Emotion badge: ONLY when `s.dialogue_chip.emotion` exists AND is NOT 'emo_unk' AND is NOT null/empty → `<span class="emo-badge">{_esc(emotion)}</span>`. spk label: `s.dialogue_chip.spk_id || '?'` (when null/missing → '?'). Truncate text_excerpt to ~30 chars in chip body (full text in `title` tooltip).
   - **Music chip**: when `s.music_chip` is truthy → `<span class="music-chip" title="{_esc(text_excerpt)}">🎵 音乐 (estimated)</span>`. The "(estimated)" suffix is a NON-NEGOTIABLE per-AF-01 micro-label on every music chip (because music_chip text comes from music_gen reproduction, which is AF-01 governed).
   - **SFX chip**: when `s.sfx_chip` is truthy → `<span class="sfx-chip" title="{_esc(description_excerpt)}">🔊 {_esc(events.join('/'))} {_esc(description_excerpt_truncated)}</span>`. When `description_excerpt` empty, omit it; when `events` empty, omit the events portion.
   - **Speaker→character chip**: when `s.speaker_chip` is truthy:
     - If `s.speaker_chip.char_id` is non-null → `<a href="#gallery-{encodeURIComponent(char_id)}" class="spk-char-chip" title="{_esc(resolved_char_name || char_id)}">🧑 {_esc(resolved_char_name || char_id)}</a>` — mirror Phase 8 PRESENT-02 char-chip pattern (lines 395-401). Use `CHARACTERS.find(x => x.id === char_id)` for name resolution.
     - If `s.speaker_chip.char_id` is null → `<span class="spk-chip" title="旁白/群杂 — 未映射到角色">🎤 {_esc(spk_id)}</span>` — speaker label alone (CONTEXT.md SC#2).
   - Concat all new chips to the existing return value (charChips + propChips + fillChip + new chips).

3. **Both shot-row build sites** must use the extended `buildShotChips(s)`:
   - Line 525 (initial render): already calls `buildShotChips(s)` — no change needed if function signature unchanged.
   - Line 979 (rebuildAll): same.
   - If for any reason the function's behavior is gated on data presence, the function should naturally return only relevant chips (each chip type guarded by truthy check).

4. **Add a new CSS class for chip-container overflow** if needed — but the existing `.times` div parent already handles horizontal overflow via the chip's `max-width` + `text-overflow:ellipsis`. No new container needed.

5. **Verify graceful-omit again** (Task 1's invariant must still hold): when `--audio-semantic` and `--speakers` both absent, no new HTML/CSS/JS emitted. Strategy: the CSS block is unconditional BUT if `s.dialogue_chip` etc. are undefined in JS, the chips never render — so CSS alone doesn't violate byte-identical (CSS rules with no matching selectors are inert). HOWEVER, to honor the strict SC#4 byte-identical guarantee, the CSS block addition needs to be inside a `{% if audio_semantic or speakers %}` style conditional. **IMPLEMENTATION CHOICE**: Since Python f-strings don't have template conditionals, use a conditional substring: build the new CSS block as a `v12_css` string variable, only include it in the `style` block when `audio_semantic_data or speakers_data`. Same for the extended JS function body — make new chip rendering conditional on `(typeof s.dialogue_chip !== 'undefined')` runtime checks (cheap) so the JS works regardless of whether consts were emitted.

6. **Run graceful-omit check** at end: generate two HTMLs (with and without flags), diff them — MUST be byte-identical when flags absent.
</actions>

<acceptance_criteria>
- `grep -n "dlg-chip\|music-chip\|sfx-chip\|spk-chip\|spk-char-chip\|emo-badge" html/gen_timeline_html.py` returns >= 12 matches (6 CSS class defs + 6 JS render branches).
- `grep -n "dialogue_chip\|music_chip\|sfx_chip\|speaker_chip" html/gen_timeline_html.py` returns >= 8 matches (build_shots_js emission + JS reads).
- **MUS-04 OMIT grep gate** (T-16-02): `grep -riE '\binstruments\b|instrument_labels|instruments_detected' html/gen_timeline_html.py` returns NOTHING.
- **Emotion null path**: `grep -n "emo_unk\|emotion.*null\|!emotion" html/gen_timeline_html.py` returns >= 1 match (the null/emo_unk → no badge guard).
- **Generate HTML with fixture data** and assert chips present:
  ```
  python3 html/gen_timeline_html.py --shots spec/fixtures/v1.2/shots.json \
    --audio-semantic spec/fixtures/v1.2/audio_semantic.json \
    --speakers spec/fixtures/v1.2/speakers.json \
    --characters spec/fixtures/v1.2/characters.json \
    --output /tmp/t2-full.html
  grep -c 'dlg-chip\|spk-char-chip\|sfx-chip' /tmp/t2-full.html  # MUST be >= 3
  ```
- **Generate HTML without flags** and assert NO new CSS/JS:
  ```
  python3 html/gen_timeline_html.py --shots spec/fixtures/v1.2/shots.json --output /tmp/t2-nov12.html
  grep -c 'dlg-chip\|spk-char-chip' /tmp/t2-nov12.html  # MUST be 0
  ```
- Commit message: `feat(16-01): per-shot dialogue/music/sfx chips + speaker→character chip + CSS`
</acceptance_criteria>
</task>

---

## Task 3: Reproduction panel with "estimated" labels (collapsible, default collapsed)

<task type="auto" id="16-01-T3">
<name>Reproduction panel with "estimated" labels (collapsible, default collapsed)</name>

<read_first>
- html/gen_timeline_html.py:319-362 (the body HTML structure — header → player-section → app; new reproduction panel goes between gallery and `.app` OR inside each shot-row body. DECISION: per-shot panel, embedded in shot-row body so it lives next to the chips — mirror CONTEXT.md "default collapsed to avoid clutter")
- html/gen_timeline_html.py:519-528 (shot-row HTML template — where to inject the panel after the `.dlg` div)
- spec/fixtures/v1.2/audio_semantic.json:27-39 (shot 1 reproduction: tts non-null + foley non-null + music_gen=null — verifies null-omit path)
- spec/SPEC.md:670-720 (§10 Fidelity Disclaimer — the prose authority; SPEC §10.1 mandates "estimated" prefix on every displayed reproduction field — this is the AF-01 mitigation)
- spec/SPEC.md:678 specifically: "HTML/SPEC 的展示标签 MUST 显式「estimated」前缀(Phase 16 PRESENT-01)" — load-bearing requirement
</read_first>

<actions>
1. **Add reproduction panel CSS** (extend the v12 CSS block from Task 2):
   ```css
   .repro-panel {{ margin-top:2px; padding:2px 6px; border-left:2px solid #bc8cff;
                  background:rgba(188,140,255,0.04); border-radius:2px; font-size:10px;
                  color:#8b949e; }}
   .repro-panel .repro-header {{ color:#bc8cff; cursor:pointer; user-select:none;
                                 font-weight:600; }}
   .repro-panel .repro-body {{ display:none; margin-top:2px; padding-left:6px;
                               border-left:1px solid #30363d; }}
   .repro-panel.open .repro-body {{ display:block; }}
   .repro-field {{ margin:2px 0; }}
   .repro-field .lbl {{ color:#bc8cff; font-weight:600; }}
   .repro-field .estimated-tag {{ display:inline-block; padding:0 3px; margin-left:3px;
                                   background:#3e2a4a; color:#bc8cff; border-radius:1px;
                                   font-size:8px; font-weight:600; }}
   .repro-field .conf {{ color:#8b949e; font-size:9px; margin-left:4px; }}
   .repro-field .disclaimer {{ display:block; color:#6e7681; font-size:9px;
                               font-style:italic; margin-left:8px; }}
   ```

2. **Extend `buildShotChips(s)`** OR add a new JS function `buildReproPanel(s)` — DECISION: new function (cleaner; panel is structurally different from chips). Signature: `buildReproPanel(s)` returns either empty string (no reproduction data) OR a `<div class="repro-panel">...</div>` block.

3. **`buildReproPanel(s)` implementation** (mirror the buildShotChips pattern at gen_timeline_html.py:394-413):
   - If `s.reproduction` is undefined OR all 3 layers (tts/music_gen/foley) are null/undefined → return `''`.
   - Header: `<div class="repro-header" onclick="this.parentElement.classList.toggle('open')">▶ 估算复现 prompt（已折叠）</div>` — clickable to toggle; default `open` class NOT applied → collapsed.
   - Body wrapper: `<div class="repro-body">`
   - For each non-null layer, render `<div class="repro-field"><span class="lbl">{label}</span><span class="estimated-tag">estimated（估算）</span><span class="conf">conf={confidence:.2f}</span>: <span>{_esc(text)}</span><span class="disclaimer">{_esc(fidelity_disclaimer)}</span></div>` where:
     - `label` = "TTS"/"music-gen"/"foley" (the 3 layers)
     - `estimated-tag` content: `"estimated（估算）"` — VISIBLE on EVERY non-null field (SC#3 + AF-01)
     - `text` = layer.text (the NL prompt — full text, not truncated; panel body has room)
     - `confidence` = layer.confidence (0.00-1.00, formatted 2-decimal)
     - `disclaimer` = layer.fidelity_disclaimer (may be missing → omit `.disclaimer` span entirely)
   - Close `</div></div>`.

4. **Inject the panel** in BOTH shot-row build sites:
   - Initial render (line 519-528): after `<div class="dlg">${{dlg}}</div>` add `+ buildReproPanel(s)`.
   - rebuildAll (line 974-986): same injection.

5. **Verify SC#3 "estimated" on EVERY field** with fixture data:
   - Shot 1 has tts (non-null) + music_gen=null + foley (non-null) → panel renders 2 fields, each with `estimated-tag`.
   - Shot 2 has no reproduction key at all → panel not rendered.
   - `grep -c 'estimated' /tmp/t3-full.html` MUST be >= 4 (header text "估算复现" + 2 estimated-tags + comment). Adjust based on actual count but every non-null layer must have its own visible "estimated" badge.

6. **Verify graceful-omit** again (the byte-identical invariant MUST hold across all 3 tasks so far).
</actions>

<acceptance_criteria>
- `grep -n "repro-panel\|repro-field\|repro-header\|estimated-tag\|repro-body" html/gen_timeline_html.py` returns >= 10 matches (CSS + JS).
- `grep -c "estimated\|估算" html/gen_timeline_html.py` returns >= 5 (CSS class name + JS template literal + header text + at least one comment).
- **AF-01 grep gate** (T-16-03 verify): `grep -n "estimated" /tmp/t3-full.html` for a fixture-driven render returns >= 2 matches per non-null reproduction layer (1 in `estimated-tag` span + 1 in header text or comment).
- **Generate HTML with fixture data**:
  ```
  python3 html/gen_timeline_html.py --shots spec/fixtures/v1.2/shots.json \
    --audio-semantic spec/fixtures/v1.2/audio_semantic.json \
    --speakers spec/fixtures/v1.2/speakers.json \
    --characters spec/fixtures/v1.2/characters.json \
    --output /tmp/t3-full.html
  grep -c 'repro-panel\|estimated-tag' /tmp/t3-full.html  # MUST be >= 3
  grep -c 'estimated' /tmp/t3-full.html  # MUST be >= 2 (1 per non-null layer: tts + foley)
  ```
- **Null layer omission**: open the HTML and verify shot 1's panel shows TTS + foley but NOT music-gen (music_gen=null in fixture).
- **Graceful-omit**: regenerate without flags and diff against t2-nov12.html — still byte-identical.
- Commit message: `feat(16-01): reproduction panel with visible "estimated" labels per AF-01`
</acceptance_criteria>
</task>

---

## Task 4: step_timeline CLI flag pass-through + XSS smoke test (verify_phase16_smoke.py)

<task type="auto" id="16-01-T4">
<name>step_timeline CLI flag pass-through + XSS smoke test (verify_phase16_smoke.py)</name>

<read_first>
- run_pipeline.py:417-504 (step_timeline function — Phase 14 cache inputs already wired at lines 461-464; Phase 16 only adds cmd argv after line 502)
- run_pipeline.py:486-502 (the existing flag pass-through pattern for --prompts/--characters/--props/--asset-json — mirror for --audio-semantic/--speakers)
- scripts/verify_phase8_smoke.py:490-633 (scenario_html_xss_inert — the EXACT pattern to clone-and-extend for Phase 16's broader attack surface)
- scripts/verify_phase8_smoke.py:525-555 (payload fixtures — JSON files with malicious names/texts; reuse pattern with v1.2 sinks)
- scripts/verify_phase8_smoke.py:637-680 (main / argparse / scenario list — clone structure for Phase 16)
- html/gen_timeline_html.py:159-163 (JSON-in-script .replace defense — the layer being verified)
- html/gen_timeline_html.py:24-41 (Python _esc — the layer being verified)
</read_first>

<actions>
1. **Wire step_timeline cmd argv** in run_pipeline.py:470-503 (add after the existing `--asset-json` block at line 502):
   ```python
   # Phase 16: pass --audio-semantic + --speakers so HTML gallery renders v1.2 chips
   # + speaker→character chip + reproduction panel. Phase 14 mtime cache (run_pipeline.py:
   # 461-464) already includes these files as inputs — Phase 16 only adds cmd argv.
   if audio_semantic_json and os.path.exists(audio_semantic_json):
       cmd += ["--audio-semantic", audio_semantic_json]
   if speakers_json and os.path.exists(speakers_json):
       cmd += ["--speakers", speakers_json]
   ```
   This mirrors the existing `if prompts_json and os.path.exists(prompts_json): cmd += ["--prompts", prompts_json]` pattern at line 489-490.

2. **Create `scripts/verify_phase16_smoke.py`** — clone-and-extend from scripts/verify_phase8_smoke.py. Structure:
   - Imports: argparse, json, os, shutil, subprocess, sys, tempfile, pathlib.Path.
   - `REPO = Path(__file__).resolve().parent.parent` (mirror verify_phase8_smoke.py:13).
   - `_tmp_work_dir()` helper (mirror).
   - `_write_synthetic_shots(path, count)` helper (mirror).
   - `_write_v12_fixtures(work_dir, **payloads)` NEW helper — writes audio_semantic.json + speakers.json with malicious payloads in dialogue.text / sfx.description / reproduction.{tts,music_gen,foley}.text / character name (for char-lookup via speakers). Default: clean fixtures; payload overrides per-scenario.
   - `_run(cmd, timeout)` helper (mirror).

3. **Five scenarios**:

   **scenario_graceful_omit_byte_identical** (T-16-04 verify):
   - Build tmp work_dir with shots.json + characters.json (Phase 8 baseline).
   - Run gen_timeline_html.py with `--shots --characters` only → write /tmp/baseline.html.
   - Run gen_timeline_html.py with `--shots --characters --audio-semantic /nonexistent --speakers /nonexistent` → write /tmp/nov12.html.
   - Assert `open('/tmp/baseline.html').read() == open('/tmp/nov12.html').read()` (byte-identical).
   - Return True with detail message including byte count.

   **scenario_chips_rendered** (SC#1 + SC#2 verify):
   - Build tmp work_dir with shots.json + audio_semantic.json (fixture-quality: shot 1 has dialogue + sfx + reproduction; shot 2 has dialogue only) + speakers.json (spk_001→char_001 resolved, spk_002→null 旁白) + characters.json (char_001 "主角").
   - Run gen_timeline_html.py with all four flags → write /tmp/chips.html.
   - Assert `dlg-chip` appears >= 2 times (one per shot with dialogue).
   - Assert `sfx-chip` appears >= 1 time (shot 1 has sfx).
   - Assert `spk-char-chip` appears >= 1 time (shot 1's spk_001 resolves to char_001).
   - Assert `spk-chip` (unresolved variant) appears >= 1 time (shot 2's spk_002 has char_id=null).
   - Assert `gallery-char_001` anchor exists (the link target — Phase 8 PRESENT-01 gallery).
   - Return True with detail.

   **scenario_reproduction_estimated_labels** (SC#3 + T-16-03 verify):
   - Reuse the chips.html from scenario_chips_rendered OR regenerate.
   - Assert `repro-panel` appears >= 1 time (shot 1 has reproduction).
   - Assert `estimated-tag` appears >= 2 times (one per non-null layer: tts + foley; music_gen=null in fixture).
   - Assert `estimated` (case-insensitive) appears >= 2 times in some visible context.
   - Assert `fidelity_disclaimer` content rendered (the SPEC §10 locked strings).
   - Return True.

   **scenario_mus_04_omitted** (T-16-02 verify):
   - Source-grep `html/gen_timeline_html.py` for `\binstruments\b|instrument_labels|instruments_detected` — MUST return 0 matches.
   - Generate HTML with fixture and assert NO rendered instruments field (grep `instruments` in HTML — SHOULD be 0 unless used in a comment or non-rendered context; document any matches).
   - Return True if source grep is clean.

   **scenario_html_xss_inert_v12** (SC#4 + T-16-01 verify — the CRITICAL security gate):
   - Build tmp work_dir with all v1.2 fixtures seeded with 6 XSS payloads (one per sink):
     - `dialogue.text = "</script><script>alert('dlg');</script>"` (JSON-in-script breakout)
     - `sfx.description = "<img src=x onerror=alert(1)>"` (body context)
     - `reproduction.tts.text = "</textarea><script>alert(1)</script>"` (textarea-like context)
     - `reproduction.music_gen.text = "data:text/html;base64,PHNjcmlwdD5hbGVydCgxKTwvc2NyaXB0Pg=="` (base64 payload)
     - `reproduction.foley.text = 'x" onerror="alert(1)'` (attribute breakout via _esc failure)
     - characters.json char_001 `name = "</script><script>alert('spk-char');</script>"` (speaker→char name lookup sink — flows via CHARACTERS find in JS)
   - Run gen_timeline_html.py with all flags → write /tmp/xss.html.
   - Read HTML; assert NONE of the following raw breakout patterns appear:
     - `</script><script>` (JSON-in-script defense failure)
     - `<img src=x onerror=` (body context _esc failure)
     - `</textarea><script>` (textarea breakout)
     - `data:text/html;base64,PHNjcmlwdD5hbGVydCgxKTwvc2NyaXB0Pg==` (raw base64 payload that could be decoded by an attacker-controlled attribute)
     - `onerror="alert(1)` (attribute breakout)
     - `<script>alert('dlg');</script>` (raw script injection)
     - `<script>alert('spk-char');</script>` (speaker→char name leak)
   - Assert escaped forms DO appear:
     - `&lt;script&gt;` (from dialogue.text escaping)
     - `&lt;img src=x onerror=alert(1)&gt;` (from sfx.description escaping)
     - `<\\/script>` (the JSON-in-script .replace defense — search for `<\\/script><\\/script>` or similar in const bootstraps)
   - Return True with detail summarizing payload matrix coverage.

4. **Main() + scenarios list** (mirror verify_phase8_smoke.py:637-680):
   ```python
   scenarios = [
       ("graceful_omit_byte_identical", scenario_graceful_omit_byte_identical),
       ("chips_rendered", scenario_chips_rendered),
       ("reproduction_estimated_labels", scenario_reproduction_estimated_labels),
       ("mus_04_omitted", scenario_mus_04_omitted),
       ("html_xss_inert_v12", scenario_html_xss_inert_v12),
   ]
   ```
   Exit 0 if all GREEN; exit 1 + list failures otherwise.

5. **Run the smoke test** end-to-end: `python3 scripts/verify_phase16_smoke.py --verbose` MUST exit 0 with all 5 scenarios GREEN.

6. **Run the Phase 8 smoke test** to confirm no regression: `python3 scripts/verify_phase8_smoke.py` MUST still exit 0.

7. **Final graceful-omit diff** — explicit verify: `diff /tmp/baseline.html /tmp/nov12.html` returns empty.
</actions>

<acceptance_criteria>
- `grep -n "audio-semantic\|--speakers" run_pipeline.py` returns >= 4 matches (Phase 14 cache inputs at 461-464 + Phase 16 cmd argv pass-through).
- `scripts/verify_phase16_smoke.py` exists with >= 5 scenario functions + a main() exit-code contract.
- **SMOKE TEST GREEN**: `python3 scripts/verify_phase16_smoke.py` exits 0 with all 5 scenarios passing.
- **PHASE 8 REGRESSION GREEN**: `python3 scripts/verify_phase8_smoke.py` still exits 0 (no regression in the prior XSS matrix).
- **T-16-01 verify** (XSS gate): the html_xss_inert_v12 scenario confirms zero raw breakout sequences across all 6 sinks × 6 payloads; escaped forms present.
- **T-16-02 verify** (MUS-04 OMITTED gate): the mus_04_omitted scenario confirms source grep returns 0 matches.
- **T-16-03 verify** (AF-01 estimated gate): the reproduction_estimated_labels scenario confirms every non-null layer has a visible `estimated` badge.
- **T-16-04 verify** (graceful-omit gate): the graceful_omit_byte_identical scenario confirms byte-identical output when flags absent.
- Commit message: `feat(16-01): step_timeline CLI pass-through + Phase 16 smoke test (5 scenarios)`
</acceptance_criteria>
</task>

---

## Deviation Handling

This plan is highly constrained by Phase 11 schema locks + Phase 10 LOCKED CONDITIONAL gating + Phase 8 PRESENT chip precedent. Expected deviations:

- **Rule 1 (auto-fix bug)**: If a chip's HTML escaping has a flaw caught by the smoke test, fix inline (add missing _esc call) and document as deviation.
- **Rule 2 (auto-add critical)**: If the smoke test reveals a payload route not covered (e.g. emotion label flows to a sink without _esc), add the _esc call inline.
- **Rule 3 (auto-fix blocking)**: If `run_pipeline.py` step_timeline cmd construction fails (wrong arg order, missing import), fix inline.
- **Rule 4 (architectural)**: If the schema turns out to require a structural redesign (e.g. music_chip needs a separate `tempo_bpm` field that doesn't exist) → STOP and checkpoint. NOT expected — the v1.2 producer embeds tempo/mood/key/VA inside music_gen.text NL, so the HTML just renders that text.

## Self-Check Before SUMMARY.md

- `python3 scripts/verify_phase16_smoke.py` exits 0.
- `python3 scripts/verify_phase8_smoke.py` exits 0 (no regression).
- `git log --oneline 16-01` shows 4 task commits + 1 docs commit.
- All 4 tasks have acceptance criteria verifiably met (re-run grep checks).
