---
phase: 03-canvas-consumer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /data/workspace/kst-canvas-consumer/src/routes/canvas/v2/import-from-dir.ts
  - /data/workspace/kst-canvas-consumer/scripts/verify-canvas-shot-timeline.ts
  - /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/asset.json
  - /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/shots.json
  - /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/audio_analysis.json
  - /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/transcript.json
  - /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/frames.json
  - /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/prompts.json
  - /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/video.mp4
  - /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/stems/vocals.wav
  - /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/stems/drums.wav
  - /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/stems/other.wav
autonomous: true
requirements: [CANVAS-01, CANVAS-02, CANVAS-03]
user_setup: []

must_haves:
  truths:
    - "把一个 ShotTimelineAsset 目录喂进 canvas 现有 import-from-dir 入口,产出一个 collection(1 个 zone 父节点包裹所有子节点),不新挂路由"
    - "collection 子节点 = N storyboard(每镜一个,来自 shots.json)+ 3 audio(vocals/drums/other)+ 1 video(master),全部由既有 5 个 renderer 渲染"
    - "每个 media 子节点满足对应 per-type Zod schema(storyboard: shot_id/shot_type/duration_sec;audio: filePath/shot_id/engine/duration_sec;video: filePath/shot_id/engine/duration_sec/resolution)—— 字段全部由合成填充,Zod 严格度零改动"
    - "storyboard 子节点之间按 shot_id 升序 emit sequence 边(data.linkType='sequence'),既有 CanvasEdge.tsx:60 把它们渲染成蓝色箭头"
    - "未知/更新 schema_version 只 warn 不 reject(运行时 graceful-degrade,schema 本身仍 additionalProperties:false)"
    - "前端零改动(packages/infinite-canvas/ diff 为空),canvasAssetSchema.ts Zod 严格度未减"
  artifacts:
    - path: "/data/workspace/kst-canvas-consumer/src/routes/canvas/v2/import-from-dir.ts"
      provides: "ShotTimelineAsset 早期识别分支 + 导出的 extractShotTimelineArtifacts helper + RawArtifact.canvasType 扩展 + buildPhaseTree L804 单行 override + sequence-edge emission"
      contains: "extractShotTimelineArtifacts"
    - path: "/data/workspace/kst-canvas-consumer/scripts/verify-canvas-shot-timeline.ts"
      provides: "Standalone verify script covering asserts A–F + additive-only 守门断言"
      contains: "validateGraphNodes"
    - path: "/data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/asset.json"
      provides: "Downsampled golden fixture manifest from real ep01 producer output"
      contains: "shottimeline"
  key_links:
    - from: "/data/workspace/kst-canvas-consumer/src/routes/canvas/v2/import-from-dir.ts::scanWorkdirForArtifacts"
      to: "/data/workspace/kst-canvas-consumer/src/routes/canvas/v2/import-from-dir.ts::extractShotTimelineArtifacts"
      via: "asset.json 早识别分支 + __shot_timeline_asset__ sentinel key + scanAndBuildTree merge"
      pattern: "extractShotTimelineArtifacts"
    - from: "/data/workspace/kst-canvas-consumer/src/routes/canvas/v2/import-from-dir.ts::extractShotTimelineArtifacts"
      to: "/data/workspace/kst-canvas-consumer/src/routes/canvas/v2/import-from-dir.ts::buildPhaseTree"
      via: "RawArtifact[] with canvasType override → Zone/Summary/Artifact tree"
      pattern: "buildPhaseTree\\("
    - from: "/data/workspace/kst-canvas-consumer/scripts/verify-canvas-shot-timeline.ts"
      to: "/data/workspace/kst-canvas-consumer/src/routes/canvas/v2/import-from-dir.ts"
      via: "import { extractShotTimelineArtifacts }"
      pattern: "from \"\\.\\./src/routes/canvas/v2/import-from-dir\""
---

<objective>
让 `@kais/infinite-canvas` 的现有 `POST /api/canvas/v2/import-from-dir` 入口能识别一份 ShotTimelineAsset 目录(根目录 `asset.json` + 5 数据 JSON + video.mp4 + 3 stems),把它折叠成画布上的 **1 个 zone 父节点 + N storyboard + 3 audio + 1 video** 子图,在 storyboard 之间按 shot_id 升序 emit sequence 边,所有子节点通过既有 per-type Zod 校验,前端零改动。

Purpose: 落地 CANVAS-01/02/03 —— 证明「collection as structural parent node」路径不需要新 renderer / 不 bump contract,producer(asset.json)↔ consumer(import-from-dir)两端通过严格 schema + 合成字段无缝对接。
Output: 1 个修改文件(import-from-dir.ts)+ 1 个新 verify 脚本 + 1 个 downsampled golden fixture 目录(共 12 个新文件)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-canvas-consumer/03-CONTEXT.md
@.planning/phases/03-canvas-consumer/03-RESEARCH.md
@.planning/phases/03-canvas-consumer/03-PATTERNS.md
@.planning/phases/03-canvas-consumer/03-VALIDATION.md
@spec/schemas/asset.schema.json

<interfaces>
<!-- 关键 contract / 既有签名 —— executor 直接消费,无需 codebase 探索 -->

### Worktree analog: /data/workspace/kst-canvas-consumer/src/routes/canvas/v2/import-from-dir.ts

RawArtifact interface (L237-254) — 本 plan 扩展点(Solution A,per CONTEXT「Collection→Canvas 节点映射」+ RESEARCH Pattern 2):
```typescript
interface RawArtifact {
  label: string;
  output_key: string;
  name?: string;
  description?: string;
  prompt?: string;
  thumbnailUrl?: string;
  filePath?: string;
  extra?: Record<string, any>;
  /* Phase 3 NEW: */ canvasType?: "script" | "asset" | "storyboard" | "audio" | "video";
}
```

buildPhaseTree signature (L592) + L804 override target:
```typescript
function buildPhaseTree(phasePrefix: string, artifacts: RawArtifact[]): {
  zoneNode: FlowNodeV2; summaryNode: FlowNodeV2; artifactNodes: FlowNodeV2[]; links: FlowLinkV2[];
}
// L804 current: type: def.canvasType as any
// L804 target:  type: (art.canvasType ?? def.canvasType) as any   ← additive opt-in
```

scanWorkdirForArtifacts signature (L996):
```typescript
async function scanWorkdirForArtifacts(workdir: string): Promise<Map<string, RawArtifact[]>>
```

scanAndBuildTree merge point (L1226-1274) — 在 buildCrossReferenceLinks 之前插入 ShotTimelineAsset 子图合并:
```typescript
async function scanAndBuildTree(workdir: string): Promise<{ nodes: FlowNodeV2[]; links: FlowLinkV2[] }>
// 内部:_workdirToOss = { workdir, ossPrefix } 在 L1235 设置 —— extractShotTimelineArtifacts 必须在此作用域内被调用
```

fsToOssUrl (L174-192): `(fsPath: string) => string | null` —— 已在 module scope,helper 直接调用。
tryReadJSON (L204-211): `(filePath: string) => Promise<any | null>` —— catch-null JSON reader。

### Worktree analog: /data/workspace/kst-canvas-consumer/src/lib/canvasAssetSchema.ts

```typescript
// L51-119: assetDataSchemas —— per-type Zod required 字段硬约束(DO NOT MODIFY)
// storyboard required: { label, shot_id, shot_type, duration_sec }
// audio required:      { filePath, shot_id, engine, duration_sec }
// video required:      { filePath, shot_id, engine, duration_sec, resolution }
// zone:                structuralTypes pass-through (L122),validateNodeData 返回 null

export function validateNodeData(nodeType: string, data: any): ZodError | null
export function validateGraphNodes(nodes: FlowNodeV2[]): Array<{ nodeId: string; errors: ZodError }>
```

### Worktree analog: /data/workspace/kst-canvas-consumer/src/types/flowgraph-v2-schema.ts
```typescript
export type FlowNodeV2 = { id: string; type: string; branchId: string; phaseIndex: number; phaseName: string; position: {x:number;y:number}; size: {width:number;height:number}; state: string; data: Record<string, any> }
export type FlowLinkV2 = { id: string; source: string; target: string; branchId: string; dataType: string; data?: Record<string, any> }
```

### Worktree analog: /data/workspace/kst-canvas-consumer/packages/infinite-canvas/src/utils/flowDataMapper.ts (frontend precedent)
```typescript
// L163-172 — sequence edge 字面形状(本 plan 在 backend 字面复刻):
{ id: `seq-${i}`, source: prevNodeId, target: nodeId, data: { dataType: 'data', linkType: 'sequence' } }
```

### Producer source: /data/workspace/kais-shot-timeline/output/虫虫武侠…/asset.json
manifest 字面:`{schema_version:"1", asset_type:"shottimeline", source:{video_filename,duration_sec:308.352}, generator:{tool,version,generated_at}, data:{shots,...5 paths}, media:{video:"video.mp4", stems:{vocals,drums,other}}}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 生成 downsampled golden fixture + worktree yarn install baseline</name>
  <files>
    /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/asset.json,
    /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/shots.json,
    /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/audio_analysis.json,
    /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/transcript.json,
    /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/frames.json,
    /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/prompts.json,
    /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/video.mp4,
    /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/stems/vocals.wav,
    /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/stems/drums.wav,
    /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/stems/other.wav
  </files>
  <read_first>
    /data/workspace/kais-shot-timeline/output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/asset.json (producer manifest, copy verbatim),
    /data/workspace/kais-shot-timeline/.planning/phases/03-canvas-consumer/03-PATTERNS.md (fixture downsample strategy + per-file size targets),
    /data/workspace/kst-canvas-consumer/scripts/fixtures/sample-manifest.json (existing in-repo fixture format analog)
  </read_first>
  <action>
    先跑 `yarn install` 在 worktree 根(`/data/workspace/kst-canvas-consumer`)—— 这是 fresh checkout,node_modules 缺失,后续 Task 3 verify 脚本依赖 `npx tsx` 可用。安装失败立刻停(无 fallback)。

    然后 mkdir fixture 目录树:`scripts/fixtures/shot-timeline-ep01/stems/`。

    **数据 JSON 直接复制**(producer 真实 ep01 路径 `/data/workspace/kais-shot-timeline/output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/`):
    - `asset.json` —— 整文件 `cp`(<1KB,manifest 本身极小)
    - `shots.json` —— 整文件 `cp`(~8KB,93 shots,schema locked)
    - `audio_analysis.json` —— 整文件 `cp`(~50KB)
    - `transcript.json` —— 整文件 `cp`(~16KB)
    - `prompts.json` —— 整文件 `cp`(~77KB)

    **frames.json 下采样**(原始 7.6MB base64 内联):用 jq 或 node 脚本只保留前 5 个 shot 的帧对象,保留 schema 形态(`[{id, first_frame, last_frame}, ...]`),输出 `frames.json`。理由:verify 脚本只检查 thumbnailUrl 路径能跑通,5 个足够;7.6MB 进 git 不友好但也不致命(planner 酌情 —— 如保留全量也可,但 < 10MB fixture 总目标是硬约束)。

    **媒体文件 ffmpeg 生成 1s silent stubs**(保留 container/duration 元数据,让 fsToOssUrl + ffprobe 路径都可测):
    - `video.mp4`:`ffmpeg -f lavfi -i color=size=1280x720:rate=24:duration=1 -f lavfi -i anullsrc=channel_layout=stereo:sample_rate=44100 -c:v libx264 -c:a aac -shortest video.mp4`(<100KB,ffprobe 返回 `1280x720`)
    - `stems/vocals.wav` / `drums.wav` / `other.wav`:每个用 `ffmpeg -f lavfi -i anullsrc=channel_layout=mono:sample_rate=16000 -t 1 -q:a 0 -y stems/<stem>.wav`(<100KB 每个采集 Demucs 16kHz mono PCM 形态)

    所有 ffmpeg 命令加 `-y` 覆盖 + `-loglevel error` 抑制噪音。失败立刻停(说明环境异常,不能跳过)。

    **不要**改动 producer 源目录;**只写** worktree 内的 fixture 目录。
  </action>
  <verify>
    <automated>
    du -sh /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/ | awk '{print $1}'  # 期望 < 10MB
    test -x /data/workspace/kst-canvas-consumer/node_modules/.bin/tsx && echo "tsx OK"
    ffprobe -v quiet -select_streams v:0 -show_entries stream=width,height -of csv=p=0:s=x /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/video.mp4  # 期望 1280x720
    python3 -c "import json; m=json.load(open('/data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/asset.json')); assert m['asset_type']=='shottimeline' and m['schema_version']=='1'; print('manifest OK')"
    python3 -c "import json; s=json.load(open('/data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/shots.json')); assert len(s)>=1; print(f'shots={len(s)} OK')"
    ls /data/workspace/kst-canvas-consumer/scripts/fixtures/shot-timeline-ep01/stems/{vocals,drums,other}.wav
    </automated>
  </verify>
  <done>
    - fixture 目录存在 12 个文件(6 JSON + 1 mp4 + 3 wav + 1 stems/ dir),总大小 < 10MB
    - `node_modules/.bin/tsx` 可执行(yarn install 成功)
    - asset.json 字面 `asset_type="shottimeline"` + `schema_version="1"`
    - video.mp4 通过 ffprobe 返回 `1280x720`(resolution 字段合成路径可测)
    - shots.json 至少 1 shot(真实 ep01 = 93)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 extractShotTimelineArtifacts + RawArtifact.canvasType 扩展 + buildPhaseTree L804 override + scanWorkdirForArtifacts 早识别分支 + sequence edges + scanAndBuildTree 合并</name>
  <files>/data/workspace/kst-canvas-consumer/src/routes/canvas/v2/import-from-dir.ts</files>
  <behavior>
    - 5 个数据 JSON 通过 tryReadJSON 并行读取;任一缺失返回 null 但不 throw(helper 继续用可用数据)
    - manifest.schema_version 不在 KNOWN_VERSIONS={"1"} 时 `console.warn` 但不 return / throw
    - 输出节点数 = 1 zone + 1 summary + N storyboard + 3 audio + 1 video(N = len(shots.json))
    - 输出边数 = (N+4) 条 zone→child `dataType:"output"` + (N-1) 条 sequence `dataType:"data"`(N≥2 时)
    - 所有 storyboard 子节点的 `data.shot_id` 按 shots.json 的 id 顺序出现,sequence 边 source→target 的 shot_id 单调递增
    - 所有 audio/video 子节点 `data.filePath` 以 `/oss/` 开头(fsToOssUrl 已归一化)或 fallback 到绝对路径
    - 所有 media 子节点通过 validateNodeData(per-type Zod) 返回 null
  </behavior>
  <read_first>
    /data/workspace/kst-canvas-consumer/src/routes/canvas/v2/import-from-dir.ts (L1-50 imports, L174-211 fsToOssUrl+tryReadJSON, L237-254 RawArtifact, L592-835 buildPhaseTree esp. L802-804, L996-1135 scanWorkdirForArtifacts, L1226-1274 scanAndBuildTree, L1393-1477 appendAndSync callers),
    /data/workspace/kst-canvas-consumer/src/lib/canvasAssetSchema.ts (L51-119 assetDataSchemas DO NOT MODIFY, L121-140 structuralTypes, L138 validateNodeData),
    /data/workspace/kst-canvas-consumer/src/types/flowgraph-v2-schema.ts (FlowNodeV2 + FlowLinkV2 type defs),
    /data/workspace/kst-canvas-consumer/packages/infinite-canvas/src/utils/flowDataMapper.ts (L163-172 sequence edge 字面形状 precedent),
    /data/workspace/kst-canvas-consumer/packages/infinite-canvas/src/components/edges/CanvasEdge.tsx (L60-75 linkType='sequence' renderer),
    /data/workspace/kais-shot-timeline/.planning/phases/03-canvas-consumer/03-PATTERNS.md (## Pattern Assignments 全部 hook 点 + Solution A 1-line diff),
    /data/workspace/kais-shot-timeline/.planning/phases/03-canvas-consumer/03-RESEARCH.md (## Field Mapping R2 表 + Code Example 1 helper 骨架)
  </read_first>
  <action>
    全部改动落在 **单一文件** `/data/workspace/kst-canvas-consumer/src/routes/canvas/v2/import-from-dir.ts`。7 个 hook 点(按文件出现顺序):

    **Hook 1 — RawArtifact interface 扩展(L237-254):** 加可选字段 `canvasType?: "script" | "asset" | "storyboard" | "audio" | "video"` + Chinese 注释「Phase 3: per-artifact canvasType override; 不设则用 phase 默认 — additive opt-in,既有 13 phase 调用者零行为变化」。这是 CONTEXT「Collection→Canvas 节点映射」+ RESEARCH Pattern 2 Solution A 的字面落地;**不**选 Solution B(并行 builder,~80 行重复代码,无 analog —— 见 03-PATTERNS.md L659)。

    **Hook 2 — buildPhaseTree L804 单行 override:** 当前 `type: def.canvasType as any` → 改为 `type: (art.canvasType ?? def.canvasType) as any`。Additive:既有 13 phase 调用者**不设** `art.canvasType` → `?? def.canvasType` 落到默认值 → 行为零变化。其余 artifact loop body(extra-merge L696-704、alias L711-716、enum-normalize L723-728、expected-fields warn L737-748、E-Konte derive L753-800)完全不动 —— 新 ShotTimelineAsset 路径继承所有 receiver-side 兼容 shim。

    **Hook 3 — 新增 `probeResolution(videoPath: string): Promise<string>` helper:** lazy `await import("child_process")` + `promisify(execFile)`,调用 `ffprobe -v quiet -select_streams v:0 -show_entries stream=width,height -of csv=p=0:s=x <videoPath>`,`return stdout.trim() || "0x0"`;try/catch 全包,失败 fallback `"0x0"`(Zod `min(1)` 仍通过)。**不**写 mp4 box parser(见 RESEARCH Don't Hand-Roll)。

    **Hook 4 — 新增 exported `extractShotTimelineArtifacts(manifest, workdir, manifestPath): Promise<{nodes, links}>`:** 在 import-from-dir.ts 末尾(或 buildPhaseTree 之后)添加。骨架按 RESEARCH Code Example 1 + 03-PATTERNS.md Shared Patterns。**必须 `export`**(verify script 要 import)。流程:
    (a) `schema_version` graceful-degrade:`const KNOWN_VERSIONS = new Set(["1"])`,不在 set 内则 `console.warn('[v2/import] ShotTimelineAsset schema_version="${...}" not known — graceful-degrade (SPEC §4)')`,不 throw / return(per CONTEXT「schema_version graceful-degrade」+ RESEARCH Pattern 4)。
    (b) 并行 `tryReadJSON(join(workdir, manifest.data.{shots,audio_analysis,transcript,frames,prompts}))` 5 路。
    (c) `probeResolution(join(workdir, manifest.media.video))` 一次。
    (d) 构造 `artifacts: RawArtifact[]`(phasePrefix 用 **`"p13"`** —— 见 RESEARCH Open Question 2 推荐「delivery」语义最贴;CanvasAssetSchema PHASE_DEFS 已有 p13 条目,CanvasAssetSchema.ts 不需要改 —— 这是 Claude's discretion 区,选择 p13 + 注释 rationale):
       - **N storyboard artifacts**(每个 shot):`{label: \`Shot \${shot.id}\`, output_key: "storyboard", canvasType: "storyboard", thumbnailUrl: framesById.get(shot.id)?.first_frame, extra: {shot_id: String(shot.id), shot_type: "scene", duration_sec: shot.duration, __synthetic_fields: ["shot_type"]}}`。shot_type 默认 "scene" per CONTEXT;Zod 不限枚举,frontend NODE_SCHEMA.storyboard 无 shot_type 字段(只有 framing/cameraMovement),任何非空字符串都渲染。thumbnailUrl 直接内联 frames.json 的 first_frame data URI(per RESEARCH A4 + Field Mapping R2),不经 fsToOssUrl —— StoryboardNode.tsx:74-75 直接 `<img src={thumbnailUrl}>` 接受 data URI。
       - **3 audio artifacts**(vocals/drums/other):`{label: \`\${stem} stem\`, output_key: "audio", canvasType: "audio", filePath: fsToOssUrl(join(workdir, manifest.media.stems[stem])) ?? <absPath>, extra: {shot_id: "collection", engine: "shot-timeline", duration_sec: manifest.source.duration_sec, __synthetic_fields: ["shot_id", "engine"]}}`。shot_id 用 sentinel `"collection"` per CONTEXT「audio/video 用集合级 sentinel」。
       - **1 video artifact**:`{label: manifest.source.video_filename, output_key: "video", canvasType: "video", filePath: fsToOssUrl(videoPath) ?? <absPath>, extra: {shot_id: "collection", engine: "shot-timeline", duration_sec: manifest.source.duration_sec, resolution, __synthetic_fields: ["shot_id", "engine", "resolution"]}}`。
       所有合成字段所在节点在 `extra` 上挂 `__synthetic_fields: string[]`(per CONTEXT「标注 `__synthetic_fields` 供溯源」+ Phase 4 VERIFY-02 可断言)。`engine="shot-timeline"` 字面 provenance 标识(Zod 不限枚举,frontend AUDIO_METADATA_LABELS.engine 也接受任意非空字符串)。
    (e) `const tree = buildPhaseTree("p13", artifacts)` —— 复用既有 Zone→Summary→Artifact 三级构建,继承所有兼容 shim。Return `{ zoneNode, summaryNode, artifactNodes, links }`。
    (f) **sequence edges**:把 `tree.artifactNodes.filter(n => n.type === "storyboard")` 按 `Number(n.data.shot_id)` 升序 sort;从 i=1 开始遍历,push `{id: \`seq-\${prev.id}-\${curr.id}\`, source: prev.id, target: curr.id, branchId: "main", dataType: "data", data: { linkType: "sequence" }} as FlowLinkV2`。形状必须**字面匹配** flowDataMapper.ts:163-172 precedent(`dataType: "data"` 而非 `"output"`,否则 CanvasEdge.tsx:60 不识别)。
    (g) Return `{ nodes: [tree.zoneNode, tree.summaryNode, ...tree.artifactNodes], links: [...tree.links, ...sequenceLinks] }`。

    **Hook 5 — scanWorkdirForArtifacts 早识别分支(L996 函数顶部,既有 `for (const file of rootFiles)` 循环 L1018 之前):** 按 RESEARCH Pattern 1 + 03-PATTERNS.md L207-211 Strategy 1:`const manifest = await tryReadJSON(join(workdir, "asset.json"))`;若 `manifest && manifest.asset_type === "shottimeline"` 则 `phaseArtifacts.set("__shot_timeline_asset__", [{ label: manifest.source?.video_filename ?? "ShotTimelineAsset", output_key: "__shot_timeline_asset__", extra: { __manifest: manifest, __manifest_path: join(workdir, "asset.json") } }])` 并 `return phaseArtifacts`(短路 —— 避免误把 ep01 数据当普通 phase 处理)。

    **Hook 6 — scanAndBuildTree 合并点(L1226-1274):** 在 `enrichMediaArtifactsFromJSON(phaseArtifacts)` 之后、`for (const def of PHASE_DEFS)` 循环之后、`buildZoneChainLinks` 之前,插入:`const stKey = "__shot_timeline_asset__"; if (phaseArtifacts.has(stKey)) { const meta = phaseArtifacts.get(stKey)![0].extra!; const sub = await extractShotTimelineArtifacts(meta.__manifest, workdir, meta.__manifest_path); allNodes.push(...sub.nodes); allLinks.push(...sub.links); }`。位置选择「在 buildCrossReferenceLinks 之前」让新子图能参与跨引用链接(更连贯),也保证 `_workdirToOss` 已在 L1235 设置好(fsToOssUrl 依赖它)。

    **Hook 7 — exports:** 在文件末尾的 export 块(或 function 定义前加 `export`)暴露 `extractShotTimelineArtifacts`。**不需要** 单独 export `buildPhaseTree`(verify script 通过 `extractShotTimelineArtifacts` 间接 exercise 它)。

    **不要** 修改:`canvasAssetSchema.ts`(Zod 严格度零改动)、`packages/infinite-canvas/` 任何文件(CANVAS-03 additive-only)、`flowgraph-v2-schema.ts`(节点 type 已含 zone/storyboard/audio/video)、`flowDataMapper.ts`(前端 mapper)。所有「修改」都在 import-from-dir.ts 单文件内。
  </action>
  <verify>
    <automated>
    cd /data/workspace/kst-canvas-consumer && {
      echo "--- grep hook 1 (RawArtifact.canvasType) ---"
      grep -E "canvasType\?\:\s*\"(script\|asset\|storyboard\|audio\|video)" src/routes/canvas/v2/import-from-dir.ts
      echo "--- grep hook 2 (buildPhaseTree L804 override) ---"
      grep -E "art\.canvasType \?\? def\.canvasType" src/routes/canvas/v2/import-from-dir.ts
      echo "--- grep hook 4 (extractShotTimelineArtifacts export + helper) ---"
      grep -c "extractShotTimelineArtifacts" src/routes/canvas/v2/import-from-line.ts 2>/dev/null || grep -c "extractShotTimelineArtifacts" src/routes/canvas/v2/import-from-dir.ts
      echo "--- grep hook 5 (sentinel key) ---"
      grep -c "__shot_timeline_asset__" src/routes/canvas/v2/import-from-dir.ts
      echo "--- grep hook 6 (scanAndBuildTree merge) ---"
      grep -E "phaseArtifacts\.has\(stKey\)|extractShotTimelineArtifacts\(meta" src/routes/canvas/v2/import-from-dir.ts
      echo "--- grep sequence edges (linkType sequence) ---"
      grep -E "linkType:\s*\"sequence\"" src/routes/canvas/v2/import-from-dir.ts
      echo "--- grep graceful-degrade warn ---"
      grep -E "KNOWN_VERSIONS|graceful-degrade" src/routes/canvas/v2/import-from-dir.ts
      echo "--- grep __synthetic_fields provenance ---"
      grep -c "__synthetic_fields" src/routes/canvas/v2/import-from-dir.ts
      echo "--- type check (no emit) ---"
      npx tsc --noEmit -p tsconfig.json 2>&1 | head -20
    }
    </automated>
  </verify>
  <done>
    - RawArtifact interface 含 `canvasType?:` 可选字段
    - buildPhaseTree L804 改为 `(art.canvasType ?? def.canvasType) as any`
    - `extractShotTimelineArtifacts` exported 且被 scanAndBuildTree 调用
    - `__shot_timeline_asset__` sentinel key 在 scanWorkdirForArtifacts 早识别分支中设置
    - sequence 边形状字面匹配 flowDataMapper.ts:163-172(dataType:"data" + data.linkType:"sequence")
    - KNOWN_VERSIONS Set + graceful-degrade warn 存在
    - 所有合成字段节点含 `__synthetic_fields` 数组(storyboard=1 字段、audio=2、video=3)
    - `npx tsc --noEmit` 通过(无类型错误)
    - packages/infinite-canvas/ 零改动(git status 干净)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 编写 verify-canvas-shot-timeline.ts —— asserts A–F + additive-only 守门断言</name>
  <files>/data/workspace/kst-canvas-consumer/scripts/verify-canvas-shot-timeline.ts</files>
  <behavior>
    - 退出码 0 当且仅当全部 assert A–F + additive-only 守门断言通过
    - 单次运行 < 5 秒(fixture 已 downsample,< 10MB)
    - 不需要 backend / DB / HTTP —— 直接 import 生产模块纯函数
    - 失败时 stderr 列出每个 FAIL assert 的 name + detail
  </behavior>
  <read_first>
    /data/workspace/kst-canvas-consumer/scripts/verify-import-roundtrip.ts (140 lines byte-for-byte template —— 复制骨架),
    /data/workspace/kst-canvas-consumer/src/lib/canvasAssetSchema.ts (validateGraphNodes signature + 用法),
    /data/workspace/kais-shot-timeline/.planning/phases/03-canvas-consumer/03-VALIDATION.md (asserts A–F 映射表),
    /data/workspace/kais-shot-timeline/.planning/phases/03-canvas-consumer/03-RESEARCH.md (## Validation Architecture + Code Example 2 verify 骨架 + Code Example 3 additive-only 守门)
  </read_first>
  <action>
    全部改动落在 **新文件** `/data/workspace/kst-canvas-consumer/scripts/verify-canvas-shot-timeline.ts`。结构按 verify-import-roundtrip.ts byte-for-byte 模板复制,替换 import + fixture loader + assertions。

    **Header / imports / scaffold:** 复制 verify-import-roundtrip.ts L1-32 骨架:`#!/usr/bin/env tsx` shebang、模块 docstring(`verify-canvas-shot-timeline.ts — Phase 3 CANVAS-01/02/03 verify. Loads downsampled golden fixture, runs extractShotTimelineArtifacts, asserts structure + Zod pass + sequence-edge order + additive-only invariant`)、`import fs from "node:fs"`、`import path from "node:path"`、`import { execSync } from "node:child_process"`、`import { extractShotTimelineArtifacts } from "../src/routes/canvas/v2/import-from-dir"`、`import { validateGraphNodes } from "../src/lib/canvasAssetSchema"`。`interface TestResult` + `const results: TestResult[] = []` + `function assert(cond, name, detail?)` 完全照抄。

    **Fixture loader:** 单一固定路径 `const FIXTURE = path.resolve(__dirname, "fixtures/shot-timeline-ep01")`;`const REPO_ROOT = path.resolve(__dirname, "..")`;worktree cwd 用 `/data/workspace/kst-canvas-consumer`(与 03-RESEARCH Code Example 3 一致)。

    **main() 流程:**
    1. 读 `path.join(FIXTURE, "asset.json")` 为 `manifest`(JSON.parse + try/catch,失败即 assert false + return)
    2. `const { nodes, links } = await extractShotTimelineArtifacts(manifest, FIXTURE, path.join(FIXTURE, "asset.json"))`
    3. 分类:`zones = nodes.filter(n => n.type === "zone")`、`storyboards = nodes.filter(n => n.type === "storyboard")`、`audios = nodes.filter(n => n.type === "audio")`、`videos = nodes.filter(n => n.type === "video")`、`summaries = nodes.filter(n => n.id.startsWith("sum-"))`、`seqEdges = links.filter((l:any) => l.data?.linkType === "sequence")`

    **Assert A (CANVAS-01 结构):**
    - `assert(zones.length === 1, "CANVAS-01: exactly 1 zone node")`
    - `assert(summaries.length === 1, "CANVAS-01: exactly 1 summary node")`
    - `assert(audios.length === 3, "CANVAS-01: exactly 3 audio stems (vocals/drums/other)", audios.map(n=>n.id).join(","))`
    - `assert(videos.length === 1, "CANVAS-01: exactly 1 master video")`
    - `assert(storyboards.length >= 1, "CANVAS-01: ≥1 storyboard child", "got ${storyboards.length}")`
    - 期望 storyboards.length === 93(fixture 是真实 ep01)—— 用 `assert(storyboards.length === 93, ...)` 字面值断言;若 executor 选 downsample shots.json 则相应调整(planner 默认 shots.json 整文件复制 = 93)

    **Assert B (CANVAS-02 sequence 边数):**
    - `assert(seqEdges.length === storyboards.length - 1, "CANVAS-02: N-1 sequence edges", "got ${seqEdges.length} expected ${storyboards.length - 1}")`

    **Assert C (CANVAS-02 sequence 边 shot_id 单调):**
    - 对 seqEdges 每条边,lookup source/target 节点的 `data.shot_id`,断言 `Number(sourceShotId) < Number(targetShotId)`;若任一违反,push fail 并附上 violating edge id
    - 额外断言:相邻 seqEdges 形成 chain(`seqEdges[i].target === seqEdges[i+1].source`),用 sort by id prefix 或 by source order

    **Assert D (CANVAS-03 per-type Zod 全过):**
    - `const errors = validateGraphNodes(nodes as any); assert(errors.length === 0, "CANVAS-03: all nodes pass per-type Zod (validateGraphNodes returns 0 errors)", errors.map(e => \`${e.nodeId}: ${JSON.stringify(e.errors)}\`).join(" | "))`

    **Assert E (CANVAS-03 additive-only 守门 1 —— 前端零改动):**
    - `const treeDiff = execSync("git diff --name-only origin/master..HEAD -- packages/infinite-canvas/", { cwd: "/data/workspace/kst-canvas-consumer", encoding: "utf8" }).trim()`
    - `assert(treeDiff === "", "CANVAS-03: packages/infinite-canvas/ diff is empty (additive-only)", treeDiff || "(empty)")`

    **Assert E2 (CANVAS-03 additive-only 守门 2 —— Zod 严格度未减):** 用 count-based 比较而不是 regex-on-diff(更鲁棒,避免 diff noise 误报):
    - master 分支 `.optional()` 计数:`const m = execSync("git show origin/master:src/lib/canvasAssetSchema.ts", {cwd, encoding:"utf8"}); const masterOpt = (m.match(/\.optional\(\)/g) || []).length`
    - HEAD 同理:`const h = fs.readFileSync(path.join(REPO_ROOT, "src/lib/canvasAssetSchema.ts"), "utf8"); const headOpt = (h.match(/\.optional\(\)/g) || []).length`
    - `assert(headOpt <= masterOpt, "CANVAS-03: canvasAssetSchema.ts strictness preserved (.optional() count not increased)", "master=${masterOpt} head=${headOpt}")`
    - 同理断言 `.nullable()` 计数 + `superStrict` 关键字(若存在)

    **Assert F (Roundtrip —— manifest 字段 survive 进 node.data,为 Phase 4 VERIFY-01 预留):**
    - zone label 来自 manifest:`assert(zones[0]?.data?.label === manifest.source.video_filename, "F: zone label = manifest.source.video_filename")`
    - video duration_sec 来自 manifest:`assert(Number(videos[0]?.data?.duration_sec) === Number(manifest.source.duration_sec), "F: video duration_sec = manifest.source.duration_sec")`
    - audio engine 合成:`assert(audios.every(a => a.data?.engine === "shot-timeline"), "F: all audio stems carry engine='shot-timeline'")`
    - video resolution 非空:`assert(typeof videos[0]?.data?.resolution === "string" && videos[0].data.resolution.length >= 3, "F: video.resolution synthesized (>= '0x0')", "got '${videos[0]?.data?.resolution}'")`
    - storyboard shot_id 字符串化:`assert(storyboards.every(s => typeof s.data?.shot_id === "string" && s.data.shot_id.length >= 1), "F: all storyboard shot_id are non-empty strings")`

    **Tally + exit:** 照抄 verify-import-roundtrip.ts L137-140:`const passed = results.filter(r => r.pass).length; const failed = results.length - passed; console.log(\`\n${passed} passed, ${failed} failed\`); process.exit(failed > 0 ? 1 : 0)`。最后 `main().catch((err) => { console.error(err); process.exit(1); })`。

    **不要** 把 verify 脚本接到 `package.json` scripts.verify:* —— 这是 Phase 3 单次 verify,Phase 4 才考虑接入 npm run 流程(CANVAS-03 字面只要求 verify 存在,不要求 npm script 注册)。
  </action>
  <verify>
    <automated>
    cd /data/workspace/kst-canvas-consumer && npx tsx scripts/verify-canvas-shot-timeline.ts; echo "EXIT=$?"
    # 期望:全部 PASS,EXIT=0;输出含 "N passed, 0 failed"
    </automated>
  </verify>
  <done>
    - verify-canvas-shot-timeline.ts 存在,可被 `npx tsx` 直接执行
    - 6 类 assert(A/B/C/D/E/E2/F)全部 PASS,exit 0
    - additive-only 守门断言通过:`packages/infinite-canvas/` diff 为空;`canvasAssetSchema.ts` 的 `.optional()` / `.nullable()` 计数未增加
    - 运行总时长 < 5 秒
    - 故意破坏测试(手动改一个字段名/删一个 assert)能触发非零 exit —— executor 自验
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producer→consumer filesystem | asset.json + 5 数据 JSON + media files 跨仓库;consumer 即使生产者是同一作者也在 runtime 当 untrusted 处理(per RESEARCH V5) |
| workdir→backend route | POST /api/canvas/v2/import-from-dir body 含 workdir 路径;backend 在 workdir 子树下读文件 |
| backend→subprocess (ffprobe) | video.mp4 路径通过 `execFile` 数组传给 ffprobe(无 shell) |
| backend→filesystem (data/oss/) | `fsToOssUrl` 创建 `data/oss/{basename} → workdir` symlink(既有 import-from-dir.ts:1330-1356 已处理) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (V5/V12) | asset.json#media 路径 → `fsToOssUrl(join(workdir, rel))` 路径穿越 | mitigate | producer schema regex `^(?!.*\.\.)` 在源头拒绝父目录穿越(见 asset.schema.json L99/L110/L115/L120 pattern);consumer 侧 `tryReadJSON` + `path.join` 不会跨 workdir;既有 import-from-dir.ts:1340-1352 catch symlink target 不一致 non-fatal warn |
| T-03-02 | Denial of Service (V7) | ffprobe 子进程在 malformed video.mp4 上挂起 | mitigate | `probeResolution` 用 `await import("child_process")` + `promisify(execFile)`,无 shell 注入面;try/catch 全包 fallback `"0x0"`;ffprobe 命令带 `-v quiet` 抑制 stderr 信息泄露。本 phase 不引入 subprocess timeout(producer-authored fixture;若未来接受第三方 asset 再加 `timeout: 5000`)—— 在 STRIDE register 记录为已知 accepted residual risk |
| T-03-03 | Tampering (V12) | `data/oss/{basename}` symlink 创建 | accept | 既有 import-from-dir.ts:1340-1352 已 non-fatal warn 处理;Phase 3 复用既有 OSS bootstrap,无新增 symlink 创建路径 |
| T-03-04 | Information Disclosure (V7) | JSON parse 错误 / ffprobe stderr 泄露 internals | mitigate | `tryReadJSON` 已 catch-null 返回 null(无 stack trace 外溢);`probeResolution` 用 `-v quiet` 丢弃 ffprobe stderr |
| T-03-05 | Spoofing (V5) | asset.json `asset_type` 字段未在 consumer 侧再验证 | accept | 早识别分支判据 `manifest.asset_type === "shottimeline"` 是字面 string 比较;producer schema `asset_type: { const: "shottimeline" }` 已在源头锁定;consumer 侧再做 schema 校验是冗余 —— 仅需 warn 渲染未知 schema_version |
| T-03-SC | (n/a — Tampering) | npm/pip/cargo install | accept | 本 phase **零新包安装**(见 RESEARCH §Package Legitimacy Audit);`yarn install` 仅解析既有 package.json lockfile,无新依赖引入;无需 slopcheck / blocking-human checkpoint |
</threat_model>

<verification>
Phase 3 整体验证流程(全在 worktree cwd `/data/workspace/kst-canvas-consumer` 内):

1. **Task 1 完成后:** `du -sh scripts/fixtures/shot-timeline-ep01/` < 10MB;`node_modules/.bin/tsx` 存在;yarn install 成功
2. **Task 2 完成后:** `npx tsc --noEmit` 通过;grep 全部 7 个 hook 点都有匹配;`git diff --name-only origin/master..HEAD -- packages/infinite-canvas/` 为空
3. **Task 3 完成后:** `npx tsx scripts/verify-canvas-shot-timeline.ts` exit 0,全部 8 个 assert(A/B/C/D/E/E2/F)PASS
4. **Phase gate(进 /gsd:verify-work 前):**
   - `npx tsx scripts/verify-canvas-shot-timeline.ts` 全绿
   - `npm run verify:phase-46-contracts` 全绿(回归保护,确认未破坏既有 13 phase 逻辑)
   - `git diff --stat origin/master..HEAD -- packages/infinite-canvas/` 输出为空
   - `git diff origin/master..HEAD -- src/lib/canvasAssetSchema.ts` 输出为空(Zod 严格度字面零改动)
   - `git diff --stat origin/master..HEAD` 显示新增/修改文件 = 12 fixture 文件 + 1 verify 脚本 + 1 modified import-from-dir.ts(共 14 entries,无意外文件)
5. **Manual visual check(CANVAS-01/02 SC,可选 —— 进 Phase 4 端到端前自验):** 起 backend + frontend,POST `/api/canvas/v2/import-from-dir` 指向 fixture 目录,canvas 上看到一个 zone 包裹 N storyboard(蓝色 sequence 箭头按 shot_id 顺序)+ 3 audio + 1 video 子节点;无 FallbackNode(⚠ icon)
</verification>

<success_criteria>
**CANVAS-01 ✓** `extractShotTimelineArtifacts(fixture manifest)` 返回 `{nodes, links}` 含 1 zone + 1 summary + N storyboard + 3 audio + 1 video;scanWorkdirForArtifacts 早识别 asset.json 短路到 helper。

**CANVAS-02 ✓** ONE zone 父节点包裹所有子节点(per Pattern 2 Solution A —— RawArtifact.canvasType override 让异构 type 同处一个 zone);storyboard 之间按 shot_id 升序 emit sequence 边(`dataType:"data" + data.linkType:"sequence"`,字面复刻 flowDataMapper.ts:163-172);既有 autoLayout.ts 按 `data.phase`/`phaseIndex` 聚合 zone 子节点。

**CANVAS-03 ✓** 全部子节点通过 `validateGraphNodes`(per-type Zod 返回 0 errors);合成字段(engine="shot-timeline" / shot_type="scene" / shot_id="collection" 或 shots[i].id / resolution="1280x720" 或 "0x0")满足 required 列表而**不**放宽任何 Zod;前端 `packages/infinite-canvas/` 零改动;`canvasAssetSchema.ts` 严格度字面零改动(.optional() / .nullable() 计数未增加)。
</success_criteria>

<output>
Create `.planning/phases/03-canvas-consumer/03-01-SUMMARY.md` when done —— 记录:executed hooks(7 个)、verify 结果(8 asserts)、fixture 大小、production diff stat、与 CONTEXT/RESEARCH 决策的对齐情况(Solution A 选定、p13 选定、graceful-degrade 落地、additive-only 不变量验证)。
</output>
