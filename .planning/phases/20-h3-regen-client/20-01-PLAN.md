---
phase: 20-h3-regen-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analysis/roundtrip/workflow_fl2va.json
  - analysis/roundtrip/h3_regen.py
  - tests/test_h3_regen.py
  - run_pipeline.py
autonomous: true
requirements: [REGEN-01, REGEN-02]
must_haves:
  truths:
    - "deepcopy 注入后每镜 workflow 携带正确首/尾帧名、prompt_text、17k+5 对齐 length、确定性 seed（不经 subagent，直接 ComfyUI 原生 /prompt）"
    - "FakeComfyUI 全链路（/upload/image→/prompt→/history→/view）下 regen mp4 落盘 output/<stem>/roundtrip/shot_XXX_regen.mp4 且 >1KB"
    - "同 4-tuple + mp4>1KB 重跑 cache-hit 不重渲；该镜 prompt_text 改一字（prompt_version 变）即重渲该镜"
    - "预置 3/5 镜 cache 重跑只提交缺失 2 镜（断点续跑）"
    - "route_cache/warnings.json 双形 merge：上一轮 dict roundtrip 条目被 strip 重写，vision-seq 等陌生 str 条目原样保留"
    - "run_pipeline --force 清单新增 roundtrip/ 产物目录 + roundtrip.json sidecar（显式清单，绝不 glob 父级）"
  artifacts:
    - path: "analysis/roundtrip/workflow_fl2va.json"
      provides: "fl2va 9 节点 native KSampler 模板（数据与代码分离）"
      contains: "MiniMaxH3ImageToVideo"
    - path: "analysis/roundtrip/h3_regen.py"
      provides: "提交/轮询/下载全链路 + 4-tuple cache + 断点续跑 + warnings 双形 merge"
      min_lines: 300
    - path: "tests/test_h3_regen.py"
      provides: "离线单测（FakeComfyUI + subprocess fake，零真引擎）"
      min_lines: 250
  key_links:
    - from: "analysis/roundtrip/h3_regen.py"
      to: "analysis/roundtrip/workflow_fl2va.json"
      via: "Path(__file__).parent 同目录加载 + copy.deepcopy 注入"
      pattern: "workflow_fl2va\\.json"
    - from: "analysis/roundtrip/h3_regen.py"
      to: "ComfyUI /prompt /history /view"
      via: "stdlib urllib（_http_json）+ /view 二进制直读"
      pattern: "(/prompt|/history/|/view)"
    - from: "analysis/roundtrip/h3_regen.py"
      to: "route_cache/h3_regen/shot_XXX.json"
      via: "4-tuple cache 元数据（与 mp4 实体分离）"
      pattern: "h3_regen/shot_"
    - from: "run_pipeline.py"
      to: "work_dir/roundtrip/ + roundtrip.json"
      via: "--force 显式清理清单扩展"
      pattern: "roundtrip_dir"
---

<objective>
交付 h3 复现客户端的核心链路：fl2va workflow 模板（数据文件）+ h3_regen.py 单模块（模板注入 / 帧提取 / 上传 / 提交 / 轮询 / 下载 / 4-tuple cache / 断点续跑 / warnings 双形 merge）+ run_pipeline --force 清单扩展 + 全离线单测。

Purpose: REGEN-01（ComfyUI 原生 API 客户端，不经 subagent——p11b Pitfall 7）与 REGEN-02（per-shot cache + 断点续跑 + --force）的代码半边；VRAM guard 与抽样 CLI 在 20-02 叠加，真机 smoke 在 20-03。
Output: analysis/roundtrip/{h3_regen.py, workflow_fl2va.json}、tests/test_h3_regen.py、run_pipeline.py（--force 块扩展）。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-h3-regen-client/20-CONTEXT.md
@.planning/phases/20-h3-regen-client/20-RESEARCH.md
@.planning/phases/20-h3-regen-client/20-PATTERNS.md
@analysis/vision_seq_facets.py
@analysis/engine_clients/qwen_eye_client.py
@scripts/export_asset.py
</context>

<interfaces>
<!-- 执行者直接使用，无需再探索代码库 -->

prompts.json（ep01 实测形状）：顶层是 **list**（非 dict），93 条，每条含
  shot_id:int, start_sec:float, end_sec:float, duration:float, prompt_text:str, subject/action/camera/scene/lighting/style facets。
shots.json：list of {id:int, start_sec, end_sec, duration}（id 与 prompts.shot_id 一一对应）。
join 口径：shots.json 是分割权威（全镜列表来源），prompts.json 按 shot_id 供 prompt_text。

export_asset.py 已锁定的 warnings 合法双形（_valid_warnings_list，L70-91）：
  元素 = str（legacy）或 dict 且 keys ⊆ {code, detail}、code ∈ closed enum
  ("comfyui_unreachable", "vram_insufficient", "scorer_model_missing")、detail str。
  → TTS kill 审计 / 跳镜等事件性 [roundtrip] warning 一律用 str 前缀 "[roundtrip] ..."（legacy 形永远合法），
    dict 形只用于三码 enum 内的 degrade 记因。code 越界的 dict 会让整个 warnings sidecar 被 export 判 None。

video_content_hash（analysis/call_shot_analysis.py:91-108 权威，等价复制进 h3_regen）：
  sha256(首1MB + 尾1MB(若 size>2MB) + str(filesize))[:16]。ep01 h264.mp4 实测 = "ece64d62bcbc534a"。

qwen_eye_client._http_json 返回对：(status:int, body)——HTTPError 返回 (exc.code, body[:300])，
URLError/OSError 返回 (0, str(exc)[:300])。h3_regen 复刻为**模块级函数**（非 class）→ 测试单一 patch 点
monkeypatch.setattr(h3m, "_http_json", fake)。

kmc h3_batch_render_v4.py 只可照抄 4 点（poll 异常 continue / 60s 节流日志 / sleep 15s / size>1000 下限）。
四不可照抄：KAP multipart 提交、docker cp 下载、length 只进日志不下发、hash(sid) seed（RESEARCH Pitfall 2）。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: workflow_fl2va.json 模板 + h3_regen.py 渲染链核心</name>
  <files>analysis/roundtrip/workflow_fl2va.json, analysis/roundtrip/h3_regen.py</files>
  <read_first>
  - .planning/phases/20-h3-regen-client/20-RESEARCH.md §Code Examples「完整 fl2va workflow 模板」+「提交 + 轮询 + 下载骨架」（模板 JSON 逐字落盘，注入表照抄）
  - analysis/engine_clients/qwen_eye_client.py L113-131（_http_json 形状）
  - analysis/vision_seq_facets.py L98-123（常量块风格）、L150-161（video_content_hash）
  - .planning/phases/20-h3-regen-client/20-PATTERNS.md §A（模块骨架）与 kmc 四不可照抄表
  </read_first>
  <behavior>
  - h3_frame_count：0.5s→124（floor）、6.73s→175、5.5s→141、19.7s→362（cap）；全部满足 n%17==5 且 124≤n≤362
  - derive_seed(vch, shot_id) 跨进程确定（int(sha256(f"{vch}:{shot_id}")[:8],16) % 2**31），异镜异 seed
  - build_workflow 注入后：node 14/15 image=上传名、node 20 prompt/width/height/length、node 30 seed/steps=15/cfg=1.0/euler/simple、node 50 filename_prefix；模板对象与磁盘文件不被改动（deepcopy 证明）
  - 提交/轮询/下载对 FakeHTTP 全链路：images 键（本 build SaveVideo 序列化处）取 type==output 且 .mp4 的确切 filename
  </behavior>
  <action>
  1) 新建 analysis/roundtrip/workflow_fl2va.json：RESEARCH §Code Examples 的 9 节点 JSON（节点 10/11/12/13/14/15/20/21/30/40/41/42/50）**逐字落盘**，占位符 &lt;FF_FILENAME&gt;/&lt;LF_FILENAME&gt;/&lt;PROMPT_TEXT&gt;/&lt;PREFIX&gt; 原样保留。不改节点拓扑——sampler=euler/scheduler=simple/steps=15/cfg=1.0/shift_video=12.0 是 CONTEXT SC1 + Pitfall 7 锁定值；unet 用 minimax_h3_fl2va_int8_convrot.safetensors。
  2) 新建 analysis/roundtrip/h3_regen.py（本 task 只写渲染链函数，cache/main 在 Task 2）：
     - 模块 docstring（中文）：背景 / 算法步骤 / graceful-degrade 语义 / cache 惯例 / argv 用法；**记入 Pitfall 6 交互**——vision_seq 的 warnings 读法会丢 dict 条目，本模块自带双形 merge 不受影响。
     - imports：argparse, copy, hashlib, json, os, subprocess, sys, time, signal, urllib.error, urllib.parse, urllib.request, pathlib.Path。全 stdlib（不引 requests——RESEARCH Pitfall 5）。
     - 常量：COMFY_URL_DEFAULT="http://127.0.0.1:8188"、ENGINE_NAME="comfyui-fl2va"、ENGINE_VERSION_TEMPLATE="fl2va-int8/euler+simple/15/{width}x{height}"（Q3 裁决：model+sampler+scheduler+steps+resolution 冻进版本串，cache 保持 4-tuple 不设第 5 维）、STEP_TAG="[roundtrip]"、_ROUNDTRIP_WARNING_CODES=（export_asset 三码）、FPS=24、LENGTH_MIN=124/LENGTH_MAX=362/LENGTH_STEP=17/LENGTH_MOD=5、LAST_FRAME_GUARD_SEC=0.04、MIN_MP4_BYTES=1024、MAX_PIXELS=1032192、NODE_FIRST="14"/NODE_LAST="15"/NODE_I2V="20"/NODE_SAMPLER="30"/NODE_SAVE="50"、TEMPLATE_PATH=Path(__file__).resolve().parent/"workflow_fl2va.json"。
     - 函数（全部 verb-noun、带类型注解）：
       video_content_hash(video_path)（照抄 vision_seq L150-161）；
       load_template() → json.load(TEMPLATE_PATH)；
       build_engine_version(width, height) → ENGINE_VERSION_TEMPLATE.format(...)；
       h3_frame_count(duration_sec, fps=FPS)：n=round(duration*fps)；while n%17!=5: n+=1；再 max(124, min(n, 362))（124/362 恰在网格上，clamp 不破网格）；
       derive_seed(vch, shot_id)；
       parse_resolution(s) → (w,h)（"WxH" split int，非法 sys.exit 中文报错）+ validate_resolution(w,h)：w%32==0 且 h%32==0 且 w*4==h*7（严格 7:4）且 w*h<=MAX_PIXELS；
       build_workflow(template, ff_name, lf_name, prompt_text, width, height, length, seed, prefix)：copy.deepcopy → 改上述五组节点 inputs → 返回新 dict（**prompt_text 只经 json.dumps 进 payload，绝不进 shell 字符串**——security 约束）；
       load_shot_prompts(work_dir)：读 shots.json（权威镜列表）+ prompts.json（按 shot_id join 出 prompt_text）；缺 prompt_text 的镜跳过并产 str warning；
       resolve_source_video(work_dir)：h264.mp4 优先、缺席回退 video.mp4、都没有 sys.exit；
       extract_endpoint_frames(src_video, shot, vch, frames_dir)：ffmpeg list-form subprocess（["ffmpeg","-y","-ss",f"{ts:.3f}","-i",src,"-frames:v","1","-q:v","2",dest,"-loglevel","error"], capture_output, timeout=60）——**绝不带 -vf/scale**（Pitfall 4：shot_frames 480×270 不可用，必须全分辨率重提）；首帧 ts=start_sec，尾帧 ts=max(start_sec, end_sec-LAST_FRAME_GUARD_SEC)；落盘 frames_dir/kst_{vch}_shot{NNN:03d}_ff.jpg 与 _lf.jpg（确定性名，NNN=shot_id 三位零填充）；
       upload_image(path, comfy_url)：subprocess.run(["curl","-s","-X","POST",f"{comfy}/upload/image","-F",f"image=@{path}","-F","type=input","-F","overwrite=true"], capture_output=True, text=True, timeout=120) → json.loads(stdout)["name"]（Pitfall 5：GET 该路由 404 属正常，只 POST）；
       _http_json(url, payload=None, timeout=30.0)：模块级函数，逐字复刻 qwen_eye_client L113-131 的 (status, body) 语义；
       submit_prompt(wf, comfy_url)：POST {comfy}/prompt body {"prompt": wf} → 有 "prompt_id" 返回之；否则 raise RuntimeError 附 node_errors 前若干条（kmc h3_ref2va 同款解析）；
       poll_and_fetch(prompt_id, comfy_url, timeout_s, shot_label)：循环 time.sleep(15)（PITFALL: 瞬态 urlopen 异常 continue——kmc crash-safe）；GET {comfy}/history/{prompt_id} → entry.status.status_str=="error" → {"ok":False,"error":str(messages)[:2000]}；status.completed → 遍历 outputs 所有 node 的 ("images","gifs","videos") 三键（Pitfall 3：本 build 在 images），取 type=="output" 且 filename.endswith(".mp4") 的**确切条目** {"ok":True,"filename":...,"subfolder":...}；超时返回 None；每 ~60s 打一条 [roundtrip] 渲染中日志（含 elapsed 秒）；
       sanitize_view_filename(filename)：拒含 "/" "\\" ".." 且必须 .mp4 结尾（security：/view 下载文件名净化）；
       _view_download(item, comfy_url, dest)：urllib.parse.urlencode({filename, subfolder, type:"output"}) → urlopen(f"{comfy}/view?{q}", timeout=300) → 写 dest（二进制直写；实测 byte-identical 路径）。
  </action>
  <verify>
    <automated>python3 -m py_compile analysis/roundtrip/h3_regen.py &amp;&amp; python3 -c "
import importlib.util, json
from pathlib import Path
spec = importlib.util.spec_from_file_location('h3_regen', 'analysis/roundtrip/h3_regen.py')
h3m = importlib.util.module_from_spec(spec); spec.loader.exec_module(h3m)
tpl = h3m.load_template()
assert len(tpl) == 12, len(tpl)
assert tpl['20']['class_type'] == 'MiniMaxH3ImageToVideo'
assert h3m.h3_frame_count(6.73) == 175 and h3m.h3_frame_count(0.5) == 124 and h3m.h3_frame_count(5.5) == 141 and h3m.h3_frame_count(19.7) == 362
for n in (124,175,141,362): assert n % 17 == 5, n
assert h3m.derive_seed('ece64d62bcbc534a', 1) == h3m.derive_seed('ece64d62bcbc534a', 1) != h3m.derive_seed('ece64d62bcbc534a', 2)
assert h3m.derive_seed('x', 1) < 2**31
assert h3m.build_engine_version(1344, 768) == 'fl2va-int8/euler+simple/15/1344x768'
wf = h3m.build_workflow(tpl, 'ff.jpg', 'lf.jpg', '中文 prompt 测试', 1344, 768, 175, 42, 'kst_t_s001')
assert wf['14']['inputs']['image'] == 'ff.jpg' and wf['20']['inputs']['length'] == 175
assert wf['20']['inputs']['prompt'] == '中文 prompt 测试' and wf['30']['inputs']['seed'] == 42
assert wf['30']['inputs']['steps'] == 15 and wf['30']['inputs']['cfg'] == 1.0
assert tpl['14']['inputs']['image'] == '<FF_FILENAME>'  # deepcopy 未污染模板
assert h3m.sanitize_view_filename('a.mp4') and not h3m.sanitize_view_filename('../a.mp4') and not h3m.sanitize_view_filename('a/b.mp4')
h3m.validate_resolution(896, 512) and h3m.validate_resolution(1344, 768)
print('20-01 T1 OK')
"</automated>
  </verify>
  <acceptance_criteria>
  - workflow_fl2va.json 含 12 个节点键（10,11,12,13,14,15,20,21,30,40,41,42,50），node 20 class_type == "MiniMaxH3ImageToVideo"，node 30 sampler_name=="euler" scheduler=="simple" steps==15 cfg==1.0，node 21 shift_video==12.0
  - h3_regen.py 无第三方 import（grep 不到 "import requests"）；subprocess 全部 list-form
  - 上述 python -c 断言全过（length 网格 / seed 确定性 / 注入 / deepcopy / 文件名净化 / 分辨率合法）
  </acceptance_criteria>
  <done>渲染链核心函数齐备且纯函数行为全部断言通过；模板数据与代码分离落盘。</done>
</task>

<task type="auto">
  <name>Task 2: 4-tuple cache + 断点续跑 main() + warnings 双形 merge + run_pipeline --force 扩展</name>
  <files>analysis/roundtrip/h3_regen.py, run_pipeline.py</files>
  <read_first>
  - analysis/vision_seq_facets.py L229-238（_cache_key）、L259-284（miss 即全新）、L287-340（read-merge-write + 原子写）、L619-644（批循环 + degrade）、L729-738（收尾 print）
  - .planning/phases/20-h3-regen-client/20-RESEARCH.md §Code Examples「warnings 追加（双形保留 merge）」
  - run_pipeline.py L740-790（--force 现有块——本 task 在此扩展）
  - scripts/export_asset.py L63-91（_ROUNDTRIP_WARNING_CODES + 双形合法规范）
  </read_first>
  <behavior>
  - cache hit：4-tuple dict 全等 + roundtrip/shot_XXX_regen.mp4 存在且 size>1KB；任一不满足即 miss 重渲（**不做 ffprobe 深检**——CONTEXT 锁定）
  - prompt_version = sha256(prompt_text)[:8]（per-shot；改一字必变）
  - 断点续跑：预置 N-k 镜 cache 后重跑，仅缺失 k 镜发生 /prompt 提交
  - warnings merge：strip 规则 = dict 且 code ∈ 三码 enum **或** str 以 "[roundtrip]" 开头；其余（如 [vision-seq] str）原样保留
  </behavior>
  <action>
  1) h3_regen.py 追加（Task 1 函数之上）：
     - prompt_version_for(prompt_text) = hashlib.sha256(prompt_text.encode("utf-8")).hexdigest()[:8]；
     - cache 路径 helper：work_dir/route_cache/h3_regen/shot_{NNN:03d}.json（NNN=shot_id 三位零填充）；mp4 产物 work_dir/roundtrip/shot_{NNN:03d}_regen.mp4（regen.path 相对 asset root——Phase 18 契约）；
     - cache_read(shot_id, work_dir, key4) → dict|None：读文件，_cache_key 形状的 4-tuple（video_content_hash/engine_name/engine_version/prompt_version）全等才继续，否则 miss；
     - cache_is_hit(meta, mp4_path)：meta 非 None 且 os.path.isfile(mp4_path) 且 getsize>MIN_MP4_BYTES；
     - cache_write(shot_id, work_dir, meta)：含 4-tuple + prompt_id + seed + length + width/height + mp4_sha256（hashlib.sha256 分块读产物）+ rendered_at（time.strftime ISO）；tmp + os.replace 原子写（mirror vision_seq L333-338，indent=2 ensure_ascii=False）；
     - append_roundtrip_warnings(work_dir, new_entries)：READ route_cache/warnings.json（OSError/JSONDecodeError → 空）→ strip 上一轮（Behavior 规则）→ 追加 new_entries（str 与 dict 双形都收）→ 原子写回 {"warnings": merged}；**绝不照抄 vision_seq 的 isinstance(w,str) 过滤**（Pitfall 6 会丢 dict 条目）；
     - main()：argparse——--work-dir（required，资产根 output/<video-stem>/）、--comfy-url（default COMFY_URL_DEFAULT）、--force（store_true）、--shot-timeout（int 秒，default 0=自动 900+3*length——Pitfall 8 超时按帧数缩放）；全部中文 help=。
       流程：resolve_source_video → video_content_hash → load_shot_prompts →（--force：显式清单删 route_cache/h3_regen/ 整目录 + roundtrip/ 整目录 + roundtrip.json 文件，print [force] 行）→ ComfyUI 可达性 gate：_http_json GET {comfy}/system_stats 非 200 → append {code:"comfyui_unreachable", detail:...} warning + return 0（graceful-degrade；VRAM guard 序列在 20-02 插入此位置之前）→ 批循环（本 plan 处理全部镜；抽样在 20-02）：每镜 prompt_version_for → cache key4（engine_version=build_engine_version(1344,768) 本 plan 固定默认分辨率，--regen-resolution flag 在 20-02 接入）→ cache hit → print "[roundtrip] shot N: cache hit, skipping" + continue；miss → extract_endpoint_frames（frames_dir=route_cache/h3_regen/frames/）→ upload_image ×2 → build_workflow → submit_prompt → poll_and_fetch → ok 且 sanitize 过 → _view_download 到 mp4 产物路径 → size>MIN_MP4_BYTES 复核 → cache_write；error/超时 → print 失败行 + failed 清单（in-memory list）+ continue（单镜失败不阻塞批——vision_seq L660 形态）；done 计数每 10 镜 print 进度。
       收尾：append_roundtrip_warnings flush 本轮 str/dict 条目（跳镜 str 在 20-02 产生；本 plan 至少 flush degrade/失败摘要）→ summary print（rendered/cache-hit/failed 计数 + 产物目录路径）→ return 0。
  2) run_pipeline.py --force 块扩展（L740 变量区 + L768 元组）：
     - 变量区加：roundtrip_dir = os.path.join(work_dir, "roundtrip") 与 roundtrip_json = os.path.join(work_dir, "roundtrip.json")，带 "Phase 20" 注释；
     - for 元组追加两项：roundtrip_dir（目录 rmtree）与 roundtrip_json（文件 unlink），各带 "# Phase 20" 尾注；
     - mirror Phase 19 注释先例加两行：route_cache 整目录 rmtree 已天然覆盖 route_cache/h3_regen/ 子目录（h3 复现元数据 cache）——无需单列进清单；
     - **守住 T-14-01 纪律：EXPLICIT LIST，绝不 glob/rmtree 父级。**
  </action>
  <verify>
    <automated>python3 -m py_compile analysis/roundtrip/h3_regen.py run_pipeline.py &amp;&amp; python3 -c "
import importlib.util, json, os, tempfile
from pathlib import Path
spec = importlib.util.spec_from_file_location('h3_regen', 'analysis/roundtrip/h3_regen.py')
h3m = importlib.util.module_from_spec(spec); spec.loader.exec_module(h3m)
assert h3m.prompt_version_for('abc') == h3m.prompt_version_for('abc') != h3m.prompt_version_for('abd')
assert len(h3m.prompt_version_for('abc')) == 8
with tempfile.TemporaryDirectory() as td:
    w = Path(td)
    sc = w / 'route_cache'; sc.mkdir()
    (sc / 'warnings.json').write_text(json.dumps({'warnings': ['[vision-seq] old', {'code': 'comfyui_unreachable', 'detail': 'stale'}]}, ensure_ascii=False), encoding='utf-8')
    h3m.append_roundtrip_warnings(str(w), [{'code': 'vram_insufficient', 'detail': 'now'}])
    got = json.load(open(sc / 'warnings.json'))['warnings']
    assert '[vision-seq] old' in got and any(isinstance(x, dict) and x['code'] == 'vram_insufficient' for x in got)
    assert not any(isinstance(x, dict) and x['code'] == 'comfyui_unreachable' for x in got)  # 上一轮被 strip
    mp4 = w / 'roundtrip'; mp4.mkdir(); f = mp4 / 'shot_001_regen.mp4'; f.write_bytes(b'x' * 2048)
    meta = {'video_content_hash': 'a' * 16, 'engine_name': 'comfyui-fl2va', 'engine_version': 'E', 'prompt_version': 'deadbeef'}
    h3m.cache_write(1, str(w), dict(meta, prompt_id='p', seed=1, length=124, width=1344, height=768, mp4_sha256='0'))
    assert h3m.cache_is_hit(h3m.cache_read(1, str(w), meta), str(f))
    bad = dict(meta, prompt_version='00000000')
    assert h3m.cache_read(1, str(w), bad) is None  # 4-tuple 不匹配即 miss
    f.write_bytes(b'x' * 500)
    assert not h3m.cache_is_hit(h3m.cache_read(1, str(w), meta), str(f))  # size 下限
src = Path('run_pipeline.py').read_text(encoding='utf-8')
i = src.index('if args.force:')
block = src[i:i + 2500]
assert 'roundtrip_dir' in block and 'roundtrip_json' in block and 'h3_regen' in block
print('20-01 T2 OK')
"</automated>
  </verify>
  <acceptance_criteria>
  - prompt_version 8-hex 且 per-shot；cache miss-on-tuple-mismatch / miss-on-small-mp4 断言通过
  - warnings merge 保留 [vision-seq] str、strip 上一轮 dict、追加新 dict（断言通过）
  - run_pipeline --force 块（if args.force: 起 2500 字符内）含 roundtrip_dir、roundtrip_json 与 h3_regen 注释；清单仍为显式枚举（无 glob/os.system）
  - h3_regen main() 对 system_stats 非 200 返回 0（FakeHTTP 单测在 Task 3 覆盖）
  </acceptance_criteria>
  <done>cache/断点续跑/warnings/force 全链路在模块内闭合；run_pipeline --force 清单覆盖 regen cache 与产物。</done>
</task>

<task type="auto">
  <name>Task 3: 离线单测 tests/test_h3_regen.py（FakeComfyUI + subprocess fake）</name>
  <files>tests/test_h3_regen.py</files>
  <read_first>
  - tests/test_vision_seq_facets.py L17-161（importlib 加载 / make_workdir / run_main argv 注入 / cache-hit 二跑零引擎调用用例）
  - tests/test_qwen_eye_client.py L20-105（FakeHTTP 记录式替身 / no_engine subprocess fake / monkeypatch time.sleep）
  </read_first>
  <behavior>
  - test_workflow_injection_*：真实模板 + build_workflow 注入五组节点；模板磁盘文件未被改动
  - test_h3_frame_count_*：{0.5:124, 5.5:141, 6.73:175, 19.7:362} + 网格不变式
  - test_seed_deterministic / test_extract_frames_no_scale（ffmpeg argv 无 -vf 无 scale，list-form，dest 在 route_cache/h3_regen/frames/）/ test_upload_curl_args（curl -F image=@ + overwrite=true）
  - test_submit_poll_download_success：FakeHTTP 序列 /prompt→/history(success, images 键)→patch _view_download 写字节；mp4 落盘 + cache 写入 + prompt_id 进 cache
  - test_history_error_branch / test_history_timeout：status{failed} 路径不抛异常、批继续
  - test_cache_hit_second_run_zero_submissions：二跑换新 FakeHTTP，断言新 fake.calls 中无 "/prompt"（断点续跑 unit 版）
  - test_prompt_version_change_invalidates / test_cache_requires_mp4_over_1kb
  - test_resume_only_missing：预置 3/5 镜 cache → 仅 2 次 /prompt 提交
  - test_warnings_dual_shape_merge（mirror Task 2 verify 场景进 pytest）/ test_comfyui_unreachable_exit0（system_stats 非 200 → rc==0 + warning dict）
  </behavior>
  <action>
  新建 tests/test_h3_regen.py，mirror test_vision_seq_facets.py 结构：
  - importlib.util.spec_from_file_location("h3_regen", REPO_ROOT/"analysis"/"roundtrip"/"h3_regen.py") 加载为 h3m（不污染 sys.path）；REPO_ROOT = Path(__file__).resolve().parent.parent。
  - make_workdir(tmp_path, n_shots=5)：写 shots.json（[{id:i+1, start_sec, end_sec, duration}...]）、prompts.json（[{shot_id, prompt_text:"测试 prompt {i}", start_sec, end_sec, duration}...]——**顶层是 list**）、假 h264.mp4（写几字节即可，提取被 fake）。
  - run_main(work, extra_args=None)：argv 注入 h3m.main()，sys.argv save/restore；返回 rc。
  - FakeHTTP（记录式 _http_json 替身，mirror test_qwen_eye_client L26-38）：按调用序回放 (status, body)；记录 (url, payload)。
  - fake_subprocess(calls)：替身 h3m.subprocess.run，按 cmd[0]/cmd[1] 分发——curl → stdout 返回 {"name": basename}；ffmpeg → 写出目标 jpg 文件（从 cmd 倒数第一个 .jpg 参数）；ss/nvidia-smi 留 20-02 扩展。
  - monkeypatch：h3m._http_json、h3m.subprocess.run、h3m.time.sleep（lambda s: None）、h3m._view_download（直接写 dest 字节 b"x"*2048）。
  - fake history entry 内联三形：success（outputs 含 images:[{filename:"kst_t_s001_00001_.mp4", subfolder:"", type:"output"}]）、error（status_str="error"）、空（触发超时——timeout 参数给 1 秒量级避免慢测）。
  - 断点续跑用例：跑两次 run_main，第二次新 FakeHTTP，断言 fake2.calls 无 "/prompt" 且 stdout 捕获 cache-hit 行（capsys）。
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_h3_regen.py -x -q &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - pytest tests/test_h3_regen.py -x -q 全绿，且无真网络/真 GPU（全 monkeypatch，单测总耗时 <15s）
  - 至少覆盖 behavior 列出的 12+ 用例；grep -c "def test_" tests/test_h3_regen.py ≥ 12
  - 全套件 python3 -m pytest tests/ -q 维持绿（不破坏既有 9 个测试文件）
  </acceptance_criteria>
  <done>REGEN-01/02 的离线证据闭合：注入/链路/cache/续跑/warnings/降级全有自动化断言。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kst 客户端 → ComfyUI :8188 | 本机环回 HTTP，无鉴权（ComfyUI 原生形态）；服务端返回（history filename）按不可信数据校验后使用 |
| 客户端 → 宿主 OS subprocess | ffmpeg / curl 均以参数 list 调用；无 shell 插值 |
| ComfyUI 响应 → 文件系统 | /view 下载字节落盘 work_dir 内自构造路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | prompt_text → workflow | mitigate | prompt_text 仅经 json.dumps 进 POST body（submit_prompt），绝不出现在任何 subprocess argv/字符串拼接中 |
| T-20-02 | Tampering | /view 下载 filename | mitigate | sanitize_view_filename 拒分隔符与 ".."、强制 .mp4 后缀；本地落盘路径由客户端自构造（shot_{NNN}_regen.mp4），不采用服务端文件名 |
| T-20-03 | Information Disclosure | warnings/error detail | mitigate | detail 只记异常类名 + 状态码 + 显存数值（无 env/token/路径外内容）；roundtrip.json status.error ≤2000 字符（schema T-18-02 上界） |
| T-20-04 | Tampering | run_pipeline --force | mitigate | EXPLICIT LIST 纪律（T-14-01）：只加 roundtrip_dir/roundtrip_json 两个显式条目，绝不 glob/rmtree 父级 |
| T-20-SC | Tampering | 包安装 | accept | 本 plan 零新包（全 stdlib + 系统 curl/ffmpeg）；无 npm/pip install 任务，slopcheck gate 不适用 |
</threat_model>

<verification>
- python3 -m pytest tests/test_h3_regen.py -x -q 全绿；python3 -m pytest tests/ -q 全绿
- Task 1/2 的 python -c 断言脚本通过（length 网格 / seed / 注入 / deepcopy / cache / warnings merge / force 清单）
- grep -c "def test_" tests/test_h3_regen.py ≥ 12
- 模板文件加载后与磁盘逐字节一致（deepcopy 不污染）
</verification>

<success_criteria>
- REGEN-01 代码半边：模板 + 提交/轮询/下载链 + FakeComfyUI 全链路单测（真机证明在 20-03）
- REGEN-02 完整：4-tuple per-shot cache + prompt_version 变化失效 + 断点续跑 + run_pipeline --force 清单扩展 + 客户端自身 --force
- 不经 subagent / 不经 KAP / 不用 websocket——grep 不到 ":10588"（KAP）与 "websocket"
</success_criteria>

<output>
Create `.planning/phases/20-h3-regen-client/20-01-SUMMARY.md` when done
</output>
