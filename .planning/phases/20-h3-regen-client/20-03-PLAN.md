---
phase: 20-h3-regen-client
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - analysis/roundtrip/h3_regen.py
  - tests/test_h3_regen.py
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip.json"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip/shot_001_regen.mp4"
  - "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip/shot_047_regen.mp4"
autonomous: false
requirements: [REGEN-01, REGEN-02, REGEN-04]
must_haves:
  truths:
    - "ep01 真机 --sample-shots 2 --regen-resolution 896x512：shots 1 与 47 各回收一个 >1KB 的 regen mp4（fl2va euler+simple+17k+5 length 真提交真下载）"
    - "同命令重跑：2 镜全 cache-hit、零新 /prompt 提交（SC1 断点续跑真机实证）"
    - "roundtrip.json 写 regen 半边（shots[]{shot_id, regen|status}，scores/verdict 缺席 = schema 合法 degrade 中间态——Open Q2 裁决）且过 roundtrip.schema.json 校验"
    - "每镜 cache 元数据含 post_render_free_mib 渲后水位（Open Q1 数据留档，进 SUMMARY 报告）"
    - "目视抽检通过：2 镜首尾帧 condition 与运动合理贴合原镜（Kai 确认）"
  artifacts:
    - path: "analysis/roundtrip/h3_regen.py"
      provides: "write_roundtrip_sidecar（regen 半边 + schema 写前自校验 + READ-merge）"
      contains: "roundtrip.schema.json"
    - path: "tests/test_h3_regen.py"
      provides: "sidecar schema/merge 单测"
      min_lines: 450
    - path: "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip.json"
      provides: "ep01 真机 smoke 产物 sidecar"
      contains: "shot_id"
    - path: "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip/shot_001_regen.mp4"
      provides: "真机 regen mp4（smoke 证据）"
  key_links:
    - from: "analysis/roundtrip/h3_regen.py"
      to: "spec/schemas/roundtrip.schema.json"
      via: "SCHEMA_PATH = Path(__file__).resolve().parent.parent.parent（off-by-one 修正：analysis/roundtrip/ 比 vision_seq 深一层）+ Draft202012Validator 写前自校验"
      pattern: "parent\\.parent\\.parent"
    - from: "analysis/roundtrip/h3_regen.py"
      to: "scripts/export_asset.py"
      via: "importlib 加载 SCHEMA_VERSION（单源，勿复制字面量）"
      pattern: "SCHEMA_VERSION"
    - from: "analysis/roundtrip/h3_regen.py"
      to: "output/<stem>/roundtrip.json"
      via: "批末 READ-merge（按 shot_id 合并，保留 Phase 21 未来 scores/verdict 字段）+ 原子写"
      pattern: "roundtrip\\.json"
---

<objective>
闭环 Phase 20：roundtrip.json regen 半边写入（Phase 18 契约的运行时兑现，Open Q2 裁决的 degrade 中间态）+ ep01 真 ComfyUI smoke（--sample-shots 2 级，896×512）+ 断点续跑真机实证 + 目视抽检 checkpoint。

Purpose: SC1 的端到端真机证明（提交→回收→cache-hit 重跑）与 SC5 的降分辨率验证模式实证；SC4 的 --force/断点续跑在真数据上的可观察行为。GPU 时长预期：896×512 @15steps 每镜约 3-8 分钟 + 首镜模型加载 1-3 分钟——**两镜总 wall time 预期 10-30 分钟**，与 5-8min/镜量级一致；这是本 phase 唯一真 GPU 任务。
Output: write_roundtrip_sidecar + 单测扩展；ep01 真机 roundtrip/ 产物 2 件 + roundtrip.json + smoke 证据进 SUMMARY。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-h3-regen-client/20-CONTEXT.md
@.planning/phases/20-h3-regen-client/20-RESEARCH.md
@spec/schemas/roundtrip.schema.json
@analysis/vision_seq_facets.py
</context>

<interfaces>
<!-- roundtrip.schema.json 本 plan 写入目标的关键约束（已从 schema 文件核对） -->

顶层：{schema_version: string(pattern semver-lite), shots: array}，additionalProperties:false。
shots[].required = ["shot_id"]（其余 optional = degrade 中间态合法）。
regen.required = [path, video_content_hash, engine_name, engine_version, prompt_version]；
  path.pattern = ^(?!.*\.\.)([^/]+/)*roundtrip/[^:*?\"&lt;&gt;|]+\.mp4$ → "roundtrip/shot_001_regen.mp4" 合法；
  video_content_hash.pattern = ^[0-9a-f]{16}$。
status = {state: "failed"(单值 enum), error: str 1..2000}；regen 与 status 互斥（producer 保证）。
scores/verdict 本 phase 不写（Phase 21 增量补）。
schema_version 取 scripts/export_asset.py 的 SCHEMA_VERSION（单源纪律——STATE 载明勿复制字面量）。

vision_seq 写前自校验先例（L703-713）：from jsonschema import Draft202012Validator → iter_errors → 前 3 错误 sys.exit → tmp + os.replace。
**路径陷阱（PATTERNS §F 已标）**：h3_regen.py 在 analysis/roundtrip/ 下，比 vision_seq 深一层——repo root 必须 .parent.parent.parent。
ep01 真机锚点：work dir 名含中文/空间/括号（命令必须加引号）；uniform_sample 2 → shots [1, 47]（6.73s / 3.2s，均 ≤10s 必渲）。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: write_roundtrip_sidecar（regen 半边 + READ-merge + schema 写前自校验）</name>
  <files>analysis/roundtrip/h3_regen.py</files>
  <read_first>
  - analysis/vision_seq_facets.py L703-717（Draft202012Validator 写前自校验 + 原子写）
  - spec/schemas/roundtrip.schema.json（全文——本 task 的校验目标）
  - scripts/export_asset.py L59-62（SCHEMA_VERSION 单源位置）
  - analysis/roundtrip/h3_regen.py 前置版本（main 批循环结果收集结构）
  </read_first>
  <behavior>
  - rendered 镜 → {shot_id, regen:{path, video_content_hash, engine_name, engine_version, prompt_version, duration_sec, width, height}}；failed 镜 → {shot_id, status:{state:"failed", error}}；未尝试（抽样外/跳过）→ 条目缺席（schema：结果集不是任务队列）
  - READ-merge：既有 roundtrip.json 里同 shot_id 条目的 scores/verdict 等 Phase 21 字段**原样保留**，只替换 regen/status 半边；JSON 损坏 → 视为空重建（Phase 18 决策：挂载前仅 JSON-parse）
  - duration_sec 来自 ffprobe（失败 0.0——repo probe_duration 吞错先例）
  - 写前 Draft202012Validator 全量 iter_errors → 有错 sys.exit 不落盘；schema_version 从 export_asset 导入
  </behavior>
  <action>
  h3_regen.py 追加：
  - 常量 SCHEMA_PATH = Path(__file__).resolve().parent.parent.parent / "spec" / "schemas" / "roundtrip.schema.json"（**三层 parent**——analysis/roundtrip/ 深一层修正）；
  - _load_schema_version() → str：importlib.util.spec_from_file_location 从 scripts/export_asset.py 加载模块取 SCHEMA_VERSION 属性（单源；加载失败 sys.exit 中文报错）；
  - probe_duration_sec(path) → float：subprocess ["ffprobe","-v","quiet","-show_entries","format=duration","-of","csv=p=0",path] 捕获输出 float，(ValueError, AttributeError, subprocess 异常) → 0.0；
  - build_sidecar_entries(results, vch, width, height) → list[dict]：results 为本轮渲染/缓存命中的镜集合（cache meta 或刚写入的 meta），regen.path=f"roundtrip/shot_{NNN:03d}_regen.mp4"；failed 用例带 error 文本截 2000；
  - write_roundtrip_sidecar(work_dir, entries)：读 work_dir/roundtrip.json（OSError/JSONDecodeError → {"shots": []}）→ by shot_id merge（Behavior 规则）→ 组 {"schema_version": _load_schema_version(), "shots": merged_sorted_by_shot_id} → Draft202012Validator(json.load(SCHEMA_PATH)).iter_errors 校验（错误前 3 条 sys.exit）→ tmp + os.replace 原子写（indent=2 ensure_ascii=False）。
  main() 收尾接线：summary print 之前调 write_roundtrip_sidecar（含 cache-hit 镜——从 cache meta 重建条目，保证断点续跑后 sidecar 完整）。
  </action>
  <verify>
    <automated>python3 -m py_compile analysis/roundtrip/h3_regen.py &amp;&amp; python3 -c "
import importlib.util, json, tempfile
from pathlib import Path
spec = importlib.util.spec_from_file_location('h3_regen', 'analysis/roundtrip/h3_regen.py')
h3m = importlib.util.module_from_spec(spec); spec.loader.exec_module(h3m)
assert str(h3m.SCHEMA_PATH).endswith('spec/schemas/roundtrip.schema.json') and h3m.SCHEMA_PATH.exists()
assert h3m._load_schema_version() == '1.3'
with tempfile.TemporaryDirectory() as td:
    w = Path(td)
    pre = {'schema_version': '1.3', 'shots': [{'shot_id': 3, 'scores': {'midframe_sim': {'score': 0.9, 'model': 'clip'}}, 'verdict': {'decision': 'accepted', 'source': 'human'}}]}
    (w / 'roundtrip.json').write_text(json.dumps(pre, ensure_ascii=False), encoding='utf-8')
    mp4dir = w / 'roundtrip'; mp4dir.mkdir(); (mp4dir / 'shot_001_regen.mp4').write_bytes(b'x' * 8)
    entries = [{'shot_id': 1, 'regen': {'path': 'roundtrip/shot_001_regen.mp4', 'video_content_hash': 'a' * 16, 'engine_name': 'comfyui-fl2va', 'engine_version': 'fl2va-int8/euler+simple/15/896x512', 'prompt_version': 'deadbeef', 'duration_sec': 5.2, 'width': 896, 'height': 512}}, {'shot_id': 2, 'status': {'state': 'failed', 'error': 'render timeout'}}]
    h3m.write_roundtrip_sidecar(str(w), entries)
    got = json.load(open(w / 'roundtrip.json'))
    by = {s['shot_id']: s for s in got['shots']}
    assert 3 in by and by[3].get('scores') == pre['shots'][0]['scores']  # Phase 21 字段保留
    assert by[1]['regen']['prompt_version'] == 'deadbeef' and by[2]['status']['state'] == 'failed'
    from jsonschema import Draft202012Validator
    schema = json.load(open(h3m.SCHEMA_PATH))
    assert not list(Draft202012Validator(schema).iter_errors(got))
print('20-03 T1 OK')
"</automated>
  </verify>
  <acceptance_criteria>
  - SCHEMA_PATH 三层 parent 命中 spec/schemas/roundtrip.schema.json（存在性断言通过）
  - merge 保留既有 scores/verdict 字段；新 regen/status 半边落位；结果过 schema 校验（断言通过）
  - schema_version 来自 export_asset 单源（值 '1.3' 断言通过；h3_regen.py 内 grep 不到 SCHEMA_VERSION = \"1.3\" 字面量赋值）
  </acceptance_criteria>
  <done>Open Q2 裁决落地：客户端批末写 regen 半边，schema 合法 degrade 中间态，Phase 21 增量补齐无全量重写负担。</done>
</task>

<task type="auto">
  <name>Task 2: sidecar 单测扩展 + ep01 真 ComfyUI smoke（--sample-shots 2）与 cache-hit 重跑实证</name>
  <files>tests/test_h3_regen.py, "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip.json", "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip/shot_001_regen.mp4", "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip/shot_047_regen.mp4"</files>
  <read_first>
  - tests/test_h3_regen.py 前置版本（run_main/FakeHTTP 既有设施）
  - .planning/phases/20-h3-regen-client/20-RESEARCH.md §Environment Availability（:8188 探活方式 / GPU1 实况）与 §ep01 数据盘点
  </read_first>
  <behavior>
  - 单测：test_sidecar_schema_valid_degenerate（regen-only + failed 混合 → jsonschema 零错）、test_sidecar_merge_preserves_future_fields、test_sidecar_refuses_invalid_path（path 含 .. → sys.exit 且文件未写）
  - 真机：预检 :8188 /system_stats 200 + GPU1 compute-apps 实况留档；smoke 选镜 [1, 47]（6.73s→175f / 3.2s→124f @896×512）
  - 重跑：同命令第二次执行 → 2 行 cache-hit、零新提交（stdout + /history 条目数不增）
  </behavior>
  <action>
  1) tests/test_h3_regen.py 追加 sidecar 三个用例（fake probe_duration_sec 固定值，避免依赖真 ffprobe 速度）。
  2) 真机 smoke（**唯一真 GPU 步骤，预期 wall 10-30 分钟；建议 nohup/后台 + tee 日志，轮询日志而非阻塞等待**）：
     a. 预检：curl -s http://127.0.0.1:8188/system_stats | head -c 300 记录版本与设备；nvidia-smi --query-gpu=memory.total,memory.free --format=csv,noheader,nounits -i 1 与 --query-compute-apps=pid,used_memory --format=csv,noheader,nounits -i 1 留档（当前实况 music3 676MiB 常驻 + ComfyUI idle ~692MiB 属正常——free ~22.5GB 应过 22528 线；若 <22528 属合法 graceful-degrade，记录 warning 后停并上报，不强跑）；
     b. 首跑：python3 analysis/roundtrip/h3_regen.py --work-dir "output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。" --sample-shots 2 --regen-resolution 896x512 （**路径必须整体加引号**——含中文/空格/括号）；
     c. 断言产物：roundtrip/shot_001_regen.mp4 与 shot_047_regen.mp4 存在且 >1KB；ffprobe 两者 duration 与 175f/124f@24fps ≈7.3s/5.2s 同量级（896×512 尺寸核对）；route_cache/h3_regen/shot_001.json 含 prompt_id/seed/length=175/post_render_free_mib；roundtrip.json 过 schema（Task 1 的校验路径）；
     d. 重跑同命令：断言 stdout 含 2 条 cache-hit 行（"cache hit"）、无新渲染日志；curl -s "http://127.0.0.1:8188/history?max_entries=5" 无新 prompt_id（与首跑结束态比对）；roundtrip.json 条目数不变；
     e. 中断变体（stretch，可选）：若时间允许，删 shot_047.mp4 后重跑验证单镜补渲；不做不阻塞验收（unit test_resume 已锚定该语义）；
     f. 汇总 post_render_free_mib 两镜数值进 SUMMARY（Open Q1 渲后水位实测——供后续 22GB 阈值调参）。
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_h3_regen.py -k sidecar -x -q &amp;&amp; python3 -m pytest tests/ -q &amp;&amp; python3 -c "
import json, os
from jsonschema import Draft202012Validator
ep01 = 'output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。'
for n in ('shot_001_regen.mp4', 'shot_047_regen.mp4'):
    p = os.path.join(ep01, 'roundtrip', n)
    assert os.path.getsize(p) > 1024, p
d = json.load(open(os.path.join(ep01, 'roundtrip.json'), encoding='utf-8'))
s = json.load(open('spec/schemas/roundtrip.schema.json', encoding='utf-8'))
errs = list(Draft202012Validator(s).iter_errors(d))
assert not errs, errs[:3]
ids = sorted(x['shot_id'] for x in d['shots'])
assert 1 in ids and 47 in ids, ids
print('sidecar shots:', ids)
"</automated>
  </verify>
  <acceptance_criteria>
  - pytest 全绿（含 sidecar 三用例）；smoke 产物 2 件 mp4 >1KB（stat 断言）
  - roundtrip.json schema 零错且含 shot_id 1 与 47；cache 元数据含 length=175（shot 1）/124（shot 47）与 post_render_free_mib
  - 重跑 stdout 2 条 cache-hit、/history 无新条目（命令输出留档进 SUMMARY）
  - 全程 GPU 行为符合 guard 语义（若批被 vram_insufficient 拒绝且实况 free 确实 <22528，属合法结果——记录并上报 Kai 决策，不算失败）
  </acceptance_criteria>
  <done>SC1 真机闭环：真提交→真回收→cache-hit 零重渲；SC5 的 896×512 模式实证；渲后水位数据留档。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 目视抽检——regen mp4 vs 原镜并排</name>
  <files>output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip/_compare/shot_001_orig.mp4, output/虫虫武侠小故事《小江湖》第01话：爸爸去哪儿？（ 画面只是工具，情绪才是目的。/roundtrip/_compare/shot_047_orig.mp4</files>
  <read_first>
  - output/&lt;ep01&gt;/roundtrip/shot_001_regen.mp4 与 shot_047_regen.mp4（Task 2 产物）
  - output/&lt;ep01&gt;/shots.json（shots 1/47 的 start_sec/end_sec）
  </read_first>
  <what-built>
  896×512 fl2va 复现的 2 个 regen mp4（首/尾帧 condition 来自 h264.mp4 全分辨率重提，prompt 来自 prompts.json 该镜 prompt_text）。自动化已证明链路正确（mp4 落盘、尺寸/时长/帧数正确、cache 语义正确）；本 checkpoint 只验机器测不了的：视觉贴合度。
  </what-built>
  <how-to-verify>
  1. Claude 先自动准备并排素材（无需人动手）：ffmpeg 从 h264.mp4 切原镜段——ffmpeg -y -ss &lt;start_sec&gt; -to &lt;end_sec&gt; -i h264.mp4 -c:v libx264 -an -loglevel error roundtrip/_compare/shot_001_orig.mp4（shot 47 同理；输出加 -movflags +faststart 便于浏览器流式播放）；
  2. 打开 output/&lt;ep01&gt;/roundtrip/ 下 _compare/shot_001_orig.mp4 与 ../shot_001_regen.mp4 并排同播（shot 47 同理）；
  3. 检查三点：① 首帧/尾帧与原镜首尾帧内容一致（fl2va condition 贴合，构图无偏移糊化）② 主体与运动方向大体符合 prompt_text 描述 ③ 无明显爆裂/黑帧/时长异常；
  4. 注意 regen 是 896×512 验证模式——比清晰度，比的是内容与运动保真（1344×768 全分辨率属 overnight 全量任务，不在本 phase）。
  </how-to-verify>
  <resume-signal>Type "approved"（2 镜均贴合）或描述问题镜号与现象（如 "shot 47 首帧偏色"）</resume-signal>
  <action>Claude 先自动化准备对比素材（人只看片不剪片）：从 h264.mp4 按 shots.json 的 start_sec/end_sec 切 shots 1/47 原镜段到 roundtrip/_compare/shot_00N_orig.mp4（ffmpeg list-form、-c:v libx264 -an、-movflags +faststart），随后暂停等待 Kai 目视裁决——automation 完成后才 checkpoint。</action>
  <verify>
    <human-check>roundtrip/_compare/ 下 2 个原镜段与 2 个 regen mp4 并排同播：① 首尾帧内容与原镜一致 ② 运动大体符合该镜 prompt_text ③ 无爆裂/黑帧/时长异常（896×512 模式比内容不比清晰度）</human-check>
  </verify>
  <done>Kai 回复 "approved"，或问题镜号已记录进 SUMMARY 的 deferred/learnings 小节。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache 元数据/渲染结果 → roundtrip.json | 自家产物自写 work_dir；schema pattern 拒路径穿越（写前校验兜底） |
| 真机 smoke → 生产 GPU（GPU1 3090） | 与 TTS/eye/music3 共卡；guard（20-02）在真实路径上执行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | Tampering | roundtrip.json regen.path | mitigate | path 由客户端自构造（roundtrip/shot_{NNN}_regen.mp4），写前 Draft202012Validator 全量校验（schema pattern 拒 .. 与绝对路径）——校验失败 sys.exit 不落盘 |
| T-20-09 | Information Disclosure | status.error 文本 | mitigate | 截断 2000 字符；内容限异常类名/状态（schema T-18-02 上界沿用） |
| T-20-10 | Denial of Service | smoke 占用生产 GPU | mitigate | 限 --sample-shots 2 + 896×512；guard 批开始 gate 生效；smoke 明确禁止扩展为全量（ep01 全量是 Phase 21 前置 overnight 任务，NOT in scope） |
| T-20-SC | Tampering | 包安装 | accept | 零新包（jsonschema 已是 spec/ 既有依赖） |
</threat_model>

<verification>
- python3 -m pytest tests/ -q 全绿（含 sidecar 用例）
- ep01 roundtrip/ 2 件 mp4 >1KB、ffprobe 尺寸 896×512、时长与 175f/124f@24fps 同量级
- roundtrip.json 过 roundtrip.schema.json 且 shots 含 {1, 47}
- 重跑 cache-hit 零提交证据（stdout + /history 对比）进 SUMMARY；post_render_free_mib 数值进 SUMMARY
- Task 3 目视抽检 approved
</verification>

<success_criteria>
- SC1 真机证明：真 ComfyUI 提交/回收 + 断点续跑 cache-hit（不经 subagent/KAP/websocket）
- SC4 真数据可观察：--force 清单与 4-tuple 失效在 ep01 上行为正确（sidecar + cache 文件布局符合 CONTEXT 约定）
- SC5：896×512 降分辨率模式真机可用
- Phase 18 契约运行时兑现：roundtrip.json regen 半边 schema 合法（RT-04 warnings 通道已在 20-01/02 兑现）
</success_criteria>

<output>
Create `.planning/phases/20-h3-regen-client/20-03-SUMMARY.md` when done（含 post_render_free_mib 水位与 smoke 时间线）
</output>
