---
phase: 20-h3-regen-client
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - analysis/roundtrip/h3_regen.py
  - tests/test_h3_regen.py
autonomous: true
requirements: [REGEN-03, REGEN-04]
must_haves:
  truths:
    - "批开始 guard：TTS（:5110/:5111 监听进程）端口→PID 归因 kill + 前后各一条审计 warning；kill 后与批开始前各一次 POST /free；此后 free<22528MiB 拒绝整批（vram_insufficient warning + exit 0）"
    - "每镜提交前复查走 PID 归因：nvidia-smi --query-compute-apps 排除基线（含 ComfyUI 自身）后 foreign 占用达阈值才等待/终止——渲后自身 cache 驻留不会自锁（Pitfall 1）"
    - "eye 串行：批开始绝对值 used≥13721MiB 视为 eye lease 在跑 → 轮询等待（--vram-wait-timeout 可配）而非提交；批中 eye 以新 foreign PID 识别"
    - "--sample-shots N 均匀抽样在全镜列表上做（先抽样后 >10s 过滤）；--max-shot-sec 默认 10，超限镜跳过 + str warning + skipped.json 可查"
    - "--regen-resolution 896x512 可用（%32 + 严格 7:4 + MAX_PIXELS 校验），分辨率冻进 engine_version → 切换分辨率旧 cache 整体失效"
  artifacts:
    - path: "analysis/roundtrip/h3_regen.py"
      provides: "VRAM guard + TTS kill + eye 串行 + 抽样/降载 CLI（叠加在 20-01 模块上）"
      contains: "query-compute-apps"
    - path: "tests/test_h3_regen.py"
      provides: "guard/抽样离线单测（假 nvidia-smi / 假 ss / 假 os.kill）"
      min_lines: 400
  key_links:
    - from: "analysis/roundtrip/h3_regen.py"
      to: "nvidia-smi（GPU index 可配，默认 1）"
      via: "subprocess list-form 查询式读数（free/total + compute-apps）"
      pattern: "query-compute-apps"
    - from: "analysis/roundtrip/h3_regen.py"
      to: "ss -tlnp / os.kill(SIGTERM)"
      via: "TTS 端口→PID 归因定向 kill（非宽 pkill）"
      pattern: "os\\.kill"
    - from: "analysis/roundtrip/h3_regen.py"
      to: "ComfyUI POST /free"
      via: "_http_json POST {unload_models:true, free_memory:true}"
      pattern: "/free"
    - from: "analysis/roundtrip/h3_regen.py"
      to: "route_cache/h3_regen/skipped.json"
      via: ">10s 跳镜清单（shot_id + reason + duration）"
      pattern: "skipped\\.json"
---

<objective>
给 20-01 的客户端叠加 GPU 编排与降载：VRAM guard（TTS 端口→PID kill + /free 时机 + 批开始严格 22GB gate + 每镜 PID 归因复查）、qwen-eye 串行编排、--sample-shots / --max-shot-sec / --regen-resolution CLI——全部离线可测（假 nvidia-smi / 假 ss / 假 os.kill）。

Purpose: REGEN-03（不撞 OOM / 黑屏——同卡 3090 上 TTS、eye、music3 共存）与 REGEN-04（首轮 ≤20 镜校准的抽样与降载）；每镜复查采用 Open Q1 裁决的 PID 归因口径。
Output: h3_regen.py 扩展（guard 函数群 + CLI flag + main 编排序列）+ tests/test_h3_regen.py 扩展。
</objective>

<execution_context>
@/home/kai/.claude/get-shit-done/workflows/execute-plan.md
@/home/kai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-h3-regen-client/20-CONTEXT.md
@.planning/phases/20-h3-regen-client/20-RESEARCH.md
@.planning/phases/20-h3-regen-client/20-PATTERNS.md
@analysis/engine_clients/qwen_eye_client.py
</context>

<interfaces>
<!-- 20-01 交付物中本 plan 要消费/扩展的接点（已存在） -->

h3_regen.py 既有：_http_json(url, payload, timeout) → (status, body)（POST /free 走它）；
main() 中「ComfyUI 可达性 gate（system_stats）」位置之前即批开始 guard 序列的插入点；
cache_write(meta) —— 本 plan 给 meta 增可选字段 post_render_free_mib（渲后水位，Open Q1 数据留档）；
build_engine_version(width, height) —— --regen-resolution 接入后由 main 传入实际 w/h，cache key 随之失效。

实测锚点（RESEARCH）：GPU1 3090 total 24576MiB；kill 后 free ~22539（阈值 22528 只剩 11MiB 余量——warning detail 必须写明 top 占用者）；eye lease 13721MiB；music3 常驻 676MiB 属正常态。
ep01 锚点：93 镜；uniform-20 = [1,5,10,14,19,24,28,33,38,42,47,52,56,61,66,70,75,80,84,89]（int(i*N/n) 0-based +1 公式已实算复现）；shot 70=19.73s 是唯一落样超 10s 镜 → 实渲 19。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: VRAM guard + TTS 端口→PID kill + eye 串行 + 每镜 PID 归因复查</name>
  <files>analysis/roundtrip/h3_regen.py</files>
  <read_first>
  - analysis/engine_clients/qwen_eye_client.py L140-153（_vram_free_mib 查询式 + fail-open）
  - .planning/phases/20-h3-regen-client/20-RESEARCH.md §Common Pitfalls Pitfall 1（批中绝对值复查自锁——PID 归因方案）、Pitfall 9（阈值余量 + detail 写 top 占用者）、§Code Examples「VRAM guard」
  - analysis/roundtrip/h3_regen.py 本 plan 前置版本（guard 插入点：main 的 system_stats gate 之前）
  </read_first>
  <behavior>
  - 批开始序列（顺序固定）：①find_tts_listeners → 有则 kill + 前后两条审计 warning（code=vram_insufficient，detail 含 pids/端口/进程名——三码 closed enum 约束下的事件记因）②POST /free ③eye 等待循环（used=total-free ≥13721 → 15s 轮询至 --vram-wait-timeout）④再 POST /free（「kill 后 + 批开始前各一次」——CONTEXT 字面）⑤严格 gate：free<22528 → vram_insufficient warning + return 0
  - 基线快照 = guard 完成后 compute-apps 全部 PID ∪ docker inspect comfyui-primary 主 PID（best-effort，失败忽略）；每镜提交前：foreign = 当前 compute-apps 中不在基线的 PID，Σused ≥4096MiB → 等待循环至超时 → warning + 优雅终止（exit 0，cache 保留续跑语义）；基线内 PID（ComfyUI 自身 cache）永不触发
  - find_tts_listeners：ss -tlnp 解析 :5110/:5111 → (pid, port, name)；name 取 /proc/{pid}/cmdline 前 120 字符做审计；ss 不可用/解析失败 → 回退 pkill -f 精确脚本名 voicedesign_server.py / indextts25-server.py（A5 兜底）；无监听 → 不 kill、仍记 after 审计 warning、继续（no-op 安全）
  - kill 用 os.kill(pid, signal.SIGTERM) 定向——**绝不宽 pattern pkill**（security 约束）
  - 每镜渲后 cache_write 增 post_render_free_mib=free_mib()（Open Q1 数据留档）
  </behavior>
  <action>
  h3_regen.py 追加常量：VRAM_GPU_INDEX_DEFAULT=1、BATCH_MIN_FREE_MIB=22528、EYE_LEASE_MIB=13721、FOREIGN_BLOCK_MIB=4096、GUARD_POLL_SEC=15、TTS_PORTS=(5110, 5111)、TTS_FALLBACK_PATTERNS=("voicedesign_server.py", "indextts25-server.py")、COMFY_CONTAINER="comfyui-primary"。
  追加函数（全部 fail-open：nvidia-smi/ss 异常返回 None/[] 不阻塞——qwen 先例）：
  - gpu_mem_mib(gpu_index) → (total, free) | None：一次查询 --query-gpu=memory.total,memory.free --format=csv,noheader,nounits -i {idx}；
  - compute_apps(gpu_index) → list[(pid, used_mib)]：--query-compute-apps=pid,used_memory 同款（"[Not Found]" 行跳过）；
  - find_tts_listeners() → list[(pid, port, name)]：subprocess.run(["ss","-tlnp"], capture_output=True, text=True, timeout=10)，行内含 f":{port} " 匹配 TTS_PORTS，pid= 正则提取；name 读 /proc/{pid}/cmdline（\0 → 空格，截 120 字符）；异常/无 ss → 返回 None（哨兵区分「无监听」与「探测失败」）；
  - kill_tts(listeners) → list[dict]：对每个 (pid,port,name) os.kill(pid, signal.SIGTERM)（ProcessLookupError 容忍），返回审计记录；listeners is None（探测失败）→ 回退 subprocess.run(["pkill","-f",pat]) 两个精确脚本名，returncode==0 记 killed；listeners == []（确认无监听）→ 空列表；
  - comfy_free(comfy_url)：_http_json POST {comfy}/free body {"unload_models": True, "free_memory": True}（KAP gpuVramManager 同款 payload，实测 200）；
  - proc_name(pid) → str：/proc/{pid}/cmdline 读名（warning detail 用）；
  - batch_start_guard(comfy_url, gpu_index, wait_timeout) → dict{blocked: bool, reason: str|None, killed: list}：按 Behavior 顺序执行①-⑤；eye 等待与超时均打 [roundtrip] 日志；gate 拒绝的 warning detail 必含当前 free 值 + compute-apps top 占用者（pid+name+MiB——Pitfall 9）；
  - baseline_pid_snapshot(gpu_index) → set[int]：compute_apps PID 集 ∪ docker inspect --format {{.State.Pid}} comfyui-primary 的主 PID（subprocess 失败静默忽略）；
  - per_shot_vram_ok(gpu_index, baseline, wait_timeout) → bool：compute_apps 差集 foreign；Σforeign_used < FOREIGN_BLOCK_MIB → True；否则 15s 等待循环（log 一次 top foreign）至超时 → False（main 收到 False → vram_insufficient warning（detail 含 foreign pid/name/MiB 与 free）+ 优雅终止批）。
  main() 接线：system_stats 可达 gate 之后、批循环之前插 batch_start_guard——blocked → append warning dict + return 0；通过 → baseline = baseline_pid_snapshot(...)；批循环内**每次 submit_prompt 之前**调 per_shot_vram_ok，False 则 warning + 收尾 flush + return 0；每镜 cache_write 成功后补 post_render_free_mib 字段（cache_write 调用点增参）。
  argparse 增：--gpu-index（int, default 1）、--vram-wait-timeout（int 秒, default 1800）。
  docstring 补 guard 语义段（含 music3 676MiB 常驻属正常态、22.5GB 仍过线的说明）。
  </action>
  <verify>
    <automated>python3 -m py_compile analysis/roundtrip/h3_regen.py &amp;&amp; python3 -c "
import importlib.util
spec = importlib.util.spec_from_file_location('h3_regen', 'analysis/roundtrip/h3_regen.py')
h3m = importlib.util.module_from_spec(spec); spec.loader.exec_module(h3m)
src = open('analysis/roundtrip/h3_regen.py', encoding='utf-8').read()
assert 'query-compute-apps' in src and 'os.kill' in src and 'SIGTERM' in src
assert 'pkill' in src  # 回退路径存在
assert '13721' in src and '22528' in src and '4096' in src
assert src.count('unload_models') >= 1
print('20-02 T1 OK')
"</automated>
  </verify>
  <acceptance_criteria>
  - guard 序列五步顺序固定（kill → /free → eye wait → /free → strict gate），/free 恰在 kill 后与批开始前各一次
  - kill 定向 os.kill(SIGTERM)；pkill 仅作 ss 探测失败的回退且 pattern 限于两个精确脚本名
  - 每镜复查基于 compute-apps 差集（基线 ∪ 容器主 PID），不含绝对值 free 下限（Pitfall 1 自锁不可能）
  - cache 元数据含 post_render_free_mib 字段写入路径
  </acceptance_criteria>
  <done>REGEN-03 guard 与串行编排全部函数 + main 接线完成（离线断言在 Task 3）。</done>
</task>

<task type="auto">
  <name>Task 2: --sample-shots 均匀抽样 + --max-shot-sec 跳镜 + --regen-resolution 降载</name>
  <files>analysis/roundtrip/h3_regen.py</files>
  <read_first>
  - .planning/phases/20-h3-regen-client/20-RESEARCH.md §ep01 数据盘点（uniform-20 锚点清单 + 「抽样在 >10s 过滤之前做」设计注意）与 §Code Examples 注入表（node 20 width/height 行）
  - analysis/roundtrip/h3_regen.py 本 plan 前置版本（load_shot_prompts / main 批循环 / build_engine_version）
  </read_first>
  <behavior>
  - uniform_sample(93 镜 ids, 20) == [1,5,10,14,19,24,28,33,38,42,47,52,56,61,66,70,75,80,84,89]（公式 int(i*N/n) 0-based +1，去重保序）；n≥N → 全镜
  - 先抽样后过滤：shot 70（19.7s）落样后被 --max-shot-sec 10 跳过 → 实渲 19（A6 语义：抽样代表性不被过滤顺序扭曲）
  - 跳镜产 str warning "[roundtrip] shot 70 skipped: 19.7s > max 10.0s" + skipped.json 条目 {shot_id, reason, duration_sec}
  - --regen-resolution "896x512" 通过校验（%32 / 7:4 / ≤MAX_PIXELS）；"100x100"（比例破）与 "897x512"（非 32 倍）报错退出；engine_version 随分辨率变化 → cache 失效
  </behavior>
  <action>
  h3_regen.py 追加：
  - uniform_sample(shot_ids, n) → list[int]：ids 升序；pos = sorted({int(i * len(ids) / n) for i in range(n)})；返回 [ids[p] for p in pos]（n>=N 时 int 覆盖全部去重即全镜）；确定性、无随机；
  - skipped 清单 writer：route_cache/h3_regen/skipped.json = list[{shot_id, reason, duration_sec}]，READ-merge（重跑替换同 shot_id 条目）+ 原子写；
  - --max-shot-sec 过滤：duration > max_shot_sec → 跳镜（str warning 进本轮 warnings 收集 + skipped.json 条目 + print [roundtrip] 行 + 计数进 summary）；**过滤发生在抽样之后**；
  - --regen-resolution（str, default "1344x768"）：parse_resolution + validate_resolution（20-01 已建）失败 sys.exit 中文报错；通过后 main 以 (w,h) 驱动 build_workflow 与 build_engine_version(w,h)——cache key 随 engine_version 变化自动整体失效（切 896×512 = 换渲染配置全集重渲，语义即 Q3 裁决）；
  - --sample-shots（int, default 0=不抽样全量）。
  main() 选择管线改为：load_shot_prompts →（--sample-shots>0 时 uniform_sample）→（--max-shot-sec 过滤 + skipped 记录）→ 得 selected 列表 → 批循环只跑 selected；summary print 增 sampled/skipped 计数。
  </action>
  <verify>
    <automated>python3 -c "
import importlib.util
spec = importlib.util.spec_from_file_location('h3_regen', 'analysis/roundtrip/h3_regen.py')
h3m = importlib.util.module_from_spec(spec); spec.loader.exec_module(h3m)
ids = list(range(1, 94))
exp = [1,5,10,14,19,24,28,33,38,42,47,52,56,61,66,70,75,80,84,89]
assert h3m.uniform_sample(ids, 20) == exp, h3m.uniform_sample(ids, 20)
assert h3m.uniform_sample(ids, 200) == ids
assert h3m.uniform_sample(ids, 2) == [1, 47]
assert h3m.build_engine_version(896, 512) != h3m.build_engine_version(1344, 768)
try:
    h3m.validate_resolution(100, 100); raise SystemExit('100x100 should fail')
except (SystemExit, ValueError): pass
try:
    h3m.validate_resolution(897, 512); raise SystemExit('897x512 should fail')
except (SystemExit, ValueError): pass
print('20-02 T2 OK')
"</automated>
  </verify>
  <acceptance_criteria>
  - uniform_sample 对 ep01 93 镜 N=20 输出与 RESEARCH 实算清单逐项相等
  - 896×512 / 1344×768 合法，100×100 与 897×512 被拒
  - main 选择管线顺序 = 抽样 → 时长过滤；skipped.json 落 route_cache/h3_regen/ 下
  </acceptance_criteria>
  <done>REGEN-04 CLI 三件套接入 main，抽样代表性与 cache 失效语义符合 CONTEXT 锁定。</done>
</task>

<task type="auto">
  <name>Task 3: guard / 抽样离线单测（假 nvidia-smi + 假 ss + 假 os.kill）</name>
  <files>tests/test_h3_regen.py</files>
  <read_first>
  - tests/test_h3_regen.py 本 plan 前置版本（fake_subprocess 分发器——本 task 扩展 ss/nvidia-smi 分支）
  - tests/test_qwen_eye_client.py L41-52（no_engine fixture + _FakeProc 形状）
  </read_first>
  <behavior>
  - test_batch_gate_refuses：假 nvidia-smi free=21000 → rc==0、零提交、warnings 含 {code:vram_insufficient}、detail 含 "22528" 与 top 占用者
  - test_batch_gate_passes：total=24576/free=23000 → 提交发生
  - test_per_shot_own_cache_not_blocking：基线含 ComfyUI pid=100；镜 2 时 pid=100 used=18432 → 不等待直接提交（Pitfall 1 反自锁证明）
  - test_per_shot_foreign_blocks：新 pid=999 used=13721 → 等待循环（sleep 已 patch）至 --vram-wait-timeout=0 → warning + rc==0 终止
  - test_kill_tts_port_pid：假 ss 输出含 :5110 pid=111 / :5111 pid=222 → monkeypatch h3m.os.kill 记录 → SIGTERM 恰发往 {111,222}；两条审计 warning（before/after）
  - test_kill_tts_no_listeners：假 ss 无监听 → 无 kill 调用、仍记 after 审计 warning、批继续（TTS 当前未运行的 no-op 安全证明）
  - test_free_called_twice：FakeHTTP calls 中 POST /free 恰 2 次（kill 后 + 批开始前）
  - test_uniform_sample_ep01 / test_sample_before_filter：93 镜 uniform-20 == 锚点清单；shot 70 落样但被 max-shot-sec 跳过 → 实渲 19 + skipped.json 有 shot_id=70
  - test_resolution_flag_invalidates_cache：1344×768 跑 1 镜 → 换 --regen-resolution 896x512 重跑 → 同镜重渲（FakeHTTP 再次出现 /prompt）
  </behavior>
  <action>
  扩展 tests/test_h3_regen.py：
  - fake_subprocess 分发器增分支：cmd 含 "nvidia-smi" → 按 --query-gpu/--query-compute-apps 参数回放假 stdout（free/total 或 "pid, used" 行）；cmd[0]=="ss" → 返回预置监听行字符串；"docker" → 非零 returncode（走 best-effort 忽略路径）；
  - monkeypatch h3m.os.kill（记录 (pid, sig) 列表）用于 kill 断言；monkeypatch h3m.time.sleep 已有；
  - guard 用例走 run_main（--gpu-index 1 显式传参避免环境耦合）+ FakeHTTP + capsys 断言 [roundtrip] 输出与 warnings.json 内容；
  - 内联假数据常量：SS_WITH_TTS / SS_EMPTY / NM_FREE_21000 / NM_FREE_23000 / NM_COMPUTE_APPS_* 等字符串样本。
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_h3_regen.py -k "vram or kill or free or sample or resolution or skip" -x -q &amp;&amp; python3 -m pytest tests/test_h3_regen.py tests/ -q</automated>
  </verify>
  <acceptance_criteria>
  - 新增 ≥9 个 guard/抽样用例全绿；全套件 tests/ 仍绿；总耗时 <20s（sleep 已 patch）
  - test_per_shot_own_cache_not_blocking 是 Pitfall 1 的回归锚（自身 18GB 驻留不触发等待）
  - test_free_called_twice 锁定 /free 时机（CONTEXT 字面语义的机器证明）
  </acceptance_criteria>
  <done>REGEN-03/04 离线证据闭合：guard 五步序列、PID 归因、kill 定向、抽样公式、降载失效全有断言。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 → 宿主 GPU/进程子系统 | nvidia-smi / ss / docker inspect / os.kill 只读+定向信号；错误输出按不可信文本解析（fail-open） |
| 客户端 → ComfyUI POST /free | 环回管理端点；payload 固定两布尔 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Elevation of Privilege / Tampering | TTS kill 目标选择 | mitigate | 端口→PID 归因（ss -tlnp :5110/:5111）+ os.kill(SIGTERM) 定向；pkill 回退仅限两个精确脚本名（ voicedesign_server.py / indextts25-server.py ）；kill 前后双 warning 审计（pid+端口+cmdline 名）——绝不宽 pattern |
| T-20-06 | Denial of Service | GPU OOM 级联（黑屏先例） | mitigate | 批开始严格 22528MiB gate + 每镜 foreign PID ≥4096MiB 等待/终止 + eye 13721MiB 串行等待；fail-open 仅在读数不可得时（qwen 先例），gate 判定本身保守 |
| T-20-07 | Tampering | nvidia-smi/ss 输出解析 | mitigate | 解析失败返回 None/[] 并降级（不崩溃不误杀）；PID 数字校验 cols[0].isdigit() |
| T-20-SC | Tampering | 包安装 | accept | 零新包（ss/nvidia-smi/docker 为系统自带；仅读取） |
</threat_model>

<verification>
- python3 -m pytest tests/test_h3_regen.py -x -q 全绿；python3 -m pytest tests/ -q 全绿
- uniform_sample 对 ep01 锚点清单逐项相等；896×512 与 1344×768 合法、畸形分辨率被拒（Task 2 的 python -c）
- grep：query-compute-apps / os.kill / SIGTERM / /free / skipped.json 均在 h3_regen.py
</verification>

<success_criteria>
- REGEN-03：批开始 kill TTS + 双 /free + 22GB 拒提交 + warning；每镜 PID 归因复查；eye 串行等待——全部有离线单测锚定
- REGEN-04：--sample-shots 均匀确定性（ep01 锚点复现）、--max-shot-sec=10 跳过 + skipped.json、--regen-resolution 896x512 校验 + engine_version 联动 cache 失效
- SC2/SC3/SC5 的代码与离线证据半边完成（真机 live 行为由 20-03 smoke 观测）
</success_criteria>

<output>
Create `.planning/phases/20-h3-regen-client/20-02-SUMMARY.md` when done
</output>
