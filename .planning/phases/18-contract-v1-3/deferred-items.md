# Phase 18 Deferred Items

Out-of-scope discoveries logged during execution (not fixed by executors — surface to orchestrator/verifier).

| # | Found | Item | Impact |
|---|-------|------|--------|
| 1 | 18-01 execution (2026-08-19) | Working tree carries uncommitted deletion of 5 v1.2-era research files (`.planning/research/{ARCHITECTURE,FEATURES,PITFALLS,STACK,audio-spike-report}.md`) — content already archived under `.planning/milestones/v1.2-research/`; deletion itself never committed by the milestone transition. | Cosmetic/git-hygiene only. Left untouched by 18-01 (scope boundary). Whichever agent commits next should either commit the deletions or restore the files; do not lose track of them. RESOLVED by orchestrator before 18-02 (commit 8cf3454). |
| 2 | 18-02 execution (2026-08-19) | **Pre-existing producer-mode failure on default ep01 dir**: `python3 scripts/verify_contract.py --mode=producer` (default `PHASE4_ASSET_DIR`) FAILs with `registry at /<root>: Additional properties are not allowed ('method', 'total_clusters', 'total_crops' were unexpected)`. Root cause: ep01 `output/…第01话…/registry.draft.json` (untracked runtime output, mtime 2026-07-30, written by a post-v1.2 `local_reid_glm46v_hist` experiment) uses root keys `method/total_clusters/total_crops` where `spec/schemas/registry.schema.json` (unchanged since Phase 5) allows only `clusters/generated_at/model/tau` — note it also says `method` where the schema expects `model`. Verified pre-existing: the pre-18-02 HEAD version of verify_contract.py fails identically; ep02/ep03 registry drafts are schema-clean. | Blocks exit-0 of producer mode ONLY on the default ep01 dir; all Phase 18 checks (cross-version, fixture consistency, EIGHT_SHAPES) are green and 18-02 verification routed through the documented `PHASE4_ASSET_DIR` override (ep02). Not fixed (scope boundary: untracked runtime data + a schema-vs-experiment drift that deserves its own decision — widen registry.schema.json vs regenerate a compliant draft). Surface to orchestrator: either regenerate ep01 registry draft or schedule a schema-widening decision. |
